---
title: "Inverse Optimal Control for Linear Quadratic Problem with Poisson Jumps: Model-Free Inverse Reinforcement Learning Approaches"
title_zh: 带泊松跳跃的线性二次型问题的逆最优控制：无模型逆强化学习方法
authors: "Wen Du, Na Li, Xun Li, Zuo Quan Xu"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03169v1"
tags: ["query:control"]
score: 9.0
evidence: 带泊松跳的线性二次问题的逆最优控制
tldr: 针对受布朗运动和泊松跳跃影响的随机线性系统，本文提出两种无模型离策略逆强化学习算法来求解逆最优控制问题。通过专家演示数据，逆Q学习算法构造数据驱动方程计算Q函数并代数更新代价权重，逆策略迭代算法利用初始稳定策略数据。两种方法均无需系统动力学或跳跃强度，从充分激励轨迹中学习等价代价权重，数值实验验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03169v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 535, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03169v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1698, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03169v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1706, \"height\": 562, \"label\": \"Figure\"}]"
motivation: 在系统动力学未知时，需要从专家反馈增益中恢复代价函数，解决带跳跃的随机线性系统逆最优控制问题。
method: 提出逆Q学习和离策略逆策略迭代两种无模型算法，利用专家轨迹数据构造方程，解耦行为策略与更新策略，避免求解广义Riccati方程。
result: 算法能准确学习等价代价权重，无需系统矩阵或跳跃强度，数值模拟验证了方法的有效性。
conclusion: 本文为带跳跃的随机线性系统逆最优控制提供了数据驱动的无模型解决方案。
---

## 摘要
本文利用逆强化学习框架，研究了同时受布朗运动和泊松跳跃驱动的随机线性系统的逆最优控制问题。给定专家提供的目标反馈增益，目标是识别出能产生该增益的等价代价泛函——具体而言，即所有代价权重的集合。为了在系统动力学未知时解决该问题，我们提出了两种完全基于数据的无模型离策略逆强化学习算法，从而无需求解广义代数Riccati方程或解析计算代价权重。第一种是逆Q学习算法，它从专家演示中构建数据驱动方程来计算Q函数矩阵，并通过代数方式更新等价代价权重，无需额外的轨迹数据。第二种是无模型离策略逆策略迭代算法，它利用在初始稳定策略下收集的数据，为不同的数据可用性场景提供了一种互补方法。关键在于，通过将数据收集的行为策略与迭代更新的策略解耦，这两种算法都能从充分激励的轨迹中学习等价代价权重，而无需辨识系统动力学或跳跃强度。数值仿真验证了所提方法的有效性。

## Abstract
This paper addresses the inverse optimal control (IOC) problem for stochastic linear systems subject to both Brownian motion and Poisson jumps, using an inverse reinforcement learning (IRL) framework. Given a target feedback gain from an expert, the objective is to identify an equivalent cost functional-specifically, the set of all cost weights-that yields this same gain. To solve this problem when system dynamics are unknown, we propose two model-free, off-policy IRL algorithms that operate entirely from data, circumventing the need to solve the generalized algebraic Riccati equation or compute the cost weights analytically. The first is an inverse Q-learning algorithm that constructs data-driven equations from expert demonstrations to compute the Q-function matrix, with equivalent cost weights updated algebraically and without requiring additional trajectory data. The second is a model-free off-policy inverse policy iteration algorithm that leverages data collected under an initial stabilizing policy, offering a complementary approach suited to different data availability scenarios. Crucially, by decoupling the data-collection behavior policies from the policies being iteratively updated, both algorithms can learn equivalent cost weights from sufficiently excited trajectories without identifying the system dynamics or jump intensity. Numerical simulations validate the effectiveness of the proposed methods.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将根据您提供的论文文本，生成一份结构化、深入、客观的中文总结。

### 论文信息
- **标题**: 带泊松跳跃的线性二次型问题的逆最优控制：无模型逆强化学习方法
- **作者**: Wen Du, Na Li, Xun Li, Zuo Quan Xu
- **年份**: 2026
- **来源**: arXiv预印本

### 核心问题与整体含义（研究动机和背景）

- **核心问题**: 本文研究了**逆最优控制（IOC）** 问题。具体来说，**给定一个专家控制的随机线性系统（专家提供最优反馈增益** ***Kt*** **）** ，目标是在**系统动力学模型未知**的前提下，找到一个代价函数（成本权重矩阵），使得该专家增益成为这个新代价函数下的最优控制。
- **系统模型**: 系统是受**布朗运动（连续波动）和泊松跳跃（随机、不连续的冲击）** 共同驱动的**随机线性系统**，这类模型能更好地模拟真实世界中的突发事件（如政策冲击、金融违约等）。
- **研究动机**: 传统IOC方法通常需要已知的系统动力学参数。但在许多实际问题中，系统模型复杂、成本高昂或难以精确建模，而专家的演示数据（如状态轨迹）相对容易获取。因此，开发一种**不依赖模型、纯粹基于数据**的IOC方法具有重要的理论意义和实用价值。
- **整体含义**: 论文旨在解决“逆向”问题——从观察行为（专家策略）逆向推断其背后的“意图”（代价函数），这在机器人模仿学习、人机交互、自动驾驶等领域有广泛应用。

### 方法论：核心思想、关键技术细节

- **核心思想**: 引入**逆强化学习（IRL）** 框架，将该问题建模为：学习者（Learner）通过与专家（Expert）共享相同的系统动力学，寻找一个**等价代价函数**，使得该函数的最优反馈增益等于专家的目标增益。
- **关键概念**:
    - **等价代价函数**: 不同权重矩阵可能对应同一个最优反馈增益，其解不唯一。本文方法旨在找到其中一个解。
    - **Q函数**: 将传统的最优控制问题（求解HJB/Riccati方程）转化为基于Q函数的贝尔曼方程，从而避免直接处理系统参数。
- **关键技术**:
    1.  **离策略（Off-Policy）学习**：用于收集数据的“行为策略”与被优化的“目标策略”相解耦。这允许算法从任何历史数据或非最优策略收集的数据中学习，极大增加了算法的灵活性和数据效率。
    2.  **持久激励（Persistent Excitation, PE）**：通过在控制输入中添加探测噪声（Probing Noise），确保收集到的数据矩阵**满秩（Full Rank）**，使得待求解的方程（如Q函数矩阵）有唯一解。
    3.  **向量化和Kronecker积**：利用此数学技巧，将矩阵形式的方程转化为标准的**最小二乘（Least Squares）** 形式，便于从批量数据中求解。
- **提出的两种算法**:
    1.  **逆Q学习算法**:
        - **数据源**: 专家的演示轨迹 *T *( ***Xt***, ***ut***) 和已知的专家增益 *Kt*。
        - **流程**:
            - **Q函数评估**: 利用专家轨迹，通过最小二乘法求解一个数据驱动的方程（公式20），得到Q函数矩阵 *Q(i+1)*。
            - **策略改进**: 从 *Q(i+1)* 中提取候选增益 *K(i+1)*。
            - **权重更新**: 使用一个代数公式（公式22），根据 *K(i+1)* 和 *KT* 的差异以及 *Q(i+1)* 直接更新代价权重 *N(i+1)*。
    2.  **无模型离策略逆策略迭代算法**:
        - **数据源**: 学习者自身收集的轨迹 *XL*( *t*), *uL*( *t*)。它需要一个初始的稳定控制器（行为策略）来开始收集数据。
        - **流程**:
            - **策略评估**: 利用收集到的轨迹数据，通过最小二乘法求解一组线性方程（公式44），同时解出价值函数 *P(i+1)* 和两个中间变量 *B(i+1)*、*D(i+1)*。
            - **策略改进**: 基于 *B(i+1)* 和 *D(i+1)* 更新增益 *K(i+1)*。
            - **权重更新**: 利用新的 *P(i+1)* 和 *K(i+1)* 通过另一个数据驱动方程（公式46）更新代价权重 *N(i+1)*。
- **核心优势**: 两种算法的迭代公式都**不包含任何系统参数（A, B, C, D, E, F）或跳跃强度（λ）**，实现了真正的“无模型”（Model-Free）。

### 实验设计

- **使用场景**: 采用一个**2维状态、1维控制输入**的线性系统进行数值仿真。系统参数包含布朗运动和泊松跳跃分量。
- **Benchmark**: 论文没有设立与其它IOC方法的直接对比基准。它**以解析解（已知系统参数下的理论最优增益** ***KT*** **）作为黄金标准**，验证算法能否学到与其一致的增益。
- **对比方法**: 论文提出的**两种算法（逆Q学习 & 离策略逆策略迭代）** 相互之间进行了对比，并各自验证了与理论最优解的接近程度。
- **场景设置**:
    - 专家代价权重 *NT*, *RT* 是已知的，用于计算出理论目标增益 *KT* 和理论最优解 *PT*, *N*。
    - 给定一个错误的初始权重 *N(0)* 和学习者假设的代价权重 *R*。
    - 算法1使用专家演示轨迹。
    - 算法2使用学习者从初始稳定控制器（ *KL*）产生的轨迹。
- **评价指标**:
    - **收敛性**: 算法迭代过程中，代价权重 *N(i)*、增益 *K(i)*、Q函数 *Q(i)*（或 *P(i)*）的变化。
    - **策略准确性**: 学习到的最终增益 *K*** 与理论最优增益 *KT* 的**误差**。
    - **控制效果**: 使用学习到的增益运行的闭环系统，其状态轨迹和专家轨迹的拟合程度。

### 资源与算力

- **本文未明确提及使用的计算资源（如GPU型号、数量、训练时长等）**。实验是基于一个简单的2维线性系统进行的，计算量很小，属于算法验证，因此作者可能认为在普通CPU上运行即可，无需特别说明。

### 实验数量与充分性

- **实验数量**: **两组基础实验**，分别对应提出的两个算法。每组实验均展示了收敛曲线、误差对比以及轨迹对比。
- **充分性分析**:
    - **不够充分**：实验规模较小，仅在一个简单的2维系统上验证。为了证明方法的普适性，建议增加：
        - **更高维系统**的测试。
        - **不同跳跃强度**和**噪声水平**下的鲁棒性测试。
        - **收敛速度**对不同初始 *N(0)* 的敏感性分析。
    - **客观性与公平性**: 实验设计基本公平，以“能否学到与理论一致的增益”作为唯一标准，这是客观的。但由于缺乏与其他现有IOC/IRL方法（如基于模型的方法）的横向对比，论文的方法优势（如模型无关、计算效率等）仅停留在理论分析层面，缺乏实验证据支持。

### 主要结论与发现

- **有效性验证**: 两种算法均能成功收敛，学习到的最终反馈增益 ***K***** 与目标增益** ***KT*** **非常接近**（误差约为1.5e-2），尽管学习到的代价权重 ***N***** 不等于目标权重** ***NT***，验证了等价代价函数的存在性。
- **稳定性保证**: 论文从理论上证明了，在满足初始条件的前提下，算法在每一步迭代产生的增益 *K(i)* 都能**保证闭环系统的L2-稳定性**。
- **模型无关性**: 实验结果表明，算法纯粹依赖数据，能够在不使用任何系统模型信息的情况下，找到与专家行为匹配的代价函数，实现了预期目标。
- **泛化能力**: 两种算法基于不同的数据源（专家轨迹 vs. 学习者轨迹），为不同场景下的IOC问题提供了灵活的解决方案。

### 优点

- **理论创新性强**: 首次为“带泊松跳跃”的随机系统提出**完全无模型**的逆最优控制算法，这是该领域的重大进展，解决了此前对已知动力学参数依赖的痛点。
- **技术细节扎实**: 算法设计（离策略、Q函数、满秩条件）和理论证明（稳定性、收敛性）非常完整和严谨，具有很高的学术水平。
- **算法框架清晰**: 提出了两种互补的算法，覆盖了专家演示和学习者自身数据两种常见数据获取场景，实用性强。
- **可复现性好**: 论文给出了完整的算法流程和关键的数学公式（如Q函数、更新公式），实验结果部分提供了具体的系统参数和仿真结果，使得算法易于复现。

### 不足与局限

- **验证规模有限**: 如“实验充分性”所分析的，仿真仅在低维度（2维）进行，缺乏对高维、复杂系统的扩展性验证。
- **缺乏消融实验**: 没有对关键的超参数（如探测噪声的幅度、批量大小、采样间隔）进行敏感性分析，也未比较离策略与在策略算法的性能差异。
- **缺乏对比基准**: 未与已有方法（如基于模型的IOC、求解Riccati方程的方法）进行直接对比，无法量化其在计算效率、数据需求和鲁棒性上的具体优势。
- **假设条件限制**: 问题建立在线性二次（LQ）框架下，代价矩阵和增益是线性的。对于非线性的、未知的代价函数结构，该方法不适用。
- **应用风险**: 虽然理论证明了稳定性，但在实际应用中，由于使用最小二乘法和有限数据，存在过拟合风险，且对数据采集的“持久激励”条件有严格要求，不满足条件时算法会失效。

（完）
