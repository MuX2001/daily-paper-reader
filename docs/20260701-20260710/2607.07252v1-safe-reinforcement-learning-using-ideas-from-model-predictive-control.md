---
title: Safe Reinforcement Learning using Ideas from Model Predictive Control
title_zh: 利用模型预测控制思想的安全强化学习
authors: "Georg Schäfer, Jakob Rehrl, Stefan Huber, Simon Hirlaender"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07252v1"
tags: ["query:control"]
score: 9.0
evidence: 将深度强化学习与模型预测控制结合，为机器人提供安全保障
tldr: 针对强化学习在物理系统中探索时需严格满足安全约束的挑战，本文提出结合深度强化学习与模型预测控制的通用框架。通过离线MPC计算全局可行状态-动作空间，并利用安全滤波器将RL动作投影至该集合内。在非线性单自由度实验台上的物理硬件验证表明，该方法实现了安全探索与策略收敛。该框架为安全关键系统的RL应用提供了有效途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07252v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1193, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 977, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07252v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1251, \"height\": 1617, \"label\": \"Figure\"}]"
motivation: 强化学习在复杂物理系统探索中必须满足硬安全约束，以避免不可逆损坏。
method: 将深度强化学习与模型预测控制结合，离线MPC定义可行域，再通过安全滤波器在线投影RL动作。
result: 在非线性1-DoF实验台上成功实现安全探索与稳定策略收敛。
conclusion: 所提通用框架为强化学习在安全关键系统中的应用提供了可行方案。
---

## 摘要
强化学习可以直接从数据中合成控制策略，因此对于复杂的网络物理系统（CPS）和机器人技术极具吸引力。然而，一个持续的挑战是在主动学习阶段确保严格、硬性的安全约束。在现实世界的物理系统中，违反机械极限可能导致不可逆的损坏，因此必须确保探索严格限制在安全运行区域内。我们提出了一个通用框架，将深度强化学习（DRL）的自适应高性能特性与模型预测控制（MPC）的形式化安全保障相结合。利用系统动力学的数学模型，离线MPC计算定义了可行的状态-动作空间，即保证约束满足的系统状态和控制输入的所有安全组合。在训练和部署过程中，通过安全过滤器将RL代理的瞬时动作投影到这个全局验证的可行集上。我们在一个非线性单自由度实验室测试平台上系统评估了我们的通用方法，展示了在物理硬件上成功的探索和稳定的策略收敛。

## Abstract
Reinforcement learning (RL) enables the synthesis of control policies directly from data, making it highly appealing for complex cyber-physical systems (CPSs) and robotics. A persistent challenge, however, is ensuring strict, hard safety constraints during the active learning phase. In real-world physical systems, violating mechanical limits can cause irreversible damage, necessitating that exploration remains strictly within safe operational regions. We propose a generalized framework that combines the adaptive, high-performance nature of deep reinforcement learning (DRL) with the formal safety guarantees of model predictive control (MPC). Using a mathematical model of the system dynamics, offline MPC computations define a feasible state-action space, representing all safe combinations of system states and control inputs that guarantee constraint satisfaction. During training and deployment, the RL agent's instantaneous actions are projected onto this globally verified feasible set via a safety filter. We systematically evaluate our generalized approach on a non-linear 1-DoF laboratory testbed, demonstrating successful exploration and stable policy convergence on physical hardware.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：强化学习（RL）在复杂物理系统（如CPS、机器人）中应用时，必须满足严格、硬性的安全约束（如机械位置、速度、力矩限制）。标准RL依赖随机探索以优化奖励，但无约束的探索可能立即导致硬件损坏或不可逆故障。
- **整体含义**：需要一种机制，在主动学习阶段**绝对保障**系统始终运行在安全状态内，同时不牺牲RL的灵活性和高性能。本文提出将深度强化学习（DRL）的探索能力与模型预测控制（MPC）的形式化安全保证相结合，通过离线预计算可行空间，在线使用轻量级投影滤波器，解决“安全探索悖论”。

## 2. 方法论

### 核心思想
- 利用离线MPC作为**可行性预言机**，预定义全局安全的状态-动作空间 **F**（所有可递归保证长期约束满足的（状态, 动作）对）。
- 在线部署时，对RL代理提出的原始动作**实时投影**到F中，确保任何执行的动作都严格安全。

### 关键技术细节

#### 1) 离线MPC可行性映射
- MPC优化问题（公式2-5）：在预测时域H内最小化代价，并强制状态约束 `X_safe` 和输入约束 `U_safe`。
- 二元预言机函数（公式10）：  
  `Oracle(s, a) = 1` 若存在未来动作序列使系统在H步内均满足约束；否则为0。
- **凸性利用**：对于控制仿射系统（输入线性），可行动作集 `FA(s)` 是连续的凸区间。因此只需通过**二分搜索**确定最小可行动作 `a_min(s)` 和最大可行动作 `a_max(s)`，即可完整描述 `FA(s)`。

#### 2) 安全滤波器（投影机制）
- **投影滤波器** `P_s`（公式11）：  
  - 若原始动作 `a_t` 在 `FA(s_t)` 内，直接执行。  
  - 否则，投影到最近的可行动作（欧氏距离）。
- **闭环训练**：将投影后的安全动作传递给环境，计算下一状态和奖励，保持反馈回路。

#### 3) 奖励塑造
- 可对投影幅度 `||π(s) - ̄π(s)||` 施以惩罚，鼓励代理学习内化边界。
- 额外引入动作平滑惩罚（滑动窗口标准差）以抑制振动控制。

### 算法流程（文字说明）
1. **离线阶段**：基于系统模型，通过网格搜索（配合二分搜索利用凸性）计算所有离散状态下的 `a_min` 和 `a_max`，构建查找表映射 `F`。
2. **在线训练阶段**：  
   - 代理观察状态 `s_t` → 输出原始动作 `a_t`。  
   - 安全滤波器查询 `F`，获得安全区间，投影得到安全动作 `̄a_t`。  
   - 环境执行 `̄a_t`，返回下一状态 `s_{t+1}` 和奖励 `r_{t+1}`。  
   - 代理更新策略（使用PPO等算法）。

## 3. 实验设计

- **实验平台**：Quanser Aero 2 实验台（双旋翼航空系统），配置为单自由度（1-DOF）俯仰控制，机械限制 ±60°（约 ±1.05 rad）。
- **场景**：非线性动力学模型（公式12-13），包含重力、摩擦、电压-推力关系。采用真实物理硬件+Simulink仿真。
- **基准（Benchmark）**：未明确列出独立的对比方法。文中提及与“无约束标准RL”比较，但未提供定量对比表。实验主要验证所提框架自身的安全探索与收敛能力。
- **对比方法**：论文未设置基线算法（如CPO、CBF-RL等）的横向对比，仅定性描述“相比基线方法大幅降低违规率”。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量或训练时长。仅提及依赖Python环境（Stable Baselines3）、PPO算法，以及Simulink模型转DLL与Gymnasium接口。对于物理硬件训练，未报告迭代次数或时间开销。
- **需指出**：该论文未提供算力相关细节，可能是由于实验主要部署在物理硬件和本地仿真，GPU并非关键瓶颈。

## 5. 实验数量与充分性

- **实验数量**：仅针对单个1-DOF测试平台（一种配置）进行验证。未涵盖多输入多输出系统、不同非线性程度或更高自由度场景。
- **消融实验**：无。例如，未对投影惩罚、预测时域长度H、网格分辨率等超参数进行系统性消融分析。
- **充分性评估**：实验属于**概念验证**级别，证明了框架在基本场景下的可行性和安全性，但不足以证明其广泛适用性。缺乏对模型失配、噪声、计算延迟等实际因素的鲁棒性量化分析。未与其他安全RL方法（如CPO、CBF、Shielding）进行公平定量比较。

## 6. 主要结论与发现

- **成功实现安全探索**：所提投影滤波器使PPO代理能够在训练过程中主动探索状态空间边界，且绝大部分时间保持约束满足。
- **稳定策略收敛**：物理硬件上的训练能够收敛到高性能策略。
- **局限性暴露**：存在“Sim-to-Real”差距，导致偶见的约束违反（归因于模型不准确、离散采样误差、延迟等）。证明绝对安全依赖于模型精度和实时执行鲁棒性。

## 7. 优点

- **创新性方法**：将离线MPC的全局可行性分析与在线RL的轻量级投影相结合，避免在线求解MPC的高计算成本。
- **凸性利用大幅降低映射成本**：对于控制仿射系统，二分搜索快速确定安全区间，网格搜索维度可管理（文中仅1-DOF状态空间）。
- **理论保证与实际部署的平衡**：通过足够长的预测时域H近似递归可行性，同时框架通用，可适用于多种动态系统。
- **实物验证**：在真实的实验台上成功测试，显示了从仿真到硬件的迁移可行性。

## 8. 不足与局限

- **对模型精度的强依赖**：安全保证仅当系统模型准确匹配。实际中“Sim-to-Real”差距会导致违反，文中也观察到偶发违规。
- **维度灾难**：网格搜索随状态-动作空间维度指数增长，未解决高维系统可扩展性问题（虽提出未来用高斯过程改进）。
- **实验覆盖不足**：仅1-DOF简单系统，未测试多自由度、非凸可行集、输入耦合等更复杂情况。没有与现有安全RL方法（如CPO、CBF）的定量对比。
- **缺乏超参数敏感性分析**：未讨论预测时域H长度、网格分辨率、投影惩罚权重等对安全性的影响。
- **违规率未量化**：仅定性描述“偶发”违规，未给出具体的违反次数或概率统计，无法客观评估安全程度。

（完）
