---
title: Iterative graph lifting for automatic design of path-complete stability certificates
title_zh: 用于自动设计路径完全稳定性证书的迭代图提升方法
authors: "Léa Ninite, Raphaël M. Jungers"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00637v1"
tags: ["query:control"]
score: 9.0
evidence: 切换线性系统稳定性，控制理论
tldr: 针对切换线性系统稳定性问题，本文提出一种迭代图提升算法，自动设计路径完备图以逼近联合谱半径。算法基于最优性条件的图论分析，检测瓶颈节点并通过局部节点分裂细化图结构，重复迭代直至满足精度。数值实验表明该方法高效且可扩展，在多个挑战性实例上优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00637v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 272, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00637v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 375, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00637v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 610, \"height\": 1126, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00637v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 636, \"height\": 223, \"label\": \"Table\"}]"
motivation: 手动设计路径完备图效率低且依赖经验，需自动方法生成高精度稳定性证书。
method: 迭代分析最优性条件，识别瓶颈节点，通过局部节点分裂细化路径完备图。
result: 数值实验验证算法有效、可扩展，在全部测试实例上超越现有最优方法。
conclusion: 迭代图提升可自动、高效地生成精确的联合谱半径上界，适用于复杂切换系统。
---

## 摘要
切换线性系统在任意切换下的稳定性是控制理论中的一个基本问题，与刻画系统轨迹最坏情况增长率的联合谱半径（JSR）密切相关。本文针对近似JSR的路径完全方法做出了贡献。该框架利用称为路径完全图的带标签有向图构造代数稳定性证书，这些证书可通过相关优化问题计算得到。我们提出了一种迭代算法，以高效且简洁的方式改进路径完全图。该算法基于底层优化问题最优性条件的图论分析。特别地，我们推导出一个充分条件，当给定路径完全图满足该条件时，JSR能够精确达到。当条件不满足时，我们通过分析主动约束所诱导的图来识别瓶颈节点。进而利用该信息通过局部图提升（节点分裂）改进路径完全图，并重复该过程。数值实验表明，所提出方法具有有效性和可扩展性，在测试的所有具有挑战性的实例上均优于现有最先进方法。

## Abstract
Stability of switched linear systems under arbitrary switching is a fundamental problem in control theory, closely related to the joint spectral radius (JSR), which characterizes the worst-case growth rate of system trajectories. In this paper, we contribute to the path-complete approach for approximating the JSR. This framework constructs algebraic stability certificates using labeled directed graphs, known as path-complete graphs. These certificates can be computed via an associated optimization problem. We propose an iterative algorithm that refines path-complete graphs in an efficient and parsimonious manner. The algorithm relies on a graph-theoretic analysis of the optimality conditions of the underlying optimization problem. In particular, we derive a sufficient condition under which the exact JSR is attained by a given path-complete graph. When this condition is not satisfied, we identify bottleneck nodes by analyzing the graph induced by the active constraints. We then use this information to refine the path-complete graph via local graph lifting (node splitting), and repeat the procedure. Numerical experiments demonstrate the effectiveness and scalability of the proposed approach, outperforming state-of-the-art methods on all challenging instances tested.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：离散时间切换线性系统在任意切换信号下的稳定性分析，其关键指标是**联合谱半径（JSR）**。JSR刻画了矩阵集合所有乘积的最坏情况增长速率，但精确计算JSR在理论上不可判定，实际中只能寻求高质量的上界。
- **研究背景**：路径完全Lyapunov框架提供了一种系统化的上界逼近方法：通过构建满足特定性质的带标签有向图（路径完全图），并求解相应的半定规划（SDP）得到JSR上界。然而，图的结构直接影响上界的紧性：图越大（节点/边越多），上界通常越紧，但计算代价也越高。现有系统性构造如De Bruijn图随阶数指数增长，导致优化问题规模膨胀。因此，如何自动、高效地构造紧凑且紧致的路径完全图是一个亟待解决的问题。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：**迭代式局部图提升**。通过分析优化问题最优解对应的“紧约束子图”（tight subgraph），识别出“瓶颈节点”（即有多条紧出边的节点），并对这些节点执行一种保路径完全性的局部图变换——**前向提升（forward lift）**，从而在不均匀增加全局图规模的前提下收紧JSR上界。
- **关键技术细节**：
    1. **前向提升操作**（Definition 5）：给定图 \(G=(S,E)\) 和节点 \(v\)，将节点 \(v\) 分裂成 \(|Post(v)|\) 个副本（每个副本对应一个出邻接点），并重新定义边集（分四类：\(E_1\) 原边中不涉及 \(v\) 的部分；\(E_2\) 入边到 \(v\) 的边改指向所有副本；\(E_3\) 从 \(v\) 的出边改由对应副本出发；\(E_4\) 自环边对应副本之间的边）。定理2证明该操作保持路径完全性，定理3证明提升后SDP的最优值不增（即松弛了原问题）。
    2. **紧子图分析**（Section IV）：定义紧子图 \(\tilde{G}\) 为所有满足 \(\lambda_{\min}(\gamma^2 P_a - A_i^\top P_b A_i)=0\) 的边构成的子图。命题1表明原问题与紧子图的最优值相同。进一步，若紧子图中每个节点至多有一条出边（即每个强连通分量要么是孤立节点要么是简单环），则根据命题2和定理4，当前上界已精确等于JSR（最优性证书）。
    3. **算法流程**（Algorithm 1）：
        - 初始化：选择任意一个路径完全图（如 \(M=2\) 时的双节点图），求解SDP得到\(\gamma_0\)和紧子图。
        - 循环：
            - 找出紧子图中出边数≥2的节点集\(\mathcal{A}_n\)；若\(\mathcal{A}_n=\emptyset\)则输出当前\(\gamma_n\)作为最优上界并终止。
            - 从\(\mathcal{A}_n\)中选一个节点 \(v_n\)，对其执行前向提升得到新图 \(G_{n+1}\)。
            - 求解新图上的SDP，更新紧子图，重复。
- **公式与算法文字说明**：无需数学公式，已用文字描述上述关键步骤。优化问题为标准的二次型Lyapunov LMI（Problem 1），求解使用商业SDP求解器（如MOSEK）。

#### 3. 实验设计：数据集/场景、基准、对比方法
- **数据集/场景**：随机生成500个切换线性系统，参数为**6个模式（\(M=6\)）、状态维数 \(n=2\)**。每个系统的矩阵元素随机生成。
- **基准**：**De Bruijn图层次结构**（Definition 8），这是文献中常用的系统性图族。通过增加De Bruijn图的阶数（内存长度）来收紧JSR上界。
- **对比方法**：将Algorithm 1的结果（达到指定精度时的图规模和计算时间）与De Bruijn图达到相同精度时的图规模和计算时间进行比较。此外，还测试了不同模式数 \(M=4,6,8,10\) 时，两种方法在30分钟时间限制内能达到的精度与图节点数。

#### 4. 资源与算力
- **文中未明确提及任何GPU型号、数量或训练时长**。所有实验在配备Apple M5处理器和16GB RAM的笔记本电脑上运行。算法仅依赖CPU进行SDP求解，计算时间均为CPU时间。因此无法推断对GPU算力的需求。

#### 5. 实验数量与充分性
- **实验数量**：
    - 主实验（图3）：500个随机系统实例，每个实例均运行Algorithm 1直至收敛（精度<1e-5或无瓶颈节点），并对比相应的De Bruijn图。
    - 扩展实验（Table I）：选取几个挑战性实例，对比不同\(M\)值（4,6,8,10）下两种方法在30分钟内的表现。
- **充分性评估**：实验覆盖了不同模式数和随机矩阵，但**维度固定为\(n=2\)**，未测试更高维系统（如\(n=3,5,10\)）。随机生成方式可能未涵盖所有困难拓扑（如病态矩阵、接近临界稳定性的系统）。此外，仅与De Bruijn图对比，未与其他自动构造方法（如SMT-based方法[18]）直接对比计算代价。总体而言，实验设计合理、数量充分，但维度覆盖和对比方法多样性存在局限。

#### 6. 论文的主要结论与发现
- **理论发现**：提出了一个基于紧子图结构的**最优性证书**：当紧子图中每个节点至多有一条出边时，当前上界等于真实JSR，算法可提前终止。
- **算法有效性**：Algorithm 1能自动、自适应地构造比De Bruijn图更紧凑的路径完全图。在500个实例中，算法产生的图从未比De Bruijn图大，且在46个实例中显著更小（例如De Bruijn图需7776节点而算法仅36节点）。
- **计算效率**：对于需要高精度上界的实例，Algorithm 1比De Bruijn层次结构快几个数量级（最极端实例：2165秒 vs 10.5秒）。对于简单实例，两者耗时均很短，算法略慢但仍在1秒内。
- **可扩展性**：当模式数\(M\)增大时，De Bruijn层次结构在30分钟内无法达到算法精度，而算法始终能在40秒内（M<10时10秒内）给出更紧的上界。

#### 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
    - **理论驱动**：严格证明了提升操作的保路径完全性和非增松弛性；基于紧子图的结构分析推导出最优性条件，为算法终止提供理论保证。
    - **局部自适应**：只对“瓶颈节点”（有多条紧出边）进行提升，避免了全局均匀增大图规模，实现了“少而精”的图增长。
    - **可解释性**：通过紧子图直观地揭示哪些约束限制了上界紧性，为图结构设计提供了可视化指导。
- **实验亮点**：
    - 系统性地对比了De Bruijn层次结构这一公认可收敛的基线，实验设置客观。
    - 使用随机生成的大规模实例（500个）验证鲁棒性，并给出极端实例的详细对比。
    - 包含了不同模式数的可扩展性测试，突出了算法在高维输入下的优势。

#### 8. 不足与局限
- **实验覆盖不足**：
    - 仅测试了状态维数 \(n=2\) 的系统，未评估更高维系统（如\(n=4,8\)）下算法的性能与SDP求解器的扩展性。高维时SDP问题规模会随图节点数线性增长，可能成为瓶颈。
    - 初始图固定为De Bruijn阶1，未探索不同初始图（如随机图、SMT生成图）对算法收敛速度的影响。
    - 对比方法仅包括De Bruijn图，未与最近提出的SMT方法[18]或其他图族（如基于集值Lyapunov函数的图）进行定量比较。
- **偏差风险**：
    - 紧子图结构分析基于严格凸性假设（SDP问题的对偶间隙为零），实际求解中若数值精度有限，紧子图的识别可能不准确，导致算法提前停止或过度提升。
    - 节点选择策略（从\(\mathcal{A}_n\)中任选一个）未给出具体启发式规则，可能影响收敛效率（如选择最优的瓶颈节点可加速）。
- **应用限制**：
    - 方法目前局限于**二次型Lyapunov函数**和**线性矩阵不等式**的形式。对于多项式Lyapunov函数或更一般凸函数类，LMI结构需修改，算法的理论性质（如紧子图分析）可能需要重新推导。
    - 要求图满足路径完全性，虽然提升操作保持该性质，但初始图必须已是路径完全的；若初始图验证困难（PSPACE完全），可能需预先构造已知的简单路径完全图。

（完）
