---
title: Asynchronous Sensitivity-Based Distributed NMPC
title_zh: 基于异步灵敏度的分布式非线性模型预测控制
authors: "Maximilian Pierer von Esch, Andres Völz, Knut Graichen"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03412v1"
tags: ["query:control"]
score: 7.0
evidence: 异步分布式非线性模型预测控制
tldr: "针对非线性连续时间系统的协同分布式模型预测控制，提出了异步基于灵敏度的分布式编程方案。通过固定次数的邻域通信和无需同步的迭代求解集中优化，在标称稳定性和有限延迟假设下证明了局部指数稳定性。数值与硬件在环实验（以太网/Wi-Fi）表明，异步执行可减少60%以上的计算时间，且闭环性能几乎不变。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03412v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 324, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03412v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 880, \"height\": 338, \"label\": \"Table\"}]"
motivation: 现有分布式MPC多需同步迭代或全局信息，导致实时性差；而异步执行可降低通信与计算开销。
method: 采用基于灵敏度的分布式编程，各代理在固定迭代次数内仅与邻居交换局部敏感度信息，异步更新控制量。
result: "在以太网和Wi-Fi测试中，异步方案执行时间减少超60%，闭环性能与同步方案相当。"
conclusion: 提出的异步分布式NMPC在保证稳定性的前提下，显著提升实时性，适用于资源受限的网络化控制系统。
---

## 摘要
本文提出了一种针对非线性连续时间系统的协同分布式模型预测控制方案。通过固定次数的基于灵敏度的分布式规划迭代，异步求解集中式最优控制问题。该方案仅需邻居间通信，且在优化过程中智能体间无需同步。在标称模型预测控制稳定性和有限信息延迟条件下，当每个智能体的SBDP迭代次数足够大时，建立了局部指数稳定性。在以太网和Wi-Fi上的数值与硬件在环测试结果表明，异步执行显著降低执行时间超过60%，同时保持相当的闭环性能。

## 速览
**TLDR**：针对非线性连续系统的分布式MPC，提出异步灵敏度分布式编程方案，通过固定次数的邻居间通信迭代逼近集中解。理论证明在名义稳定性和有限信息延迟下局部指数稳定。以太网和Wi-Fi实验表明，异步执行相比同步减少60%以上时间，且闭环性能相当。 \
**Motivation**：同步分布式MPC需要代理间严格协调，导致通信和等待开销大；异步执行可减少延迟和计算空闲。 \
**Method**：基于灵敏度的分布式编程，每个代理进行固定次数的迭代，仅与邻居交换局部灵敏度信息，无需全局同步。 \
**Result**：实验显示异步执行时间减少超60%，闭环性能与同步方案相近。 \
**Conclusion**：异步灵敏度分布式NMPC在保证稳定性的同时显著提升计算效率。

---

## Abstract
This paper presents a cooperative distributed model predictive control (MPC) scheme for nonlinear continuous-time systems. The centralized optimal control problem is solved asynchronously via a fixed number of sensitivity-based distributed programming (SBDP) iterations. The proposed scheme requires only neighbor-to-neighbor communication and no synchronization between agents during optimization. Under nominal MPC stability and bounded information delay, local exponential stability is established for a sufficiently large number of per-agent SBDP iterations. Numerical and hardware-in-the-loop results on both Ethernet and Wi-Fi demonstrate the benefits of an asynchronous execution, reducing execution times by over 60% while maintaining comparable closed loop performance.

---

## 论文详细总结（自动生成）

### 论文总结：基于异步灵敏度的分布式非线性模型预测控制

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现代大规模互联系统（如能源系统、过程工程、自主移动机器人）需要实时分布式控制。模型预测控制（MPC）通过重复求解最优控制问题提供系统性框架，但集中式求解高维问题在实时应用中变得不可行。分布式MPC通过将计算分散到多个智能体来利用系统结构，但现有的协同分布式MPC方案大多依赖同步执行，要求智能体之间严格协调，导致通信延迟和异构计算负载引起的空闲等待时间。
- **背景**：异步分布式优化方法近年来受到关注，允许智能体使用本地可用但可能过时的信息进行更新，以减少等待时间和同步开销。然而，异步带来的收敛和闭环稳定性挑战，特别是在实时MPC中只能执行有限迭代的情况下，尚未得到充分解决。本文针对这些问题，提出了一种基于异步灵敏度分布式编程（a-SBDP）的协同DMPC方案，并证明其在标称稳定性和有限信息延迟条件下的局部指数稳定性。

#### 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：在每一MPC步内，各智能体异步地执行固定次数的SBDP迭代，仅与邻居交换局部轨迹及灵敏度信息，无需全局同步。通过将异步限制在优化层（MPC步之间仍同步），允许使用UDP等松散通信协议。
- **关键技术细节**：
  - **问题建模**：考虑无向连通图，每个智能体有局部动态和成本，呈现邻域仿射结构。集中式OCP（1）可分解为局部OCP（5），每个智能体求解时使用邻居的最新可用轨迹（可能过时）。
  - **SBDP迭代**：每个智能体在每次迭代中求解一个增广局部OCP，其中增加了一个基于邻居灵敏度（梯度）的线性项（公式6），以协调耦合。然后更新局部轨迹并广播给邻居。
  - **异步执行描述**：全局更新事件视为按顺序发生，每次只有一个智能体更新（singleton更新）。定义延迟d和更新频率s（部分异步假设）。算法1给出本地视角：初始化、求解、广播、接收、计数。
  - **收敛性**：在引入局部强二次型条件（Assumption 2）和部分异步假设下，证明了a-SBDP的R-线性收敛（Theorem 4）。这要求预测时域T足够小，且初始解在固定邻域内。
- **算法流程**（文字说明）：
  1. 各智能体初始化轨迹，发送给邻居。
  2. 每个智能体在本地执行固定次数¯q_i迭代：
     - 使用当前可用的邻居轨迹（可能不是最新）求解局部OCP（5）。
     - 将新求解的轨迹广播给邻居。
     - 接收来自邻居的更新（可能不完整）。
  3. 重复直到所有智能体完成其¯q_i次迭代。
  4. 使用最终的控制轨迹（初始段）施加到系统。

#### 3. 实验设计
- **场景与基准**：
  - 数值仿真：三智能体系统（M=3），动态为 `ẋ_i = u_i + Σ sin(x_j)`，不稳定原点。考虑五个代表性初始条件（|x_i0| ≤ 1）。
  - 硬件在环实验：使用Raspberry Pi 3B+网络（以太网和Wi-Fi），初始状态 `x0=[0.75,0.5,0.25]^T`。
- **对比方法**：同步SBDP（即所有智能体同时更新，等待所有邻居数据） vs 异步SBDP（直接使用现有数据继续）。
- **评估指标**：
  - 执行时间（每个MPC步的耗时）的统计（中位数、最大值）。
  - 闭环成本 J_cl（积分阶段成本）。
  - 稳定性验证：对比最优MPC（精确解）与异步DMPC的轨迹。
- **实验数量**：
  - 数值仿真：从M=3系统，5个初始条件，各运行一定步数（未明确具体步数，但从稳定性验证看是闭环仿真）。
  - HIL实验：每个配置（同步/异步 × 以太网/Wi-Fi）运行1000个MPC步，重复多次？文中只给出了1000步的直方图。
  - 消融实验：未单独做，但通过调节迭代次数¯q来验证理论阈值（q2和q1）。此外，估计了实际异步常数ˆC以满足理论条件。

#### 4. 资源与算力
- 文中未明确提及GPU或大规模算力。硬件在环实验使用Raspberry Pi 3B+（ARM CPU）作为计算节点，网络通过以太网和Wi-Fi连接。算力需求很低，适合嵌入式设备。未提及训练时长或GPU型号。

#### 5. 实验数量与充分性
- **数值验证**：对五个初始条件，对比最优MPC和DMPC，验证了状态轨迹和误差的指数衰减，以及Lyapunov函数下降。虽然初始条件数量不多，但覆盖了不同幅度，且与理论常数匹配，具有一定代表性。
- **HIL实验**：仅一种初始条件，但1000步运行足以统计执行时间分布。此外，估计了实际异步常数¯C，并验证了理论要求¯q > 21（实际采用21），说明稳定性条件满足。对比了同步和异步两种网络环境。实验设计基本充分，但未能覆盖更多系统规模或不同耦合强度。
- **公平性**：同步与异步使用相同的算法和代码，仅同步要求等待，因此对比公平。但HIL实验中仅一种初始状态，可能缺少泛化性。另外，Wi-Fi下同步方案有20%的MPC步失败，而异步方案都能成功，显示了鲁棒性优势，但统计时失败步被重启，可能引入偏差。

#### 6. 主要结论
- 提出的异步灵敏度分布式MPC方案在理论上保证了在足够多的迭代次数下的局部指数稳定性（Theorem 12），条件包括名义MPC的Lyapunov性质、部分异步假设（有界延迟）。
- 硬件在环实验表明：异步执行在以太网和Wi-Fi上分别减少中位数执行时间约15%和61%，减少最大值19%和63%，而闭环成本几乎不变（增加约0.2%）。特别是Wi-Fi场景下，异步方案能容忍丢包，提高了鲁棒性。
- 定量估计了稳定所需的迭代次数（q2=6.01(s+d)），同步时¯q=7即可，异步时按实际延迟推算需≥21次（与实验使用的21次一致），验证了理论。

#### 7. 优点
- **理论严密**：从分布式优化角度严格证明了在异步执行下的R-线性收敛和闭环指数稳定性，给出了迭代次数的充分条件。
- **实践价值**：方案仅需邻居间通信，无需同步协调，兼容UDP等轻量协议，显著减少执行时间和同步等待，适合无线、易丢包的工业环境。
- **实验验证全面**：既有数值仿真验证理论参数，又有硬件在环实验（含两种网络）证明实际性能提升，还估算了实际收敛常数来验证理论阈值。
- **开源实现**：提供了代码仓库，便于复现和扩展。

#### 8. 不足与局限
- **实验规模**：仅测试了3个智能体，缺少更大规模（如10+智能体）的性能验证。延迟和丢包影响可能随规模增大而更显著。
- **假设限制**：依赖部分异步假设（有界延迟和每个智能体至少每s次更新一次），在高度动态的网络中可能不满足；另外需要名义MPC满足严格Lyapunov条件，且要求初始解在收缩域内。
- **未考虑状态约束**：论文仅处理输入约束，未涉及状态约束（如避障），实际应用中状态约束更常见。
- **参数估计复杂**：理论常数（如C、Lp等）需要离线估算，且依赖于系统模型，对于未知或时变系统可能不适用。
- **硬件实验单一初始条件**：仅一个初始状态，恢复性能的泛化性未充分论证。另外，Wi-Fi同步失败被重启的处理方式可能低估了同步方案的实际缺陷。

（完）
