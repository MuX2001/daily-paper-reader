---
title: Data-driven Kernel-based Predictive Control with Stability and Robustness Guarantees
title_zh: 数据驱动的基于核的预测控制：具有稳定性和鲁棒性保证
authors: "Wenjie Liu, Yifei Li, Gang Wang, Lihua Xie"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02851v1"
tags: ["query:control"]
score: 8.0
evidence: 数据驱动的核预测控制，稳定性与鲁棒性保证
tldr: 数据驱动的非线性预测控制常缺乏严格的稳定性与鲁棒性理论支撑。本文提出基于核方法的隐式多步预测器，构建了数据驱动核预测控制(DDKPC)框架。理论证明在无噪声条件下，通过足够长预测时域和足够小核表示误差可实现递归可行性与闭环稳定性，并引入罚松弛降低非凸约束计算复杂度。针对噪声与时变系统，进一步统一不确定性边界并利用在线字典更新，维持稳定性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02851v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 926, \"height\": 664, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 915, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 893, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 913, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02851v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 899, \"height\": 588, \"label\": \"Figure\"}]"
motivation: 为数据驱动非线性预测控制提供稳定性与鲁棒性的严格理论分析。
method: 提出基于核表示定理的多步预测器，集成鲁棒预测框架，并采用罚松弛处理非凸约束。
result: 理论上保证了无噪声时递归可行性和稳定性的充分条件；对噪声和时变系统给出鲁棒性扩展。
conclusion: 建立数据驱动核预测控制的理论基础，适用于非线性及时变系统。
---

## 摘要
本文从理论上分析了仅使用输入输出数据开发的数据驱动基于核的预测控制（DDKPC）方案的闭环特性。所提出的公式将鲁棒数据驱动预测控制框架与通过基于核方法构建的非线性系统多步预测器相结合。该预测器利用表示定理隐式捕捉系统的非线性行为。对于无噪声数据的标称情况，我们证明，如果预测范围足够长且核表示误差足够小，DDKPC方案能保证递归可行性和闭环稳定性。为便于实时实现，我们引入惩罚松弛公式，以减轻由非凸隐式约束引起的计算负担。此外，通过将表示失配和有界噪声聚合为一个统一的不确定性界，该框架对测量噪声具有鲁棒性。最后，我们将DDKPC框架扩展到缓慢时变非线性系统，通过使用由近似线性依赖（ALD）标准管理的固定预算在线字典定期重建核预测器。在输入输出演化变化率和在线预测误差的适当条件下，保持了递归可行性和实际闭环稳定性。通过数值示例说明了所提出方法的有效性。

## 速览
**TLDR**：针对非线性系统的数据驱动控制缺乏理论保证的问题，提出一种基于核方法的预测控制（DDKPC）方案，仅依赖输入输出数据构建多步预测器。理论分析表明，在无噪声情形下，若预测时域足够长且核表示误差充分小，该方案保证递归可行性与闭环稳定性。通过引入惩罚松弛处理非凸约束，并将表示误差与有界噪声聚合成统一不确定性边界，增强了对测量噪声的鲁棒性。进一步利用近似线性依赖准则在线更新核预测器，将框架扩展至缓慢时变非线性系统，维持了递归可行性和实际稳定性。 \
**Motivation**：现有数据驱动预测控制针对核方法缺乏稳定性与鲁棒性的理论保证，亟需从数据中构建可靠的非线性预测控制器。 \
**Method**：基于核方法的非线性多步预测器结合鲁棒预测控制框架，引入惩罚松弛解决非凸隐式约束，将表示误差与噪声聚合为统一不确定性边界，对时变系统采用ALD在线字典更新。 \
**Result**：证明无噪声时递归可行性与闭环稳定性；在有界噪声下保持鲁棒性；对缓慢时变系统仍保证实际稳定性。 \
**Conclusion**：DDKPC为非线性系统提供具有理论保证的数据驱动预测控制方案，并通过数值示例验证了其有效性。

---

## Abstract
In this paper, we provide a theoretical analysis of the closed-loop properties of a data-driven kernel-based predictive control (DDKPC) scheme developed solely from input-output data. The proposed formulation integrates a robust data-driven predictive control framework with a multi-step predictor for nonlinear systems constructed via kernel-based methods. This predictor implicitly captures the system's nonlinear behavior using the representer theorem. For the nominal case with noise-free data, we prove that the DDKPC scheme guarantees recursive feasibility and closed-loop stability, provided that the prediction horizon is sufficiently long and the kernel representation error is sufficiently small. To facilitate real-time implementation, we introduce a penalty relaxation formulation to alleviate the computational burden inherently caused by nonconvex implicit constraints. Furthermore, the framework is robustified against measurement noise by aggregating the representation mismatch and the bounded noise into a unified uncertainty bound. Finally, we extend the DDKPC framework to slowly time-varying nonlinear systems by periodically reconstructing the kernel predictor from a fixed-budget online dictionary managed by the approximate linear dependency (ALD) criterion. Under suitable conditions on the rate of variation of the input-output evolution and the online prediction error, recursive feasibility and practical closed-loop stability are preserved. The effectiveness of the proposed approach is illustrated through numerical examples.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有数据驱动的预测控制（DDPC）方法多针对线性系统，对于非线性系统，特别是基于核函数或神经网络的预测控制方案，普遍缺乏严格的闭环理论保证（如递归可行性、稳定性等）。同时，非线性系统的预测控制面临建模困难、在线计算负担重、测量噪声干扰以及系统时变带来的挑战。

- **研究背景**：模型预测控制（MPC）广泛用于处理非线性动态与约束，但其性能高度依赖精确模型。为了摆脱复杂建模过程，学术界转向数据驱动方法。Willems 基本引理提供了线性系统的数据参数化，但非线性系统的DDPC缺少稳定性与鲁棒性方面的理论支撑。现有基于核的方法（如Kernelized DDPC [20]）虽然取得实证成功，但缺乏递归可行性和稳定性的证明。此外，未知非线性系统通常存在时变特性，固定离线数据集的预测器效果会逐渐下降。

- **整体含义**：本文旨在建立一种仅基于输入输出数据的、具有严格理论保证的基于核的预测控制（DDKPC）框架，填补非线性数据驱动预测控制在稳定性、鲁棒性、在线更新方面的理论空白。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用核方法（Reproducing Kernel Hilbert Space, RKHS）和表示定理，从历史输入输出数据中构建一个隐式的多步预测器，无需显式系统模型。该预测器被嵌入到鲁棒预测控制优化问题中，通过引入松弛变量处理表示误差，并通过调整预测时域长度和核误差界来保证闭环稳定性。

- **关键技术细节**：
  - **系统建模**：假设系统可以用有限记忆的输入输出表示（NARX形式），定义扩展状态 $\xi_t = [u_{t-\eta}^\top, \dots, u_{t-1}^\top, y_{t-\eta}^\top, \dots, y_{t-1}^\top]^\top$。
  - **核预测器构建**：利用离线数据构造 Gram 矩阵 $\bar{K}$ 和输出矩阵 $Y_F$，基于表示定理得到输出预测：$y_{\text{ker}}[1:pL] = Y_F (\bar{K} + \gamma I)^{-1} k(\zeta)$，其中 $\zeta = \text{col}(u_{\text{ini}}, y_{\text{ini}}, \bar{u}_{[1:mL]})$。该预测器满足确定性有限样本误差界。
  - **DDKPC 优化问题（标称情况）**：
    \[
    \begin{aligned}
    \min_{g,\bar{u},\bar{y},h} &\quad \sum_{i=0}^{L-1} \ell(\bar{u}_{i|t},\bar{y}_{i|t}) + \frac{\lambda_h}{\bar{d}}\|h_t\|^2 + \lambda_g \bar{d}\|g_t\|^2 \\[4pt]
    \text{s.t.} &\quad \begin{bmatrix} \bar{K}+\gamma I \\ Y_F \end{bmatrix} g_t = \begin{bmatrix} k(u_{\text{ini}},y_{\text{ini}},\bar{u}_{[0,L-1]|t}) \\ \bar{y}_{[0,L-1]|t} + h_{[0,L-1]|t} \end{bmatrix}, \\[4pt]
    &\quad \begin{bmatrix} u_{\text{ini}} \\ y_{\text{ini}} \end{bmatrix} = \begin{bmatrix} u_{[t-\eta,t-1]} \\ y_{[t-\eta,t-1]} \end{bmatrix}, \quad \bar{u}_{i|t} \in \mathcal{U}.
    \end{aligned}
    \]
    其中 $h$ 为松弛变量，补偿核表示误差；$\bar{d}$ 为误差上界。
  - **名义稳定性保证（定理1）**：在假设1-6下，若预测时域 $L > L_{\bar{Y}}$ 且核误差 $\bar{d} \leq \bar{d}_0$，则函数 $Y_L(\xi_t)=J_L^*(\xi_t)+V_o(\xi_t)$ 满足实际李雅普诺夫递减不等式，保证递归可行性和实际稳定性。
  - **计算加速**：提出将硬约束（19b）放松为目标函数中的二次罚项，得到松弛问题 $J_{\text{relaxed}}$，可用梯度法或加速梯度法（FastGD）求解，降低非凸隐式约束的计算负担。
  - **鲁棒化（带噪声）**：将测量噪声与核近似误差聚合为统一不确定性界 $\bar{w}=\bar{n}+\bar{d}$，修改优化问题中正则化权重和初始条件约束（30c），证明鲁棒稳定性（定理2）。
  - **时变系统扩展（算法2）**：采用固定预算在线字典管理策略根据 ALD 准则（近似线性依赖）动态更新，每 $T_0$ 步重建核预测器。在输入输出变化率有界且在线预测误差足够小的条件下，证明递归可行性与实际稳定性（定理3）。

- **算法流程**：
  - **标称 DDKPC（算法1）**：离线构造 $\bar{K},Y_F$；在每个采样时刻 $t$，获取过去 $\eta$ 个测量值，求解优化问题（19），应用最优输入 $u_t = \bar{u}_{0|t}^*$，重复。
  - **在线 DDKPC（算法2）**：初始化活跃字典 $\mathcal{D}^a_0$；每 $t$ 步判断是否需要更新字典（ALD 准则）；每 $T_0$ 步冻结当前字典，重建核预测器；每个时刻求解修改后的在线问题（38）。

## 3. 实验设计：数据集 / 场景、基准方法、对比方法

- **实验一：一维车辆模型（仿真）**
  - **场景**：一维车辆模型，动力学 $m_0\ddot{d} = u - c_1\dot{d} - c_2\dot{d}^2$，离散时间状态空间。参数仅在仿真中用于生成数据和控制，控制器未知。
  - **数据集**：离线收集 600 个样本（脉冲+白噪声）。
  - **基准**：线性 DDPC（基于 Hankel 矩阵）作为对比基线。
  - **对比方法**：
    - 不同核函数：高斯核 vs 多项式核。
    - 不同求解器：通用 NLP 求解器（fmincon）、投影梯度下降（GD）、加速梯度下降（FastGD）。
  - **测试内容**：
    1. 标称（无噪声）DDKPC 跟踪性能。
    2. 时变系统（参数 $c_1,c_2$ 正弦变化）下，静态离线 DDKPC vs 在线 DDKPC（GD、FastGD），不同更新周期 $T_0$。
    3. 有噪声情况（$\bar{n}=0.001$）下，静态 vs 在线 DDKPC。

- **实验二：A1 四足机器人（PyBullet 仿真）**
  - **场景**：四足机器人携带偏心载荷 (2 kg)，运行在 PyBullet 物理仿真环境中。DDKPC 作为外环速度残差控制器，与已有的全身运动控制栈串联。
  - **数据集**：离线收集两个 90 秒数据片段（无载荷和有载荷），构造固定字典（60 或 300 元素）。
  - **对比方法**：名义控制器（无 DDKPC） vs GD-DDKPC vs NLP-DDKPC。
  - **测试内容**：
    1. 前向速度跟踪（单变量）固定载荷。
    2. 载荷切换场景（0→2 kg→0）下静态 vs 在线 DDKPC。
    3. 平面速度跟踪（同时前向和横向双变量）。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的 GPU 型号、数量或训练时长。所有仿真在 14 核 Intel i7-12700H 处理器（2.3 GHz）上运行。时间开销仅给出求解时长（例如 GD-DDKPC 平均 23.43 ms，NLP-DDKPC 平均 82.66 ms）。未涉及大规模并行训练。

## 5. 实验数量与充分性

- **实验数量**：两个主要案例（车辆模型和四足机器人），每个案例包含多个子实验：
  - 车辆模型：至少 4 组（标称比较、时变静态 vs 在线、不同更新周期、有噪声）。
  - 四足机器人：至少 3 组（前向跟踪固定载荷、载荷切换、平面跟踪）。
- **充分性分析**：
  - **覆盖性**：考虑了无噪声、有噪声、时变非线性、不同优化求解器、不同核函数、不同更新周期，覆盖面较广。
  - **客观性**：与线性 DDPC 基线、名义控制器对比，展示了 DDKPC 的必要性和优势。
  - **公平性**：对于不同求解器和核函数，未进行详尽的超参数调优说明，但给出了性能排名。对比中默认使用相同问题设置。
  - **可重复性**：提供了关键参数（$\eta, L, Q, R, \gamma, \sigma_f, \ell$ 等），但未提供完整随机种子和多次运行的统计量（如标准差），可能影响对结果鲁棒性的判断。

## 6. 论文的主要结论与发现

- **理论层面**：
  - 标称 DDKPC 在预测时域足够长且核误差足够小时，保证递归可行性和实际闭环稳定性（定理1）。
  - 通过松弛隐式约束、聚集噪声误差，可处理非凸计算负担和测量噪声，保持鲁棒稳定性（定理2）。
  - 对缓慢时变非线性系统，在线字典更新与周期性预测器重建能维持实际稳定性（定理3）。
- **仿真验证**：
  - 线性 DDPC 无法稳定非线性车辆模型，而 DDKPC（高斯核或多项式核）均可稳定并跟踪参考。
  - NLP 求解器精度最高，但计算时间最长；FastGD 在精度和速度之间取得较好平衡。
  - 在线 DDKPC 比起静态离线方案显著降低了载荷切换场景下的跟踪误差（RMSE 降低 51.6%）。
  - 平面速度跟踪中，DDKPC 将平面 RMSE 降低了 24.7%。
  - 有噪声环境下，在线 DDKPC 仍保持稳定。

## 7. 优点

- **理论贡献**：首次为基于核的非线性数据驱动预测控制提供了递归可行性和闭环稳定性的严格证明。
- **方法创新**：
  - 将表示定理与鲁棒预测控制框架有机结合，避免显式模型辨识。
  - 提出惩罚松弛策略处理非凸隐式约束，减少实时计算负担。
  - 引入 ALD 管理的在线字典更新机制，使框架适用于缓慢时变系统。
- **实验设计**：覆盖无噪声、有噪声、时变系统等多种情形，且使用两种不同物理仿真（车辆、四足机器人）展示泛化能力。
- **性能验证**：在四足机器人上实现了可量化的性能提升，证实了实际应用潜力。

## 8. 不足与局限

- **理论假设较强**：
  - 假设核函数能使未知函数位于 RKHS 中且范数有界（Assumption 2），实际中难以严格验证。
  - 标称稳定性要求核误差界 $\bar{d}$ 足够小，但 $\bar{d}$ 的值依赖于紧集和核参数，实际可能难以精确界定。
  - 时变系统假设变化率有界（Assumption 9），该界可能需要离线估计。
- **实验局限性**：
  - 所有实验均在仿真环境中进行，未涉及真实硬件平台。
  - 未报告多次运行的统计结果（均值、标准差），无法判断随机性带来的变异性。
  - 对于不同核超参数、不同 NLP 求解器初值敏感性缺乏分析。
  - 计算时间虽给出，但未与非数据驱动方法（如基于模型的 MPC）对比。
- **可扩展性**：算法在字典规模较大时计算成本呈二次增长，且在线字典维护（ALD 阈值、更新周期）的选择缺少 systematic 指导。
- **鲁棒性边界**：统一不确定性界 $\bar{w}$ 采用简单加和形式，可能过于保守；也未讨论噪声分布非一致的情况。

（完）
