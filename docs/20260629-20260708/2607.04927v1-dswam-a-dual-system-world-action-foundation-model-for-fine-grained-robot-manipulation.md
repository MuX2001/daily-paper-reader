---
title: "DSWAM: A Dual-System World Action Foundation Model for Fine-Grained Robot Manipulation"
title_zh: DSWAM：面向细粒度机器人操作的双系统世界动作基础模型
authors: "Jian Zhu, Jianjun Zhang, Taiyi Su, Tianbin Liu, Zhangyuan Wang, Kai Xie, Zitai Huang, Chong Ma, Youzhang He, Tianjian Wang, Hanyang Wang, Weihao Ding, Yi Xu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04927v1"
tags: ["query:world-action"]
score: 9.0
evidence: 世界动作模型用于机器人操作
tldr: 现有世界动作模型(WAM)缺乏语言级规划接口，难以分解粗指令。本文提出DSWAM双系统模型：System 1为WAM执行器，System 2为视觉语言子任务规划器，仅在需要时激活。执行器采用动作预测与视频协同训练，推理时直接输出动作块，并通过TensorRT等加速。在DeMaVLA设置下与VLA公平比较，实现精细操作。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 887, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 2126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1656, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1209, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1215, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1290, \"height\": 280, \"label\": \"Table\"}]"
motivation: WAM缺乏语言级规划接口，难以分解复杂多步任务；且缺乏与VLA的公平实物比较。
method: DSWAM包含System 1 WAM执行器和System 2视觉语言子任务规划器，执行器通过动作预测和视频协同训练，推理直接输出动作块。
result: 在DeMaVLA设置下，与VLA策略在相同机器人、数据和指标上公平比较，验证了双系统框架的有效性。
conclusion: DSWAM通过双系统设计弥合了WAM的规划缺口，并为WAM-VLA公平比较提供了基准。
---

## 摘要
世界动作模型（WAMs）通过使用基于视频的世界建模作为机器人动作学习的密集监督，为视觉-语言-动作（VLA）策略提供了一种有前景的替代方案。现有的WAMs擅长物理上可落地的执行，但通常缺乏基于VLM的VLA中用于分解粗略指令的显式语言级规划接口。当家庭任务涉及复杂的多步目标时，这种分解变得重要，因为需要将粗略的用户指令转换为一系列细粒度的可执行子任务。同时，该领域仍然缺乏VLA与WAM执行能力之间公平的真实机器人比较，因为现有系统通常在数据、机器人本体和任务协议上存在差异。为了解决分解差距以及对受控WAM-VLA比较的需求，我们提出了DSWAM，一种面向细粒度机器人操作的双系统世界动作基础模型。DSWAM保留系统1的WAM执行器作为默认控制路径，并仅在任务分解有益时可选地激活系统2的视觉-语言子任务规划器。规划器从短期视觉历史和全局任务提示中预测可执行的子任务，而WAM执行器为每个指令或子任务执行面向世界的动作生成。执行器通过动作预测和视频协同训练进行训练，但推理时直接预测动作块，无需显式生成未来视频。为了使该执行路径在实际机器人上实用化，我们进一步集成了TensorRT加速、异步执行和实时分块（RTC），使得策略查询不会阻塞机器人控制。为了与VLA策略进行公平的真实机器人比较，我们在DeMaVLA真实世界可变形操作设置下构建并评估DSWAM，该设置具有匹配的机器人平台、预训练数据、后训练数据和评估标准。

## Abstract
World Action Models (WAMs) provide a promising alternative to Vision-Language-Action (VLA) policies by using video-based world modeling as dense supervision for robot action learning. Existing WAMs excel at physically grounded execution, but typically lack the explicit language-level planning interface in VLM-based VLAs for decomposing coarse instructions. Such decomposition becomes important when household tasks involve complex multi-step goals, where coarse user commands need to be converted into sequences of fine-grained executable subtasks. Meanwhile, the field still lacks a fair real-robot comparison between VLA and WAM execution capabilities, since existing systems often differ in data, robot embodiments, and task protocols. To address both the decomposition gap and the need for a controlled WAM-VLA comparison, we introduce DSWAM, a Dual-System World Action Foundation Model for fine-grained robot manipulation. DSWAM keeps a System 1 WAM executor as the default control path and optionally activates a System 2 vision-language subtask planner only when task decomposition is useful. The planner predicts executable subtasks from short-term visual history and a global task prompt, while the WAM executor performs world-aware action generation for each instruction or subtask. The executor is trained with action prediction and video co-training, but inference directly predicts action chunks without explicit future video generation. To make this execution path practical on real robots, we further integrate TensorRT acceleration, asynchronous execution, and real-time chunking (RTC) so that policy queries do not block robot control. To provide a fair real-robot comparison with VLA policies, we build and evaluate DSWAM under the DeMaVLA real-world deformable manipulation setting with matched robot platform, pretraining data, post-training data, and evaluation criteria.

---

## 论文详细总结（自动生成）

# DSWAM 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：世界动作模型（WAM）通过视频基础的世界建模为机器人动作学习提供密集监督，在接触丰富的操作任务（如抓取、放置、可变形物体操作）中表现优异，但现有 WAM 主要设计为执行策略，缺乏类似 VLM 的显式语言级规划接口，无法将粗粒度的用户指令分解为细粒度子任务序列。
- **核心问题**：
  - **分解缺口**：家庭任务（如“整理桌子”）需要将粗略指令分解为有序的可执行子任务，而 WAM 不具备这一能力。
  - **公平比较缺失**：现有 WAM 与 VLA 之间的实物对比常因数据来源、机器人本体、任务协议不同而混淆，难以确认性能差异源于模型设计还是实验设置。
- **论文目标**：提出 **DSWAM（双系统世界动作基础模型）**，通过解耦语义任务分解与物理执行，实现：
  - 保持 WAM 在物理执行上的优势；
  - 仅在需要时激活高层规划器；
  - 在匹配的 DeMaVLA 设置下与 VLA 进行公平的实物比较。

## 2. 论文提出的方法论

### 2.1 核心思想：双系统架构

- **System 1（WAM 执行器）**：默认控制路径，基于多视角 RGB、语言指令、本体感受，预测连续的动作块（action chunk）。训练时使用动作预测与视频协同训练（video co-training）提供世界建模监督，推理时直接预测动作块，不生成未来视频。
- **System 2（可选视觉-语言子任务规划器）**：基于 Rynnbrain4B 风格的 VLM，仅在指令需要分解时激活。输入短期视觉历史（最近 5 帧，1Hz）和全局任务提示，输出下一个可执行子任务指令。训练使用过渡感知的子任务监督（transition-aware subtask supervision）。

### 2.2 关键技术细节

- **System 1 执行器**：
  - 输入：多视角图像 $I_t^{1,2,3}$、语言条件 $\ell_t$、本体状态 $q_t$ → 多模态上下文 $O_t$。
  - 编码为潜在世界特征 $z_t = f_\theta(O_t)$。
  - 输出动作块 $A_t = [a_t, a_{t+1}, \dots, a_{t+H-1}]$，使用条件流匹配（conditional flow matching）生成连续动作。
  - 训练损失：动作流匹配损失 $L_{\text{act}}$ + 视频协同损失 $L_{\text{vid}}$（$\lambda_{\text{vid}}$ 平衡），未来视觉 token 提供世界建模监督，但动作 token 不能利用未来信息。
  - 推理时直接预测动作块，无需显式视频生成。

- **System 2 规划器**：
  - 输入：$X_t = \{x_{t-4}, x_{t-3}, \dots, x_t\}$（5 帧历史）+ 全局任务提示 $p$。
  - 输出子任务 $s_t = P_\phi(X_t, p)$。
  - 训练数据：从机器人轨迹视频中分割出子任务区间，构造过渡感知目标（窗口位于子任务内时输出当前子任务，位于边界时输出下一个子任务，终止窗口输出 `done`）。
  - 优化目标：自回归语言建模 $L_{\text{plan}}$。

- **高效部署**：
  - 推理时使用 **TensorRT** 加速（分离视觉上下文引擎与动作去噪引擎），**BF16** 模式延迟从 198.2ms 降至 73.8ms（2.69 倍加速）。
  - 异步执行 + **实时分块（RTC）**：策略查询与机器人控制解耦，避免阻塞。

### 2.3 算法流程（文字说明）

1. 初始指令 $p$ 传入系统。
2. 若 System 2 未激活，则 System 1 直接以 $p$ 为语言条件执行动作块预测；若激活，System 2 每隔 2s 根据当前视觉历史输出子任务 $s_t$，传递给 System 1。
3. 执行器对每个指令（或子任务）生成闭环动作块，机器人执行，同时缓存最近帧供下次规划器更新。

## 3. 实验设计

### 3.1 数据集与场景

- **仿真基准**：RoboTwin 2.0，包含 50 个双臂操作任务，分**干净设置**（固定初始配置）和**随机设置**（物体位姿与场景布局随机）。
- **实物基准**：DeMaVLA 家庭折叠协议，使用 ALOHA 风格双臂平台，4 种可变形物体（衬衫、裙子、裤子、毛巾），每种物体 2 个物理实例（easy + hard），每个实例 10 次尝试，共 20 次/类别。任务包括从篮子中取衣、铺平、折叠。
- **System 2 实物排序任务**：桌面整理任务，粗指令"Sort objects on the table"，分解为两个子任务（拾取玩具车放入盒子、拾取面包放在盘子）。比较原始粗指令监督与子任务级指令监督。

### 3.2 对比方法

- 仿真：π0、π0.5、DeMaVLA、Motus、Fast-WAM。
- 实物折叠：π0、DeMaVLA（DSWAM 以 WAM-only 模式评估，System 2 不激活，确保公平比较）。
- System 2 排序：仅与自身变体比较（不同训练步数和指令粒度）。

### 3.3 评估指标

- 仿真：成功率（SR，干净/随机各一个值）。
- 实物折叠：成功率 + 平均完成时间（失败试次用超时时间计入）。
- 排序：成功率 + 每次执行中的平均错误数。

## 4. 资源与算力

- 文中明确提到 TensorRT 加速在 **NVIDIA GeForce RTX 5090**（CUDA 12.9，TensorRT 10.16.1）上进行，单卡批大小 1 的延迟测试。
- **未明确说明**训练使用的 GPU 数量、总训练时长、总计算量等。仅提及系统 1 基于预训练的 Wan2.2-TI2V-5B 视频模型和流匹配，系统 2 基于 Rynnbrain4B 风格模型，但具体训练算力细节缺失。

## 5. 实验数量与充分性

- **仿真实验**：在 RoboTwin 2.0 的 50 个任务上报告了干净和随机两种设置下的成功率，共 100 个数值（但每个任务仅一个随机种子？未说明）。对比 5 种基线，覆盖了主流 VLA 和 WAM 方法。
- **实物折叠实验**：4 个类别 × 2 种难度 × 10 次 = 80 次实物试次（每类别 20 次）。与 π0 和 DeMaVLA 在完全匹配的设置下比较，确保了公平性。
- **System 2 消融**：仅在一个排序任务上进行了小规模实验（每个设置 14-18k 训练步？未明确试次数），显示子任务监督优势。
- **部署消融**：同步 TensorRT vs 异步 TensorRT+RTC 在衬衫和裤子的 easy 实例上各 10 次，共 40 次。
- **充分性评价**：仿真覆盖面较广（50 任务），实物聚焦于折叠操作，但任务类型相对单一（可变形物体）。System 2 验证仅在简单排序场景，缺乏更复杂多步任务的评估。总体实验设计较为系统，但**多任务泛化性**和**规划器通用性**有待加强。

## 6. 论文的主要结论与发现

1. **双系统设计有效**：DSWAM 在实物折叠中（WAM-only 模式）将平均成功率从 DeMaVLA 的 92.5% 提升至 **96.3%**，完成时间从 2 分 18 秒缩短至 **1 分 44 秒**，证明 WAM 执行器在匹配设置下优于 VLA。
2. **仿真泛化强**：在 RoboTwin 2.0 上，DSWAM 在干净和随机设置下分别达到 **92.38% 和 91.90%** 平均成功率，超越所有基线（包括 Fast-WAM 的 91.88%/91.78%）。
3. **可选规划器提升稳定性**：在排序任务中，子任务级指令监督将成功率从 75.7% 提升至 **100%**，每次试次错误从 3.53 降至 **0.65**。
4. **高效部署可行**：TensorRT BF16 加速 2.69 倍，异步 RTC 进一步减少执行时间且保持成功率。

## 7. 优点

- **创新双系统架构**：首次将 WAM 执行器与可选的高层规划器解耦，兼顾了 WAM 物理执行优势与 VLM 语义分解能力。
- **公平实物比较**：在 DeMaVLA 设置下匹配了机器人平台、预训练数据、后训练数据、任务协议和评价标准，使 WAM 与 VLA 的比较更具说服力。
- **高效推理设计**：采用视频协同训练但推理无需生成视频，结合 TensorRT 和异步 RTC，实现了低延迟实物控制。
- **实验设置严谨**：实物折叠中明确区分 easy/hard 实例，完成时间计入失败试次 timeout，避免偏差。

## 8. 不足与局限

- **任务覆盖有限**：实物实验仅限于可变形物体折叠（衣物类），未涉及其他常见操作（如刚性物体装配、工具使用等），泛化性尚未验证。
- **规划器验证不足**：System 2 仅在单一排序任务上测试，未在大规模多步场景（如整理厨房、组装家具）中进行评估，其有效性在更复杂任务中存疑。
- **训练算力信息缺失**：未报告训练过程的 GPU 数量、时长、能耗等，限制了可复现性和对计算成本的评估。
- **随机种子与统计显著性**：仿真实验未明确试次重复次数或置信区间，可能遗漏变异性分析；实物实验每组 10 次试次规模偏小，统计显著性未报告。
- **消融不够深入**：未对视频协同训练的权重 $\lambda_{\text{vid}}$ 进行敏感性分析，也未单独消融 TensorRT 各组件（如仅量化 vs 引擎拆分）的贡献。
- **与其他 WAM 的对比局限**：在实物中仅对比 VLA 方法（π0, DeMaVLA），未与同期的 WAM（如 Fast-WAM 或 GigaWorld-Policy）在实物上直接比较，削弱了“WAM 执行器的优势”这一论点的证据链。

（完）
