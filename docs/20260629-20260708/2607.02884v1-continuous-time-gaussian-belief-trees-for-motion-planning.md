---
title: Continuous-Time Gaussian Belief Trees for Motion Planning
title_zh: 连续时间高斯信念树用于运动规划
authors: "Rayan Mazouz, Qi Heng Ho, Zachary N. Sunberg, Morteza Lahijanian"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02884v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 连续时间高斯信念树用于运动规划
tldr: 针对连续时间随机系统运动规划中的过程与测量不确定性，本文提出一种基于高斯信念树的规划方法。通过混合信念传播模型，结合连续时间ODE演化与离散卡尔曼滤波更新，并引入信念障碍函数进行段级概率验证，确保整个轨迹段的安全性。在RRT和SST规划器中集成后，实验表明该方法在窄通道等场景下成功率高，能有效检测样本间违规，克服了离散时间方法的局限。其贡献在于为连续时间不确定性传播与安全约束提供了统一的原理性框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02884v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02884v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 834, \"height\": 1109, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02884v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 775, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02884v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 794, \"height\": 582, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02884v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 843, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02884v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 842, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02884v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1858, \"height\": 510, \"label\": \"Table\"}]"
motivation: 现有离散时间信念规划忽略样本间连续不确定性演化，导致窄通道等场景安全违规，需连续时间建模与段级验证。
method: 构建混合信念传播模型，测量间连续ODE更新，测量时刻卡尔曼滤波跳跃；设计信念障碍函数实现段级机会约束验证。
result: 在多个基准环境与RRT/SST集成，窄通道等任务成功率高，有效避免离散方法因样本间遗漏导致的违规。
conclusion: 提出连续时间信念规划框架，兼顾不确定性传播与安全验证，提升规划鲁棒性，可扩展至更复杂系统。
---

## 摘要
我们针对存在过程和测量不确定性的连续时间随机系统，提出了一种基于采样的运动规划方法，并提供安全性和性能的概率保证。机器人动力学建模为连续时间线性随机微分方程，而传感器测量在离散时间点到达。我们推导了一个离线的混合信念传播模型，其中信念在测量之间根据连续时间ODE演化，并在测量时刻经历离散的卡尔曼滤波更新跳跃。为确保安全性，我们引入了一种基于信念障碍函数的安全检查器，用于线段级概率验证。这使得规划器能够验证整个连续轨迹线段的安全性，并检测传统基于节点的检查所遗漏的样本间机会约束违反。这些组件共同为基于采样的信念规划提供了一个原则性框架，该框架兼顾了连续时间不确定性传播和连续时间安全性要求。我们将该方法与RRT和SST规划器集成，并在多个基准环境中进行了评估。结果表明，所提出的方法实现了高成功率和鲁棒的概率约束执行，包括在离散时间方法因遗漏样本间不安全行为而失效的狭窄通道场景中。

## Abstract
We address sampling-based motion planning for continuous-time stochastic systems under process and measurement uncertainty, with probabilistic guarantees on safety and performance. The robot dynamics are modeled as a continuous-time linear stochastic differential equation, while sensor measurements arrive at discrete time instants. We derive an offline hybrid belief propagation model in which the belief evolves according to continuous-time ODEs between measurements and undergoes discrete Kalman filter update jumps at measurement times. To ensure safety, we introduce a belief-barrier-function-based safety checker for segment-level probabilistic verification. This enables the planner to certify safety over entire continuous trajectory segments and detect inter-sample chance-constraint violations that are missed by conventional node-based checks. Together, these components provide a principled framework for sampling-based belief planning that accounts for both continuous-time uncertainty propagation and continuous-time safety requirements. We integrate the method with RRT and SST planners and evaluate it across multiple benchmark environments. The results show that the proposed method achieves high success rates and robust enforcement of chance constraints, including in narrow-passage scenarios where discrete-time counterparts fail due to missed inter-sample unsafe behavior.

---

## 论文详细总结（自动生成）

# 连续时间高斯信念树用于运动规划——论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：现实机器人系统在连续时间中运动，但传感器仅在离散时刻提供测量。传统的离散时间信念空间规划方法只在离散节点上评估安全性，忽略了节点之间连续时间区间内信念变化导致的潜在碰撞风险。例如，在狭窄通道环境中，机器人状态的不确定度在两个测量时刻之间可能增长，导致实际状态分布与障碍物重叠，而离散检查却认为安全。
- **研究动机**：现有基于采样的信念规划（如GBT）局限于离散时间动力学和测量，直接应用于连续时间系统会丧失安全性保证。简单地减小离散时间步长会增加计算负担，且传感器采样率受硬件限制。因此需要一种原则性方法，在连续时间框架下同时处理过程噪声、测量噪声和连续时间安全约束。
- **整体含义**：本文提出连续时间高斯信念树（CT-GBT）框架，通过混合信念传播模型（连续时间ODE+离散KF更新）和基于信念障碍函数的段级概率验证，实现鲁棒的安全运动规划，解决了离散时间方法在样本间遗漏不安全行为的根本缺陷。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 将信念在连续时间区间上的演化建模为混合过程：测量之间遵循连续时间ODE，测量时刻经历离散卡尔曼滤波更新。
- 利用信念障碍函数（Belief Barrier Function）将连续时间概率安全约束转化为对半空间约束的验证，并在整个轨迹段上检查，而非仅检查节点。

### 关键技术细节

**① 混合信念传播模型**  
- 系统动力学：线性SDE \( dx(t) = (Ax(t)+Bu(t))dt + G dw(t) \)，测量模型在离散时刻 \( t_k = k\Delta t \) 提供 \( z_k = C x_k + v_k \)。
- 在线执行时使用卡尔曼滤波+反馈控制器 \( u(t) = \bar{u}_k - K(\hat{x}_k(t)-\bar{x}_k(t)) \)。
- 离线规划时，期望信念 \( b(x(t)) = \mathcal{N}(\bar{x}(t), \Gamma(t)) \)，其中总协方差 \( \Gamma(t) = \Sigma(t) + \Lambda(t) \)（估计误差协方差+估计协方差）。
- 连续时间ODE（定理1）：
  - 均值：\( \dot{\bar{x}}(t) = A\bar{x}(t) + B \bar{u}(t) \)
  - 估计协方差：\( \dot{\Sigma}(t) = A\Sigma(t) + \Sigma(t)A^\top + GQG^\top \)
  - 估计协方差：\( \dot{\Lambda}(t) = (A-BK)\Lambda(t) + \Lambda(t)(A-BK)^\top \)
- 离散更新：在测量时刻，按照标准KF更新公式对 \( \Sigma(t_k^+) \) 和 \( \Lambda(t_k^+) \) 进行跳跃。

**② 连续时间概率碰撞检查（基于信念障碍函数）**  
- 状态约束：对每个障碍物构造分离半空间 \( \alpha^\top x \ge \gamma \)，计算概率满足公式（14）。定义信念障碍函数 \( h_j^x(b(t)) = \alpha^\top \bar{x} - \gamma - \text{erf}^{-1}(1-2\delta_j)\sqrt{2\alpha^\top(\Sigma+\Lambda)\alpha} \)。
- 控制约束：由于控制输入依赖于状态估计，其协方差为 \( K\Lambda K^\top \)，类似地定义 \( h_j^u(b(t)) \)（命题2）。
- 风险分配：利用Boole不等式均匀分配风险预算 \( \delta_j = P_{\text{safe}}/M \)。
- 段级充分条件（定理2）：若所有障碍函数在时间区间内满足 \( \dot{h} + h \ge 0 \)，则整个区间安全。实际中使用均匀网格点检查，网格分辨率 \( \delta_t \ll \Delta t \)。

**③ 算法流程（算法1：GBT-RRT）**  
- 初始化树，根节点为初始信念。
- 循环：采样随机信念 → 最近邻 → 采样持续时间和控制输入 → 根据连续时间ODE传播信念 → 对整段轨迹执行isCollisionFreeBelief检查（基于障碍函数）→ 若安全则加入树。
- 分支定界：SST版本使用稀疏化策略保持多样性。

### 理论保证
- 正确性引理（Lemma 1）：若算法返回解，则满足连续时间机会约束。
- 概率完备性定理（Theorem 3）：若存在有效轨迹，随着迭代次数趋于无穷，找到解的概率趋于1。

## 3. 实验设计

- **系统模型**：
  - 2D单积分器（状态 \( x \in \mathbb{R}^2 \)），输入 \( u \in [-10,10]^2 \) 或 \( [-1,1]^2 \)。
  - 4D双积分器（位置+速度），输入为加速度 \( u \in [-10,10]^2 \)。
- **环境（Environments）**：共6个基准环境（Env 1-6），范围 \( [0,50]^2 \)，障碍物布局从简单到复杂，包括无障碍、多障碍、窄通道等（图2展示轨迹）。
- **基准方法**：对比离散时间（DT）和连续时间（CT）版本的GBT，分别集成RRT和SST规划器。共4种变体：CT-RRT, DT-RRT, CT-SST, DT-SST。
- **评价指标**：
  - 规划时间（秒）
  - 轨迹成本（通常为控制代价或路径长度）
  - 成功率（到达目标且无碰撞的概率）
  - 机会约束满足概率

## 4. 资源与算力

- 论文明确提及计算硬件为“3.9 GHz 8-Core CPU and 128 GB of memory”。未提及GPU型号或数量，所有实验均在CPU上完成。
- 未报告训练时长或具体算力成本。由于算法是采样规划，每次迭代为单线程，未使用并行加速。

## 5. 实验数量与充分性

- **实验数量**：
  - 表I：每个环境/每种算法进行100次模拟，报告平均时间和成本。
  - 表II和III：基于5000次蒙特卡洛（MC）运行（每个环境生成100条轨迹，每条轨迹执行5000次roll-out？实际描述：生成100个运动计划，每个roll-out沿轨迹碰撞检查，重复5000次MC运行）。样本量较大，统计显著。
  - 图3：比较控制限 \( U = [-1,1]^2 \) 下的时间和成本。
- **充分性评估**：
  - 覆盖了多个障碍物复杂度（从简单到窄通道）、两种动力学系统（2D和4D）、两种规划器（RRT和SST）、两种时间离散化策略（DT vs CT）。
  - 额外进行了控制约束严格化（小控制限）的对比，进一步展示CT优势。
  - 缺少消融实验（例如，单独去掉连续时间传播或段级检查的效果）。但整体对比DT已能说明连续时间组件必要性。
- **公平性**：DT和CT使用相同的采样策略、环境参数和风险阈值 \( P_{\text{safe}}=0.1 \)，对比相对公平。但DT版本未调整时间步长优化精度，可能处于劣势。

## 6. 主要结论与发现

- 连续时间GBT（CT-GBT）在所有环境中成功率接近1（≥0.94），而离散时间GBT（DT-GBT）在复杂环境中成功率急剧下降（如窄通道Env 6，DT-RRT仅0.06）。
- 机会约束满足概率：CT版本维持在0.91以上，DT版本在复杂环境降至0.00。
- 规划时间：DT通常稍快（因无段级密集检查），但CT在控制限较小（\( U = [-1,1]^2 \)）时反而更快且成本更低，表明缩小离散时间步长不能等价替代连续时间建模。
- 连续时间方法能检测到样本间违反，而离散方法完全遗漏，导致实际执行时碰撞。

## 7. 优点（方法或实验设计亮点）

- **方法亮点**：
  - 首次将信念空间规划扩展到连续时间，推导了混合传播ODE（定理1），理论严谨。
  - 引入信念障碍函数进行连续时间段级验证，避免离散节点检查的盲区，计算效率高（闭式梯度）。
  - 将控制约束纳入概率框架（命题2），覆盖完整控制系统。
  - 保留原算法的概率完备性和正确性。
- **实验设计亮点**：
  - 使用多种环境（6个）和两种动力学系统，增强泛化性。
  - 5000次MC统计结果显示鲁棒性。
  - 控制限缩减实验揭示了DT时间步长细化的局限性。

## 8. 不足与局限

- **实验覆盖**：
  - 仅测试线性高斯系统，未涉及非线性、非高斯噪声或部分可观测性更复杂的情况。
  - 障碍物均为凸多面体，非凸或动态障碍未考虑。
  - 未与其它不确定性规划方法（如FIRM、信念路线图）对比。
- **偏差风险**：
  - DT版本使用固定时间步长，未针对性能优化（如自适应离散），可能放大CT优势。
  - 风险分配采用均匀分配，可能过于保守，未探索优化分配策略。
- **应用限制**：
  - 离线规划假设测量频率、噪声协方差已知，实际中参数可能变化。
  - 网格密集检查 \( \delta_t \ll \Delta t \) 增加计算开销，虽可控制但未给出具体复杂度。
  - 未考虑实时在线重规划。
- **理论方面**：
  - 段级充分条件（定理2）依赖比较引理，但实际用网格近似，严格连续时间证书需已知Lipschitz常数，文中未实现。
  - 概率完备性依赖于控制输入离散化假设，未讨论较宽松控制空间情形。

（完）
