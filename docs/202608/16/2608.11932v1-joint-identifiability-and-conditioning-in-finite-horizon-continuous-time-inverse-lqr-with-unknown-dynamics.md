---
title: Joint Identifiability and Conditioning in Finite-Horizon Continuous-Time Inverse LQR with Unknown Dynamics
title_zh: 有限时域连续时间逆LQR中未知动力学的联合可辨识性与条件性
authors: "Meiling Yu, Yuan-Hua Ni, Lei Jiang"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11932v1"
tags: ["query:control"]
score: 9.0
evidence: 连续时间逆LQR的可辨识性与良态性分析
tldr: "逆最优控制旨在从专家轨迹推断成本函数，本文针对有限时域连续时间逆LQR，考虑系统矩阵与二次成本均未知。利用有限时域引起的时变最优增益作为内生激励，提出三个可计算条件指数刻画状态丰富性、增益变化丰富性与成本算子注入性，建立联合可辨识条件。基于此设计条件感知的采样数据重构方法，从噪声测量中重构增益与闭环动力学，闭式恢复(A,B)并通过凸半定规划识别二次权重。理论保证恢复真实参数而非行为等价替代，给出非渐近扰动界与一致性，实验验证了诊断价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11932v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 925, \"height\": 246, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11932v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1653, \"height\": 1131, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11932v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1654, \"height\": 735, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11932v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1649, \"height\": 1145, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.11932v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1649, \"height\": 1087, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.11932v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 416, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.11932v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 941, \"height\": 375, \"label\": \"Table\"}]"
motivation: 有限时域逆LQR中系统矩阵与成本未知时，现有工作缺乏联合可辨识性刻画与真实恢复保证。
method: 利用时变增益内生激励，提出三个条件指数刻画可辨识性，并设计条件感知的采样数据重构与凸半定成本识别方法。
result: "建立联合可辨识条件，保证恢复真实(A,B)与成本权重，给出非渐近扰动界与一致性。"
conclusion: 提供有限时域逆LQR可辨识性量化工具与鲁棒重构算法，为实际应用提供理论保障。
---

## 摘要
逆最优控制（IOC）旨在从观测到的智能体专家行为中推断其潜在的代价泛函。本文研究在系统矩阵和二次代价均未知的情况下，基于闭环状态-输入轨迹的有限时域连续时间逆LQR问题。有限时域引入了时变最优增益，而这种内源性激励作为结构机制使得联合恢复成为可能。我们通过三个可计算的条件性指标来量化该机制，这些指标衡量状态丰富度、增益变化丰富度以及结构化代价算子的单射性。利用这些指标，我们为所考虑的逆问题建立了联合可辨识性条件。关键在于，这些条件保证恢复真实系统矩阵$(A,B)$和真实代价权重矩阵，而不仅仅是行为等价的替代物。我们还开发了一种条件性感知的采样数据重构方法，该方法从含噪测量中重构增益$K(\cdot)$和闭环动力学矩阵$A_c(\cdot)$，以闭式形式恢复$(A,B)$，并通过凸半定规划辨识二次权重。我们进一步建立了在次高斯观测噪声下的非渐近扰动界以及完整重构方法的一致性，其显式依赖于相同的条件性指标。数值实验支持了该理论，并展示了条件性指标的诊断价值。

## Abstract
Inverse Optimal Control (IOC) aims to infer the underlying cost functional of an agent from observations of its expert behavior. This paper studies the finite-horizon continuous-time inverse LQR problem from closed-loop state--input trajectories, where both the system matrices and the quadratic cost are unknown. The finite horizon induces a time-varying optimal gain, and this endogenous excitation serves as the structural mechanism that makes joint recovery possible. We quantify this mechanism through three computable conditioning indices, which measure state richness, gain-variation richness, and injectivity of a structured cost operator. Using these indices, we establish joint identifiability conditions for the inverse problem considered here. Crucially, these conditions guarantee recovery of the ground-truth system matrices $(A,B)$ and the true cost weighting matrices, rather than merely a behaviorally equivalent surrogate. We also develop a conditioning-aware sampled-data reconstruction method that reconstructs the gain $K(\cdot)$ and the closed-loop dynamics matrix $A_c(\cdot)$ from noisy measurements, recovers $(A,B)$ in closed form, and identifies the quadratic weights through a convex semidefinite program. We further establish the non-asymptotic perturbation bounds and the consistency of the full reconstruction method under sub-Gaussian observation noise, with explicit dependence on the same conditioning indices. Numerical experiments support the theory and illustrate the diagnostic value of the conditioning indices.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：逆最优控制（IOC）的目标是从观测到的专家行为中推断其背后的代价函数。传统逆LQR方法大多假设系统动力学已知，但在实际应用中，系统模型往往不确定。本文研究的是**系统矩阵与二次代价均未知**的有限时域连续时间逆LQR问题的联合可辨识性与重构。
- **核心问题**：当系统动力学(A, B)和代价权重(Q, R, H)都未知时，仅从闭环状态-输入轨迹能否**唯一恢复真实参数**，而非仅恢复行为等价的替代参数？
- **关键洞见**：有限时域Riccati终端条件导致最优增益K(t)是**时变的**。这种由终端条件诱导的增益变化作为内源性激励，提供了分离恒定开环矩阵(A, B)与随时间变化的闭环矩阵A_c(t) = A − BK(t)所需的结构性信息。这一机制使得联合恢复成为可能。
- **整体意义**：论文将联合可辨识性条件、条件性诊断指标、采样数据重构算法、非渐近误差传播分析统一在同一框架内，为有限时域连续时间逆LQR的未知动力学场景提供了完整的理论基础和算法实现，填补了该领域系统性端到端分析的空白。

## 2. 论文提出的方法论

### 2.1 核心思想

- **三大条件性指标**：将逆问题分解为三个机制，分别由三个可计算的指标刻画：
  - **c_X（状态丰富度）**：c_X := inf_t λ_min(X(t)X(t)ᵀ)，衡量状态轨迹的丰富性，控制从数据中恢复K(·)和A_c(·)的能力；
  - **c_AB（增益变化丰富度）**：c_AB := λ_min(G_K)，其中G_K = ∫₀ᵀ ΔK(t)ΔK(t)ᵀdt为增益变化Gramian，衡量增益时间变化的丰富性，控制从闭环中分离(A, B)的能力；
  - **c_QRH（成本算子单射性）**：结构化平稳残差算子M_α在归一化差分空间V_α上的受限最小增益，控制成本权重恢复的注入性。

### 2.2 联合可辨识性理论

- **增益恢复**：由U(t) = −K⋆(t)X(t)，当c_X > 0时（即rank(X(0)) = n），K⋆(t)可由K⋆(t) = −U(t)X(t)ᵀ(X(t)X(t)ᵀ)⁻¹唯一确定；
- **动力学分离**（定理3.3）：c_AB > 0当且仅当(A⋆, B⋆)是满足A_c(t) = A − BK(t)的唯一常数对。B通过增益变化交叉Gramian闭式恢复，A由平均化恒等式获得；
- **成本识别**（定理3.7）：c_QRH > 0保证F_α,m单点集，即归一化结构化成本唯一可辨识；当Q⋆ ≻ 0时该条件也是必要的；
- **联合可辨识定理**（定理3.8）：在假设2.1和2.2下，c_AB > 0且c_QRH > 0共同保证真实参数元组Θ⋆全局可辨识。

### 2.3 采样数据重构算法（CR-IOC）

算法流程分为五个阶段：
1. **时间去噪**：使用局部平均平滑器（带宽BW）对含噪采样状态/输入进行去噪；
2. **增益重构**：在每个采样节点用最小二乘估计K̂_k；
3. **闭环动力学重构**：使用Richardson外推偏差消除技术（结合半窗口与全窗口增量）避免直接数值微分，重构Â_c(t_k)；
4. **动力学分离**：通过离散化增益变化Gramian Ĝ_K和交叉项Ĉ_AK，闭式恢复(B̂, Â)；
5. **成本恢复**：通过基响应叠加构造结构化Lyapunov方程的线性表示，组装加权残差矩阵M̂_L，求解凸半定规划(SDP)：min ‖M̂_Lθ‖² s.t. Q ⪰ 0, R ⪰ εI, tr(R) = m。

### 2.4 扰动分析与一致性

- 建立了端到端的非渐近扰动界，误差通过时间去噪 → K恢复 → A_c重构 → (A,B)分离 → 成本恢复逐级传播，每级由对应条件性指标控制；
- 在次高斯观测噪声、采样网格细化和适当参数选择下，证明了估计量(Â, B̂, Q̂, R̂, Ĥ)的一致性。

## 3. 实验设计

### 3.1 基准系统

- **主基准**：两质量弹簧-阻尼系统（n=4, m=2），具有明确的物理意义，状态为[q₁, q₂, q̇₁, q̇₂]ᵀ，输入为[u₁, u₂]ᵀ；
- **中规模扩展**：六质量链系统（n=12, m=4），驱动器置于质量块1、3、5、6上，用于检验方法在更大规模上的表现；
- **稠密4×2基准**：随机生成一次并固定的稠密系统，不继承质量-弹簧动力学的稀疏性和对称性，用于评估闭环重构。

### 3.2 对比方法

- **CR-IOC**（本文方法）；
- **SysID+IOC**（匹配基线）：与CR-IOC共享相同的去噪步骤、增益重构和成本恢复SDP，唯一差异在于动力学阶段不使用有限时域增益变化结构，而是通过最小二乘拟合并联模型ẋ = Ax + Bu。

### 3.3 主要实验组

1. **可恢复性扫描**（图2）：两个基准系统 × {N ∈ 30, 100, 300, 1000} × {SNR ∈ ∞, 40, 30, 20 dB}；
2. **参数敏感性**（表1、表2）：变化增量窗口Δ和去噪带宽BW；
3. **基线对比**（图3）：六质量链，N=1200，SNR ∈ {10, 15, 20, 30, 40} dB；
4. **固定稠密4×2基准**的闭环重构评估（表3、图4）；
5. **条件性诊断研究**（图5）：三种设置R1（可恢复）、R2（联合病态）、R3（仅成本阶段退化）。

## 4. 资源与算力

- **论文未提及任何具体算力信息**，包括GPU型号、数量、训练时长等。
- 实验均为小规模仿真（状态维度n=4~12，轨迹数N≤1200），所用SDP通过CVXPY建模、Clarabel求解器求解，属于轻量级优化计算，对计算资源需求有限。
- 从运行时间看，稠密4×2基准中CR-IOC中位运行时间为0.38秒，SysID+IOC为0.27秒，说明整体计算开销较低。

## 5. 实验数量与充分性

- **总体实验量较为充分**：
  - 两种规模基准的可恢复性扫描共约40个条件组合，每个条件30次蒙特卡洛试验；
  - 参数敏感性表2张，覆盖多个窗口/带宽取值；
  - 一组5个SNR水平的基线对比；
  - 一个固定稠密基准的30次试验闭环评估；
  - 一组3种设置的条件性诊断研究。
- **优点**：每个实验都报告了中位数和四分位距，体现了统计稳健性；参数敏感性测试覆盖了两个主要调参维度；条件性诊断实验设计巧妙，能分别触发动力学和成本阶段的退化。
- **可改进之处**：缺乏与其他现有IOC方法（如[16, 20]）的直接对比；缺乏欠驱动（m < n）情况下潜在势整形模糊性的专门实验验证；缺少对非高斯噪声（如重尾分布）的鲁棒性测试。

## 6. 论文的主要结论与发现

- **联合可辨识性成立**：在归一化tr(R) = m和H = αQ条件下，c_AB > 0且c_QRH > 0保证真实参数元组的全局可辨识；当Q⋆ ≻ 0时c_QRH > 0为必要条件；
- **有限时域增益变化是核心信息源**：正是K(t)的时间变化使得从闭环轨迹中分离恒定的(A, B)成为可能，这与无限时域场景（增益恒定）形成关键对比；
- **条件性指标具有诊断价值**：三个指标分别对应重构各级的敏感性，指标趋零时预测精度快速恶化；实验显示R2中ĉ_QRH数值为零导致全流程失败，R3中仅ĉ_QRH退化则仅成本阶段受损；
- **CR-IOC优于SysID+IOC**：在六质量链上，20dB时动力学误差降低约54%，成本误差降低约65%；在稠密4×2基准上，闭环NMSE从1.22×10⁻¹降至1.20×10⁻³，改进约两个数量级；
- **参数改进转化为行为改进**：更准确的(A,B)和(Q,R)恢复带来了显著更优的闭环轨迹重建。

## 7. 优点

- **理论-算法-实验的完整闭环**：从可辨识性条件出发，将条件性指标嵌入算法设计各阶段，扰动界显式依赖同一指标集，实验验证各环节的理论预测；
- **端到端非渐近扰动分析**：提供了从含噪采样到最终成本恢复的完整误差传播链，每级放大因子由对应指标控制，这是现有IOC文献中尚未系统建立的；
- **无需求解数值微分**：Richardson偏差消除技术避免了直接数值微分的病态性，且理论上具有明确的∆²偏差阶和η_x/∆噪声放大阶；
- **条件性指标可计算且具实用价值**：经验指标(ĉ_X, ĉ_AB, ĉ_QRH)直接从数据计算，可用于实验设计的数据质量筛选、视界选择、数值正则化，实验R2/R3展示了其识别阶段失效的诊断能力；
- **基线设计合理**：SysID+IOC在共享所有其他模块的前提下仅替换动力学阶段，有效隔离了有限时域增益变化结构的贡献；
- **符号与表述严谨**：所有定理均有完整证明，非渐近界中所有常数均有明确表达式。

## 8. 不足与局限

- **假设限制较强**：
  - 需要已知比例α使得H = αQ；若α未知，可辨识性条件需另行分析；
  - 需要全状态观测和全输入观测，未覆盖部分观测/输出反馈场景；
  - 仅限于确定性LTI系统，不含过程噪声；实验同样限于无过程噪声的确定性动力学；
  - 需要rank(X(0)) = n的满秩初始条件。
- **欠驱动下成本非唯一性**：论文虽指出m < n时存在势整形(potential-shaping)模糊性并通过H = αQ参数化规避，但未在实验中专门验证该参数化如何消除该模糊性；
- **实验规模有限**：最大系统为n=12，未涉及更高维度；实验均为仿真，未使用真实机器人或物理系统数据；
- **缺乏与其他方法的直接对比**：未与[16]的3DIOC、[20]的凸数据驱动IOC等方法在相同基准上比较，消融基线（SysID+IOC）实际上只是省去了有限时域增益变化信息，可视为更贴近于在无限时域或离散上下文中的做法；
- **调参依赖**：算法需要选择带宽BW、增量窗口Δ、SDP边际ε等参数，虽然敏感性实验显示结果对参数不过度敏感，但缺少自动调参策略；
- **rollout分析缺失**：对闭环NMSE的改进缺乏理论界的直接支撑，作者也承认该指标未被定理直接约束。

（完）
