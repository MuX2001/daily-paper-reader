---
title: "Scalable Gaussian Process Regression via Deterministic Trigonometric Features: Uniform Bounds for Safe Model Predictive Control"
title_zh: 基于确定性三角特征的可扩展高斯过程回归：用于安全模型预测控制的统一界
authors: "Julius Jagdt, Johanna Menn, Sebastian Trimpe, Melanie N. Zeilinger, Anna Scampicchio"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16415v1"
tags: ["query:control"]
score: 9.0
evidence: 面向安全模型预测控制的可扩展高斯过程回归（均匀不确定性界）
tldr: 基于高斯过程（GP）的模型预测控制在存在模型失配时能实现安全控制，但高概率安全保证需要全域一致的不确定性边界，且现有结果仅适用于精确GP，在大数据下计算不可行。本文提出一种确定性的三角特征高斯过程（DTF-GP），利用离散三角特征将GP回归转化为特征空间中的贝叶斯线性回归，从而获得可扩展的核近似。作者推导了该模型的高概率一致不确定性边界，并给出平方指数核情形的闭式解。将该模型集成到学习型MPC中，在保持安全保证与探索性能的同时显著提升计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 761, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16415v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16415v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1795, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16415v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 824, \"height\": 756, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 250, \"label\": \"Table\"}]"
motivation: 现有GP回归的均匀不确定性边界在大数据下不可扩展，导致安全MPC难以用于大规模数据场景。
method: 提出确定性的三角特征高斯过程DTF-GP，用有限维离散三角函数近似核函数，将GP回归转化为特征空间贝叶斯线性回归并推导均匀边界。
result: 推导出DTF-GP的闭式均匀不确定界，在安全MPC中计算效率远高于完整GP，且安全保证与探索性能相当。
conclusion: DTF-GP在保持高概率安全保证的前提下实现可扩展的GP回归，为大规模学习型MPC提供实用方案。
---

## 摘要
基于高斯过程（GP）的学习型模型预测控制（MPC）是在存在模型失配时实现安全控制的有效方法。高概率安全保证通常需要在整个状态-输入域上一致成立的不确定性界，但现有界仅适用于完整的高斯过程回归。由于精确的高斯过程推理随数据点数量扩展性较差，在大数据场景下部署不切实际。我们通过开发一种可扩展的高斯过程框架来弥补这一差距，该框架允许推导统一的不确定性界。我们形式化了一种确定性三角特征高斯过程（DTF-GP），这是一种基于离散化三角特征的有限维核近似，将高斯过程回归简化为特征空间中的贝叶斯线性回归。我们为所提出的DTF-GP推导了一个高概率统一不确定性界，并给出了其在平方指数核情况下的闭式解。最后，我们将DTF-GP集成到基于学习的MPC方案中，并证明其在提供高概率安全保证和与完整GP相当的探索性能的同时，在大数据场景下提高了计算效率。

## Abstract
Learning-based Model Predictive Control (MPC) using Gaussian processes (GPs) is an effective approach for safe control in the presence of model mismatch. High-probability safety guarantees typically require uncertainty bounds that hold uniformly over the entire state--input domain, but existing bounds are available only for full GP regression. Since exact GP inference scales poorly with the number of data points, its deployment is impractical in large-data regimes. We close this gap by developing a scalable GP framework that admits the derivation of uniform uncertainty bounds. We formalize a deterministic trigonometric feature Gaussian process (DTF-GP), a finite-dimensional kernel approximation based on discretized trigonometric features that reduces GP regression to Bayesian linear regression in feature space. We derive a high-probability uniform uncertainty bound for the proposed DTF-GP and provide its closed-form solution for the squared-exponential kernel case. Finally, we integrate the DTF-GP into a learning-based MPC scheme and demonstrate that it provides high-probability safety guarantees and exploration performance comparable to a full GP while improving computational efficiency in large-data regimes.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：学习型模型预测控制（MPC）借助高斯过程（GP）对系统模型失配进行建模，并提供不确定性量化，是应对模型误差的有效方法。然而，安全关键型 MPC 要求不确定性界在整个状态–输入域上**均匀成立**，现有这类高概率界仅适用于**完整 GP 回归**。
- **核心矛盾**：完整 GP 精确推理的计算复杂度为 \(O(N^3)\)（\(N\) 为数据点数），在大数据场景下不可用；而现有可扩展 GP 近似方法（如诱导点、专家混合、数据子集、随机傅里叶特征、稀疏谱 GP 等）虽然提升了计算效率，却**均未提供均匀不确定性界**，因此无法直接用于需要安全保证的 MPC 框架。
- **本文意义**：提出**确定性三角特征高斯过程（DTF-GP）**，用有限维三角特征近似核函数，将 GP 回归转化为特征空间贝叶斯线性回归，在保持高概率安全保证的前提下，实现了可扩展的 GP 推理，弥补了可扩展 GP 与安全 MPC 之间的缺口。

## 2. 论文提出的方法论

- **核心思想**：基于 Bochner 定理，将平稳核表示为连续三角特征的加权积分；通过确定性均匀频率网格上的离散化，将无限维核截断为有限维特征映射，从而将 GP 回归转化为有限维贝叶斯线性回归。
- **DTF-GP 特征构造**：
  - 在半个格点 \(\mathbb{Z}_+^d\) 上取一组频率 \(\{\omega^{(s)}\}\)，每个频率对应一对余弦/正弦基函数，加上常数项构成特征映射 \(\phi(z)\in\mathbb{R}^M\)（\(M=2Q+1\)）。
  - 核近似为 \(\tilde{k}_{\mathrm{DTF}}(z,z')=\phi(z)^\top\phi(z')\)，后验均值与方差由 (4)、(5) 给出，复杂度从 \(O(N^3)\) 降为 \(O(M^3)\)。
- **谱参数化与频率选择**：
  - 对平方指数（RBF）核，其谱密度为高斯形，因此权重取 \(\lambda_q=C\exp(-q^\top\tilde{D}q)\)，其中 \(C\) 对应先验方差，\(\tilde{D}\) 控制谱衰减率，相当于长度尺度。
  - 频率按椭球区域 \(q^\top\tilde{D}q\le r^2\) 截断，保留谱质量最大的分量。
- **均匀不确定性界**（定理 2）：
  - 假设未知函数属于 DTF 核对应的无穷维 RKHS 且范数有界 \(B\)，观测噪声为条件 R-次高斯。
  - 误差分解为噪声项 + 投影误差项，借助自归一化浓度不等式，得到对任意 \(N\ge1\)、所有 \(z\in Z\) 一致成立的界：
    \[
    |g(z)-\mu_N(z)| \le |g(z)-Pg(z)| + \sigma_N(z)\beta_N,
    \]
    其中 \(\beta_N\) 包含与完整 GP 类似的信息增益项，另含投影残差项。
- **RBF 核投影误差界**（命题 4）：给出截断频率导致的投影误差上界，仅依赖 \(B\)、\(C\)、\(\tilde{D}\) 和截断半径 \(r\)，可通过一维积分数值计算，因此 DTF-GP 的置信界所需假设与完整 GP 一致。
- **集成 SafeMPC**：
  - 将 DTF-GP 的置信界替代完整 GP 界，用于 SafeMPC 的不确定集传播，得到高概率安全保证（推论 6）。
  - 目标函数使用后验标准差的负和进行“安全探索”。
  - 实现中采用线性核 + RBF 核组合，线性部分精确表示，投影误差仅来自 RBF 分量。
  - 在线更新通过秩一更新加速（每样本 \(O(M^2)\)），预测每测试点仅 \(O(M)\)。

## 3. 实验设计

- **场景**：倒立摆系统（inverted pendulum），模型为 \(ml^2\ddot{\vartheta}=mgl\sin(\vartheta)+u\)，状态 \(x=[\dot{\vartheta},\vartheta]^\top\)，输入 \(u\)。
- **名义模型**：围绕竖直平衡点的线性化，质量参数略有摄动（\(m_{\mathrm{nom}}=0.149\,\mathrm{kg}\)），从而产生需要学习的模型失配。
- **数据集**：在紧致域 \(Z=[-3,3]\times[-1.25,1.25]\times[-1,1]\) 上生成初始安全样本，动态探索使用 \(N_{\mathrm{init}}=\{50,100,200,400\}\) 等不同初始样本数；静态探索使用 \(N_{\mathrm{init}}\) 从 10 到 1000 不等。
- **Benchmark / 对比方法**：
  - 完整 GP（full GP） + SafeMPC，使用式 (20) 的置信界；
  - DTF-GP + SafeMPC，使用定理 2 的置信界和命题 4 的投影误差界。
  - 两种模型使用相同的核、数据、超参数学习方法，仅核表示不同。
- **评估指标**：
  - **探索性能**：动态探索中累积互信息（mutual information），衡量收集数据对未知模型的降不确定能力。
  - **计算效率**：静态探索中每个 SafeMPC 迭代的平均计算时间，分解为 GP 在线更新时间和 MPC 求解时间（含 GP 预测）。
  - 代表性轨迹可视化（图 2）。
- **实验设置**：
  - 置信水平 \(1-\delta=0.95\)；
  - 动态探索平均 50 个随机种子，静态探索平均 100 个随机种子；
  - 实现采用 NumPy（CPU），非 GPU。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。
- 代码基于纯 NumPy 实现，运行在 CPU 上，符合算法对比目的（公平比较 DTF-GP 与完整 GP）。
- 由于未提供具体硬件配置和时长，无法量化绝对算力消耗；但从复杂度分析看，DTF-GP 在 \(N\gg M\) 时节省大量计算。

## 5. 实验数量与充分性

- **实验数量**：
  - 动态探索：50 个随机种子的平均值；
  - 静态探索：100 个随机种子的平均值；
  - 多种初始数据规模（\(N_{\mathrm{init}}\) 从 10 到 1000）下的对比；
  - 图表包括轨迹图、互信息曲线、计算时间曲线、置信界宽度分解图（图 1，100 seeds）。
- **充分性评估**：
  - 实验覆盖了探索性能、计算效率、不同数据规模，主流维度较全面；
  - 多种子平均保证了统计可靠性；
  - 对比公平：除核表示外其他设置一致；
  - 但仅有一个倒立摆任务，未涉及更高维系统或实际硬件，泛化性验证有限。

## 6. 论文的主要结论与发现

- **理论层面**：首次为可扩展 GP（基于确定性三角特征）提供了高概率统一不确定性界，并给出 RBF 核下闭式投影误差界，使 DTF-GP 的安全保证假设与完整 GP 相同。
- **算法层面**：DTF-GP 将 GP 推理转换为有限维贝叶斯线性回归，边际似然计算复杂度从 \(O(N^3)\) 降为 \(O(M^3)\)，在线更新从 \(O(N^2)\) 降为 \(O(M^2)\)，预测从 \(O(N)\) 降为 \(O(M)\)。
- **实验层面**：
  - 在倒立摆安全探索任务中，DTF-GP 与完整 GP 的累积互信息几乎一致，即探索性能相当；
  - 计算时间几乎不随 \(N_{\mathrm{init}}\) 增长，而完整 GP 随数据增多显著变慢；在大数据区（如 \(N_{\mathrm{init}}\ge500\)）DTF-GP 优势明显；
  - 通过调整截断半径控制投影误差，可平衡保守性与计算代价。

## 7. 优点

- **理论贡献**：填补了可扩展 GP 无均匀不确定性界的空白，为安全学习控制提供了理论支撑。
- **确定性方法**：与随机频率（RFF）或优化频率（稀疏谱 GP）不同，频率由确定性网格选取，便于理论分析。
- **闭式可计算界**：RBF 核下投影误差界可通过数值积分快速评估，实际可用性强。
- **计算复杂度透明**：给出完整的复杂度对比表，清晰展示大数据场景下的优势。
- **公平实验设置**：与完整 GP 使用相同数据、核、超参数训练流程，仅更换核表示，对比客观。

## 8. 不足与局限

- **核类型限制**：详细理论推导主要针对 RBF 核（谱参数化和投影误差界），虽然作者指出原则上可推广到其他平稳核，但未给出具体结果。
- **周期性假设**：DTF 核本身是周期性的，对非周期函数引入隐式周期外推误差；作者用附录证明该误差随周期与域距离指数衰减，但在实际应用中需谨慎选择周期。
- **RKHS 范数界需求**：置信界仍依赖未知函数的 RKHS 范数上界 \(B\)，该估计问题本身具有挑战性（文中引用其他文献，但未解决）。
- **实验覆盖有限**：仅单个倒立摆仿真，未测试更高维系统、真实机器人、非平稳环境或噪声分布偏离次高斯假设的场景。
- **计算优势依赖 \(M\)**：特征数 \(M\) 由投影误差目标决定，若目标过小，\(M\) 可能接近或超过 \(N\)，计算优势消失；文中指出可通过固定 \(M\) 换取稳定运行时间，但保守性波动。
- **资源信息缺失**：未报告具体硬件、运行时间绝对数值，读者难以精确复现绝对性能，只能参考相对比较。

（完）
