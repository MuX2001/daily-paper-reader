---
title: Operator-based data embedding for data-driven control of continuous-time systems from noisy data
title_zh: 基于算子的数据嵌入：面向含噪数据连续时间系统的数据驱动控制
authors: Masashi Wakaiki
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17518v1"
tags: ["query:control"]
score: 9.0
evidence: 基于噪声数据的数据驱动H∞鲁棒控制设计（连续时间系统）
tldr: 针对连续时间系统，提出一种从含噪采样数据直接设计状态反馈增益的数据驱动方法。通过算子式数据嵌入刻画与噪声数据一致的所有系统，给出数据信息性的充要条件，转化为线性矩阵不等式求解镇定、H2与H∞控制器。同时推导连续时间信号重构误差的上界，为直接利用含噪离散数据设计控制器奠定基础。
source: arxiv
selection_source: fresh_fetch
motivation: 现有数据驱动控制多面向离散时间或忽略噪声，难以在连续时间系统中从含噪采样数据可靠设计反馈增益。
method: 利用算子式数据嵌入表征与含噪数据一致的系统集合，将数据信息性条件转化为LMI，并估计连续时间信号重构误差。
result: 获得了数据信息性的充要条件，基于LMI解可直接构造镇定、H2和H∞状态反馈增益，且给出重构误差界。
conclusion: 该方法为连续时间系统从含噪数据直接设计鲁棒控制器提供了可验证的理论保证与实用求解路径。
---

## 摘要
我们提出了一种数据驱动方法，用于设计实现连续时间系统镇定、$H_2$控制和$H_\infty$控制的状态反馈增益。假设状态-输入数据受到过程噪声、测量噪声和输入扰动的污染。我们首先利用基于算子的数据嵌入来刻画与含噪数据一致的所有系统集合。这种刻画给出了在某一类噪声下数据信息性的充分必要条件。这些条件被表述为线性矩阵不等式，并由此构造反馈增益。为了实现直接从含噪采样数据对连续时间系统进行控制器设计，我们还获得了连续时间信号重建误差的上界。

## Abstract
We propose a data-driven method for designing state-feedback gains that achieve stabilization, $H_2$-control, and $H_\infty$-control for continuous-time systems. The state-input data are assumed to be corrupted by process noise, measurement noise, and input disturbances. We first characterize the set of all systems consistent with the noisy data using operator-based data embedding. This characterization yields necessary and sufficient conditions for data informativity under a certain class of noise. These conditions are formulated as linear matrix inequalities, and the feedback gains are constructed from their solutions. To enable direct controller design from noisy sampled data for continuous-time systems, we also obtain an upper bound on the reconstruction error of continuous-time signals.

---

## 论文详细总结（自动生成）

# 《基于算子的数据嵌入：面向含噪数据连续时间系统的数据驱动控制》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

该论文面向**连续时间线性系统**的数据驱动控制问题，核心动机是：现有数据驱动控制研究大多针对离散时间系统，即使处理连续时间系统，也普遍存在以下限制：需要导数测量、数据离散化造成信息丢失、或依赖滤波方法导致设计条件受滤波参数影响。论文试图在**存在过程噪声、测量噪声与输入扰动**的情况下，直接从连续时间状态-输入数据（甚至含噪采样数据）出发，设计状态反馈增益，实现镇定、$H_2$控制和 $H_\infty$控制。其理论核心是**数据信息性**（data informativity）：刻画与观测数据一致的所有系统集合，并保证所设计的控制器对该集合中所有系统均有效。

## 2. 方法论：核心思想、关键技术细节

### 核心思想：合成算子数据嵌入

论文不采用离散化或滤波，而是将连续时间信号（状态、输入、噪声）嵌入到 **$H_{10}$ 空间上的合成算子**中：

- 对每个轨迹定义合成算子 $F$ 与微分合成算子 $F_d$，它们将 $H_{10}$ 中的测试函数映射到 $\mathbb{R}^n$；
- 将状态-输入数据整体嵌入为算子 $Z = [X_d^\top, -X^\top, -U^\top]^\top$，噪声嵌入为 $W = [P^\top, -Q^\top, -R^\top]^\top$；
- 由于合成算子是有限秩的，$ZZ^\*$ 与 $WW^\*$ 表现为具体的 Gramian 矩阵，可实际计算。

### 关键技术细节与核心定理

- **一致性刻画（引理 2.1）**：数据满足微分方程（式 (2)）等价于算子方程 $[I_n\ A\ B](Z - W) = 0$。
- **噪声类定义（式 (8)）**：$W W^\* \preceq \Theta$，用噪声强度矩阵 $\Theta$ 约束噪声能量。
- **主定理（定理 2.2）**：在 $\Theta_{11} - \Theta_{12}\Theta_{22}^+\Theta_{12}^\top \succ 0$ 条件下，系统 $(A,B)$ 属于数据一致集合 $\Sigma_{D,\Theta}$ 当且仅当满足二次矩阵不等式（QMI）：

$$
[I_n\ A\ B]^\top(\Theta - ZZ^*)[I_n\ A\ B] \succeq 0
$$

其证明难点在于必要性：需要构造出具有合成算子结构的噪声 $W$ 使 $S_\Theta = W$ 成立。作者利用 Douglas 引理构造算子 $S$，并通过引理 2.3（核空间条件等价于 Schur 补正定）绕过结构约束。

- **LMI 充要条件**：在 Assumption 3.1（数据真实性、持续激励条件 $\Theta_{22} \prec \sum(X_kX_k^\* + X_kU_k^\* + U_kX_k^\* + U_kU_k^\*)$、技术条件 $\Theta_{11} - \Theta_{12}\Theta_{22}^+\Theta_{12}^\top \succ 0$）下，利用严格矩阵 S 引理，将 QMI 分别转化为：
  - **定理 3.4（镇定）**：式 (47) 的 LMI；
  - **定理 3.5（$H_2$）**：式 (57)–(59) 的 LMI 组；
  - **定理 3.6（$H_\infty$）**：式 (61) 的 LMI。
  
  求解 LMI 后取 $K = L\Phi^{-1}$ 即得反馈增益。

- **信号重构误差（第 4 节）**：对采样数据构造分段线性插值 $\hat{x}$ 或 $\tilde{x}$，在 Wirtinger 不等式和 $\|x'\|_{L^2} \le M\|x\|_{L^2} + c$ 假设下，给出重构误差 Gramian 的上界（命题 4.2、定理 4.6），从而将重构误差视为测量噪声并入 $\Theta$。附录 A 还给出了合成算子的范数与噪声频率分布之间的关系。

## 3. 实验设计

论文的数值验证部分（第 5 节）是**单一仿真示例**，具体设置如下：

- **场景**：线性化飞机模型（A0, B0），在 184 m/s 真空速工作点附近；状态包含俯仰速率、空速、攻角、俯仰角；输入为升降舵偏角和推力（缩放 $10^5$）。
- **数据生成**：生成 30 条独立状态-输入轨迹，时长 [0, 0.4]，采样周期 $h = 3.125 \times 10^{-4}$；输入为多正弦叠加信号；状态受过程噪声（白噪声，PSD $10^{-7}$）和输入扰动（低通滤波白噪声）影响；状态采样附加均匀分布测量噪声，分量上界约 $10^{-4}$ 量级。
- **方法流程**：从含噪采样数据重构状态 $\tilde{x}_k$，并输出输入 $u_k$；基于定理 4.6 和 Remark 4.1 构造噪声强度矩阵 $\Theta$；代入定理 3.6 的 LMI 进行求解。
- **对比基准**：仅与**无控制（$K = 0$）时 $H_\infty$ 范数 $\|G_K\|_{H_\infty} = 68.99$** 对比。论文没有与其他数据驱动方法（如滤波方法、正交多项式方法、广义采样方法）进行数值对比。

## 4. 资源与算力

论文**未明确提及任何算力信息**。文中只在第 5.3 节提到求解 LMI 使用的软件环境为 MATLAB R2026a + YALMIP + MOSEK，未涉及 GPU 型号、数量或训练时长等信息。由于该示例仅涉及规模很小的 LMI（变量维度约 4 维状态、2 维输入），计算开销很低。

## 5. 实验数量与充分性

- **实验数量**：仅一个数值示例（飞机模型），没有消融实验、没有参数敏感性分析、没有多种噪声水平对比、也没有多个不同系统的验证。
- **充分性与客观性评价**：从控制理论论文的标准看，该示例的主要作用是**展示理论框架的可行性和计算可实现性**，而非系统性验证算法性能。实验充分性偏低：缺少与现有方法（如滤波方法、正交多项式基、广义采样）的公平对比，缺少对 Assumption 3.1 中参数（M, c, σ, sδ）选择影响的讨论，也缺少对采样间隔 $h$ 变化时控制性能变化的分析。不过，作者在引言中已明确将“现有滤波方法的条件依赖滤波参数”作为其方法优势，在实验中却未对两种方法的性能进行对比，这是一个可见的缺憾。

## 6. 主要结论与发现

- **三点理论贡献**：
  1. 在同时存在过程噪声、测量噪声和输入扰动时，用含合成算子的 QMI 对与数据一致的系统集合给出**充要表征**；
  2. 基于严格矩阵 S 引理，分别建立镇定、$H_2$、$H_\infty$ 控制的数据信息性 LMI **充要条件**，并可由 LMI 解直接构造反馈增益；
  3. 从合成算子角度给出采样重构误差的上界，使方法可直接应用于含噪采样数据。
- **数值结论**：对飞机模型示例，用 30 条含噪轨迹即可通过 LMI 求得反馈增益，$H_\infty$ 性能从无控制时的 68.99 改善到 γ = 1.35。
- **核心理论意义**：QMI 必要性证明中处理合成算子结构约束的方法是全文的技术亮点，也是相比仅含过程噪声情形（[32]）的关键推广。

## 7. 优点

- **理论与基础坚实**：提供的是数据信息性的**充要条件**，而非仅充分条件，理论完备性高。
- **不需导数测量**：避免了实际中难以获得可靠导数信号的困难。
- **不依赖滤波/离散化参数**：直接嵌入原始连续时间数据进行分析，设计条件与滤波参数无关。
- **统一处理多种控制目标**：在同框架下覆盖镇定、$H_2$ 与 $H_\infty$，并支持外部信号加权矩阵的灵活选取（Remark 3.3、3.5）。
- **从含噪采样到控制器的完整闭环**：重构误差分析（第 4 节）与信息性框架结合，为从实际传感器采样数据到控制器设计提供了完整的理论依据。
- **QMI 必要性证明中的算子构造方法**（Step 1–Step 4）具有方法论上的推广价值。

## 8. 不足与局限

- **数值实验薄弱**：仅有单一飞机模型示例，没有与现有方法（滤波方法、正交多项式、广义采样）的对比，也没有消融研究和参数敏感性分析，实验说服力有限。
- **噪声类约束的保守性**：噪声用 $\Theta$ 作为 Gramian 上界刻画，形式简洁但可能限制更能反映噪声频域结构的刻画；附录 A 虽给出频域解释，但没有直接用于数据一致性分析。
- **状态测量假设**：方法需要状态-输入数据，不能直接用于纯输入-输出数据情形（作者在结论中已明确列为未来工作）。
- **参数选择的工程可用性**：重构误差界依赖常数 $M$、$c$、噪声上界 $s_\delta$ 和 $\sigma$，实际中这些量依赖对系统的先验知识，论文对这些参数选取不当的后果讨论不足。
- **采样噪声与采样间隔的权衡未充分讨论**：虽然定理 4.6 给出了三角不等式形式的上界，但关于采样间隔趋近于零时噪声放大效应的定量分析有待深化。

（完）
