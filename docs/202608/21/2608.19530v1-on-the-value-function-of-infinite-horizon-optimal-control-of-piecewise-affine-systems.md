---
title: On the Value Function of Infinite-Horizon Optimal Control of Piecewise Affine Systems
title_zh: 分段仿射系统无限时域最优控制的值函数研究
authors: "Francesco Cordiano, Kanghui He, Bart De Schutter"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19530v1"
tags: ["query:control"]
score: 9.0
evidence: 无限时域最优控制值函数结构的理论分析
tldr: 本文研究分段仿射(PWA)系统在ℓ1或ℓ∞阶段代价下无限时域最优控制的值函数结构。现有结论仅保证值函数为PWA，但未讨论其仿射片数量；本文构造显式反例表明值函数可能具有无穷多仿射片，并非proper PWA。随后提出易于验证的充分条件，确保值函数在紧集上只有有限个仿射片，即proper PWA。这一结果补充了线性二次型特例，并为PWA系统的学习控制方案提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.19530v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 507, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19530v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 783, \"height\": 309, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19530v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 696, \"height\": 430, \"label\": \"Figure\"}]"
motivation: 现有结果仅证明值函数为PWA，但未分析其是否为有限仿射片的proper PWA，本文旨在填补这一理论空白。
method: 通过构造显式反例展示无穷片可能性，并提出易于验证的充分条件保证值函数为proper PWA。
result: 证明值函数可能非proper，但满足充分条件时在紧集上具有有限仿射片，补充了线性二次情形。
conclusion: 为PWA系统的学习控制方案提供理论支撑，深化了对无限时域最优控制值函数结构的理解。
---

## 摘要
本文研究具有ℓ1或ℓ∞阶段代价的分段仿射（PWA）系统在约束无限时间最优控制（CITOC）问题中值函数的结构。现有工作（如文献[1]）已证明所得值函数在状态上是PWA的。然而，现有结果并未分析值函数是否为合适（proper）的PWA函数，即在紧集上具有有限个仿射片，或者仿射片的数量是否可能无限。我们通过一个显式例子证明后一种情况确实可能发生，该例子也有助于建立严格且易于验证的充分条件，确保所得值函数为合适的PWA函数。我们的理论发现补充了众所周知的结论（如线性二次情形），并为近期基于学习的PWA系统控制方案提供支持。本文通过数值例子对所提出的结果进行了说明。

## Abstract
In this paper, we study the structure of the value function in constrained infinite-time optimal control (CITOC) problems of piecewise affine (PWA) systems, with $\ell_1$ or $\ell_\infty$ stage cost. Existing works, such as [1], establish that the resulting value function is PWA in the state. However, existing results do not analyze whether the value function is a proper PWA function, i.e., with a finite number of affine pieces over compact sets, or whether the number of pieces can be infinite. We show that the latter case is indeed possible by means of an explicit example, which is also instrumental in establishing rigorous and easily verifiable sufficient conditions that ensure that the resulting value function is a proper PWA function. Our theoretical findings complement well-known results, e.g., the linear-quadratic case, and serve as support for recent learning-based control schemes for PWA systems. Throughout the paper, the proposed results are illustrated by means of a numerical example.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机：** 分段仿射（PWA）系统是混合动力系统中的重要一类，能有效建模具有混合动态的系统（如储能系统）或近似非线性系统。在约束无限时间最优控制（CITOC）问题中，值函数的结构对于动态规划、模型预测控制以及近年兴起的基于学习的控制算法都有直接影响。例如，已有研究依赖"值函数为PWA"这一结构性质来设计PWA神经网络逼近器；文献[1]（Baotić et al., 2006）虽已证明值函数是PWA的，但并未澄清一个关键问题：该值函数是否满足通常意义下对PWA函数的要求——即在紧集上仅有**有限个仿射片（proper PWA）**。

**核心问题：** 本文直接回答了上述问题：CITOC问题的值函数虽然按文献[1]是PWA的，但它的仿射片数量可能是**无穷的**，因此未必是通常定义下的proper PWA函数。作者通过构造显式反例证明这一结论，并进一步提出了保证值函数为proper PWA的充分条件。

**整体意义：** 该研究填补了文献中关于无限时域PWA最优控制值函数理论结构的一个空白，纠正或补充了以往可能被简化的理解（PWA ≠ 有限个仿射片），为基于PWA结构假设的学习控制方案提供了严格的理论基础，增强了此类方法在理论上的可靠性。

## 2. 论文提出的方法论

论文采用**理论证明与反例构造相结合**的方法，整体思路如下：

### (1) 反例构造（核心贡献之一）

- 研究一类简单的**二维线性系统**（线性系统是PWA系统的特例）：

  \[
  A = \begin{pmatrix} a_1 & a_2 \\ 0 & a_1 \end{pmatrix}, \quad B = I_2
  \]

  其中 \(a_1 \in (-1,1)\) 且 \(a_1 \neq 0\)，系统满足可控性、稳定性等优良性质。

- 定义 \(\ell_1\) 阶段代价：\(g(x,u) = |x_1| + |x_2| + R_1|u_1| + R_2|u_2|\)。

- 通过定义系数 \(c_k, d_k, r_{1,k}, r_{2,k}\) 的递推关系（式(11)-(12)），在满足 \(R_i \geq r_{i,\infty}\) 的条件下，证明最优策略为 \(\mu^*(x) = 0\)，且值函数的闭式解可解析求出（命题 1）。

- 选取 \(a_1=0.8, a_2=1\)，得到 \(R_1=5, R_2=31\)，值函数在紧集 \(X=[-10,10]\times[-2,2]\) 上被下述无穷多超平面切割出**无限多个仿射区域**：

  \[
  0.8x_1 + kx_2 = 0, \quad \forall k \in \mathbb{Z}_{\ge 1}
  \]

  这些超平面全部穿过原点，从而在紧集内产生无穷多仿射片，且每个区域内斜率不同——值函数既不属于proper PWA，也非光滑函数。

### (2) 充分条件（核心贡献之二）

为保证值函数为有限片数的proper PWA，论文提出两个关键假设：

- **假设3（局部单步可控性）**：在原点所在的多面体区域 \(D_j\) 的某个邻域 \(\mathcal{E}\) 内，对任意状态 \(x\)，都存在输入 \(u\) 使得一步转移到原点（即 \(A_j x + B_j u = 0\)）。

- **假设4（输入权重与动态矩阵的匹配条件）**：存在矩阵 \(M\) 满足 \(R = M^\top Q B_j\) 且 \(\|M\|_q \le 1\)，其中 \(\|\cdot\|_q\) 是 \(\|\cdot\|_p\) 的对偶范数。

在此基础上，论文通过两个辅助引理完成主要证明：

- **引理1**：在假设3、4下，存在原点的邻域 \(\Omega_r\)，使得在\(\Omega_r\)内的最优一步控制能将该邻域内的任意状态一步驱动到原点。
- **引理2**：对于任意初始状态 \(x_0\) 和任意 \(r>0\)，存在有限时间步 \(\bar{N}(x_0, r)\)，使得最优状态轨迹在该时刻进入 \(\Omega_r\)。证明基于反证法和值函数有限性的矛盾。

- **命题2（主定理）**：在假设1–4下，对于任意 \(C \ge 0\)，值函数 \(J_\infty^*(x)\) 和最优策略 \(\mu_\infty^*(x)\) 在集合 \(X_C := \{x : J_\infty^*(x) \le C\}\) 的任意多面体子集上是proper PWA函数。证明要点：在 \(\Omega_r\) 内通过归纳法证明DP迭代两步收敛；在 \(\Omega_r\) 外利用引理2保证有限步内进入 \(\Omega_r\)，从而整体只有有限个仿射片。

## 3. 实验设计

本文为**理论性论文**，不涉及大规模数据集或标准benchmark对比，所有实验以**数值示例**的形式辅助验证理论结论，共包含三类场景：

1. **反例演示**：取 \(a_1=0.8, a_2=1\)，\(X=[-10,10]\times[-2,2]\)，\(R_1=5, R_2=31\)。以3000次DP迭代逼近值函数，通过图1和图2展示值函数在紧集上的分区随迭代不断增加，分区超平面穿越原点，说明无限多仿射片的存在。

2. **充分条件的数值验证**：取 \(Q=B=I_2\)，状态空间 \(X=[-0.25,0.25]^2\)，在假设4中选择 \(M \in \{0.9I_2, I_2, 1.1I_2\}\) 三种情形，通过网格化求解优化问题(15)并追踪满足一步到原点的区域，绘制相应的 \(\Omega_r\)（图3）。结果表明 \(M=0.9I_2\) 时\(\Omega_r\)为整个状态空间 \(X\)，\(M=I_2\) 时\(\Omega_r\) 为更小区域，\(M=1.1I_2\) 时\(\Omega_r\) 为空集，与命题2的理论预测一致。

3. **边界情形讨论**：在违反假设4（\(M=1.1I_2\)）的情况下，选取状态空间为更窄的 \(X=[-0.25,0.25]\times[-0.05,0.05]\)，通过数值验证发现DP迭代在3步后即收敛，值函数仍为PWA。这说明假设4是充分但非必要的，值函数可为PWA而DP迭代不需恰好两步终止。

## 4. 资源与算力

论文**未提及**任何关于GPU型号、数量、训练时长或分布式计算资源的信息。这符合理论类论文的通常做法——数值实验仅涉及低维小规模优化问题（二维状态空间的网格化求解和3000次DP迭代），在普通计算机上即可完成，无需大规模算力。

## 5. 实验数量与充分性

**实验数量：** 共3组数值验证场景，对应论文三个主要论点（反例存在性、充分条件有效性、充分条件非必要性）。

**充分性评估：**
- 反例演示有充分的解析支持（附录V-B给出了值函数的显式表达式），数值图仅作直观展示，证据充分。
- 充分条件的验证示例较简洁——仅取了一个特定系统和一个维度的参数变化（\(M\) 的三种取值），未做多个系统参数组合的系统性检验。
- 但鉴于本文属于理论分析型论文，核心结论由定理保证，数值实验定位为"说明性验证"而非"实证评估"，因此实验设计是**合理且自洽**的。
- 在验证充分条件时，\(\Omega_r\) 通过网格化近似确定，未提供网格密度或误差分析，在严谨性上略有不充分感，但不影响理论结论。

## 6. 论文的主要结论与发现

1. **重要反例**：存在具有稳定性和可控性的简单线性（因而也是PWA的）系统，其CITOC值函数在紧集上具有**无限多个仿射片**，因此并非通常意义下的proper PWA函数。这推翻了文献[1]定理IV.9可能给人"值函数自动为有限片PWA"的隐含印象。

2. **核心理论补充**：仅靠系统的可控性和稳定性**不足以**保证值函数为有限片PWA；值函数结构还与代价矩阵 \(R\) 的设计密切相关。当输入权重过大时，最优策略可能选择指数衰减而不触碰原点，导致无限多仿射片。

3. **充分条件**：在假设3（局部单步可控性）和假设4（输入代价与动态矩阵匹配，即 \(R = M^\top Q B_j\) 且 \(\|M\|_q \le 1\)）下，值函数和最优策略在任意 \(X_C\) 的多面体子集上为proper PWA函数。直观地说，当输入代价"不太大"时，最优策略在原点附近两步内将状态驱动到原点，从而DP迭代两步收敛。

4. **充分非必要**：所提充分条件并非最小必要条件——存在违反假设4但仍得到有限片PWA值函数的例子（DP迭代三步收敛的情形）。

## 7. 优点

- **问题切入精准**：准确识别了现有文献中一个被忽视的理论缺口——PWA值函数与"有限仿射片"之间的区别，具有较高的理论价值。
- **反例构造巧妙且具启发性**：仅利用一个简单的二维线性系统（具备稳定性和可控性）即揭示了值函数结构的复杂行为，展示清晰，使读者能直观理解无限仿射片产生的机理。
- **理论证明严谨完整**：引理1利用凸优化的次梯度条件证明局部单步最优性，引理2利用值函数有限性保证有限时间进入局部邻域，主定理采用归纳与反证结合的证明方式，逻辑链条完整。
- **条件易于验证**：假设3和假设4均为可直接检验的代数/几何条件，便于工程应用。
- **与学习控制方法形成互补**：为PWA神经网络的逼近质量提供了理论依据；附录中给出了值函数显式表达式，便于复现。

## 8. 不足与局限

- **充分条件过强且非必要**：论文仅提出了充分条件而未给出必要条件的完整刻画；假设4中的匹配条件 \(R = M^\top QB_j\) 形式较为特殊，实际系统中可能较难精确满足。
- **反例依赖特定参数**：反例的构造依赖于 \(a_1=0.8\)、\(a_2=1\) 等特定参数和 \(\ell_1\) 代价；对于更一般的 \(\ell_p\) 代价或更复杂的PWA动力学的结论，论文未做系统性推广分析。
- **\(\Omega_r\) 的显式刻画缺失**：命题2中 \(\Omega_r\) 的存在性由连续性保证，但未给出其大小的定量估计或构造方法，实际应用中需通过数值手段近似确定，不利于直接部署。
- **数值验证规模有限**：仅用单一系统参数组合验证理论，未覆盖高维情况；作为说明性示例已足够，但实证支撑强度有限。
- **未讨论随机PWA系统的推广**：文中仅在结论部分提出未来工作将向随机PWA系统扩展，当前给出的充分条件是否适用于随机情形仍然未知。
- **对工程应用的指导有限**：虽然论文支撑学习控制方法的理论合理性，但未提供关于如何在实践中设计 \(R\) 或验证假设条件的具体算法或工程指南。

（完）
