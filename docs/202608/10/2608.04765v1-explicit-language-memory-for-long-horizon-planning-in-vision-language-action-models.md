---
title: Explicit Language Memory for Long-Horizon Planning in Vision-Language-Action Models
title_zh: 面向长时程规划的视觉-语言-动作模型的显式语言记忆
authors: "Houze Xu, Jizhong Li, Ziyi Ye"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04765v1"
tags: ["query:vla"]
score: 9.0
evidence: 为VLA模型引入显式语言记忆以支持长时程规划，是VLA模型的核心进展。
tldr: 针对VLA模型在长时程任务中存在的专家示范稀疏导致泛化不足、非马尔可夫性破坏时序一致性、误差累积难以闭环纠正、微调弱化语义表征等问题，提出一种带有显式语言记忆模块的层次化VLA架构。该方法将离散时序观测转化为带时序逻辑的文本记忆序列，由高层VLM基于记忆递归更新子任务指令，低层VLA执行连续控制。实验结果表明，显式语言记忆显著提高了复杂长时程任务的成功率和鲁棒性，在仿真和真实机器人上均验证有效，并提供了可解释的语义决策依据。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 711}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1542, \"height\": 715}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 765}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1597, \"height\": 1048}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1330, \"height\": 211}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 395}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 210}]"
motivation: VLA模型在长时程任务中面临专家示范稀疏、时序不一致、误差累积及语义表征弱化等挑战。
method: 提出层次化VLA架构，引入显式语言记忆模块，将时序观测转化为文本记忆，由高层VLM递归更新子任务指令，低层VLA执行控制。
result: 在多个仿真环境和真实机器人上验证，显式语言记忆提升了复杂长时程任务的成功率和鲁棒性，并提供可解释决策。
conclusion: 显式语言记忆为长时程VLA规划提供了有效的时序追踪与动态纠错机制，增强了可解释性与任务成功率。
---

## 摘要
视觉-语言-动作（VLA）模型为连接视觉感知、语言理解和机器人控制提供了统一范式。然而，现有VLA模型在长时程任务中仍面临主要挑战：稀疏的专家演示限制了跨任务的组合泛化；长时程任务的非马尔可夫性质使得仅以当前观测为条件的策略难以保持时间一致性；有限的闭环错误校正使执行误差不断累积；端到端的动作微调可能削弱视觉语言模型（VLM）骨干的高级语义表示。针对这些问题，我们提出了一种具有显式语言记忆模块的分层长时程VLA架构。核心思想是将离散的时间观测转换为具有时间逻辑的连贯文本记忆序列。该系统被解耦为高层VLM和低层VLA：高层VLM通过视觉问答训练范式进行语义推理，而低层VLA根据子任务指令和视觉观测执行精确的连续控制。高层VLM以先前的记忆作为上下文锚点，递归更新语言记忆和子任务指令，从而在长时程执行过程中实现持续的时间跟踪和动态校正。我们在多个模拟环境中评估了所提出方法，并在真实机器人平台上进行了Sim-to-Real实验。结果表明，显式语言记忆提高了VLA模型在复杂长时程任务上的成功率和鲁棒性，同时为决策过程提供了可解释的语义说明。

## Abstract
Vision-language-action (VLA) models provide a unified paradigm for connecting visual perception, language understanding, and robotic control. However, existing VLA models still face major challenges in long-horizon tasks: sparse expert demonstrations constrain cross-task compositional generalization; the non-Markovian nature of long-horizon tasks makes it difficult for policies conditioned only on current observations to maintain temporal consistency; limited closed-loop error correction allows execution errors to accumulate; and end-to-end action fine-tuning may weaken the high-level semantic representations of vision-language model (VLM) backbones. To address these issues, we propose a hierarchical long-horizon VLA architecture with an explicit language-memory module. The central idea is to convert discrete temporal observations into a coherent textual memory sequence with temporal logic. The system is decoupled into a high-level VLM and a low-level VLA: the high-level VLM performs semantic reasoning through a visual question answering training paradigm, while the low-level VLA executes precise continuous control conditioned on subtask instructions and visual observations. The high-level VLM recursively updates both language memory and subtask instructions using the previous memory as a contextual anchor, enabling persistent temporal tracking and dynamic correction during long-horizon execution. We evaluate the proposed method in multiple simulation environments and conduct sim-to-real experiments on a real robotic platform. The results demonstrate that explicit language memory improves the success rate and robustness of VLA models on complex long-horizon tasks while providing an interpretable semantic account of the decision process.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：视觉-语言-动作（VLA）模型为机器人控制提供了统一范式，但在长时程任务中面临四大挑战：
  - **稀疏专家演示**限制了跨任务的组合泛化能力；
  - **非马尔可夫性质**使仅依赖当前观测的策略难以保持时间一致性（同一观测在不同历史状态下可能对应不同决策）；
  - **闭环纠错能力有限**，执行误差沿序列不断累积；
  - **端到端动作微调**可能削弱 VLM 骨干的高层语义表示。
- **核心含义**：论文提出一种层次化长时程 VLA 架构，通过显式语言记忆模块将离散时序观测转化为连贯的文本记忆序列，使策略能够递归追踪任务状态、保持时间一致性并支持动态纠错，为长时程机器人决策提供紧凑且可解释的解决方案。

## 2. 论文提出的方法论

- **核心思想**：将长时程策略解耦为高层 VLM（语义决策）与低层 VLA（连续动作生成）两个独立模块。高层 VLM 以全局指令、当前观测和上一轮语言记忆为输入，递归更新记忆并输出简洁子任务；低层 VLA 以当前观测和子任务为条件，预测固定视界（H=30）的连续动作块。
- **策略分解形式**：
  - π(a_{t:t+H}, s_t, m_t | o_t, m_{t-1}, l) = π_LL(a_{t:t+H} | o_t, s_t) · π_HL(s_t, m_t | o_t, m_{t-1}, l)
- **递归语言记忆机制**：
  - m_t 为结构化自然语言总结，包含已完成里程碑（过去时）、决策相关的对象属性状态、观察到的失败信息及下一个待办意图；
  - 采用**滚动压缩规则**：子任务未完成时保留目标细节（类别、颜色、位置）；完成后压缩为简短过去时陈述，注意力转移到下一阶段；
  - 仅当当前观测提供里程碑阶段变化的证据时，高层模型才更新记忆。
- **自动化监督标注管线**：
  - 利用专家视频和技能级标注（技能名称、描述、起止时间戳），将视频帧与活动技能索引对齐；
  - 通用 VLM（Gemini 3.5 Flash API 或本地 Qwen3.5-9B）基于当前帧和完整技能标注，生成“子任务（动作+目标对象+上下文）”和“第一人称语言记忆（过去时总结+未来导向过渡句）”；
  - 生成结果作为高层策略的监督目标。
- **独立分支训练**：
  - 高层使用 PaliGemma，自回归预测记忆和子任务，目标函数为 L_HL = −E[log π^m_HL(m_t | o_t, m_{t-1}, l) + log π^s_HL(s_t | o_t, m_{t-1}, l)]；
  - 低层遵循 π0.5 的动作专家，采用 flow-matching 动作目标（论文中以简化的 MSE 形式给出 L_LL）；
  - 两分支独立训练，避免在单一端到端目标中手动平衡语义生成与连续控制的损失。
- **闭环推理流程**：
  - 初始化 m_0 = ∅ → 获取观测 o_t → 更新 (m_t, s_t) ← π_HL(o_t, l, m_{t-1}) → 生成动作块 a_{t:t+H} ← π_LL(o_t, s_t) → 执行 → 根据记忆状态判定是否终止；
  - 输出记忆和子任务均为可见文本，可形成可解释的决策日志。

## 3. 实验设计

- **仿真场景 1：BEHAVIOR-1K“打开收音机”任务**
  - 指令：打开客厅桌子上收音机；需完成寻找接近、抓取、按下按钮、放置到桌面四个阶段（MoveTo、PickUp、PressOn、PlaceOn）。
- **仿真场景 2：Genie Sim 3.0 人形操作平台**
  - 单包裹任务：追踪包裹、抓取、放置扫描区、条形码朝上、重新抓取、放入蓝色箱子、回到初始朝向（6个计分阶段）；
  - 连续包裹任务：按任意顺序分拣4个不同颜色包裹，每个包裹成功直立放置计入一个阶段。
- **真实机器人场景：XLeRobot 平台**
  - 双臂 SO-ARM 101 机械臂 + LeKiwi 移动底盘；任务为从桌上抓取积木并放到盘子中（Follow、PickUp、MoveTo、Place 四阶段）；
  - 数据集包含25条人工遥操作轨迹，评估20次试验。
- **对比方法**：
  - 基线：原生 π0.5 策略；
  - 提出方法：π0.5 + 显式语言记忆（高层 PaliGemma VLM）。
- **评测指标**：阶段级成功率（Stage-wise Success Rate），即每个阶段完成与否的平均值，比末端任务成功率更细粒度。

## 4. 资源与算力

- **训练资源**：4 张 NVIDIA A100 GPU（训练和微调），推理在 1 张 NVIDIA RTX 5880 Ada Generation GPU 上完成；
- **模型配置**：动作块长度 H=30；高层 PaliGemma 与低层 VLA 的 VLM 骨干采用 LoRA 适配；300M 参数的 Gemma 动作专家全量微调；
- **优化器与超参数**：AdamW，warmup + 余弦学习率衰减，峰值学习率 2.5×10⁻⁵，batch size 64，训练步数 50,000；
- **其他**：论文未明确报告总训练时长（小时数/天数）。

## 5. 实验数量与充分性

- **实验覆盖**：3 个 benchmark（两个仿真 + 一个真实机器人），其中 Genie Sim 包含单包裹和连续包裹两个任务变体；每个 benchmark 均对比了基线与提出方法。
- **实验量级**：BEHAVIOR-1K 和 Genie Sim 未报告评估轨迹数量；真实机器人仅 25 条训练轨迹、20 次评估试验。
- **充分性评价**：
  - **优点**：覆盖家庭仿真、人形操作仿真、真实低成本机器人，场景多样性较好；阶段级指标能定位具体失效环节。
  - **不足**：缺少传统消融实验（如记忆模块有无、不同记忆表示对比）；未报告随机种子方差或置信区间，改善的统计稳定性未验证；真实场景仅限短任务，不足以支撑分钟级复杂任务的结论。

## 6. 论文的主要结论与发现

- **整体性能提升**：
  - BEHAVIOR-1K 开启收音机：平均阶段成功率从 30.0% 提升到 40.0%，其中 PickUp 从 10% 提升到 30%，PressOn 从 0% 提升到 10%；MoveTo 二者均饱和。
  - Genie Sim 单包裹分拣：平均成功率从 41.7% 提升至 63.9%；Follow 和 PickUp-1 从 50% 翻倍到 100%，Inside-1 从 50% 提升到 83.3%。
  - Genie Sim 连续分拣：平均成功率从 31.3% 提升至 46.9%，基线从未完成第 3/4 个包裹直立放置，而记忆模型在 12.5% 的评估中完成了全部四个。
  - 真实 XLeRobot 抓取放置：从 62.5% 提升至 66.3%，提升较小（任务阶段少，历史信息帮助有限）。
- **阶段一致性与跳过步骤减少**：定性日志显示，基线在抓取收音机后直接预测“放到桌上”，跳过了按按钮阶段；记忆模型则能记录已抓取、下一步按按钮，并在观测证据支持后推进。包裹分拣中的同样模式也得到缓解。
- **语义规划与运动控制的边界**：PressOn 成功率仍低，说明语言记忆解决“做什么/当前阶段是什么”，但不直接提升接触丰富动作的几何对齐、力控等低级执行质量。
- **上下文内失败恢复**：真实机器人日志中，抓取失败后记忆模型能记录“对象未固定”，发出重新打开夹具并重试的子任务，无需独立恢复控制器。

## 7. 优点

- **方法创新性**：将语言记忆设计为递归更新的显式状态变量，区别于一次性 CoT 推理轨迹或视觉 token 压缩记忆，为长时程 VLA 提供了紧凑可扩展的历史表示。
- **层次化解耦**：语义规划与连续控制分别优化，避免多目标平衡难题，同时保留 VLM 语义表征能力。
- **全自动监督标注管线**：从专家视频、技能标签和时间戳自动生成帧对齐的记忆与子任务目标，可扩展到大规模示范数据。
- **可解释性**：记忆和子任务均为自然语言，可直接观察任务进度、定位失败环节、诊断错误类型（语义 vs 控制）。
- **跨场景验证**：同时在家庭仿真、人形操作仿真和真实低成本机器人上验证，结果具有外部一致性。
- **分析细致**：区分了语义决策错误与低级控制错误，用阶段级指标和定性日志阐明机制而非仅报告总体成功率。

## 8. 不足与局限

- **实验覆盖有限**：真实机器人任务较短（抓取放置）、数据量小（25条演示、20次试验），不足以评估分钟级多阶段任务；BEHAVIOR-1K 和 Genie Sim 未报告评估轨迹数量。
- **统计验证不足**：未报告随机种子方差、多次运行均值和置信区间，改善的显著性和稳定性未得到检验。
- **计算开销增加**：高层 VLM 的递归调用提高了计算量、端到端延迟和部署复杂度，论文未报告实测延迟或额外 token 消耗。
- **无法弥补低级控制缺陷**：接触丰富动作（如 PressOn 按钮）失败率高，语言记忆对几何对准、力控制无直接

- **无法弥补低级控制缺陷**：接触丰富动作（如 PressOn 按钮）失败率高，语言记忆对几何对准、力控制无直接帮助；该局限在论文中作为语义/控制边界问题被明确承认，但没有提出针对性的低层改进方案。
- **高层模型依赖 VLM 的先验能力**：高层 PaliGemma 的记忆生成质量受基础 VLM 的常识推理和视觉理解上限约束，若 VLM 对场景状态感知错误（如误判物体已抓取），错误会沿递归记忆链持续传播，论文未设计针对记忆错误的显式校验或回滚机制。
- **递归记忆的一致性风险**：滚动压缩规则依赖手工设计（如"完成→过去时"），未对不同压缩策略做系统比较；m_t 的语义漂移或信息丢失在长任务中可能累积，论文未分析记忆遗忘曲线或长视界下的稳健性。
- **动作块执行与语义更新频率解耦不充分**：低层一次执行 H=30 步动作块，高层记忆仅在里程碑变化时更新；若任务阶段切换发生在动作块内部，策略需等待当前块执行完毕才能响应新状态，存在固有的响应延迟。

## 9. 与相关工作的区别与定位

- **与一次性 CoT 推理的区别**：现有 VLA 方法多将任务分解作为上下文推理，产生一次性"子任务序列"或单步描述；本文的记忆是递归的、随观测更新的显式状态变量，在闭环执行中动态推进，而非一次性规划结果。
- **与视觉记忆（token 压缩/记忆缓冲区）的区别**：不使用视觉特征或嵌入向量的隐式压缩，而是将历史压缩为自然语言——token 成本低、可直接解释，且利用 VLM 的语言先验进行结构化总结（过去时/将来时分离）。
- **与分层强化学习的区别**：不使用 Skill 库或值函数进行高层决策，而是端到端监督学习语义记忆和子目标；低层是现成的 VLA 动作专家，无需重新训练强化学习策略。
- **与 LLM-based planner + 执行器的区别**：以往方法常将 LLM 作为离线任务规划器，执行阶段不再参与；本文的高层 VLM 持续参与在线推理，以递归记忆形式执行闭环监控，与执行器同步动作。

## 10. 潜在的改进与未来方向

- **记忆校验与回滚**：在高层引入视觉证据校验机制，当记忆内容与当前观测矛盾时触发重新推理或回溯，防止错误沿记忆链传播。
- **端到端联合微调**：在独立训练基础上，可考虑对高低层进行轻量联合微调，使子任务表达与低层动作专家能力对齐，避免高层生成低层无法执行的目标。
- **长视界与记忆遗忘分析**：在更长任务（数十阶段）中定量评估记忆长度、信息保留率与成功率的关系，探索记忆内容压缩策略的可学习化。
- **低层控制增强**：针对接触丰富动作，引入触觉/力反馈或局部视觉闭环模型，与高层语言记忆形成互补。
- **成本-性能权衡**：研究高层递归调用频率的调度策略（如事件驱动而非固定步长），降低推理开销。

## 11. 总体评价与适用性判断

- **学术贡献**：论文为长时程 VLA 控制提供了一种简单而有效的语义记忆视角，在不改动低层控制算法的情况下显著提升多阶段任务的阶段完成率，尤其适合阶段边界清晰、依赖历史上下文的操作任务。
- **工程可复现性**：方法组件均为公开模型（PaliGemma、π0.5）和标准微调流程，数据标注管线自动化程度高，工程复现门槛较低。
- **适用边界**：适合任务序列由离散子阶段构成、每阶段目标可自然语言化的场景；对连续流式操作（如擦拭、搅拌）或对低级动态控制有高要求的任务，增益可能有限。
- **证据强度**：阶段级指标和定性日志增强了结论的可信度，但实验规模和统计严谨性目前处于初步验证水平，尚不足以作为 SOTA 结论的最终判据。

## 12. 阅读建议

- **重点章节**：第 3 节方法（递归语言记忆与滚动压缩规则）、第 4 节实验（阶段级分析）、第 5 节结论（语义/控制边界）。
- **谨慎之处**：对比基线仅 π0.5 原版，未与"CoT 无记忆""视觉token记忆"等其他记忆方案比较，读者不宜将提升幅度直接归因于"语言记忆"本身，而应理解为"显式可更新语义记忆"在示例场景中的有效性。
- **可延伸阅读**：π0/VLA 系列（低层动作模型）、Gemini/Qwen 等通用 VLA 的 CoT 研究、Embodied Chain-of-Thought 记忆方法。

## 13. 一句话总结

论文提出了一种层次化 VLA 架构，用递归更新的自然语言记忆将长时程任务的历史状态显式压缩为可解释的子任务序列，显著改善多阶段机器人操作中的阶段一致性与成功率，但改善主要局限于语义决策层面，且当前实验规模尚不足以全量验证其长期稳健性。

（完）
