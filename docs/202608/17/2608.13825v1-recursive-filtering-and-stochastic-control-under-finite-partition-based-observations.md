---
title: Recursive Filtering and Stochastic Control under Finite Partition-Based Observations
title_zh: 基于有限划分观测的递归滤波与随机控制
authors: "Saul Díaz-Infante Velasco, Yofre H. Garcia, Jesús Adolfo Minjárez-Sosa"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13825v1"
tags: ["query:control"]
score: 9.0
evidence: 针对部分可观测随机系统提出滤波与最优控制框架，支持有限划分观测
tldr: 针对有限划分观测下部分可观测随机系统，提出统一的递归滤波与随机控制框架。核心在测度层定义类别限制的未归一化条件测度，经预测归一化构造信息状态，将原问题转化为完全可观测马尔可夫决策过程。推导贝尔曼方程并证明平稳最优策略存在；同时提出类依赖有限维近似，建立近似滤波误差与值函数误差的抽象界限。框架统一覆盖阈值、量化、删失、事件触发等观测机制，并提供可计算实现路线。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13825v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 571, \"height\": 178, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13825v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 557, \"height\": 145, \"label\": \"Table\"}]"
motivation: 实际系统中观测常基于有限划分，如阈值、量化、事件触发等，但现有滤波与控制框架缺乏统一处理，导致理论割裂。
method: 在测度层对每个观测类定义未归一化条件测度，经预测概率归一化得后验，并以观测类与条件测度作为信息状态，转化为完全可观测MDP。
result: 推导出贝尔曼方程，证明平稳最优策略的存在性，并给出近似滤波误差与值函数误差之间的抽象界限。
conclusion: 该框架统一覆盖阈值、量化、删失、事件触发等观测机制，为有限划分观测下的随机滤波与控制提供可计算的理论基础。
---

## 摘要
我们针对部分可观测随机系统开发了一个滤波和最优控制框架，其中每个观测识别隐藏状态空间的有限可测划分的一个类别。该结构涵盖了与阈值、量化、审查、事件触发和间歇观测相关的区域信息机制，并允许具有原子、连续或混合分量的可观测类别。该公式首先在测度层面构建：对于每个可观测类别，我们定义一个类别限制的未归一化条件测度，后验分布通过其预测概率归一化获得。基于该递归，我们引入一个信息状态，由观测类别和其上支持的条件测度组成，从而将原始问题转化为完全可观测的马尔可夫决策过程。我们制定了折扣成本准则，推导了贝尔曼方程，并建立了平稳最优策略存在的条件。为了解决信息空间的无限维性质，我们提出了类相关的有限维近似，能够保留连续分量和原子质量。我们还推导了一个抽象界，将近似滤波器的误差与值函数的误差联系起来。一个参考模型通过基于直方图的近似说明了该构造。

## Abstract
We develop a filtering and optimal-control framework for partially observable stochastic systems in which each observation identifies a class of a finite measurable partition of the hidden state space. This structure covers regional-information mechanisms associated with threshold, quantized, censored, event-triggered, and intermittent observations, and allows observable classes with atomic, continuous, or mixed components. The formulation is constructed first at the level of measures: for each observable class, we define a class-restricted unnormalized conditional measure, and the posterior distribution is obtained by normalizing with its predictive probability. Based on this recursion, we introduce an information state consisting of the observed class and the conditional measure supported on it, thereby transforming the original problem into a fully observable Markov decision process. We formulate the discounted-cost criterion, derive the Bellman equation, and establish conditions for the existence of stationary optimal policies. To address the infinite-dimensional nature of the information space, we propose class-dependent finite-dimensional approximations capable of preserving both continuous components and atomic masses. We also derive an abstract bound linking the error of the approximate filter to the error of the value function. A reference model illustrates the construction through histogram-based approximations

---

## 论文详细总结（自动生成）

# 论文总结：基于有限划分观测的递归滤波与随机控制

## 1. 核心问题与整体含义
- **研究背景**：实际系统中观测往往不是隐藏状态的精确值，而是其所属的某个区域/类别，例如阈值传感器、量化数据、删失测量、事件触发观测、间歇信息等。现有非线性滤波与 POMDP 理论多假设连续有噪观测或使用抽象观测核，缺乏对“有限划分观测”这一共同结构的统一处理。
- **核心问题**：如何为“观测仅揭示隐藏状态属于某个可测类别”的部分可观测随机系统，建立一套统一的递归滤波与最优控制框架？
- **整体含义**：论文在测度层面提出“类别限制未归一化条件测度”的递归更新；将“观测类别 + 条件测度”定义为信息状态，从而把原始部分可观测问题转化为完全可观测的马尔可夫决策过程（MDP）。该框架统一覆盖阈值、量化、删失、事件触发、间歇观测等机制，并给出了有限维近似与误差分析。

## 2. 方法论
- **核心思想**：
  - 观测过程由有限可测划分 \(\{O_1,\ldots,O_r\}\) 诱导：\(Y_t=j \iff X_t\in O_j\)，类别分配在给定状态后是确定性的，但从控制器视角仍是随机的。
  - 在测度层面定义预测传播算子 \(L_u\mu(C)=\int Q(C|x,u)\mu(dx)\)。
  - 对每个类别 \(O_j\) 定义类别限制未归一化条件测度 \(\tilde{L}_{u,j}\mu(C)=(L_u\mu)(C\cap O_j)\)，其总质量 \(p_j(\mu,u)=(L_u\mu)(O_j)\) 就是下一观测类别的预测概率。
  - 正常化后得到滤波更新 \(\Phi_j(\mu,u)=\tilde{L}_{u,j}\mu/p_j(\mu,u)\)，并约定零质量分支用固定参考测度扩展（不影响可达轨迹）。
  - 信息状态 \(I_t=(Y_t,\pi_t)\)，其中 \(\pi_t\) 是支持在当前观测类别上的条件分布。
  - 通过信息状态定义观测概率 \(\vartheta_k(I,u)=p_k(\mu,u)\) 和转移核
    \[
    K(D|I,u)=\sum_{k\in J}\vartheta_k(I,u)\mathbf{1}_D(k,\Phi_k(\mu,u)),
    \]
    并证明原 POMDP 与信息状态 MDP 等价。
- **关键技术细节**：
  - 允许原子、连续、混合类别的观测类，采用类依赖支配测度 \(\lambda_j\)，无需全局绝对连续性。
  - 推导了递归滤波定理，给出预测、类别限制、归一化三步更新，以及密度表示（原子/连续/混合）。
  - 折扣准则下建立贝尔曼方程
    \[
    V^*(I)=\inf_{u\in U}\{g(I,u)+\alpha\sum_{k\in J}\vartheta_k(I,u)V^*(k,\Phi_k(\mu,u))\},
    \]
    并在紧致性、连续性假设下证明平稳确定性最优策略存在。
  - 提出类依赖有限维近似：对每个类别使用直方图族（或混合 Dirac-直方图族），原子质量精确保留，连续部分用直方图投影。
  - 建立抽象误差界：若投影误差为 \(\varepsilon^\Pi_N\)，则值函数误差满足
    \[
    \sup_{I\in R}|V^*_N(I)-V^*(I)|\le \frac{L_g+\alpha L_V}{1-\alpha}\varepsilon^\Pi_N,
    \]
    将滤波近似误差与值函数误差直接联系起来。

## 3. 实验设计
- 论文**没有使用真实数据集或 benchmark**，也没有与现有方法进行对比。
- 提供了一个**合成参考模型（synthetic reference model）** 说明完整构造：
  - 状态空间 \(X=[0,1]\)，动作空间 \(U=\{-1,0,1\}\)。
  - 饱和动力学 \(X_{t+1}=S(0.85X_t+0.12U_t+\eta_{t+1}+\varepsilon_{t+1})\)，其中 \(\eta,\varepsilon\) 为均匀噪声。
  - 观测划分为 \(O_1=\{0\}, O_2=(0,0.35], O_3=(0.35,1), O_4=\{1\}\)，边界为原子，内部为连续类。
  - 成本含目标偏差、控制惩罚和边界惩罚。
  - 直方图近似分辨率 \(N\in\{10,20,40,80\}\)，积分用 \(17\times17\) 张量积矩形规则，概率低于 \(10^{-4}\) 的分支剪枝，有限时域 \(H=5\)。
- 报告了近似有限时域折扣价值函数和初始最优动作（表 1），以及一步观测概率（表 2）。这些结果仅是合成演示，不构成实证验证。

## 4. 资源与算力
- 论文**未提及任何算力信息**（GPU 型号、数量、训练时长等）。
- 由于是理论方法论文且参考模型规模很小，计算需求低；但作者没有明确说明使用的计算资源。

## 5. 实验数量与充分性
- 实验数量较少：一个合成模型、四种分辨率、单一初始分布、单一成本参数、单一时域 H=5。
- 作为理论论文的“说明性参考模型”，足以展示滤波更新、信息状态、直方图近似的可行性。
- 但作为系统性验证不够充分：没有不同参数敏感性分析、没有不同观测划分比较、没有与粒子滤波等已有方法对比、没有验证理论误差界与数值结果之间的定量关系。
- 因此，实验覆盖有限，但符合该理论导向论文的定位。

## 6. 主要结论与发现
- 建立了“预测传播 → 类别限制 → 归一化”的递归滤波公式，后验自动支持在观测类别上。
- 信息状态 \((Y_t,\pi_t)\) 构成完全可观测的受控马尔可夫过程，原 POMDP 与信息状态 MDP 在有限时域和折扣准则下等价。
- 在紧致性、连续性和有界性假设下，贝尔曼方程有唯一解，且存在平稳确定性最优策略。
- 类依赖的有限维近似（直方图等）在可达集上一致收敛，值函数误差可由投影误差线性控制。
- 参考模型显示，随分辨率增大，近似值函数快速稳定（N=20后变化很小）；初始最优动作为 \(u=0\)，与观测概率分析一致（\(u=0\) 几乎将质量保留在目标类 \(O_3\) 且无控制代价）。

## 7. 优点
- **统一框架**：将阈值、量化、删失、事件触发、间歇观测统一为有限划分观测，填补了相关理论割裂的空白。
- **测度优先**：在测度层面构建滤波，避免全局密度假设，自然支持原子、连续、混合类别。
- **理论严谨**：包含滤波定理、POMDP–MDP 等价性、贝尔曼方程、平稳策略存在性以及误差界的完整证明。
- **近似方法可操作**：类依赖直方图保留原子质量，误差界将滤波误差与值函数误差联系起来，为算法设计提供了理论依据。
- **参考模型可复现**：给出了明确的参数和数值流程，便于他人复现。

## 8. 不足与局限
- **实验局限**：仅有合成示例，无真实数据、无 benchmark、无对比方法、无消融实验，验证力度较弱。
- **理论假设较强**：
  - 折扣控制部分要求单步代价有界、折扣因子 \(\alpha<1\)；
  - 弱连续性条件包括原始核弱连续、边界零测、可达分支概率正下界等，验证这些条件可能较困难；
  - 近似误差界仅在可达集合上成立，且需要类概率保持正下界以避免归一化放大误差。
- **近似的未建模误差**：核心理论假设观测概率精确，但数值实现中引入了积分近似、采样、剪枝等额外误差，这些未被纳入正式误差界。
- **应用限制**：参考模型是一维简单系统；高维状态空间下直方图近似面临维数灾难，文中未讨论高维扩展。
- **可验证性**：如何实际估计可达集、Lipschitz 常数、投影误差等关键量，论文未给出具体操作指导。

（完）
