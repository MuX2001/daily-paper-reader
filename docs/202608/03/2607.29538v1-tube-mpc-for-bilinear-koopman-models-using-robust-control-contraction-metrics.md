---
title: Tube MPC for Bilinear Koopman Models using Robust Control Contraction Metrics
title_zh: 基于鲁棒控制收缩度量的双线性Koopman模型的管式MPC
authors: "Thomas de Jong, Mircea Lazar"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29538v1"
tags: ["query:control"]
score: 9.0
evidence: 鲁棒管式模型预测控制、双线性Koopman模型、鲁棒控制收缩度量
tldr: 针对数据驱动双线性Koopman模型与真实系统的失配，本文提出鲁棒管MPC。用离散时间鲁棒控制收缩度量显式刻画失配，神经网络在稀疏采样状态上学习度量和反馈律。进而构造收紧约束的管MPC，证明递归可行性、鲁棒约束满足和输入到状态稳定性。利用LPV结构使MPC凸化，测地线计算转为二次规划，并在非线性摆上验证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.29538v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1882, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 562, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1755, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 850, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 854, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 858, \"height\": 496, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 692, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29538v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 916, \"height\": 883, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.29538v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 641, \"height\": 129, \"label\": \"Table\"}]"
motivation: 双线性Koopman模型用于非线性预测控制时存在模型失配，影响闭环稳定性与约束满足。
method: 提出鲁棒管MPC，用神经网络学习鲁棒控制收缩度量及反馈，并利用LPV结构凸化问题。
result: 证明递归可行性和输入到状态稳定性，实现约束收紧，数值仿真验证了框架有效性。
conclusion: 该框架为高维Koopman模型提供可扩展的鲁棒控制方案，兼顾理论保证与计算效率。
---

## 摘要
本文提出了一种针对由数据辨识得到的双线性Koopman模型所表示的非线性系统的鲁棒管式模型预测控制(MPC)框架。我们为双线性Koopman模型推导了离散时间鲁棒控制收缩度量(RCCMs)，该度量验证了显式考虑Koopman模型与真实动力学之间失配的收缩性质。为了综合这种通常针对高维提升状态的证书，我们提出了一种可扩展的学习方法，该方法在提升状态空间的稀疏采样子集上训练度量及关联反馈控制器的神经网络参数化。所得的证书用于构造具有收紧约束的鲁棒管式MPC方案，并为此建立了递归可行性、鲁棒约束满足以及闭环系统相对于Koopman模型与真实动力学之间失配的输入到状态稳定性(ISS)。通过利用双线性Koopman模型的线性参数变化结构，对于固定的调度序列，MPC问题变为凸问题，而基于收缩反馈的测地线计算则通过切比雪夫伪谱离散化被重新表述为一系列二次规划。该完整框架在具有状态相关输入增益的非线性摆基准上得到了验证。

## Abstract
This paper presents a robust tube model predictive control (MPC) framework for nonlinear systems represented by bilinear Koopman models identified from data. We derive discrete-time robust control contraction metrics (RCCMs) for bilinear Koopman models, which certify a contraction property that explicitly accounts for the mismatch between the Koopman model and the true dynamics. To synthesize such certificates for the typically high-dimensional lifted state, we propose a scalable learning approach that trains neural network parameterizations of the metric and of an associated feedback controller over a sparsely sampled subset of the lifted state space. The resulting certificates are used to construct a robust tube MPC scheme with tightened constraints, for which we establish recursive feasibility, robust constraint satisfaction and input-to-state stability (ISS) of the closed-loop system with respect to the mismatch between the Koopman model and the true dynamics. By exploiting the linear parameter-varying structure of bilinear Koopman models, the MPC problem becomes convex for a fixed scheduling sequence, while the geodesic computation underlying the contractive feedback is reformulated, via a Chebyshev pseudospectral discretization, as a sequence of quadratic programs. The complete framework is validated on a nonlinear pendulum benchmark with a state-dependent input gain.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- 论文针对**数据驱动的双线性 Koopman 模型**在非线性预测控制中的应用瓶颈：有限数据和有限维提升映射导致 Koopman 模型与真实非线性动力学之间存在不可避免的模型失配（`wk`）。  
- 若直接采用名义 Koopman 模型做 MPC，失配会破坏闭环约束满足与稳定性。  
- 现有鲁棒管式 MPC 多基于线性 Koopman 实现或低维连续时间系统，难以直接推广到**高维提升状态**的双线性模型（SOS 综合不可行、离散时间收缩度量耦合复杂）。  
- 本文的核心意义在于：**用鲁棒控制收缩度量（RCCM）显式刻画模型失配，并以此构建具有递归可行性、鲁棒约束满足和输入到状态稳定性（ISS）保证的管式 MPC 框架**，为高维双线性 Koopman 模型提供了兼顾理论保证与计算可行性的鲁棒控制路线。

## 2. 方法论

### 2.1 整体思路
- 将真实非线性系统视为不确定模型：`xk+1 = f(xk, uk)`，而近似模型为 `xk+1 = f_hat(xk, uk) + wk`，其中 `wk` 为有界建模误差。  
- 利用 Koopman 提升，构造双线性模型：
  `zk+1 = A zk + B0 uk + Σ_i [uk]_i Bi zk + wk`。  
- 通过神经网络同时训练提升函数 `φ_θ(x)` 和模型矩阵（Problem 1），获得训练后的模型 `f_K^*`。

### 2.2 核心理论：离散时间鲁棒控制收缩度量（RCCM）
- 对双线性 Koopman 模型推导离散时间稳健收缩不等式，要求存在度量 `W(z)` 与反馈增益 `L(z)`，满足：
  - 一致有界：`α1 I ⪯ W(z) ⪯ α2 I`
  - 收缩 LMI（公式 28）：
    `[[W(z+), A*(u)W(z)+B*(z)L(z), I], [*, (1-α)W(z), 0], [*, *, νI]] ⪰ 0`
- 该不等式保证鲁棒收缩性质：
  `E_{γ_{k+1}} ≤ (1-α) E_{γ_k} + ν ||wk - w̄k||²`  
  从而由 Lemma 1 得到两条轨迹间的欧氏误差界，这是后续管式 MPC 收紧约束的基础。

### 2.3 神经网络度量学习（Problem 2 & Algorithm 1）
- 由于提升状态维度高，不使用 SOS，而是采用**稀疏采样 + 神经网络参数化**：
  - 在状态、输入、扰动空间生成虚拟数据点 `DM`
  - 提升后叠加局部扰动（off-manifold 采样），增强鲁棒性
  - 度量 `W_θw(z) = L_θ(z)L_θ(z)ᵀ + γI` 保证正定性
  - 用 softplus 损失惩罚负特征值，并用条件数正则化
- 训练完成后得到 `W_θw^*` 与 `L_θl^*`。

### 2.4 管式 MPC（Problem 3）与终端设计
- 利用双线性模型的**精确 LPV 嵌入**（调度变量 `ρk = z̄k`）将预测写成 `z̄ = Φ z̄0|k + Γ(ρk)ūk`，从而对固定调度序列问题变凸。
- 约束收紧采用 `δmax = √ν δ̄ / (1 - √(1-α))` 及梯度常数 `cz, cu`。
- 初始条件约束 `||z0|k - z*_{1|k-1}|| ≤ w̄` 保证可行轨迹的一致性。
- 终端集合与终端代价基于 LPV 技术设计（Assumption 2），但实际因高维约束过多而直接取 `ZT = Z̄`。

### 2.5 测地线计算与在线算法（Problem 4/5 & Algorithm 2）
- 测地线 `γ(s)` 由切比雪夫多项式参数化，通过 Chebyshev-Gauss-Lobatto 节点和求积公式离散化。
- 由于度量逆的存在，原问题非线性，作者将其转化为**序列二次规划（QP）**：每次迭代固定度量在上一迭代解处的值，从而得到一个正半定二次规划（Problem 5）。
- 在线流程：测量状态→提升→迭代求解 MPC（固定调度）→迭代求解测地线 QP→计算收缩反馈修正 `Δuk` 并施加 `uk = ū*_{0|k} + Δuk`。

### 2.6 理论保证
- **Theorem 2**：鲁棒约束满足（真实闭环轨迹始终在约束内）。
- **Theorem 3**：递归可行性（基于收缩轨迹构造候选解）。
- **Theorem 4**：闭环 ISS —— 真实系统轨迹与参考之间的偏差由 `KL` 函数加线性扰动增益界定，且当扰动趋零时可恢复渐近稳定性（比一般实践稳定/ISpS 更强）。

## 3. 实验设计

- **Benchmark**：单摆系统，具有**状态相关输入增益**：
  `ẋ1 = x2，ẋ2 = -g/l sin(x1) - d/(ml²) x2 + cos(x1)/(ml²) u`，参数 m=1, l=1, g=9.81, d=0.3，采样 Ts=1/30。
- **数据**：
  - 辨识数据集：50000 个样本（分段恒定输入 + 随机抖动），训练/验证损失曲线如图 7。
  - 度量学习数据集：16875 个虚拟状态-输入-扰动样本，状态在 `x1∈[-2,2]`、`x2∈[-3,3]`、`u∈[-25,25]` 采样，扰动 `w∈[-0.01,0.01]`，并施加 Nε=5 组局部扰动（0, 0.001, 0.0025, 0.005, 0.01）。
- **MPC 设置**：预测步数 N=30（与辨识目标一致），Hyperparameters 表 I 给出 α=0.1, ν=0.5, γc=15, β=10, ε=1 等。  
  终端集因高维约束过多直接用约束集本身（并说明是未来工作）。
- **对比方法**：文中未进行与其它鲁棒 MPC 方法的显式数值对比；仅在引言中与并行工作 [25]（在原始状态空间合成度量）做概念性对比。仿真只展示本文方法对参考轨迹的跟踪效果。

## 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量或训练耗时**，也未给出神经网络训练的具体时间。
- 仅在在线计算中给出：在 13th Gen Intel Core i7-1370P CPU (1.90 GHz) 上，测地线与收缩反馈计算平均 0.079s，MPC 求解平均 0.023s，使用 CasADi + MOSEK（MPC）+ OSQP（测地线）。
- 因此，离线训练算力信息缺失，无法评估其实际训练成本。

## 5. 实验数量与充分性

- **实验数量少**：只有一个非线性摆基准，没有多系统验证，也没有消融研究（如不同采样策略、不同神经网络规模、不同 α 或 ν 的敏感性）。
- **充分性评价**：作为概念验证，验证了整体框架在单一场景下的可行性和基本性能（约 25 秒闭环，显示轨迹跟踪、管边界、输入约束满足），并报告了收缩 LMI 满足率（训练集 90.4%，验证集 90.2%）。  
  但缺乏与现有鲁棒 MPC 或 Koopman-MPC 方法的定量对比，也没有分析失配大小对性能的影响，因此**实验不够充分，难以证明方法普遍优越性**。

## 6. 主要结论与发现

- 所提出的 RCCM 管式 MPC 能显式处理双线性 Koopman 模型与真实动力学之间的失配，并获得**递归可行性、鲁棒约束满足、ISS** 三类闭环保证，且 ISS 比常见的实践稳定（ISpS）更强。
- 利用 LPV 结构将 MPC 凸化，利用切比雪夫拟谱法将测地线转为序列 QP，使在线计算可负担（总时间约 0.1s）。
- 数值实验表明参考跟踪准确，存在小的稳态偏移；管子较保守，尤其是 x1 方向。
- 验证了神经网络学习度量在提升空间的可扩展性，但收缩不等式仅在采样点近似成立（约 90%），并不能保证全局满足。

## 7. 优点

- **理论新颖性**：首次将离散时间 RCCM 与双线性 Koopman 管式 MPC 结合，并证明 ISS（而非仅 ISpS 或集合收敛）。
- **可扩展性**：绕开 SOS 的低维限制，用神经网络+稀疏采样在高维提升空间合成证书，复杂度只取决于提升维度而非提升函数复杂度。
- **计算友好**：利用双线性的 LPV 结构和假谱离散化，将 MPC 和测地线都转化为凸问题/序列 QP，便于在线实现。
- **设计闭环**：从模型学习、度量学习、MPC 公式到在线算法形成完整流程，并有明确的块图（Fig. 4）和算法伪代码（Algorithm 1, 2）。
- **考虑实际细节**：提出 off-manifold 采样应对初始条件偏差，增加正则项促进优化初始状态与实际状态靠近，处理了终端集高维问题（虽然简化）。

## 8. 不足与局限

- **全局性保证缺乏**：神经网络学习度量只在采样点满足 LMI（~90%），无法提供全局验证，存在安全隐患；作者也承认采样策略需要改进。
- **保守性过高**：均匀采样全约束集导致证书在低频访问区域过于保守，管子较大；作者建议沿闭环轨迹采样但未实现。
- **终端集处理粗糙**：因高维约束过多，直接取 `ZT = Z̄`，未真正利用 LPV 终端设计，可能削弱递推可行的实际效果。
- **实验单一**：仅一个两维摆系统，无比较基准、无消融、无统计性重复；说服力有限。
- **失配假设简单**：`W` 取均匀范数球（`||w||≤0.01`），未讨论如何从数据严格估计，也未与比例/核化误差界结合（仅列为未来方向）。
- **输入-状态稳定性证明依赖初始化条件** `z*_{0|0}=z0` 及特殊约束，且实际实现中为性能加入正则项，理论证明与实现细节之间略有偏差。
- **在线计算仍偏慢**：每步约 0.1s 超过采样时间 1/30s（约 0.033s），尚不满足实时要求。

（完）
