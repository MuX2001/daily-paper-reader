---
title: Explicit Language Memory for Long-Horizon Planning in Vision-Language-Action Models
title_zh: 显式语言记忆用于视觉-语言-动作模型的长时程规划
authors: "Houze Xu, Jizhong Li, Ziyi Ye"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04765v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向机器人长时程规划的视觉语言动作模型与显式语言记忆
tldr: 针对VLA模型在长时序任务中泛化差、时序一致性弱、误差累积和语义表征退化等问题，提出分层架构，引入显式语言记忆模块，将时间观测转换为带时序逻辑的文本记忆。高层VLM通过VQA范式进行语义推理并递归更新记忆与子任务指令，低层VLA执行连续控制。实验证明该方法提升成功率与鲁棒性，提供可解释决策过程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 711}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1542, \"height\": 715}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 765}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1597, \"height\": 1048}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1330, \"height\": 211}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 395}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 210}]"
motivation: 现有VLA模型在长时序任务中受限于稀疏专家示范、非马尔可夫性、误差累积以及微调弱化语义表征，难以保持时间一致性和跨任务泛化。
method: 提出分层VLA架构，高层VLM将离散观测转化为文本记忆序列，以旧记忆为上下文锚递归更新子任务指令；低层VLA基于指令和视觉执行连续控制。
result: 在多个仿真环境和真实机器人平台上的实验表明，显式语言记忆显著提高了复杂长时序任务的成功率和鲁棒性，并给出可解释的语义过程。
conclusion: 显式语言记忆为长时序VLA规划提供了一种简单有效的方案，增强了时序跟踪与动态纠错能力，同时保持视觉语言模型的高层语义。
---

## 摘要
视觉-语言-动作（VLA）模型为连接视觉感知、语言理解和机器人控制提供了统一范式。然而，现有VLA模型在长时程任务中仍面临重大挑战：稀疏的专家示范限制了跨任务的组合泛化能力；长时程任务的非马尔可夫性质使得仅基于当前观测的策略难以保持时间一致性；有限的闭环纠错能力导致执行误差不断累积；端到端的动作微调可能削弱视觉-语言模型（VLM）骨干网络的高层语义表征。为解决这些问题，我们提出了一种带有显式语言记忆模块的分层长时程VLA架构。其核心思想是将离散的时间观测转换为具有时间逻辑的连贯文本记忆序列。该系统被解耦为高层VLM和低层VLA：高层VLM通过视觉问答训练范式进行语义推理，而低层VLA在子任务指令和视觉观测的条件下执行精确的连续控制。高层VLM利用先前的记忆作为上下文锚点，递归更新语言记忆和子任务指令，从而在长时程执行中实现持久的时间跟踪和动态纠错。我们在多个仿真环境中评估了所提方法，并在真实机器人平台上进行了仿真到现实（sim-to-real）实验。结果表明，显式语言记忆提高了VLA模型在复杂长时程任务上的成功率和鲁棒性，同时为决策过程提供了可解释的语义说明。

## Abstract
Vision-language-action (VLA) models provide a unified paradigm for connecting visual perception, language understanding, and robotic control. However, existing VLA models still face major challenges in long-horizon tasks: sparse expert demonstrations constrain cross-task compositional generalization; the non-Markovian nature of long-horizon tasks makes it difficult for policies conditioned only on current observations to maintain temporal consistency; limited closed-loop error correction allows execution errors to accumulate; and end-to-end action fine-tuning may weaken the high-level semantic representations of vision-language model (VLM) backbones. To address these issues, we propose a hierarchical long-horizon VLA architecture with an explicit language-memory module. The central idea is to convert discrete temporal observations into a coherent textual memory sequence with temporal logic. The system is decoupled into a high-level VLM and a low-level VLA: the high-level VLM performs semantic reasoning through a visual question answering training paradigm, while the low-level VLA executes precise continuous control conditioned on subtask instructions and visual observations. The high-level VLM recursively updates both language memory and subtask instructions using the previous memory as a contextual anchor, enabling persistent temporal tracking and dynamic correction during long-horizon execution. We evaluate the proposed method in multiple simulation environments and conduct sim-to-real experiments on a real robotic platform. The results demonstrate that explicit language memory improves the success rate and robustness of VLA models on complex long-horizon tasks while providing an interpretable semantic account of the decision process.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：视觉-语言-动作（VLA）模型为连接视觉感知、语言理解与机器人控制提供了统一范式，可借助大规模视觉-语言模型的语义知识迁移到机器人控制任务。
- **核心问题**：现有VLA模型在**长时程（long-horizon）任务**中表现不佳，主要面临四大挑战：
  - **稀疏的专家示范**限制了跨任务的组合泛化能力；
  - 长时程任务具有**非马尔可夫性**，仅依赖当前观测无法区分相同的视觉输入在不同任务阶段所对应的不同决策（如同一副收音机画面可能意味着“已抓取”或“尚未抓取”）；
  - **有限的闭环纠错能力**导致执行误差沿动作序列累积；
  - 端到端动作微调可能**削弱VLM骨干的高层语义表征**。
- **整体含义**：论文主张将“任务历史”用显式自然语言记忆来压缩表示，使策略具备阶段一致性和可解释性，从而突破长时程VLA规划的瓶颈。

### 2. 论文提出的方法论

- **核心思想**：将长时程策略分解为**高层语义分支**（π_HL）和**低层动作分支**（π_LL），以显式语言记忆为中间载体实现递归的语义状态跟踪与连续动作生成。
- **整体策略分解**（公式1）：
  - π(a_{t:t+H}, s_t, m_t | o_t, m_{t-1}, l) = π_LL(a_{t:t+H} | o_t, s_t) · π_HL(s_t, m_t | o_t, m_{t-1}, l)
  - 其中 l 为全局指令，o_t 为当前观测，m_{t-1} 为上一时刻语言记忆，s_t 为子任务指令，a_{t:t+H} 为H步连续动作块。
- **递归语言记忆机制**：
  - 记忆 m_t 是结构化自然语言摘要，记录已完成阶段（过去时）、完成证据、相关物体属性、观察到的失败及下一待办里程碑；
  - 采用**滚动压缩规则**：子任务完成前保留细节（目标类别、颜色、位置），完成后压缩为短句，注意力转向下一步；
  - 示例（公式2）：从 "I am moving to the red radio on the coffee table" 更新为 "I moved to the radio, and I am going to pick up the red radio"。
- **自动化记忆监督管道**：
  - 每个专家示范包含视频、技能级标注（名称、描述、起止时间戳）；
  - 通用VLM结合当前帧和完整技能标注，自动生成对齐的**子任务指令**和**第一人称语言记忆**；
  - 提示词要求使用自然物体名称和可见属性，禁止出现 "frame"、"index" 等实现术语；
  - 支持两种标注路线：Gemini 3.5 Flash API 和本地部署的 Qwen3.5-9B。
- **独立分支训练**：
  - 高层分支使用 **PaliGemma**（与π0.5基线同族VLM），以自回归方式预测记忆更新和子任务（公式3）；
  - 低层分支接收观测和子任务指令，以 **flow-matching action objective** 优化（公式4），沿用π0.5的300M Gemma动作专家；
  - 两分支独立训练，避免单一端到端目标中语义生成损失与连续控制损失的平衡问题。
- **闭环推理流程**：
  - m_0 初始化为空，循环执行：获取观测 → 更新(m_t, s_t) → 生成H=30步动作块 → 执行 → 检查终止条件；
  - 记忆与子任务均为可见文本，输出可解释的决策日志，便于与动作失败对齐分析。

### 3. 实验设计

- **BEHAVIOR-1K 收音机任务**：
  - 指令为"打开客厅桌子上收音机的电源"；
  - 分为 MoveTo、PickUp、PressOn、PlaceOn 四个评估阶段；
  - 对比方法：π0.5（基线）vs π0.5 + language memory。
- **Genie Sim 3.0 包裹分拣（人形操作仿真平台）**：
  - **单包裹任务**：跟随包裹→拾取→放入扫描区（条码朝上）→再次拾取→放入蓝色箱子→返回起始朝向，六个计分阶段（Follow、PickUp-1、Inside-1、Upright、PickUp-2、Inside-2）；
  - **连续分拣变体**：按任意顺序分拣四个不同颜色的包裹，以每次条码朝上放入扫描区计为完成一个阶段（Upright-1~4）；
  - 同样对比 π0.5 与 π0.5 + language memory。
- **真实 XLeRobot 平台抓取放置任务**：
  - 使用双 SO-ARM 101 机械臂 + LeKiwi 移动底盘 + RGB 头相机 + 手部相机；
  - 任务：从桌面拾取积木块并放到盘子上，分为 Follow、PickUp、MoveTo、Place 四个阶段；
  - 数据集仅含25条人工遥操作轨迹，评估20次试验；
  - 对比 π0.5 与 π0.5 + language memory。
- **评估指标**：阶段级成功率（stage-wise success rate，公式5），即各阶段成功率的平均值，用于暴露长序列中具体失败位置，而非仅报告最终任务完成率。

### 4. 资源与算力

- **训练阶段**：4块 **NVIDIA A100 GPU**；
- **推理阶段**：1块 **NVIDIA RTX 5880 Ada Generation GPU**；
- **关键超参数**：
  - 动作块长度 H = 30；
  - 高层 PaliGemma 与低层VLA中的VLM骨干使用 **LoRA** 适配；
  - 低层 300M Gemma 动作专家进行全量微调；
  - 优化器 AdamW，warmup + 余弦学习率衰减，峰值学习率 2.5×10⁻⁵，batch size 64，共50,000训练步。
- **论文未明确说明**：各实验场景的具体训练时长/运行时间、以及真实机器人训练与仿真训练的计算量分配。

### 5. 实验数量与充分性

- **实验场景数量**：3个（两个仿真 + 一个真实机器人），覆盖家居仿真、人形操作仿真与真实低成本移动操作平台，具备一定多样性。
- **任务类型**：涉及3类不同的长时程任务（收音机操控、包裹分拣、抓取放置），且包含单任务与连续多目标变体。
- **对比方法**：以原生π0.5为唯一基线，重点验证语言记忆模块的增量贡献。
- **实验充分性评估**：
  - **未做消融实验**：未单独验证记忆压缩策略、高层VLM模型选择、标注方式（Gemini vs Qwen3.5）等设计选择的影响；
  - **缺乏统计稳健性分析**：论文明确承认未报告不同随机种子的方差或置信区间，改进的统计显著性尚未验证；
  - **真实机器人评估规模有限**：仅25条训练轨迹、20次试验，且任务仅含4个阶段，不足以证明分钟级复杂真实场景的有效性；
  - **仿真任务范围有限**：BEHAVIOR-1K 仅选取了一个任务，未在多样化任务上验证泛化性。
- 总体而言，实验结果呈一致正向趋势，但在广度与统计严谨性上仍有提升空间。

### 6. 论文的主要结论与发现

- **显式语言记忆显著提升长时程VLA性能**：
  - BEHAVIOR-1K 收音机任务平均阶段成功率从30.0%提升至40.0%，PickUp阶段从10%提升至30%，最大的增益出现在拾取阶段；
  - Genie Sim 单包裹分拣平均成功率从41.7%提升至63.9%；连续四包裹分拣从31.3%提升至46.9%，其中12.5%的评估能完成全部四次正确摆放（基线为0%）；
  - 真实机器人任务（较短）仅小幅提升：从62.5%到66.3%，符合“阶段越少、历史信息帮助越小”的预期。
- **语言记忆改善阶段一致性并减少跳步**：无记忆基线会在按下按钮前就预测“将收音机放到桌上”，导致阶段跳越；带记忆模型只在观察到完成证据后才推进阶段。
- **语义规划与运动控制互补**：PressOn（按按钮）阶段成功率仍很低，说明语言记忆解决“该做什么”和“处于哪一阶段”，但不解决接触丰富动作的几何对齐、力控等问题。
- **支持上下文内失败恢复**：语言记忆可将“抓取未成功”这一失败状态文本化，使后续决策可重新尝试，而无需单独设计恢复控制器。

### 7. 优点

- **架构设计清晰**：将高层语义决策与低层连续控制解耦，各自拥有独立的优化目标，避免了单一大模型多目标平衡的困难。
- **显式语言记忆具有可解释性**：记忆和子任务均为文本输出，可直接检查任务进度与失败原因，便于故障定位与分析。
- **自动化监督管道**：利用通用VLM从专家视频和技能标注中自动生成记忆与子任务目标，使高层分支可训练，无需额外人工标注的语义状态。
- **滚动压缩机制高效**：以有界长度的文本记忆替代拼接全部历史帧的做法，兼顾信息保持与计算成本。
- **横向覆盖多平台验证**：同时在家居仿真、人形仿真与真实低造价机器人上验证，增加了结论的可信度。
- **诚实的边界分析**：论文明确区分了语义决策与运动控制的贡献边界，未过度声称方法的适用范围。

### 8. 不足与局限

- **真实实验覆盖不足**：真实平台仅做短时程的单物体抓取放置任务（25条训练轨迹、20次试验），未验证分钟级、多物体、重复交互或环境变化的真实场景。
- **统计严谨性欠缺**：未报告随机种子间的方差或置信区间，改进的统计稳定性未被确认。
- **缺少消融研究**：没有单独验证记忆机制的各个组成要素（如滚动压缩策略、记忆更新频率、标注模型选择）的贡献。
- **计算开销增加**：分层设计在每次决策前需要额外调用高层VLM，增加了端到端延迟和部署复杂度，未量化具体推理耗时。
- **低层控制瓶颈无法靠语言记忆解决**：PressOn阶段成功率极低（10%），表明通过语言记忆无法弥补接触丰富动作的低层控制能力不足。
- **失败恢复能力依赖数据覆盖**：可靠的恢复行为需要包含多样化失败与纠正轨迹的训练数据，当前数据集中此类样本较少。
- **潜在偏差风险**：样例量小、任务选择有限、未做统计显著性检验，导致结论的泛化性存在不确定性。

（完）
