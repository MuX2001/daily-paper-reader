---
title: "Joint Communication-Control Strategy Optimization with Partially Nested Information Structures: The Linear-Quadratic Case"
title_zh: 具有部分嵌套信息结构的联合通信-控制策略优化：线性二次情形
authors: "Haoyi You, Kaiqing Zhang"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13535v1"
tags: ["query:control"]
score: 9.0
evidence: 形式化分散随机控制中的通信与控制联合优化
tldr: 针对多智能体线性二次系统中的联合通信-控制策略优化问题，在公共信息框架下研究部分嵌套信息结构在通信策略优化下得以保持的条件。提出基于动态规划的新解法，对开环通信策略得到闭式Riccati方程，并扩展至闭环通信场景。该方法同时为部分嵌套信息结构下输出反馈的分散线性二次控制提供了求解途径，相比传统无限维信念状态动态规划，计算更易处理。所提框架为通信与控制协同优化奠定了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1574, \"height\": 586, \"label\": \"Figure\"}]"
motivation: 联合通信-控制优化中，通信策略可能破坏部分嵌套性，导致计算困难；需明确保持条件并寻找可解方法。
method: 基于公共信息框架，对开环通信策略设计动态规划算法，推导闭式Riccati方程；再扩展至闭环策略。
result: 建立了部分嵌套性保持条件，违反时最优策略非线性；算法给出闭式解，并解决输出反馈分散LQ控制问题。
conclusion: 为JCCO提供更易处理的动态规划，显著降低计算复杂度，奠定通信-控制协同优化基础。
---

## 摘要
本文在分散随机控制的基于公共信息（CIB）框架下，形式化了多智能体线性系统中具有二次成本的联合通信-控制策略优化（JCCO）问题。为便于计算处理，我们关注具有部分嵌套（PN）信息结构（ISs）的此类JCCO问题。特别地，在导致PN IS的基线通信协议下，我们建立了一系列条件，在这些条件下，部分嵌套性在（额外的）待优化通信策略下得以保持，而违反这些条件通常可能导致最优策略的非线性，且通信策略为开环。然后，我们开发了一种基于动态规划的方法来计算具有开环通信策略的JCCO的最优控制策略，该方法产生一组闭式Riccati方程。作为独立感兴趣的副产品，该方法还提供了一种在CIB框架下求解具有PN IS和输出反馈的分散线性二次控制的途径。最后，我们将该方法扩展到具有闭环通信策略的JCCO，产生比基于无限维CIB信念的动态规划更易处理的动态规划。

## Abstract
In this paper, we formalize a joint communication-control strategy optimization (JCCO) problem in multi-agent linear systems with quadratic costs, under the common-information-based (CIB) framework from decentralized stochastic control. For computational tractability, we focus on such JCCO problems with partially nested (PN) information structures (ISs). In particular, with a baseline communication protocol that leads to a PN IS, we establish a series of conditions under which the partial nestedness is preserved under the (additional) communication strategies to be optimized, while violating them may cause nonlinearity of the optimal strategies in general, with open-loop communication strategies. We then develop a dynamic-programming-based approach to compute the optimal control strategies of JCCO with open-loop communication strategies, which yields a set of closed-form Riccati Equations. As a byproduct of independent interest, such an approach also offers a way to solve decentralized linear-quadratic control with PN ISs and output feedback, under the CIB framework. Finally, we extend such an approach to JCCOs with closed-loop communication strategies, yielding a more tractable dynamic program than an infinite-dimensional CIB-belief-based one.

---

## 论文详细总结（自动生成）

# 论文中文深度总结

## 一、核心问题与整体含义（研究动机与背景）

- **研究背景**：在控制论与多智能体学习文献中，通信协议的设计已有大量研究（如信道容量约束、量化与控制联合设计、通信架构与控制共设计等）。然而，如何将通信策略本身作为待优化变量，与控制策略进行**联合优化**（Joint Communication-Control Strategy Optimization, JCCO），其理论基础仍然薄弱。
- **核心问题**：本文聚焦于**多智能体线性二次（LQ）系统**中的JCCO问题：每个智能体既可执行控制动作，也可执行“通信动作”来决定分享哪些私有信息；目标是最小化控制成本与通信成本的累计期望。其核心挑战在于：（a）通信策略的加入会改变信息结构（IS）；（b）若信息结构失去**部分嵌套性（Partially Nested, PN）**，最优控制策略可能非线性且计算上难以处理。
- **研究意义**：本文是此前离散空间JCCO形式化框架[14]在连续空间LQ设定下的系统化推演；同时，它推进了[17]中在公共信息（CIB）框架下对分散LQ控制问题的求解结果，为“通信-控制协同优化”提供了理论基础。

## 二、方法论：核心思想与关键技术细节

### 2.1 问题形式化

- 在**基于公共信息（CIB）的分散随机控制框架**下，形式化了联合通信-控制策略优化（JCCO）问题，定义总成本为目标函数（控制成本+通信成本+终端成本）。
- 信息结构由两部分构成：**基线分享**（固定通信协议）与**额外分享**（由待优化的通信策略决定）。
- 区分了两类通信策略：**开环**（open-loop，预选固定通信动作）与**闭环**（closed-loop，依赖历史信息与先前通信动作）。

### 2.2 结构条件：部分嵌套性的保持

- 论文围绕“何时附加通信策略不破坏PN信息结构”展开讨论，得到如下关键条件：
  - **Assumption III.2**：若某控制输入完全不影响状态演化（\(B_{i,h}=0\)），则该控制动作不得出现在后续任何智能体的信息集中（即“无用动作不共享”）。
  - **Assumption III.4**：若 \(B_{i,h}\neq 0\)，则至少存在一个其他智能体 \(j\neq i\) 能观测到该控制对状态的影响（\(E_{j,h+1}B_{i,h}\neq 0\)），保证“有影响必可被观测”。
- **通过反例证明必要性**：
  - Lemma III.1：基线IS非PN时，最优控制策略可非线性。
  - Lemma III.3：违反Assumption III.2时，最优策略可能非线性甚至不存在。
  - Lemma III.5：违反Assumption III.4时，最优策略可非线性。
- **正面结论（Theorem III.6）**：若JCCO问题满足上述假设且基线IS为PN，则对于任意固定开环通信策略，所得子问题仍是PN的分散LQG问题，且存在**线性最优控制策略**。

### 2.3 动态规划解法：闭式Riccati方程

- 处理思路：将原问题 \(D(g^m_{1:H})\) 扩展为 \( \tilde{D}(g^m_{1:H}) \)（严格PN问题），该构造的关键在于将“影响他人但不在公共信息中的动作”也纳入公共信息（参见式IV.1）。
- 核心理论链条：
  1. **Lemma IV.1**：扩展问题 \(\tilde{D}\) 的最优线性策略可通过函数 \(\phi\) 映射回原问题 D 的最优策略，且成本相等。
  2. **Theorem IV.2**：在假设成立时，扩展问题 \(\tilde{D}\) 满足**策略无关CIB信念（SI-CIB）条件**。
  3. **Lemma IV.3**：SI-CIB条件下，CIB信念退化为**高斯分布**，仅需条件均值 \(\tilde{\Theta}_h\) 与条件协方差 \(\tilde{\Sigma}_h\) 即可完全刻画，且协方差是策略无关的确定性序列。
  4. **Theorem IV.4**：构造等价马尔可夫决策过程 \(\tilde{D}^\ddagger\)，证明存在CIB-Markovian最优策略。
  5. **Theorem IV.6**：在CIB-Markovian+线性策略参数化下，价值函数为二次型，最优控制增益通过**一组闭式Riccati方程（式IV.5–IV.7）** 反向递推计算。

### 2.4 扩展到闭环通信策略

- 当通信策略为闭环时，信息结构不再保证PN/SI-CIB条件成立，CIB信念不再一定为有限维高斯分布。
- 采用与[14]一致的思路：通过**严格扩展（strict expansion）** 构造新问题，将影响他人的动作纳入公共信息，从而恢复SI-CIB条件（Theorem V.3）。
- 最终将问题转化为在较低维空间 \((\tilde{M}_{1:h}, \tilde{\Theta}_{h^-})\) 和 \((\tilde{M}_{1:h}, \tilde{\Theta}_{h^+})\) 上的动态规划（Algorithm 1），显著降低了计算复杂度。

## 三、实验设计

- **数值示例（VI-A）**：\(n=2\)、\(H=2\) 的二维JCCO问题，基线共享为一步观测延迟，通信动作空间为 \(\{0,1\}\)，逐步展示了四步求解流程（枚举通信策略→构造扩展问题→计算滤波器和Riccati方程→搜索最优通信策略）。
- **系统性实验（VI-B）**：
  - 四种信息结构作为基准场景：
    - Single-Controller（单控制器+延迟分享）
    - Turn-based-Controllers（轮流控制器）
    - One-Step-Delay（一步延迟共享）
    - One-Direction-One-Step-Delay（单向一步延迟共享）
  - 参数设置：\(n=3\)，\(H=6\) 或 \(7\)；通信成本系数 \(\alpha \in \{0.05, 0.1, 0.2, 0.5\}\)；10个随机种子（系统矩阵、噪声方差等从中随机采样）。
- **评估内容**：测量不同 \(\alpha\) 下的控制成本与通信成本分解、总成本变化，以及不同通信策略（不同额外共享元素数量N）的表现对比。
- **未提及**：论文未设置与已有方法（如深度强化学习学习通信方法[8,9]）的实证对比基准。

## 四、资源与算力

- **论文未明确说明所用算力资源**：未报告GPU型号、数量、训练时长或任何计算资源信息。
- 从方法性质看，本文方法（Riccati方程+有限枚举通信策略）属于**解析/半解析算法**，计算量远小于学习类方法；但其复杂度随通信动作空间大小呈组合增长（如数值示例中需枚举16种通信策略），可能存在状态空间爆炸问题。

## 五、实验数量与充分性分析

- **实验数量**：
  - 一个详细的数值示例（该示例证明了算法步骤的可行性）。
  - 系统性实验覆盖4种信息结构 × 4个α值 × 10个随机种子 = **160组独立运行**。
  - 图2中还考察了不同通信策略（不同共享元素数量N）与α的交互影响。
- **充分性评价**：
  - **优点**：覆盖了多种典型信息结构，对通信成本权重α进行了参数扫描，随机种子确保了结果的统计可靠性。
  - **不足**：
    - 缺少**消融实验**来独立验证各假设（如III.2、III.4）在正面对实验结果中的贡献。
    - 未与已有方法（如学习类通信策略算法）进行对比，无法体现本方法的相对优势（如计算时间或样本效率方面）。
    - 闭环通信策略部分（§V）**没有对应的数值实验**，其可行性仅以理论形式出现。
    - 所有系统均为小规模（\(n=2,3\)，\(H=6,7\)），未测试更大规模系统下的扩展性。

## 六、主要结论与发现

1. **结构条件**：在PN基线IS + 有影响必有观测 + 无用动作不共享的条件下，JCCO问题在任意开环通信策略下仍保持PN结构，最优控制策略可保持线性；违反任一条件，最优策略可能非线性甚至不存在。
2. **闭式解**：对于开环通信策略下的JCCO，最优控制策略可通过一组闭式Riccati方程高效求解，其计算复杂度远低于一般CIB框架下的无限维DP。
3. **独立副产品**：该动态规划方法还提供了一种解决PN信息结构+输出反馈下分散LQG问题的新途径，推进了[17]的成果。
4. **闭环扩展**：借助严格扩展技术，可将方法推广至闭环通信策略，得到比一般CIB信念DP更低维的可处理DP。
5. **实验验证**：数值实验表明，通信成本权重α直接影响最优共享量：α越小，共享信息越多，控制成本越低，总成本也越低；反之，α较小时更多共享的通信策略更优，α较大时更少共享更有利。

## 七、方法优点

- **理论严谨性**：对每个关键条件均给出必要的反例验证（非线性/无解），从正反两面严谨论证了结构条件的重要性，论证层次清晰。
- **技术新颖性**：首次将PN信息结构与SI-CIB条件之间的连接系统化，利用这一联系将分散LQ控制问题转化为可解的有限维DP，突破了[17]中非凸优化的瓶颈。
- **简化计算**：所得Riccati方程为闭式递推，避免了无限维CIB信念DP，实用性显著提高，且为后续研究提供可直接调用的算法。
- **覆盖范围广**：从开环到闭环通信策略的扩展使得方法覆盖面更广，潜在应用场景更多。
- **理论与实验结合**：既有严格的理论推导，也有可复现的数值演示与参数实验。

## 八、不足与局限

- **强假设局限**：
  - 系统限于线性动力学、高斯噪声、二次成本，实际应用中非线性/非高斯场景无法直接套用。
  - Assumption III.2和III.4尽管在证明上必要，但在实际多智能体系统中"有影响必可观测"和"无用动作不共享"不一定总能满足，限制了应用范围。
- **实验验证薄弱**：
  - 仅测试了无噪声相对较小规模系统（\(n\leq 3\)），未展示中大规模场景下的计算可行性。
  - 缺少对闭环通信策略部分的数值模拟，论文的核心贡献之一（§V）缺乏实证背书。
  - 未与业界主流的“学习如何通信”方法（如深度多智能体强化学习）进行成本或计算效率对比。
- **计算复杂度仍可能过高**：开环策略情形下需枚举全部通信动作组合，通信动作空间为组合爆炸时（如智能体数增加或私有信息维度增大），枚举开销不可忽略。
- **未讨论非合作场景**：论文面向“团队最优”单一目标，未考虑代理间存在利益冲突时的博弈情形。
- **未讨论鲁棒性或模型参数不确定性问题**：Riccati方程解对模型参数敏感，文中未做鲁棒性分析。

（完）
