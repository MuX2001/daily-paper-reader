---
title: "Towards safe and optimal flight: Viability Kernel MPC for Fully Actuated Multirotor"
title_zh: 迈向安全与最优飞行：全驱动多旋翼的生存核模型预测控制
authors: "Massimiliano Bertoni, Alberto Piccina, Gianni Lunardi, Elias Fontanari, Andrea Del Prete, Angelo Cenedese, Giulia Michieletto"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.25459v1"
tags: ["query:control"]
score: 9.0
evidence: 结合可行域核的模型预测控制用于多旋翼安全最优飞行
tldr: 针对工业无人机在非结构化环境中安全导航与性能优化难以兼顾的问题，提出一种用于全驱动多旋翼的可行性核MPC轨迹生成方法。该方法在MPC框架内引入可行性理论，并利用数据驱动方式动态计算轴对齐边界盒来强制避障，避免离线可达性分析。仿真显示在杂乱环境中能成功导航并满足实时计算要求。工作为全驱动多旋翼提供了兼顾安全与最优的形式化保证和高效实现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.25459v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 692, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25459v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 897, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25459v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1784, \"height\": 1554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.25459v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1870, \"height\": 1217, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.25459v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 736, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.25459v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 242, \"label\": \"Table\"}]"
motivation: 工业多旋翼在复杂环境中需要形式化安全保证，同时兼顾最优性能与实时计算，离线可达性分析成本高、难以在线应用。
method: 基于可行性核MPC，利用数据驱动动态计算轴对齐边界盒强制避障约束，在MPC框架内生成安全位姿轨迹，无需离线可达性分析。
result: 在全驱动倾斜六旋翼仿真中，方法成功穿越杂乱环境，并满足实时计算性能要求。
conclusion: 证明了可行性核MPC能够在保证形式化安全的同时兼顾最优性与计算效率，适合工业飞行应用。
---

## 摘要
工业空中机器人在非结构化环境中的导航需要安全保障，同时优化性能和计算效率。本文提出了一种在模型预测控制（MPC）框架内为全驱动多旋翼生成安全位姿轨迹的方法，结合了生存理论与数据驱动方法。通过动态计算的轴对齐包围盒实现避障，提供形式化的安全保障，无需详尽的离线可达性分析。在全驱动倾斜六旋翼上的数值模拟验证了该方法，展示了在杂乱环境中成功导航并具备实时计算性能。

## Abstract
Industrial aerial robotics demands safety guarantees for navigation in unstructured environments while optimizing performance and computational efficiency. This paper presents a method for generating safe pose trajectories for fully actuated multirotors within a Model Predictive Control (MPC) framework, leveraging both viability theory and data-driven methods. Obstacle avoidance is enforced through dynamically computed axis-aligned bounding boxes, providing formal safety guarantees without exhaustive offline reachability analysis. Numerical simulations on a fully actuated tilted hexarotor validate the approach, demonstrating successful navigation in cluttered environments with real-time computational performance.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义（研究动机与背景）

- **核心问题**：工业多旋翼在非结构化、杂乱环境中的安全导航是一个关键挑战，但目前大多数方法存在两类缺陷：
  - **仅保证短期规避，不保证长期生存性**：许多控制策略只关注当前时刻的避障，而无法证明系统在未来所有时刻都能保持安全。
  - **离线可达性分析计算代价高**：传统安全保证方法需要对高维非线性系统进行穷举式可达性分析，难以扩展到动态变化的环境，也无法满足实时性要求。
- **研究动机**：需要一种既能提供**形式化安全保证**（而非反应式安全），又能**兼顾最优控制性能**和**计算效率**的方法，使多旋翼能够在人类附近或杂乱环境中可靠部署。
- **整体含义**：本文将生存理论（viability theory）引入MPC框架，通过离线近似生存核（viability kernel）并嵌入在线MPC终端约束，实现了"可认证的安全中止能力"与"实时最优轨迹生成"的统一。这是朝着工业级安全飞行控制迈出的基础性一步。

## 2. 论文提出的方法论

- **核心思想**：将**生存理论**与**数据驱动方法**结合，基于预计算的生存核近似集合，在MPC预测时域末端强制要求状态落入该集合，从而保证即使MPC未来连续不可行，系统仍能安全抵达平衡点。
- **关键步骤与公式流程**：
  1. **生存核定义**：对离散动力学系统 xₖ₊₁ = f(xₖ, uₖ)，生存核 V₀ 是从中可无限期维持状态在安全集 X 内的最大状态子集。
  2. **VBOC方法近似生存核**：采用论文[9]提出的Viability-Boundary Optimal Control方法，将生存核求解转化为有限时域最优控制问题（OCP），通过最大化初始状态沿指定方向的范数来逼近生存核边界 ∂V。关键设置包括：
     - 成本向量 a 与约束矩阵 S 的选择（式6）；  
     - 输入集 U 由螺旋桨饱和约束定义时，状态约束为障碍物环境下的轴对齐包围盒（AABB）成员关系；  
     - 对 AABB 参数 b、初始姿态 ξ₀ 和速度方向 d 进行均匀随机采样，生成训练数据集。
  3. **神经网络编码**：训练一个两层 512 单元、GeLU 激活的 MLP，学习映射 φ(η, d, b)，即给定方向角、速度方向和包围盒参数下最大可行速度范数 γ。若 γ = φ(η, d, b)，则状态 (ξ, γd) 位于生存核边界上。
  4. **MPC终端约束嵌入**：在标准MPC问题（式8）中添加硬终端约束 x_N ∈ X_N，其中 X_N = {x | φ(η, d, b⋆) ≥ ||ν||₂} ∩ V̂，即预测终端状态必须落在生存核内部。
  5. **动态碰撞规避**：在每个MPC周期，采用**贪心收缩算法**计算以当前位置为中心的最大无碰撞AABB（B⋆），该算法优先选择能使体积减小的最小的面位移，重复迭代直至所有面均无碰撞，保证计算实时性。
  6. **安全中止机制**：若MPC不可行，则应用上一次可行解的控制序列并计数；当计数达到预测时域 N 时，从最后一个可行终端状态 x⋆_N 出发执行一个预计算的中止OCP（式11），将系统平稳驱动至平衡状态。

## 3. 实验设计

- **平台**：α-Ted6R——共面星形结构、相邻螺旋桨轴反向倾斜的全驱动六旋翼（可独立控制六自由度）。
- **场景设置**：
  - 起点 x₀ = 0₁₂，目标位姿 ξr = (2, 0, 5, 0, 0, 0)ᵀ，零速度。
  - 环境中有3个静态箱形障碍物（见表I），尺寸与中心位置各不相同，覆盖 x、y、z 不同方向。
  - 仿真总时长 5s，MPC采样周期 20ms，预测时域 N = 60。
- **数据集（离线生存核训练）**：200,000 个样本，由不同 AABB 参数、初始姿态和速度方向组合下的 OCP（式5）解构成，样本来自均匀随机采样。
- **验证指标**：
  - 对照 OCP 精确解的 NN 逼近质量（图2：仅沿y轴运动的2D切片对比）。
  - MPC 闭环轨迹跟踪效果（图3：3D快照展示 AABB 动态更新与避障过程；图4：位置和线速度随时间变化）。
  - 计算性能：单次 MPC 迭代的平均时间等统计量。
- **Benchmark / 对比**：文中未与任何其他规划或控制方法（如势场法、RRT、其他MPC变体）进行对比，主要验证自身框架的可行性与有效性。

## 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量、训练时长、CPU 配置等算力资源。
- 仅提及：NN 用 200,000 个样本训练，网络规模为主干两层×512 单元，通过数据驱动方式离线编码生存核。
- 在线 MPC 计算部署在 Python + CasADi + ACADOS 环境中，采用 RK4 积分，时间步长 0.1ms，足以满足 20ms 采样周期。

## 5. 实验数量与充分性

- **实验数量**：
  - 1 个使用场景（单起止点 + 3 个静态障碍物）。
  - 1 个 NN vs OCP 的精度对照（限制在 y 轴平面内）。
  - 未包含不同障碍物布局、不同目标点、不同初始状态的多场景实验。
  - 未包含消融实验（如无生存核终端约束的MPC对比、无安全中止机制的表现、不同 NN 规模/数据集量对精度的影响）。
- **充分性与客观性评价**：
  - **优点**：仿真验证结果清晰，展示了系统成功到达目标并回避障碍，实时性数据（平均迭代 1ms）具有说服力。
  - **不足**：实验量偏少，缺乏与现有方法（如势场法、RRT、标准 NMPC）的公平对比，缺乏统计显著性和边际案例分析；仅在单一确定性环境中验证，无法充分证明方法的鲁棒性和泛化能力。NN 精度仅在一个二维切片上做了视觉对比，缺乏定量误差统计。

## 6. 论文的主要结论与发现

- **主要结论**：
  - 提出的生存核 + MPC 框架可以成功完成杂乱环境下的全驱动多旋翼位姿轨迹跟踪任务，到达目标位姿且全程保证碰撞安全。
  - 神经网络可以有效编码非线性系统的生存核近似，且能嵌入 MPC 硬约束，使实时求解成为可能。
  - 动态 AABB + 贪心收缩策略提供了一种轻量、实时的碰撞约束建模方法，避免了穷举可达性分析。
  - 计算性能优异（平均单次迭代 1.00ms，最大 11.72ms），远低于 20ms 采样周期，满足实时性要求。
- **发现**：在 z 方向运动受阻（安全球与 AABB 面切触）时，MPC 会自动调整 x 方向速度来补偿，体现了轨迹优化的耦合协调能力。

## 7. 优点（亮点）

- **理论框架新颖**：将生存理论引入全驱动多旋翼的MPC控制，通过终端约束实现"长期安全"而非"瞬时规避"，这在文献中有显著的新意。
- **数据驱动的生存核近似**：用 NN 离线逼近生存核边界，在线评估成本极低，解决了传统可达性分析的计算瓶颈。
- **实用的避障建模**：动态 AABB + 贪心收缩算法简单高效，适合嵌入式实时实现，同时保留了形式化安全保证。
- **安全中止机制设计**：提供了 MPC 不可行时的备份方案，从最后可行终端状态执行预计算 OCP，增强系统鲁棒性和工程可部署性。
- **计算效率突出**：所报 1ms 级 MPC 迭代时间超越了多数同类安全 MPC 实现，具备实际应用潜力。

## 8. 不足与局限

- **环境模型受限**：只考虑静态障碍物，未涉及动态障碍物、移动目标或多机器人场景；AABB 对非凸/斜面障碍物可能过度保守。
- **安全保证的保守性**：NN 学习到的生存核是保守近似，可能拒绝部分可行状态，导致性能略差；贪心收缩算法得到的不是全局最优 AABB，在复杂环境中可能存在"次优收缩"问题。
- **实验覆盖不足**：
  - 缺少多场景、多起点、多目标点的系统评估；
  - 缺少与基线方法的对比实验；
  - 缺少对 NN 近似精度的系统性统计分析（仅在 2D 切片进行视觉对照）；
  - 缺乏真实硬件实验验证（仅数字仿真）。
- **假设与简化**：
  - 假设 AABB 参数 b 的上下界已知且固定；在实际中，感知误差和障碍物不确定性的鲁棒性未考虑；
  - 模型参数（质量、惯性、执行器饱和）未考虑不确定性或故障情形。
- **可扩展性疑点**：对更高维、更复杂动力学平台或环境，NN 训练与 OCP 离线求解的计算成本可能显著上升，文中未讨论这一扩展的复杂度问题。

## （完）
