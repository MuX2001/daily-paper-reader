---
title: A New Approach for Feedback Stabilization and its Application for Data-Driven Control of Polynomial Systems
title_zh: 一种反馈稳定的新方法及其在多项式系统数据驱动控制中的应用
authors: "Diego de S. Madeira, Joao Gabriel N. Silva, Wilkley B. Correia, Antonis Papachristodoulou"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.10158v1"
tags: ["query:control"]
score: 9.0
evidence: 非线性多项式系统渐近镇定新条件与数据驱动SOS设计
tldr: 非线性系统数据驱动镇定是近年热点，但传统SOS交替方法需要预设CLF，且对噪声敏感。受耗散性控制启发，提出新的状态反馈镇定充分条件，并为此设计两种基于SOS的迭代数据驱动控制器求解算法。新算法无需CLF初始化，可直接利用含噪测量数据，数值示例显示与传统方法相比更简便且表现更优。为多项式系统数据驱动控制提供了一种无需模型、抗噪的新框架，拓展了耗散性理论的应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10158v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 695, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10158v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 702, \"height\": 583, \"label\": \"Figure\"}]"
motivation: 传统SOS交替方法在数据驱动控制器设计中需要初始化CLF，且对噪声测量适应性差，本文旨在解决这些局限。
method: 基于耗散性理论提出新的镇定充分条件，并设计两种迭代SOS优化程序用于含噪数据下的状态反馈设计。
result: 数值示例表明新方法无需CLF初始化，能处理含噪测量，相比传统交替方法更具适用性与优势。
conclusion: 所提框架为多项式系统数据驱动镇定提供了更简便高效的途径，扩大了耗散性控制的应用范围。
---

## 摘要
受最近耗散性控制发展的启发，本文提出了使用状态反馈对非线性系统进行渐近稳定的新的充分条件。我们证明了这一新框架非常适合使用含噪声测量数据对多项式系统进行数据驱动控制，该主题近年来引起了相当大的关注。利用所提出的框架，提供了两种基于数据的状态反馈设计的迭代程序，使用平方和（SOS）优化。本文克服了传统基于交替（D-K）过程的SOS控制器设计方法的典型局限性，例如需要为控制李雅普诺夫函数（CLF）提供初始化。数值例子证明了新策略的适用性和优势。

## Abstract
Inspired by recent developments in dissipativity-based control, this work proposes new sufficient conditions for asymptotic stabilization of nonlinear systems using state feedback. We prove that this new framework is well suited for data-driven control of polynomial systems using noisy measurements, a topic that has lately attracted considerable attention. Two iterative procedures for data-based state feedback design are provided using the proposed framework, that use sum-of-squares (SOS) optimization. Typical limitations of conventional SOS methods based on alternating (D-K) procedures for controller design, such as the need to provide an initialization for a control-Lyapunov function (CLF), are overcome in this paper. Numerical examples demonstrate the applicability and the advantages of the new strategies.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：数据驱动控制是近年来非线性系统理论的研究热点，尤其是在系统模型不精确已知、但可获取丰富测量数据的场景下。多项式系统因能较好地逼近非线性行为，并可采用平方和（SOS）优化求解，受到广泛关注。
- **现有方法的痛点**：基于交替（D-K）迭代的 SOS 控制器设计方法（如 [10]、[11]、[13]）存在典型局限：需要为控制李雅普诺夫函数（CLF）提供初始化（通常通过模型线性化获得），或需要预先指定虚构输出变量，这些假设在不同场景下难以保证。
- **本文目标**：提出一种新的反馈镇定充分条件框架，该框架天然适配含噪数据驱动的多项式系统控制，能够克服传统 SOS 方法对 CLF 初始化和 D-K 交替迭代的依赖，实现无需模型先验的稳定控制器设计。

## 2. 方法论

### 2.1 核心思想

- 受耗散性控制框架（[16]）启发，提出新的闭环镇定条件：**不直接寻找控制律以满足耗散不等式，而是寻找一组与候选 CLF 兼容的闭环动态**。
- 核心定理（Thm. 1）：若存在正定函数 \(V(x)\)、\(T(x)\)、常数矩阵 \(R\succ 0\)，使得不等式 (12) 成立，且 \(\Delta_{cl}(x)\succeq 0\)（式中 (13)），则状态反馈 \(u(x)\) 渐近镇定该系统。
- 关键技术在于令 \(R=\gamma I_n\)（γ>0），将非线性耦合项线性化，转化为 SOS 可解的形式（条件 (21)）。

### 2.2 两种数据驱动控制算法

- **策略一（算法 1）：参考动态周围的鲁棒镇定**
  - 利用数据构建参考模型 \((A^\star, B^\star)\)（式 (38)-(40)）。
  - 定义集合 \(C_d\)（式 (44)）：满足耗散条件的动态集合；通过引理 3 证明在条件 (47)-(48) 下 \(C_d\supseteq C\)（\(C\) 为与数据一致的全部系统集合），从而将数据驱动问题转化为鲁棒镇定问题。
  - 采用引理 4 的迭代条件（式 (57)）保证 \(\Delta_{cl,k+1}\succeq \Delta_{cl,k}\) 单调不减，逐步改进控制器，并以条件 (59) 为停止判据。

- **策略二（算法 2）：直接数据驱动控制**
  - 数据直接通过 S-过程（S-procedure）进入稳定性条件，不构建参考模型。
  - 命题 1 给出基于 SOS 的充分条件（式 (61)-(63)），通过矩阵椭球（7）描述数据一致集。
  - 同样采用类似引理 1 的迭代条件（引理 5），保证 \(\bar{\Delta}_{cl,k}\) 单调不减。
  - 停止判据复用策略一的 (59)（相较 (62) 效率更高、保守性更低）。

### 2.3 共同特点

- 所有 SOS 条件均限于区域 \(E(L,1)\) 内验证，可估计闭环吸引域 \(E(V,\rho)\)。
- 两个算法均包含三步：STEP 1 初始求解、STEP 2 迭代改进、STEP 3 吸引域最大化。

## 3. 实验设计

- **测试系统**：采用 [22] 中的二维多项式系统（\(\delta_{fg}=3\)），状态维度 \(n=2\)，\(Z(x)\) 包含 9 个单项式（从 \(x_1,x_2\) 到 \(x_1^2x_2\)、\(x_2^3\) 等），\(W(x)=1\)。
- **数据采集**：仿真时间区间 \([t_I,t_F]=[0,500]\)，初始状态 \(x(t_I)=[-2\ 2]^\top\)，控制激励 \(u=1.1\sin(3t)\)，采集 \(T=6000\) 个样本（采样时间非均匀）。
- **噪声模型**：对 \(\dot{x}(t)\) 的每个采样施加噪声 \(d(t)=\frac{\delta}{\sqrt{n}}[\sin(5t)\ \cos(5t)]^\top\)，\(\delta=0.04\)，满足假设 2，且 \(R_D R_D^\top=T\delta^2 I\)。
- **基准对比**：**未直接对比其他方法**。仅独立演示了两种算法的表现，通过与文献中方法（[10]、[11]、[13]）的定性对比说明优势。

## 4. 资源与算力

- **硬件**：文中未明确提及 GPU 型号或数量等算力信息。
- **软件与求解器**：使用 MATLAB 环境下的 SOSTOOLS [20] 和 MOSEK 求解器。
- **计算复杂度指标**：
  - 算法 1：STEP 1 含 34 个决策变量、7 个 SOS 约束；STEP 2 含 76 个决策变量、7 个 SOS 约束；停止判据测试含 97 个决策变量、7 个 SOS 约束；共迭代 \(\kappa=7\) 次，**总耗时 3.2286 秒**。
  - 算法 2：STEP 1 含 75 个决策变量、11 个 SOS 约束；STEP 2 含 75 个决策变量、14 个 SOS 约束；停止判据测试含 55 个决策变量、12 个 SOS 约束；在**第一次迭代即得解**，**总耗时 2.3103 秒**。

## 5. 实验数量与充分性

- **实验数量**：仅使用一个数值算例、一组数据集、两种算法各验证一次。
- **消融实验**：无。
- **对比实验**：无（未与文献 [10]-[13] 等方法在同一算例下进行定量对比）。
- **充分性评估**：
  - **不足**：单一算例和单一噪声水平（\(\delta=0.04\)）不足以全面验证方法在不同系统维度、不同噪声强度、不同数据量下的泛化能力。
  - **缺失**：未讨论激励信号的选择对数据质量及控制器性能的影响；未提供与已有方法在同一算例下的定量对比（如 DOA 大小、计算时间对比）。
  - **优点**：对两种算法各自的复杂度（决策变量数、SOS 约束数、迭代次数、耗时）有详细报告，便于横向比较。

## 6. 主要结论与发现

- 提出的新镇定框架与数据矩阵椭球表示（式 (7)、(9)、(34)）天然兼容，为数据驱动控制提供了新的理论桥梁。
- 算法 1 每次迭代的计算复杂度较已有 SOS 方法更低，不需要 CLF 初始化，也不需要 D-K 交替过程。
- 算法 2 通常能在极少数迭代（甚至第一次迭代）内获得稳定控制器，对数据本身具有较强的鲁棒性，但单次迭代计算负荷更高。
- 两种算法都能在含噪测量下同时给出控制器 \(u(x)\) 以及吸引域估计 \(E(V,\rho)\)。
- 文中指出所提方法同样适用于 d(t) 为过程扰动（而非仅测量噪声）的情形，机制相同但数据采集方式不同。

## 7. 优点

- **理论创新**：基于耗散性思想提出新的闭环动态集合刻画方式，区别于传统"给定系统找控制律"的思路，而是"给定 CLF 找兼容的动态集"，更适合数据驱动场景。
- **克服传统痛点**：不需要 CLF 初始化、不需要 D-K 交替迭代、不需要模型线性化，显著降低了应用门槛。
- **算法互补性**：算法 1 轻量高效，适合对计算资源敏感的场景；算法 2 收敛快、对数据稳健，适合在线或快速原型设计。
- **完备的 SOS 可解化**：通过 \(R=\gamma I_n\) 的设定和引理 1/4/5 保证迭代过程中 \(\Delta_{cl}\) 单调不减，提供了严格的迭代收敛保障。
- **吸引域估计**：提供了系统的吸引域估计方法（条件 (60)），工程实用性强。
- **代码开源**：作者提供 MATLAB 代码仓库 [21]，便于复现和后续研究。

## 8. 不足与局限

- **实验覆盖有限**：仅一个二维算例，未在更高维、更复杂的多项式系统上验证；不同噪声强度（\(\delta\)）、不同数据规模（\(T\)）的影响未被探讨。
- **缺乏定量对比**：未与文献中已有方法（如 [10]、[11]、[12]、[13]）在同一基准上对比吸引域大小、计算时间、保守性等指标，削弱了"优势"结论的定量说服力。
- **假设条件限制**：方法依赖假设 1（已知平衡点和 f、g 的最高次数）和假设 2（噪声矩阵有界且已知 \(R_D\)），实际应用中需要额外的先验知识。
- **保守性**：所有条件均为充分条件，虽避免了 D-K 迭代，但保守性水平未与现有方法系统比较；迭代停止判据 (59) 本身也是充分条件，可能错过可行解。
- **应用限制**：仅讨论连续时间系统；非零平衡点镇定、离散时间系统、状态导数有界约束等情形（作者列出的未来方向）尚未覆盖。
- **可扩展性风险**：算法 2 的单次迭代所需决策变量和 SOS 约束数随系统维度和多项式次数增长显著（本例中已达 75 个变量、14 个约束），扩展到更高维系统时可能面临可扩展性挑战。

（完）
