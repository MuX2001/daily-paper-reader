---
title: Path Integral Value Matching for Linear Quadratic Stochastic Optimal Control
title_zh: 线性二次随机最优控制的路径积分值匹配
authors: "Bangyan Liao, Chenglei Yu, Yuchen Yang, Chuanrui Wang, Zhisheng Song, Peidong Liu, Tailin Wu"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10777v1"
tags: ["query:control"]
score: 9.0
evidence: 利用路径积分控制求解线性二次随机最优控制的值函数方法
tldr: 针对线性二次随机最优控制（LQ-SOC）中现有策略方法依赖全轨迹模拟导致计算昂贵且不稳定，以及标准路径积分控制（PIC）高方差的问题，本文提出基于值的路径积分值匹配（PI-VM）算法。通过对路径积分进行截断与边缘化推导出值函数的时间递归形式，结合时序差分学习与Girsanov定理实现离策略训练。在低维基准上以数量级效率达到SOTA精度，在高维场景有效缓解模式崩溃，为复杂SOC问题提供可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10777v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 365, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.10777v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1435, \"height\": 776, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 559, \"height\": 346, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 848, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 739, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 965, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 971, \"height\": 142, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10777v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 814, \"height\": 455, \"label\": \"Table\"}]"
motivation: 现有策略方法计算开销大且不稳定，标准PIC也受高方差困扰，需更高效稳定的值函数方法。
method: 截断并边缘化路径积分导出递归值函数，用TD学习更新，结合Girsanov定理与经验回放实现离策略训练。
result: 在低维SOC基准上精度相当但效率高一个数量级，高维场景缓解模式崩溃。
conclusion: 提出可扩展的PI-VM算法，为复杂SOC问题提供高效稳定的解决思路。
---

## 摘要
线性二次随机最优控制（LQ-SOC）为控制含噪声动态系统建立了基础框架，近期在机器学习领域重新引起了关注。然而，当前最先进的基于策略的方法由于严重依赖全轨迹模拟，面临高昂的计算成本和稳定性问题。为克服这些限制，我们通过重新审视路径积分控制（PIC），提出向基于值的方法的范式转变。尽管标准PIC与基于策略的方法一样存在高方差瓶颈，但我们发现，通过截断和边缘化原始路径积分公式，可以推导出值函数的时间递归形式。在此理论基础上，我们提出了路径积分值匹配（PI-VM）算法。具体而言，我们采用时序差分学习来逼近递归值动态，并进一步将Girsanov定理与经验回放相结合，以实现离策略训练。我们在各种SOC基准和采样任务上将PI-VM与最先进的基于策略的方法进行基准比较。实证结果表明，在低维设置中，PI-VM以数量级的效率提升达到了最先进的精度，同时在高维场景中有效缓解了模式崩溃。因此，PI-VM为解决复杂SOC问题提供了一种可扩展的解决方案。

## Abstract
Linear Quadratic Stochastic Optimal Control (LQ-SOC) establishes a fundamental framework for steering noisy dynamical systems and has recently gained renewed interest in the machine learning community. However, current state-of-the-art policy-based methods suffer from prohibitive computational costs and instability due to their heavy reliance on full-trajectory simulation. To overcome these limitations, we propose a paradigm shift toward a value-based approach by revisiting Path Integral Control (PIC). Although standard PIC suffers from the same high-variance bottleneck as policy-based methods, we discover that by truncating and marginalizing the original path integral formulation, we can derive a temporal recursive form of the value function. Building upon this theoretical foundation, we propose the Path Integral Value Matching (PI-VM) algorithm. Specifically, we employ temporal-difference learning to approximate the recursive value dynamics, and further integrate the Girsanov theorem with experience replay to enable off-policy training. We benchmark PI-VM against SOTA policy-based methods across various SOC benchmarks and sampling tasks. Empirical results demonstrate that PI-VM matches SOTA precision with an order-of-magnitude efficiency gain in low-dimensional settings, while effectively mitigating mode collapse in high-dimensional scenarios. Consequently, PI-VM offers a scalable solution for solving complex SOC problems.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 核心问题与整体含义（研究动机与背景）

- **研究对象**：线性二次随机最优控制（LQ-SOC）问题，即在含噪动态系统中设计最优控制信号以最小化期望代价。
- **现有瓶颈**：
  - 当前主流**基于策略（policy-based）**的方法（如 Iterative Diffusion Optimization）通过最小化参数化路径测度与最优路径测度之间的散度来学习控制策略，但严重依赖 **on-policy 全轨迹前向模拟**，导致计算开销大、梯度方差高、训练不稳定。
  - 经典**基于值（value-based）的路径积分控制（PIC）**虽然理论上可通过 Feynman-Kac 引理将 HJB 方程转化为采样问题，但同样依赖**全轨迹蒙特卡洛采样**，在高维噪声环境中方差爆炸，实际不可扩展。
- **核心挑战**：两种范式都受困于“全轨迹模拟带来的高方差与高成本”，需要一种能够**避免长程随机模拟**的稳定训练方案。
- **本文立场**：提出从“策略搜索”转向“值函数学习”的范式转变，通过**截断并边缘化路径积分**得到值函数的时间递归形式，从而将长视界积分替换为短时程自举（bootstrapping），实现高效稳定的 SOC 求解。

### 2. 方法论：核心思想与关键技术

- **核心思想**：将原始路径积分表示（式 5）分解为区间 [t, s] 与 [s, 1]，利用条件期望的塔性质，推导出值函数的**连续时间递归形式**（Proposition 3.1）：
  
  \[
  \exp(-V(X_t, t)) = \mathbb{E}_{P^0}\left[\exp\left(-V(X_s, s)\right)\exp\left(-\int_t^s f(X_r, r)dr\right) \mid \mathcal{F}_t\right]
  \]

  — 这意味着只需局部短时积分即可通过自举逼近值函数。

- **PI-VM 算法（Path Integral Value Matching）**：
  - **TD 学习**：将值函数参数化为神经网络 Vθ(x, t)，定义**路径积分 TD 损失**：
    \[
    \ell(\theta) = \left(V_\theta(x, t) - \hat{V}_\theta(x, t, s)\right)^2
    \]
    目标值通过 N 条 M 步短轨迹的蒙特卡洛估计获得（式 13），其中使用了 running cost 积分与后继值函数（或终端代价）。
  - **离策略训练**：引入 **Girsanov 定理**，将基础测度 P0 与受控测度 Pu 之间的转移密度用指数形式表示（式 15），得到**离策略 PI-VM 损失**（式 17），使训练可以在经验回放缓冲区中灵活采样，兼顾探索与利用。
  - **经验回放（Experience Replay）**：用当前最优控制 u = −σᵀ∇Vθ 滚动生成轨迹，对每个状态生成 N 条短分支，存储转移元组，训练时均匀采样小批量更新值网络与目标网络（EMA 更新）。
- **理论保证**：
  - **Theorem 3.3**：在运行代价有正下界等温和假设下，递归更新收敛到最优值函数。
  - **Proposition 3.4**：方差分解展示递归估计相较于全轨迹蒙特卡洛估计的方差减小量。
  - **Theorem 4.3**：当采样控制 u 接近最优控制 u* 时，离策略估计器的条件方差上界以 κ = sup‖u − u*‖² 线性衰减，即接近最优时方差渐近消失。

### 3. 实验设计

- **任务/场景（共 5 个）**：
  - 三个**单峰 SOC 控制任务**：
    - Linear Ornstein-Uhlenbeck（线性 OU）
    - Quadratic OU（Easy 设置）
    - Quadratic OU（Hard 设置）
  - 两个**多峰采样任务**：
    - 20 维高斯混合模型（GMM）采样，含 Dist.（远/近）与 Var.（大/小）四种难度组合
    - 50 维 Many Well 多稳态势阱采样（正文描述维度为 50；附录中参数表写 d=10，存在文本不一致）

- **对比方法（7 个 policy-based 基线）**：Relative Entropy (RE)、Cross Entropy (CE)、Variance (VAR)、Log-Variance (LVAR)、Adjoint Matching (AM)、Stochastic Optimal Control Matching (SOCM)、SOCM with Adjoint loss (SOCM-A)，以及 REINFORCE。

- **评估指标**：以 ground truth 最优控制可解析求解为前提下，采用 **control L2 Error**（对比学习策略与解析最优控制之间的期望距离），并记录**单次运行时耗**。

### 4. 资源与算力

- 论文使用**单张 NVIDIA RTX 4060 GPU** 完成全部实验。
- 每个实验在 **4 个不同随机种子**上重复并报告均值与标准差。
- 论文**未明确报告**训练总时长、GPU 总耗时或具体的 epoch 数量级等信息（仅给出每次迭代/单次推理耗时，如 PI-VM 约 7–21 ms）。

### 5. 实验数量与充分性

- **实验数量**：
  - 5 个基准任务的定量对比（表 1、2、4）；
  - 3 个任务×多个基线的控制精度与时耗比较；
  - 1 个高维扩展性测试（d = 10、20、80、150、200）；
  - 1 个 50 维模式覆盖可视化分析（Many Well）；
  - 3 组消融实验：前向步数 M 与样本数 N（表 3）、网络深度（附录 D.1）、批量大小 B 与样本数 N（附录 D.2、D.3）。
- **充分性评估**：
  - **相对充分**：覆盖了低维、高维、单峰、多峰、消融、可视化多类场景，既报告了控制误差，也报告了运行效率，结论支撑较完整。
  - **潜在不足**：
    - 缺少与其它 value-based/PIC 方法（如 MPPI）的直接对比。
    - 高维实验仅针对 Quadratic OU Easy 任务，未覆盖稀疏高维或多模态高维场景。
    - 多峰任务的维度设置（GMM 仅 20 维、Many Well 图纸为 50 维）相对受限。
    - 时耗对比缺少明确的“达到目标精度所需总训练时间”的整体对比（仅给单步时耗）。

### 6. 主要结论与发现

- **效率优势**：在低维 SOC 任务上，PI-VM 以与 SOTA 相当（或更优）的精度，实现了**一个数量级（10–20×）**的速度提升（如 Quadratic OU Easy 单次耗时 7 ms 对比 AM 的 21 ms、SOCM 的 183 ms）。
- **精度优势**：在 Quadratic OU（Hard）等非凸困难场景中，SOCM 与 SOCM-A 无法收敛，而 PI-VM 仍能逼近全局最优值函数并取得最低控制误差（0.00078 vs. 基线最低 0.00674）。
- **高维可扩展性**：在维度提升到 200 时，SOCM 在 d=80 时即显存溢出（OOM），AM 在高维发生发散，而 PI-VM 保持稳定收敛、恒定的实时推理速度。
- **模式覆盖能力**：在多峰采样任务中，PI-VM 在 Small Variance（尖峰分布）的困难场景下仍保持低误差（如 GMM Far-Small 设置下误差 0.024 vs. AM 的 4.317），有效缓解模式崩溃；在 Many Well 可视化中采样点严格集中于稳态平衡点，优于 AM 的高方差散乱采样。

### 7. 优点

- **理论创新**：从路径积分出发推导出连续时间递归值函数形式，证明收敛性与方差衰减，理论链条完整，为 TD 学习提供了物理含义清晰的损失函数。
- **范式突破**：将 SOC 求解从“全轨迹模拟 + 策略优化”转换为“短时程自举 + 值函数回归”，避免了长视界积分与随机反向传播，从根本上降低了方差与计算量。
- **离策略训练设计**：利用 Girsanov 定理对经验回放中的轨迹进行数学上有根据的加权，实现 on-policy 利用与 off-policy 探索的灵活切换，并证明控制接近最优时估计方差趋零。
- **工程稳健性**：目标网络 + EMA 更新保证训练稳定；对批次大小、网络深度、采样步数等超参数具有较好的鲁棒性（消融结果显示性能差异小）。
- **高维可扩展性**：在 d=200 时仍保持毫秒级推理与稳定收敛，突破了现有 policy-based 方法的显存瓶颈。

### 8. 不足与局限

- **控制信号需梯度计算**：作为 value-based 方法，最终需通过自动微分计算 ∇Vθ 来得到控制信号 u = −σᵀ∇V，这在高维或实时推理场景下本身有计算开销（论文结论中亦承认此局限）。
- **实验覆盖有限**：
  - 高维扩展仅测试至 d=200 且仅一条任务族（Quadratic OU），GMM 与 Many Well 的维度较低；
  - 未报告在多模态**高维（如 d>100）**下的模式覆盖表现。
- **基线选择偏向 policy-based**：未与 MPPI、Cross-Entropy PIC、有限差分 HJB 等 value-based 经典方法对比，可能影响“value-based 通用优势”结论的完整性。
- **指标口径问题**：以“单次迭代/单步耗时”对比效率不够全面；若计入轨迹生成、缓冲区维护等端到端训练总成本，结论可能需进一步验证。
- **未报告总训练时长**：缺少对“达到给定精度所需总时间/总样本/总迭代数”的整体对比，效率论证稍显薄弱。
- **文本不一致**：Many Well 任务维度在正文描述为 50，附录参数表标注为 10，影响可复现性。

（完）
