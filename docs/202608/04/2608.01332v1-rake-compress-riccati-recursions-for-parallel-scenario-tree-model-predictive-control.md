---
title: Rake-Compress Riccati Recursions for Parallel Scenario-Tree Model Predictive Control
title_zh: 用于并行场景树模型预测控制的耙压缩Riccati递归
authors: João Sousa-Pinto
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01332v1"
tags: ["query:control"]
score: 9.0
evidence: 针对场景树模型预测控制的并行求解方法与里卡第递推
tldr: 场景树模型预测控制需反复求解分支线性二次调节器子问题，标准树 Riccati 递归虽有线性工作量但依赖链随树高增长。本文提出基于 rake-compress 树收缩的代数精确并行求解器，通过 rake 与 compress 操作收缩任意树至根，再反向恢复所有系数，具有 O(N) 工作量与 O(log N) 跨度，不依赖树高与分支度，支持半正定对偶正则化，并给出 KKT 等价性证明与三个 JAX 实现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01332v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 314, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01332v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01332v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 915, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01332v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1876, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01332v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 276, \"height\": 279, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01332v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 584, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01332v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 679, \"height\": 719, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01332v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 631, \"height\": 212, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01332v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 792, \"height\": 258, \"label\": \"Table\"}]"
motivation: 标准场景树 MPC 的 Riccati 递归串行依赖树高，难以并行，需要代数精确的并行求解方法。
method: 基于 rake-compress 树收缩，用 rake 和 compress 操作组合条件二次函数，双向收缩与恢复。
result: 实现 O(log N) 跨度、O(N) 工作量，支持半正定正则化，证明与 KKT 等价，提供三个 JAX 包。
conclusion: 该方法为大规模场景树 MPC 提供可并行的高效求解框架，具有理论保证与开源实现。
---

## 摘要
场景树模型预测控制（MPC）通过有根树表示未来信息，并优化该树上的非预期策略。求解所得非线性规划的数值方法通常通过一系列分支线性二次调节器（LQR）子问题计算搜索方向。标准的树Riccati递归需要线性工作量，但其依赖链与树高成正比。我们提出了一种基于耙压缩树收缩的代数精确并行求解器。在独立的局部控制凝聚之后，其两种操作作用于表示条件二次函数的节点和边数据。耙操作消除一个叶子节点及其父边，将其缩减贡献添加到父节点数据中。压缩操作消除单子节点，并用一条边替换其两条相邻边，使用与链上并行Riccati方法相同的条件值组合。两者结合可将任意有根树收缩到其根；反向恢复收缩可得到每个Riccati系数、状态、控制和乘子。给定可复用的拓扑计划，具有$N$个节点和固定状态与控制维度的求解具有$O(N)$的算术工作量和存储量以及$O(\log N)$的跨度，与树高、平衡性和最大出度无关。该公式允许半正定对偶正则化，包括无正则化情况，且精确的线性规模提升覆盖了每个信息节点一个控制的标准场景MPC约定。我们证明了收缩恒等式及其与Karush-Kuhn-Tucker（KKT）系统的等价性。三个MIT许可的JAX包实现了双向收缩、对偶正则化LQR求解器以及面向用户的树结构最优控制原始对偶内点求解器。

## Abstract
Scenario-tree model predictive control (MPC) represents future information by a rooted tree and optimizes a nonanticipative policy over that tree. Numerical methods for solving the resulting nonlinear program typically compute their search directions through a sequence of branched linear-quadratic regulator (LQR) subproblems. The standard tree Riccati recursion requires linear work but has a dependency chain proportional to tree height.   We present an algebraically exact parallel solver based on rake-compress tree contraction. After independent local control condensation, its two operations act on node and edge data that represent conditional quadratic functions. A rake eliminates a leaf and its parent edge, adding their reduced contribution to the parent-node data. A compress eliminates a unary node and replaces its two adjacent edges by one edge, using the same conditional-value composition as parallel Riccati methods on a chain. Together they contract an arbitrary rooted tree to its root; reversing the contraction recovers every Riccati coefficient, state, control, and multiplier. Given a reusable topology plan, a solve with $N$ nodes and fixed state and control dimensions has $O(N)$ arithmetic work and storage and $O(\log N)$ span, independently of tree height, balance, and maximum out-degree.   The formulation allows positive-semidefinite dual regularization, including the unregularized case, and an exact linear-size lifting covers the standard scenario-MPC convention of one control per information node. We prove the contraction identities and equivalence to the Karush-Kuhn-Tucker (KKT) system. Three MIT-licensed JAX packages implement the bidirectional contraction, the dual-regularized LQR solver, and a user-facing primal-dual interior-point solver for tree-structured optimal control.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Rake-Compress Riccati Recursions for Parallel Scenario-Tree Model Predictive Control》的详细中文总结。

## 1. 论文核心问题与整体含义

- **研究动机**：场景树模型预测控制（MPC）将未来的不确定性建模为一棵有根树，并在树上优化非预期性（nonanticipative）控制策略。这种模型被广泛应用于随机电力调度、双控制（dual control）、多模态运动规划等场景。求解这类问题通常需要反复求解一系列分支线性二次调节器（LQR）子问题。
- **核心瓶颈**：标准的树Riccati递归虽然具有O(N)的线性工作量，但其前后向扫描的依赖链深度与树高成正比。即使引入层级并行，也无法缩短一条很长的“树干”（长链结构）。这使得在大规模、高度不规则场景树上进行实时控制变得困难。
- **整体目标**：本文旨在提出一种**代数精确**的并行求解器，基于 **rake-compress树收缩** 技术，打破依赖链随树高增长的限制，实现与树高、树平衡性和最大出度无关的对数级（O(log N)）并行跨度。

## 2. 论文提出的方法论

### 2.1 核心思想
- 论文将场景树优化问题建模为**边控制形式的双正则化LQR**问题。其KKT系统具有特定的稀疏结构。
- 算法基于经典的 **fork 树收缩算法**。它通过两种基本操作：
  - **Rake**：移除一个非根叶子节点及其父边，将消除后的贡献合并到父节点数据中。
  - **Compress**：移除一个单子节点，将其入边和出边合并为一条新边，并将消除节点的贡献吸收进新边的数据中。
- 这两种操作均作用于一个称为**条件二次函数**的封闭代数结构。这种表示与并行扫描LQR方法中的“条件值组合”代数相同。
- 通过反复应用Rake和Compress，可以将任意树收缩至根节点；反向展开（reverse expansion）则可以恢复所有被消除节点的Riccati系数、状态、控制量和乘子。

### 2.2 关键技术细节
- **条件二次函数**：每条边e=(i,j)用一个三元组αe = (Ae, Ce, Pe)表示，对应于一个条件二次函数Φe(xi, xj)，允许在半正定矩阵Ce（对偶正则化）奇异时处理精确约束。
- **Rake更新公式**：对叶子j和父边e，贡献到父节点数据Ui的增量为 τ(αe, Uj) = Pe + A_e^T Uj (I + Ce Uj)^{-1} Ae。
- **Compress更新公式**：对单子节点j及其入边eℓ和出边er，组合公式为 α_{eℓr} = α_{er} ∘_{Uj} α_{eℓ}，其中 S = I + C_{eℓ}(P_{er} + Uj)，新三要素可由此计算。
- **算法流程**：
  1. **拓扑计划构建**：根据父数组构建仅与树结构相关的收缩计划（一次构建，多次复用）。
  2. **局部控制凝聚与因子分解**：先独立消除所有边控制，然后执行Rake-Compress收缩与反向展开，恢复所有子树的P_i矩阵，并构造局部求解器（F_i, G_e等）。
  3. **仿射求解**：对于每个右端项(q,r,c)，在收缩计划上运行仿射数据的收缩与展开，恢复所有线性系数p_i。
  4. **状态恢复**：通过收缩和展开状态转移映射，从根状态xρ广播恢复所有节点状态，并独立计算控制量ue和乘子yi。

### 2.3 核心理论保证
- **正确性**：论文证明Rake和Compress的代数更新公式与KKT系统完全等价，且压缩操作具有结合性，因此收缩顺序不影响结果。
- **复杂度**：给定拓扑计划，对于N个节点、固定维度的问题，算法具有O(N)的算术工作量和存储量，以及O(log N)的跨度，且与树高、平衡性和最大出度无关。
- **调度器**：论文提出了一个确定性静态调度器，每个结构轮次移除至少三分之一节点，并通过“就绪度加权”的兄弟归约，保证O(log N)的原始依赖深度。

## 3. 实验设计

- **无传统数据集**：本文方法不涉及图像、文本等数据集，而是针对**树拓扑结构**和**合成LQR问题**进行性能验证。
- **基准与对比方法**：
  - **标准串行树Riccati递归**是在CPU上实现的主要对比基线。
  - **稠密KKT求解**：用于验证算法的数值精度，在NumPy中独立组装KKT矩阵并与算法结果对比。
- **实验场景**：
  1. **合成树状LQR**：测试了链 (Chain)、平衡二叉树 (Balanced)、梳子状树 (Comb)、星形树 (Star) 等拓扑，规模从128到8192个节点不等。
  2. **约束自动驾驶示例**：构建了一个具有不规则场景树（N=102，后来细化到8185）的复杂非线性MPC问题，包含多模态意图、路面摩擦、骑行者进入等分支，用于端到端集成验证。
- **对比内容**：CPU与GPU平台上的因子分解时间、右端项求解时间，以及端到端非线性求解时间。

## 4. 资源与算力

- **硬件**：作者在对比实验中明确使用了以下硬件：
  - **CPU**：Kaggle托管的虚拟化2.00 GHz Intel Xeon（双核/四线程）。
  - **GPU**：16 GiB显存的 **NVIDIA Tesla P100 PCIe**。
- **软件/框架**：JAX（0.10.1）进行加速计算，NumPy用于独立验证。
- **训练时长**：论文中未提及“训练”时长。由于是数学优化方法，具体时间为**后编译（post-compilation）的求解时间**。性能测量排除了问题生成、计划构建、后端初始化、数据传输和JIT编译时间。
- **说明**：论文明确指出，当前JAX实现存在对不规则拓扑（如梳状树）在CPU上编译过慢的问题，这是实现层面的限制，并非算法本身的瓶颈。

## 5. 实验数量与充分性

- **数量**：
  - **KKT精度验证**：26个实例，包含固定拓扑（链、星、不规则）与随机生成且节点标签被打乱的树，状态维度1-4，控制维度1-3。结果最大绝对KKT残差为3.89e-15，与稠密解最大差异为1.25e-14，精度很高。
  - **收缩深度测量**：对四种拓扑（链、平衡树、梳、星）在16到4096节点规模下进行了测量，验证了O(log N)深度。
  - **合成性能基准**：对链、平衡树、梳、星四种拓扑，在128到8192节点规模下进行计时对比。
  - **自动驾驶案例**：在不同细化粒度（N=129到8185）下进行端到端测试，并报告了外迭代和线搜索迭代次数。
- **充分性与客观性**：
  - **优点**：KKT精度验证独立、严格；性能基准涵盖多种极端的拓扑（高树、高分支），能有效验证算法的理论优势；自动驾驶案例验证了算法的实用性。
  - **局限**：GPU对比实验中缺少梳状树（Comb）的CPU数据，削弱了该场景下的对比完整性。CPU和GPU的绝对速度差异不仅来自算法，也受硬件架构、编译优化和内存带宽影响，论文虽已分离编译时间，但未完全控制这些因素。

## 6. 论文的主要结论与发现

- **理论层面**：该算法是**代数精确**的，其收缩和展开过程与原始KKT系统严格等价。
- **复杂度层面**：实现了**O(N)工作量与O(log N)跨度**，独立于树高、平衡性和最大出度，理论上解决了标准树Riccati递归并行性不足的问题。
- **数值表现**：数值精度很高（残差在1e-14量级），验证了实现与理论的一致性。
- **性能表现**：在N=8192时，GPU相较于串行CPU在因子分解上获得11-15倍加速，在求解上获得19-24倍加速；同一并行代码从CPU迁移到GPU可获得约14-17倍加速，且加速比随问题规模增大而提高。
- **应用价值**：通过自动驾驶复杂场景的端到端案例，验证了该算法在非线性场景树MPC中的实际应用潜力。

## 7. 优点

- **开创性理论贡献**：首次将rake-compress树收缩技术系统性地应用于场景树Riccati求解，提供了严格的复杂度保证（O(log N) 跨度）。
- **高度的普适性**：算法适用于**任意有根树**，不依赖特定拓扑（如链或星形），对树的不规则性具有鲁棒性。
- **支持对偶正则化**：公式允许半正定对偶正则化（包括零，即无正则化情况），使之能完美嵌入原始对偶内点法框架。
- **工程实现完整**：提供了三个MIT许可的JAX包，将理论算法落地，并考虑了JAX的JIT、vmap和自动微分特性，工程实践性强。
- **严格的数值验证**：通过与独立组装的稠密KKT系统对比，提供了高置信度的正确性验证。

## 8. 不足与局限

- **复杂度过高导致小规模问题不具优势**：在N=128的小规模问题上，GPU运行相比CPU串行或并行没有优势甚至更慢，由于启动和调度开销，GPU优势只有在树规模足够大时才能体现。
- **实现层面的编译代价**：论文承认，当前JAX实现中，CPU上编译高度不规则拓扑（如梳状树）的速度过慢，这限制了该方法在无需编译的一次性（one-shot）场景中的应用。
- **硬件通用性受限**：算法依赖共享内存读（CREW）模型和稠密线性代数，传统CPU的并行能力较弱，性能优势主要依赖GPU等加速器实现，对仅具备CPU的工业场景可能不友好。
- **实验对比的局限性**：性能基准主要对比了自身算法在不同硬件上的表现，虽然与串行树Riccati对比，但未与**其他并行LQR方法**（如端点显式分解或并行扫描）进行同类算法间的直接对比，削弱了外部对比说服力。
- **复杂度假设**：理论O(log N)跨度建立在“固定状态和控制维度”前提下，未充分探讨当状态维度随应用增长时对常数因子和通信开销的影响。

（完）
