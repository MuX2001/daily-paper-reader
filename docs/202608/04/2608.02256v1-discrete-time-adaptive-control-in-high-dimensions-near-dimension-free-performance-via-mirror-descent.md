---
title: "Discrete-Time Adaptive Control in High Dimensions: Near Dimension-Free Performance via Mirror Descent"
title_zh: 高维离散时间自适应控制：通过镜像下降实现接近维度无关的性能
authors: "Mohammad Boveiri, Peyman Mohajerin Esfahani"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02256v1"
tags: ["query:control"]
score: 9.0
evidence: 镜像下降高维自适应控制
tldr: 高维离散时间非线性自适应控制中，未知参数矩阵常具有稀疏、低秩等结构，但标准欧氏更新（如递推最小二乘）的瞬态性能随维度恶化。本文提出基于镜像下降的新型自适应律，配合非欧几里得Polyak步长，利用参数结构诱导的几何。理论证明状态渐近收敛并提供至多随环境维度对数增长的遗憾界。数值实验验证了高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02256v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1776, \"height\": 335, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02256v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1784, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02256v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1793, \"height\": 1135, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02256v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1756, \"height\": 1113, \"label\": \"Figure\"}]"
motivation: 标准欧氏更新在高维参数控制中瞬态性能随维度下降，即使真实参数具有低复杂度。
method: 提出镜像下降型自适应律，采用非欧几里得Polyak步长，利用参数结构（稀疏、低秩等）的几何。
result: 建立状态渐近收敛，遗憾界对环境维度仅对数依赖，数值实验验证有效性。
conclusion: 该方法实现近维度无关的性能，为高维结构化自适应控制提供了可行方案。
---

## 摘要
受实时控制问题中现代高容量模型应用的启发，本文研究了具有未知矩阵值参数的高维离散时间非线性系统的自适应控制。我们关注未知参数条目数量较多但参数矩阵具有可利用结构（如逐元素稀疏性、组稀疏性、低秩以及行随机或密度矩阵结构）的情形。为了量化暂态性能，我们考虑相对于完全知道真实参数的标称控制器的遗憾准则。我们表明，标准欧几里得更新方案（包括递归最小二乘和梯度下降）不适合此设置：即使真实参数具有低内在复杂度，其暂态性能也会随维度增加而恶化。为解决这一局限性，我们提出了一类新颖的镜像下降型自适应律，配备非欧几里得Polyak型步长，利用参数结构引起的几何特性。对于所提出的更新律，我们建立了渐近状态收敛性，并导出了对环境维度至多具有对数依赖性的遗憾界。数值实验证明了所提出方案的有效性。

## Abstract
Motivated by the use of modern high-capacity models in real-time control problems, this paper studies the adaptive control of high-dimensional discrete-time nonlinear systems with an unknown matrix-valued parameter. We focus on regimes where the number of unknown parameter entries is large, but the parameter matrix possesses exploitable structure, such as entrywise sparsity, group sparsity, low rank, and row-stochastic or density-matrix structure. To quantify transient performance, we consider a regret criterion relative to a nominal controller with full knowledge of the true parameter. We show that standard Euclidean update schemes, including recursive least squares and gradient descent, are ill-suited to this setting: their transient performance deteriorates as the dimension increases, even when the true parameter has low intrinsic complexity. To address this limitation, we propose a novel class of mirror-descent-type adaptive laws equipped with a non-Euclidean Polyak-type step size that exploit the geometry induced by the parameter structure. For the proposed update laws, we establish asymptotic state convergence and derive regret bounds with at most logarithmic dependence on the ambient dimension. Numerical experiments demonstrate the effectiveness of the proposed schemes.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机**：现代高容量模型（如大规模参数化模型）在实时控制中的应用日益广泛，但传统自适应控制理论将高维参数化视为问题来源。标准自适应控制器在参数维度增大时，瞬态性能会显著恶化。本文的核心问题是：**这种性能退化是固有属性，还是由更新律的几何选择不当造成的？**

**核心问题**：考虑如下离散时间非线性系统：
```
X_{t+1} = A(X_t) + B(ΘΨ(X_t, t) + U_t)
```
其中 Θ ∈ R^{m×k} 是未知参数矩阵，其维度 d = mk 很大，但参数本身具有可利用的结构（逐元素稀疏、组稀疏、低秩、行随机矩阵/密度矩阵等）。目标是设计自适应控制律，使其参数估计误差导致的累积损失（遗憾）对环境维度具备**接近不依赖（dimension-free）**的性质，即至多随维度对数增长。

**整体含义**：本文论证了高维自适应控制的性能退化并非不可避免，通过将参数结构的几何信息编码进镜像下降更新律中，可以同时实现闭环稳定性、状态渐近跟踪和对数级遗憾界，为高容量模型在实时控制中的安全使用奠定了理论基础。

---

## 2. 论文提出的方法论

### 核心思想

- 抛弃标准欧几里得更新几何（梯度下降/RLS），采用**镜像下降（Mirror Descent）**框架，选择与参数结构相匹配的非欧几里得镜像映射函数 f，从而引入与结构对应的范数和几何。
- 使用**非欧几里得Polyak型步长**：
```
η_{t+1} = 2μ J_t(Θ̂_t) / (ε_{t+1} + ‖∇J_t(Θ̂_t)‖_*²)
```
该步长是经典Polyak步长在自适应控制、非欧几里得几何下的在线推广，对保证系统稳定性和收敛至关重要。

### 镜像更新律

```
Z_{t+1} = Z_t − η_{t+1}∇J_t(Θ̂_t),   Z_0 ∈ ∂f(Θ̂_0)
Θ̂_{t+1} = ∇f*(Z_{t+1})
```
其中 f 是 μ-强凸的镜像映射函数，f* 是其Fenchel共轭，∂f 为次微分。控制律采用确定性等价形式：
```
U_t = −K(X_t) − Θ̂_t Ψ(X_t, t) + U_t^d
```

### 关键技术细节

- **损失函数**：J_t(Θ̂_t) = ½‖X̃_{t+1}‖_F²，其中 X̃_{t+1} 是可观测的残差。
- **梯度**：∇J_t(Θ̂_t) = −B^T(X̃_{t+1})Ψ(X_t, t)^T。
- **Bregman散度**：V_t = D_f(Θ, Θ̂_t; Z_t) 作为Lyapunov函数，证明其单调非增，从而获得稳定性。
- **四类结构化参数及其对应镜像映射**：
  1. **逐元素稀疏**：基于 ℓ_p 范数的镜像映射（p = 1+1/ln d），对 ℓ₁ 范数 1-强凸，对偶范数为 ℓ_∞。
  2. **组稀疏**：混合 ℓ_{p,2} 范数版镜像映射，对偶范数为 ℓ_{∞,2}。
  3. **低秩**：Schatten-p 范数镜像映射，对偶范数为谱范数。
  4. **单纯形/密度矩阵**：Shannon熵 + 指示函数（逐元素单纯形）与von Neumann熵（谱单纯形），产生乘法更新律。

### 主要理论结果

- **定理1（稳定性与收敛）**：在回归器满足线性增长假设下，状态和参数估计一致有界，且 ‖X_t − X_t^d‖_F → 0（渐近跟踪）。
- **定理3（一般遗憾界）**：
```
Reg(T) ≤ √(D_f(Θ,Θ̂_0;Z_0)/(2μ) · Σ_{t=0}^{T-1}(ε_{t+1} + ‖∇J_t(Θ̂_t)‖_*²))
```
- **四个推论（Corollary 4-8）**：对每种结构，遗憾界对环境维度仅有 O(ln d) 的量级，而非欧几里得方法的 O(√d)。

---

## 3. 实验设计

### 场景一：标量激励示例（第二节）

- **系统**：x_{t+1} = θ^T ψ(x_t, t) + u_t，其中 θ ∈ R^k 只有前3个非零分量（稀疏）。
- **回归器**：随机符号回归器，ψ_i = ±(0.5x_t + 1)。
- **维度设置**：k ∈ {10, 50, 500, 2000}，维度增大不改变真实动力学。
- **对比方法**：归一化梯度下降(13)、递归最小二乘RLS(14)、稀疏镜像下降(29)。
- **基准**：无（无噪确定性系统，目标为镇定 x_t → 0）。

### 场景二：多智能体分布式系统（第七节）

- **系统**：X_{t+1} = ΘΨ(X_t, t) + U_t + W_t，n = N = 4 个智能体，k = 3000 维特征。
- **代价函数**：Laplacian正则化二次目标 F_t(X)，含图拉普拉斯 L_g（环形通信图）。
- **参考轨迹**：对 F_t 做在线梯度下降生成的 X_t^d。
- **真实参数**：每行只有2个非零元素且行和为1（稀疏 + 行随机结构）。
- **回归器**：随机Rademacher特征矩阵 Γ ∈ R^{k×n} 与状态非线性项的Hadamard积。
- **两种场景**：无噪声（W_t = 0）和有噪声（W_t 均匀分布在[−1,1]）。
- **对比方法**：RLS、梯度下降（欧几里得镜像）、行熵镜像下降(40)、稀疏镜像下降(29)。
- **评估指标**：累积遗憾 Reg(T) 随时间演化，以及参数估计热力图。

---

## 4. 资源与算力

**论文未明确说明使用的算力资源**（如GPU型号、数量、训练时长、运行环境等）。实验规模较小（n = N = 4，k = 3000），主要涉及一阶迭代更新和矩阵运算，推测在普通工作站或笔记本电脑上即可完成，但不排除使用了轻量级并行加速。论文未提供任何关于硬件配置、运行时间或能耗的信息。

---

## 5. 实验数量与充分性

### 实验数量

- **示例实验**（图1）：设置了4种维度（10/50/500/2000）× 3种算法 = 12组状态轨迹实验，用于展示维度增加对欧几里得方法的恶化影响。
- **主实验**（图2-4）：1个系统设置（n=N=4, k=3000）× 2种噪声条件（无噪/有噪）× 4种算法 = 8组核心对比实验，分别展示遗憾曲线、参数估计可视化。

### 充分性与公平性评估

**优点**：
- 参数维度 k = 3000 具有代表性，能体现高维特性。
- 对比了经典基准（RLS、梯度下降），公平性较好。
- 同时展示了无噪和有噪两种重要场景。
- 参数估计可视化（图3、4）直观展示了欧几里得方法产生密集估计而镜像下降保持稀疏结构，佐证了理论分析。

**不足**：
- 缺少消融研究（如ε_t序列不同取值、不同初始化方式、不同步长设计的影响）。
- 缺少对**组稀疏**和**低秩/谱单纯形**结构的数值验证（论文在第四、五节给出了理论，但实验仅验证了逐元素稀疏和行随机单纯形两种情况）。
- 每种算法仅报告单次运行结果，未包含多次独立实验的均值/方差或置信区间。
- 未测试不同参数结构混合（如稀疏 + 低秩）或更大规模（如 n = N = 10, k = 10000）的情形。
- 缺少与其他非欧几里得自适应算法（如基于 ℓ₁ 正则化的在线学习）的对比。

---

## 6. 论文的主要结论与发现

1. **标准欧几里得更新在高维下本质失效**：即使真实参数低复杂度，梯度下降和RLS的遗憾随维度呈 O(√d) 增长，原因是其梯度范数/对偶范数为 O(√d)，且参数估计误差弥散到所有坐标。数值实验验证了这一退化现象（图1）。

2. **镜像下降+结构自适应几何是关键**：通过选择与参数结构匹配的镜像映射（ℓ_p、混合范数、Schatten-p、熵），可以得到对偶范数为 ℓ_∞ 或谱范数的设计，其量级不随维度增长，从而获得对数遗憾界。

3. **理论保证完备**：在仅假设回归器线性增长的宽松条件下，所提方案保证闭环信号有界 + 状态渐近跟踪（定理1），且遗憾上界为 O(√T · ln d)（定理3 + 推论4-8）。

4. **数值验证**：稀疏镜像下降和熵镜像下降在图2-4中显著优于RLS和梯度下降；在噪声环境中RLS性能急剧恶化而镜像下降几乎不受影响，说明一阶镜像方法在高维噪声环境中比RLS更鲁棒。

5. **随机扩展的开放问题**：论文指出在过程噪声下步长与噪声耦合，标准的无偏随机近似证明难以直接应用，需要更精细的分析或可预测步长机制。

---

## 7. 优点

- **理论深度与完备性**：从一般框架（定理1、3）出发，经四种具体结构（推论4-8）逐步细化，逻辑严密。利用Bregman散度作为Lyapunov函数证明稳定性，优雅且统一。
- **Polyak步长的非欧推广**：从经典Polyak步长到非欧几里得在线形式的推广（Remark 1）具有原创性，并巧妙利用了 J_t(Θ) = 0 的结构特性。
- **"维度不变性"的明确定量刻画**：明确指出欧几里得方法 O(√d) 的退化来源（对偶范数的维度依赖），不是保守分析的产物，而是可观测的真实行为，具有说服力。
- **方法普适性强**：框架适用于多种结构（稀疏/组稀疏/低秩/单纯形/密度矩阵），通过选择不同的镜像映射即可灵活适配，覆盖面广。
- **允许非光滑镜像映射**：通过指示函数将约束直接嵌入镜像映射，使约束和正则化无缝统一。
- **附录证明充分**：关键引理（Lemma 2）的证明给出了线性增长条件下梯度范数和残差之间的完整递推关系。

---

## 8. 不足与局限

- **随机噪声下的理论缺失**：第六节仅给出启发式分析和经验观察，未提供在过程噪声下的严格遗憾界或稳定性的证明。步长与噪声相关性构成根本障碍，作者也承认这一点。
- **实验覆盖有限**：
  - 缺少对组稀疏和低秩/谱单纯形更新律的数值验证（理论与实验之间存在缺口）。
  - 未进行消融实验（如 ε_t 的选择、初始化影响、不同步长范围）。
  - 无多次运行的统计测量（均值/方差）。
  - 真实参数结构仅测试了逐元素稀疏和行随机两种，未覆盖低秩、密度矩阵等。
- **缺少与相关文献的定量对比**：如与基于 ℓ₁ 正则化、Frank-Wolfe 或 FISTA 等自适应算法的性能对比。
- **假设条件**：需要 A−BK Schur稳定（即要求已知名义模型 A、B 和反馈增益 K），这在完全模型未知的场景中可能不成立。
- **理论遗憾界的根号 T 依赖性**：作者指出在持续激励条件下或许可以改进到对数 T，但没有在本文中深入探索。
- **Bregman散度的初始化假设**：推论中的零初始化或均匀初始化在部分应用（如已有先验知识）中可能不自然，且对偏离该初始化的情形理论不明确。
- **计算复杂度讨论不足**：虽然镜像下降的一阶特性优于RLS的 O(k²) 矩阵维护，但Schatten-p范数镜像映射（低秩场景）需要奇异值分解，其计算成本并未在文中分析。

---

（完）
