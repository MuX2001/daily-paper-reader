---
title: Robust Model Reference Adaptive Control with Combined Adaptation under Finite Excitation Condition
title_zh: 有限激励条件下具有复合自适应的鲁棒模型参考自适应控制
authors: "Manish Patel, Arnab Maity"
date: 2026-08-23
pdf: "https://arxiv.org/pdf/2608.22562v1"
tags: ["query:control"]
score: 9.0
evidence: 有限激励下的鲁棒模型参考自适应控制
tldr: 针对线性参数化自适应控制中参数收敛依赖持续激励的问题，提出基于修正Gram-Schmidt正交化构造记忆项的鲁棒模型参考自适应控制。在有限激励条件下，记忆项使参数估计误差动力学中的系数矩阵为单位阵，无需时变自适应增益，并给出显式最终界。结合组合自适应律，闭环跟踪和估计误差指数收敛到原点邻域，收敛速率仅由用户增益和系统常数决定，与激励水平无关。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22562v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1232, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1649, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 759, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 762, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1630, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1656, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1661, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22562v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 784, \"height\": 502, \"label\": \"Figure\"}]"
motivation: 持续激励条件依赖未来数据，在线无法保证；现有方法收敛速率受激励水平影响。
method: 用Modified Gram-Schmidt正交化构造记忆项，结合组合自适应律估计控制器增益，在有限激励下使参数误差系数矩阵为单位阵。
result: 闭环误差指数收敛到原点邻域，显式最终界，收敛速率独立于回归器激励水平。
conclusion: 消除了对持续激励和时变增益的依赖，放宽了收敛速率对激励的限制。
---

## 摘要
在自适应控制中，参数化不确定性以线性参数化形式由未知参数和已知回归器信号组成。未知参数收敛到其理想值要求回归器满足持续激励（PE）条件，该条件依赖于未来数据，因此在线难以保证。基于记忆的参数更新律通过在在线可验证的有限激励（FE）条件下实现理想参数收敛来解决此问题。本文针对一类具有未知对角控制效率矩阵和有界非参数不确定性的多输入多输出非线性系统，提出了一种通过修正Gram-Schmidt正交化过程构造记忆项的新算法。在有限激励条件下，所构造的记忆项在参数估计误差动态中产生单位系数矩阵。单位系数矩阵消除了对时变自适应增益的需求，使得参数估计误差具有显式终极界，并在记忆项下保持非参数不确定性界的结构。在此基础上，开发了一种在FE条件下用于控制器增益估计的复合自适应律。闭环跟踪和估计误差被证明以指数衰减至原点附近的一个邻域，该邻域由显式终极界表征，衰减率仅取决于用户定义的增益和系统常数，与回归器激励水平无关。这消除了收敛速度对回归器激励水平的依赖，而这是现有方法（如并发学习、记忆回归器扩展和DREM）的关键局限性。

## Abstract
In adaptive control, parametric uncertainties in linear-in-parameter form consist of unknown parameters and known regressor signals. Convergence of the unknown parameters to their ideal values requires the regressor to satisfy a persistent excitation (PE) condition, which depends on future data and is therefore infeasible to guarantee online. Memory-based parameter update laws address this by enabling ideal parameter convergence under the online-verifiable finite excitation (FE) condition. In this paper, a new algorithm is proposed to construct a memory term via the Modified Gram-Schmidt orthogonalization procedure for a class of multi-input multi-output nonlinear systems with an unknown diagonal control effectiveness matrix and bounded nonparametric uncertainties. Under the finite excitation condition, the constructed memory term yields an identity coefficient matrix in the parameter estimation error dynamics. The identity coefficient matrix eliminates the need for time-varying adaptation gains, enables an explicit ultimate bound on the parameter estimation error, and preserves the structure of the nonparametric uncertainty bound under the memory term. Building on this, a combined adaptation law is developed for controller gain estimation under FE. The closed-loop tracking and estimation errors are shown to decay exponentially to a neighborhood of the origin, characterized by an explicit ultimate bound, with a decay rate that depends solely on user-defined gains and system constants, independent of the level of regressor excitation. This removes the dependence of the convergence rate on the level of regressor excitation, a key limitation of existing approaches such as concurrent learning, memory regressor extension, and DREM.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **背景**：在自适应控制中，参数化不确定性通常以“线性参数化”形式出现（未知参数 × 已知回归器）。传统上，参数估计若要收敛到理想值，回归器必须满足**持续激励（PE）**条件。但 PE 依赖未来时刻数据，在线无法验证或保证，因此在实际任务中（如参考信号由任务决定、无法人为注入频谱）难以实现理想参数收敛。
- **已知缓解方法**：基于记忆项的更新律可在**有限激励（FE）**条件下实现参数收敛，例如并发学习（CL）、记忆回归器扩展（MRE）、动态回归器扩展与混合（DREM）等。但这些方法存在两个关键缺陷：
  - 收敛速率依赖回归器激励水平（如矩阵最小特征值或行列式），激励弱时收敛极慢，甚至需要很大的时变自适应增益，导致误差动态刚化；
  - 记忆项会对非参数不确定性进行复杂变换，难以得到显式的最终界。
- **本文目标**：针对一类**多输入多输出（MIMO）非线性系统**（含未知对角控制效率矩阵、匹配参数不确定性、有界非参数不确定性），提出一种新的记忆项构造算法，使参数估计误差动力学在 FE 条件下具有**单位系数矩阵**，从而消除时变增益需求、获得显式最终界，并进一步设计“复合自适应律”，使闭环误差以**与激励水平无关的速率**指数收敛到原点邻域。

## 2. 方法论

### 2.1 系统模型与问题转化

- 系统：$\dot{x}=Ax+B\Lambda\{u+\Theta^\top\phi(x)\}+\xi$，其中 $A,\Lambda$ 未知，$B$ 已知，$\xi$ 为非参数不确定性（有界）。
- 参考模型与匹配条件：存在理想增益 $K_x,K_r$，满足 $A+B\Lambda K_x^\top=A_r$，$B\Lambda K_r^\top=B_r$。
- 通过 $B^\dagger$（Moore–Penrose 逆）将系统变换为线性参数化形式：
  $$y = W^\top \phi(x,u) + \xi_y,\quad y:=B^\dagger\dot{x},\quad \xi_y:=B^\dagger\xi$$
  再经一阶稳定滤波器得到 $y_f=\Phi_f^\top w+\xi_f$。

### 2.2 核心算法：基于修正 Gram-Schmidt（MGS）的记忆项构造

- 在 FE 条件下，收集 $q$ 个时刻的滤波回归器 $\phi_f(t_i)$ 和输出 $y_f(t_i)$，构成矩阵 $\Phi_{fc}\in\mathbb{R}^{q\times q}$ 与 $Y_{fc}$。
- **Algorithm 1** 对 $\Phi_{fc}$ 执行带阈值（$\delta_1,\delta_2$）的修订 Gram-Schmidt 正交化，生成正交矩阵 $\Phi_{fb}$，并同步对 $Y_{fc}$ 做相同变换得到 $Y_{fb}$。
- 该变换保持线性参数化结构（Theorem 1）：
  $$y_{fb}=\Psi_{fb}^\top w+\xi_{fb},\quad \Psi_{fb}\Psi_{fb}^\top=I$$
  其中 $\Psi_{fb}$ 由块对角 $\Phi_{fb}$ 构成。这使参数误差动态的系数矩阵成为**单位阵**。

### 2.3 参数更新律与收敛性

- 更新律：$\dot{\hat{w}}=\gamma_w \Gamma_w \Psi_{fb}(y_{fb}-\Psi_{fb}^\top \hat{w})$，其中 $\gamma_w=0$ 在正交基未完成时（$k\le q$），完成后 $\gamma_w=1$。
- 误差动态：$\dot{\tilde{w}}=\gamma_w\Gamma_w(-\Psi_{fb}^\top\tilde{w}+\xi_{fb})$；由于 $\Psi_{fb}$ 正交，当 $\gamma_w=1$ 时简化为 $\dot{\tilde{w}}=-\Gamma_w\tilde{w}+\Gamma_w\xi_{fb}$，因此：
  - 无需求解时变增益；
  - 无非参数不确定性时退化为 $\dot{\tilde{w}}=-\Gamma_w\tilde{w}$，指数收敛；
  - 有非参数不确定性时，给出**显式最终界**。

### 2.4 复合自适应控制律

- 从估计 $\hat{w}$ 中提取 $\hat{A},\hat{\Lambda}$，进一步计算控制器增益的间接估计。
- 为保证安全，设置条件（35）：$\hat{\Lambda}\Lambda_s>0$ 且 $\lambda_{\min}(\hat{\Lambda})>\underline{\lambda}$；若满足且正交基完成，则 $\gamma_i=1$，否则 $\gamma_i=0$。
- 控制器增益更新律（36）包含两个模式：
  - $\gamma_i=0$：退化为带 $\sigma$-修正的标准自适应律，保证有界性；
  - $\gamma_i=1$：启用记忆项，利用参数估计误差驱动增益修正。
- Theorem 3 证明：综合误差 $\chi$ 满足 $\|\chi(t)\|^2\le \kappa_1\|\chi(t_q)\|^2 e^{-k(t-t_q)}+\rho_{ub}$，其中**指数衰减率 $k$ 与回归器激励水平无关**，仅由用户增益和系统常数决定；最终界 $\rho_{ub}$ 由非参数不确定性、理想增益和参数误差界显式刻画。

## 3. 实验设计与基准对比

- **Example 1（参数辨识）**：
  - Study 1：回归器仅依赖时间，未知参数 $w=[1,2]^\top$；对比 CL、MRE、DREM，学习率 $\gamma_l=1$ 与 $10$。
  - Study 2：回归器包含状态变量（由已知二阶动态产生），未知参数 $w_z=[-1,-1.4,1,2]^\top$；统一 $\gamma_l=1$ 进行对比，随后对 CL、MRE、DREM 分别“调参”后进行公平对比。
- **Example 2（闭环控制）**：某型飞机纵向动力学（单输入，升降舵 $\delta_e$），含状态线性不确定性、控制效率不确定性、高斯型状态非线性不确定性；增加积分误差扩展状态；采用所提复合自适应律，与文献 [27] 的复合自适应控制器对比。基准场景为迎角指令跟踪，没有加入非参数不确定性。
- **对比方法**：并发学习（CL）、记忆回归器扩展（MRE）、动态回归器扩展与混合（DREM），以及文献 [27] 的控制器。

## 4. 资源与算力

- **论文未明确说明**使用了何种计算资源（如 GPU 型号、数量、训练时长等）。
- 从内容看属于理论推导加数值仿真，问题规模较小（二维/三维状态、几个未知参数），仿真量级较轻，通常使用 MATLAB/Python 在普通 CPU 上即可完成；但文中未给出任何运行时或硬件信息。

## 5. 实验数量与充分性

- **实验数量**：共 3 个示例（Example 1 两子研究 + Example 2 闭环控制）。
- **基准覆盖**：参数辨识部分对比了 CL、MRE、DREM 三种典型记忆类方法；闭环控制部分对比了文献 [27] 的复合自适应控制器。
- **充分性评价**：
  - 优点：参数辨识设计了两种不同激励结构（时间依赖 vs 状态依赖），并考察了学习率对收敛性和数值稳定性的影响；闭环实验验证了真实航空模型上的有效性。
  - 不足：
    - **未做多输入（MIMO）数值实验**：理论对象是多输入多输出，但 Example 2 是单输入飞机纵向模型，未展示 $m>1$ 的情形；
    - **未验证非参数不确定性**：理论部分重点讨论了对有界非参数不确定性的鲁棒性及显式最终界，但所有仿真示例均未加入非零 $\xi$；
    - **没有消融实验**：未分析 $\delta_1$、$\delta_2$、滤波器频率等关键参数对性能的影响；
    - 公平性方面：DREM 需要 $10^7$ 量级的学习率才能收敛，虽然论文批评了 DREM 的缩放问题，但这可能让对比显得偏“标靶式”；不过论文也说明了这样设定是为了保证公平对比。

## 6. 主要结论与发现

- 通过 MGS 正交化构造记忆项，可在 FE 条件下使参数估计误差动力学具有**单位系数矩阵**，从而：
  - 不需要时变自适应增益，避免因矩阵病态导致的刚度问题；
  - 参数估计误差具有**显式最终界**，且非参数不确定性经正交变换后结构保持。
- 结合该记忆项与控制器增益的复合自适应律，闭环跟踪和估计误差以**指数速率**收敛到原点邻域，衰减率 $k$ **独立于回归器激励水平**，仅取决于用户增益和系统常数。
- 在无非参数不确定性时，参数估计误差可以达到严格的指数收敛（无偏置项）。
- 数值仿真表明，与 CL、MRE、DREM 相比，所提方法在同样学习率下收敛更快、无需针对激励水平重新调参，且在闭环飞机控制中能改善指令跟踪并减少控制面振荡。

## 7. 优点

- **理论创新**：MGS 正交化构造记忆项、单位系数矩阵的设计思想新颖，从根本上解耦了收敛速率与激励水平的关系。
- **较强的鲁棒性保证**：对非参数不确定性给出显式最终界，而非仅定性“有界”。
- **算法实用性**：FE 条件可在线验证，且避免了 DREM 中滤波器个数随参数维度增长、难以选择截止频率的问题。
- **理论完整性**：从系统变换、滤波、正交基构造、参数更新、增益更新到 Lyapunov 稳定性分析，全部给出严格证明，数学推导详实。
- **对比实验设计合理**：引入不同学习率、不同回归器结构、不同方法调参后的对比，清晰地展示了所提方法的优势。

## 8. 不足与局限

- **数值验证覆盖面有限**：缺少 MIMO 闭环实例，也无法控制器对非参数不确定性的鲁棒性效果；实验均为仿真，无硬件实验。
- **理论界的保守性**：作者本人指出非参数不确定性变换后的最终界是保守估计；实际不确定性可能远小于理论界。
- **参数敏感性分析欠缺**：$\delta_1,\delta_2$ 的选择仅有定性建议，缺少系统化的鲁棒性分析；滤波器带宽、采样间隔等关键参数也未讨论。
- **对测量噪声和实际工程因素未建模**：论文没有考虑传感器噪声、执行器饱和、离散化实现等，影响实际部署可信度。
- **与 DREM 对比的公平性问题**：DREM 需要极大学习率才能工作，虽然作者给出了合理解释，但可能削弱对比的说服力；未在相同闭环任务中进行端到端对比。
- **对理想条件依赖较强**：如 $B$ 完全已知、$\Lambda$ 对角已知符号且有已知下界、$\xi$ 上界已知等假设，在部分工程场景中难以满足。

（完）
