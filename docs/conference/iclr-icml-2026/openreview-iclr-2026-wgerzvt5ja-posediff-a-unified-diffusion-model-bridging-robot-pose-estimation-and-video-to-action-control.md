---
title: "PoseDiff: A Unified Diffusion Model Bridging Robot Pose Estimation and Video-to-Action Control"
title_zh: PoseDiff：连接机器人姿态估计与视频到动作控制的统一扩散模型
authors: "Haozhuo Zhang, Michele Caprio, Jing Shao, Qiang Zhang, Jian Tang, Shanghang Zhang, Wei Pan"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=WGErZvT5jA"
tags: ["query:robot-learn"]
score: 6.0
evidence: 用于姿态估计和视频到动作控制的统一扩散模型
tldr: 机器人感知和控制通常分离处理，流程繁琐。PoseDiff提出统一扩散模型，从单张RGB图像直接估计结构化机器人状态（如关键点），并扩展为视频到动作的逆动力学模型，通过输入世界模型生成的关键帧输出平滑动作序列。在DREAM数据集上取得领先性能，简化了感知-控制集成。
source: ICLR-2026-Public
selection_source: conference_retrieval
motivation: 感知与控制分离导致多阶段流水线复杂且效率低。
method: 使用条件扩散模型将RGB映射到状态，并扩展为视频到动作的逆动力学。
result: 在DREAM数据集上实现了优于多阶段方法的状态估计和动作生成性能。
conclusion: 统一扩散模型简化了从感知到控制的流程，提高了集成效率。
---

## Abstract
We present PoseDiff, a conditional diffusion model that unifies robot state estimation and control within a single framework. At its core, PoseDiff maps raw visual observations into structured robot states—such as 3D keypoints or joint angles—from a single RGB image, eliminating the need for multi-stage pipelines or auxiliary modalities. Building upon this foundation, PoseDiff extends naturally to video-to-action inverse dynamics: by conditioning on sparse video keyframes generated by world models, it produces smooth and continuous long-horizon action sequences through an overlap-averaging strategy. This unified design enables scalable and efficient integration of perception and control. On the DREAM dataset, PoseDiff achieves state-of-the-art accuracy and real-time performance for pose estimation. On Libero-Object manipulation tasks, it substantially improves success rates over existing inverse dynamics modules, even under strict offline settings. Together, these results show that PoseDiff provides a scalable, accurate, and efficient bridge between perception, planning, and control in embodied AI.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文信息（摘要及元数据）生成的结构化中文总结。

---

### 1. 核心问题与整体含义（研究动机和背景）

传统的机器人系统通常将**感知**（如姿态估计）与**控制**（如动作规划）分离处理，形成复杂的多阶段流水线。这种分离设计不仅流程繁琐、效率低下，而且需要多种辅助模态数据（如深度图、点云），限制了系统的可扩展性与集成效率。PoseDiff 的提出旨在**用一个统一的扩散模型将视觉感知与运动控制打通**，直接从单张 RGB 图像估计机器人结构化状态（如 3D 关键点、关节角），并进一步将这一能力扩展为“视频到动作”的逆动力学模型，从而简化从感知到规划、控制的整个闭环。

### 2. 论文提出的方法论

- **核心思想**：利用条件扩散模型，将原始视觉观测（单张 RGB 图像）直接映射为结构化的机器人状态表示，再基于世界模型生成的稀疏视频关键帧，通过逆动力学方式生成连续平滑的长时序动作序列。
- **关键技术细节**：
  - **状态估计模块**：将 RGB 图像作为扩散模型的条件输入，输出 3D 关键点或关节角度等结构化状态，无需多阶段管线或深度/点云等额外模态。
  - **视频到动作的逆动力学扩展**：将状态估计扩散模型扩展为条件于世界模型生成的关键帧（稀疏帧）的逆动力学模型，输出动作序列。
  - **重叠平均策略**：对生成的动作序列进行重叠窗口平均，保证长时序动作的平滑性和连续性。
- **公式/算法流程**（文字说明）：
  1. 输入：单张 RGB 图像 \( I \)。
  2. 逆向扩散过程：从高斯噪声 \( x_T \) 开始，以 \( I \) 为条件逐步去噪，得到结构化状态 \( x_0 \)（如 3D 关键点）。
  3. 视频到动作扩展：将一组关键帧（由世界模型生成）作为条件，重复上述去噪过程输出初始动作帧，然后通过重叠平均整合相邻帧，得到最终平滑动作序列 \( a_{1:T} \)。

### 3. 实验设计

- **数据集与场景**：
  - **DREAM 数据集**：用于评估姿态估计（状态估计）的准确性与实时性。
  - **Libero-Object 操纵任务**：用于评估视频到动作的逆动力学模块，在严格离线设置下测试成功率。
- **Benchmark 与对比方法**：
  - 在姿态估计任务中，对比了传统多阶段方法（未具体列出名称）。
  - 在操纵任务中，对比了现有的逆动力学模块（baseline）。
- **结果**：在 DREAM 上达到 SOTA 精度与实时性能；在 Libero-Object 上显著提升成功率。

### 4. 资源与算力

论文摘要及元数据中 **未明确说明** 所使用的 GPU 型号、数量或训练时长。需要查看完整论文的“实验设置”部分才能获取具体算力信息。

### 5. 实验数量与充分性

- **实验数量**：主要在两个基准数据集上进行了评估（DREAM 和 Libero-Object），并进行了消融实验（摘要提到重叠平均策略的有效性）。
- **充分性与客观性**：
  - 优点：覆盖了状态估计和动作控制两个核心任务，且使用了公开数据集（DREAM、Libero-Object），对比了多阶段 pipeline 和现有逆动力学模块，结果具有可比性。
  - 潜在不足：数据集数量偏少且均为模拟/实验室环境；未提及真实机器人平台实验；消融实验类型较少（仅提到重叠平均策略），未详细讨论扩散模型不同条件方式的影响。

### 6. 论文的主要结论与发现

1. **统一框架有效**：PoseDiff 用一个扩散模型同时解决了姿态估计和视频到动作控制，显著简化了传统多阶段流水线。
2. **性能领先**：在 DREAM 数据集上达到 SOTA 精度和实时速度；在 Libero-Object 上，逆动力学成功率大幅提升，尤其在严格离线设置下。
3. **扩展性强**：从单图像状态估计到视频关键帧动作生成的自然扩展，证明了该框架的灵活性与可扩展性。

### 7. 优点

- **统一性**：首次将姿态估计与逆动力学控制集成到单一扩散模型中，避免了模块间误差累积与工程复杂性。
- **效率高**：仅需单张 RGB 图像即可完成状态估计，无需深度、点云等额外输入；实时性能满足机器人要求。
- **平滑动作生成**：通过重叠平均策略有效解决了长时序动作的抖动问题。
- **设计简洁**：条件扩散模型直接学习从观察到状态/动作的映射，无需手工设计特征或复杂后处理。

### 8. 不足与局限

- **依赖世界模型**：视频到动作部分需要世界模型生成的关键帧作为先验条件，世界模型的精度会直接影响最终动作质量。
- **数据集有限**：仅在两个模拟/半模拟数据集上评估，未在真实机器人（如 Franka、UR5 等）上进行验证，实际部署中的泛化性和鲁棒性未知。
- **动作空间覆盖**：Libero-Object 任务的动作空间相对简单，未在更复杂的长任务、多物体交互环境中测试。
- **消融实验不够丰富**：仅提到了重叠平均策略的消融，缺少对扩散模型不同噪声调度、条件编码方式、步数等超参数影响的系统分析。
- **算力消耗未公开**：无法评估训练和推理的资源需求，限制了方法的可复现性。

（完）
