---
title: A Generalization Theory for JEPA-Based World Models
title_zh: JEPA基世界模型的泛化理论
authors: "Jingyi Cui, Qi Zhang, Hongwei Wen, Yisen Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27014v1"
tags: ["query:world-model"]
score: 9.0
evidence: JEPA世界模型的理论分析用于规划
tldr: 联合嵌入预测架构（JEPA）通过潜在空间学习预测动力学，但缺乏理论理解。本文将JEPA预训练建模为条件谱图学习问题，证明其目标等价于动作条件共现矩阵的低秩分解。建立了预训练误差与下游规划遗憾的关联，得到有限样本泛化界。揭示潜在维度上近似误差与样本误差的固有权衡，为潜在预测模型相比输入级预测的优势与局限提供理论洞见。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 728, \"label\": \"Figure\"}]"
motivation: JEPA世界模型经验成功但缺乏理论基础，需要理论解释其泛化特性与设计原则。
method: 将JEPA预训练形式化为条件谱图学习，等价于动作条件共现矩阵的低秩分解，并连接预训练误差与下游规划遗憾。
result: 推导出JEPA世界模型的有限样本泛化界，揭示潜在维度上近似误差与样本误差的权衡关系。
conclusion: 理论分析为JEPA相比输入级预测模型的优势与局限提供解释，指导实际设计。
---

## 摘要
联合嵌入预测架构（JEPAs）最近作为一种有前景的世界建模范式出现，它通过在潜在空间中学习预测动态，而不是在输入层级生成未来观测。尽管取得了实证成功，但对JEPA基世界模型的理论理解仍然有限。在本文中，我们为JEPA基世界模型建立了第一个泛化理论。我们将JEPA预训练表述为一个条件谱图学习问题，并证明JEPA目标等价于一个动作条件共现矩阵的低秩分解。基于这一表征，我们建立了JEPA预训练误差与下游规划遗憾之间的连接，从而为JEPA基世界模型导出了一个有限样本泛化界。我们的分析揭示了关于潜在维度的近似误差和样本误差之间的固有权衡，为潜在预测模型相较于输入级预测方法的优势和局限性提供了理论见解。

## Abstract
Joint Embedding Predictive Architectures (JEPAs) have recently emerged as a promising paradigm for world modeling by learning predictive dynamics in a latent space rather than generating future observations at the input level. Despite their empirical success, the theoretical understanding of JEPA-based world models remains limited. In this paper, we develop the first generalization theory for JEPA-based world models. We formulate JEPA pretraining as a conditional spectral graph learning problem and show that the JEPA objective is equivalent to a low-rank factorization of an action-conditioned co-occurrence matrix. Building on this characterization, we establish a connection between JEPA pretraining error and downstream planning regret, leading to a finite-sample generalization bound for JEPA-based world models. Our analysis reveals an inherent trade-off between approximation and sample errors with respect to the latent dimension, providing theoretical insights into the advantages and limitations of latent predictive models compared with input-level predictive approaches.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：联合嵌入预测架构（JEPAs）在基于潜在空间预测的世界建模中取得了显著实证成功，但其理论基础严重缺失。现有理论工作主要关注JEPA作为静态表征学习框架或参数化模型分析，缺乏对JEPA作为世界模型在下游规划任务中泛化能力的理论解释。
- **整体含义**：本文首次为JEPA基世界模型建立泛化理论，通过等价于动作条件共现矩阵的低秩分解，揭示了预训练误差与下游规划遗憾之间的关系，并导出了有限样本泛化界，从而解释了潜在预测模型相比输入级预测模型的优势与局限性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）
- **核心思想**：将JEPA预训练形式化为**条件谱图学习**问题。给定动作 \(a\)，定义条件共现矩阵 \(M(a)\) 其元素为 \(w(x,x^+,a) = P(x,x^+|a)\)。通过归一化得到 \(\bar{M}(a) = D^{-1/2}M(a)D_+^{-1/2}(a)\)。
- **关键技术细节**：
  - **等价性定理（Theorem 3.1）**：JEPA预训练风险（含均匀性正则项）等价于矩阵分解的Frobenius范数误差：  
    \[ R_{\text{JEPA}}(f,g,a) = \|\bar{M}(a) - G(F,a)^\top F\|_F^2 + \text{const}. \]  
    其中 \(F = [\sqrt{w(x|a)}f(x)]\)，\(G(F,a) = [\sqrt{w(x)}g(f(x),a)]\)。
  - **近似误差**：最优潜在维数 \(k\) 下的近似误差等于 \(\bar{M}(a)\) 第 \(k+1\) 大奇异值平方和（Theorem 4.3）。
  - **样本误差**：通过Rademacher复杂度给出样本误差上界（Theorem 4.4），依赖于潜在维数 \(k\) 和函数类复杂度。
  - **规划遗憾上界**：单步规划（Theorem 4.1）和多步规划（Theorem 4.2）的期望遗憾均被JEPA预训练风险的平方根所界，多步规划存在误差累积（因子 \(T\)）。
  - **有限样本泛化界**（Theorem 4.5、4.6）：结合近似误差和样本误差，得到 \(\sqrt{\text{近似误差} + \text{样本误差}} + \text{常数}\) 形式的上界，揭示了潜在维数 \(k\) 带来的**权衡**：增大 \(k\) 减小近似误差但增大样本误差。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法
- **数据集/场景**：全控制的**合成连续控制环境**——2D点质量系统，底层状态为位置和速度，动作是加速度。观测向量由三部分组成：状态特征（任务相关）、干扰特征（非线性投影）、随机噪声特征（与动作和任务无关）。
- **Benchmark**：下游任务是**目标到达规划**，使用交叉熵方法（CEM）进行滚动时域规划。评价指标是**成功率**（位置误差≤0.08）。
- **对比方法**：**潜在级预测**（JEPA风格：编码器→GRU预测器→预测潜在表示，训练使用MSE(预测潜在, stopgrad(编码器(真实)))) vs **输入级重建**（预测潜在后解码回观测空间，训练使用MSE(重建观测, 真实观测)）。两种方法共享相同的主干网络（编码器、动作编码器、GRU预测器、解码器）。

### 4. 资源与算力（GPU型号、数量、训练时长等）
- 论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。

### 5. 实验数量与充分性
- **实验数量**：主要实验为图2所示，在六种不同规划步数（1,5,10,15,20,25）和连续变化的噪声标准差下比较两种方法的成功率曲线。此外未报告其他数据集或消融实验。
- **充分性与公平性**：
  - 实验设计针对性强，直接验证理论预测（高噪声、长规划下潜在预测优势）。
  - 但仅使用单一合成环境，缺乏真实世界或复杂视觉场景的验证；未进行超参数敏感性分析、不同正则化强度比较或与其它JEPA变体（如V-JEPA2、DINO-WM）对比。统计信息（如多次运行的均值/方差）未提供，可能影响结论可靠性。

### 6. 论文的主要结论与发现
- JEPA预训练等价于动作条件共现矩阵的低秩分解。
- 预训练误差直接控制下游规划遗憾，为泛化提供理论保证。
- 潜在维数 \(k\) 存在**近似误差与样本误差的固有权衡**：\(k\) 太小则欠拟合，太大则过拟合。
- 输入级预测可视为 \(k=n\) 的极端情况：近似误差为零，但样本误差最大，因此对噪声和长规划尤其脆弱。
- 实验验证：在**高噪声**和**长规划步数**下，潜在级预测显著优于输入级重建，与理论预测一致。

### 7. 优点：方法或实验设计上的亮点
- **理论创新**：首次为JEPA世界模型建立完整的泛化理论，连接预训练与下游规划，揭示误差权衡，填补了重要空白。
- **数学严谨**：采用谱图理论、矩阵分解、Rademacher复杂度等工具，推导出清晰的上界形式。
- **实验设计针对性**：合成环境允许独立控制噪声和规划难度，直接验证理论核心预测（高噪声/长规划时潜在预测优势）。
- **概念清晰**：将输入级预测视为潜在预测的特例，统一分析。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **实验覆盖不足**：仅在一个合成2D点质量环境上验证，缺乏真实机器人、视频或复杂物理仿真中的评估。任务单一（目标到达），未测试其他规划类型（如避障、操控）。
- **假设较强**：理论部分假设转移确定性（多步规划时）、嵌入函数范数一致有界、动作条件边际概率相等（\(E_{x_g}w(x_g|a)\) 与动作无关）等，实际应用中可能不完全满足。
- **缺少消融研究**：未分析不同正则项强度、预测器结构、不同潜在维数选择对泛化界的具体影响。
- **无统计重复**：实验结果没有报告多次运行的平均值与方差，无法判断曲线稳定性。
- **算力信息缺失**：不利于复现和资源评估。
- **应用限制**：理论主要针对JEPA的特定正则化形式（谱对比损失项），对于使用stop-gradient或EMA的实际JEPA变体（如V-JEPA）是否直接适用需进一步验证。

（完）
