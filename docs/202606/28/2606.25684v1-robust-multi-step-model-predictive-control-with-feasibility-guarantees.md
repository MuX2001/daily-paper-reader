---
title: Robust Multi-step Model Predictive Control with Feasibility Guarantees
title_zh: 具有可行性保证的鲁棒多步模型预测控制
authors: "Sebastian Steffen, Mark Cannon"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25684v1"
tags: ["query:control"]
score: 9.0
evidence: 具有可行性保证的鲁棒多步MPC
tldr: 针对多步预测鲁棒非线性MPC中因预测不一致导致的递归不可行问题，提出先验检查优化问题不可行性，并利用历史解提供基于已验证可达集的备用预测策略。数值实验表明该方法有效保证了递归可行性与稳定性，填补了多步预测MPC可行性保证的理论空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25684v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 811, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25684v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25684v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 928, \"height\": 2512, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25684v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25684v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 1047, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25684v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 929, \"height\": 429, \"label\": \"Table\"}]"
motivation: 多步预测模型在MPC中可能因预测不一致导致递归不可行，现有单步模型保证不适用。
method: 先验检查优化问题不可行性，利用先前解提供基于已验证可达集的备用预测策略。
result: 数值例子验证了该方法能保证递归可行性和稳定性。
conclusion: 为多步鲁棒非线性MPC提供了可行性保证方法，促进实际应用。
---

## 摘要
我们提出了一种确保具有多步预测器的鲁棒非线性模型预测控制（MPC）的递归可行性和稳定性的方法。尽管在有限时域上递归应用单步模型的情况下，可行性保证已经得到了很好的建立，但在使用不同的多步模型来预测系统在未来不同时间点状态的朴素MPC公式中，这些保证是缺失的。这个问题的出现是因为在不同时间生成的多步预测之间存在潜在的不一致性。我们的方法先验地检查鲁棒非线性MPC优化问题可能存在的不可行性，并利用先前解的信息，基于先前验证的可达集提供后备方案。我们通过一个简单的数值例子说明了所提出的预测器替换策略。

## Abstract
We present a method of ensuring recursive feasibility and stability for robust nonlinear Model Predictive Control (MPC) with multi-step predictors. Although feasibility guarantees are well-established for the case of single-step models applied recursively over a finite horizon, such guarantees are missing in naive MPC formulations that use distinct multi-step models to predict the system state at different future points in time. This issue arises because of potential inconsistencies in multi-step predictions generated at different times. Our approach checks for possible infeasibility of the robust nonlinear MPC optimisation problem \textit{a priori}, and uses information from previous solutions to provide a fall-back based on previously verified reachable sets. We illustrate the proposed predictor-substitution strategy with a simple numerical example.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：在鲁棒非线性模型预测控制（MPC）中，多步预测模型（即对不同未来时刻使用不同的预测函数）可能产生不一致的预测，从而破坏递归可行性（recursive feasibility）——即当前时刻可行解无法保证下一时刻仍有可行解。而使用单步递归模型的传统MPC可行性保证理论无法直接适用于多步模型场景。
- **动机**：多步模型在数据驱动控制、降低线性化误差累积、提高预测精度等方面有显著优势（如文中引用的[7,8,9]），但缺乏递归可行性保证限制了其在实际中的应用。因此需要一种通用的方法，在不依赖多速率（multi-rate）框架的前提下，确保多步MPC每次都能找到一个可行解。
- **整体含义**：填补了多步鲁棒非线性MPC可行性保证的理论空白，为基于学习或非线性多步预测的MPC方案提供了坚实的理论基础，推动其向工程应用迈进。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：通过**先验检查**（a priori check）和**预测器替换**（predictor substitution）来避免因多步预测不一致导致的不可行性。具体地，在求解当前RHOCP之前，先检查一组候选控制扰动序列是否满足约束；若不满足，则利用上一时刻的可行解，将当前时刻的预测器替换为已验证的历史预测器（例如用第i+1步预测器代替第i步预测器），同时引入**可变预测时域**策略来处理终端约束无法满足的情况。
- **关键技术细节**：
  1. **不可行性指标集 I**：定义在 (17) 中，表示给定初始状态和扰动序列后，哪些预测步违反了状态/输入/终端约束。
  2. **候选扰动序列集 V_k**：包含时间平移后的上一时刻最优解 `ṽ_{0:N-1|k}` 以及其他可能减少替换次数的扰动序列（如线性插值序列）。
  3. **预测器替换规则**：如 (20) 所示，对 `i∈I_k+1` 的步骤，将 `X_i(x_{k+1}, ṽ_{0:i-1|k+1})` 替换为上一时刻的 `X^*_{i+1|k}`，从而保证约束满足。
  4. **可变时域**：当终端约束无法满足时，通过 (19) 逐步缩短预测时域 `N̄_k`，直至能找到一个满足终端约束的可行解，同时避免无意义缩短。
  5. **成本上界约束**：在 RHOCP 中加入 (24e)，确保闭环稳定性，其中 `σ̄^2` 由终端集和终端代价决定 (25)。
- **算法流程**（文字说明）：
  - **Algorithm 1**（预测器替换与时域调整）：输入当前状态、候选扰动序列、上一时刻最优代价等。外层循环递减 `N̄`（从 N 开始），内层循环遍历候选扰动序列，计算不可行指标集 I；若能在当前 `N̄` 下找到满足约束且替换次数最少的可行解（同时满足成本上界），则输出对应的 `N̄, S_k`（预测器索引序列）。
  - **Algorithm 2**（主MPC流程）：每个时刻利用 Algorithm 1 得到 `N̄_k, S_k`，然后求解 RHOCP (24) 得到最优扰动序列，施加第一个控制量。

### 3. 实验设计
- **使用的系统场景**：一个二维非线性系统（式 (26)），形式为 `x_{k+1}=Ax_k+Bu_k+ϕ(x_k)+w_k`，其中 `ϕ(x)=[0, e^{α x_2}-1]^T`，扰动有界。状态和输入均为线性约束。预测时域 N=10。
- **benchmark**：未提供明确的对比方法。文中仅通过图1示意图对比了多步MPC（使用预测器替换）与标准管式MPC（递归单步模型）的行为，并指出标准管式MPC在递归模型下具有天然可行性保证，而多步MPC在无替换时可能不可行。
- **对比方法**：没有与其他多步MPC方案（如多速率方法）进行定量比较。实验主要目的是验证所提算法能否解决人为制造的不可行问题。
- **人为制造不一致**：将第3、4步的扰动集增大一倍，从而造成预测管不一致，导致无替换时在 k=1,2 时刻不可行。这样能清晰展示算法效果。

### 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量或训练时长。实验平台仅提及使用CVXPY和CLARABEL求解器，属于常规控制优化，计算负荷较低，通常可在普通工作站CPU上完成。因此资源需求未展开讨论。

### 5. 实验数量与充分性
- **实验数量**：仅有一个数值例子，且只展示了一个特定轨迹（图3给出了多个时刻的预测管和闭环轨迹）。无多组随机初始状态、无不同噪声水平、无不同系统维度的消融研究。
- **充分性评估**：实验本身旨在作为概念验证（proof-of-concept），表明算法能恢复可行性并保持稳定性。从理论证明角度（Theorem 1,2）已较完整，但实验覆盖不足：未测试高维系统、无对照方法（如不采用替换策略直接求解的不可行频率），也未分析替换策略对亚优性的量化影响。因此实验充分性有限，但作为理论论文，实验部分尚可接受。

### 6. 论文的主要结论与发现
- **定理保证**：若初始时刻可行，则所提算法能保证所有后续时刻的RHOCP可行（递归可行性），且闭环系统满足输入-状态实际稳定（ISpS），时间平均代价上界为 `σ̄²`。
- **数值验证**：在人为制造不一致的例子中，算法在 k=1,2 时刻通过预测器替换成功得到可行解，此后无需替换，证明了算法的有效性。
- **保留标准MPC优点**：与多速率方案不同，该方法可在每个采样时刻重新计算最优控制序列，不损失控制灵活性。

### 7. 优点
- **理论创新**：首次为多步非线性鲁棒MPC提供了递归可行性保证，且不依赖于多速率框架，具有普适性。
- **方法简洁**：通过历史解信息进行预测器替换，计算开销低（仅需检查有限候选序列和替换），易于集成到现有MPC求解器中。
- **灵活性高**：可变时域策略允许在必要时期缩短预测时域，减少替换次数，从而降低亚优性。
- **算法可扩展**：候选人扰动集 `V_k` 可根据具体问题灵活构造（如文中所提线性插值），不限定求解器类型。

### 8. 不足与局限
- **实验覆盖不足**：仅一个低维例子，缺乏高维系统、不同非线性类型、不同噪声分布下的验证，通用性未充分展示。
- **亚优性分析欠缺**：预测器替换可能引入亚优性（如缩短时域、使用非最优历史解），但文中未定量评估这种亚优性对闭环性能的影响。
- **候选人集构造依赖经验**：`V_k` 仅包含平移解和极值解的线性插值，对于更复杂系统可能不足以保证总能找到可行解。虽然理论保证存在可行解（通过缩短时域），但实际搜索效率可能受影响。
- **缺乏与现有多步MPC方案的定量对比**：未与多速率MPC（如[10,11]）或其它处理不一致的方法比较控制性能、计算时间等指标。
- **应用限制**：假设多步预测模型满足 Assumption 1（一致性），但在数据驱动中严格验证该假设可能困难；另外，终端集和终端代价的构造需要系统模型信息，不适用于完全无模型情况。

（完）
