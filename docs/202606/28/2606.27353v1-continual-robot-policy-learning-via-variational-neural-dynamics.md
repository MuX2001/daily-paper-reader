---
title: Continual Robot Policy Learning via Variational Neural Dynamics
title_zh: 基于变分神经动力学的连续机器人策略学习
authors: "Jiaxu Xing, Zhiyuan Zhu, Yunfan Ren, Ismail Geles, Yifan Zhai, Rudolf Reiter, Davide Scaramuzza"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27353v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 持续机器人策略学习
tldr: "机器人部署在真实环境中常受隐藏且反复出现的动力学变化影响，现有学习控制器通常一次性训练后不再更新。本文提出一种持续学习框架，通过结合分析物理先验和神经残差构建条件感知动力学模型，并利用可微仿真在多样化隐条件采样下训练策略。部署时，策略通过在线识别近期交互推断的隐条件恢复重复动力学，避免残差重新拟合。在四旋翼飞行器风扰跟踪实验中，策略恢复时间约1秒，比在线残差拟合快5倍，大扰动悬停和跟踪误差分别降低65.7%和53.3%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27353v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 851, \"height\": 1100, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1465, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1453, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1365, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 870, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1456, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27353v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1456, \"height\": 717, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27353v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 637, \"height\": 615, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27353v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 600, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27353v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1400, \"height\": 534, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27353v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 738, \"label\": \"Table\"}]"
motivation: 机器人实际部署中动力学频繁变化，但大多数学习控制器训练后不再更新，无法利用部署经验提升性能。
method: 提出持续学习框架：结合物理先验与神经残差的条件感知动力学模型，用可微仿真采样隐条件训练策略，部署时在线推断隐条件。
result: "四旋翼风扰跟踪：恢复时间约1秒，快5倍；大扰动悬停和跟踪误差降低65.7%和53.3%。"
conclusion: 该方法通过隐条件识别而非残差拟合，显著提升机器人应对重复扰动的性能与适应速度。
---

## 摘要
部署在现实世界中的机器人很少在单一固定动力学模型下运行：风向变化、载荷变化、电池耗尽、接触点移位以及硬件磨损。然而，大多数基于学习的控制器仅训练一次便部署，仿佛学习已经完成。这阻止了机器人利用部署经验进一步提升任务性能。在这项工作中，我们提出了一种连续学习框架，利用真实世界经验在隐藏且重复出现的动力学条件下改进机器人策略。我们的方法通过将分析物理先验与用于未建模效应的神经残差相结合，从真实状态-动作轨迹中学习一个条件感知的动力学模型。一个循环编码器从最近的交互中推断出当前隐藏条件，该估计同时为残差模型和策略提供条件。策略学习通过可微仿真进行，使用从潜模型采样的多样化学习动力学。部署时，这些采样条件被在线从最近真实交互推断出的条件所替代，使策略能够通过识别而非残差重新拟合来恢复重复出现的动力学。通过广泛的仿真研究和真实世界实验，我们展示了该框架在多种未观测干扰下提升了策略性能。在变化风下的真实四旋翼轨迹跟踪中，策略大约在1秒内从重复干扰中恢复，比在线残差重新拟合快约5倍。与最先进的在线自适应方法相比，它还分别将大干扰悬停误差和跟踪误差降低了65.7%和53.3%。

## Abstract
Robots deployed in the real world rarely operate under a single fixed dynamics model: wind changes, payloads vary, batteries drain, contacts shift, and hardware wears. Yet most learning-based controllers are trained once and deployed as if learning were complete. This prevents the robot from using deployment experience to further improve task performance. In this work, we propose a continual learning framework that uses real-world experience to improve robot policies under hidden and recurring dynamics. Our method learns a condition-aware dynamics model from real state-action trajectories by combining an analytical physics prior with a neural residual for unmodeled effects. A recurrent encoder infers the current hidden condition from recent interaction, and this estimate conditions both the residual model and the policy. Policy learning is performed via differentiable simulation using diverse learned dynamics sampled from the latent model. At deployment, these sampled conditions are replaced by conditions inferred online from recent real interaction, allowing the policy to recover recurring dynamics by recognition rather than residual re-fitting. Through extensive simulation studies and real-world experiments, we demonstrate that the framework improves policy performance under diverse unobserved disturbances. On real quadrotor trajectory tracking under changing wind, the policy recovers from recurring disturbances in roughly 1s, about 5x faster than online residual re-fitting. It also reduces large-disturbance hover and tracking errors by 65.7% and 53.3% over the state-of-the-art online adaptation approaches

---

## 论文详细总结（自动生成）

# 基于变分神经动力学的连续机器人策略学习——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：机器人在真实世界部署时，其动力学环境（如风力、载荷、电池状态、硬件磨损等）是**隐藏且非平稳**的，并且这些条件会**反复出现**。现有的大多数学习型控制器采用“一次性训练、永久部署”的范式，无法利用部署中获得的经验持续改进策略。当环境条件变化或重复时，机器人需要重新适应，效率低下。

- **研究动机**：作者希望构建一个**持续学习**框架，使机器人能够在真实交互中识别隐藏的动力学条件，利用历史经验（而非从零适应）快速恢复，并**持续提升**任务性能。

- **整体含义**：本文挑战了“训练即结束”的传统思想，提出将部署视为学习过程的延续。通过构建一个**条件感知的动力学模型**，将识别（inference）与策略优化（policy optimization）统一在一个隐空间框架中，实现高效、可持续的适应。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想

- 结合**分析物理先验**（rigid-body model）与**神经残差**（neural residual），学习一个**隐条件（latent condition）调制**的动力学模型。
- 使用**循环编码器**（GRU）从近期状态-动作历史中推断隐藏条件（隐变量 \( z \)），该隐变量同时用于：
  - 在**部署时**：在线推断当前条件，以使策略自适应调整行为。
  - 在**策略学习时**：从先验分布 \( \mathcal{N}(0,I) \) 中采样隐变量，作为多样性模拟环境来训练策略。
- 通过**最大均值差异（MMD）** 将编码器的聚合分布与采样先验对齐，使隐空间可采样；通过**动力学预测损失**保证隐变量携带条件信息；辅以**辅助重构损失**保持轨迹信息。

### 2.2 关键技术细节

- **状态与动作空间**：四旋翼状态 \( s_t = (p_t, q_t, v_t) \in \mathbb{R}^{10} \)；动作 \( a_t = (T_t, \omega_t^\top) \in \mathbb{R}^4 \)。
- **动力学模型**：\( \hat{s}_{t+1} = f_{\text{prior}}(s_t, a_t) + D_\psi(s_t, a_t, z_t) \)，其中 \( D_\psi \) 是 FiLM 调制的 MLP，输出位置、速度、旋转向量残差。
- **编码器**：GRU 处理长度为 \( C=20 \)（0.4秒）的上下文窗口 \( H_{t-C:t} \)，输出隐变量 \( z_t = E_\phi(H_{t-C:t}) \in \mathbb{R}^{12} \)。
- **训练目标**：
  \[
  \mathcal{L}_{\text{vnd}} = \mathcal{L}_{\text{dyn}} + \lambda_{\text{rec}} \mathcal{L}_{\text{rec}} + \lambda_{\text{mmd}} \text{MMD}(q_\phi, p)
  \]
  - \( \mathcal{L}_{\text{dyn}} \)：Huber 损失，预测残差 \( r_t = s_{t+1} \ominus f_{\text{prior}}(s_t, a_t) \)。
  - \( \mathcal{L}_{\text{rec}} \)：辅助重构损失，解码隐变量为归一化上下文序列。
  - MMD：对齐编码器分布与标准高斯先验。
- **策略学习**：冻结 \( E_\phi\) 和 \( D_\psi \)，从先验采样 \( z \)，在并行可微仿真环境中通过 BPTT 优化策略参数 \( \theta \)：
  \[
  \max_\theta \mathbb{E}_{z\sim p(z), s_0\sim \rho_0} \left[ \sum_{t=0}^{H-1} \gamma^t r(s_t, a_t, s_{t+1}) \right]
  \]
  其中策略 \( a_t = \pi_\theta(\bar{o}_t, z) \)。

### 2.3 算法流程（文字说明）

1. 初始化基础策略（在简化仿真中训练），收集真实轨迹到缓冲区 \( \mathcal{D} \)。
2. 在部署循环中交替：
   - 在线推断隐变量 \( z_t \)，用策略执行动作，存储转换。
   - 更新变分动力学模型（编码器 + 残差模型）通过最小化 \( \mathcal{L}_{\text{vnd}} \)。
   - 冻结动力学模型，从先验采样隐变量，在可微仿真中训练策略（BPTT）。
3. 部署时：使用编码器在线推断隐变量，策略据此调整行为。

## 3. 实验设计：数据集/场景、benchmark、对比方法

### 3.1 实验场景与任务

- **真实世界实验**：四旋翼（Agilicious平台）在**受控风环境下**跟踪“8”字轨迹，频率50Hz，使用运动捕捉提供状态估计。
  - **恢复实验**：改变风速/方向，测量跟踪误差恢复时间。
  - **持续精炼实验**：间歇性风扰下，随时间收集数据并更新策略，观察误差下降。
  - **未建模硬件扰动**：切割螺旋桨，测试对全新动力学的适应能力。
- **仿真实验**：
  - 四旋翼任务：**悬停**、**轨迹跟踪**、**着陆**、**视觉感知悬停**。
  - 操作任务：**末端执行器跟踪**（Franka机械臂）、**箱体推挤**（平面推箱，隐藏物体质量/摩擦）。
- **hidden condition**：四旋翼用风（方向/速度）；操作任务用载荷质量、接触参数。

### 3.2 Benchmark与对比方法

- 对比方法：
  - **Base DiffSim**：固定仿真训练的策略。
  - **L1-MPC**：经典自适应控制。
  - **RMA**：模拟中训练隐条件策略（使用特权信息），部署时固定。
  - **DATT**：深度自适应轨迹跟踪。
  - **LOTF**：在线残差拟合（最先进的在线适应基线）。
  - **GT Dist.**：使用真实扰动标签训练的策略（作为近似上界）。
- 消融实验：移除 FiLM、MMD、重构损失、残差、替换 MLP 编码器等。

### 3.3 数据集

- 使用机器人自主收集的真实/仿真轨迹，无扰动力学标签。所有方法在相同状态/动作接口、奖励、训练/测试划分下评估。

## 4. 资源与算力

- 文中明确说明：策略训练在单个 **NVIDIA RTX 4090 GPU** 上进行。
- 四旋翼悬停/着陆：512个并行环境，1500 epochs，约 1.92×10⁸ 次可微过渡评估，约 5秒 rollout。
- 轨迹跟踪：512个环境，1500 epochs，125步 rollout，约 9.6×10⁷ 次过渡。
- Franka BPTT 训练约 10 分钟，热启动约 5 分钟；箱体推挤 PPO 使用 8192 并行环境，约 2.8–2.9 小时。
- 动力学模型训练：每轮约 15–20 秒（50000 过渡数据）。
- **注意**：真实实验的算力需求未详细给出（仅说明离线计算运行于 GPU）。

## 5. 实验数量与充分性

- 实验数量**比较丰富**：
  - 真实世界：3 组实验（重复风扰恢复、持续精炼、螺旋桨切割）。
  - 仿真：多项四旋翼任务 + 2 项操作任务。
  - 动力学模型消融：6 种变体对比（图3）。
  - 策略学习消融：与 4 种基线 + GT 上界在5个设置下对比（图5）。
  - 可重复性：真实实验每设置重复 5 次。
- **充分性与公平性**：
  - 所有方法使用相同状态、动作、奖励、训练/测试划分。
  - 消融覆盖了关键组件（FiLM、MMD、重构、RNN等）。
  - 对比了经典自适应控制、在线拟合、隐条件适应等多种基线。
  - **潜在不足**：真实实验仅在单一机器人（Agilicious）上进行；未提供多机器人或不同硬件平台的验证；感知实验依赖运动捕捉，未在纯视觉状态估计下验证。

## 6. 论文的主要结论与发现

1. **持续学习有效**：连续收集真实数据并更新策略，可将跟踪误差从 41 cm 降至 9 cm。
2. **条件识别优于重新拟合**：对于已知重复风扰，恢复时间约 1 秒（比在线残差拟合快 5 倍）。
3. **隐空间结构化**：无监督学习下，编码器根据风的方向/幅度形成清晰聚类。
4. **性能优势**：在大扰动下，悬停误差降低 65.7%，跟踪误差降低 53.3%（对比 LOTF）。
5. **泛化性**：不仅用于四旋翼，也适用于接触操作任务（末端执行器、箱体推挤），接近 GT 上界。

## 7. 优点

- **创新性**：将**条件识别**与**策略优化**统一在可采样的隐空间中，无需重拟合残差，实现“识别而非重新适应”。
- **实用性**：在真实四旋翼上验证，恢复速度快，改进持续，且能处理未预见的硬件变化（切螺旋桨）。
- **模块化设计**：分析先验 + 残差，可微分仿真，自监督学习，无需要扰动力学标签。
- **实验充分**：真实 + 仿真、多项任务、多种基线、详尽的消融实验。
- **公平比较**：所有方法在相同接口、奖励、扰动分布下评估。

## 8. 不足与局限

- **依赖较高精度状态估计**：当前实验使用运动捕捉提供准确状态；在纯视觉 SLAM 或 VIO 中，噪声和延迟可能使动力学学习混淆实际物理与传感器误差。
- **算力与实时性**：策略学习需要 GPU 离线处理，未实现完全机载实时持续学习。
- **任务范围有限**：主要验证了四旋翼和简单操作任务，未在复杂接触、非刚体或高度非结构化环境（如野外地面机器人）中测试。
- **隐空间可解释性**：虽然聚类良好，但未提供明确物理含义；MMD 对齐可能使分布丢失极端条件。
- **安全性保证**：文中未讨论策略更新时的安全约束；在线策略改变可能引入不稳定风险。
- **实验样本量**：真实实验重复 5 次，统计显著性有限；仿真未提供方差估计。

（完）
