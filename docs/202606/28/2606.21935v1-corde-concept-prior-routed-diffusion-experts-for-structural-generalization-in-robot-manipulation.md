---
title: "CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation"
title_zh: "CoRDE: 基于概念先验路由的扩散专家实现机器人操作中的结构泛化"
authors: "Haidong Huang, Xixin Zhao, Yaohua Zhou, Jiayu Song, Jiayi Zhang, Jun Ma, Haiyue Zhu, Xiaocong Li"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21935v1"
tags: ["query:robot-learn"]
score: 10.0
evidence: 机器人操作技能与抓取
tldr: 多任务长周期机器人操作中，单体扩散模型结构泛化差，语义子阶段梯度冲突。现有MoE方法路由崩溃且参数膨胀。CoRDE提出概念先验路由，利用冻结编码器语义分布通过软映射引导变分责任，结合熵控制与低秩适配专家池，实现稳健语义对齐专家分配。实验表明CoRDE减少路由崩溃，提升动作质量与增量学习效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 372, \"label\": \"Table\"}]"
motivation: 单体扩散模型在多任务长周期场景缺乏结构泛化，纯数据驱动MoE导致路由崩溃和参数爆炸。
method: 提取冻结概念编码器的语义分布，通过可学习软映射矩阵引导变分责任，并采用LoRA参数高效专家池。
result: CoRDE系统性减少路由崩溃，形成稳健语义对齐专家分配，动作质量与增量学习效率优于基线。
conclusion: 结构引导变分蒸馏框架有效解决扩散模型的结构泛化与路由崩溃问题。
---

## 摘要
扩散模型在机器人模仿学习中擅长捕捉多模态动作分布。然而，在多任务和长视野场景中，单一架构缺乏结构泛化能力，受限于不同语义子阶段之间的梯度冲突。尽管纯数据驱动的专家混合（MoE）方法引入了分工，但经常引发路由崩溃，且实例化全尺寸专家会导致参数爆炸和高扩展成本。为解决这些问题，我们提出了基于概念先验路由的扩散专家（CoRDE），一种结构引导的变分蒸馏框架。CoRDE从冻结的概念编码器中提取语义分布，通过可学习的软映射矩阵引导变分后验责任。该机制引入了一个熵控制的责任推理过程，在可靠语义预测下鼓励自信路由，同时保留随机扩散项以实现行为多样性。为了避免参数膨胀，CoRDE采用参数高效的专家池，在共享冻结主干上使用低秩适配（LoRA）。理论分析表明，混合分数差异受责任加权局部专家误差的约束，支持低秩专家适配下的高保真生成。实证评估证实，与现有基线相比，CoRDE系统地减少了路由崩溃，形成了鲁棒的、语义对齐的专家分配，同时实现了优越的动作质量和增量学习效率。

## Abstract
Diffusion models excel at capturing multi-modal action distributions in robot imitation learning. However, in multi-task and long-horizon scenarios, monolithic architectures lack structural generalization capabilities, suffering from gradient conflicts between distinct semantic sub-stages. While pure data-driven Mixture-of-Experts (MoE) methods introduce labor division, they frequently trigger routing collapse, and instantiating full-scale experts causes parameter explosion and high expansion costs. To address these issues, we propose Concept-prior Routed Diffusion Experts (CoRDE), a structure-guided variational distillation framework. CoRDE extracts semantic distributions from a frozen concept encoder to guide the variational posterior responsibility via a learnable soft mapping matrix. This mechanism introduces an entropy-controlled responsibility inference process that encourages confident routing under reliable semantic predictions while preserving the stochastic diffusion term for behavioral diversity. To overcome parameter inflation, CoRDE employs a parameter-efficient expert pool using Low-Rank Adaptation (LoRA) on a shared frozen backbone. Theoretical analysis shows that the mixture score discrepancy is bounded by responsibility-weighted local expert errors, supporting high-fidelity generation under low-rank expert adaptation. Empirical evaluations confirm that, compared to existing baselines, CoRDE systematically reduces routing collapse, forming robust, semantically aligned expert allocations while achieving superior action quality and incremental learning efficiency.

---

## 论文详细总结（自动生成）

# CoRDE: 基于概念先验路由的扩散专家实现机器人操作中的结构泛化

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：扩散模型在机器人模仿学习中能捕捉多模态动作分布，但在多任务和长视野场景下，单一架构（monolithic）缺乏结构泛化能力，不同语义子阶段（如低频大尺度导航 vs 高频精细接触）之间产生梯度冲突，导致灾难性干扰。现有纯数据驱动的专家混合（MoE）方法虽引入分工，但存在路由崩溃（routing collapse）和参数爆炸问题。
- **核心问题**：如何在不依赖人类标注的前提下，引入高层语义先验来引导专家分配，并实现参数高效的专家池，从而同时解决路由不稳定、参数膨胀和多样性保留的挑战。
- **整体含义**：提出一种结构引导的变分蒸馏框架（CoRDE），通过将冻结的概念编码器的语义分布与可学习软映射结合，形成结构化先验，与行为证据融合得到后验责任，驱动低秩适配（LoRA）专家池的训练。理论证明混合分数误差受局部专家误差约束，且通过保留SDE随机扩散项保证行为多样性。

## 2. 方法论：核心思想与关键技术细节

### 核心思想
- 利用冻结的概念编码器提取稳定的语义分布 \( p_{\phi}(c|o) \)，通过可学习的软映射矩阵 \( P_{CE}(z|c) \) 构建结构先验 \( p_{\text{struct}}(z|o) = \sum_c p_{\phi}(c|o) P_{CE}(z|c) \)。
- 在变分期望-最大化（Expectation-Maximization, EM）框架中，将结构先验与行为证据（基于教师分数场的局部拟合精度）通过几何平均融合，得到最优责任后验 \( q^{*}(z|o,a_0) \)。
- 通过控制结构引导强度 \( \lambda(o) \)（基于概念置信度自适应调整），实现双熵动力学：最小化路由熵（宏观确定性）同时保留随机扩散项的满秩方差（行为多样性）。
- 专家池参数高效化：共享冻结的扩散骨干网络，仅对每个专家注入注意力LoRA模块（低秩适配），避免全尺寸专家参数膨胀。

### 关键技术细节
- **责任推理公式**：  
  \( q^{*}(z|o,a_0) = \frac{1}{Z} \bar{r}(z|o,a_0)^{\frac{1}{1+\lambda(o)}} p_{\text{struct}}(z|o)^{\frac{\lambda(o)}{1+\lambda(o)}} \)，其中 \( \lambda(o) = \lambda_{\max} \gamma(o) \)，\( \gamma(o) \) 为归一化概念置信度。
- **路由熵定理**：当 \( \lambda \to \infty \)，后验熵趋近于结构先验熵；当结构先验置信度高时，融合偏向低路由熵 (更稳定分配)。
- **LoRA专家训练**：每个专家最小化责任加权的分数匹配损失 \( \mathcal{L}_{\text{expert}}(z) = \mathbb{E}[\ldots] \)。
- **混合分数逼近定理**：全局分数误差被责任加权局部专家误差上界控制（应用Jensen不等式）。
- **Full-Rank多样性保持**：LoRA仅修改确定性漂移项，而反向SDE中的Wiener过程持续注入满秩各向同性噪声，保证行为熵最大。
- **路由解耦与软对齐**：路由器通过KL散度拟合后验责任，避免与分数匹配任务梯度冲突；软映射矩阵通过Dirichlet平滑和EMA迭代更新，建立可解释的概念-专家多对多映射。

### 算法流程（文字说明）
- **训练阶段（离线变分蒸馏）**：
  1. 从数据集采样观察-动作对。
  2. 冻结概念编码器提取先验分布，计算置信度。
  3. 通过软映射矩阵构建结构先验。
  4. 采样扩散时间步和噪声，计算行为证据。
  5. 执行E-step：根据公式计算责任后验。
  6. 执行M-step：更新LoRA专家（责任加权分数匹配）、更新路由器（KL散度）、更新软映射矩阵（Dirichlet计数+EMA）。
- **推理阶段（状态只执行）**：
  1. 提取概念先验，计算结构先验。
  2. 路由器输出原始logits，注入结构先验偏置 \( \tilde{h}_z = h_z + \beta \log p_{\text{struct}}(z|o) \)。
  3. 选择top-k专家，通过SDE求解器利用混合分数场生成干净动作序列；单步生成（NFE=1）实现极低延迟。

## 3. 实验设计

### 数据集/场景与Benchmark
- **LIBERO**：四个标准套件（L-Spatial, L-Object, L-Goal, L-Long），用于评估长视野多任务操作的成功率及跨套件结构泛化。
- **D3IL**：七个核心任务（Avoiding, Aligning, Pushing, Stacking-1, Stacking-2, Sorting-Image, Stacking-Image），提供高度多模态人类示范，用于评估行为多样性和推理效率。

### 对比方法
- 教师基线：Diffusion Policy（Teacher，单体扩散策略）。
- 蒸馏MoE基线：Distill-MoE（Evidence-only, 基于VDD，仅依赖数据似然，无结构先验）。
- CoRDE变体：使用HiMaCon编码器、使用AutoCGP编码器（均为无标签概念编码器）。
- 在D3IL上额外对比全参数VDD（Full-parameter Variational Distillation of Diffusion）。

### 公平性控制
- 所有蒸馏学生方法共享相同的参数预算、架构、专家数K、LoRA配置和训练步数，唯一独立变量是结构先验的集成。

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量或训练时长。仅在方法论中提及利用了冻结的扩散骨干和低秩适配，但未提供具体的计算开销数据。

## 5. 实验数量与充分性

### 实验数量
- LIBERO：4个套件，每个套件报告成功率的宏观平均值；对比了4种方法（教师、证据-only、两种CoRDE变体）。
- D3IL：7个任务，每个任务报告成功率和任务熵；对比了4种方法（教师、VDD、两种CoRDE变体），并在Pushing任务上测推理时间（200次预测平均）。
- 消融实验：在LIBERO和D3IL上进行了5种变体消融（去除PCE、去除自适应λ、证据-only、去除路由器责任监督），并报告了路由诊断指标（Hroute, Hpost, PPL）。

### 实验充分性与客观性
- 在两个不同特色的基准上验证了性能（成功率、多样性、效率），消融覆盖了关键组件（结构先导、自适应融合、路由监督），证明各模块的必要性。
- 路由诊断指标明确区分了结构化分工与专家崩溃，支持了“避免路由崩溃”的声明。
- 实验设计公平：控制了参数预算和架构。但部分实验（如ABLATION）仅使用AutoCGP编码器默认设置，未重复使用HiMaCon进行交叉验证。
- 未提供统计学显著性检验（如p值或置信区间），但报告了多次种子实验结果（±标准差）。

## 6. 主要结论与发现

- **性能优势**：CoRDE在LIBERO四个套件上均优于Diffusion Policy教师和Distill-MoE基线，宏观平均成功率最高，尤其在L-Long和L-Goal套件中提升明显，验证了结构先验对长视野任务的稳定性。
- **路由机制**：CoRDE的路由熵低且专家使用均衡（PPL≈3.6），表明形成了语义对齐的专家分配而非崩溃；消融移除组件后PPL下降或路由熵升高，支持核心机制有效性。
- **多样性保持**：在D3IL上，CoRDE的任务熵接近原始教师，显著高于确定性基线；保留了多模态行为，验证了SDE随机扩散项对满秩多样性的保证。
- **推理效率**：单步生成（NFE=1）实现2.63ms延迟，较教师策略（55.62ms, NFE=64）降低一个数量级；参数增量仅为LoRA权重，无参数爆炸。
- **软映射可解释性**：通过EM自组织更新，建立了概念-专家的多对多映射，无需人工标注。

## 7. 优点

- **方法创新**：将高层语义先验与变分蒸馏深度融合，通过几何均值融合先验与证据，提出双熵动力学平衡路由确定性及行为多样性。理论证明（路由熵定理、混合分数逼近定理）提供了强数学支撑。
- **参数高效**：LoRA专家池设计避免了全尺寸专家参数膨胀，同时保留了生成模型的满秩多样性，理论解释清晰（SDE随机项独立于低秩参数）。
- **路由稳定**：通过解耦路由器优化（仅拟合后验责任而非分数梯度）和自适应λ融合，系统性地消除了路由崩溃，形成结构化、可解释的专家分配。
- **实验全面**：覆盖两个不同维度基准（LIBERO关注成功率与结构泛化，D3IL关注多模态多样性与效率），消融实验验证了每个设计的必要性，路由诊断指标直接测量专家使用健康度。

## 8. 不足与局限

- **概念编码器依赖**：性能依赖于冻结的概念编码器质量（HiMaCon/AutoCGP）；若概念提取不准确（如高模糊场景），结构先导可能引入偏差。论文通过自适应λ缓解，但未在编码器失效的极端场景下测试。
- **实验覆盖有限**：未在真实机器人平台进行物理部署验证，全部实验为仿真环境；未讨论开放词汇概念或在线分布漂移场景。未来工作提及了这些方向但尚未实现。
- **缺少完整计算开销报告**：未提供训练耗时、GPU型号/数量等，无法评估方法的实际计算成本。
- **统计显著性不足**：消融实验仅展示点估计和标准差，未做假设检验，部分差异较小（如Full CoRDE vs w/o PCE在LIBERO上0.87 vs 0.76），可能无法排除随机波动。
- **专家数K的选择**：论文未消融专家数K的影响，也未说明如何确定K值；可能影响路由诊断指标和性能。
- **可解释性局限性**：虽然软映射建立了概念-专家关联，但未展示实际的映射示例或可视化分析，对“可解释”的支撑尚不充分。

（完）
