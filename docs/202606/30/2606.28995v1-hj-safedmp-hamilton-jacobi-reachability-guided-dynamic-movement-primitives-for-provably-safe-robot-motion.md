---
title: "HJ-SafeDMP: Hamilton-Jacobi Reachability-Guided Dynamic Movement Primitives for Provably Safe Robot Motion"
title_zh: HJ-SafeDMP：基于Hamilton-Jacobi可达性引导的动态运动基元实现可证明安全的机器人运动
authors: "Siddhanth Ramesh, Ravi Prakash"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28995v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 使用动态运动原语和HJ可达性进行安全运动规划
tldr: 机器人安全运动需兼顾鲁棒性与形式安全保证。动态运动基元（DMP）具备稳定性和泛化能力但缺乏安全证书，而汉密尔顿-雅可比（HJ）可达性分析可提供最坏情况安全裕度却计算复杂。本文提出HJ-SafeDMP框架，通过离线学习控制屏障值函数（CBVF）并使用闭式控制律实时调制DMP输出，避免了在线二次规划求解。在7自由度机器人上，该方法以数量级更快的速度实现了形式安全保证，同时保持了DMP的适应性，为安全人机交互提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1812, \"height\": 923, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 319, \"label\": \"Table\"}]"
motivation: 现有DMP缺乏安全保证，HJ可达性分析计算成本高，难以实时应用。
method: 融合DMP与学习到的HJ可达性安全值函数，离线训练CBVF并在线以闭式控制律实现安全滤波。
result: 在7自由度机器人上，执行速度比优化基线快数个数量级，同时保持DMP的鲁棒性。
conclusion: 提出HJ-SafeDMP框架，实现了可证明安全、计算高效的机器人运动，适用于人机交互场景。
---

## 摘要
在安全关键环境中部署的机器人必须执行既对干扰具有鲁棒性又可证明避免碰撞的运动。动态运动基元（DMP）具有内在稳定性、时间灵活性以及从单次演示中高效泛化轨迹的能力，但缺乏形式化的安全保证。相反，Hamilton-Jacobi（HJ）可达性分析为计算最坏情况下的安全裕度和前向不变安全集提供了理论框架，但经典的基于网格的方法受到维度诅咒的制约，不适用于实时控制。本文提出了HJ-SafeDMP框架，该框架将DMP与基于学习的HJ可达性安全值函数相结合，以实现可证明安全、鲁棒且计算高效的机器人运动。我们通过无模型的有限差分HJ递归从离线演示数据中学习控制障碍值函数（CBVF），并通过一个闭式控制律对DMP输出进行调制，将其部署为实时安全滤波器。与基于优化的CBF-QP方法不同，我们的方法无需在线求解二次规划即可实现安全滤波，保持了DMP的计算效率。我们进一步引入了基于expectile的离线学习目标，避免查询分布外的动作，以及一个共形预测校准步骤，提供有限样本概率安全覆盖。在7自由度机器人机械臂上的实验评估表明，HJ-SafeDMP实现了形式化的安全保证，执行速度比基于优化的基线方法快数个数量级，同时保持了DMP在人机交互中的鲁棒性和适应性。

## Abstract
Robots deployed in safety-critical environments must execute motions that are simultaneously robust to disturbances and provably safe from collisions. Dynamic Movement Primitives (DMPs) offer inherent stability, temporal flexibility, and efficient trajectory generalization from single demonstrations, but they lack formal safety certificates. Conversely, Hamilton-Jacobi (HJ) Reachability analysis provides a principled framework for computing worst-case safety margins and forward-invariant safe sets, but classical grid-based methods suffer from the curse of dimensionality and are impractical for real-time control. This paper introduces HJ-SafeDMP, a framework that integrates DMPs with learned HJ Reachability-based safety value functions to achieve provably safe, robust, and computationally efficient robot motion. We learn a Control Barrier Value Function (CBVF) from offline demonstration data using a model-free, finite-difference HJ recursion and deploy it as a real-time safety filter via a closed-form control law that modulates the DMP output. Unlike optimization-based CBF-QP approaches, our method achieves safety filtering without online quadratic program solves, preserving the computational efficiency of DMPs. We further incorporate an expectile-based offline learning objective that avoids querying out-of-distribution actions, and a conformal prediction calibration step that provides finite-sample probabilistic safety coverage. Experimental evaluation on a 7-DOF robot manipulator demonstrates that HJ-SafeDMP achieves formal safety guarantees with orders-of-magnitude faster execution than optimization-based baselines, while maintaining the robustness and adaptability of DMPs for human-robot interaction.

---

## 论文详细总结（自动生成）

## 论文：HJ-SafeDMP：基于Hamilton-Jacobi可达性引导的动态运动基元实现可证明安全的机器人运动

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：机器人（尤其是部署在人机共融环境中的机械臂）在执行运动时，需要同时满足**鲁棒性**（能稳定、有目的地完成任务）和**形式化安全保障**（可证明避免碰撞）。然而，现有方法难以兼顾这两者。
- **现有方法的局限性**：
  - **Dynamic Movement Primitives (DMP)**：能通过单次演示学习轨迹，具有内在稳定性和时间/空间灵活性，但**缺乏形式化安全保证**；常与人工势场（APF）等反应式方法结合，但APF存在局部极小值、振荡、无安全证书。
  - **Control Barrier Functions (CBF)** 及其基于二次规划（QP）的滤波器：可提供严格的安全前向不变性，但每次控制周期需**在线求解QP**，计算开销大（～0.32 s/步），难以满足高频控制。
  - **Hamilton-Jacobi (HJ) 可达性分析**：可计算最坏情况下的安全裕度和前向不变安全集，但传统基于网格的方法受“维度诅咒”影响，对高维系统不实用且无法实时。
- **本文目标**：将DMP的**计算高效性**与HJ可达性的**形式化安全保证**结合起来，提出一个**离线学习、在线闭式调制**的框架，使机器人运动同时具备鲁棒性、安全性和计算效率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 离线阶段：从演示数据中学习一个**Control Barrier Value Function (CBVF)**，该函数编码了未来最坏情况下的安全裕度；通过有限差分HJ递归和expectile回归实现无模型学习；并通过共形预测校准提供有限样本概率安全覆盖。
  - 在线阶段：利用训练好的CBVF的梯度，构造一个**闭式安全调制项**，直接叠加到DMP的命令加速度上，无需在线优化。
- **关键技术细节**：
  1. **数据收集**：从演示+探索轨迹中获取数据集 `D = {(x_t, u_t, x_{t+1})}`，并计算每个状态的安全指标 `ℓ(x)`（到障碍物的带符号距离）。
  2. **CBVF学习**：采用有限差分迭代公式 `B_D_target = (1-γ)ℓ(x) + γ min{ℓ(x), B_ψ(x')}`，其中γ接近1（贴现因子）。用**expectile损失函数**（τ > 0.5）训练神经网络B_θ，使得B_θ逼近数据支持的“最佳安全值”，避免查询分布外动作。
  3. **共形预测校准**：在安全边界附近采样校准状态，计算校正边距δ*，使得概率上1-βs的水平上，安全覆盖率达到1-εs。
  4. **闭式安全调制**：将DMP原本的加速度命令加上一个**安全调制度**：
     - `f_HJ(x) = k_s / ξ(B_θ(x)) · ∇_x B_θ(x)`，当B_θ < B_thresh时激活；
     - `ξ(b) = max(b - δ*, ε_min)`，确保边界附近产生强排斥力；
     - 该调制项使系统在靠近安全边界时自动偏离危险方向，无需求解QP。
  5. **自适应时间缩放**：根据安全调制引起的偏差量动态调整DMP的时间缩放因子τ，保证平滑恢复。
- **算法流程（文字说明）**：
  - **离线阶段**：收集数据集 → 计算安全标签 → 用expectile回归训练CBVF → 共形预测校准得到δ* → 学习DMP参数。
  - **在线阶段**：每个控制周期：计算DMP加速度 → 评估CBVF及其梯度 → 若接近安全边界则叠加闭式安全调制 → 更新τ → 输出最终加速度。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **场景**：7自由度 Franka Emika Panda 机械臂在仿真环境中运行，工作空间为1.1×1.1×1.1 m，障碍物为球体（静态和动态），控制器频率200 Hz。
- **数据集**：
  - 使用 **LASA手写数据集**（2D轨迹），提升至3D（固定z高度），随机旋转、时间重采样、低通滤波。
  - DMP使用25个基函数。
  - CBVF训练数据集：由演示轨迹+探索轨迹组成，共计75K条转移样本。
- **对比方法**（baselines）：
  - **NODE-CLF-CBF**：基于神经ODE的动力学 + CBF-QP在线优化（提供形式化安全保证，但计算量大）。
  - **DMP-APF**：标准DMP + 人工势场（无形式化安全保证）。
  - **SafeDMPs**：DMP + 时空管（STT，闭式调制，但管边界为手工设计）。
- **评价指标**：
  - 执行时间（计算效率）
  - 名义轨迹MAE、受扰后MAE及收敛时间
  - 碰撞率、最小距障碍物距离、避障收敛时间
  - 是否提供形式化安全/鲁棒恢复/无需在线优化

### 4. 资源与算力

- **论文未明确说明**使用了何种GPU型号、数量及训练时长。文中仅提到CBVF是一个2层隐藏层、每层256个神经元的MLP，使用Adam优化器，学习率3e-4，训练样本75K。没有提及训练时间或硬件配置。**这属于论文信息缺失之处。**

### 5. 实验数量与充分性

- **主要对比实验**：在**100次试验**中评估碰撞率（静态和动态障碍物），每次试验是否成功。结果表明HJ-SafeDMP实现0%碰撞。
- **消融实验**：
  - **Expectile参数τ**：比较了τ=0.5,0.7,0.8,0.9,0.99对安全率的影响，选择τ=0.9。
  - **共形预测校准**：对比有无校准（δ*=0 vs δ*=-0.04），校准后碰撞率从2%降至0%。
- **充分性评估**：
  - 实验覆盖了**名义性能、鲁棒性（扰动）、安全性（静态/动态障碍物）**，且对比了不同方法的计算时间、MAE、碰撞率等关键指标。
  - 消融实验对核心超参数（τ、校准）进行了验证，但**缺乏对ks、B_thresh等其他设计参数的敏感性分析**。
  - 仅使用了单一机器人（Franka Panda）仿真，未扩展到真实机器人或不同构型机器人，也未与其他场景（如移动机器人）进行比较。泛化性有限。

### 6. 论文的主要结论与发现

- HJ-SafeDMP 在**计算效率**上比基于QP的CBF方法（NODE-CLF-CBF）快**三个数量级**（1.2e-4 s vs 0.32 s），同时保持与DMP-APF和SafeDMPs相近的效率。
- 在**安全性**上，达到与NODE-CLF-CBF相同的**0%碰撞率**，而DMP-APF为14%。
- **鲁棒性**：受扰后可平滑恢复，收敛时间（0.031 s）优于NODE-CLF-CBF（1.294 s）和DMP-APF（0.411 s）。
- **避障收敛时间**优于SafeDMPs（0.287 s vs 0.633 s），因为CBVF具备全局前瞻信息。
- 共形预测校准和expectile参数选择对最终安全性能至关重要。

### 7. 优点：方法或实验设计上的亮点

- **创新性**：首次将**离线学习的HJ可达性安全值函数**与DMP结合，替代了传统在线QP优化，保留了DMP的实时性。
- **闭式调制**：仅需一次前向+梯度计算，省去在线优化，实现数量级加速。
- **离线学习与数据驱动**：采用expectile回归避免分布外动作，共形预测提供概率安全覆盖，增加了实际部署可靠性。
- **实验设计合理**：对比了多种代表性方法（基于QP、基于APF、基于手工管），覆盖了计算、安全、鲁棒等多个维度。

### 8. 不足与局限

- **形式化保证并非绝对**：CBVF为神经网络近似，未经过形式化验证，安全保证是经验性的；论文自己也承认这一点。
- **安全规范需预先已知**：假设能用带符号距离函数描述障碍物几何，对复杂形状障碍物可能不适用。
- **数据集依赖**：需要覆盖安全与不安全区域的**多样化离线数据**；若训练数据分布偏移，部署时可能失效。
- **实验覆盖有限**：仅使用单一机器人仿真，未在真实物理机器人上验证；也未涉及多障碍物密集场景或高维全身避障。
- **未报告训练算力信息**：缺少GPU型号、训练耗时等，影响可复现性。
- **超参数选择缺少系统分析**：如增益k_s、激活阈值B_thresh等没有进行详细敏感性实验。
- **缺乏与其他离线安全RL方法（如SafeFQL、EpiFlow）的直接比较**，仅与在线方法和手工管方法对比。

（完）
