Title: Specification-aware Robustness Margins for Symbolic Controllers

URL Source: https://arxiv.org/pdf/2606.28143v1

Published Time: Mon, 29 Jun 2026 00:57:03 GMT

Number of Pages: 17

Markdown Content:
> 1

# Specification-aware Robustness Margins for Symbolic Controllers 

Youssef Ait Si, Antoine Girard, and Adnane Saoud 

Abstract 

We address the problem of robust controller synthesis for a class of linear temporal logic (LTL) specifications over families of perturbed systems using symbolic control techniques. Given a dynamical system, a specification, and a symbolic controller synthesized using the fixed-point algorithm of the specification, the objective is to find the maximal perturbation we can apply to the system while the system continues to satisfy the same specification under the same controller. We first provide general results, by demonstrating that controllers synthesized based on the symbolic model can be refined back to a perturbed version of the concrete system while preserving their correctness. Focusing on four fundamental temporal logic specifications, namely safety, reachability, persistence, and recurrence, we introduce a general measure of the maximal robustness margin. Then, for each class of specifications, we derive a customized version of the measure and establish the corresponding theoretical guarantees. Importantly, the robustness margin depends explicitly on the sequence of sets generated during the fixed-point computation, allowing for specification-dependent and less conservative bounds compared to generic abstraction-based approaches. The theoretical developments are illustrated on two examples, demonstrating the practical applicability and effectiveness of the proposed approach. 

I. I NTRODUCTION 

The growing complexity of cyber-physical systems (CPS) has highlighted the critical role of formal methods in controller design, particularly for systems governed by complex temporal logic specifications. Ensuring correctness in such systems is essential, as failures can lead to safety risks and performance degradation in applications like autonomous vehicles, robotics, and industrial automation [1]–[4]. One effective methodology to achieve such guarantees is through Abstraction-Based Controller Design (ABCD) [5], [6], [7]. The process of ABCD begins by discretizing the state and input spaces of the nonlinear continuous system to create a finite-state symbolic abstraction. This step employs reachability analysis techniques [8], [9] to approximate the system’s reachable set of each state and input. The construction of such symbolic models is a well-established foundation [10]–[12]. Next, controllers are synthesized on this abstraction using tools from model checking and formal methods to ensure that complex temporal logic specifications are satisfied [13], with correct by constructions controllers. Finally, the discrete controller is refined back into the continuous domain, ensuring that the correctness properties established on the abstraction hold for the original system. The soundness of this entire procedure depends fundamentally on relationships such as alternating simulation or feedback refinement relations that formally link the original concrete system with its discrete abstraction [5], [14]–[16], thereby preserving correctness guarantees across the abstraction-refinement stage. Controller synthesis under complex temporal specifications has been extensively studied for dynamical systems subject to uncertainties and disturbances. One line of work investigates robustness through the lens of assume-guarantee contracts, examining how a system’s ability to satisfy a specification with respect to assumption’s violation [17]. A complementary perspective defines robustness in terms of trajectory proximity: perturbed trajectories are required to remain close to their nominal disturbance-free counterparts [18]–[20]. More recently, new resilience metrics [21]–[23] have been introduced for both controlled and autonomous discrete-time systems operating under finite linear temporal logic (LTL f ) specifications [24]. Nevertheless, these approaches are limited in scope: they do not yield deterministic procedures for nonlinear systems and are limited to fragments of LTL f . To address the presence of disturbances and modeling errors, abstraction-based methods often incorporate explicit uncertainty bounds and compute conservative over-approximations of reachable sets under worst-case realizations of the uncertainties [5], [6], [25]. Along these lines, [16] proposes the construction of robust finite abstractions with suitable safety margins, facilitating controller synthesis from temporal-logic specifications despite intersample behavior, imperfect state measurements, and unmodeled dynamics. To reduce the conservatism of these methods, [26], [27] develop adaptive abstractions based on state-input dependent disturbance bounds, together with an incremental refinement algorithm that updates only the regions affected by changes in the uncertainty model. Existing robust abstraction methods presented above typically rely on a fixed and predefined set of disturbances. In contrast, this paper focuses on synthesizing controllers that ensure a given specification is satisfied under the maximal admissible disturbance without requiring prior knowledge of the disturbance set. Indeed, given a system, a specification, and a symbolic controller designed to satisfy that specification for the nominal (disturbance-free) system, this paper addresses the following  

> Youssef Ait Si and Adnane Saoud are with the College of Computing, University Mohammed VI Polytechnic, Benguerir, Morocco. (e-mail: {youssef.aitsi, adnane.saoud }@um6p.ma) Antoine Girard is with Universit´ e Paris-Saclay, CNRS, CentraleSup´ elec, Laboratoire des Signaux et Syst` emes, 91190, Gif-sur-Yvette, France (e-mail:
> {antoine.girard@centralesupelec.fr).
> arXiv:2606.28143v1 [eess.SY] 26 Jun 2026 2

TABLE I: Organization of theoretical results across the four temporal specifications.            

> Specification Controllers Refinement equivalence theorem
> Safety Prop. 2 Thm. 4 Reachability Prop. 4 Thm. 5 Persistence Prop. 6 Thm. 6 Recurrence Prop. 8 Thm. 7

question: What is the maximum disturbance that the system can tolerate while still satisfying the specification under the same designed controller? 

Our work significantly advances the state of the art beyond [28] by introducing a more flexible and less conservative framework for controller synthesis under uncertainty. While [28] relies on ensuring an alternating simulation relation between the abstract and concrete models and computes the maximal admissible perturbation to preserve this relation, its generality leads to overly conservative results. In our approach, by focusing on commonly encountered specification classes such as reachability, safety, persistence, and recurrence, we are able to tolerate significantly larger perturbations while still ensuring the correctness of the synthesized controllers. Moreover, we provide proofs establishing the new robustness margins exceed the previously bounds in [28]. To achieve these results, we consider a discrete-time control system and construct its symbolic abstraction to enable formal controller synthesis. We introduce the notion of a perturbed symbolic abstraction , formally defined as the symbolic model of a perturbed system. The core of our approach lies in determining the maximal perturbation bound that preserves equivalence between the predecessor operators for the nominal and perturbed abstractions. We seek the maximal perturbation magnitude that preserves this equivalence at each iteration of the fixed-point algorithm [29], thus ensuring identical outcomes. This equivalence condition is key to preserving correctness across iterations, enables the quantification of the specification-dependent maximum robustness margin, and implies the resulting controllers from the fixed-point algorithm are guaranteed to be functionally equivalent. We then demonstrate how controllers synthesized for the nominal abstraction can be correctly refined to a perturbed concrete system, as long as the perturbations introduced remain below this computed maximal margin bound. This refinement procedure ensures the controller maintains correctness-by-construction for the concrete system. The perturbed symbolic abstraction and its controller synthesis thus serves as an analytical tool for determining exact bound on the robustness margins. Two numerical examples are proposed to show the performance of the proposed approach. The contributions developed in this paper are as listed below. 1) A general definition and characterization of the robustness margin for safety, reachability, persistence, and recurrence specifications, computed directly from the sets generated by the corresponding fixed-point synthesis algorithm. 2) A refinement procedure that maps a controller synthesized on the disturbance-free symbolic abstraction directly to the perturbed continuous system. 3) Theoretical proofs showing our margins exceed previously known bounds in [28]. Although the technical development is presented for the four specifications above, controller synthesis for several important fragments of LTL can be reduced to these classes. Specifically, synthesis for an LTL formula φ is typically performed on the product of the system and a deterministic ω-automaton recognizing φ, where the automaton’s acceptance condition determines the resulting specification class: safety-LTL reduces to safety, co-safety-LTL to reachability, LTL formulas recognized by deterministic B¨ uchi automata reduce to recurrence, and those recognized by deterministic co-B¨ uchi automata reduce to persistence [13], [15], [30]. Consequently, the robustness margins developed in this work extend directly to controllers synthesized for these LTL fragments. Table I summarizes the parallel structure of Sections IV, V, VI and VII, each treating one of the four temporal specifications using the general results established in Section III. The remainder of the paper is organized as follows.: Section II shows some required preliminaries on transition systems, control systems, symbolic abstractions, fixed-point and mu-calculus theory. In Section III, we provide general results, for refinement procedure and robust predecessor operator. In Sections IV, V, VI, and VII, we address the specifications of safety, reachability, persistence, and recurrence, respectively. For each specification, we present the corresponding fixed-point algorithm, demonstrate how to compute the maximal robustness margins, and show how to refine the associated robust controller for the concrete perturbed system. Section VIII shows that, the margins obtained in Sections IV–VII are less conservative than the abstraction-based margin of [28]. Finally, in Section IX, illustrative examples are proposed in order to show the efficiency of the proposed approach. II. P RELIMINARIES 

Notations: The symbols N, N≥0, R, and R≥0, denote the set of positive integers, nonnegative integers, real and non-negative real numbers, respectively. Given sets X and Y , we denote by f : X → Y an ordinary map from X to Y , whereas f : X ⇒ Y

denotes a set valued map. The notation 2X represents the power set of X, which is the set of all possible subsets of X. For a set 

X, cl( X) denotes its closure, Int( X) its interior. For x ∈ Rn, the infinity norm of x is denoted by ∥x∥. The closed ball centered 3

at x ∈ Rn with radius r is defined by Br (x) = {y ∈ Rn|∥ x − y∥ ≤ r}. The projection map taking (x, u ) ∈ X × U to x ∈ X

is denoted by πX . Let X∗ denotes the set of finite sequences of elements in X, and Xw denotes the set of infinite sequences of elements in X. Given a collection of sets L = {X0, X 1, . . . , X n} ⊆ Xn+1 , we define the index map j : X → N S{∞} as follows: jL(x) = inf {i ∈ N | x ∈ Xi} with the convention inf {∅} = ∞.

A. Transition Systems 

We introduce the concept of a transition system [3], which serves as a common modeling framework for both continuous control systems and their symbolic models. 

Definition 1. A transition system is a tuple S = ( X, X 0, U, ∆) , where:  

> •

X is the set of states,  

> •

X0 ⊆ X denotes the set of initial states,  

> •

U is the set of control inputs,  

> •

∆ ⊆ X × U × X defines the transition relation. A transition (x, u, x ′) ∈ ∆ signifies that the state x′ is reachable from the state x under input u. This can be denoted as 

x′ ∈ ∆( x, u ), and we refer to x′ as a u-successor of x.

Given a state x ∈ X, the set of admissible (or enabled) inputs is denoted by U a(x) = {u ∈ U | ∆( x, u )̸ = ∅} . A transition system S is called non-blocking if for every state x ∈ X, we have U a(x)̸ = ∅. Throughout the paper, we will utilize one fundamental operator to formalize state-control pairs that leads to successors within a given target set Z ⊆ X × U , called predecessor operator [31]. 

Pre ∆(Z) = {(x, u ) ∈ X × U | ∅̸ = ∆( x, u ) ⊆ πX (Z)}, (1) Where πX (Z) = {x ∈ X|∃ u ∈ U, (x, u ) ∈ Z} is the projection of Z in the space of states X and with the convention that 

Pre ∆(∅× U ) = ∅× U . A behavior of the transition system S = ( X, X 0, U, ∆) is a sequence σ = (( x0, u 0), (x1, u 1), (x2, u 2), . . . , x N ),

with N ∈ N S{+∞} , such that x0 ∈ X0, ui ∈ U a(xi), and xi+1 ∈ ∆( xi, u i) for all i < N . The associated state trajectory is 

σx = ( x0, x 1, . . . , x N ). A behavior is:  

> •

maximal if it cannot be extended further while preserving the same initial segment,  

> •

complete if its length is infinite, i.e., N = + ∞.The set of all maximal behaviors of S is denoted by H(S), and the corresponding set of all maximal state trajectories is written 

Hx(S). A specification for S is a subset of X∗ ∪ Xw, i.e., HSpec ⊆ X∗ ∪ Xw, representing admissible state trajectories. The system satisfies the specification if Hx(S) ⊆ H Spec .

B. Controlled transition systems 

We now formalize the notion of a controller for a transition system. Consider the transition system S(Σ) = ( X, X 0, U, ∆) ,and let a controller be defined as a set-valued map C : X ⇒ U , where C(x) ⊆ U a(x) for every state x ∈ X. The domain of the controller is denoted by dom( C) = {x ∈ X | C (x)̸ = ∅} ⊆ X, representing the set of states for which control actions are enabled. Based on this, we define the control transition system as the tuple SC = ( XC , X C,0, U C , ∆C ), where:  

> •

XC = X ∩ dom( C) is the set of admissible states;  

> •

XC,0 = X0 ∩ dom( C) denotes the set of initial states;  

> •

U C = U is the input space;  

> •

The control transition relation is given by: 

x′ ∈ ∆C (x, u ) ⇐⇒ x′ ∈ ∆( x, u ) and u ∈ C (x),

meaning that a transition is allowed if and only if the input u is permitted by the controller at state x. Given a specification 

HSpec ⊆ Xw ∪ X∗, C is said to be a controller for the system S and specification HSpec if Hx(SC ) ⊆ H Spec .

C. Control Systems 

Consider a discrete-time control system Σ described by 

xk+1 = f (xk, u k, d k), (2) where xk ∈ X ⊆ Rn is the state, uk ∈ U ⊆ Rp the control input, and dk ∈ D ⊆ Rq a disturbance input. Moreover, we define a perturbed version of this system, denoted Σε, as xk+1 ∈ f (xk, u k, d k) + Bε(xk ,u k )(0) , where Bε(xk ,u k )(0) denotes the closed ball centered at zero with radius ε(xk, u k), and ε : X × U → R≥0. We can model the system Σ as a transition system 

S(Σ) = ( X, X 0, U, ∆) , where:  

> •

X ⊆ Rn is the state space,  

> •

X0 ⊆ X the initial states,  

> •

U ⊆ Rp the control inputs, 4 

> •

∆( x, u ) = f (x, u, D ) defines the set of possible successors for each (x, u ).Similarly, the perturbed system Σε can be described as a transition system S(Σ ε) = ( X, X 0, U, ∆ε), where x′ ∈ ∆ε(x, u ) if and only if 

f (x, u, D ) + Bε(x,u )(0) .

D. Symbolic abstraction of dynamical systems 

The symbolic abstraction of S(Σ) , denoted Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d), is constructed as follows:  

> •

Xd = {q0, q 1, . . . , q N }, where q0 = Rn \ X, and {qi}Ni=1 is a finite partition of X, 

> •

X0 

> d

⊆ Xd is the set of symbolic initial states,  

> •

Ud = {v1, . . . , v M } ⊆ U is the set of symbolic inputs,  

> •

∆d ⊆ Xd × Ud × Xd is the transition relation defined by: 1) For q ∈ Xd \ { q0}, q′ ∈ ∆d(q, v ) if cl( f (q, v, D )) ∩ cl( q′)̸ = ∅, where f (q, v, D ) is an over-approximation of f (q, v, D ),i.e f (q, v, D ) ⊆ f (q, v, D ).2) ∆d(q0, v ) = Xd for all v ∈ Ud.Using this construction of the symbolic model Sd(Σ) , one can ensure the existence of an alternating simulation relation from 

Sd(Σ) to S(Σ) [3]. Given a perturbation map ε : X × U → R≥0, the symbolic model of the perturbed system Σε is the tuple Sd(Σ ε) = (Xd, X 0 

> d

, U d, ∆d,ε ), where Xd, X0 

> d

, and Ud are defined as for the nominal system Σ and the transition relation is defined as follows: 1) For q ∈ Xd \ { q0}, q′ ∈ ∆d,ε (q, v ) ⇐⇒ {cl  f (q, v, D ) + Bεd(q,v )(0) } ∩ cl( q′)̸ = ∅, where 

εd(q, v ) = sup 

> x∈q

ε(x, v ) (3) bounds the perturbation over the couple (q, v ).2) For q = q0: ∆d,ε (q0, v ) = Xd, ∀v ∈ Ud.

Note that this symbolic model of the perturbed system will be only used as analytical tools for proving the main results in this work. We define a quantizer Q : X → Xd that maps each continuous state x ∈ X to a corresponding discrete (symbolic) state 

q ∈ Xd, such that x ∈ q. The inverse quantization map Q−1 : Xd ⇒ X is formally given by Q−1(q) = {x ∈ X | x ∈ q}

for each state q ∈ Xd. For a discrete subset A ⊆ Xd, the inverse image is extended as Q−1(A) = S 

> q∈A

Q−1(q), implying that x ∈ Q−1(A) if and only if there exists q ∈ A s.t. x ∈ q. This inverse mapping can also be generalized to trajectory-level specifications. Let HSpec ⊆ Xwd ∪ X∗ 

> d

denote a set of symbolic trajectories satisfying a specification. The corresponding set of continuous trajectories is defined as Q−1(HSpec ) ⊆ Xw ∪ X∗, where Q−1(HSpec ) = {σx = ( x0, x 1, . . . ) ∈ Xw ∪ X∗ |

(Q(x0), Q (x1), . . . ) ∈ H Spec }.

We define the maximal robustness margin by the map η : Xd × Ud × 2Xd → R≥0, quantifying the largest admissible perturbation for a symbolic state-control pair (q, v ) ∈ Xd × Ud such that all its successors remain within the interior of a target set A ⊆ Xd. Formally, it is defined as 

η(q, v, A ) = 



sup ε ≥ 0 cl( f (q, v, D )) + Bε(0) ⊆

Int( Q−1(A)) 

if cl( f (q, v, D )) ⊆ Int( Q−1(A)) ,

0 otherwise .

(4) We now introduce the maximal robustness margin over a collection of sets L = {Z0, Z 1, . . . , Z m}, where Zi ⊆ Xd × Ud for i ∈{0, 1, . . . , m }. This margin is defined by the map γ : Xd × Ud × (2 Xd×Ud )m → R≥0, which assigns to each (q, v ) ∈ Xd × Ud and L, the value 

γ(q, v, L ) = η(q, v, I (q, v, L )) , (5) with 

I(q, v, L ) = 

> m

\

> i=0

{πX (Zi) | Zi ∈ L, (q, v ) ∈ P re ∆d (Zi)},

=

> m

\

> i=0

{πX (Zi) | Zi ∈ L, ∆d(q, v ) ⊆ πX (Zi)} ⊆ Xd.

The map γ provides a unified measure based on the map η, but considers the intersection of all sets πX (Zi) with Zi in L and for which the successors of (q, v ) belongs to Zi. This construction allows us to provide a common robustness margin for a collection of sets L. Thus, the map γ seeks the maximum margins expanding the reachable set of (q, v ) to remain inside the intersection of all the sets containing the successors of (q, v ). The intersection I(q, v, L ) ensures only the minimal zone of sets containing the successors are considered. If the sets 

Zi are ordered, i.e., Zi ⊆ Zi+1 , then I(q, v, L ) corresponds to the smallest set Zi that contains the successors of (q, v ).Before presenting our main results, we first introduce the fundamental concept of fixed-point theory and the μ-calculus [3], [30]. 5

E. fixed-points and μ-calculus 

In the study of algorithms characterized by fixed-point expressions, a central concept is monotone functions. Given the transition system 

S(Σ) = ( X, X 0, U, ∆) in (2), a map G : 2 X×U → 2X×U is said to be monotone if for all Z1, Z 2 ⊆ X ×U , with Z1 ⊆ Z2, G(Z1) ⊆ G(Z2).A subset Z∗ ⊆ X × U is considered a fixed-point of G if Z∗ = G(Z∗). Among the fixed-points of G, two particular types are of special interest: maximal and minimal fixed-points. A fixed-point ˆZ of G is called the maximal fixed-point if, for any Z ⊆ X × U , Z ⊆

G(Z) = ⇒ Z ⊆ ˆZ. Conversely, a fixed-point ˇZ of G is called the minimal fixed-point if, for any Z ⊆ X × U , Z ⊇ G(Z) = ⇒ Z ⊇ ˇZ. 

To denote these fixed-points, we use ν for the maximal fixed-point and μ for the minimal fixed-point, defined as follows: ˆZ = νZ.G (Z), and 

ˇZ = μZ.G (Z). The computation of the maximal fixed-point can be achieved through non-growing iterations as follows: ν0Z.G (Z) = X ×U

and νi+1 Z.G (Z) = G(νiZ.G (Z)) . In this iteration process, νiZ.G (Z) represents the result after i iterations, starting from the initial set 

X×U . Similarly, the minimal fixed-point is computed using non-shrinking iterations μ0Z.G (Z) = ∅×U and μi+1 Z.G (Z) = G(μiZ.G (Z)) .

Here, μiZ.G (Z) denotes the result after i iterations, starting from the set ∅ × U . The following theorem from [30], [32] states the existence and uniqueness of minimal and maximal fixed-points for monotone functions over finite domains. 

Theorem 1 ( [30], [32]) . Let G : 2 X×U → 2X×U be a monotone function with X and U finite sets. Then there exist integers n1, n 2 ∈ N such that νn1+1 Z.G (Z) = νn1 Z.G (Z) and μn2+1 Z.G (Z) = μn2 Z.G (Z). Moreover, the fixed-points are unique and satisfy νZ.G (Z) = 

νn1 Z.G (Z) and μZ.G (Z) = μn2 Z.G (Z).

The next section presents general theoretical results applicable to any specification. 

III. G ENERAL RESULTS FOR ROBUSTNESS MARGINS 

In this section, we present two general results that are independent of the specification. First, we establish a refinement procedure that lifts a controller synthesized for the abstract nominal system to a controller for the perturbed concrete system under any specification. This refinement is a key step to ensures correctness-by-construction is preserved when transferring the controller to the perturbed concrete system. Compared with existing refinement frameworks based on simulation and approximate bisimulation relations [3], [33], [34], our approach explicitly characterizes the perturbation bounds required for correctness preservation. Second, considering two predecessor operators defined for the nominal and perturbed symbolic models, we derive conditions under which the perturbation guarantees equality of the two predecessor operators. 

A. Refinement procedure 

In the next lemma, we establish a result showing that the behavior of the perturbed concrete system is contained in that of the perturbed symbolic system, under appropriate conditions on the respective perturbation maps. 

Lemma 1. Consider the discrete-time control system Σ in (2) and its symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D. Let the maps ε : X × Ud → R≥0 and μ : X × Ud → R≥0 satisfying μ(x, u ) ≤ ε(x, u ) for all (x, u ) ∈ X × Ud. The systems S(Σ μ) and 

S(Σ ε) represent perturbed versions of the system Σ. Let Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ) denote the symbolic model of the perturbed system 

Σε, and Cd,ε be a controller for this system. We define a controller C : X ⇒ U for the system S(Σ μ) as C(x) = Cd,ε (Q(x)) for x ∈ X.For any behavior σ = (( x0, u 0), (x1, u 1), . . . ) ∈ H (SC (Σ μ)) we have that σd = (( q0, u 0), (q1, u 1), . . . ) ∈ H (SCd,ε  

> d

(Σ ε)) , with qi = Q(xi)

for all i ∈ N.

We derive the following auxiliary proposition, which enables the refinement of a controller synthesized for the disturbance-free abstraction into a controller for the concrete perturbed system. 

Proposition 1. Consider the discrete-time control system Σ and its perturbed version Σε in (2) , for a perturbation map ε : X × Ud → R≥0

along with their corresponding symbolic models Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) and Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ), constructed in Section II-D. Let Cd and Cd,ε denote controllers synthesized for the symbolic models Sd(Σ) and Sd(Σ ε), respectively, with respect to the same specification 

HSpec ⊆ Xwd ∪ X∗ 

> d

. We define the controller C : X ⇒ Ud by C(x) = Cd(Q(x)) . If the following holds: ∀q ∈ Xd, Cd(q) = Cd,ε (q),

then C is a controller for the perturbed system S(Σ μ) and the specification Q−1(HSpec ), for any map μ : X × Ud → R≥0 satisfying 

μ(x, u ) ≤ ε(x, u ) for all (x, u ) ∈ X × Ud.

B. Robust predecessor operator 

In this section, we characterize a bound on the system’s perturbation map for the predecessor operator, defined in (1), to preserve equality across a predefined list of sets, irrespective of whether the nominal or perturbed system is considered. 

Theorem 2. Consider the discrete-time control system Σ and its perturbed version Σε in (2) , for a perturbation map ε : X × Ud → R≥0

along with their corresponding symbolic models Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) and Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ), constructed in Section II-D. Let L = {Z0, . . . , Z m} ⊆ (Xd × Ud)m+1 a collection of sets. If the map εd : Xd × Ud → R≥0 as constructed in (3) satisfies for all 

(q, v ) ∈ Xd × Ud, εd(q, v ) < γ (q, v, L ), then we have for all j ∈ { 0, 1, 2, . . . , m }, Pre ∆d (Zj ) = Pre ∆d,ε (Zj ).

We now prove that exceeding the bound defined by the map γ violates the equality of the predecessors. 

Theorem 3. In the setting of Theorem 2, let L = {Z0, . . . , Z m} ⊆ (Xd × Ud)m+1 , and assume there exists (x∗, v ∗) ∈ X × Ud with 

q∗ = Q(x∗) such that εd(q∗, v ∗) > γ (q∗, v ∗, L ).Then Pre ∆d,ε (Zj∗ )̸ = Pre ∆d (Zj∗ ) for some j∗ ∈ { 0, . . . , m }.

Remark 1. Theorems 2 and 3 together show that γ(·, ·, L ) is the sharp threshold for preservation of the predecessor operator across L:the equality Pre ∆d (Zj ) = Pre ∆d,ε (Zj ) holds for every Zj ∈ L when ε < γ , and fails on at least one Zj as soon as ε strictly exceeds γ at any state-input pair. Consequently, having a system with perturbations exceeding γ means we can no longer satisfy the refinement procedure in Proposition 1 and thus we cannot guarantee the correctness of the controller for the perturbed concrete system. 

In the next sections, we follow the procedure illustrated in Fig. 1. We start from the disturbance-free system S(Σ) and construct its symbolic model Sd(Σ) . We then synthesize a controller Cd for a given LTL specification. Using the same procedure we theoretically 6

construct a controller Cd,ε for the perturbed symbolic model Sd(Σ ε) for the same LTL specification. The next step consists in quantifying the bound on the perturbation map to preserve the equality of the predecessor operator and thus the equality Cd,ε = Cd. Then using Proposition 1, we show that the controller Cd can be refined to the perturbed concrete system S(Σ ε) under the same condition on the perturbation. In summary, this procedure allows us to determine, for a given controller synthesized for the system to satisfy a specification, the maximal robustness margin the system can tolerate while still satisfying the specification under the same controller. nominal & perturbed controllers coincide nominal perturbed results of this paper 

Fig. 1: Workflow for abstraction based robust control synthesis. We synthesis the controller Cd for the nominal system and show the same controller can be refined for perturbed system under conditions on the perturbation. IV. S AFETY CONTROLLER 

Consider the system S(Σ) = ( X, X 0, U, ∆) in (2), the safety synthesis problem seeks a controller C such that all closed-loop behaviors remain within a prescribed safe set XT ⊆ X. This can be formally defined as follows. 

Definition 2. Consider the system S(Σ) = ( X, X 0, U, ∆) in (2) , a controller C : X ⇒ U and its domain dom( C). The controller C is a safety controller for the safe set XT ⊆ X if for any initial state x0 ∈ dom( C), all maximal trajectories σ = (( x0, u 0), (x1, u 1), (x2, u 2) . . . ) ∈H(SC ) are complete and satisfy, for all k ∈ N, σx(k) = xk ∈ XT . We denote the corresponding safety specification as HSpec =

HXT 

> Saf ety

= {σx ∈ Xw | ∀ k ∈ N, σ x(k) = xk ∈ XT }.

The classical approach to synthesize a safety controller is the safety fixed-point Algorithm 1. This algorithm takes as input a transition system S(Σ) and a safe set XT ⊆ X, and computes the set Safe( XT ) and a sequence L = {Zi ⊆ X × U | i ∈ N} containing the set of states and their admissible inputs computed at each iteration of the algorithm. The set Safe( XT ) ⊆ X × U consists of all state-input pairs (x, u ) such that every possible successor state under input u remains within XT . Formally, (x, u ) ∈ Safe( XT ) if and only if 

∆( x, u ) ⊆ πX (Safe( XT )) ⊆ XT , where πX (Safe( XT )) denotes the projection of Safe( XT ) on the state space X.Note that we have outlined a general structure for this algorithm, regardless whether the system being symbolic or concrete. However, the algorithm operates on the symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D, where the domain Xd × Ud is finite. 

Algorithm 1: Computation of Safe( XT ) 

> 1:

Input: S(Σ) , W = XT × U where XT ⊆ X 

> 2:

Output: Safe( XT ), L = {Zi | i ∈ N} 

> 3:

Z0 := X × U 

> 4:

repeat  

> 5:

Zi+1 := W ∩ Pre ∆(Zi) 

> 6:

until Zi+1 = Zi 

> 7:

Safe( XT ) := Zi

The safety controller synthesis for the symbolic system Sd(Σ) relies on the fixed-point computation of the monotone operator G :2Xd×Ud → 2Xd×Ud , defined as G(Z) = Pre ∆d (Z) ∩ W, where W = XT × Ud, and XT ⊆ Xd is the discrete safe set. The iterates 7

Zi = νiZ. G (Z), collected in the sequence L = {Z0, Z 1, . . . , Z n}, converge in finite time to the maximal fixed-point Safe( XT ) = Zn =

νZ. G (Z), as established in Theorem 1. The safety controller Cd : Xd ⇒ Ud is then constructed as follows: 

Cd(q) = 



{v ∈ Ud | (q, v ) ∈ νZ.G (Z)}

if q ∈ πX (νZ.G (Z)) ,

∅ otherwise .

(6) This construction yields a correct-by-construction controller for the symbolic abstraction of the safety specification HXT 

> Safety

[3]. To account for perturbations, consider a function ε : X × Ud → R≥0 characterizing uncertainty, and let Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ) denote the symbolic abstraction of the perturbed system defined in Section II-D. Applying the same fixed-point procedure to Sd(Σ ε) using the monotone operator 

Gε(Z) = Pre ∆d,ε (Z) ∩ W yields the maximal fixed-point νY.G ε(Y ) and the corresponding sequence Lε = {Y0, Y 1, . . . , Y m}. The safety controller for the perturbed abstraction is then given by 

Cd,ε (q) = 



{v ∈ Ud | (q, v ) ∈ νY.G ε(Y )}

if q ∈ πX (νY.G ε(Y )) ,

∅ otherwise .

(7) In the subsequent result, we derive a sufficient condition on the perturbation map ε under which the two controllers Cd and Cd,ε are guaranteed to be equivalent. 

Proposition 2. Consider the discrete-time control system Σ and its perturbed version Σε defined in (2) , along with their corresponding symbolic models Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) and Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ), constructed in Section II-D. The perturbation map is given by ε : X × Ud → R≥0 and XT ⊆ Xd is the safe set. Let Cd and Cd,ε denote the safety controllers constructed in (6) and (7) 

with the associated sequences L = {Z0, Z 1, . . . , Z n} and Lε = {Y0, Y 1, . . . , Y m}. If the perturbation map ε : X × Ud → R≥0 satisfies 

sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ) for all (x, v ) ∈ X × Ud then the following holds: ∀q ∈ Xd, Cd(q) = Cd,ε (q).

In this theorem, we have demonstrated that the controller of the symbolic model is equal to the controller of the perturbed symbolic model, provided that the perturbation is lower than a threshold map. This non-uniform maximum robustness margin map corresponds to the map γ defined in (5) that can be explicitly computed using the output list containing the sets generated during the safety algorithm’s iterations. In the next theorem which constitutes the main results for this subsection, we show how to refine this controller for the concrete perturbed system. 

Theorem 4. Consider the discrete-time control systems Σ and its perturbed version Σε in (2) , where ε : X × Ud → R≥0 and the symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D. Let Cd denote the safety controller constructed in (6) with the associated sequence L = {Z0, Z 1, . . . , Z n} and XT ⊆ Xd the safe set. We define the controller C : X ⇒ Ud by C(x) = Cd(Q(x)) . For any map 

ε : X × Ud → R≥0 that satisfies sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ) for all (x, u ) ∈ X × Ud, the controller C is a safety controller for the concrete perturbed system S(Σ ε) and the safety specification Q−1(HXT 

> Saf ety

).

In the following, we present a simplified expression of the admissible robustness margin for safety specifications. 

Proposition 3. Consider the discrete-time control system Σ in (2) , its symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D, and the safety controller Cd defined in (6) with associated iterate sequence L = {Z0, Z 1, . . . , Z n}, where Zn = νZ. G (Z). If (x, v ) ∈

Q−1(Zn), then γ(Q(x), v, L ) = η(Q(x), v, dom( Cd)) .

Proposition 3 gives a simple and interpretable form for the safety margin: the intersection over the entire iterate list L in the definition of 

γ collapses to the single fixed-point set Zn, so the margin reduces to η(Q(x), v, dom( Cd)) . The admissible perturbation at a state is thus governed only by the distance from its over-approximated reachable set to the boundary of the safe domain. Specifically, for any state within the safety set (the controller’s domain), any perturbation that keeps the successor states inside this safety set can be tolerated. The reachability, persistence, and recurrence subsections that follow adopt the same organization in this subsection. 

V. R EACHABILITY CONTROLLER 

Let S = ( X, X 0, U, ∆) be the transition system in (2) and let XT ⊆ X be a target set. The reachability synthesis problem consists of finding a controller C such that every behavior of the closed-loop system SC reaches XT in finite time. No requirement is imposed on the system’s behavior thereafter. 

Definition 3. Consider the system S(Σ) in (2) , a controller C : X ⇒ U and its domain dom( C). The controller C is a reachability controller for the target set XT ⊆ X if for any initial state x0 ∈ dom( C), for all maximal trajectories σ = (( x0, u 0), (x1, u 1) . . . ) of H(SC ) there exist k ∈ N such that σx(k) = xk ∈ XT . We denote the corresponding reachability specification as HSpec = HXT 

> Reach

= {σx ∈ X∗ ∪ Xw |∃k ∈ N, σx(k) = xk ∈ XT }.

To synthesize the reachability controller we rely on Algorithm 2. This algorithm computes Reach( XT ), which includes all states, with the corresponding admissible inputs, from which one can reach XT . Furthermore, the algorithm records the sequence of state-input pairs 

L = {Zi ⊆ X × U | i ∈ N} computed at each iteration of the algorithm. Let Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) denote the symbolic abstraction of Σ constructed in Section II-D. The monotone operator associated with the reachability specification is G(Z) = Pre ∆d (Z) ∪ W, with W = XT × Ud. The fixed-point iterations (cf. Section II-E) are given by Zi = μiZ.G (Z), and converge to the least fixed-point Reach( XT ) = μZ.G (Z). Let L = {Z0, Z 1, . . . , Z n} ⊆ Xd × Ud denote the sequence recording these iteration sets, where Zn = μZ.G (Z). Define the index function jL : X → N ∪ {∞} as 

jL(x) = inf {i ∈ N | x ∈ πX (Zi)}, (8) with inf ∅ = ∞. The reachability controller is given by 

Cd(q) = 

(

{v ∈ Ud | (q, v ) ∈ ZjL(q)}, jL(q) < ∞,

∅, otherwise . (9) 8

Algorithm 2: Computation of Reach( XT ) 

> 1:

Input: S, W = XT × U where XT ⊆ X 

> 2:

Output: Reach( XT ), L = {Zi | i ∈ N} 

> 3:

Z0 := ∅ × U 

> 4:

repeat  

> 5:

Zi+1 := W ∪ Pre ∆(Zi) 

> 6:

until Zi+1 = Zi 

> 7:

Reach( XT ) := Zi

For a perturbation map ε : X ×Ud → R≥0, consider the perturbed symbolic model Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ) constructed in Section II-D. Analogously, the reachability controller for Sd(Σ ε) is defined by 

Cd,ε (q) = 

(

{v ∈ Ud | (q, v ) ∈ YjLε (q)}, jLε (q) < ∞,

∅, otherwise . (10) where Lε = {Y0, Y 1, . . . , Y m} is the analogous iterate of Sd(Σ ε). By construction, Cd,ε is a reachability controller for Sd(Σ ε) and specification HXT

> Reach

. Next, we provide a bound on ε ensuring the equivalence between Cd and Cd,ε .

Proposition 4. Consider the discrete-time control system Σ and its perturbed version Σε in (2) , with symbolic models Sd(Σ) and 

Sd(Σ ε) defined in Section II-D. Let Cd and Cd,ε denote the corresponding reachability controllers in (9) and (10) , respectively, with associated sequences L = {Z0, . . . , Z n} and Lε = {Y0, . . . , Y m}. If sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ), ∀(x, v ) ∈ X × Ud, then 

∀q ∈ Xd, Cd(q) = Cd,ε (q).

In the next theorem, we derive a reachability controller for the perturbed continuous system using the synthesized controller for the disturbance-free abstraction. 

Theorem 5. Consider the setting of Proposition 4. We define the controller C : X ⇒ Ud as C(x) = Cd(Q(x)) . For any map ε : X × Ud →

R≥0 satisfying sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ) and for all (x, v ) ∈ X × Ud, the controller C is a reachability controller for the concrete perturbed system S(Σ ε) and the reachability specification Q−1(HXT

> Reach

).

In the following, we present a simplified expression of the admissible robustness margin for reachability specifications. 

Proposition 5. Consider the discrete-time control system Σ in (2) , its symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D, and the reachability controller Cd for the target set XT ⊆ Xd defined in (9) with associated iterate sequence L = {Z0, Z 1, . . . , Z n}, where 

Zn = μZ. G (Z). If (x, v ) ∈ Q−1(Zn), then for q = Q(x), we have γ(q, v, L ) = η( q, v, π X (ZjL(q)−1)) .

Intuitively, for any (q, v ) ∈ Zμj , all of its successors reach the lower layer Zμj−1, bringing the system closer to the target set XT = πX (Z1).As a result, computing the robustness margin based on this lower layer preserves the guaranteed progression toward XT .

VI. P ERSISTENCE CONTROLLER 

In this section, we consider the synthesis problem that consists in determining the robust controller such that all the behaviors of the derived system SC reach the set XT and remain there for all future time and determining the maximum robustness margin for the robust controller. We first provide a formal definition of the persistence specification. 

Definition 4. Consider the system S(Σ) in (2) , a controller C : X ⇒ U and its domain dom( C) as defined in Section II-B. The controller 

C : X ⇒ U is a persistence controller for the target set XT if for any initial state x0 ∈ dom( C), all maximal trajectories σ ∈ H (SC ) are complete and satisfy 

∃k ∈ N, ∀m ≥ k, σx(m) = xm ∈ XT .

We denote the corresponding persistence specification as HSpec = HXT 

> P ers

= {σx ∈ Xw | ∃ k ∈ N, ∀m ≥ k, σx(m) = xm ∈ XT }.

The controller synthesis for the persistence problem is done using the fixed-point Algorithm 3. This algorithm calculates the winning set denoted by Z∞, which characterizes the maximal set of state-input pairs (x, u ) from which the system can be steered into XT in finite time and confined there indefinitely. Additionally, the algorithm records the intermediate sets generated during each iteration leading to the computation of the final winning set. Consider the symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D and let 

Lμ = {Zμ 

> 0

, Z μ 

> 1

, . . . , Z μn }, with Zμn = Z∞, be the outer iterations list produced by Algorithm 3 for Sd(Σ) . These sequences provide valuable insight into the convergence behavior of the algorithm and will be used as theoretical tools in the proofs. The winning set can written using 

μ-calculus as Z∞ = μZ μ.νZ ν . (Pre ∆d (Zν ) ∩ W ) ∪ Pre ∆d (Zμ) , with W = XT × Ud. The iterations used in the controller synthesis correspond to the outer loop iterations, satisfying the formula: Zμi = μiZμ.νZ ν . (Pre ∆ (Zν ) ∩ W ) ∪ Pre ∆ (Zμ) , where Zμ 

> 0

= ∅ × U .The persistence controllers for the symbolic model Sd(Σ) and its perturbed version Sd(Σ ε) with respect to the target set XT are constructed by the same index-based rule as the reachability controllers in (9) and (10), with the reachability iterate sequences L and Lε replaced by the outer persistence iterate sequences Lμ and Lμε produced by Algorithm 3. Specifically, 

Cd(q) = 

(

{v ∈ Ud | (q, v ) ∈ ZμjLμ (q)} if jLμ (q) < ∞,

∅ otherwise, (11) where jLμ (q) = inf {i ∈ N | q ∈ πX (Zμi )}, and Cd,ε (q) is defined analogously to Cd(q), using the list Lμε . By construction, Cd and Cd,ε 

are persistence controllers for Sd(Σ) and Sd(Σ ε), respectively, both enforcing specification HXT 

> Pers

[3], [35]. The following two lemmas establish building blocks for the proof of the subsequent results, where the proofs can be found in the appendix. 9

Algorithm 3: Computation of Z∞ 

> 1:

Input: S(Σ) , W = XT × U where XT ⊆ X 

> 2:

Output: Pers( XT ), L ν = {Zνi | i ∈ N}, L μ = {Zμi | i ∈ N} 

> 3:

Zν 

> 0

= X × U 

> 4:

Zμ 

> 0

:= ∅ × U 

> 5:

repeat  

> 6:

repeat  

> 7:

Zνj+1 :=  Pre  Zνj

 ∩ W  ∪ Pre( Zμi ) 

> 8:

until Zνj = Zνj+1  

> 9:

Zμi+1 := Zνj 

> 10:

until Zμi+1 = Zμi 

> 11:

Z∞ := Zμi

Lemma 2. Consider the discrete-time control systems Σ and its symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D. Let G, G ε : 2 Xd×Ud → 2Xd×Ud be monotone operators. Define the sequences Z0 = Zε 

> 0

= Xd × Ud, ν i+1 Z.G (Zi) = G(νiZ.G (Z)) 

and νi+1 Z.G ε(Z) = Gε(νiZ.G ε(Z)) . as in Section II-E. If Gε(Z) ⊆ G(Z) hold for all Z ⊆ Xd × Ud, then Zεi ⊆ Zi for all i ∈ N, in particular νZ.G ε(Z) ⊆ νZ.G (Z).

Lemma 3. Consider the discrete-time control systems Σ and its symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D. Let Lμ = {Zμ 

> 0

, Z μ 

> 1

, . . . , Z μn }, with Zμn = Z∞, be the outer iterations list produced by Algorithm 3 for Sd(Σ) and XT ⊆ Xd. We have 

Zμi+1 = (Pre ∆d (Zμi+1 ) ∩ W ) ∪ Pre ∆d (Zμi ), where W = XT × U for i ∈ 1, . . . , n − 1.

In the following theorem, we provide an explicit bound on the perturbation ε under which both controllers Cd and Cd,ε are equivalent. 

Proposition 6. Consider the discrete-time control system Σ and its perturbed version Σε in (2) for a perturbation map ε : X × Ud → R≥0,along with their corresponding symbolic models Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) and Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ), constructed in Section II-D. The perturbation map is given by ε : X × Ud → R≥0. Let Cd and Cd,ε denote the corresponding persistence controllers in (11) with associated sequences Lμ = {Zμ 

> 0

, Z μ 

> 1

, . . . , Z μn } and Lμε = {Y μ 

> 0

, Y μ 

> 1

, . . . , Y μm}. If the perturbation map ε : X × Ud → R≥0 satisfies 

sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all (x, v ) ∈ X × Ud then ∀q ∈ Xd, Cd(q) = Cd,ε (q).

The next theorem is the main result of this section. We derive a persistence controller for the perturbed continuous system using the controller of the nominal symbolic model. 

Theorem 6. Consider the setting of Proposition 6.We define the controller C : X ⇒ Ud for the continuous-space system as C(x) = Cd(Q(x)) .For any map ε : X × Ud → R≥0 that satisfies sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all (x, v ) ∈ X × Ud, the controller C is a persistence controller for the concrete perturbed system S(Σ ε) and the persistence specification Q−1(HXT

> P ers

).

In the following, we present a simplified expression of the admissible robustness margin for persistence specifications. 

Proposition 7. Consider the setting of Proposition 6. For (x, v ) ∈ Q−1(Zμn ) with q = Q(x) and j = jLμ (q), we have 

I(q, v, L μ) = 



Zμj , if j > 1 and 

(q, v ) /∈ Pre ∆d (Zμj−1),Zμj−1, if j > 1 and 

(q, v ) ∈ Pre ∆d (Zμj−1),Zμ 

> 1

, if j = 1 .

which simplifies the expression of the robustness margin γ(q, v, L μ) = η(q, v, I (q, v, L μ)) for each case. 

Remark 2. Recall that the sets Zμi are ordered by inclusion. At a given pair (q, v ), three situations may occur. If (q, v ) belongs to 

Pre ∆d (Zμj−1), then all its successors can be steered into a lower layer Zμj−1, meaning that the controller has the option to “descend” closer to the target set XT . Thus, we calculate the robustness margin based on this lower layer. Otherwise, if (q, v ) /∈ Pre ∆d (Zμj−1), such a descent is no longer possible, and the system is constrained to remain within the current layer Zμj , and we have (q, v ) ∈ Pre ∆d (Zμj ) ∩ W ⊆ W

which means the successor states of (q, v ) reach the target XT . Finally, if (q, v ) ∈ Zμ 

> 1

, then Zμ 

> 1

= Zμ 

> 1

∩ W ⊆ W is the maximal fixed-point contained in W . Consequently, any trajectory inside this set satisfies the safety specification with respect to XT , and thus remains in XT

for all future times. 

VII. R ECURRENCE CONTROLLER 

Let S(Σ) = ( X, X 0, U, ∆) be a transition system in (2) and let XT ⊆ X be a target set. We consider the synthesis problem that consists in determining a controller C, such that all the behaviors of the corresponding control system SC (Σ) can reach the set XT infinitely many times. 

Definition 5. Consider the system S(Σ) in (2) , a controller C : X ⇒ U and its domain dom( C) as defined in Section II-B. A controller 

C : X ⇒ U for the system S(Σ) is a recurrence controller for target set XT ⊆ X if for any initial state x0 ∈ dom( C), all maximal trajectories σ ∈ H (SC ) are complete and satisfy: 

∀k ∈ N, ∃m ≥ k, σx(m) = xm ∈ XT .

We denote the corresponding recurrence specification as HSpec = HXT 

> Rec

= {σx ∈ Xw | ∀ k ∈ N, ∃m ≥ k, σx(m) = xm ∈ XT }.10 

The solution to the recurrence problem is obtained via the fixed-point Algorithm 4, which computes the set Z∞ = Rec( XT ). Let 

Lν = {Zν 

> 0

, Z ν 

> 1

, . . . , Z νn } and Lμ,i = {Zμ,i  

> 0

, Z μ,i  

> 1

, . . . , Z μ,i m(i)}, for i = 0 , . . . , n , be the outer and inner iteration lists produced by Algorithm 4 for the symbolic model Sd(Σ) constructed in Section II-D and target set XT ⊆ Xd, where m(i) is the minimal integer satisfying Zμ,i m(i) =

Zμ,i m(i)+1 and Zνn = Z∞. Define the combined list 

Lμ =

> n

[

> i=0

{Zμ,i  

> 0

, Z μ,i  

> 1

, . . . , Z μ,i m(i)}. (12) The final set computed by the algorithm admits the nested fixed-point characterization Z∞ = νZ ν . μZ μ.(Pre ∆d (Zν ) ∩ W ) ∪ Pre ∆d (Zμ),

where W = XT × Ud. Consider the list Lμ,n = {Zμ,n  

> 0

, Z μ,n  

> 1

, . . . , Z μ,n m(n)} the sequence corresponding to the last inner iterations of Algorithm 4. This list will be used for the definition of the recurrence controller, and to simplify the notation, we note it as 

L = {Z0, Z 1, . . . , Z m(n)} (13) .

Algorithm 4: Computation of Rec( XT )

1: Input: S(Σ) , W = XT × U where XT ⊆ X

2: Output: Rec( XT ), L ν = {Zνi | i = 0 , . . . , n }, L μ = {{ Zμ,i j | j = 0 , . . . , m (i)} | i = 0 , . . . , n }

3: Zν 

> 0

← X × U

4: i ← 0

5: repeat 

6: Zμ,i  

> 0

← ∅ × U

7: j ← 0

8: repeat 

9: Zμ,i j+1 ← (Pre ∆ (Zνi ) ∩ W ) ∪ Pre ∆(Zμ,i j )

10: j ← j + 1 

11: until Zμ,i j = Zμ,i j−1

12: Zνi+1 ← Zμ,i j

13: i ← i + 1 

14: until Zνi = Zνi−1

15: Rec( XT ) ← Zνi

The recurrence controller for Sd(Σ) uses the last inner iterate sequence L of Algorithm 4, and is defined by: 

Cd(q) = 

(

{v ∈ Ud | (q, v ) ∈ ZjL(q)} if jL(q) < ∞,

∅ otherwise , (14) where jL(q) = inf {i ∈ N | q ∈ πX (Zi)}. We define Cd,ε (q) analogously to Cd(q), using the list Lε which is the final inner iterate sequence of Algorithm 4 applied to Sd(Σ ε). By construction, Cd and Cd,ε are recurrence controllers for Sd(Σ) and Sd(Σ ε), respectively, both enforcing specification HXT 

> Rec

[3], [35]. We are now ready to derive an explicit bound on the perturbation ε under which the two controllers Cd and 

Cd,ε are equivalent. 

Proposition 8. Consider the discrete-time control systems Σ and Σε in (2) , with symbolic models Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) and Sd(Σ ε) = (Xd, X 0 

> d

, U d, ∆d,ε ) constructed in Section II-D, where ε : X × Ud → R≥0 is the perturbation map. Let Cd and Cd,ε be the recurrence controllers synthesized by Algorithm 4 for Sd(Σ) and Sd(Σ ε), respectively, with respect to the target set XT ⊆ Xd. Denote by Lν =

{Zν 

> 0

, . . . , Z νn } and Lμ,i = {Zμ,i  

> 0

, . . . , Z μ,i m(i)}, i = 0 , . . . , n , the outer and inner iterate lists produced by Algorithm 4 for Sd(Σ) , and let Lμ

and L be as defined in (12) and (13) . The corresponding lists for Sd(Σ ε) are denoted Lνε = {Y ν 

> 0

, . . . , Y νn1 }, Lμ,i ε = {Y μ,i  

> 0

, . . . , Y μ,i mε(i)}, i =0, . . . , n 1, where mε(i) ∈ N is the smallest integer such that Y μ,i mε(i) = Y μ,i mε(i)+1 , and Lμε , Lε are defined analogously. If sup z∈Q(x) ε(z, v ) <γ(Q(x), v, L μ), ∀ (x, v ) ∈ X × Ud, then the following holds: ∀q ∈ Xd, Cd(q) = Cd,ε (q).

Note that for calculating the robustness margin we use the list Lμ whereas for the controller definition we use the list L. The next theorem is the main result of this section, we derive a recurrence controller for the concrete system S(Σ ε).

Theorem 7. Consider the setting of Proposition 8. Let Cd be the recurrence controller in (14) , and define C(x) = Cd(Q(x)) . For any map 

ε : X × Ud → R≥0 that satisfies sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all (x, v ) ∈ X × Ud, the controller C is a recurrence controller for the concrete perturbed system S(Σ ε) and the recurrence specification Q−1(HXT

> Rec

).

VIII. C OMPARISON OF ROBUSTNESS MARGINS 

In this section, we show within the controller’s domain, the robustness margin for the four specifications studied in the previous sections is larger than the inherent robustness margin proposed in [28]. Figure 2 illustrates that the difference between the two robustness measures in the case of safety can be substantial, thus highlighting the added value and effectiveness of the proposed approach. 

Proposition 9. Let Φ ∈ { Safe , Reach , Pers , Rec } be one of the four specifications of Sections IV–VII, with target set XT ⊆ Xd, and let 

Cd be the corresponding controller synthesized for the symbolic model Sd(Σ) . Denote by Z∞ the winning set returned by the associated 11 

fixed-point algorithm, so that πX (Z∞) = dom( Cd), and by LΦ the iterate list of the fixed-point algorithm governing the robustness margin of Cd. Then, for all (x, v ) ∈ Q−1(Z∞),

η Q(x), v, ∆d(Q(x), v ) ≤ γ Q(x), v, L Φ

. (15) q 1u1 u1 

Fig. 2: Illustration of Proposition 9 where we compare two robustness margin for the overapproximation of the reachable set cl( f (q1, u 1, D ), represented by the dark region, for the state q1 under the input u1. We can clearly notice the difference between the abstraction-based margin η(q1, u 1, ∆d(q1, u 1)) (dark orange) as in [28] and the specification-based margin for the safety specification γ(q1, u 1, L ) (light orange) for the safety controller C with its domain of definition dom( C).IX. N UMERICAL EXAMPLE 

In this section, we present numerical examples to illustrate the theoretical part of our work. We consider two examples, a first example of a double integrator with a safety specification and a mobile robot example with an LTL specification. 

A. Double integrator 

We study a double-integrator system governed by 



x1 

> k+1

= x1 

> k

+ τ x 2 

> k

+ τ 2

2 uk + τ 2

2 w1 

> k

,x2 

> k+1

= x2 

> k

+ τ u k + τ w 2 

> k

,

where the state vector x = ( x1, x 2) ∈ X = [0 , 6] × [−6, 6] consists of the position and velocity components. The control input u ∈ [−1, 1] ,while the disturbance vector w = ( w1, w 2) ∈ [−0.01 , 0.01] × [−0.01 , 0.01] represents additive perturbations. The sampling period is 

τ = 0 .5 seconds. We borrowed the same configuration as in [28] to be able to compare and show that our new results allow larger robustness margins. A symbolic abstraction of the system is constructed using the procedure described in Section II-D. To this end, the state space X

is uniformly partitioned into Nx1 = 80 intervals along the first coordinate and Nx2 = 160 intervals along the second coordinate. Similarly, the control input set is discretized into Nu1 = 5 as in [28]. The transition relation of the resulting symbolic model is then obtained via reachability analysis. The exact reachable sets can be computed by evaluating the dynamics at the extreme values of the state and disturbance variables (see, e.g., [8]) because we are dealing with a linear dynamics. Next, the safety fixed-point Algorithm 1 is applied to synthesize the maximal safety controller C for the symbolic model Sd(Σ) with respect to the safe set XT = [0 , 6] × [−6, 6] , which yields the list of sets generated at each iteration L. Figure 3 shows the deterministic controller C(x) = arg max u∈C (x) γ(Q(x), u, L ), which assigns to each state x the control input that maximizes the robustness margin. Based on these controller domain, the admissible robustness margin 

γ : Xd × Ud × 2Xd×Ud → R≥0 is computed according to Proposition 3. In this example, the admissible robustness margin lies in the interval [0 .0, 1.5] . Table II reports the maximum admissible robustness margin over all state and input obtained for various choices of state and input discretization parameters using the notion of robustness margin in [28] and the robustness margin defined in our paper. We have two main observations. First, refining the discretization reduces the free maximum robustness margin in [28], highlighting the inherent trade-off between abstraction accuracy and robustness. In contrast, our robustness margin exhibits the opposite trend. This trade-off is no longer limiting, and finer abstractions yield improved robustness. Second, our robustness margin is 7 times higher than the robustness margin in [28] in the worst case and 48 times higher in the optimal case. The heatmap in the right of Figure 4 illustrates the corresponding maximal admissible robustness margin γ(Q(x), C(x), dom( C)) for each state x ∈ dom( C). As expected, the robustness values increase as we move farther from the border of the safety domain. The heatmap at the left shows the maximal values using the robustness in [28]. These values are significantly smaller (dark red) compared to our robustness margins that can be seen in the dark blue on the heatmap at the right. Finally, the right panel of Figure 4 illustrates closed-loop trajectories starting from the initial condition x0 = (0 .5, 1) . The red curve corresponds to the disturbance-free closed-loop system, while the blue curve represents the trajectory of the system under maximal additive disturbances. In these cases, the perturbed trajectory remains within the safe set, in agreement with Theorem 4. The green curve illustrates the evolution of the disturbed system when the disturbance exceeds the maximal robustness margins that go beyond the controller domain after two time steps illustrating the tightness of our results. 12 

Fig. 3: The input value maximizing the robustness margin for each state. 

Fig. 4: Two heatmaps with the same graded colorbar. The left heatmap shows the maximal free admissible robustness margin (computed as in [28]) for each state. The right heatmap shows the admissible robustness margin for the maximal controller described in Section IX-A along with trajectories under different perturbations. 

B. Mobile robot 

In this example, we study a unicycle-type mobile robot. The system state consists of the planar position  x1, x 2 ∈ [0 , 10] × [0 , 10] and the orientation angle x3 ∈ [−π, π ]. The robot evolves according to 



x1 

> k+1

= x1 

> k

+ τ  u1 

> k

cos( x3 

> k

) + w1

> k

,x2 

> k+1

= x2 

> k

+ τ  u1 

> k

sin( x3 

> k

) + w2

> k

,x3 

> k+1

= x3 

> k

+ τ  u2 

> k

+ w3

> k

 (mod 2 π),

(16) where the control input (u1, u 2) ∈ [0 .25 , 1] × [−1, 1] corresponds to the linear and angular velocities, respectively. The terms wik ∈

[−0.05 , 0.05] , for i = 1 , 2, 3, model bounded disturbances, and the sampling period is set to τ = 1 . To formalize the specification, we first introduce five disjoint regions within the domain X = [0 , 10] × [0 , 10] × [−π, π ]. These regions: center C1, center C2, center C3,Obstacle R1 and obstacle R2, are shown in Figure 5, and their numerical values are given in Table III. To construct the symbolic model, the state space X is uniformly partitioned using a Cartesian grid with Nx1 = 120 , Nx2 = 100 , and Nx3 = 60 subdivisions along the respective dimensions as described in Section II-D. The control inputs are uniformly discretized into Nu1 = 3 and Nu2 = 5 values. The                                                   

> NuSource Nx=1600 Nx=3200 Nx=6400 Nx=12800
> 3Paper [28] 0.118 0.044 0.025 0.031 This paper 0.899 0.975 1.35 1.5
> ×7↑×22 ↑×54 ↑×48 ↑
> 5Paper [28] 0.118 0.056 0.056 0.031 This paper 0.899 0.975 1.35 1.5
> ×7↑×17 ↑×24 ↑×48 ↑
> 10 Paper [28] 0.129 0.055 0.055 0.047 This paper 0.899 0.975 1.35 1.5
> ×6↑×17 ↑×24 ↑×31 ↑

TABLE II: Maximum robustness over all states and inputs, computed using the robustness notion from [28] and that of Proposition 3, for the safety specification under different discretizations Nx = Nx1 × Nx2 (the total number of discrete states) and Nu (the number of symbolic inputs). 13 

transition relation of the symbolic model is computed via reachability analysis. Specifically, the reachable sets are over-approximated using the growth-bounds-based approach in [5]. We consider a combined specification formed through the sequential composition of three basic specifications: two persistence specifications followed by one recurrence specification, with the additional requirement that the system avoids obstacle regions R1 and R2 at all times. We first synthesis a safety specification over X\{ R1, R 2} to remove the non safe states. Beginning from an initial state x0, the system must satisfy persistence within region C2 for 100 time steps. The final state of this phase then serves as the initial condition for the second phase, which requires persistence in region C1 for another 100 steps. The resulting endpoint state initiates the third and final phase, which requires the system to satisfy the recurrence for region C3 over 100 steps. For each specification, we construct the nondeterministic controllers C1, C2, and C3 using Theorems 6 and 7, with their corresponding lists L1, L 2 and L3. Subsequently, we deduce the maximal robustness controllers defined by Ci(x) = arg max u∈C i(x) γ(Q(x), u, L i). The margins are computed leveraging the robustness margin characterizations as given in Propositions 7 for the persistence specification.                                           

> Region Coordinates (x1, x 2, x 3)
> Center C1[0 .3,3] ×[8 ,9.8] ×[−π, π ]
> Center C2[4 ,6.5] ×[0 .2,2] ×[−π, π ]
> Center C3[7 .3,9.9] ×[8 ,9.8] ×[−π, π ]
> Obstacle R1[2 ,3] ×[0 ,7] ×[−π, π ]
> Obstacle R2[6 ,7] ×[3 ,10] ×[−π, π ]

TABLE III: Regions defining the specification. 

Figure 5 shows the combined specification for the initial state x0 = (1 , 2) and displays two distinct closed-loop trajectories starting from the same initial condition. The green trajectory represents the nominal case, where the system evolves without any external disturbance. 

Fig. 5: Two controlled trajectories of the system in (16) starting from x(0) = (1 , 2) over 300 time steps, both satisfying the specification described in mobile robot Section IX-B. Green: evolution without disturbances ( ε = 0 ). Blue: evolution with disturbance at the maximum robustness margin. 

The blue trajectory corresponds to the robust case, where at each step the system is subjected to the maximal admissible disturbance, calculated respectively via Proposition 7 for the persistence phases, implemented as the perturbation γi(Q(x), Ci(x), L i). One can see that the control objective is achieved, which is consistent with Theorem 6 and Theorem 7. Two video simulations of the robot satisfying the specification for the nominal system, implemented using PyBullet [36], are available at the following links: https://youtu.be/h9OsOwvBguk and https://youtu.be/4O9C uTQX7g. The first video shows the nominal case without perturbations, while the second illustrates the scenario with disturbances corresponding to external slippery road condition. In Figure 6, we focus only on the recurrence specification over the center C3. The graded color layers represent the sets L3 =

{Z0, Z 2, . . . , Z 32 }, where each color corresponds to the region Zi+1 \Zi. The darkest violet denotes set Z0, which must be visited infinitely often, while the lightest yellow corresponds to the layer Z32 \Z31 . Here Z32 is the fixed-point of the recurrence algorithm where Zi ∈ Z32 

for i = 0 , 1 . . . 31 . We simulate three closed-loop trajectories from the initial state x0 = (1 .2, 2.2) under the maximal robustness controller 

C3. The first (blue) trajectory corresponds to nominal control without disturbances. The second (red) trajectory shows the system under maximal robustness perturbation given by γ(Q(x), C3(x), L 3) for each state x ∈ X. Both trajectories satisfy the specification: they avoid obstacles R1 and R2, reach the center C3, and revisit it infinitely often (simulated up to 100 steps). Additionally, each state xk ∈ Zi

transitions to the next layer satisfying xk+1 ∈ Zi−1. In contrast, the green trajectory illustrates a case where the applied disturbance exceeds the computed maximal robustness margin. This violation causes the system to fail the specification by entering in obstacle R2. The sharp transition from satisfaction to violation at this threshold demonstrates the tightness of our result. The admissible robustness margin lies in the range [0 .0, 0.7] , with a mean robustness margin in each direction equal to 0.0794 . The computation times are: abstraction construction (2 min 2 s), recurrence fixed-point iteration ( 2 min 4.68 s), controller synthesis ( 1 min 18 .37 s) using a processor 2.20 GHz Intel(R) Xeon(R) Platinum 56 cores CPU. 14 

Fig. 6: Three trajectories of the system in (16) starting from x0 = (1 .2, 2.2) over 100 time steps under different perturbations. X. C ONCLUSION 

We introduce a new concept of specification-based maximal robustness margin for discrete-time dynamical systems. This quantity characterizes the largest admissible disturbance under which the system still satisfies a given temporal specification. The key innovation is the specification-dependent robustness margin that ensures fixed-point computations remain invariant under perturbations. By computing margins that preserve the predecessor operator used in the fixed-point synthesis algorithm, we derive non-uniform, state- and input-dependent robustness bounds tailored to safety, reachability, persistence, and recurrence. Theoretical guarantees show our margins strictly exceed previously known bounds, reducing conservatism. Our method enables the synthesis of controllers that are robust to disturbances below the characterized margin, guaranteeing specification satisfaction for both the nominal and perturbed systems. Simulation results on two numerical examples confirm the practical effectiveness of the approach. Future work will focus on extending these results to stochastic systems with stochastic uncertainties. 

REFERENCES [1] J. Vargas, S. Alsweiss, O. Toker, R. Razdan, and J. Santos, “An overview of autonomous vehicles sensors and their vulnerability to weather conditions,” 

Sensors , vol. 21, no. 16, p. 5397, 2021. [2] H. Zhang, W. Xiang, W. Lin, and J. Wen, “Grid forming converters in renewable energy sources dominated power grid: Control strategy, stability, application, and challenges,” Journal of modern power systems and clean energy , vol. 9, no. 6, pp. 1239–1256, 2021. [3] P. Tabuada, Verification and control of hybrid systems: a symbolic approach . Springer Science and Business Media, 2009. [4] A. Saoud, A. Girard, and L. Fribourg, “Contract-based design of symbolic controllers for safety in distributed multiperiodic sampled-data systems,” 

IEEE Transactions on Automatic Control , vol. 66, no. 3, pp. 1055–1070, 2020. [5] G. Reissig, A. Weber, and M. Rungger, “Feedback refinement relations for the synthesis of symbolic controllers,” IEEE Transactions on Automatic Control , vol. 62, no. 4, pp. 1781–1796, 2017. [6] K. Hsu, R. Majumdar, K. Mallik, and A.-K. Schmuck, “Multi-layered abstraction-based controller synthesis for continuous-time systems,” in Proceedings of the 21st International Conference on Hybrid Systems: Computation and Control , 2018, pp. 120–129. [7] A. Saoud, “Compositional and efficient controller synthesis for cyber-physical systems,” Ph.D. dissertation, Universit´ e Paris Saclay (COmUE), 2019. [8] M. Althoff, G. Frehse, and A. Girard, “Set propagation techniques for reachability analysis,” Annual Review of Control, Robotics, and Autonomous Systems , 2021. [9] S. Coogan and M. Arcak, “Finite abstraction of mixed monotone systems with discrete and continuous inputs,” Nonlinear Analysis: Hybrid Systems ,vol. 23, pp. 254–271, 2017. [10] M. Zamani, P. M. Esfahani, A. Abate, and J. Lygeros, “Symbolic models for stochastic control systems without stability assumptions,” in 2013 European Control Conference (ECC) . IEEE, 2013, pp. 4257–4262. [11] A. Girard, G. Pola, and P. Tabuada, “Approximately bisimilar symbolic models for incrementally stable switched systems,” IEEE Transactions on Automatic Control , vol. 55, no. 1, pp. 116–126, 2009. [12] G. Reissig, “Computing abstractions of nonlinear systems,” IEEE Transactions on Automatic Control , vol. 56, no. 11, p. 2583–2598, 2011. [13] C. Baier and J.-P. Katoen, Principles of model checking . MIT press, 2008. [14] J. Calbert, A. Girard, and R. M. Jungers, “Classification of simulation relations for symbolic control,” 2024. [Online]. Available: https://arxiv.org/abs/2410.06083 [15] R. Alur, T. A. Henzinger, O. Kupferman, and M. Y. Vardi, “Alternating refinement relations,” in CONCUR ’98: Concurrency Theory, 9th International Conference, Nice, France, September 8-11, 1998, Proceedings , ser. Lecture Notes in Computer Science, vol. 1466. Springer, 1998, pp. 163–178. [16] J. Liu and N. Ozay, “Finite abstractions with robustness margins for temporal logic-based control synthesis,” Nonlinear Analysis: Hybrid Systems , 2016. [17] R. Ehlers and U. Topcu, “Resilience to intermittent assumption violations in reactive synthesis,” in Proceedings of the 17th international conference on Hybrid systems: computation and control , 2014, pp. 203–212. [18] P. Tabuada, S. Y. Caliskan, M. Rungger, and R. Majumdar, “Towards robustness for cyber-physical systems,” IEEE Transactions on Automatic Control ,vol. 59, no. 12, pp. 3151–3163, 2014. [19] M. Rungger and P. Tabuada, “A notion of robustness for cyber-physical systems,” IEEE Transactions on Automatic Control , vol. 61, no. 8, pp. 2108–2123, 2015. [20] W. A. Apaza-Perez and A. Girard, “Synthesis of input-to-state attractivity controllers for transition systems with disturbances,” IEEE Transactions on Automatic Control , 2024. [21] A. Saoud, P. Jagtap, and S. Soudjani, “Temporal logic resilience for dynamical systems,” IEEE Transactions on Automatic Control , 2025. [22] Y. A. Si, R. Das, N. Monir, S. Soudjani, P. Jagtap, and A. Saoud, “Maximally resilient controllers under temporal logic specifications,” in 2025 IEEE 64th Conference on Decision and Control (CDC) , 2025. 15 

[23] N. Monir, Y. A. Si, R. Das, P. Jagtap, A. Saoud, and S. Soudjani, “Computation of feasible assume-guarantee contracts: A resilience-based approach,” in 2025 IEEE 64th Conference on Decision and Control (CDC) , 2025. [24] S. Zhu, L. M. Tabajara, J. Li, G. Pu, and M. Y. Vardi, “Symbolic ltlf synthesis,” in Proceedings of the Twenty-Sixth International Joint Conference on Artificial Intelligence, IJCAI-17 , 2017, pp. 1362–1369. [25] A. Borri, G. Pola, and M. D. Di Benedetto, “Symbolic models for nonlinear control systems affected by disturbances,” International Journal of Control ,vol. 85, no. 10, pp. 1422–1432, 2012. [26] Y. Bai, K. Mallik, A.-K. Schmuck, D. Zufferey, and R. Majumdar, “Incremental abstraction computation for symbolic controller synthesis in a changing environment,” in 2019 IEEE 58th Conference on Decision and Control (CDC) . IEEE, 2019, pp. 6261–6268. [27] Y. Bai and K. Mallik, “Accurate abstractions for controller synthesis with non-uniform disturbances,” in Formal Methods and Software Engineering .Springer International Publishing, 2020, pp. 297–307. [28] Y. Aitsi, A. Girard, and A. Saoud, “Symbolic control: Unveiling free robustness margins,” in 23rd European Control Conference (ECC25) , 2025. [29] V. I. Istratescu, Fixed point theory: an introduction . Springer Science & Business Media, 2001, vol. 7. [30] E. Gr¨ adel, W. Thomas, and T. Wilke, Automata, logics, and infinite games: a guide to current research . Springer, 2003. [31] J. Cai and R. Paige, “Program derivation by fixed point computation,” Science of Computer Programming , vol. 11, no. 3, pp. 197–261, 1989. [32] B. A. Davey and H. A. Priestley, Introduction to Lattices and Order , 2nd ed. Cambridge University Press, 2002. [33] A. Girard and G. J. Pappas, “Approximation metrics for discrete and continuous systems,” IEEE Transactions on Automatic Control , vol. 52, no. 5, pp. 782–798, 2007. [34] M. Zamani, P. M. Esfahani, R. Majumdar, A. Abate, and J. Lygeros, “Symbolic control of stochastic systems via approximately bisimilar finite abstractions,” IEEE Transactions on Automatic Control , vol. 59, no. 12, pp. 3135–3150, 2014. [35] O. Maler, A. Pnueli, and J. Sifakis, “On the synthesis of discrete controllers for timed systems,” in Annual symposium on theoretical aspects of computer science . Springer, 1995, pp. 229–242. [36] E. Coumans and Y. Bai, “Pybullet, a python module for physics simulation in robotics, games and machine learning,” 2016–2021. [Online]. Available: https://pybullet.org 

APPENDIX 

We have the following auxiliary result for which the proof is straightforward, and thus omitted. 

Lemma 4. Consider the discrete-time control system Σ in (2) and its associated symbolic model Sd(Σ) = ( Xd, X 0 

> d

, U d, ∆d) constructed in Section II-D. Let (q, v ) ∈ Xd × Ud and A ⊆ B ⊆ Xd. We have that η(q, v, A ) ≤ η(q, v, B ).

A. Proof of Lemma 1 

Proof. To show the result, we show that for all i ∈ N we have ui ∈ C d,ε (qi) and qi+1 ∈ ∆d,ε (qi, u i). Let i ∈ N, first we have ui ∈ C (xi) = 

Cd,ε (Q(xi)) = Cd,ε (qi) therefore the first condition is satisfied. For the second condition, we need to show that qi+1 ∈ ∆d,ε (qi, u i) which is equivalent to proving that {cl( f (qi, u i, D )) + Bεd(qi,u i)(0) } ∩ cl( qi+1 )̸ = ∅. Using the fact that μ(xi, u i) ≤ ε(xi, u i) ≤ sup x∈qi ε(x, u i) = 

εd(qi, u i) where εd defined in (3), one gets cl( f (xi, u i, D )) + Bμ(xi,u i)(0) ⊆ cl( f (qi, u i, D )) + Bεd(qi,u i)(0) . By definition, we have 

xi+1 ∈ f (xi, u i, D ) + Bμ(xi,u i)(0) , which implies xi+1 ∈ cl( f (qi, u i, D )) + Bεd(qi,u i)(0) . Moreover, since xi+1 ∈ Q(xi+1 ) = qi+1 , it implies cl( qi+1 ) ∩ { cl( f (qi, u i, D )) + Bεd(qi,u i)(0) }̸ = ∅. Therefore, qi+1 ∈ ∆d,ε (qi, u i), which concludes the proof. 

B. Proof of Proposition 1 

Proof. Let σ = (( x0, u 0), (x1, u 1), . . . ) ∈ H (SC (Σ μ)) where x0 ∈ dom( C). Since C(x) = Cd(Q(x)) = Cd,ε (Q(x)) for all x ∈ X, one gets from Lemma 1 the existence of a behavior σd = (( q0, u 0), (q1, u 1), . . . ) ∈ H (SCd,ε  

> d

(Σ ε)) with qi = Q(xi) for all i ∈ N. Moreover, we know that x0 ∈ dom( C), thus q0 = Q(x0) ∈ dom( Cd,ε ). Therefore, from the fact that Cd,ε is a controller for the system Sd(Σ ε) and specification HSpec it follows σd,x = ( q0, q 1, q 2, . . . ) ∈ H Spec . Hence, we deduce that σx = ( x0, x 1, x 2, . . . ) ∈ Q−1(HSpec ). Hence, C is a controller for the perturbed system S(Σ μ) and the specification Q−1(HSpec ).

C. Proof of Theorem 2 

Proof. Consider j ∈ { 0, 1, 2, . . . , m }, we first prove the first inclusion Pre ∆d (Zj ) ⊆ Pre ∆d,ε (Zj ). To simplify the notation, we denote 

Xj = πX (Zj ) ⊆ Xd. Consider (q, v ) ∈ Pre ∆d (Zj ), which implies that 

∆d(q, v ) ⊆ Xj . (17) By definition of I in (5), the inclusion (17) gives (q, v ) ∈ Pre ∆d (Zj ) with Zj ∈ L, hence 

> m

\

> i=0

{πX (Zi) | Zi ∈ L, (q, v ) ∈ P re ∆d (Zi)} ⊆ Xj . (18) Using the definition of the perturbed symbolic model, we have εd(q, v ) = sup x∈q ε(x, v ) < γ (Q(x), v, L ) = γ(q, v, L ), and according to the definition of the maps η and γ in (4) and (5), respectively, we deduce that εd(q, v ) < γ (q, v, L ) = η(q, v, I (q, v, L )) ≤ η(q, v, X j ).Where the last inequality comes from (18) and Lemma 4. Moreover, we have cl( f (q, v, D )) ⊆ Int( Q−1(Xj )) due to Equation (17). Hence, one gets from the definition of the map η in (4) and from εd(q, v ) < η (q, v, X j ) that 

cl( f (q, v, D )) + Bεd(q,v )(0) ⊆ Int( Q−1(Xj )) . (19) Now consider q′ ∈ Xd such that cl( q′) ∩ { cl( f (q, v, D )) + Bεd(q,v )(0) }̸ = ∅. We have from (19) that {cl( f (q, v, D )) + Bεd(q,v )(0) } ⊆ 

Int( Q−1(Xj )) = Int( S 

> q1∈Xj)

{q1}). Hence, it follows that cl( q′) ∩ Int( S

> q1∈Xj

{q1})̸ = ∅, which in turn implies that q′ ∈ Xj . Therefore, 

(q, v ) ∈ Pre ∆d,ε (Zj ). Now, we prove that Pre ∆d,ε (Zj ) ⊆ Pre ∆d (Zj ). For any (q, v ) ∈ Pre ∆d,ε (Zj ), we have ∆d,ε (q, v ) ⊆ πX (Zj ), and since ∆d(q, v ) ⊆ ∆d,ε (q, v ) the result follows directly. 16 

D. Proof of Theorem 3 

Proof. Set A∗ = I(q∗, v ∗, L ), so that γ(q∗, v ∗, L ) = η(q∗, v ∗, A ∗). By the definition of η, εd(q∗, v ∗) > γ (q∗, v ∗, L ) implies cl( f (q∗, v ∗, D ))+ 

Bεd(q∗,v ∗)(0) ̸ ⊆ Int( Q−1(A∗)) . Pick y in the left-hand set but not in Int( Q−1(A∗)) . Since {q : q ∈ Xd} partitions Rn, there exists q′ ∈ Xd

with y ∈ cl( q′), and q′ can be chosen outside A∗. The relation y ∈ cl( f (q∗, v ∗, D )) + Bεd(q∗,v ∗)(0) together with y ∈ cl( q′) gives, by definition of ∆d,ε , q′ ∈ ∆d,ε (q∗, v ∗). Since q′ /∈ A∗ = mT

> i=0

{πX (Zi) | Zi ∈ L, (q∗, v ∗) ∈ P re ∆d (Zi)}, there exists j∗ ∈ { 0, . . . , m } with 

(q∗, v ∗) ∈ Pre ∆d (Zj∗ ) and q′ /∈ πX (Zj∗ ). Hence ∆d,ε (q∗, v ∗)̸ ⊆ πX (Zj∗ ), so (q∗, v ∗) /∈ Pre ∆d,ε (Zj∗ ), which conclude the results. 

E. Proof of Proposition 2 

Proof. To show the result, we first show by recurrence that Zi = Yi for all i ∈ N. We have that Z0 = Y0 = Xd × Ud. Now consider i ∈ N,assume Zi = Yi. It follows that Zi+1 = Yi+1 = Pre ∆d (Zi) ∩ W = Pre ∆d,ε (Yi) ∩ W , using the fact that Pre ∆d (Zi) = Pre ∆d,ε (Yi)

as per Theorem 2. By induction, we conclude that Zi = Yi for all i ∈ N. Thus, m = n and consequently πX (Zn) = πX (Ym) = dom( Cd) = dom( Cd,ε ). Moreover, for every q ∈ dom( Cd), Cd(q) = Cd,ε (q) = {v ∈ U | (q, v ) ∈ Zn}, otherwise q / ∈ πX (Zn), implies 

Cd(q) = Cd,ε (q) = ∅.

F. Proof of theorem 4 

Proof. Consider a map ε : X × Ud → R≥0 satisfying sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ) and the symbolic model of the perturbed system 

Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ). From Proposition 2 where Cd,ε is a safety controller for Sd(Σ ε) and safe set XT , we have Cd(q) = Cd,ε (q).Hence, it follows from Proposition 1 that the controller C is a controller for the perturbed system S(Σ ε) and the safety specification 

Q−1(HXT 

> Saf ety

).

G. Proof of Proposition 3 

Proof. From the definition of the monotone function G of the safety specification, we have Zn ⊆ Zn−1 · · · ⊆ Z0. Moreover, since the 

Pre ∆d operator is monotone, we have Pre ∆d (Zn) ⊆ Pre ∆d (Zn−1) · · · ⊆ Pre ∆d (Z0). Using the fact that Zn = Pre ∆d (Zn) ∩ W one gets Zn ⊆ Pre ∆d (Zn). Therefore, for (q, v ) ∈ Zn, we have nT

> i=1

{Zi ∈ L | (q, v ) ∈ Pre ∆d (Zi)} = Zn. Hence, γ(Q(x), v, L ) = 

η(Q(x), v, π X (Zn)) = η(Q(x), v, dom( Cd)) .

H. Proof of Proposition 4 

Proof. We show by induction that Zi = Yi for all i ∈ N. The base case holds since Z0 = Y0 = ∅ × Ud. Assume Zi = Yi. Then 

Zi+1 = Pre ∆d (Zi) ∪ W and Yi+1 = Pre ∆d,ε (Yi) ∪ W. By Theorem 2, Pre ∆d (Zi) = Pre ∆d,ε (Yi), hence Zi+1 = Yi+1 . Thus Zi = Yi for all i, implying n = m and L = Lε. Consequently, jL(q) = jLε (q) for all q ∈ Xd, which yields Cd(q) = Cd,ε (q).

I. Proof of Proposition 5 

Proof. From the definition of the monotone function G of the reachability , we have Z0 ⊆ Z1 · · · ⊆ Zn. Moreover, since the Pre ∆d is monotone, we have 

Pre ∆d (Z0) ⊆ Pre ∆d (Z1) · · · ⊆ Pre ∆d (Zn). (20) Now consider (x, v ) ∈ Q−1(Zn) which implies (q, v ) = ( Q(x), v ) ∈ Zn. We have that (q, v ) ∈ ZjL(q) = Pre ∆d (ZjL(q)−1)

∪ W with W = XT × Ud and jL(q) ∈ { 2, . . . , n } as the objective is to reach Z1 = W . Thus, (q, v ) ∈ Pre ∆d (ZjL(q)−1). By the minimality definition of jL(q) in (8), for all i < j L(q), (q, v )̸ ∈ Zi = Pre ∆d (Zi−1) ∪ W , which implies (q, v )̸ ∈ Pre ∆d (Zi−1). Hence, it follows from (20) for (q, v ) ∈ Zn that nT

> i=0

{Zi ∈ L | (q, v ) ∈ Pre ∆d (Zi)} = ZjL(q)−1. Hence, one gets that γ(q, v, L ) = η(q, v, π X (ZjL(q)−1)) .

That concludes the result. 

J. Proof of Theorem 5 

Proof. Consider a map ε : X × Ud → R≥0 satisfying sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L ) and the symbolic model of the perturbed system 

Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε d ). Let Cd,ε the reachability controller for Sd(Σ ε) and the target set XT , from Proposition 4 we have Cd = Cd,ε .Hence, it follows from Proposition 1 that the controller C is a reachability controller for the perturbed system S(Σ ε) and the reachability specification Q−1(HXT

> Reach

).

K. Proof of Proposition 6 

Proof. Let us prove by induction that Zμi = Y μi for all i ∈ N. Base case: by definition, Zμ 

> 0

= Y μ 

> 0

= ∅ × Ud. Suppose Zμi = Y μi for some i ≥ 1. From Lemma 3 we have Zμi+1 = (Pre ∆d (Zμi+1 ) ∩ W ) ∪ Pre ∆d (Zμi ) and Y μi+1 = (Pre ∆d,ε (Y μi+1 ) ∩ W ) ∪ Pre ∆d,ε (Zμi ).Since Zμj ∈ Lμ for all j ∈ N and sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all (x, v ), Theorem 2 implies: Pre ∆d (Zμi ) = Pre ∆d,ε (Zμi ) and 

Pre ∆d (Zμi+1 ) = Pre ∆d,ε (Zμi+1 ). Thus 

Zμi+1 =  Pre ∆d,ε (Zμi+1 ) ∩ W  ∪ Pre ∆d,ε (Zμi ) (21) Both Zμi+1 and Y μi+1 satisfy the fixed-point equation S = (Pre ∆d,ε (S) ∩ W ) ∪ Ai, where Ai := Pre ∆d,ε (Zμi ). Define 

Φε(S) :=  Pre ∆d,ε (S) ∩ W  ∪ Ai,

Φ( S) :=  Pre ∆d (S) ∩ W  ∪ Ai.17 

Since Pre ∆d,ε (S) ⊆ Pre ∆d (S) for all S ⊆ Xd, it follows that Φε(S) ⊆ Φ( S) for all S ⊆ Xd, so Lemma 2 gives νS. Φε(S) ⊆ νS. Φ( S),i.e. Y μi+1 ⊆ Zμi+1 . For reverse inclusion, note that Zμi+1 = νS. Φ( S) is a fixed-point of Φε (by (21)), so the maximality of Y μi+1 = νS. Φε(S)

yields Zμi+1 ⊆ Y μi+1 . Therefore Zμi+1 = Y μi+1 . By induction, Zμi = Y μi for all i ≥ 0, and thus n = m. Finally, since Zμi = Y μi for all 

i ∈ { 0, 1, . . . , n }, we conclude that Lμ = Lμε . Consequently, for any q ∈ Xd, the index function jLμ (q) satisfies jLμ (q) = jLμε (q).Therefore, for any q ∈ Xd with jLμ (q) < ∞, we have: Cd(q) = Cd,ε (q) = {v ∈ Ud | (q, v ) ∈ ZμjLμ (q) = Y μjLμε

(q)}. This completes the proof. 

L. Proof of Theorem 6 

Proof. Consider a map ε : X × Ud → R≥0 satisfying sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all (x, u ) ∈ X × Ud and the symbolic model of the perturbed system Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ). Let Cd,ε the persistence controller for Sd(Σ ε) for the target set XT . From Proposition 6 we have Cd = Cd,ε . Hence, it follows from Proposition 1 that the controller C is a persistence controller for the perturbed system S(Σ ε) and the persistence specification Q−1(HXT

> P ers

).

M. Proof of Proposition 7 

Proof. From the definition of the non-shrinking iterations of the persistence specification, we have Zμ 

> 0

⊆ Zμ 

> 1

· · · ⊆ Zμn . Moreover, since the 

Pre ∆d operator is monotone, we have Pre ∆d (Zμ 

> 0

) ⊆ Pre ∆d (Zμ 

> 1

) · · · ⊆ Pre ∆d (Zμn ). Let (x, v ) ∈ Q−1(ZμjLμ (q)), q = Q(x) and j = jLμ (q).We consider three mutually exclusive cases. Case 1: j = 1 . Then (q, v ) ∈ Zμ 

> 1

= Pre ∆d (Zμ 

> 1

) ∩ W . By definition, ∆d(q, v ) ⊆ Zμ

> 1

which is the smallest set in the list Lμ, hence I(q, v, L μ) = Zμ 

> 1

. Case 2: j > 1 and (q, v ) /∈ Pre ∆d (Zμj−1). Since (q, v ) ∈ Zμj = Pre ∆d (Zμj )∩W ∪Pre ∆d (Zμj−1), the assumption implies (q, v ) ∈ Pre ∆d (Zμj )∩W . From (q, v ) ∈ Pre ∆d (Zμj ) and (q, v ) /∈ Pre ∆d (Zμj−1),we conclude I(q, v, L μ) = Zμj . Case 3: j > 1 and (q, v ) ∈ Pre ∆d (Zμj−1). By minimality of j = jLμ (q), we have (q, v ) /∈ Zμj−1. Since 

Zμj−1 =  Pre ∆d (Zμj−1) ∩ W  ∪ Pre ∆d (Zμj−2), it must hold that (q, v ) /∈ Pre ∆d (Zμj−2). From (q, v ) ∈ Pre ∆d (Zμj−1) and (q, v ) /∈

Pre ∆d (Zμj−2), we conclude I(q, v, L μ) = Zμj−1. All possible cases are covered, which completes the proof. 

N. Proof of Lemma 2 

Proof. We proceed by induction on i. For i = 0 , by definition, Zε 

> 0

= Xd × Ud = Z0, so Zε 

> 0

⊆ Z0 holds trivially. Assume νiZ. G ε(Z) ⊆

νiZ. G (Z) for some i ≥ 0, we have νi+1 Z. G ε(Z) = Gε

 νiZ. G ε(Z) ⊆ Gε

 νiZ. G (Z) ⊆ G νiZ. G (Z) = νi+1 Z. G (Z), where the first inclusion comes from the induction hypothesis and the second inclusion from hypothesis Gε ⊆ G point-wise. This completes the induction. We can deduce that νZ.G ε(Z) ⊆ νZ.G (Z) by taking a large number of iterations in which both fixed-points are reached. 

O. Proof of Lemma 3 

Proof. At the i-th iteration of the outer loop in Algorithm 3, we have from line 9 that (Zμi+1 = Zνj ), where j denotes the final iteration index of the inner fixed-point computation. Combining this with line 8 yields Zμi+1 = Zνj = Zνj+1 . Hence, replacing both Zνj and Zνj+1 by 

Zμi+1 in line 7 provides the required results. 

P. Proof of Proposition 8 

Proof. The proof proceeds by induction on the iteration steps of Algorithm 4. We have Zν 

> 0

= X × U for both the nominal and perturbed systems, so Zν 

> 0

= Y ν 

> 0

. Assume that for some i ≥ 0, we have Zνi = Y νi . Consider the inner loop for outer index i in Algorithm 4. We prove by induction on j that for all j, Zμ,i j = Y μ,i j . Base case ( j = 0 ): Zμ,i  

> 0

= ∅× U = Y μ,i  

> 0

. Induction step: Assume Zμ,i j = Y μ,i j . From Algorithm 4, the update is: Zμ,i j+1 =  Pre ∆d (Zνi ) ∩ W  ∪ Pre ∆d (Zμ,i j ). Since Zνi ∈ Lμ and Zμ,i j ∈ Lμ, and sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) for all 

(x, v ), Theorem 2 implies: Pre ∆d (Zνi ) = Pre ∆d,ε (Y νi ) and Pre ∆d (Zμ,i j ) = Pre ∆d,ε (Y μ,i j ). Therefore, Zμ,i j+1 =  Pre ∆d,ε (Y νi ) ∩ W  ∪

Pre ∆d,ε (Y μ,i j ) = Y μ,i j+1 . Thus, by induction on j, we have Zμ,i j = Y μ,i j for all j, including the fixed-point j = m(i). The outer update sets Zνi+1 = Zμ,i m(i) and Y νi+1 = Y μ,i m(i). Since Zμ,i m(i) = Y μ,i m(i), we conclude Zνi+1 = Y νi+1 . By induction on i, we have Zνi = Y νi for all i,including the final fixed-point i = n. Therefore, Zνn = Y νn and n1 = n, which implies: dom( Cd) = πX (Zνn ) = πX (Y νn ) = dom( Cd,ε ).

Now consider q ∈ Xd, since Lμ = Lμε , it follows that L = Lε, which implies that jL(q) = jLε (q). Therefore, for any q ∈ Xd with 

jL(q) < ∞, we have Cd(q) = Cd,ε (q) = {v ∈ U | (q, v ) ∈ ZjL (q) = YjLε (q)}.

Q. Proof of Theorem 7 

Proof. Consider a map ε : X × Ud → R≥0 satisfying sup z∈Q(x) ε(z, v ) < γ (Q(x), v, L μ) and the symbolic model of the perturbed system 

Sd(Σ ε) = ( Xd, X 0 

> d

, U d, ∆d,ε ). From Proposition 8 where Cd,ε is a recurrence controller for Sd(Σ ε) and the same target set XT , we have 

Cd = Cd,ε . Hence, it follows from Proposition 1 that the controller C is a recurrence controller for the perturbed system S(Σ ε) and the recurrence specification Q−1(HXT

> Rec

).

R. Proof of Proposition 9 

Proof. Let (x, v ) ∈ Q−1(Z∞) and set q = Q(x). By the definition of γ in (5), we have γ(q, v, L Φ) = η(q, v, I (q, v, L Φ)) , so by the monotonicity of η (Lemma 4) it suffices to establish ∆d(q, v ) ⊆ I(q, v, L Φ). We first verify that the family defining I(q, v, L Φ)

is non-empty by showing (q, v ) ∈ Pre ∆d (Z∞) equivalent to ∆d(q, v ) ⊆ πX (Z∞), for each specifications. Indeed, for safety, Z∞ =Pre ∆d (Z∞) ∩ W ⊆ Pre ∆d (Z∞) gives ∆d(q, v ) ⊆ πX (Z∞); for persistence, Z∞ =  Pre ∆d (Z∞) ∩ W  ∪ Pre ∆d (Z∞) = Pre ∆d (Z∞)

gives ∆d(q, v ) ⊆ πX (Z∞); for recurrence, Z∞ =  Pre ∆d (Z∞) ∩ W  ∪ Pre ∆d (Z∞) = Pre ∆d (Z∞) gives ∆d(q, v ) ⊆ πX (Z∞); and for reachability, (q, v ) ∈ Z∞ = Pre ∆d (Z∞) ∪ W and (q, v ) /∈ W gives ∆d(q, v ) ⊆ πX (Z∞), since (q, v ) ∈ W = XT × Ud means we already reach the target and no succession required. All the sets πX (Zi) considered for the intersection I(q, v, L Φ) contain ∆d(q, v ) which implies 

∆d(q, v ) ⊆ I(q, v, L Φ).