---
title: "Scaling Nonlinear Optimization: Many Problems One GPU"
title_zh: 扩展非线性优化：单GPU处理多问题
authors: "John Viljoen, Johanna Haffner, Masayoshi Tomizuka, Negar Mehr"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26341v1"
tags: ["query:control"]
score: 9.0
evidence: 在GPU上扩展机器人控制和运动规划的非线性优化
tldr: 现有NLP求解器如IPOPT受限于CPU，一次只能求解单个问题，无法融入GPU批处理学习流水线。本文提出jaxipm，首个基于JAX的GPU批处理NLP求解器，通过异构迭代融合消除控制流、迭代级批处理减少GPU空闲时间。在四旋翼非线性MPC基准上，吞吐量较IPOPT提升最高32.85倍，为大规模机器人优化任务提供高效GPU加速方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26341v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 904, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 819, \"height\": 749, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 602, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 890, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26341v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 875, \"height\": 544, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26341v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 921, \"height\": 1064, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 823, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 766, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26341v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 753, \"height\": 310, \"label\": \"Table\"}]"
motivation: 机器人学中梯度优化方法依赖CPU求解器，一次解一个问题，无法利用GPU批处理加速，限制其在现代学习框架中的集成。
method: 基于IPOPT重构算法，利用JAX实现异构迭代融合消除控制流，并采用迭代级批处理最大化GPU利用率，实现多问题并发求解。
result: 在四旋翼MPC基准上，吞吐量比IPOPT最高提升32.85倍，支持多机避障、障碍跟踪等任务。
conclusion: jaxipm是首个GPU批处理NLP求解器，显著提升求解吞吐量，促进约束优化与GPU批处理管线的融合。
---

## 摘要
许多机器人学问题，包括轨迹优化、逆运动学和接触丰富的运动规划，都可以归结为非线性规划问题。成熟的NLP求解器如IPOPT可以解决这些问题，提供硬约束满足、最优性保证以及随问题维度有利的扩展性。这些求解器支撑着机器人学中的基于梯度的方法，但仍然是CPU绑定的，并且一次只解决一个问题，阻碍了它们集成到GPU批处理学习流程中。另一方面，基于采样的方法，如强化学习、模型预测路径积分和模仿学习，由于能够利用GPU批处理模拟器，已成为现代机器人学研究的核心。这些模拟器每秒可以生成的动力学展开数量比以前多几个数量级。如果存在GPU批处理NLP求解器，它将解锁每秒生成的带约束的局部最优解数量的类似加速。这种同时解决多个问题与一次解决一个问题的范式，是将NLP求解器集成到现代GPU批处理机器人学框架的关键要求。为此，我们介绍了\texttt{jaxipm}，这是第一个基于IPOPT并在JAX中实现的GPU批处理NLP求解器。我们通过重新设计IPOPT算法，采用异质迭代融合消除控制流，并通过迭代级批处理最小化GPU空闲时间来实现这一目标。我们在各种四旋翼非线性模型预测控制基准上评估了\texttt{jaxipm}，包括存在障碍物时的参考跟踪、多四旋翼无碰撞导航以及杂乱环境中的导航。我们展示了相对于IPOPT高达32.85倍的吞吐量提升。我们的完整开源代码库可在https://github.com/johnviljoen/jaxipm获取。

## Abstract
Many robotics problems, including trajectory optimization, inverse kinematics, and contact-rich motion planning, reduce to nonlinear programs (NLPs). Mature NLP solvers such as IPOPT can solve these problems, offering hard constraint satisfaction, optimality guarantees, and favorable scaling with problem dimension. These solvers underpin gradient-based methods in robotics, yet remain CPU-bound and solve only one problem at a time, preventing their integration into GPU-batched learning pipelines. On the other hand, sampling-based approaches such as reinforcement learning, model predictive path integral, and imitation learning have become the core of modern robotics research due to their ability to leverage GPU-batched simulators. These simulators can generate orders of magnitude more dynamics rollouts per second than was previously possible. If a GPU-batched NLP solver existed, it would unlock similar speedups in the number of constrained, locally optimal solutions generated per second. This regime of solving many problems concurrently versus solving a single problem at a time is a key requirement for integrating NLP solvers in modern GPU-batched robotics frameworks. To this end, we introduce \texttt{jaxipm}, the first GPU-batched NLP solver, based on IPOPT, and implemented in JAX. We accomplish this by redesigning IPOPT's algorithm to eliminate control flow with \textit{heterogeneous iteration fusion}, and by minimizing GPU idle time with \textit{iteration level batching}. We evaluate \texttt{jaxipm} on a variety of quadrotor nonlinear model predictive control benchmarks, including reference tracking in the presence of obstacles, multi-quadrotor navigation without collision, and navigation in a cluttered environment. We demonstrate up to a $32.85\times$ increase in throughput over IPOPT. Our complete open-source codebase is available at https://github.com/johnviljoen/jaxipm.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Scaling Nonlinear Optimization: Many Problems One GPU》的结构化、深入、客观的中文总结。

### 论文核心问题与整体含义（研究动机与背景）

- **核心问题**：机器人学中许多核心问题（如轨迹优化、逆运动学、接触丰富的运动规划）可转化为非线性规划（NLP）。成熟的NLP求解器（如IPOPT）能提供硬约束满足、最优性保证和良好的扩展性，但它们是**CPU绑定**的，且**一次只能求解单个问题**。这使其无法集成到需要同时处理大量任务的现代GPU批处理学习流水线（如强化学习、模型预测路径积分控制）中，成为性能瓶颈。
- **整体含义**：如果存在一个GPU批处理NLP求解器，就能像GPU模拟器大幅加速动力学展开一样，大幅提升每秒能生成的带约束的局部最优解数量。这将是弥合基于梯度的优化方法与GPU批处理机器人框架之间差距的关键一步。

### 论文提出的方法论

- **核心思想**：基于广泛使用的IPOPT算法，利用JAX框架重新设计并实现了首个GPU批处理NLP求解器——`jaxipm`。其核心在于解决IPOPT无法在GPU上高效并行的问题，主要挑战是IPOPT为处理不同情况而设计的多种**控制流分支（如回溯线搜索、二阶校正、可行性恢复等）** 在GPU统一执行模型中无法并行。
- **关键技术细节**：
    1.  **异构迭代融合 (Heterogeneous Iteration Fusion)**：将IPOPT中所有可能的控制流分支合并成单一、统一的迭代类型。其关键洞察是，多数控制流分支共享大量的**公共计算**（如KKT系统的矩阵分解）。因此，可以一次性为批次中所有问题并行计算这些公共部分。对于剩余的、分支特有的少量计算，则对所有问题无差别地执行所有分支的计算，然后根据每个问题实际所处的分支，选择正确的输出结果。这保证了批次中所有问题在每一步执行相同的操作，实现了锁步前进。
    2.  **迭代级批处理 (Iteration Level Batching)**：不同于“求解级”批处理（等待整个批次都完成才进行下一批），`jaxipm`在更细粒度的“迭代级”进行批处理。当一个优化问题收敛后，它会**热启动**一个新问题到该位置，而其他未收敛的问题则继续迭代，从而最大限度地减少GPU的空闲时间。
- **算法流程（文字说明）**：
    1.  初始化一批NLP问题。
    2.  **循环**直到所有问题收敛或达到迭代上限：
        -   **计算公共量**：并行计算批次中所有问题的KKT矩阵、残差等公共部分。
        -   **计算全部分支的独有量**：为批次中所有问题计算所有可能分支（如回溯、二阶校正）所需的独有计算（如额外的线性求解或向量运算）。
        -   **分支选择与状态更新**：根据每个问题的具体迭代状态，从计算好的结果中选择正确的输出，更新该问题的迭代变量（primal-dual iterate）。
        -   **收敛检测与热启动**：检测哪些问题已收敛，将其解保存，并用新问题替换其位置，继续在下一轮迭代中处理。

### 实验设计

- **场景与数据集**：所有实验基于**四旋翼飞行器的非线性模型预测控制（NMPC）**。涵盖了三个任务场景：
    1.  **带障碍物的导航**：从一个圆形区域出发，飞向共同目标点，需避开障碍物。
    2.  **参考轨迹跟踪**：跟踪一个已知的时变参考轨迹，同时躲避时变障碍物。并通过改变参考轨迹的平均速度$\\bar{v}$来控制问题难度。
    3.  **多四旋翼编队协同**：多架四旋翼交换位置，同时保持彼此间的安全距离，通过改变四旋翼数量（2架或4架）来改变问题维度。
- **基准 (Benchmark)**：每个优化问题是一个30步预测时域的NMPC，具有506个优化变量。
- **对比方法**：
    -   **IPOPT**：经典的CPU单线程NLP求解器，通过Python库CasADi调用。
    -   **MadNLP**：一个GPU加速的IPOPT变体，它使用cuDSS在GPU上加速KKT系统的线性求解和稀疏导数计算，但其算法逻辑（控制流）仍在CPU上执行，未能实现真正的批处理。

### 资源与算力

- 论文未明确说明实验所用GPU的具体型号、数量或训练时长，仅提及在支持JAX和CUDA的GPU硬件上运行（使用了NVIDIA cuDSS库）。

### 实验数量与充分性

- **实验组数**：论文在3个不同的任务场景（导航、跟踪、编队）下进行了评估。每个场景包含不同的难度或维度变体（如180° vs 90°起始区域，不同$\\bar{v}$值，2架 vs 4架四旋翼），共约6组主实验结果，并额外进行了正确性验证实验。
- **充分性与客观性**：
    - **公平性**：对比了IPOPT和MadNLP，这是该领域最相关的基线方法。
    - **指标全面**：从**吞吐量**（solves/s，关键指标）、**解的质量**（平均成本）、**计算效率**（平均迭代次数）三个维度进行对比，部分实验结果呈现在图表中。
    - **不足之处**：
        - **缺乏消融实验**：论文没有单独进行消融实验来量化“异构迭代融合”和“迭代级批处理”两个贡献各自的提升效果。尽管从效率角度看两者结合明显必要，但单独量化能更好地体现技术贡献。
        - **场景单一**：所有实验仅基于四旋翼系统。虽然体现了NLP求解器的通用性，但缺乏在更广泛机器人问题（如机械臂、足式机器人）上的验证，限制了其泛化性结论的强度。

### 论文的主要结论与发现

- `jaxipm`作为首个GPU批处理NLP求解器，成功地实现了并行求解数千个NLP问题。其主要结论包括：
    1.  **显著的吞吐量提升**：在所有测试基准中，`jaxipm`的吞吐量（solves/s）均大幅超过IPOPT，最高可达**32.85倍**（在多四旋翼2机场景下）。在单次求解时间上，也有明显优势。
    2.  **迭代级批处理的有效性**：在异构问题（如180°扇形起始区域，不同问题迭代次数分布不均）的场景下，`jaxipm`通过迭代级批处理保持了与同构场景（90°扇形）相似的吞吐量优势，证明了其减少GPU空闲时间的有效性。
    3.  **与IPOPT的算法一致性**：通过正确性验证实验，`jaxipm`在每一步迭代的结果（状态向量）与IPOPT的偏差在$10^{-8}$量级以内，证明了其在算法上的忠实复现。
    4.  **随问题难度与维度的可扩展性**：
        - 随问题**难度（$\\bar{v}$）增加**，`jaxipm`的吞吐量优势（相对于IPOPT）反而增大，表明其鲁棒性较好。
        - 随问题**维度（四旋翼数量）增加**，`jaxipm`的吞吐量优势减弱（从32.85x降至16.03x），原因是GPU内存限制导致单批次能容纳的问题数量减少。

### 优点

- **创新性**：首次提出并实现了GPU批处理NLP求解器，解决了该领域一个长期存在的核心瓶颈，思路新颖且实用。
- **实用性与影响力**：解决了模型预测控制等非线性优化方法无法有效融入现代GPU加速机器人学习流水线的问题，有望推动大规模约束优化数据集的生成，对模仿学习、强化学习等领域有直接应用价值。
- **方法清晰有效**：“异构迭代融合”和“迭代级批处理”两个贡献点明确，技术路径解释清晰，且实验证明了其有效性。
- **开源与兼容性**：使用JAX实现，与现代GPU加速的深度学习框架和模拟器（如MuJoCo MJX, Brax）原生兼容，降低了集成门槛，有助于实际应用。

### 不足与局限

- **算力信息不完整**：未提供GPU型号、数量、显存、运行时间等关键硬件信息，降低了实验的可复现性。
- **消融实验缺失**：未设计单独实验来量化“异构迭代融合”和“迭代级批处理”各自的贡献，使得每种技术带来的具体加速效果难以判定。
- **实验覆盖有限**：仅在一个类型的机器人（四旋翼）上验证。问题结构（如KKT矩阵的稀疏性模式、非线性程度）在不同机器人系统上差异很大，其泛化性能仍需更多场景验证。
- **应用局限**：
    - **对动态模型要求高**：要求动力学模型是已知且至少二阶可微的，这限制了其在纯数据驱动或不可微模型上的应用。
    - **内存限制**：如实验所示，对于大规模高维问题，GPU内存可能成为新的瓶颈，限制了单次批处理的数量。
    - **理论分析缺失**：论文主要侧重于工程实现和性能对比，缺乏对融合算法或批处理策略对收敛性理论影响的深入分析（如是否会影响收敛率和保证）。
- **与MadNLP的对比不全面**：MadNLP是一个GPU加速但仍是顺序求解的求解器。它与`jaxipm`是不同维度的加速（单问题加速 vs 批处理吞吐量增强），直接比较solves/s可能对MadNLP不够公平，因为它在内存、预处理等方面的开销可能不同，且未考虑MadNLP在单问题求解时间上的潜在优势。

（完）
