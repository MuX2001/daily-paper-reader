---
title: "RS-Diffuser: Risk-Sensitive Diffusion Planning with Distributional Value Guidance"
title_zh: RS-Diffuser：具有分布价值指导的风险敏感扩散规划
authors: Shiqiang Gong
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27766v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 风险敏感的离线强化学习与扩散规划
tldr: 现有扩散规划器在离线强化学习中风险中性，忽视了安全关键场景的罕见灾难性后果。RS-Diffuser框架融合扩散轨迹生成与蒙特卡洛分布价值批评家，通过分位数回归估计完整回报分布，并在去噪过程中利用条件风险价值等尾部目标进行风险敏感引导。仅调整推理时的风险参数，即可在单个模型上实现风险规避、中性或寻求行为。在风险敏感D4RL和机器人导航任务上，该方法达到SOTA，显著提升平均回报与最坏情况鲁棒性，同时减少安全违规。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27766v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 236, \"height\": 164, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27766v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1256, \"height\": 871, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27766v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 269, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27766v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 330, \"label\": \"Table\"}]"
motivation: 现有扩散规划器风险中性，难以处理安全关键场景中的罕见灾难性后果。
method: RS-Diffuser结合扩散轨迹生成与分布价值批评家，通过分位数回归估计回报分布，并用条件风险价值等尾部目标在去噪过程中引导。
result: 在风险敏感D4RL和机器人导航基准上取得SOTA，提升平均回报和最坏情况鲁棒性，降低安全违规。
conclusion: 单个RS-Diffuser模型通过调整风险参数可灵活适应不同风险偏好，为安全关键离线决策提供有效框架。
---

## 摘要
离线强化学习使得从固定数据集中进行策略学习成为可能，无需额外的环境交互，这对于在线探索成本高昂或不安全的安全关键型应用很有吸引力。基于扩散的决策方法最近通过建模丰富的多模态轨迹分布，在离线RL中取得了强劲表现。然而，现有的扩散规划器通常是风险中性的，因此可能会忽略在实际部署中至关重要的罕见但灾难性的结果。在这项工作中，我们提出了RS-Diffuser，一个风险敏感的离线扩散规划框架，它将基于扩散的轨迹生成与分布价值评论家相结合。RS-Diffuser学习一个针对未来状态轨迹的扩散规划器、一个用于动作解码的独立逆动力学模型，以及一个通过分位数回归估计候选计划的完整回报分布的蒙特卡洛分布评论家。在采样时，我们将风险敏感指导信号融入去噪过程，使用从尾部感知目标（如条件风险价值）计算的梯度来引导生成朝向期望的风险分布。因此，一个训练好的模型可以通过仅改变推理时的风险参数，灵活地产生风险规避、风险中性或风险寻求的行为。在风险敏感的D4RL和有风险的机器人导航基准上的大量实验表明，RS-Diffuser达到了最先进的性能，提高了整体回报和最坏情况下的鲁棒性，同时减少了安全违规。

## Abstract
Offline reinforcement learning enables policy learning from fixed datasets without additional environment interaction, making it appealing for safety-critical applications where online exploration is costly or unsafe. Diffusion-based decision-making methods have recently achieved strong performance in offline RL by modeling rich, multimodal trajectory distributions. However, existing diffusion planners are typically risk-neutral and therefore may overlook rare but catastrophic outcomes that are crucial in real-world deployment. In this work, we propose RS-Diffuser, a risk-sensitive offline diffusion planning framework that combines diffusion-based trajectory generation with distributional value critics. RS-Diffuser learns a diffusion planner over future state trajectories, a separate inverse dynamics model for action decoding, and a Monte Carlo distributional critic that estimates the full return distribution of candidate plans through quantile regression. At sampling time, we incorporate a risk-sensitive guidance signal into the denoising process, using gradients computed from tail-aware objectives such as Conditional Value at Risk to steer generation toward desired risk profiles. As a result, a single trained model can flexibly produce risk-averse, risk-neutral, or risk-seeking behaviors by changing only the inference-time risk parameter. Extensive experiments on risk-sensitive D4RL and risky robot navigation benchmarks demonstrate that RS-Diffuser achieves state-of-the-art performance, improving both overall return and worst-case robustness while reducing safety violations.

---

## 论文详细总结（自动生成）

# RS-Diffuser 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **动机**：离线强化学习在安全关键领域（如自动驾驶、机器人）很有前景，但现有扩散规划器（如Diffuser）是**风险中性**的，仅优化期望回报，忽略罕见但灾难性的尾部风险。两段轨迹可能期望回报相近但尾部风险差异巨大，现实部署中需要更谨慎。
- **背景**：风险敏感RL通过条件风险价值（CVaR）等尾部目标优化策略。但现有风险敏感离线RL方法多基于actor-critic架构，策略表达能力有限；而扩散模型虽擅长建模多模态分布，却缺乏显式风险控制。
- **整体含义**：本文提出RS-Diffuser，将扩散轨迹生成与分布价值评论家结合，首次实现**推理时灵活控制风险偏好**（风险规避、中性、寻求），无需重新训练。在离线RL中兼顾性能与安全性。

## 2. 论文提出的方法论

### 核心思想
- 将扩散规划器（生成状态序列）与一个**蒙特卡洛分布价值评论家**（通过分位数回归估计完整回报分布）解耦。推理时，从评论家提取尾部风险度量（如CVaR）的梯度，引导去噪过程产生符合期望风险分布的轨迹。

### 关键技术细节（三步训练 + 一步推理引导）

1. **状态轨迹扩散规划器**  
   - 定义未来状态序列 \(x_0 = (s_{t+1},..., s_{t+H})\)，条件为当前状态 \(s_t\)。  
   - 训练DDPM去噪网络 \(\epsilon_\theta(x_k, k, s_t)\)，最小化标准扩散损失 \(L_{\text{plan}}\)。

2. **逆动力学动作解码器**  
   - 学习确定性映射 \(f_\psi(s_t, s_{t+1}) \to a_t\)，通过监督回归损失 \(L_{\text{inv}}\) 训练。

3. **蒙特卡洛分布价值评论家**  
   - 为每个轨迹段计算蒙地卡罗折扣回报 \(R_t\)。  
   - 训练分位数回归网络 \(z_\phi(x_k, s_t, k)\) 预测N个分位值 \(\{z^i_\phi\}\)，损失为分位数Huber损失 \(L_{\text{dist}}\)。  
   - 评论家输出完整回报分布，可计算：  
     - 下尾CVaR：\(\rho^\text{low}_\alpha = \frac{1}{|I_\alpha|}\sum_{i\in I_\alpha} z^i_\phi\) (其中 \(I_\alpha = \{i: \tau_i \le \alpha\}\))。  
     - 上尾目标类似 \(\rho^\text{up}_\alpha\)。

4. **风险引导扩散采样**  
   - 在去噪每一步，计算风险梯度 \(g_k = \nabla_{x_k} \rho_\alpha(x_k, s_t, k)\)。  
   - 用公式 \(p_{\theta,\phi}(x_{k-1}|x_k, s_t, \text{risk}) \approx \mathcal{N}(\mu_\theta + w \Sigma_k g_k, \Sigma_k)\) 修改反向过程，引导轨迹偏向所选风险偏好。  
   - **关键优势**：规划器只训练一次；通过改变 \(\rho_\alpha\) 和参数 \(\alpha\) 即可在推理时切换风险偏好。

### 算法流程（Algorithm 1）
- 离线训练：依次更新规划器、逆动力学模型、分布评论家。  
- 推理：给定当前状态 → 从噪声开始 → 每步计算风险梯度 → 执行引导去噪 → 获取计划状态 → 用逆动力学解码动作并执行。

## 3. 实验设计

### 数据集/场景与Benchmark
- **风险敏感D4RL**：在MuJoCo环境（Half-Cheetah, Walker2D, Hopper）中加入随机惩罚（如速度超限、跌倒）模拟安全故障。每个环境有Medium、Expert、Mixed三种数据质量。
- **Risky Robot Navigation**：Risky PointMass和Risky Ant，要求从随机起点到达目标，但捷径存在高风险区域（惩罚大）；需要平衡效率与安全。

### 对比方法
- 风险中性基线：CQL、Diffusion-QL。  
- 风险敏感基线：OWCPG（最坏情况策略梯度）、ORAAC（分布评论家+模仿策略）、CODAC（保守离线分布RL）、UDAC（不确定性感知扩散策略）。

### 评价指标
- 主要：**平均回报（Mean）** 和 **CVaR_{0.1}**（最差10%的均值）。  
- 导航任务额外报告**违规次数**（进入危险区域的步数）。  
- 所有结果基于5个随机种子，报告均值±标准差。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量或训练总时长。仅提到模型训练1M步，batch size 256，采用DDIM 20步规划，DDPM 10步逆动力学。  
- 推断：由于CleanDiffuser库和Transformer（2块，隐藏256）等配置，推测在单卡或少量GPU上可完成，但具体算力未公开。

## 5. 实验数量与充分性

- **实验数量**：  
  - 主实验：3个D4RL环境 × 3种数据质量 = 9个设置（表1）；2个导航场景 = 2个设置（表2）；共11组对比。  
  - 消融实验：在Half-Cheetah Medium/Expert/Mixed上测试不同风险度量（VaR vs CVaR）及α取值（表3），包含2种度量×3种α=6种配置。  
- **充分性**：  
  - 对比基线全面，涵盖风险中性和主流风险敏感方法。  
  - 指标多样（均值、CVaR、违规数），并能体现尾部性能变化。  
  - 消融实验证实风险参数可灵活控制偏好，且CVaR_{0.1}为默认选择的合理性。  
- **客观公平**：所有方法在同一设置下评估，重复5次种子，报告波动范围，结论可靠。

## 6. 论文主要结论与发现

- RS-Diffuser在几乎所有设置下均取得**最优或接近最优**的平均回报与CVaR_{0.1}，且违规次数最少。  
- 风险中性方法（CQL）在安全敏感任务中表现差；风险敏感基线（UDAC）需训练固定风险目标，灵活性不足。  
- **推理时调整风险偏好**是可行且有效的：小α提高尾部鲁棒性，大α提升平均回报。CVaR引导比VaR引导更有利于尾部性能。  
- 该方法在高维复杂任务（Risky Ant）中仍能平衡安全（违规低）与效率（回报高），体现扩散模型强大的状态建模能力。

## 7. 优点

- **方法创新**：首次将分布价值评论家与扩散规划结合，实现推理时无缝切换风险偏好，无需重训练。  
- **灵活性**：一个模型覆盖所有风险策略，实际部署价值高。  
- **性能领先**：在11个设置中表现SOTA，尤其尾部鲁棒性突出。  
- **可解释性**：风险引导梯度源自明确的分位数目标，理论清晰。  
- **模块化设计**：扩散规划器、逆动力学、分布评论家独立训练，便于调试和替换。

## 8. 不足与局限

- **计算开销**：推理时需多次计算风险梯度（每个去噪步），可能增加延迟，实时应用需优化。  
- **实验覆盖**：仅在模拟环境（MuJoCo、简单点质量/蚂蚁）验证，未在真实机器人或高维真实场景测试。  
- **假设限制**：依赖蒙特卡洛回报（需完整轨迹标签），在长周期或稀疏奖励任务中可能效率低。  
- **评估指标单一**：仅考虑回报与违规，未分析稳健性对模型不确定性、数据偏移等影响。  
- **公平性**：对比方法（如UDAC）可能未针对本文任务最佳调参；消融仅在一个环境进行，泛化性待验证。  
- **论文未公开代码**，可复现性受限。

（完）
