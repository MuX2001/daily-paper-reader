---
title: Direct Action-Head Injection of A Grounded 3D Point Unlocks Spatial and Task Generalization
title_zh: 直接动作头注入接地3D点实现空间与任务泛化
authors: "Shiang-Feng Tsai, Jin-Cheng Jhang, Yen-Ling Tai, Jia-Hong Lai, Shih-Yun Wong, KangTung-Hsu, Yi-Ting Chen"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27663v1"
tags: ["query:vla"]
score: 9.0
evidence: 3D点注入提升VLA空间泛化
tldr: "VLA模型在空间和任务泛化上存在瓶颈，现有通过2D像素或语言提示注入grounding的方法效果有限。本文提出轻量级模块，将3D点表示的grounding信号与夹爪相对位移编码，通过自适应层归一化直接注入动作头。在LIBERO-PRO上，GR00T-N1.6在任务扰动下成功率从31.2%提升至77.5%，位置扰动下从28.1%提升至60.2%；π0.5也获得类似增益。结果证明，将3D grounding直接注入动作头是解锁泛化的关键，且方法轻量、骨干无关。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27663v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 878, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1233, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 706, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 777, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 733, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 731, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 1260, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1440, \"height\": 1611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1445, \"height\": 1270, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27663v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1301, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1083, \"height\": 363, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 489, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 491, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 1857, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1080, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1083, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1439, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1435, \"height\": 222, \"label\": \"Table\"}]"
motivation: 现有VLA模型通过语言或视觉提示注入grounding信号，未能有效解决空间和任务泛化问题。
method: 提出轻量模块，将3D点表示的grounding信号与夹爪相对位移编码，通过自适应层归一化直接注入动作头。
result: 在LIBERO-PRO上，GR00T-N1.6在任务扰动下成功率提升46.3个百分点，位置扰动下提升32.1个百分点。
conclusion: 将3D grounding直接注入动作头而非语言或视觉提示，是解锁VLA泛化的关键，且方法骨干无关、轻量高效。
---

## 摘要
视觉-语言-动作（VLA）模型利用大规模视觉-语言预训练实现灵活的机器人操作，但在测试时它们仍然在两个方面脆弱：空间泛化（当物体位置与训练时不同）和任务泛化（当熟悉场景与训练时不同的语言指令配对）。越来越多的方法通过赋予策略空间和任务感知信息（如用于物体定位和放置的2D像素坐标）来解决这种脆弱性。然而，我们发现现有的语言提示或视觉提示表示方法并未解决这些限制；相反，利用基于3D点的表示并将其直接馈送到动作头可以带来显著改进——揭示出接地信号如何表示并注入VLA才是真正的变革因素。因此，我们提出一个轻量级、模型无关的模块，该模块以3D形式表示接地信号，计算其相对于夹爪的位移，并通过自适应层归一化将产生的空间嵌入直接注入动作头。整个模块是一个两层MLP，无需更改VLA主干或预训练流程。在LIBERO-PRO上，我们的方法将GR00T-N1.6在任务扰动下的平均成功率从31.2提高到77.5，在位置扰动下从28.1提高到60.2（分别提高46.3和32.1个百分点）。对于π₀.₅也实现了相当的增益，证明该机制与主干无关。这些结果共同支持我们的核心发现：给定提升到3D的充分接地，将其直接注入动作头是解锁VLA中空间和任务泛化的关键——只需在预训练主干之上添加一个轻量级模块即可实现。

## Abstract
Vision-Language-Action (VLA) models leverage large-scale vision-language pretraining for flexible robot manipulation, yet at test time they remain brittle along two axes: spatial generalization, when object positions differ from those seen during training, and task generalization, when a familiar scene is paired with a different language instruction than the one seen in training. A growing family of methods addresses this brittleness by endowing a policy with the spatial and task-aware information such as 2D pixel-coordinate for object localization and placement. However, we find that existing representation through language prompting or visual prompting does not address the limitations; in contrast, exploiting a 3D point-based representation and feeding it directly to the action head leads to substantial improvements-revealing that how the grounding signal is represented and injected into the VLA is the true game changer. Thus, we propose a lightweight, model-agnostic module that represents the grounding signal in 3D, computes its relative displacement to the gripper, and injects the resulting spatial embedding directly into the action head through adaptive layer normalization. The entire module is a two-layer MLP that requires no changes to the VLA backbone or pretraining pipeline. On LIBERO-PRO, our method improves the average success rate of GR00T-N1.6 from 31.2 to 77.5 points under task perturbation and from 28.1 to 60.2 points under position perturbation (gains of 46.3 and 32.1 points). Comparable gains are achieved for $π_{0.5}$ as well, demonstrating that the mechanism is backbone-agnostic. Together, these results support our central finding: given adequate grounding lifted into 3D, injecting it directly into the action head is what unlocks both spatial and task generalization in VLAs-achievable with nothing more than a lightweight module on top of a pretrained backbone.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

**核心问题**：视觉-语言-动作（VLA）模型在大规模预训练后具备语义理解能力，但在测试时仍存在两个关键泛化瓶颈：
- **空间泛化**：物体位置与训练时不同时，动作预测失败。
- **任务泛化**：同一场景下语言指令发生变化时，模型仍执行记忆中的轨迹而非新指令。

**背景**：现有方法试图通过向策略注入显式的空间接地（grounding）信息来缓解这一问题，通常使用**2D像素坐标**，并通过语言提示（text prompt）、视觉标记（visual prompt）或中间推理链（chain-of-thought）传递给策略。然而这些方法提升有限。作者发现，**接地信号的表示方式（2D vs 3D）和注入方式（语言/视觉 vs 直接动作头注入）** 才是真正的瓶颈所在。

---

## 2. 论文提出的方法论

### 核心思想
将2D接地点提升到3D空间，计算其与夹爪的相对位移，编码为空间嵌入，并通过**自适应层归一化（AdaLN）** 直接注入到扩散Transformer（DiT）动作头中，实现对动作预测的几何条件化。

### 关键技术细节
1. **2D到3D提升**：  
   给定2D目标点 \((u,v)\)，利用深度图像和相机参数（内参 \(K\)、外参 \(R,t\)）通过针孔模型反投影到3D相机坐标系，再转换到机器人基坐标系得到目标位置 \(p_t \in \mathbb{R}^3\)。  
   夹爪位置 \(p_g\) 来自机器人状态（正运动学）。

2. **相对位移计算**：  
   \[
   d = p_t - p_g \in \mathbb{R}^3
   \]

3. **空间嵌入编码**：  
   使用一个两层MLP（隐藏维度和输出维度均为1024，匹配动作头隐藏大小）将 \(d\) 编码为空间嵌入 \(z_{\text{spatial}}\)。
   \[
   z_{\text{spatial}} = \text{MLP}(d)
   \]

4. **注入到动作头**：  
   原始DiT动作头中的AdaLN仅使用时间步嵌入 \(z_{\text{time}}\) 回归缩放和偏移参数 \(\gamma, \beta\)。本文将 \(z_{\text{spatial}}\) 与 \(z_{\text{time}}\) 逐元素相加后再送入线性层：
   \[
   \gamma, \beta = \text{Linear}(z_{\text{time}} + z_{\text{spatial}})
   \]
   从而每个DiT块都能利用几何信息调制归一化特征。

5. **初始化技巧**：MLP的第二层权重和偏置初始化为零，确保训练开始时 \(z_{\text{spatial}}=0\)，不破坏预训练AdaLN的初始行为。

### 算法流程
1. 给定任务指令，使用任何现成的视觉接地模块（如VLM）获取2D目标点。
2. 根据当前子目标（抓取或放置），选择对应的2D点。
3. 利用深度图像和相机参数提升为3D点 \(p_t\)，从机器人状态获取 \(p_g\)，计算 \(d\)。
4. 通过两层MLP编码为 \(z_{\text{spatial}}\)。
5. 在动作头的每个DiT块中，将 \(z_{\text{spatial}}\) 与 \(z_{\text{time}}\) 融合后通过AdaLN调制特征，输出动作。

---

## 3. 实验设计

### 数据集与场景
- **仿真**：LIBERO（包含LIBERO-Object和LIBERO-Spatial）和LIBERO-PRO（扩展了任务扰动和位置扰动两种测试条件）。  
  默认训练集：从LIBERO-Object、LIBERO-Spatial和LIBERO-90中选出**69个抓放任务**（在消融中也使用原始20任务设置）。
- **真实世界**：使用Franka Emika Panda机器人，两个RealSense D435相机（前视RGB-D和腕部RGB），收集**8个抓放任务**，每任务40个演示。

### Benchmark
- **LIBERO**：分布内性能。
- **LIBERO-PRO**：两种OOD测试：
  - **Task Perturbation**：替换目标对象为场景中另一合理对象（指令变化）。
  - **Position Perturbation**：改变初始物体位置（位置变化）。
- 每个任务**50次rollout**（仿真）或**10次rollout**（真实世界），报告平均成功率。

### 对比方法
- **开源VLA基线**：OpenVLA、π₀、MolmoAct。
- **同数据训练基线**：π₀.₅、GR00T-N1.6。
- **控制变量变体**（均使用**相同oracle 2D点**）：
  - 2D via Text Prompt：将2D坐标追加到语言提示。
  - 2D via Visual Prompt：在图像上覆盖标记。
  - 2D via AdaLN：将2D坐标及2D相对位移通过MLP编码后注入AdaLN。
  - 3D via Text Prompt：将3D坐标追加到语言提示。
  - **Ours（3D via AdaLN）**：完整方法。

---

## 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量及具体训练时长。仅提供了训练超参数（如batch size、学习率、优化器、训练步数等）：
- GR00T-N1.6：全局batch size 384，训练20,000步，学习率1e-4。
- π₀.₅：全局batch size 256，训练30,000步，学习率5e-5。
- 真实世界GR00T-N1.6：全局batch size 256，训练20,000步。

未提及使用的GPU个数及类型，因此无法评估算力成本。

---

## 5. 实验数量与充分性

### 实验数量
- **表1**：LIBERO和LIBERO-PRO主结果（两个骨干×两种扰动×两个子套件），共多组对比。
- **表2**：控制变量实验（5种变体 + 基线）在LIBERO-PRO上。
- **图3**：数据规模对比（20任务 vs 69任务），包含所有变体。
- **图4**：深度噪声实验（两种噪声水平、不同训练/测试组合）。
- **表3**：空间嵌入输入成分消融（仅 \(p_t\)、仅 \(p_g\)、仅 \(d\)、三者组合）。
- **表4**：注入机制消融（机器人状态注入 vs AdaLN）。
- **真实世界实验**：8任务×3条件×10rollout，对应表8-10及图5。

### 充分性评价
- **充分**：从表示方式（2D vs 3D）和注入机制（文本、视觉、AdaLN）两个维度进行了系统消融，隔离了各因素贡献。
- **客观公平**：所有变体使用相同的oracle 2D点，排除接地质量差异。对比基线包括同数据训练和开源VLA，覆盖不同骨干。
- **不足**：
  - 仿真中2D点来自oracle分割掩码，真实世界使用Qwen3-VL-4B（也存在误差），但未比较不同接地源（如检测器 vs VLM）对最终性能的影响。
  - 仅涉及抓放任务，未验证其他操作类型（如推动、拧螺丝等）。
  - 深度噪声实验仅在仿真中进行，未在真实世界验证。

---

## 6. 论文的主要结论与发现

1. **3D表示优于2D**：动作预测在3D物理空间进行，2D信号迫使网络内部学习2D→3D映射，而直接提供3D相对位移则消除了这一负担。
2. **直接注入动作头优于语言/视觉提示**：将3D信号通过AdaLN注入DiT动作头，保留了几何信息的完整性；而通过文本或视觉提示传递时，几何结构在VLM处理中丢失。
3. **轻量且骨干无关**：仅需一个两层MLP（零初始化），无需修改VLA主干或预训练流程；在GR00T-N1.6和π₀.₅上均取得显著增益。
4. **设计选择优于数据规模**：在20任务上训练的Ours方法，其性能已超过其他方法在69任务上的表现，表明接地表示与注入方式比单纯增加数据多样性更重要。
5. **对深度噪声鲁棒**：训练时加入噪声会轻微下降，但测试时不同噪声水平下性能稳定，表明方法容忍深度误差。
6. **真实世界有效**：使用消费级RGB-D传感器和通用VLM（Qwen3-VL-4B）作为接地源，Ours方法在任务和位置扰动下仍保持高成功率，而基线趋于零。

---

## 7. 优点

- **极简设计**：仅两层MLP，注入机制复用现有AdaLN，几乎无额外参数和计算开销。
- **模型无关**：可在不同VLA骨干（GR00T-N1.6、π₀.₅）上即插即用。
- **保持分布内性能**：在LIBERO分布内测试中性能不变或略有提升（表1）。
- **消融彻底**：将表示和注入两个维度完全解耦，每个变体均使用相同接地源，结论可信度高。
- **真实世界验证**：不仅限于仿真，在真实噪声环境下同样有效，证明实用性。
- **训练稳定**：通过第二层零初始化避免干扰预训练行为。

---

## 8. 不足与局限

- **任务假设限制**：假设每个子目标可关联一个目标物体或区域，不适用于物体无关运动（如推动、擦拭等无明确目标点的操作）。作者建议未来可将2D轨迹提升为3D轨迹后通过类似机制注入。
- **接地源依赖**：假设存在可用的2D接地信号。复杂场景或指令下，鲁棒识别目标仍是开放问题，本文未深入探讨。
- **单臂机器人**：实验仅在单臂平台上进行，未验证双臂或人形机器人。
- **深度依赖**：需要深度图才能进行2D→3D提升；深度噪声实验仅仿真，真实世界中深度传感器失效情况未评估。
- **泛化范围有限**：任务扰动仅涉及同场景内物体替换，未测试完全未见过的物体或场景的零样本泛化。
- **资源信息缺失**：未报告训练所需GPU型号、数量及实际运行时间，难以复现或计算成本。

（完）
