---
title: "PAMAE: Phase-Aware-MoE Action Experts Towards Reliable Flow-Matching Vision-Language-Action Policies"
title_zh: "PAMAE: 面向可靠流匹配视觉-语言-动作策略的相位感知混合专家动作模块"
authors: "Jiayu Yang, Tao Yang, Xiang Chang, Fei Chao, Changjing Shang, Qiang Shen"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27144v1"
tags: ["query:vla"]
score: 9.0
evidence: 相位感知专家混合动作模块用于流匹配VLA策略
tldr: "多阶段机器人操作中，现有流匹配视觉-语言-动作模型因使用单一动作专家，难以适应不同执行阶段的控制模式，导致可靠性不足。本文提出即插即用的PAMAE模块，采用稀疏混合专家架构并引入阶段感知路由器，通过两阶段训练实现阶段一致的专家分配。在模拟多阶段操作任务中，PAMAE将任务成功率提升9.2%，优于强基线。该方法表明基于阶段线索的专家分配是提升流匹配VLA策略可靠性的有效机制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27144v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1803, \"height\": 502, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27144v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27144v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 696, \"height\": 293, \"label\": \"Table\"}]"
motivation: 多阶段机器人操作中，现有流匹配VLA模型因单一专家无法捕获阶段特有控制模式，导致动作生成不可靠。
method: 提出PAMAE模块，用阶段感知稀疏专家混合替换原动作专家，配合阶段预测头和路由对齐目标，经两阶段训练实现阶段一致性。
result: "在模拟多阶段操作任务中，PAMAE将成功率提升高达9.2%，强于基线，且阶段监督路由与分阶段优化均不可或缺。"
conclusion: 阶段一致性专家分配能有效提升流匹配VLA策略的可靠性和动作质量。
---

## 摘要
对于多阶段机器人操作，可靠的行动生成对视觉-语言-动作（VLA）模型来说仍然是一个挑战。现有的流匹配VLA策略虽然提供了强大的多模态基础泛化能力，但它们通常采用单一的共享动作专家，限制了其在不同执行阶段捕捉特定阶段控制模式的能力。我们提出了一种即插即用的相位感知混合专家动作模块（PAMAE），作为迈向更可靠的阶段一致动作生成的一步。PAMAE将原始的流匹配动作专家替换为稀疏专家混合体，同时保留预训练的VLA骨干网络。PAMAE引入了一个相位感知路由器，利用执行阶段线索在专家之间分配动作生成，并辅以轻量级的相位预测头和路由对齐目标。为了稳定专业化，我们采用了两阶段训练方案：首先在标准流匹配损失下预热专家模块，然后在辅助监督下优化阶段一致路由。在多阶段操作模拟任务中，与强大的VLA基线相比，PAMAE将任务成功率提高了高达\textbf{9.2\%}。进一步的消融研究表明，相位监督路由和分阶段优化对于观察到的增益都是必不可少的。我们的结果强调了阶段一致专家分配作为提高流匹配VLA策略可靠性和动作质量的有效机制。

## Abstract
Reliable action generation for multi-stage robotic manipulation remains challenging for Vision-Language-Action (VLA) models. While existing flow-matching VLA policies offer strong multimodal grounding and generalization, they typically employ a single shared action expert, limiting their ability to capture phase-specific control patterns across distinct execution stages. We propose a plug-and-play Phase-Aware Mixture-of-Experts Action Module (PAMAE), as a step towards more reliable phase-consistent action generation. PAMAE replaces the original flow-matching action expert with a sparse expert mixture while preserving the pretrained VLA backbone. PAMAE introduces a phase-aware router that leverages execution-phase cues to allocate action generation across experts, supported by a lightweight phase prediction head and a routing alignment objective. To stabilize specialization, we adopt a two-stage training scheme that first warms up the expert module under the standard flow-matching loss and then optimizes phase-consistent routing under auxiliary supervision. On multi-stage manipulation simulation tasks, PAMAE improves task success by up to \textbf{9.2\%} over strong VLA baselines. Further ablations show that both phase-supervised routing and staged optimization are essential for the observed gains. Our results highlight phase-consistent expert allocation as an effective mechanism for improving the reliability and action quality of flow-matching VLA policies.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

多阶段机器人操作（如接近、接触、运输等）在不同执行阶段展现出截然不同的控制模式、过渡动态和精度要求。现有的流匹配（Flow-Matching）视觉-语言-动作（VLA）策略（如 π0、π0.5）虽然具备强大的多模态基础泛化能力，但**采用单一共享动作专家**来预测所有阶段的控制信号，导致阶段特异性行为被模糊化，降低了动作生成的可信度与鲁棒性。即使部分工作引入混合专家（MoE）架构，也并非针对低层动作的执行阶段结构设计，专家分配与阶段特异性控制模式对齐不足。因此，本文的核心问题是：**如何在不增加推理阶段标签开销的情况下，让流匹配VLA策略在低层动作生成中实现阶段一致的专家分配，从而提升多阶段操作任务的成功率？**

## 2. 方法论

### 核心思想
提出 **PAMAE（Phase-Aware Mixture-of-Experts Action Module）**，一种即插即用的阶段感知稀疏混合专家动作模块。保留预训练VLA骨干（π0/π0.5），将原始的共享动作专家替换为一个**稀疏专家混合体**，并引入**阶段感知路由器**，利用执行阶段的轻量线索（如夹爪状态、动作幅值、归一化进度）来动态分配专家，使动作生成更好地捕获阶段依赖的控制模式。

### 关键技术细节
- **专家模块设计**：在VLA骨干提取上下文特征 \( h_t \) 后，通过共享动作主干 \( f_{\text{shared}} \) 得到共同潜在表示 \( q_t^\tau \)，实例化 \( M=6 \) 个动作专家，每个专家预测候选速度场分量。路由器输入路由特征 \( r_t^\tau = [h_t, \phi_t, \tau] \)，其中 \( \phi_t \) 包含夹爪状态、夹爪变化量、最近动作幅值、归一化进度 \( \rho_t = t/T \)（T为训练集95%分位数）。路由器输出专家权重分布，采用top-k（k=3）稀疏路由，最终速度预测为加权聚合。
- **阶段标签定义**：训练时由规则生成伪阶段标签（pre-contact, contact/manipulation, post-contact），基于夹爪闭合、末端执行器运动速度等阈值。
- **两阶段训练策略**：
  - **Stage 1（预热）**：仅用标准流匹配损失 \( L_{\text{fm}} \) 和极小负载均衡项 \( L_{\text{lb}} \) 训练，稳定专家形成，防止过早路由崩溃。
  - **Stage 2**：加入阶段监督路由目标：阶段预测头损失 \( L_{\text{phase}} \)（鼓励路由特征保留相位信息）、路由对齐损失 \( L_{\text{route}} \)（KL散度使专家分配接近阶段特定的目标分布）、路由平滑损失 \( L_{\text{smooth}} \)（减少相邻时间步的路由抖动）。在Stage2最后30%训练中，将辅助损失权重线性退火至初始值的10%，使优化转向动作质量精炼。
- **推理阶段**：无需任何阶段标签，路由器自主基于 \( r_t^\tau \) 进行稀疏专家分配，保持原始流匹配集成流程不变。

## 3. 实验设计

### 任务与场景
- **五个模拟多阶段操作任务**：
  - Table-Cleaning (T.C.)
  - Drawer-Cycle (D.C.)
  - Lid-Open (L.O.)
  - Shelf-Insert (S.I.)
  - Cup-Upright (C.U.)
  覆盖接近、接触、运输、插入、释放等多种阶段行为。

### Benchmark 与对比方法
- **基线方法**：
  - π0（原始流匹配VLA骨干）
  - π0 (labeled)：在训练时使用相同粗粒度阶段监督但保持单一专家
  - π0.5（更强骨干）
  - ProgressVLA（基于进度引导的扩散策略）
- **本文方法**：PAMAE(π0) 和 PAMAE(π0.5)，只替换动作专家，骨干和流程不变。

### 实验设置
- 每个任务20次trials × 5个随机种子 = 100次运行，计算成功率。
- 视觉观测尺寸256×256。
- 所有方法在相同模拟器、观测、动作接口、训练配置下比较。

## 4. 资源与算力
- 文中说明：所有实验在 **4×A100 GPU** 上进行。
- **未明确**说明训练总时长、单次训练迭代数或收敛时间。

## 5. 实验数量与充分性
- **主要结果（表I）**：5个任务上对比5种方法（π0、π0(labeled)、π0.5、ProgressVLA、PAMAE(π0)、PAMAE(π0.5)），共6个变体，每个任务100 runs，统计可靠。
- **消融实验（表II）**：5个任务 × 5个变体（Base, -S1移除Stage1预热, -R移除路由对齐损失, -P移除阶段预测损失, Full），共25组条件。此外还分析了路由行为（平均主导专家运行长度8个动作块、阶段条件主导纯度PCP=89.0%）。
- **充分性评价**：实验覆盖了多种多阶段操作场景，消融设计完整，分离了预热、路由对齐、阶段预测的贡献。对比基线包括强baseline（π0.5）和控制变体（π0 labeled）。但**局限**在于：
  - 所有实验均在**模拟环境**中进行，未在真实机器人上验证。
  - 仅与有限的MoE风格VLA基线比较（引用了Himoe-VLA、DiTea等，但未直接对比数值）。
  - 阶段标签基于**手工规则**，可能不适用于所有复杂任务（如包含多个子阶段的循环任务）。

## 6. 主要结论与发现
1. **PAMAE显著提升任务成功率**：在π0骨架上平均成功率从73.8%提升至83.0%（+9.2%）；在π0.5骨架上从85.8%提升至91.4%（+5.6%）。
2. **阶段监督路由是关键**：消融中移除路由对齐损失 \( L_{\text{route}} \) 导致平均成功率下降6.8个百分点（从83.0%降至76.2%）。
3. **两阶段训练必不可少**：移除Stage1预热使平均成功率降至78.2%，说明过早引入辅助路由损失会破坏专家专业化。
4. **推理无额外开销**：PAMAE在推理时不需阶段标签，路由器自主产生时间一致且与执行阶段高度相关的专家分配（PCP=89.0%）。
5. **对比ProgressVLA表明**：粗粒度进度线索不足，阶段一致专家分配才是提升关键。

## 7. 优点
- **即插即用设计**：保留预训练VLA骨干，只需替换动作专家模块，兼容多种流匹配VLA模型。
- **无推理额外标签**：无需人工阶段标注或额外传感器，路由器仅依赖轻量轨迹信号。
- **两阶段训练稳定**：预热阶段防止路由崩溃，后期退火辅助损失平衡专家分离与动作质量。
- **路由行为可解释**：分析显示专家分配具有时间一致性和阶段相关性，有利于调试和理解。
- **计算开销可控**：参数从300M增至450M（基于π0骨干），使用4×A100即可训练。

## 8. 不足与局限
- **仅限模拟**：所有实验在仿真中进行，真实机器人上的泛化性、噪声鲁棒性、硬件延迟等未验证。
- **阶段标签依赖规则**：基于夹爪闭合和速度阈值的粗粒度三阶段划分，可能不适用于无夹爪或连续接触的任务；手工规则可能引入噪声，且对长时任务中多个子阶段的聚合并未精细处理。
- **对比基线不够全面**：未与更多MoE风格VLA方法（如Himoe-VLA、DiTea、DREAM等）在同一任务上直接数值比较，虽然文中引用但缺乏实验结果。
- **超参数设定固定**：专家数M=6、top-k=3，未探索影响。
- **推理效率未报告**：虽然参数增加，但未说明推理速度或实时性是否满足机器人控制频率要求。
- **无开源代码或模型权重**：仅提供arXiv预印本，可复现性受限。

（完）
