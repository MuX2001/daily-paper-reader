---
title: Robust Receding Horizon Games with Additive Uncertainty
title_zh: 带有加性不确定性的鲁棒滚动时域博弈
authors: "Dinesh Patra, Tanish Jain, Ashish R. Hota"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04213v1"
tags: ["query:control"]
score: 9.0
evidence: 鲁棒滚动时域博弈与模型预测控制
tldr: 针对多智能体系统在加性扰动、私有约束和共享耦合约束下的博弈控制问题，本文提出一种鲁棒滚动时域博弈框架。该框架结合管式约束收紧与有限时域广义纳什均衡问题，并采用基于离散代数Riccati方程的终端成本和解耦正不变终端集。理论证明对所提算法保证任意有界扰动下的递归可行性，且利用势博弈结构，每个智能体的名义状态渐近收敛到稳态变分广义纳什均衡，实际状态收敛到该均衡的最小鲁棒正不变集邻域。
source: arxiv
selection_source: fresh_fetch
motivation: 现有博弈控制方法难以同时处理加性扰动、私有约束和共享约束，需保证鲁棒递归可行性与收敛性。
method: 提出管式约束收紧结合有限时域GNEP框架，配备DARE终端成本和解耦正不变终端集。
result: 任意有界扰动下递归可行性；名义状态渐近收敛到稳态vGNE；实际状态收敛到其最小鲁棒正不变集邻域。
conclusion: 实现扰动下多智能体博弈控制的鲁棒可行与渐近收敛，拓展了滚动时域博弈理论。
---

## 摘要
我们研究了一个滚动时域博弈问题，其中多个智能体驱动受加性扰动、私有状态和输入约束以及共享耦合约束的线性系统。我们提出了一种鲁棒博弈论控制框架，该框架将基于管道的约束收紧与有限时域广义纳什均衡问题相结合，并配备了基于离散代数Riccati方程的终端代价和去耦的正不变终端集。该框架保证了对于每个有界扰动实现的递归可行性。利用由跟踪代价引发的势博弈结构，我们进一步建立了每个智能体名义状态向稳态变分广义纳什均衡的渐近收敛性，并展示了每个智能体的实际状态收敛到由最小鲁棒正不变集确定的vGNE的一个邻域。

## Abstract
We study a receding horizon game in which multiple agents drive linear systems subject to additive disturbances, private state and input constraints, and shared coupling constraints. We propose a robust game-theoretic control framework that combines tube-based constraint tightening with a finite-horizon generalized Nash equilibrium problem (GNEP), equipped with a discrete algebraic Riccati equation (DARE)-based terminal cost and a decoupled positively invariant terminal set. The framework guarantees recursive feasibility for every bounded disturbance realization. Exploiting the potential-game structure induced by tracking costs, we further establish asymptotic convergence of each agent's nominal state to a steady-state variational generalized Nash equilibrium (vGNE), and show that each agent's actual state converges to a neighborhood of the vGNE determined by the minimal robust positively invariant set.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现代工程系统（如智能电网、自动驾驶车队、工业机器人）中，多个自利智能体在共享不确定环境下协同与竞争，但现有集中式控制难以扩展，且分布式鲁棒MPC大多假设合作目标而非博弈均衡。同时，已有滚动时域博弈（RHG）工作未考虑加性扰动，缺乏鲁棒性保证。
- **核心问题**：如何在每个智能体拥有私有动力学、成本与约束，且存在有界加性扰动的情况下，设计分布式控制方案，使得多智能体系统在满足私有与共享约束的同时，渐近收敛到稳态变分广义纳什均衡（vGNE）的邻域，并保证任意扰动下的递归可行性。
- **整体含义**：首次将鲁棒管式MPC与滚动时域博弈结合，为存在不确定性下的多智能体非合作博弈提供了理论框架和收敛性保证，拓展了博弈控制理论的应用边界。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：采用刚性管（rigid tube）方法将实际状态分解为名义状态和误差状态，名义状态由确定性动力学驱动，误差状态被限制在最小鲁棒正不变集（mRPI）内。通过管式约束收紧，将原鲁棒约束转化为名义状态上的确定性约束，进而构建有限时域广义纳什均衡问题（GNEP），并结合势博弈结构和终端代价/集合实现递归可行性与收敛性。
- **关键技术细节**：
  - **管式分解**：\( x_{k|t}^i = s_{k|t}^i + e_{k|t}^i \)，\( u_{k|t}^i = v_{k|t}^i + K_p^i e_{k|t}^i \)，其中\( K_p^i \)为预稳定反馈增益，使得误差动力学矩阵\( \Phi_p^i = A_i + B_i K_p^i \)舒尔稳定。
  - **mRPI集**：\( Z_\infty^i = \bigoplus_{n=0}^\infty (\Phi_p^i)^n \mathcal{W}^i \)，保证误差始终在该集内。
  - **约束收紧**：私有约束\( \mathcal{S}^i = \mathcal{X}^i \ominus Z_\infty^i \)，\( \mathcal{V}^i = \mathcal{U}^i \ominus K_p^i Z_\infty^i \)；共享约束每行分别收紧：\( \bar{b}_i = b_i - \sum_{j\in\mathcal{N}_i} d_j \)，其中\( d_j \)为邻居的误差贡献支持函数向量。
  - **终端代价与终端集**：基于DARE求解\( P_i \)和最优反馈\( K_f^i \)，终端代价\( l_f^i(s_i) = \|s_i - s_i^*\|_{P_i}^2 \)；终端控制律\( \kappa_i(s_i) = v_i^* + K_f^i(s_i - s_i^*) \)；终端集\( \mathcal{S}_f^i \)为满足局部约束且正不变的最大集合，通过分配合约（每个邻居共享\( \alpha_j \)）实现去耦。
  - **在线GNEP**：每个时间步，各智能体并行求解有限时域优化问题（20），以跟踪稳态vGNE为目标，并使用分布算子分裂算法（如[24][29]）求解。实际施加输入为\( u_t^i = v_{t|t}^{*0} + K_p^i(x_t^i - s_{t|t}^{*0}) \)。
- **算法流程**（Algorithm 1）：
  - 离线：选择权重，计算mRPI，收紧约束，计算稳态vGNE，计算DARE解，计算终端集。
  - 在线：测量状态，求解GNEP，施加控制，迭代。

#### 3. 实验设计
- **数据集/场景**：论文为纯理论工作，未设计数值实验或仿真场景。
- **Benchmark**：未提及基准方法。
- **对比方法**：无实验对比，主要与先前确定性RHG（[5][6]）和分布式鲁棒MPC（[14]-[22]）在方法论上进行了比较，指出本工作在非线性、隐私性、均衡收敛性方面的差异。

#### 4. 资源与算力
- **未提及**：论文未描述任何GPU型号、数量、训练时长或计算资源消耗，因为无实验验证。

#### 5. 实验数量与充分性
- **无实验**：论文没有进行实验或仿真，因此无法评价实验充分性。所有结果均为理论证明（递归可行性、收敛性）。理论推导本身是严谨的，但缺乏数值验证可能削弱说服力。

#### 6. 主要结论与发现
- **递归可行性**：证明了若初始可行，则对于任意有界扰动序列，GNEP（20）在每一步都可行（Theorem 1）。
- **名义状态收敛**：利用势博弈性质，名义状态\( s_{t|t}^{*0} \)和名义输入\( v_{t|t}^{*0} \)渐近收敛到稳态vGNE（Theorem 2(ii)）。
- **实际状态收敛**：实际状态\( x_t^i \)收敛到\( \{s_i^*\} \oplus Z_\infty^i \)（最小RPI邻域）（Theorem 2(iii)）。
- **鲁棒约束满足**：原始约束对所有扰动实现始终满足（Remark 4）。
- **隐私保护**：智能体只广播标量收紧项，不泄露自身动力学、成本等私有信息，满足问题设置要求。

#### 7. 优点
- **理论创新**：首次将鲁棒管式MPC思想引入滚动时域博弈，处理加性扰动，同时保留博弈论均衡概念。
- **完备的终端设计**：通过分配合约实现共享约束的去耦正不变终端集，避免了分布式协调的复杂性，且保证递归可行性。
- **势博弈结构**：利用跟踪代价构造势函数，作为Lyapunov函数实现名义状态收敛证明，思路清晰。
- **隐私保护**：设计的协议仅交换标量，不暴露私有模型参数，符合实际应用需求。
- **理论证明严密**：给出了递归可行性和渐近收敛的完整数学证明，包括终端代价降低引理。

#### 8. 不足与局限
- **无实验验证**：论文缺乏仿真或实际平台验证，理论结果在实际问题中的有效性、计算效率、对假设违背后的鲁棒性未知。
- **假设较强**：要求每个智能体的成本为强凸且为势博弈（仅与自身变量有关），且图连通，稳态vGNE存在且唯一。实际非合作博弈中成本耦合或非凸情况无法处理。
- **保守性**：刚性管方法假设扰动有界且已知，误差完全由mRPI吸收，可能过于保守；未考虑概率约束或风险度量。
- **状态和输入约束**：私有约束被收紧后可能变紧，导致可行域缩小，影响性能。
- **隐私限制**：虽然智能体不共享模型，但仍需邻域内共享收紧后的聚合界\( \bar{b}_i \)，且需知道稳态vGNE中的邻居信息（通过广播），严格来说并非完全零知识。
- **未讨论通信和计算复杂度**：分布式求解GNEP的实时性、通信轮次未分析。
- **扩展局限性**：只考虑线性动力学和二次成本，一般经济成本或耦合成本的情况留待未来工作。

（完）
