---
title: L1 Optimal Control of Continuous-Time Stochastic Positive Systems
title_zh: 连续时间随机正系统的L1最优控制
authors: "Alba Gurpegui, Takashi Tanaka, Anders Rantzer"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03952v1"
tags: ["query:control"]
score: 9.0
evidence: 随机正系统的L1最优控制
tldr: 针对连续时间随机正系统，提出一类L1最优控制问题，包含线性非负成本和乘性伊藤扩散噪声。通过建立正象限的前向不变性，给出有限时域和无限时域问题的显式解，分别由向量值常微分方程和代数方程刻画。发现最优值函数和反馈策略与对应确定性系统一致，表明对乘性随机不确定性具有鲁棒性。组合投资示例验证了理论结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03952v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1111, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03952v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1248, \"height\": 293, \"label\": \"Figure\"}]"
motivation: 研究具有线性非负成本和乘性噪声的随机正系统的L1最优控制，并利用正象限不变性求解。
method: 利用正象限前向不变性，推导有限时域和无限时域问题的显式解，分别由向量ODE和代数方程表征。
result: 最优值函数和反馈策略与确定性情况相同，对乘性随机不确定性具有鲁棒性。
conclusion: 所提方法有效求解随机正系统L1最优控制，组合投资实例验证了鲁棒性。
---

## 摘要
我们提出了一类L1最优控制问题，其具有线性非负代价，受乘性Itô扩散过程约束，且输入约束为元素线性。对于所考虑的随机动力学，建立了正象限的前向不变性，并提出了一种与该不变性一致的模拟方法。考虑了有限时域和折扣无限时域随机L1最优控制问题。这些问题具有显式解，在有限时域情况下由向量值常微分方程表征，在无限时域情况下由代数方程表征。值得注意的是，最优值函数和反馈策略与相应确定性问题的相同，表明了对乘性随机不确定性的鲁棒性。一个投资组合示例说明了我们的结果。

## Abstract
We present an L1-optimal control problem class with linear nonnegative costs subject to multiplicative Itô diffusion processes with elementwise linear input constraints. Forward invariance of the positive orthant is established for the considered stochastic dynamics, and a simulation method consistent with this invariance property is proposed. Both finite-horizon and discounted infinite-horizon stochastic L1-optimal control problems are considered. These problems admit explicit solutions characterized by a vector-valued ordinary differential equation in the finite-horizon case and by an algebraic equation in the infinite-horizon case. Notably, the optimal value function and feedback policy coincide with those of the corresponding deterministic problem, demonstrating robustness to multiplicative stochastic uncertainty. A portfolio example illustrates our results.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）
- **研究动机**：现有L₁最优控制主要针对**确定性正系统**，而实际系统中常存在随机扰动（如金融市场、流行病传播等），且噪声往往是**乘性**的（与状态成比例）。然而，对于连续时间随机正系统，标准的L₂（如LQG）控制虽然成熟，但在存在激励约束和稀疏资源分配时，L₁范数更为合适（如最小燃料控制、最大无操作控制）。此外，随机系统可能因扩散项导致状态离开正象限，需要建立对应不变性。
- **核心问题**：针对一类由**乘性Itô扩散**描述、具有**元素线性输入约束**的连续时间随机正系统，研究L₁最优控制问题，包括**有限时域**和**折扣无限时域**两种情形，并希望得到显式解。
- **整体含义**：本文证明了在特定假设下，该随机最优控制问题的**最优值函数和反馈策略与对应的确定性L₁控制问题完全一致**，从而揭示了该问题对乘性随机不确定性的**鲁棒性**，为实际应用（如高频交易）提供了理论基础。

### 论文提出方法论
#### 核心思想
- 利用随机正系统的**正象限前向不变性**（Theorem 2），将状态约束转化为连续时间保证条件，进而通过**动态规划**（随机HJB方程）求解最优控制问题。
- 由于线性代价和线性动力学，值函数假设为线性形式 \(J(t,x)=p(t)^\top x\)，使得HJB方程中的二阶项（来自扩散）自动为零，从而将随机问题退化为确定性ODE或代数方程，这正是显式解的关键。

#### 关键技术细节
1. **系统模型**（式(1)）：
   \[
   dx(t) = \left( Ax(t) + \sum_{m=1}^M B_m u_m(t) \right) dt + \sum_{n=1}^N F_n x(t) dw_n(t)
   \]
   其中 \(F_n\) 为对角矩阵（乘性噪声），输入约束 \(|u_m(t)| \le e_m x(t)\)（元素线性），且需满足 \(A - \sum_m |B_m| e_m\) 为Metzler矩阵以保证漂移项的正性。

2. **正象限不变性证明**（Theorem 2）：通过引入变换 \(y_i(t) = x_i(t) e^{-f_i v_i(t)}\) 将原SDE转化为一个漂移项具有非负非对角元素的线性ODE（利用Itô公式），再结合确定性正系统性质（Lemma 1）证明。

3. **有限时域最优控制**（Theorem 3）：
   - 值函数 \(J(t,x)=p(t)^\top x\)，其中 \(p(t)\) 满足逆向ODE：
     \[
     -\dot p(t) = q + A^\top p(t) - \sum_{m=1}^M e_m |r_m + B_m^\top p(t)|, \quad p(T)=0
     \]
   - 最优控制为**开关策略**（bang-bang）：
     \[
     u_m^*(t) = -K_m(t)x(t), \quad K_m(t) \in \operatorname{diag}\left( \operatorname{sign}(r_m^\top + p(t)^\top B_m) \right) e_m
     \]
   - 需满足条件 \(q > \sum_m |r_m| e_m\) 保证代价有下界。

4. **折扣无限时域最优控制**（Theorem 4）：
   - 值函数 \(J(x)=p^\top x\)，其中 \(p\) 满足代数方程：
     \[
     \beta p = q + A^\top p - \sum_{m=1}^M e_m |r_m + B_m^\top p|
     \]
   - 最优控制为静态反馈开关策略（式(18)），需验证横截条件 \(\lim_{T\to\infty} E[e^{-\beta T} p^\top x(T)]=0\)（通过谱条件 \(\beta > \alpha(\bar A)\) 或系统可镇定保证）。

5. **保持正性的模拟算法**（Algorithm 1）：基于变换 \(y_i = x_i e^{-f_i v_i}\)，通过离散化\(y\)的线性ODE（矩阵指数）保持正性，避免Euler–Maruyama出现负值的缺点。算法具体步骤：输入参数，生成布朗运动增量，更新\(v_i\)，构建\(\tilde A\)矩阵，计算\(y(t_{k+1})=\exp(\tilde A \Delta t) y(t_k)\)，最后恢复\(x\)。

### 实验设计
- **实验场景**：高频交易投资组合分配问题。状态 \(x(t) \in \mathbb{R}^{50}_{+}\) 表示50支科技股的资本分配，动力学模型为式(1)，控制输入 \(u_m(t)\) 表示对个股的加/减仓速率，约束 \(|u_m(t)|\le e_m x(t)\) 体现流动性限制。
- **数据集/参数**：随机生成参数（满足假设条件）：对角线\(A\)元素在 \([-5\times10^{-4},5\times10^{-4}]\)，非对角线在 \([0,2\times10^{-4}]\)；控制增益\(b_m\)在 \([0.6,1.7]\)；乘性噪声\(F_1,F_2\)对角元素分别在 \([0.02,0.05]\)和\([0.02,0.08]\)；初始分配 \(x_0\)分量在 \([0,10]\)；成本参数\(q\)在 \([2\times10^{-4},10^{-3}]\)，\(r_m\)在 \([0.01,0.02]\)。
- **Benchmark与对比方法**：仅对比了**被动策略**（\(u=0\)）和**最优开关控制**。无其他随机控制方法（如LQG）或基线对比。
- **结果展示**：给出了单次随机参数下的状态轨迹图（左：被动；中：最优）和开关信号图（右）。最优值 \(J \approx 5.50\)，被动策略值为4.31，说明动态开关显著提高收益。

### 资源与算力
- **未明确说明**：论文未提及任何GPU型号、数量或训练时长。所有数值实验仅涉及ODE求解和单次仿真，计算量很小，可能在普通计算机上用MATLAB或Python完成。

### 实验数量与充分性
- **实验数量**：仅**一个示例**（单一参数随机生成），未进行统计重复或参数敏感性分析。无消融实验（如不同噪声强度、不同约束上界、不同维度的对比）。
- **充分性评价**：**不够充分**。虽然理论证明完整，但数值验证偏简单，缺乏与现有方法的对比（如LQG、确定性L₁控制的随机化版本），也未讨论高维或长时间步长的模拟稳定性。结论的泛化性仅靠逻辑推理。

### 论文的主要结论与发现
1. 对于满足Assumption 1的随机正系统，在乘性Itô扩散下**正象限几乎必然不变**（Theorem 2）。
2. 有限时域与折扣无限时域的L₁最优控制问题均存在**显式解**，分别归结为求解ODE（9）或代数方程（17），最优控制为**门限开关策略**。
3. **最优值函数和反馈策略与对应确定性L₁问题完全相同**，不受噪声强度影响，体现了对乘性随机不确定性的**鲁棒性**（Remark 4）。
4. 高频交易示例验证了开关控制优于被动策略，且值函数与噪声强度无关（\(p\)不依赖\(F_n\)）。

### 优点
- **理论深度**：首次给出随机正系统L₁最优控制的完整HJB求解框架，并证明了解的存在性与显式结构。
- **鲁棒性揭示**：发现最优解与确定性解一致，为实际应用提供简单可靠的控制器（无需估计噪声参数）。
- **数值方法**：提出保持正性的模拟算法（Algorithm 1），克服了标准Euler–Maruyama会违反正性的问题，具有实用价值。
- **应用场景**：高频交易示例展示了方法在金融领域的潜力。

### 不足与局限
- **实验验证薄弱**：仅一个示例，缺乏系统性的数值对比（如与LQG、模型预测控制或随机逼近方法的比较），也未提供统计结果（均值、方差、失败率）。
- **假设限制较强**：要求\(F_n\)为对角矩阵、\(A-\sum|B_m|e_m\)为Metzler、输入约束与状态成比例（\(u \le e_m x\)）。这些假设在一般应用中可能难以满足（如非对角噪声、非比例约束）。
- **随机稳定性未深入讨论**：对于无限时域问题，仅给出充分条件（谱条件或可镇定），但未提供具体构造或验证步骤，也未与其他稳定性概念（如矩稳定性、几乎必然稳定性）关联。
- **高昂代价的隐式假设**：线性值函数假设依赖于HJB二阶项为零，这仅对特定线性–线性结构成立，推广到其他随机系统需谨慎。
- **应用局限**：示例中参数的选取为了使开关现象可见，未讨论参数偏离假设（如非Metzler矩阵）时的行为或鲁棒性边界。

（完）
