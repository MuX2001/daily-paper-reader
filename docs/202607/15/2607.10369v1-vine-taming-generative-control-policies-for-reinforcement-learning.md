---
title: "VINE: Taming Generative Control Policies for Reinforcement Learning"
title_zh: VINE：驯服强化学习的生成控制策略
authors: "Rushuai Yang, Zhuo Han, Houlin Li, Hecheng Wang, Zhichao Wu, Rui Zhang, Zhaowei Zhang, Zihong Chen, Xiaohan Yan, Chiming Liu, Yi Chen, Wei Shan, Maoqing Yao"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10369v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 流匹配策略用于机器人强化学习
tldr: 流匹配策略在机器人学习中因值梯度强化学习出现训练不稳定，传统观点归因于迭代生成，但本文发现根源在于行为克隆的采样策略。提出VINE方法，在每个去噪步骤重建插值状态，构造稳定的可微路径，实现端到端优化。在OGBench和真实机器人操作任务上，VINE稳定提升策略并超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10369v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1414, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 778, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1436, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1300, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1284, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10369v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1389, \"height\": 1347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10369v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1465, \"height\": 427, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1433, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 653, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1213, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10369v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1544, \"height\": 1685, \"label\": \"Table\"}]"
motivation: 流匹配策略在值梯度强化学习下训练不稳定，现有方法牺牲迭代生成或端到端优化，需探究真正原因并解决。
method: VINE在每个去噪步骤重建新插值状态而非单一流轨迹，形成稳定可微路径，兼容原始去噪过程并支持端到端值梯度传播。
result: VINE在OGBench离线RL基准和真实机器人操作任务上，稳定提升策略并持续超越SOTA强化学习方法。
conclusion: 通过改进采样策略而非放弃迭代生成，VINE实现了流匹配策略在值梯度RL下的稳定端到端优化。
---

## 摘要
流匹配策略已成为机器人学习中的一种有效策略参数化方法。它通过从噪声中迭代生成动作，实现了对复杂和多模态动作分布的高度表达性建模。然而，先前的研究观察到，使用值梯度强化学习（RL）对这些策略进行扩展往往会导致训练不稳定。现有方法将这种不稳定性归因于迭代生成，因此通过牺牲迭代生成、高表达性或值梯度优化来避免端到端的值梯度优化。与先前观点相反，我们发现不稳定性并非源于迭代生成本身，而是源于最初为行为克隆设计的朴素采样策略，该策略在值梯度强化学习下变得脆弱。基于这一洞察，我们提出了VINE，一种面向强化学习的采样方法，能够对流匹配策略实现稳定的端到端值梯度优化。VINE不遵循单一的流轨迹，而是在每个去噪步骤中重构一个新的插值状态，为值梯度传播创建了一条稳定的可微路径，同时保持与原始流匹配去噪过程的兼容性。因此，VINE保留了流匹配的表达性和迭代生成，而无需牺牲端到端的值梯度优化。尽管通过所有十个去噪步骤进行端到端反向传播，VINE仍能实现稳定的策略改进，并在OGBench离线强化学习基准和真实世界机器人操作任务中始终优于最先进的强化学习方法。视频可在我们的网站上获取：https://agibottech.github.io/vine。

## Abstract
Flow-matching policies have emerged as an effective policy parameterization for robot learning. They iteratively generate actions from noise, enabling highly expressive modeling of complex and multimodal action distributions. However, prior works observed that scaling these policies with value-gradient reinforcement learning (RL) often leads to training instability. Existing methods attribute this instability to iterative generation and therefore avoid end-to-end value-gradient optimization by sacrificing iterative generation, high expressiveness, or value-gradient optimization. Contrary to prior belief, we show the instability does not stem from iterative generation itself, but from the vanilla sampling strategy originally designed for behavior cloning, which becomes brittle under value-gradient RL. Motivated by this insight, we propose VINE, an RL-oriented sampling method that enables stable end-to-end value-gradient optimization for flow-matching policies. Instead of following a single flow trajectory, VINE reconstructs a new interpolation state at every denoising step, creating a stable differentiable path for value-gradient propagation while remaining compatible with the original flow-matching denoising process. As a result, VINE preserves the expressiveness and iterative generation of flow-matching without sacrificing end-to-end value-gradient optimization. Despite performing end-to-end backpropagation through all ten denoising steps, VINE achieves stable policy improvement and consistently outperforms state-of-the-art RL methods on the OGBench offline RL benchmark and real-world robotic manipulation task. Videos are available on our website: https://agibottech.github.io/vine.

---

## 论文详细总结（自动生成）

# 论文 VINE：驯服强化学习的生成控制策略 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- 流匹配（Flow-matching）策略在机器人学习中被广泛采用，因其能通过多步去噪过程从噪声迭代生成动作，建模复杂、多模态的动作分布，表达能力极强。
- 为了优化回报，研究者尝试用值梯度强化学习（Value-gradient RL）微调这类策略，但普遍遭遇训练不稳定问题。
- 现有方法将不稳定归因于**迭代生成过程本身**，因此采用三种牺牲性策略：①冻结生成策略，用外部引导信号绕开反向传播；②将多步生成压缩为单步或使用高斯策略；③弃用值函数的动作梯度，仅用标量值加权。这些方法虽提升稳定，但损失了表达性、迭代生成能力或值梯度优化的效率。
- **本文核心发现**：不稳定性并非源于迭代生成，而是源于**最初为行为克隆设计的采样策略**。行为克隆的采样器追求重构固定分布，而RL要求采样器能平滑地在不同高价值模式间移动，原采样策略无法适应这种动态需求，导致梯度传播不稳定。
- **整体意义**：重新设计了面向RL的采样方法 VINE，在不牺牲迭代生成和端到端值梯度优化的情况下，稳定微调流匹配策略，在多个基准和真实机器人任务上达到最优。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

### 2.1 核心思想

- VINE 不再沿单一噪声轨迹进行去噪，而是在每个去噪步骤**重构一个新的插值状态**，该状态由当前动作估计加上独立采样的高斯噪声组成，然后使用预训练的速度场预测下一步的动作估计。
- 这样构建的路径具有稳定的可微性，便于值梯度通过所有去噪步骤进行反向传播（BPTT），同时每个网络查询的输入（插值状态）与流匹配训练时的语义完全一致，因此无需修改预训练模型或训练目标。

### 2.2 关键技术细节

- **状态重构**：在步骤 \(k\)，给定当前动作估计 \(\hat{a}_k\)，采样独立噪声 \(z_k \sim \mathcal{N}(0,I)\)，构造插值状态：
  \[
  \hat{x}_k = t_k \hat{a}_k + (1 - t_k) z_k
  \]
  其中 \(t_k = k/K\) 是单调递增的时间步。
- **动作估计更新**：使用速度场 \(v_\theta\) 预测下一个动作估计：
  \[
  \hat{a}_{k+1} = \hat{x}_k + (1 - t_k) v_\theta(\hat{x}_k, t_k; s)
  \]
- **随机性逐级减小**：因为 \((1 - t_k)\) 随 \(k\) 递减，前期噪声权重较大，后期接近确定性细化，实现从粗探索到精细调整的过渡。

### 2.3 算法流程（文字说明）

1. 初始化：\(\hat{a}_0 \sim \mathcal{N}(0,I)\)。
2. 对于 \(k = 0\) 到 \(K-1\)：
   - 设定 \(t_k = k/K\)。
   - 使用标准欧拉步骤得到 \(x_k\)（实际算法中此步被替换，但论文描述为：先用速度场更新得到 \(x_k\)，但 VINE 实际上用 \(x_k\) 仅作为中间变量，然后采样新噪声 \(z_k\)，构造 \(\hat{x}_k\)，最后用公式更新 \(\hat{a}_{k+1}\)）。
   - 注意：算法伪代码中第5行“xk ← xk + (1/K) vθ(xk, tk; s)”是欧拉方法，但紧接着第6-8行用新噪声构造ˆxk并重新计算ˆak+1，因此实际采样路径不同于欧拉。
3. 输出最终动作 \(\hat{a}_K\)。
4. 训练时，将 \(\hat{a}_K\) 代入 actor 损失 \(-\mathbb{E}[Q_\phi(s,\hat{a}_K)] + \alpha \|\hat{a}_K - a\|^2\)，并通过 BPTT 计算梯度，更新速度网络参数 \(\theta\)。

- **兼容性保证**：定理1证明了最优速度场下的端点预测等于条件期望 \(\mathbb{E}[a|\hat{x}_k, t_k, s]\)，与状态来源无关，因此预训练流匹配策略可以直接用于 VINE 采样和微调。
- **伪代码**：见 Algorithm 1（论文第5页）。

## 3. 实验设计：数据集 / 场景、基准、对比方法

### 3.1 离线 RL 实验

- **基准**：OGBench，包含 8 个域，每个域 5 个任务，共 40 个任务。
  - 域：antmaze-large, antmaze-giant, humanoidmaze-medium, humanoidmaze-large, scene-sparse, puzzle-3x3-sparse, cube-double, cube-triple。
  - 测试长时导航和稀疏奖励操纵任务。
- **对比方法**：分为四类：
  - 高斯策略：ReBRAC
  - Around the Chain（避免完整 BPTT）：FQL, FAWAC, IFQL, QAM, DAC, QSM, CGQL 变体, DSRL, FEdit, FBRAC, BAM
  - Outside the Chain：FEdit, FBRAC 等（论文中分类略有重叠，但主要区分了是否通过完整采样链传播梯度）
  - Through the Chain（直接 BPTT）：VINE 属于此类。
- **评估协议**：每个任务 12 个随机种子，训练 1M 梯度步，每 20k 步用 50 个 episode 评估，报告成功率及95%置信区间（bootstrap 5000样本）。

### 3.2 在线 RL 实验

- **场景**：真实世界插座插入任务（Plug Insertion），机器人需抓取插头并插入固定插座，要求精密接触控制。
- **对比方法**：
  - SAC-Flow（流匹配 + 直接 BPTT，不稳定）
  - Hil-SERL（人类协作 RL）
  - DSRL（潜在空间强化学习）
  - RLT（使用 VLA 模型）
  - EXPO（稳定表达性策略）
  - VINE（从零训练，无预训练初始化）
- **评估指标**：20 次试验的成功率、墙钟微调时间、需要人类干预的 rollout 比例。

## 4. 资源与算力

- 论文**未明确说明**具体使用的 GPU 型号、数量及训练时长。
- 仅提及离线训练使用 1M 梯度步，在线训练约 20 分钟，但未提供硬件细节（如单卡/多卡，T4/V100/A100等）。
- 因此无法精确评估算力成本。

## 5. 实验数量与充分性

- **大规模离线实验**：40 个任务 × 12 个种子 = 480 个独立的训练运行，结果具有统计显著性（报告置信区间）。
- **消融与分析**：
  - 采样策略消融：VINE vs. 标准欧拉采样器，并附梯度范数曲线对比，证明 VINE 梯度稳定。
  - 迭代步骤数消融：K=1,4,6,8,10，验证更多去噪步提高动作质量。
  - 定理证明与理论分析（高斯分布下的确定性 VINE 收敛性）。
- **真实世界实验**：一个任务 20 次试验，对比多种基线，结果可靠。
- **充分性评价**：实验覆盖多种难度的域（导航、操纵），并在真实机器人上验证，消融实验直接支撑核心观点。对比方法全面（包括近期 SOTA），实验设计客观、公平。

## 6. 论文的主要结论与发现

- VINE 实现了**稳定的端到端值梯度优化**，无需牺牲迭代生成、表达性或值梯度。在 40 个 OGBench 任务上平均成功率为 60%，远超所有对比方法（第二名 55%）。
- 在真实世界插座插入任务中，VINE 达到 20/20 成功率（100%），训练仅需 20 分钟，人类干预率仅 19.2%，优于所有基线（SAC-Flow 成功率 60%，干预率 74.6%）。
- 不稳定性根源被澄清：不是迭代生成，而是行为克隆的采样策略（单轨迹、无逐级噪声注入）在值梯度下失效。
- **理论贡献**：证明了 VINE 的采样过程与预训练流匹配策略的兼容性，以及在高斯假设下确定性 VINE 的动作估计指数收敛到均值。
- VINE 在后续步骤中进行精细化修正，使迭代生成仍然关键（少步数策略失败）。

## 7. 优点：方法或实验设计上的亮点

- **问题洞察深刻**：找出真正的不稳定根源，而非归咎于迭代生成，解决了误区。
- **方法简洁有效**：仅修改采样器，无需额外的指导模型、目标修改或网络结构改动，可即插即用于预训练流匹配策略。
- **理论支撑**：定理1保证贝叶斯最优性，推论1证明收敛性，增强可信度。
- **实验全面且客观**：大规模离线RL统计严谨，真实世界验证有定量指标，消融实验直接验证核心假设（采样方式、迭代深度）。
- **对比方法覆盖充分**：包括高斯、各类约束策略、近期 SOTA 方法，分类清晰，结果具有说服力。
- **开源与可复现**：提供伪代码，承诺网站有视频。

## 8. 不足与局限

- **算力成本未公开**：无法评估训练资源需求，影响实用性判断。
- **真实世界实验任务单一**：仅一个插座插入任务，泛化性受限（如多任务、灵巧手、长时域）。
- **假设值函数准确**：VINE 依赖 critic 提供准确梯度，若 critic 本身训练不稳定（尤其在离线数据分布外），仍可能失效。
- **超参数敏感**：BC 系数 α 需针对不同任务调整（附录表4给出具体值），缺乏自适应机制。
- **主要验证了流匹配**：虽提及可推广到扩散模型，但无相应实验佐证。
- **长期在线 RL 稳定性未充分探讨**：仅 20 分钟在线微调，更长训练或更复杂任务下的稳定性未知。
- **对比方法中未包含最新的端到端 BPTT 类方法**：仅 SAC-Flow 对等，其他方法多数避免 BPTT，但 VINE 的优势关键正在于 BPTT 稳定性。

（完）
