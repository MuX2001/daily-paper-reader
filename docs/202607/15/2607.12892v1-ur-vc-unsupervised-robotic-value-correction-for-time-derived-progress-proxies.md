---
title: "UR-VC: Unsupervised Robotic Value Correction for Time-Derived Progress Proxies"
title_zh: UR-VC：面向时间衍生进度代理的无监督机器人价值纠正
authors: "Lirui Zhao, Modi Shi, Li Chen, Qi Liu, Ping Luo, Hongyang Li"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12892v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 无监督修正时间衍生的进度代理以提升机器人学习
tldr: 机器人学习常使用时间导出标签作为进度信号，但接触丰富的操作中该标签无法反映进度倒退等局部变化。UR-VC是一种无监督、免训练的方法，通过跨片段检索相似状态并聚合其时间标签来校正进度。在真实双臂布料折叠任务中，校正标签能准确捕捉局部倒退和非均匀进度，用于VLA训练后展现出正向的任务成功率趋势。该方法无需额外标注或模型训练，有效提升了基于时间代理的机器人学习质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12892v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 798, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12892v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 889, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12892v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1714, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12892v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 887, \"height\": 522, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12892v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 563, \"height\": 344, \"label\": \"Table\"}]"
motivation: 时间导出进度标签在接触丰富的机器人操作中噪声大，无法反映实际进度倒退，影响策略学习效果。
method: 无监督校正方法，通过跨片段检索相似状态并聚合其时间标签，得到更准确的进度估计，无需训练或额外标签。
result: 在真实双臂布料折叠任务上，校正标签能捕捉局部进度倒退和非均匀进度，用于VLA训练后任务成功率呈现正向趋势。
conclusion: UR-VC提供了一种简单有效的离线进度校正方法，提升基于时间代理的机器人学习质量。
---

## 摘要
现代机器人学习系统越来越依赖密集的进度或价值信号来评估中间状态、指导策略学习和检测任务完成，使得这些信号的质量至关重要。由于这种密集标签很少能大规模获得，演示中的归一化时间常被用作可扩展的替代：后期帧被视为更高进度。然而，这种时间衍生标签只是物理任务进度的有噪声代理。在接触丰富的操作中，机器人可能取得进展，然后通过滑动、抓取失败或部分撤销而丢失进展，而时间衍生标签继续单调增加。我们提出了无监督机器人价值纠正（UR-VC），一种离线、无需训练的方法，用于纠正时间衍生进度标签。UR-VC利用了演示数据中的简单规律：相似状态常在不同情节中重复出现，但时间戳不同。UR-VC不信任单个轨迹的时间戳，而是从其他情节中检索相似状态并聚合它们的时间衍生标签，以获得纠正后的进度估计。UR-VC不需要手动进度标签、奖励注释或额外的价值模型。我们在真实的双臂布匹展开与折叠数据上评估UR-VC，这是一个具有可见中间进度的长时域可变形物体操作任务。纠正后的标签捕捉了归一化时间无法表示的局部回归和非均匀进度，同时保留了整体任务趋势。我们进一步使用纠正后的信号构建用于VLA训练的优劣标签，遵循最近的优劣条件策略学习。在匹配的数据、模型和训练设置下，UR-VC在真实机器人任务成功率上显示出积极趋势。

## Abstract
Modern robot learning systems increasingly rely on dense progress or value signals to evaluate intermediate states, guide policy learning, and detect task completion, making the quality of these signals critical. Since such dense labels are rarely available at scale, normalized time within a demonstration is often used as a scalable substitute: later frames are treated as higher progress. However, this time-derived label is only a noisy proxy for physical task progress. In contact-rich manipulation, a robot may make progress and then lose it through slips, failed grasps, or partial undoing, while the time-derived label continues to increase monotonically. We introduce Unsupervised Robotic Value Correction (UR-VC), an offline, training-free method for correcting time-derived progress labels. UR-VC exploits a simple regularity in demonstration data: similar states often recur across different episodes, but at different timestamps. Instead of trusting the timestamp from a single trajectory, UR-VC retrieves similar states from other episodes and aggregates their time-derived labels to obtain a corrected progress estimate. UR-VC requires no manual progress labels, reward annotations, or additional value model. We evaluate UR-VC on real bimanual cloth flatten-and-fold data, a long-horizon deformable-object manipulation task with visible intermediate progress. The corrected labels capture local regressions and non-uniform progress that normalized time cannot represent, while preserving the overall task trend. We further use the corrected signal to construct advantage labels for VLA training, following recent advantage-conditioned policy learning. UR-VC shows a positive trend in real-robot task success under matched data, model, and training settings.

---

## 论文详细总结（自动生成）

# UR-VC：面向时间衍生进度代理的无监督机器人价值纠正

## 1. 核心问题与整体含义（研究动机与背景）
- **背景**：现代机器人学习系统（特别是VLA模型）依赖密集的进度或价值信号来评估中间状态、指导策略学习、检测任务完成。由于真实进度标签难以大规模获取，常采用**归一化时间**作为可扩展的替代代理（即将演示中更晚的帧视为更高进度）。
- **问题**：时间衍生标签是物理任务进度的一种有噪声的代理。在接触丰富的操作中（如布料展开与折叠），机器人可能取得进展后又因滑动、抓取失败或部分撤销而**倒退**，但时间标签仍单调递增；此外，不同演示的执行速度、恢复动作不同，导致相同物理状态在不同片段中获得不同的时间标签。这种噪声会误导下游策略：线性时间代理无法区分推进动作与破坏动作，模型可能学习到与进度无关的线索（如外观、纹理、执行速度）。
- **核心动机**：能否在监督信号本身被使用之前，对时间衍生代理标签进行**无监督校正**，使其更接近真实物理进度，从而提升下游学习质量？

## 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：利用演示数据中的固有规律——相似物理状态常在不同片段中重复出现，但时间戳不同。通过跨片段检索并聚合这些相似状态的时间标签，可以降低单一片段的时序噪声，获得更可靠的进度估计。
- **关键技术细节**：
  1. **视觉编码**：对每个帧使用SigLIP-2提取L2归一化视觉嵌入。
  2. **跨片段匹配**：对查询帧 i，从每一个其他片段 e 中，在时间带 τ 内（|g_j - g_i| ≤ τ）寻找候选帧，选择余弦相似度最高的代表帧j*_i,e，若相似度低于阈值ρ则丢弃。
  3. **片段平衡聚合**：每个片段最多贡献一个代表帧，最终对收集到的代表帧的原始时间标签 g_j 取平均，得到校正后估计 ĝ_i。若没有符合条件代表，则保留原始g_i。
  4. **优势标签构造**：使用校正后进度 ĝ 计算固定视野 H 的优势：r_i = ĝ_{i+H} - ĝ_i。在片段末尾使用差值缩放。将排名前20%的帧标记为正优势（文本提示后缀如“advantage: positive”）。
- **公式**：  
  ĝ_i = (1/|M_i|) * Σ_{e∈M_i} g_{j*_{i,e}}，其中 M_i 为有效代表集合。
- **算法流程**：离线计算所有帧的嵌入 → 对所有帧执行跨片段检索 → 选代表 → 聚合 → 输出校正后进度标签 → 可选转换为优势标签用于条件策略训练。无需训练、无需手动标签、无需奖励注释。

## 3. 实验设计
- **任务与场景**：真实双臂布料**展开与折叠**（flatten-and-fold）任务，长时域可变形物体操作。
- **数据集**：主评估集为150个真实机器人演示片段；部分分析扩展至10^4片段规模（源自文献[27]）。
- **Benchmark**：无标准公开基准，论文内部对比**基线**：不使用优势标签（no-advantage）的VLA训练，以及使用原始时间标签（单调）的隐含对比。
- **下游对比方法**：除无优势基线外，无其他基于标签校正的方法进行比较。重点是与原始时间代理（单调）的效果对比（通过校正前后标签统计差异）。
- **评价指标**：覆盖率（有跨片段匹配的帧比例）、语义一致性（检索到的帧是否对应相同任务状态）、非单调恢复（校正后出现负优势的比例）、下游真实机器人任务成功率（6种桌面条件，各30试次，共180试次）。

## 4. 资源与算力
- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长等硬件资源。仅提及使用了SigLIP-2视觉编码器（预训练模型），以及π0.5作为VLA骨干。校正过程本身无需训练，仅需计算嵌入和相似度检索，算力需求较低。下游VLA训练的具体计算资源未披露。

## 5. 实验数量与充分性
- **实验数量**：
  - 覆盖率分析：随检索集规模（50→10^4）的变化。
  - 语义一致性：定性示例（图3）。
  - 非单调恢复：统计主评估集13.4%帧获得负优势（horizon=5%时间）。
  - 下游评估：6种桌面条件×30试次=180试次/方法，共2种方法（基线、UR-VC）。
- **充分性评估**：
  - **优点**：覆盖了方法所需的前提条件验证（覆盖率、语义一致性、非单调性），并提供了下游真实机器人验证，控制变量较好（相同数据、模型、训练配置）。
  - **不足**：
    - 仅在一个任务（布料折叠）上评估，任务类型单一（可变形物体、接触丰富）。
    - 仅与无优势基线对比，未与其他校正方法（如学习型进度估计器、对比学习）对比。
    - 缺乏对超参数（τ、ρ、horizon、top%优势）的系统消融实验。
    - 下游评估每种条件30试次，样本量中等，但未报告置信区间或统计显著性检验。
  - **客观性**：实验设置清晰，但结论推广性有限。论文也承认了局限性。

## 6. 主要结论与发现
- UR-VC能有效校正时间衍进度代理：覆盖率在主评估集（150片段）已达98%（相似度≥0.90）；校正后标签可捕捉局部倒退（13.4%帧获得负优势），同时保持与时间标签高度相关（0.98）。
- 随着检索集规模增大（至10^4），校正估计的粗糙度下降约三分之一，覆盖率升至99.9%。
- 下游优势条件VLA训练中，UR-VC校正标签使平均成功率从72.8%提升至78.9%，在5/6桌面条件下优于基线，表明校正信号具有实际效用。

## 7. 优点
- **无监督、免训练**：无需手动进度标签、奖励注释或额外价值模型，仅基于现有演示数据离线处理。
- **简单高效**：只需嵌入计算和近邻检索，计算开销小，易于并入现有流程。
- **通用性**：可作为任意下游策略的预处理步骤，不改变模型架构或训练目标。
- **实证充分**：验证了跨片段冗余的存在、语义匹配的有效性，以及下游真实机器人的正向趋势。
- **动机清晰**：明确指出了时间代理的局限性，并针对性地提出解决方案。

## 8. 不足与局限
- **假设依赖**：视觉相似性假设等同于任务进度相似性，当视觉外观相近但实际进度不同时（如不同阶段但外观类似的状态）可能出错；算法无法纠正系统性的检索误差。
- **任务单一**：仅在布料展开折叠任务上验证，缺乏对刚体操作、长时域多阶段任务等的泛化证据。
- **对比不充分**：未与学习型进度估计器（如对比学习、value pre-training）或基于模态融合的方法比较，基线仅是无优势设置，未能说明相对于其他标签校正方法的优劣。
- **超参数敏感性未分析**：τ、ρ、优势选取比例等超参数对性能的影响未被消融。
- **评价指标有限**：下游成功率提升幅度（约6%），但未报告统计显著性，且30试次/条件存在随机性。
- **未探讨失败案例**：未分析UR-VC校正失败的情况（如检索到错误匹配导致标签变差）。

（完）
