---
title: "D-SafeMPC: Diffusion-Driven Safe Model Predictive Control with Discrete-Time Control Barrier Functions"
title_zh: D-SafeMPC：基于扩散的安全模型预测控制与离散时间控制障碍函数
authors: "Erdi Sayar, Ersin Daş, Joel W. Burdick, Alois Knoll, Erdal Kayacan"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10842v1"
tags: ["query:control"]
score: 9.0
evidence: 扩散驱动的安全模型预测控制与控制障碍函数
tldr: 扩散模型在机器人规划中难以保证安全和动力学可行性，现有混合MPC方法因初始轨迹不佳导致不稳定。本文提出D-SafeMPC，在反向扩散过程中利用控制障碍函数(CBF)和控制李雅普诺夫函数(CLF)引导采样，并通过迭代投影使MPC逐步细化轨迹。在Franka机械臂的静态和动态障碍场景中，该方法显著提升安全性和任务成功率，同时提高规划效率。主要贡献在于将安全约束直接嵌入扩散过程，实现了稳定高效的规划。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1779, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10842v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 898, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10842v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 804, \"height\": 957, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10842v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 490, \"label\": \"Table\"}]"
motivation: 扩散模型生成轨迹常违反安全或动力学约束，与MPC结合时因初始质量差导致收敛不稳定。
method: 在扩散反向过程中引入CBF/CLF约束引导采样，并迭代使用MPC优化每步去噪轨迹，为MPC提供可靠热启动。
result: 在Franka机械臂仿真和实物实验中，D-SafeMPC相比基线方法提升了安全率、任务成功率和规划效率。
conclusion: 通过CBF/CLF引导扩散过程，D-SafeMPC有效融合安全约束与生成式规划，提升了可行性与鲁棒性。
---

## 摘要
扩散模型在机器人规划中的一个关键局限性是它们无法固有地强制执行安全性或动力学约束，这常常导致物理上不可行或不安全的输出。采用模型预测控制（MPC）来解决这一问题的混合方法可能不稳定，因为扩散模型提供的较差轨迹初始化会阻止MPC收敛到安全可行的解。为克服这些挑战，我们提出了D-SafeMPC，它增强了扩散与控制之间的交互。我们的方法利用控制障碍函数（CBFs）和控制李雅普诺夫函数（CLFs）引导逆向扩散过程，并采用迭代投影方案，在每个去噪步骤中MPC优化轨迹。这将采样引导向安全、目标导向的区域，并提供可靠的MPC热启动。在Franka机械臂的四个场景（一个静态障碍物和三个动态障碍物设置）的仿真中，以及在实体Franka机器人的仿真到真实实验中，D-SafeMPC在安全性、任务成功率和规划效率方面均优于最先进的基线方法。为促进可重复性，我们的源代码和实验配置可在存储库https://github.com/erdiphd/D-SafeMPC中获取。

## Abstract
A key limitation on the use of diffusion models in robotic planning is their inability to inherently enforce safety or dynamical constraints, which often results in physically infeasible or unsafe outputs. Hybrid approaches that employ model predictive control (MPC) to address this problem can be unstable, as poor trajectory initializations from the diffusion model prevent the MPC from converging to a safe and feasible solution. To overcome these challenges, we propose D-SafeMPC, which enhances the interaction between diffusion and control. Our method guides the reverse diffusion process with control barrier functions (CBFs) and control Lyapunov functions (CLFs) and employs an iterative-projection scheme where an MPC refines the trajectory at each denoising step. This steers sampling toward safe, goal-directed regions and provides reliable MPC warm starts. In simulations on a Franka manipulator across four scenarios (one static-obstacle and three dynamic-obstacle settings) and in a sim-to-real experiment on a physical Franka robot, D-SafeMPC improves safety, task success rates, and planning efficiency over state-of-the-art baselines. To facilitate reproducibility, our source code and experimental configurations are available in a repository at https://github.com/erdiphd/D-SafeMPC

---

## 论文详细总结（自动生成）

# 论文总结：D-SafeMPC: Diffusion-Driven Safe Model Predictive Control with Discrete-Time Control Barrier Functions

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：扩散模型在机器人轨迹规划中虽然能生成高质量、多模态的轨迹，但缺乏内在机制来强制执行安全约束（如避障、动力学可行性）和硬约束，导致生成轨迹常违反系统物理限制。现有混合方法将扩散模型与模型预测控制（MPC）结合，但扩散模型输出的初始轨迹质量差时，MPC可能无法收敛到安全可行解，引发不稳定或失败。
- **研究背景**：机器人安全规划需要严格满足动力学和避障约束。扩散模型（如Diffuser）擅长从演示中学习，但无法保证约束。控制障碍函数（CBF）和控制李雅普诺夫函数（CLF）可提供软引导，但不足以施加硬约束。MPC能强制执行硬约束，但依赖良好初始化。本文提出的D-SafeMPC通过将CBF/CLF引导与MPC迭代投影相结合，在扩散过程每个去噪步骤中既引导轨迹朝向安全目标区域，又通过MPC确保硬约束满足，从而提升安全性和可行性。

## 2. 方法论
### 核心思想
- 在逆向扩散过程中，使用离散时间CBF和CLF的梯度来调整去噪均值，引导轨迹生成朝向安全、目标导向的区域；然后在每个去噪步骤中（烧入期后），用MPC将引导后的轨迹投影到安全且动力学可行的集合并反馈给去噪过程，实现迭代优化。

### 关键技术细节
- **CBF/CLF引导**：在去噪步骤中，对噪声预测网络输出的均值μθ，加上CBF梯度∇uψh（负梯度拉离障碍物）和CLF梯度∇uΦV（正梯度拉向目标），得到修正均值：  
  `ˆμ ← μθ + λc∇uψh − λl∇uΦV`  
  其中∇uψh和∇uΦV基于离散时间CBF条件（式22）和CLF条件（式25）对控制输入的梯度。λc自适应（距离障碍近时增大），λl固定。早期去噪步骤中λc=λl=0。
- **MPC投影**：MPC以引导后的轨迹˜τ^{i-1}为热启动，求解优化问题（式28）：  
  `min_{u0:H, x̄0:H} Σ (1/2 (τ^i)^T Q τ^i − (˜τ^i)^T Q τ^i)`  
  满足安全约束`h(x̄_k) ≥ 0`和动力学`x̄_{k+1}=F̄(x̄_k, u_k)`。MPC的输出τ^{i-1}用于下一步去噪。
- **迭代流程**（算法1）：从纯噪声τ^N开始，从i=N到1执行：计算μθ，添加CBF/CLF梯度得到ˆμ，采样得˜τ^{i-1}，当i ≤ N/2时执行MPC投影得τ^{i-1}，否则τ^{i-1}=˜τ^{i-1}。最终轨迹τ^0用于控制。

## 3. 实验设计
- **数据集**：使用D3IL数据集，包含Franka Emika Panda机器人在MuJoCo仿真中的成功演示轨迹（7自由度，末端执行器控制）。训练扩散模型在无障环境中学习动力学。
- **场景**：四个模拟场景：一个静态障碍物场景 + 三个动态障碍物场景（障碍物沿圆形轨迹运动，参数随机采样）。
- **基准方法**：对比13种方法，包括：CoBL-Diffusion（CBF/CLF引导）、Diffuser（无约束）、DPCC-C/R/T（扩散MPC的不同变体及其收紧版本）、Guidance（仅引导）、Model-Free（无模型）、Post-Processing（后处理）。所有方法使用相同扩散步数N=20。
- **评估指标**：Goal Reach Rate（成功到达率）、Safety-Compliant Goal Reached Rate（安全合规成功率）。每个场景用10个随机种子，每个种子测试2次，共20次评估。

## 4. 资源与算力
- **文中未明确说明**：未提及GPU型号、数量、训练时长等具体算力信息。实验在仿真中运行，未给出硬件配置细节。

## 5. 实验数量与充分性
- **实验数量**：共4个场景（1静态+3动态），每个场景对比13种方法，提供均值和标准差。此外进行了Sim-to-Real实验（实物Franka机械臂，静态障碍物验证）。
- **充分性**：
  - 覆盖了静态和动态障碍环境，动态场景具有不同的复杂度（三个不同参数设置）。
  - 对比基线丰富，包括基于扩散的方法和传统MPC后处理方法，且包含收紧（Tightened）变体，增加了对照。
  - 评估指标包括安全合规成功率，直接反映硬约束满足情况。
  - 使用随机种子保证统计可靠性，并给出了标准差。
- **公平性**：所有方法使用相同的扩散步数、烧入策略（i≤N/2）和超参数设置，与基线DPCC一致，比较合理。但训练扩散模型的细节（如数据量、训练轮数）未提供，可能影响可重复性。此外，对比方法如DPCC可能有不同的实现细节，但作者声称复现时保持公平。

## 6. 主要结论与发现
- D-SafeMPC在动态障碍场景中表现最优，安全合规成功率和目标到达率均最高或接近最高（如动态环境1中均达100%）。
- 在静态场景中，虽然DPCC-C的安全合规成功率更高（0.9 vs 0.8），但D-SafeMPC的整体表现仍具竞争力。
- 计算效率方面，D-SafeMPC的决策时间少于其他MPC方法（如DPCC变体），归因于CBF/CLF引导提供了更好的MPC热启动，加速收敛。
- Sim-to-Real实验验证了方法在真实机器人上的可行性（静态障碍物场景）。

## 7. 优点
- **方法创新**：首次将离散时间CBF/CLF引导与MPC迭代投影紧密结合，在扩散过程中同时实现软引导和硬约束，兼顾了生成多样性和约束满足。
- **理论性**：CBF给出一阶安全保证（式22），CLF保证目标收敛；MPC确保最终轨迹严格满足动力学和安全性，优于纯软引导。
- **实用性**：通过引导改善MPC初始解，提升了收敛速度和稳定性，避免因初始轨迹差导致MPC失败；计算效率优于纯MPC方法。
- **实验可靠**：多个场景、多种基线对比、有统计指标和真实实验验证，代码开源。
- **通用性**：不依赖奖励指定，仅需演示数据。CBF/CLF可针对不同任务灵活定义。

## 8. 不足与局限
- **可扩展性问题**：MPC求解对于高维状态空间（如视觉输入、全状态空间）的计算代价高，可能限制实时应用。论文未讨论高维场景的性能。
- **误差累积**：早期定位不准确会递归影响后续预测（扩散模型以当前状态为条件），可能导致不稳定。
- **依赖障碍物未来信息**：假设动态障碍物的未来轨迹已知（式17中的自主动力学），实际中需要预测或感知，这可能在复杂环境中不现实。
- **仅验证单机器人场景**：未考虑多机器人或多障碍物交互，CBF条件可能变得复杂。
- **实验覆盖面有限**：真实实验仅在静态障碍物下进行，缺少动态障碍物的实物验证；未提供训练扩散模型的超参数和硬件资源细节，影响可重复性。
- **有偏风险**：基线方法可能未经过充分调优（如DPCC的变体较多但性能不佳），但作者已声明使用了相同设置，整体对比相对客观。

（完）
