---
title: Learning-based Homothetic Tube MPC with Non-Asymptotic Guarantees
title_zh: 基于学习的同伦管MPC与非渐近保证
authors: "Changyi Lei, Seth Siriya, Dragan Nešić, Ye Pu"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12343v1"
tags: ["query:control"]
score: 9.0
evidence: 基于学习的同伦管MPC
tldr: 针对参数未知且存在有界扰动的线性系统约束镇定问题，提出一种基于学习的位似管MPC方法。采用非渐近正则化最小二乘估计构建高概率参数置信集，将其嵌入鲁棒管传播与约束收紧中，形成凸优化问题。理论证明该方法具有高概率递推可行性、鲁棒约束满足和输入-状态稳定性，并给出显式非渐近状态界。数值实验验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12343v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 608, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12343v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 673, \"height\": 818, \"label\": \"Figure\"}]"
motivation: 现有基于学习的MPC常依赖先验不确定性或渐近保证，缺乏对有限样本下非渐近性能的严格分析。
method: 利用非渐近正则化最小二乘估计生成高概率参数置信集，嵌入位似管MPC框架，通过凸优化实现鲁棒约束收紧。
result: 证明高概率递推可行性、鲁棒约束满足和输入-状态稳定性，并给出显式非渐近状态界。
conclusion: 所提方法在有限样本下提供可证性能保证，数值实例验证了有效性和理论优势。
---

## 摘要
本文研究了针对具有未知系统参数和加性有界扰动的离散时间线性系统约束镇定的学习型MPC。我们开发了一种易于处理的同伦管MPC方案，其中高概率参数置信集来自非渐近正则化最小二乘估计，而非先验假设。所得的不确定集被嵌入到鲁棒管传播和约束收紧中，产生具有线性和二阶锥约束的凸优化形式。我们证明了高概率递归可行性、鲁棒约束满足和输入到状态稳定性，同时给出了显式的非渐近状态界限。数值例子说明了其有效性和理论保证。

## Abstract
This paper studies learning-based MPC for constrained stabilization of discrete-time linear systems with unknown system parameters and additive bounded disturbances. We develop a tractable homothetic-tube MPC scheme in which a high-probability parameter confidence set is generated from non-asymptotic regularized least-squares estimation, rather than assumed a priori. The resulting uncertainty set is embedded into robust tube propagation and constraint tightening, yielding a convex formulation with linear and second-order-cone constraints. We prove high-probability recursive feasibility, robust constraint satisfaction, and input-to-state stability, together with explicit non-asymptotic state bounds. A numerical example illustrates the effectiveness and theoretical guarantees.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：针对参数未知且存在加性有界扰动的离散时间线性系统，现有基于学习的MPC方法通常依赖**先验假设**的紧致参数集或**渐近一致性**保证，缺乏有限样本下的严格、非渐近的闭环性能分析。尤其在系统初始不稳定或数据由不良控制器生成时，经典的渐近辨识结果无法直接提供适合鲁棒MPC初始化的有限样本不确定集。
- **整体含义**：本文旨在设计一个**同时在线学习与约束镇定**的框架，将**非渐近正则化最小二乘（RLS）** 的高概率估计误差界嵌入到**位似管MPC**中，使得控制器在有限样本下即可获得可证的概率递推可行性、约束满足性和输入-状态稳定性（ISS），并且随着数据积累，不确定性集单调收缩，状态最终收敛到仅由无法消除的扰动决定的邻域。

### 2. 论文提出的方法论

- **核心思想**：
  - 利用非渐近RLS辨识（式(4)）产生时序参数估计 \(\bar{\theta}_k = [\bar{A}_k, \bar{B}_k]\)，并基于引理1得到**高概率非渐近误差界** \(\bar{\epsilon}(k, \delta)\)（式(8)）。
  - 由该界构造**外逼近超矩形**参数置信集 \(\Theta_k\)（式(12)），并通过**交集更新**使其单调收缩（引理2）。
  - 将 \(\Theta_k\) 和点估计 \(\hat{\theta}_k\) 嵌入**位似管参数化**（式(21)），将原始非凸min-max问题转化为**凸优化**：目标为确定性等价二次代价（式(27)），约束包括线性约束（式(22a)-(22b)）和**二阶锥约束**（式(22c)），具体推导见命题2。
- **关键技术细节**：
  - 位似管参数化：\(X_{l|k} = \{z_{l|k}\} \oplus \alpha_{l|k} X_0\)，其中 \(X_0\) 为基多面体。
  - 控制参数化：\(u_{l|k}(x) = Kx + v_{l|k}\)，预稳定增益 \(K\) 满足对初始参数集 \(\Theta_{k_0}\) 的Lyapunov不等式（假设3）。
  - 终端集 \(X_f\) 满足鲁棒正不变性（假设4）。
  - 算法1总结完整流程：离线阶段收集 \(k_0\) 个数据初始化RLS和终端集；在线阶段每步求解凸优化问题(29)，若可行则存储最优序列，否则复用最近可行解；施加激励信号 \(\eta_k \sim \text{Unif}([-\eta_{\max}, \eta_{\max}]^m)\) 以保证持续激励条件（引理3）。
- **理论贡献**：
  - 命题3：以概率≥1-δ建立递推可行性与约束满足。
  - 命题4：证明最优代价函数 \(V_N\) 是一个**随机实用ISS-Lyapunov函数**，满足漂移条件（式(31)）。
  - 定理1：得到高概率非渐近状态界（式(32)），其中 \(c_4\) 由扰动决定，\(\beta(k)\) 随估计误差衰减，最终收敛到扰动相关邻域。

### 3. 实验设计

- **使用的场景**：一个开环不稳定的二阶线性系统（式(33)），状态维度 \(n=2\)，输入维度 \(m=1\)。
- **数据集/扰动**：扰动 \(w_k \sim \text{Unif}([-0.1,0.1]^2)\)，即均匀分布，有界 \(w_{\max}=0.1\)。
- **基准对比**：**没有**与其他方法进行对比。仅展示了所提算法在单个轨迹上的运行效果。
- **评估内容**：展示了在位似管约束下真实轨迹被管包含的情况（图1），以及闭环状态和输入始终满足约束（图2）。

### 4. 资源与算力

- **未明确说明**。文中没有提及任何GPU型号、数量或训练时长等信息。仅提到使用某种凸优化求解器求解SOC约束问题，算力需求应为常规水平。

### 5. 实验数量与充分性

- **实验数量**：**仅有一个数值例子**，未做消融实验、参数敏感性分析、多随机种子跑均值、或与其他基线对比。
- **充分性评价**：**不充分**。虽然该例子验证了理论的主要预测（管包含、约束满足），但缺少以下内容：
  - 不同系统参数、不同扰动幅度、不同预测时域、不同初始参数集大小下的鲁棒性测试。
  - 与标准鲁棒MPC（如固定参数集）或现有鲁棒自适应MPC（如Lorenzen et al. 2019）的对比。
  - 对概率保证（如1-δ中不同δ取值）的实际经验验证。
- **客观性**：仅展示一条轨迹成功，无法排除偶然性。作者未报告失败概率或重复实验的统计数据。

### 6. 论文的主要结论与发现

- 所提出的学习型位似管MPC能够**在线学习系统参数**同时**以高概率保证约束满足**和**闭环稳定性**。
- 利用非渐近RLS界构造的置信集**单调收缩**，使鲁棒控制的保守性随数据积累而降低。
- 理论证明：在初始可行性下，系统状态在高概率事件上**最终收敛到仅由不可消除扰动决定的紧集**，而估计误差的影响随时间衰减（定理1）。
- 凸优化重构使得在线计算可处理（QCQP/SOCP形式）。

### 7. 优点

- **理论严密性**：提供了**完整的非渐近高概率保证**（递推可行性、约束满足、ISS），不同于现有工作常依赖渐近一致性或先验紧致参数集的假设。
- **方法普适性**：不要求系统稳定或初始控制器良好；通过激励信号保证持续激发，适用于不稳定系统。
- **可操作性**：将复杂min-max问题转化为**带线性与二阶锥约束的凸规划**，利于现有求解器。
- **管集合单调收缩**：利用RLS界与交集更新，主动减少保守性。

### 8. 不足与局限

- **实验验证严重不足**：仅用单一仿真实例，缺乏与基线的对比、消融研究和统计重复，无法体现方法在实际场景中的优越性或鲁棒性。
- **假设适度**：仍需要加性扰动有界（硬约束所必需）、扰动协方差下界已知、初始离线轨迹有界等假设，实际系统中可能存在部分违反。
- **计算复杂度**：位似管MPC的SOC约束涉及对所有顶点和所有约束行的最大化项，状态维度和预测时域增大时**扩展性**未见分析。
- **概率保证的事件依赖**：实际概率事件仅保证以1-δ成立，δ的选择影响置信集半径；且初始离线数据需满足 \(k_0 \geq T_{\text{burn-in}}\)，但未给出 \(T_{\text{burn-in}}\) 的具体计算示例或上界紧致性分析。
- **未处理输出噪声或部分观测**：假设状态完全可观测，公式框架局限于LTI系统。

（完）
