---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:control"]
score: 9.0
evidence: 通过可行动作映射整合强化学习与最优控制
tldr: 操作受限动力系统需要高效控制且保证递归可行性和安全。本文提出FAOC框架，通过优化映射将RL抽象动作转换为满足约束的OCP可行参数，兼顾OC的可预测安全与RL的灵活性。实验表明，在机器人乒乓球实时规划中，FAOC在样本效率和闭环性能上超越现有方法。贡献在于无需专家设计动作空间，且不损害OCP可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1621, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1314, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1617, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 1814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1604, \"height\": 619, \"label\": \"Figure\"}]"
motivation: 现有方法难以同时保证约束满足和RL灵活性，FAOC通过可行动作映射解决此矛盾。
method: 提出FAOC，利用优化算法将RL动作映射到状态依赖的OCP可行参数集，保证约束严格满足。
result: 在机器人乒乓球仿真中，FAOC相比基线大幅提升样本效率和闭环性能。
conclusion: FAOC有效融合RL与OC，为约束系统提供安全且高效的控制框架。
---

## 摘要
操作受约束动态系统需要控制器在强制执行递归可行性和安全约束的同时有效解决复杂任务。为解决这些竞争性需求，我们提出了可行动作最优控制（FAOC），一种集成强化学习（RL）和最优控制（OC）的新颖控制框架。关键贡献是一种计算高效的、基于优化的映射算法，它将RL智能体的动作从静态抽象集合转换为最优控制问题（OCP）的状态依赖可行参数集，保证严格满足动态系统的约束。因此，FAOC有效结合了OC的可预测安全性和RL的灵活性。与先前工作相比，RL智能体的抽象动作空间不需要专家或启发式设计，并且OCP公式不会因RL无法保证可行性而受损。我们将我们的方法应用于机器人乒乓球实时运动规划，这体现了这些挑战。通过模拟实验，我们展示了FAOC在样本效率和闭环性能上均优于最先进的基线。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **问题本质**：约束动态系统的控制需要在保证递归可行性和安全约束的同时高效完成复杂任务。强化学习（RL）灵活但缺乏严格约束保证；最优控制（OC/MPC）能显式处理约束，但在非线性、长时域、稀疏奖励等问题上计算困难且敏感。
- **现有局限**：结合RL与OC时，RL动作常直接作为OCP参数，但静态动作空间可能包含不可行参数，导致OCP无解；而启发式或专家设计的动作空间又限制了表达性或包含冗余，影响学习效率和最终性能。
- **本文目标**：设计一个中间映射算法，将RL智能体在静态抽象集上的输出转化为状态依赖的OCP可行参数集，从而在保留RL探索灵活性的同时严格保证OCP可行性。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：FAOC（Feasible Action for Optimal Control）框架在RL策略与OCP之间插入一个可逆的、基于优化的映射。RL输出（经过tanh等）得到抽象动作 ā ∈ Ā（静态凸紧集，如超立方体），映射算法将其转化为OCP参数 p ∈ P(x)（状态x的可行参数集），使得OCP总是可行。
- **关键算法（Algorithm 1）**：径向缩放映射。
  - 输入：基集X（抽象集Ā）和目标集Y（参数集P(x)），各需一个内点（xc, yc）以及一个连续可逆正齐次的变换φ。
  - 步骤：若x=xc，直接映射到yc；否则计算从xc出发沿方向d=x-xc到X边界的缩放因子α；对d施加变换φ得d'；计算从yc沿d'到Y边界的缩放因子β；最终映射 y = yc + (β/α) d'。
  - 映射可逆（Proposition 1），交换X和Y并应用φ⁻¹即可求逆。
- **缓解点堆积（点分布不均）**：
  - **2D面积匹配变换**：利用归一化面积函数（CDF）匹配基集和目标集的角度分布，使均匀采样在映射后保持面积比例（Proposition 2, 3）。适用于二维。
  - **线性变换（高维）**：用最大体积内接椭球（MVIE）构造代理集，计算可逆矩阵Φ≈Q_Y Q_X⁻¹，令φ(d)=Φd，近似保持分布形状（Proposition 4）。
- **隐式定义目标集的处理**：当P(x)由OCP约束隐式定义（如线性MPC）时，通过“提升”到更高维空间（决策变量z）计算内点、变换矩阵和边界缩放因子，避免显式几何表示（Proposition 5-8, Example 1-3）。关键：利用MVIE离线计算状态无关的变换矩阵，在线仅解简单线性规划或边值问题。
- **OCP公式**：以线性MPC为例（Corollary 1），加入终端状态等式约束（如Cx_N=p），保证参数集P(x)凸、紧、实心，前提是C满秩且存在严格可行轨迹。

### 3. 实验设计：场景、数据集、基准与对比方法
- **场景**：8-DoF机器人乒乓球运动规划。RL策略（157维状态）输出16维目标位姿/速度（每个关节2维），32ms内到达。OCP规划4段8ms的常加加速度，保证位置、速度、加速度、加加速度约束。
- **数据集**：训练中混合了合成轨迹和真实人类击球，每回合随机选择一个技能（落点、上旋/下旋、扣杀）。奖励为稀疏（仅在回合结束时）。
- **基准方法**：
  - **1Dpos / 1Dvel / 1Dacc**：采用[30]的“limits”库，仅指定一个维度（位置/速度/加速度），其余由OCP优化。
  - **2Dsoft**：二维动作空间，但使用软约束（终端惩罚）替换等式约束，需调优权重（ws, Wt），动作空间静态且可能包含不可行点。
  - FAOC（本文）：二维动作，严格等式约束，动作空间映射至可行参数集。
- **对比维度**：
  1. **基线实验**：用SAC算法和BRONet网络架构，比较5种方法。
  2. **算法与架构影响**：改用更强的方法SimBaV2（分布式批评家、更大网络），验证FAOC优势是否稳健。
  3. **低RL频率影响**：将决策间隔从32ms增至128ms（7.8125 Hz），测试可控性。

### 4. 资源与算力
- **未明确说明**：文中未提及使用的GPU型号、数量、训练时长或显存。仅报告了环境交互步数（如1.2e8步），未涉及具体硬件配置。因此无法量化算力消耗。

### 5. 实验数量与充分性
- **实验数量**：
  - 每组实验5个随机种子（共5种方法×3组实验=75个训练运行）。
  - 每个评估点基于1000个评估回合。
  - 统计检验：使用Mann-Whitney U检验，报告p值（显著性水平0.05）。
- **充分性评估**：
  - 覆盖了不同RL算法（SAC、SimBaV2）、不同动作空间维度（1D、2D）、不同决策频率，设计较为全面。
  - 统计比较客观，报告了关键指标（返回率、上旋、速度等）的分布。
  - 但仅在一个场景（乒乓球）上验证，缺乏对其他类型约束系统（如自主驾驶、机器人操作等）的泛化性验证。

### 6. 主要结论与发现
- FAOC在所有实验中均取得最高或并列最高的样本效率和最终性能。
- 在基线实验（SAC+BRONets）中，FAOC在返回率、上旋、速度上显著优于所有对比方法；下旋和落点精度无显著差异。
- 使用更强算法（SimBaV2）后，所有方法性能提升，但FAOC仍保持显著优势（8个对比显著）。
- 低RL频率时，FAOC性能下降最小，而其他方法显著退化，FAOC在13个对比中显著更优——说明映射带来的可达性意识提高了长时程控制的可控性。
- 1D方法因可控性差，表现最差；2Dsoft因静态动作空间含不可行点，探索效率低于FAOC。

### 7. 优点
- **方法创新性**：将RL动作空间设计与OCP可行性解耦，首次实现任意紧凑凸集间的可逆映射，且支持隐式定义的目标集。
- **通用性与可扩展性**：理论框架适用于一般凸OCP；2D面积匹配精确，高维线性变换逼近；可在线高效运行（仅需线性规划或简单计算）。
- **实验设计严谨**：多种子、多指标、统计检验；对比了不同动作空间维度、不同RL算法、不同频率，消除了对特定架构的依赖。
- **实际应用价值**：工作已部署在真实机器人上，并在正式ITTF比赛中击败专业选手，表明方法的实际可行性。
- **开源**：映射算法和OCP实现已开源（GitHub），促进可复现性。

### 8. 不足与局限
- **凸性假设的局限性**：映射算法假定P(x)为凸紧凑集。在非凸OCP（如含障碍物避让、非线性动力学）中，可行参数集可能非凸。论文通过层次化结构（RL处理非凸，OCP处理凸子问题）缓解，但未严格证明一般情况下的适用性。
- **单一场景验证**：所有实验仅在机器人乒乓球仿真中进行。虽然提到真实机器人胜利，但未提供真实环境下的对比实验数据，泛化性有待在其他领域验证（如无人机、自动驾驶等）。
- **计算开销**：在线计算内点（通过SDP或线性规划）和边界缩放因子（解凸优化）可能在高维或复杂约束下仍不满足实时性。论文效率主要依赖线性MPC的特性和离线预计算。
- **映射的近似性**：高维线性变换仅近似保持点分布，无法完美避免点堆积；二维面积匹配虽然在理论上完美，但限于低维。
- **对比方法的公平性**：2Dsoft需要手动调整权重（ws, Wt），且文中未展示调优过程的最优性，可能存在不公平比较（但作者已尽力覆盖不同权重组合）。
- **缺少消融研究**：没有单独验证映射算法（如去掉面积匹配或线性变换）对性能的影响，点堆积缓解的贡献是间接推导而非直接实验证实。

（完）
