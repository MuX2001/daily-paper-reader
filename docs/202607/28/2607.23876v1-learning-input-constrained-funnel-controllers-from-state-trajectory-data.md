---
title: Learning Input-Constrained Funnel Controllers from State Trajectory Data
title_zh: 从状态轨迹数据学习输入受限的漏斗控制器
authors: "Panagiotis S. Trakas, Omid Mirzaeedodangeh, Lars Lindemann"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23876v1"
tags: ["query:control"]
score: 9.0
evidence: 从状态轨迹数据学习输入受限的漏斗控制器
tldr: 针对硬输入约束下的性能规格设计难题，本文提出从状态轨迹数据中联合学习性能漏斗和反馈控制器的优化框架。该方法不依赖控制输入数据，通过标称模型补偿与学习的状态依赖增益合成控制器，并开发了可行性驱动的主动集求解非凸问题。理论给出了半全局保守性和局部数据驱动的双重保证。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23876v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23876v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 907, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23876v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 561, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23876v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 533, \"label\": \"Table\"}]"
motivation: 现有方法在硬输入约束下难以同时满足性能规格，而状态轨迹隐含可行性能与输入限制信息。
method: 提出基于优化学习的框架，从状态轨迹中联合学习性能漏斗和反馈控制器，采用标称模型补偿与学习增益，并设计主动集求解非凸问题。
result: 实现了无需控制输入数据的控制器合成，并建立了半全局保守性与局部数据驱动的性能与输入满足保证。
conclusion: 本文框架有效从轨迹数据中学习输入约束下的性能控制器，提供理论保证，拓展了数据驱动控制方法。
---

## 摘要
设计满足预设性能规范同时强制执行硬输入约束的反馈控制器是一项具有挑战性的任务。我们的工作受到以下想法的启发：状态轨迹数据（例如，从专家控制器获得的数据）通常隐式地编码了可行的性能属性和输入限制。我们提出了一种基于优化的框架，利用状态轨迹数据联合学习：(i) 一个模仿观测轨迹中编码的瞬态和稳态行为的性能漏斗，以及 (ii) 一个在硬输入约束下强制执行所学性能规范的反馈控制器。与模仿学习方法不同，所提出的方法不依赖于控制输入数据，也不重建专家策略。相反，它通过将标称模型补偿与学习的状态相关反馈增益相结合，综合出一个预设性能控制器。由此产生的综合问题是非凸的，为此我们开发了一种基于可行性驱动的主动集综合程序。最后，我们建立了两个互补的保证：一个用于预设性能和输入满足的半全局保守执行器权限证书，以及一个确保这些属性在足够密集的演示轨迹附近成立的局部数据驱动证书。

## Abstract
Designing feedback controllers that satisfy predefined performance specifications while enforcing hard input constraints is a challenging task. Our work is motivated by the idea that state trajectory data, e.g., obtained from an expert controller, often implicitly encode feasible performance attributes and input limitations. We propose an optimization-based framework that uses state trajectory data to jointly learn: (i) a performance funnel that mimics the transient and steady-state behavior encoded within the observed trajectories, and (ii) a feedback controller that enforces the learned performance specifications under hard input constraints. Unlike imitation learning methods, the proposed approach does not rely on control input data and does not reconstruct an expert policy. Instead, it synthesizes a prescribed performance controller by combining nominal model compensation with a learned state-dependent feedback gain. The resulting synthesis problem is nonconvex, for which we develop a feasibility-driven active-set synthesis procedure. Finally, we establish two complementary guarantees: a semi-global conservative actuator-authority-based certificate for prescribed performance and input satisfaction, and a local data-driven certificate ensuring these properties near sufficiently dense demonstrated trajectories.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在硬输入约束（actuator saturation）下，如何设计反馈控制器同时满足预设的瞬态和稳态性能规范（如收敛速率、超调量、稳态精度）。传统预设性能控制（PPC）和漏斗控制（FC）方法通常需要事先手动选择性能漏斗，但若过于激进会超过控制权限，过于保守则损失性能。此外，现有方法常依赖在线放宽约束或高增益反馈，难以在硬约束下保证性能。
- **研究动机**：状态轨迹数据（例如来自专家控制器或人工操作的演示）隐含地编码了可行的性能属性和输入限制。本文旨在仅利用状态轨迹数据（而非控制输入数据）联合学习一个性能漏斗和一个满足输入约束的反馈控制器，而不重建专家策略。这避免了模仿学习需要同时记录状态-输入对的问题，并且不依赖专家输入数据。
- **整体含义**：将数据驱动方法与预设性能控制相结合，首次从纯状态轨迹中联合学习漏斗和控制器，实现了在硬输入约束下对非线性系统性能的保证，为无输入数据场景下的控制器合成提供了新范式。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - **漏斗参数化**：采用三次B样条线性参数化性能函数 \(\rho_i(t;\theta) = \theta_i^\top \Psi(t)\)，保证C²光滑性，同时满足正性和数据驱动上下界约束（基于经验分位数）。
  - **控制器结构**：结合标称模型补偿与学习的状态依赖反馈增益：  
    \( u(t,x) = \text{sat}_{\bar{u}}\big( \hat{G}(x)^{-1}[q(t,x;\theta) - \hat{f}(x) - K(\xi;w) s] \big) \)  
    其中 \(K(\xi;w)\) 为对角增益矩阵，每个分量由径向基函数（RBF）参数化：\(k_i(\xi_i;w_i) = k_{\min} + w_i^\top \phi_i(\xi_i), w_i \succeq 0\)，确保严格正增益。
  - **约束条件**：漏斗需满足分位数上下界、平滑性约束；控制器需在采样点上满足 Lyapunov 递减条件（仅当 \(\|s\| \geq \delta\) 时激活），结合饱和函数保证输入约束。

- **关键技术细节**：
  - 使用经验分位数（α/β）构建数据驱动漏斗区间，降低对离群值敏感度。
  - Lyapunov 函数 \(V = \frac{1}{2}\|\varepsilon\|^2\)，推导出采样点上的线性不等式约束（含饱和项）。
  - 非凸性处理：提出**可行性驱动的主动集合成算法**。外层搜索收缩率参数v（从大到小遍历）；内层固定θ后，迭代求解凸二次规划（固定饱和模式），逐步修正饱和模式直至满足原始约束。

- **算法流程**（文字描述）：
  1. 输入：状态轨迹数据、分位数参数、超参数。
  2. 外层循环：按递减序列尝试不同的v值（初始v₀递减至v_min）。
  3. 对每个v，求解线性规划漏斗问题（约束20b-20e），得到θₓ。
  4. 内层循环：初始化增益权重w₀=0。
     - 对每个r，根据当前wᵣ确定每个采样点上每个输入分量的饱和模式（饱和/非饱和）。
     - 固定该模式后，求解凸二次规划（目标最小化\(\|w\|^2\)，约束(20g)和线性化饱和后的Lyapunov递减条件(22)），获得w_{r+1}。
     - 检查用实际饱和函数后原始约束(20f)是否满足；若满足则返回(θₓ, w_{r+1})；若模式不变且不满足，则跳出；否则更新模式继续迭代。
  5. 若循环结束未找到可行解，则返回“未找到”。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **实验场景**：耦合水箱系统（coupled-tank system），状态为液位偏离设定值的偏差，非线性控制仿射模型，输入矩阵恒定。标称模型与真实模型存在参数偏移（不确定界已知）。
- **数据集**：利用30条演示轨迹，由不同参数的PPC控制器生成（参数各异），模拟专家操作。轨迹采样281个时间点，时域[0,5]秒。
- **Benchmark和对比方法**：论文**未进行定量对比**，仅展示了学习结果的可视化（漏斗拟合、闭环轨迹、输入幅值）。作者指出模型-free PPC（未补偿漂移）只能实现实际调节，而本文方法实现了指数收敛（得益于模型补偿）。但未与任何现有数据驱动或PPC方法进行数值比较。
- **消融或参数敏感性**：论文未进行系统的消融实验，但讨论了参数（α,β,λ_i,v等）的作用（表I），并在图中展示了不同λ_i 对下界的影响。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- **未提及**：论文全文未提及任何关于GPU型号、数量、训练时长、内存等计算资源信息。所有实验应为CPU仿真，计算量较小（状态维数2，30条轨迹，样条维数250，RBF中心28个），算法在标准计算机上即可运行。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

- **实验数量**：仅一组场景（水箱系统），20个未见初始条件的闭环验证。未进行不同系统、不同噪声水平、不同参数配置的系统性实验。
- **充分性评价**：实验**不够充分**。虽然展示了学习效果和约束满足，但缺乏：
  - 与其他方法（如标准PPC、CBF、行为克隆）的定量对比；
  - 对不同数据量、不同α/β取值、不同模型不确定性大小的敏感性分析；
  - 更高维系统或更复杂的非线性系统的验证；
  - 对理论保证（定理2和3）中假设的数值验证（如Lipschitz常数、覆盖半径等）。
- **客观性**：仅有的实验展示结果积极，但受限于单一场景和缺少对比，结论的泛化能力待验证。

### 6. 论文的主要结论与发现

- 提出**从纯状态轨迹数据联合学习性能漏斗和输入约束PPC控制器**的框架，无需控制输入数据。
- 开发了**可行性驱动的主动集合成算法**，有效处理非凸优化问题，迭代调整饱和模式。
- 建立了**两个理论保证**：半全局保守证书（基于执行器权限条件）和局部数据驱动证书（基于Lipschitz连续性和密集采样覆盖）。
- 仿真案例验证：学习到的漏斗能捕捉异构演示的瞬态和稳态行为，闭环轨迹严格保持在漏斗内，且控制输入满足限幅。相比模型-free PPC，补偿模型漂移后实现了零误差指数收敛。

### 7. 优点：方法或实验设计上有哪些亮点

- **创新性**：首次从纯状态轨迹数据联合学习漏斗和输入约束控制器，避免了需要控制输入数据或重建专家策略的限制。
- **实用性假设**：仅需标称模型和不确定界，不需要精确模型；利用B样条和RBF实现灵活参数化，计算复杂度可控。
- **算法设计**：主动集合成巧妙地利用饱和模式固定后的凸性，将非凸问题转化为序列凸子问题；外层搜索v平衡性能与可行性。
- **理论保证**：给出两种互补证书（保守但不依赖数据密度，以及数据密集时的局部保证），为数据驱动安全控制提供了理论支撑。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验不足**：仅在一个耦合水箱系统上验证，缺少与基线方法的定量对比，也未进行充分消融研究。证据链薄弱，通用性存疑。
- **理论局限**：半全局证书（定理2）条件保守，要求执行器权限满足均匀上界，实际中可能过于严格；局部证书（定理3）依赖于Lipschitz常数和数据覆盖假设，实际验证困难。
- **算法局限**：主动集算法可能收敛缓慢或停滞于局部可行解，没有保证全局最优甚至可行解存在性；外层v搜索有限步终止可能错过可行解。算法规模受限于采样点数，对于高维状态或大量数据可能计算量大。
- **应用限制**：
  - 要求系统是控制仿射且标称模型存在且输入矩阵可逆。
  - 对于非光滑或时变系统需另行处理。
  - 演示轨迹需具有足够的状态空间覆盖和时间覆盖，否则学习效果差。
  - 未考虑测量噪声或状态估计误差，实际实现需额外鲁棒性设计。
- **偏差风险**：演示数据由特定PPC控制器生成，可能偏向可控场景；实验者未测试更复杂或对抗性演示下的鲁棒性。

（完）
