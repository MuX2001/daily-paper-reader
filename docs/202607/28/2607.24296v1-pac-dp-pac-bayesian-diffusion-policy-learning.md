---
title: "PAC-DP: PAC-Bayesian Diffusion Policy Learning"
title_zh: "PAC-DP: PAC-贝叶斯扩散策略学习"
authors: "Mohammad Hasan Yeganegi, Dian Yu, Andrea Del Prete, Majid Khadiv, Matteo Saveriano"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24296v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: PAC-贝叶斯扩散策略用于机器人操作
tldr: 扩散策略在机器人操作中表现优异，但有限数据下泛化受限。本文提出PAC-DP，将模型视为贝叶斯神经网络，引入PAC-Bayes泛化界，用KL散度正则化增强训练。实验显示，该方法在多个基准上提升去噪性能、降低负对数似然并提高成功率，尤其在低数据和复杂任务中显著。PAC-DP提供了理论支撑的机器人策略学习框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24296v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1510, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 304, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1887, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24296v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1867, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1870, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 245, \"label\": \"Table\"}]"
motivation: 标准扩散策略训练在有限数据中泛化能力不足，缺乏理论指导的正则化方法。
method: 将扩散策略建模为贝叶斯神经网络，基于PAC-Bayes理论推导泛化界，添加KL散度正则化项到去噪损失中。
result: 多个机器人操作基准中，PAC-DP相比标准DP实现更高的去噪性能和成功率，低数据场景提升最大。
conclusion: PAC-DP为机器人策略学习提供了理论扎实的框架，有效提升有限数据下的泛化能力。
---

## 摘要
扩散策略能够执行复杂的操作任务。然而，扩散策略通常通过最小化去噪目标进行训练，这在机器人学常见的有限数据场景中对泛化能力的控制有限。在本文中，我们提出PAC-DP，一种提升扩散策略在机器人操作任务中性能的方法。通过将扩散策略建模为贝叶斯神经网络，并定义PAC-贝叶斯泛化界，我们推导出一种新的训练目标，该目标在标准去噪损失基础上增加了后验与先验参数分布之间的KL散度正则化项。从理论角度看，我们的方法提供了一种在几乎不增加训练时间的情况下正则化扩散策略训练的原则性方法。从实践角度看，实验结果在多个机器人操作基准测试中展示了改进的去噪性能、更低的变分负对数似然以及更高的成功率。关键的是，在低数据训练场景和复杂任务中观察到最大的改进，使PAC-DP成为机器人策略学习的理论基础框架。

## Abstract
Diffusion Policies (DPs) are able to perform complex manipulation tasks. However, DPs are typically trained by minimizing a denoising objective, which provides limited control over generalization in the finite-data regimes common in robotics. In this letter, we propose PAC-DP, an approach that increases the performance of DPs in robotic manipulation tasks. By modeling the DP as a Bayesian neural network, and defining a PAC-Bayes generalization bound, we derive a novel training objective that augments the standard denoising loss with a Kullback-Leibler divergence regularizer between the posterior and prior parameter distributions. From the theoretical perspective, our approach provides a principled approach to regularize the training of DPs without significantly increasing the training time. From the practical point of view, experimental results demonstrate improved denoising performance, lower variational negative log-likelihood, and higher success rates across multiple robotic manipulation benchmarks. Crucially, the largest improvements are observed in low-data training regimes and complex tasks, establishing PAC-DP as a theoretically grounded framework for robot policy learning.

---

## 论文详细总结（自动生成）

# PAC-DP: PAC-贝叶斯扩散策略学习 — 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：扩散策略（Diffusion Policy, DP）在机器人模仿学习中表现出色，但标准训练仅最小化去噪损失，在有限数据场景（机器人学中常见）下泛化能力不足。已有研究表明，在低数据训练中，仅优化去噪目标可能导致模型记忆训练样本而非学习真实分布，缺乏对泛化的理论控制。
- **研究动机**：现有改进方法多依赖架构归纳偏置或大规模数据，缺乏对训练目标本身的理论指导。作者希望利用PAC-贝叶斯学习理论，为扩散策略提供具有泛化保证的训练目标，提升有限数据下的性能。
- **整体含义**：本文提出PAC-DP，将扩散策略建模为贝叶斯神经网络，基于PAC-Bayes泛化界导出新的训练目标（去噪损失 + KL散度正则化），在理论和实践上同时改善扩散策略的泛化能力。

## 2. 方法论
- **核心思想**：将扩散策略中的噪声预测网络建模为贝叶斯神经网络（BNN），学习参数的后验分布 \(Q(w)\) 而非单一确定值。利用PAC-Bayes不等式推导出泛化界，并将该理论界转化为可训练的目标函数。
- **关键技术细节**：
    - **贝叶斯参数化**：后验 \(Q(w)\) 和先验 \(P(w)\) 均设为对角高斯分布，可学习参数为均值 \(\mu\) 和标准差 \(\sigma\)（通过 \(\sigma = \log(1+e^\rho)\) 保证非负）。训练时使用重参数化技巧采样权值 \(w = \mu + \sigma \odot \xi\)。
    - **PAC-Bayes目标函数**：基于Refined Pinsker不等式放松后的PAC-Bayes-kl界，得到代理损失：
      \[
      L_{\text{PAC-DP}} = \hat{R}(Q) + \sqrt{\frac{2\hat{R}(Q)(\lambda D_{\text{KL}}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}} + \frac{2(\lambda D_{\text{KL}}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}
      \]
      其中 \(\hat{R}(Q) = \mathbb{E}_{w\sim Q}[L_{\text{DP}}(w)]\) 为经验去噪风险的期望，\(\lambda\) 控制正则化强度，\(D_{\text{KL}}\) 为KL散度（闭式可算）。
    - **架构细节**：仅在去噪网络部分使用贝叶斯层，条件投影层保持确定性（避免引入噪声破坏观测表征）。
    - **推理**：可选择使用后验均值（确定性策略）或采样（随机策略）。
- **算法流程**：
    - 训练：每次迭代从数据集采样 \((x_0, o)\)、时间步 \(t\) 和噪声 \(\epsilon\)；构造 \(x_t\)；从 \(Q\) 采样权值 \(w\)；预测噪声并计算 \(L_{\text{PAC-DP}}\)；反向传播更新后验参数。
    - 推理：用后验均值或采样权值初始化，通过DDIM调度器迭代去噪生成动作序列。

## 3. 实验设计
- **数据集与场景**：
    - **仿真**：使用DP基准套件，包含**PushT**任务（推块）和**Robomimic**五个任务：Lift、Can、Square、Transport、ToolHang。均使用状态观测和专家演示。
    - **真实机器人**：Franka Emika Panda机器人上的Lift、Can、Square、ToolHang任务，每个任务5条不同初始配置的轨迹各执行10次，共200次试验。
- **基准与对比方法**：
    - **DP**：标准扩散策略（确定性网络，标准去噪损失）。
    - **BDP**（贝叶斯DP消融）：使用贝叶斯网络但仅优化去噪损失（无PAC-Bayes正则化）。
- **评估指标**：
    - 噪声预测误差（NPE）— 在验证集上衡量。
    - 变分负对数似然（NLL）下界 — 评估生成分布质量。
    - 任务成功率（SR）— 仿真每次任务1000个随机环境（×3种子共30000次 rollout）。

## 4. 资源与算力
- **文中未明确说明**具体GPU型号、数量、训练时长。仅提及训练步数：PushT约 \(10^6\) 步（1e6），Robomimic任务中Lift/Can/Square为 \(2.5\times10^5\) 步，Transport/ToolHang为 \(5\times10^5\) 步。可推测使用了常见GPU（如NVIDIA RTX系列）进行训练，但无详细资源信息。

## 5. 实验数量与充分性
- **主要实验**：
    - **PushT动态分析**（图3）：训练过程NPE、NLL、SR随时间变化，3个种子，最后10个检查点平均。
    - **Robomimic基准（200个演示）**：5个任务×3方法×3种子，报告NPE、NLL、SR（表I）。
    - **低数据场景**：20/40/80/200个演示（PushT、Lift、Can、Square），Transport和ToolHang仅200个演示（表II）。
    - **超参数敏感性**：\(\lambda \in \{10^{-5},10^{-6}\}\)，\(\rho \in \{-3.5,-4.0,-4.5,-5.0\}\)，共8种组合，报告平均SR和最好SR（表III）。
    - **贝叶斯网络架构消融**（Full BN vs 仅去噪网络BN）在ToolHang上（表IV）。
    - **真实机器人实验**：4个任务各50次执行，共200次。
- **充分性评价**：
    - 实验覆盖了不同难度、不同数据量、多个指标，并包含消融和敏感度分析，设计较为充分。
    - 所有仿真实验均使用3个随机种子，统计均值与标准差，具有统计意义。
    - 对比了直接相关消融（BDP），有效分离了贝叶斯参数化和正则化的贡献。
    - 真实实验验证了可部署性，但样本量有限（每个任务50次），且未与真实环境中的其他方法对比。

## 6. 主要结论与发现
- PAC-DP在所有任务上均优于DP和BDP，尤其在**低数据场景**和**复杂任务**（如ToolHang）中提升显著。
- 在PushT上，PAC-DP的NPE比DP降低约67%（0.060 vs 0.184），NLL下界从-3.24降至-8.275，成功率从86.1%提升至93.7%。
- 在Robomimic基准中，PAC-DP在ToolHang上将成功率从40.1%（DP）和54.4%（BDP）提升至73.8%，在200个演示下提升至85.8%。
- 超参数敏感性分析显示，PAC-DP在不同\(\lambda\)和\(\rho\)下表现稳健，平均性能接近最佳配置。
- 贝叶斯网络架构消融证明，仅在去噪网络使用贝叶斯层是关键（Full BN导致成功率极低），确定性条件投影稳定了观测表征。
- 真实机器人实验100%成功率，验证了实际可部署性。

## 7. 优点
- **理论驱动**：直接从PAC-Bayes泛化界导出正则化项，为扩散策略训练提供了理论保障。
- **轻量改进**：无需修改网络架构或增加推理复杂度，仅改变训练目标；训练时间几乎不增加。
- **有效泛化**：在低数据和复杂任务上显著优于标准DP，解决了实际部署中数据稀缺的痛点。
- **全面验证**：包括仿真指标（NPE、NLL、SR）和真实机器人实验，以及消融和超参数敏感性分析。
- **开源可复现**：代码基于标准DP实现，易于集成。

## 8. 不足与局限
- **算力细节缺失**：未报告GPU型号、数量、训练时间，不利于资源评估和复现。
- **真实实验规模小**：仅4个任务各50次，且无定量对比其他方法（如DP在真实环境中的成功率），说服力有限。
- **部分任务SR未提升**：在Square和Transport任务中，虽然NPE下降，但成功率与DP/BDP持平或略低（表I），说明去噪误差与下游控制性能之间不完全正相关。
- **超参数选择依赖**：\(\lambda\)和\(\rho\)需根据任务调整，尽管表现稳健，但仍需初步网格搜索。
- **假设限制**：PAC-Bayes界假设有界损失，而实际使用平方误差（无界），尽管在实践中可用clipped loss近似，但理论严格性存在折扣。
- **仅考虑状态观测**：未扩展到视觉/多模态任务，未来工作需验证。

（完）
