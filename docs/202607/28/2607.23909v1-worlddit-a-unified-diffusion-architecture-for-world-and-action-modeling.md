---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：面向世界与动作建模的统一扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:world-model"]
score: 9.0
evidence: 无需大型VLM骨干的统一扩散Transformer用于世界和动作建模
tldr: 当前机器人策略常依赖大型预训练视觉语言模型(VLM)作为动作骨干，导致计算开销大且依赖外部知识。WorldDiT提出统一扩散Transformer架构，将动作生成与视觉世界建模耦合，训练时同时输出连续动作块并预测未来相机帧的归一化RGB补丁，无需大型VLM。在LIBERO四个仿真套件上，该方法以少于十亿参数达到帕累托前沿，在总参数量与平均成功率之间取得优异平衡，为未来扩展研究提供了强基线。这项工作证明，通过巧妙的联合建模，无需庞大预训练模型即可实现高效机器人控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略常依赖大型预训练视觉语言模型作为动作骨干，导致模型庞大、计算开销高，因而需要更轻量且统一的架构来同时处理动作与视觉世界。
method: WorldDiT是一种统一扩散Transformer架构，在训练中同时输出连续动作块并预测未来相机帧的归一化RGB补丁，无需大型预训练视觉语言模型。
result: 在LIBERO全部四个仿真套件上，WorldDiT以少于十亿参数达到参数量与平均成功率的帕累托前沿，性能与更大模型相当。
conclusion: WorldDiT证明了无需大型预训练VLM，通过统一扩散Transformer耦合动作与视觉建模即可实现高效控制，为未来扩展提供了强基线。
---

## 摘要
许多最新的机器人策略通过使用大型预训练视觉-语言模型（VLM）作为动作骨干网络来追求更强的控制能力。我们提出WorldDiT，一种统一的扩散变换器架构，它结合了动作生成与视觉世界建模，并在没有大型预训练VLM动作骨干网络的情况下实现了强性能。在训练过程中，单个扩散变换器生成连续的动作片段，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO仿真套件中，WorldDiT处于已报告的总模型参数与平均成功率帕累托前沿上。这些结果为未来的规模扩展研究提供了一个强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# 论文《WorldDiT: A Unified Diffusion Architecture for World and Action Modeling》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前许多机器人策略遵循语言模型的范式，将动作生成附加到大型预训练视觉‑语言模型（VLM）上。这种做法虽然带来了广泛的感知和语言理解能力，但也使得规模与架构的贡献难以分离——当策略包含数十亿参数时，强控制表现可能来自动作设计、预训练骨干或二者的组合。
- **核心问题**：是否可以采用统一的扩散Transformer架构，在没有大型预训练VLM作为动作骨干的情况下，同时执行连续动作生成和辅助的未来视觉预测，并保持强大的控制性能？
- **整体含义**：论文试图证明，通过巧妙的联合建模（动作生成 + 视觉世界建模），小规模模型也能达到与大型VLM骨干方法相媲美的性能，从而为未来模型扩展提供一个紧凑、可解释的基线。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- **统一扩散Transformer（WorldDiT）**：共享一个DiT骨干网络，同时学习连续机器人动作生成和未来相机帧的归一化RGB补丁预测。
- **训练阶段**：使用流匹配（flow matching）目标，对动作块和未来RGB补丁一起进行去噪训练。
- **推理阶段**：仅使用动作路径，移除RGB补丁相关的token和预测头，保持轻量。

### 2.2 关键技术细节
- **输入构建**：
  - 观察窗口：`C=3`个历史步骤（包括主相机和腕相机图像、机器人状态） + 语言指令。
  - 动作目标：`H=7`步的连续动作块。
  - 未来RGB补丁目标：从未来第`H`步的相机帧中提取，每帧分为16×16补丁，每补丁768维，归一化后保留64个均匀间隔的补丁，两个相机共128个token。
- **编码器**：
  - 冻结的MAE图像编码器 + 共享Perceiver Resampler → 视觉token。
  - 冻结的CLIP文本编码器 → 语言指令token。
  - 可训练的机器人状态编码器 → 状态token。
  - 所有token拼接后作为条件输入DiT骨干。
- **模型架构**：
  - DiT骨干：深度4，隐藏尺寸1024，16个注意力头，4个寄存器token。
  - 可训练线性输入/输出投影处理RGB补丁token。
- **训练损失**：
  - 流匹配损失：`L_flow = E[||v_θ( x_τ, τ, c) - (y - ε)||^2]`。
  - 总损失：`L_total = w_action * L_action_flow + w_rgb * L_rgb_flow`，权重分别为0.1和0.001。
- **推理过程**：
  - 从高斯噪声初始化动作token，用20步Euler积分求解流场。
  - 预测动作块后执行前3步，更新观察窗口，重新规划（receding-horizon control）。
  - 应用时间集成（temporal ensembling）处理重叠预测。

## 3. 实验设计：数据集、基准、对比方法

- **数据集与基准**：
  - 使用LIBERO操作基准中的四个下游套件：`libero_spatial`、`libero_object`、`libero_goal`、`libero_10`（报告为LIBERO Long）。
  - 预训练使用大规模多任务拆分`libero_90`（包含所有任务）。
- **对比方法**：
  - 共24种方法，分为两大类：
    - **使用大型预训练VLM动作骨干**：如ACoT VLA、MMaDA VLA、VLANeXt、π_0.5、MemoryVLA等（共16种）。
    - **不使用大型预训练VLM动作骨干**：如WorldDiT、DreamVLA、FlowVLA、DiT Policy、Octo、Diffusion Policy等（共8种）。
  - 报告每个套件的成功率及四套均值，以及总参数数。
- **评估协议**：
  - 每个套件500个模拟episode，但其中300个用于阶段性checkpoint选择，因此报告均值（94.9%）并非无偏测试估计算法。
  - 使用headless EGL渲染，统一配置进行推理。

## 4. 资源与算力

- **训练硬件**：单个节点，配备8块NVIDIA RTX Pro 6000 GPU。
- **训练配置**：
  - 预训练（liber_90）：30个epoch，per‑GPU batch size=40，梯度累积2，学习率1e-4（cosine调度，1个warmup epoch），bf16混合精度。
  - 微调（每个套件）：有效batch size=512（per‑GPU batch size=32 + 2梯度累积），学习率1e-4，bf16。
- **训练时长**：论文未明确给出总训练时长或GPU小时数。

## 5. 实验数量与充分性

- **实验数量**：
  - 仅评估一个WorldDiT配置（参数固定）。
  - 在每个套件上单独微调，各运行500个episode。
  - 无消融实验（例如不使用RGB目标、不同损失权重、不同上下文长度等）。
- **实验充分性评估**：
  - **客观性**：报告分数由于包含部分checkpoint选择数据而有偏差，作者明确指出不应视为无偏估计。对比方法使用其发表报告中的分数，而非统一复现，可能导致协议差异。
  - **公平性**：参数数比较考虑了推理时必需的冻结模块，但不同方法的评估协议、种子、硬件等可能不一致。
  - **充分性**：缺乏消融实验和更多配置的下游验证，仅在一个仿真基准上测试，领域泛化和real-world性能未知。

## 6. 论文的主要结论与发现

- **主要结论**：WorldDiT以399.084M总参数（135.107M可训练）在LIBERO四个套件上达到平均成功率94.9%，位于已报告方法的总参数-成功率的帕累托前沿。
- **关键发现**：
  - 无需大型预训练VLM作为动作骨干，通过统一扩散Transformer耦合动作生成与视觉世界建模，即可获得与大型VLM方法竞争的性能。
  - 在所有四套均报告的方法中，任何成功率高于WorldDiT的方法总参数更多（如ACoT VLA 98.5%但参数量更大），而参数更少的方法成功率均低于WorldDiT。
  - LIBERO Long（多阶段长任务）依然是最困难套件，与其他方法一致。

## 7. 优点：方法或实验设计上的亮点

- **架构创新**：
  - 统一扩散Transformer同时处理动作和视觉预测，训练时利用未来视觉信息提供额外监督，推理时保持纯动作路径，无额外开销。
  - 使用归一化RGB补丁（而非原始像素或重构图像），减小预测难度，与动作共享流匹配目标。
- **轻量高效**：
  - 参数量低于十亿，在计算资源有限的情况下达到高成功率，为低资源场景提供可行方案。
  - 训练和推理流程清晰，易于实现和扩展。
- **基准贡献**：
  - 提供了明确的参数-性能帕累托前沿分析，便于未来研究进行公平对比。
  - 强调其作为“强亚十亿参数基线”的价值，为规模化研究提供起点。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在一个仿真基准（LIBERO）上评估，缺乏真实机器人、其他仿真环境或多种任务的验证。
  - 仅测试了一个固定配置（深度4、隐藏1024），未进行规模缩放实验或超参数敏感性分析。
- **评估偏差**：
  - 报告的成功率包含用于checkpoint选择的300个episode，不是完全独立测试集上的无偏估计。
  - 对比方法均直接引用其原始报告分数，未统一评估协议，可能存在数据泄露、随机种子、rollout次数等差异。
- **缺乏消融研究**：
  - 未量化未来RGB补丁辅助目标的贡献（删除该目标后的性能变化）。
  - 未分析不同损失权重、上下文长度、动作块长度等设计选择的影响。
- **应用限制**：
  - 使用冻结的MAE和CLIP编码器，依赖其预训练质量；若更换视觉/语言骨干，性能可能下降。
  - 归一化RGB补丁预测仅从未来一帧采集，未建模多帧时序动态，可能限制复杂世界模型能力。
  - 当前配置下参数数虽较小，但在更大规模场景下能否保持优势尚未验证。

（完）
