---
title: Robust Adaptive Backup Control Barrier Functions
title_zh: 鲁棒自适应备份控制障碍函数
authors: "Ersin Daş, David E. J. van Wijk, Tamas G. Molnar, Aaron D. Ames, Joel W. Burdick"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20842v1"
tags: ["query:control"]
score: 9.0
evidence: 针对参数不确定非线性系统的鲁棒自适应备份控制障碍函数
tldr: 针对参数不确定性下备份CBF依赖模型预测可能不准确的问题，本文提出鲁棒自适应备份CBF，利用元素级认证自适应估计器估计未知参数并给出误差界收紧安全条件，对执行矩阵不确定性用对偶性重新表述。所设计的基于二次规划的安全滤波器保证参数不确定和输入约束下的安全，将自适应估计与备份CBF结合提升安全鲁棒性。该成果扩展了备份CBF的适用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 730, \"label\": \"Figure\"}]"
motivation: 备份CBF依赖模型预测，参数不确定性可能导致安全违规，需设计鲁棒自适应方法。
method: 利用元素级认证自适应估计器估计参数并给出误差界，收紧安全条件；对执行矩阵不确定性采用对偶性重新表述。
result: 设计基于二次规划的安全滤波器，在参数不确定和输入约束下保证系统安全。
conclusion: 提出的鲁棒自适应备份CBF有效处理参数不确定性，扩展了备份CBF在不确定系统中的应用。
---

## 摘要
我们提出了一种鲁棒自适应备份控制障碍函数的概念，用于漂移动力学和执行矩阵均含有参数不确定性的非线性控制仿射系统。备份控制障碍函数通过预测系统在预认证安全控制器下的轨迹来保证安全性。然而，这些预测依赖于模型，当系统包含未知参数时可能不准确。为解决此问题，我们使用逐元素认证的自适应估计器来估计未知参数，该估计器提供参数自适应律和逐分量估计误差界。我们使用估计模型计算备份流，并利用这些认证界收紧安全条件。由此产生的安全条件考虑了预测流对参数估计误差的敏感性。此外，为处理执行矩阵中的不确定性，我们采用基于对偶的重新表述，从而能够使用计算高效的基于二次规划的安全滤波器。我们证明，满足所提出的鲁棒自适应备份控制障碍函数约束的控制器在参数不确定性和输入约束下能保证安全性。

## Abstract
We propose a notion of robust adaptive backup control barrier functions for nonlinear control affine systems with parametric uncertainty in both the drift dynamics and actuation matrix. Backup control barrier functions guarantee safety by predicting the system's trajectory under a pre-certified safe controller. However, these predictions rely on the model and can be inaccurate when the system contains unknown parameters. To address this issue, we estimate the unknown parameters using element-wise certified adaptive estimators that provide a parameter adaptation law and component-wise estimation error bounds. We compute the backup flow using the estimated model and tighten the safety conditions using these certified bounds. The resulting safety conditions account for the sensitivity of the predicted flow to parameter estimation errors. Moreover, to handle uncertainty in the actuation matrix, we use a duality-based reformulation that enables the use of a computationally efficient quadratic-program-based safety filter. We prove that controllers satisfying the proposed robust adaptive backup control barrier function constraints guarantee safety under parametric uncertainty and input constraints.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：备份控制障碍函数（bCBF）通过预测系统在预认证安全控制器下的轨迹来保证安全性，但该预测依赖于精确的模型。当系统存在**参数不确定性**（包括漂移动力学和执行矩阵）时，基于名义模型的预测可能不准确，导致安全违规。
- **整体含义**：本文旨在解决参数不确定非线性系统在输入约束下的安全控制问题，提出一种**鲁棒自适应备份CBF**框架，通过在线估计参数并利用认证误差界收紧安全条件，同时用对偶性处理执行矩阵不确定性，最终设计一个基于二次规划（QP）的安全滤波器，保证系统的正向不变性和安全性。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将自适应参数估计与备份CBF结合，利用估计模型预测备份轨迹，并通过**元素级认证自适应估计器**提供的误差界对安全条件进行**收紧**，得到隐式安全集的内近似；对执行矩阵不确定性采用**对偶性重新表述**，保持QP形式。
- **技术细节**：
  1. **不确定系统模型**：考虑参数仿射形式的不确定性 \(\dot{x} = f(x) + g(x)u + \phi(x,u)\theta\)，其中 \(\theta\) 为未知但有界参数（已知上下界）。
  2. **元素级认证自适应估计器**：定义满足（11）和（12）的估计器，能够提供参数估计 \(\hat{\theta}(t)\) 和逐分量误差界 \(\rho_i(t)\)，使得 \(|\tilde{\theta}_i(t)| \leq \rho_i(t)\)。文中以DREM估计器为例，给出了具体更新律和误差界推导。
  3. **估计备份流与误差界**：使用当前估计参数冻结后积分得到估计备份流 \(\hat{\varphi}_b(\tau, x, t)\)，并通过Gronwall引理推导实际流与估计流之间的误差上界 \(\delta_\varphi\)。
  4. **收紧安全条件**：基于误差界构造收紧函数 \(\epsilon, \epsilon_b\)（如利用Lipschitz常数），定义时变内近似安全集 \(\hat{\mathcal{C}}_I(t) \subseteq \mathcal{C}_I\)，并施加类似CBF的约束（27）。
  5. **对偶性鲁棒化**：将含未知 \(\tilde{\theta}\) 的安全约束转化为线性规划的对偶形式（31-34），引入对偶变量 \(\mu\)，使约束成为关于 \(u\) 和 \(\mu\) 的仿射不等式，从而可以构建QP（35）。
  6. **QP安全滤波器**：在每一时刻求解QP，最小化与主控制器 \(k_p\) 的偏差，同时满足离散化的鲁棒备份CBF约束和输入约束。
- **算法流程**（文字说明）：
  1. 初始化参数估计 \(\hat{\theta}(0)\) 及误差界 \(\rho(0)\)。
  2. 每一时刻 \(t\)，利用当前 \(\hat{\theta}(t)\) 和估计器得到误差界 \(\rho(t)\)。
  3. 从当前状态 \(x\) 出发，用估计模型积分得到备份轨迹和灵敏度矩阵。
  4. 计算收紧函数 \(\epsilon, \epsilon_b\) 及其偏导数。
  5. 构建QP问题（35），求解最优控制输入 \(u\)。
  6. 若QP可行则应用该输入，否则切换为鲁棒备份控制器 \(k_b\)（通过Assumption 2保证安全）。

### 3. 实验设计
- **实验场景**：平面四旋翼飞行器的有效载荷插入任务，要求避开墙壁、保持最小高度和俯仰角边界，最终飞向目标点。
- **使用的数据集/场景**：单次仿真，参数设置详见代码仓库，四旋翼真实参数与名义模型偏差约47%（质量更重）。无公开benchmark数据集。
- **对比方法**：
  - **主控制器** \(k_p\)：级联位置-姿态控制器，不考虑安全与不确定性。
  - **名义bCBF**：使用名义模型（\(\theta=0\)）传播备份流，不做鲁棒化。
  - **提出的鲁棒自适应bCBF**：使用DREM估计器与对偶性收紧。
- **结果**：主控制器和名义bCBF均违反安全（进入不安全区域），而提出的方法保持状态在安全集内，输入满足约束，参数估计误差界随时间缩小。

### 4. 资源与算力
- 文中**未明确提及**所使用的GPU型号、数量、训练时长等算力信息。实验为单次仿真，可能仅需CPU即可完成，未涉及大规模训练。

### 5. 实验数量与充分性
- **实验数量**：仅有一组仿真（平面四旋翼），未包含多组不同参数场景的定量分析或消融实验。
- **充分性与客观性**：实验覆盖面较窄，仅展示定性轨迹和参数收敛过程，缺乏统计性结果（如成功率、对噪声或不同不确定程度的鲁棒性）。对比方法仅两种，且未在相同条件下重复多次。因此**实验不够充分**，结论的说服力有限，但作为概念验证可接受。

### 6. 论文的主要结论与发现
- 提出的鲁棒自适应bCBF框架能够处理参数不确定性（包括执行矩阵不确定性），在输入约束下保证系统状态始终保持在安全集内。
- 随着自适应估计误差界的缩小，内近似安全集 \(\hat{\mathcal{C}}_I\) 逐渐逼近真实隐式安全集，保守性降低。
- 理论证明：满足鲁棒约束的控制器可保证 \(\hat{\mathcal{C}}_I\) 的正向不变性，从而确保安全。

### 7. 优点
- **方法创新**：首次将元素级认证自适应估计器与备份CBF结合，利用误差界收紧安全条件，而非仅靠点估计，提高了安全性。
- **处理执行矩阵不确定性**：通过对偶性重新表述，避免了在QP中包含双线性项，保持了计算效率（QP形式）。
- **理论保证**：给出了鲁棒备份CBF约束的充分条件，并证明其保证安全。
- **灵活性**：估计器类定义宽泛，不仅限于DREM，还可与其他自适应或集员估计器结合。

### 8. 不足与局限
- **实验覆盖不足**：仅一个仿真场景，缺乏在复杂系统（如高维机器人、多旋翼）或真实硬件上的验证；未进行消融研究（如不同估计器、不同误差界计算方式）。
- **保守性与可行性**：内近似安全集 \(\hat{\mathcal{C}}_I\) 可能为空，导致QP不可行，需依赖鲁棒备份控制器。文中仅定性讨论了参数设计使非空，缺乏定量分析或自适应调整策略。
- **流误差界推导**：基于全局Lipschitz常数，可能偏保守；未考虑更紧的边界（如收缩理论或线性系统特例）。
- **未讨论离散化误差**：QP使用时间网格约束，仅提及可加安全余量，未详细分析网格密度与安全保证的关系。
- **未涉及持续激励条件**：文中称不需要PE，但实际估计误差界缩小依赖于激励；若缺乏激励，误差界不缩小，保守性可能长期存在。

（完）
