---
title: Non-asymptotic Bounds of Learning-based Linear MPC With Input Constraints and Unbounded Stochastic Noise
title_zh: 基于学习的带输入约束和无界随机噪声的线性MPC的非渐近界
authors: "Changyi Lei, Seth Siriya, Dragan Nešić, Ye Pu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13513v1"
tags: ["query:control"]
score: 9.0
evidence: 基于学习的线性MPC非渐近界包含输入约束与随机噪声
tldr: 针对未知线性系统在硬输入约束和次高斯噪声下的稳定控制问题，提出一种结合切换MPC与在线正则化最小二乘参数估计的确定性等价设计。该方法混合MPC与饱和死区控制器保证全局稳定性，并基于最小二乘非渐近误差界推导出闭环系统的高概率稳定性界。数值实验验证了理论结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13513v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 385, \"label\": \"Figure\"}]"
motivation: 现有基于学习的MPC缺乏对无界次高斯噪声和硬输入约束下非渐近稳定性分析。
method: 采用确定性等价设计，融合切换MPC控制律、在线正则化最小二乘估计与饱和死区控制器。
result: 导出闭环系统的非渐近高概率稳定性界，确保全局稳定性的。
conclusion: 为学习型MPC在随机噪声下提供严格理论保证，数值实验支持了分析。
---

## 摘要
本文研究了针对未知离散时间线性系统的基于学习的模型预测控制（MPC），该系统具有硬输入约束和加性无界亚高斯扰动。我们采用确定性等价（CE）设计，将切换MPC控制律与在线正则化最小二乘（RLS）参数估计相结合。由此产生的切换控制律将MPC与饱和无差拍控制器相结合，确保全局闭环稳定性。基于最小二乘的非渐近误差界，我们推导了所提出的切换控制器下闭环系统的非渐近、高概率稳定性界。数值实验说明并支持理论结果。

## Abstract
This paper studies learning-based model predictive control (MPC) for stabilizing unknown discrete-time linear systems with hard input constraints and additive unbounded sub-Gaussian disturbances. We adopt a certainty-equivalence (CE) design that combines a switching MPC control law with online regularized least-squares (RLS) parameter estimation. The resulting switching control law blends the MPC with a saturated deadbeat controller, ensuring global closed-loop stability. Building upon non-asymptotic error bound of least-squares, we derive non-asymptotic, high-probability stability bounds for the closed-loop system under the proposed switching controller. Numerical experiments illustrate and support the theoretical findings.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：模型预测控制（MPC）在约束多变量系统控制中应用广泛，但其性能高度依赖精确的系统模型。实际系统中模型不确定性不可避免，特别是对于未知线性系统、硬输入约束和不可忽略的随机噪声。现有基于学习的MPC方法（如鲁棒MPC、数据驱动MPC）要么需要预先假设紧凑的参数集合或预稳定控制器，要么只能提供局域稳定性保证，缺乏在同时进行在线学习与控制过程中的全局、非渐近稳定性分析。
- **核心问题**：针对**输入饱和**和**无界次高斯噪声**的未知离散时间线性系统，如何设计学习型MPC控制器，使其能够在**无预稳定控制器、无先验紧凑不确定性集、无终端约束**的条件下，同时实现全局随机稳定和在线系统辨识，并提供**非渐近的高概率稳定性界**。
- **整体意义**：该工作首次为同时学习和稳定控制的设定提供了严格的非渐近理论保证（概率意义上的有界性），填补了现有理论在无界噪声和硬约束下全局稳定性分析的空白。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：采用**确定性等价（Certainty-Equivalence, CE）** 框架，即将在线估计的系统参数代入MPC优化问题求解控制律，同时引入**切换控制**和**激发噪声**以保证持续激励（PE）条件，并结合**无终端约束的MPC**与**饱和无差拍控制器**实现全局稳定。
- **关键技术细节**：
    1. **在线参数估计**：使用正则化最小二乘（RLS）在线估计系统矩阵 \( \theta^* = [A, B] \)，并利用**非渐近误差界**（Proposition 1）量化估计误差 \( \|\hat{\theta}_t - \theta^*\| \leq \epsilon_t \)。
    2. **κ步子采样**：考虑κ步可达性，定义子采样系统 \( \bar{x}_{\tau+1} = A^\kappa \bar{x}_\tau + R^* \bar{u}_\tau + \dots \)，使系统在子采样时间尺度上一步可达。
    3. **切换控制律**：包含两个候选控制器：
        - **饱和无差拍控制器**（Deadbeat, DB）：\( \pi_{\text{DB}}(x, \hat{\theta}, C) = \text{sat}_{u_{\max}-C}(-R_\kappa(\hat{A},\hat{B})^\dagger \hat{A}^\kappa x) \)
        - **MPC控制器**：求解有限时域优化问题（5），使用指数型阶段成本 \( \ell(x,u) = Q(e^{|x|}-1)^2 + u^\top R u \)，无终端约束。
        - 根据成本和滞回切换机制（公式8）在两者之间切换，保证控制律关于参数全局Lipschitz连续（避免小参数误差导致控制突变）。
    4. **激发噪声**：在每一步注入均匀分布的高维随机噪声 \( \bar{v}_\tau \sim \text{Uni}([-C,C]^{\kappa m}) \)，确保持续激励条件，驱动在线辨识。
- **算法流程**（Algorithm 1）：每个κ步周期内，测量状态，分别计算DB与MPC的成本，根据切换规则决定下一模式，采样激发噪声，按序施加κ步控制输入并更新参数。
- **稳定性理论**：通过构造**凸衰减随机ISS函数**（Convex-Decay Stochastic ISS Function），结合估计误差界，证明经过有限时间后，状态以高概率 \(1-3\delta\) 有界，并给出时变衰减上界（Theorem 1）。主要条件是预测时域N足够大（满足不等式(12)），输入约束足够宽松（Corollary 1）。

#### 3. 实验设计：使用的数据集/场景、基准、对比方法
- **场景**：一个2维线性系统，矩阵为旋转矩阵和任意输入矩阵，输入约束 \( u_{\max}=6 \)，噪声 \( w_t \sim \mathcal{N}(0,1) \)（次高斯），初始状态 \( x_0 = [20, -20]^\top \)。
- **数据集**：在线生成的数据（即系统在控制下的闭环轨迹），无预先存在的离线数据集。
- **基准与对比**：论文未设置显式基准或对比方法。它主要验证自身算法（Algorithm 1）的有效性，展示了状态轨迹和参数估计误差的均值表现。
- **对比方法**：无。属于理论证明后的验证性实验，而非系统性benchmark比较。

#### 4. 资源与算力
- **文中未明确说明**：没有提及使用的GPU型号、数量、训练时长、内存等计算资源。仿真采用MATLAB或Python（未说明），每个episode 8000步，20个episode，属于小规模计算，不需要高性能算力。

#### 5. 实验数量与充分性
- **实验数量**：仅在一组系统参数和固定设置下进行了**20个独立episode**的仿真，每个episode 8000步，并计算了均值。
- **充分性评价**：
    - **不足**：缺乏对不同系统维数、不同约束松紧度、不同噪声分布、不同初始条件或不同超参数（如预测时域N、激发强度C）的系统性消融实验。没有与现有方法（如鲁棒MPC、自适应MPC）进行对比，无法量化本方法相对于现有技术的实际优势。
    - **客观性**：图表显示状态和估计误差收敛，但仅凭均值曲线难以全面评估概率保证。实验设计偏向于验证可行性，而非严格验证理论界的紧度。
    - **偏差风险**：单一系统且参数为旋转矩阵（A范数为1），满足假设条件；但对于临界稳定或不满足‖A‖≤1的系统未测试，存在选择偏差。

#### 6. 论文的主要结论与发现
- 提出了一种无需终端约束、无需预稳定控制器、无需先验不确定性集的**确定性等价学习型MPC切换控制**方案。
- 利用**非渐近系统辨识界**和**凸衰减随机ISS**框架，首次为未知线性系统在硬输入约束和无界次高斯噪声下同时学习和稳定控制提供了**高概率非渐近稳定性保证**（Theorem 1）。
- 给出了预测时域N需满足的显式条件（(12)），并示例了当惩罚状态足够严、输入约束足够大时该条件可满足（Corollary 1）。
- 仿真验证了所提算法能使状态和估计误差收敛，符合理论预期。

#### 7. 优点：方法或实验设计上的亮点
- **理论贡献突出**：将非渐近辨识与全局切换到MPC稳定性分析结合，推导出明确的高概率界，这是该领域的重要进展。
- **设计精巧**：采用κ步子采样将可达性简化，利用饱和无差拍控制器作为基准以及滞回切换保证Lipschitz连续性，这些技术细节较好解决了同时学习和稳定控制的冲突。
- **成本函数选择**：指数型阶段成本使得无终端约束的MPC仍能获得全局稳定（通过足够长的预测时域），避免了终端约束的局域性限制。
- **实验简洁实用**：虽然实验数量少，但精确展示了算法的典型行为，对理论结论起到了辅助验证作用。

#### 8. 不足与局限
- **实验覆盖严重不足**：仅测试了一个简单2维系统，没有在更高维、不同结构（如非定常、不稳定、多输入多输出）系统中验证。缺乏与现有方法的定量对比。
- **未讨论实际应用中的关键问题**：如计算实时性、预测时域N与实际工程可接受的计算负担之间的权衡、激发噪声对控制性能的负面影响等。
- **理论界可能较保守**：推导过程中使用了多个不等式放缩，得到的非渐近界可能在实际中难以达到紧致，论文未讨论界的紧度。
- **假设限制**：要求 ‖A‖ ≤ 1（至少边际稳定），这排除了开环不稳定但可达空压的系统（尽管文中声称该条件是必要的）。此外，已知方差下界σ_W² > 0用于PE条件，实际中难以获得。
- **未涉及状态估计或输出反馈**：仅考虑全状态反馈，对更现实的输出反馈情况未拓展。

（完）
