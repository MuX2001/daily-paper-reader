---
title: "BridgeFlow: Fast and Robust SE(2)-Equivariant Motion Planning with Flow Matching"
title_zh: "BridgeFlow: 基于流匹配的快速鲁棒SE(2)等变运动规划"
authors: "Xinzhe Zhou, Xuyang Wang, Xiaoming Duan, Jianping He"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14725v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: SE(2)等变运动规划，结合流匹配
tldr: 机器人运动规划要求对刚体变换具有等变性，但现有方法要么缺乏等变性，要么依赖计算昂贵的专用网络。本文提出BridgeFlow，通过轻量级任务中心规范化模块实现严格SE(2)等变，兼容标准架构。结合布朗桥先验和上下文感知小批量最优传输，构建笔直矢量场加速推理。在2D和7-DoF机械臂上，实现15倍加速和2倍有效轨迹率，鲁棒泛化至未见环境。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14725v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14725v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1653, \"height\": 591, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14725v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14725v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1644, \"height\": 737, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14725v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 714, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14725v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1839, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14725v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 399, \"label\": \"Table\"}]"
motivation: 现有运动规划方法在等变性和实时性之间存在矛盾：缺乏等变性导致泛化差，强制等变则计算昂贵，无法满足实时推理。
method: 提出BridgeFlow，通过轻量级任务中心规范化模块实现精确SE(2)等变，并采用布朗桥先验和上下文感知小批量最优传输构建笔直矢量场，结合无分类器指导嵌入环境感知。
result: 在密集2D环境和Franka机械臂上，推理速度提升15倍，有效轨迹率提高2倍，且对未见环境和任意空间变换具有鲁棒泛化能力。
conclusion: BridgeFlow以极低计算开销实现严格等变和快速推理，为高效鲁棒的运动规划提供了新范式。
---

## 摘要
在机器人运动规划中，对刚体变换的等变性对于鲁棒的空间泛化至关重要。然而，当前基于学习的规划器面临一个关键困境：它们要么缺乏内在等变性，将变换后的任务视为新场景，要么通过计算昂贵的专用架构强制执行等变性，从而成为实时推理的瓶颈。为打破这一权衡，我们提出BridgeFlow——一个快速且严格SE(2)等变的生成式运动规划框架。BridgeFlow不依赖重的等变网络，而是通过轻量级的以任务为中心的规范化模块实现精确的空间等变性，从而允许使用标准架构进行泛化。为进一步加速推理，我们将布朗桥信息先验与上下文感知的小批量最优传输相结合。这构建了一个笔直的向量场，最小化传输成本并稳定训练。此外，通过无分类器指导显式嵌入环境感知。在密集二维环境以及7自由度Franka机械臂上的评估表明，BridgeFlow相比最先进的扩散基线实现了高达15倍的推理加速和2倍的有效轨迹率，同时对完全未见环境和任意空间变换具有鲁棒泛化能力。

## Abstract
In robotic motion planning, equivariance to rigid body transformations is crucial for robust spatial generalization. However, current learning-based planners face a critical dilemma: they either lack inherent equivariance, treating transformed tasks as novel scenarios, or enforce it via computationally expensive specialized architectures that bottleneck real-time inference. To break this trade-off, we propose BridgeFlow, a fast and strictly SE(2)-equivariant generative motion planning framework. Rather than relying on heavy equivariant networks, BridgeFlow achieves exact spatial equivariance via a lightweight task-centric canonicalization module, enabling generalization using standard architectures. To further accelerate inference, we pair a Brownian bridge informative prior with context-aware mini-batch optimal transport. This constructs a straightened vector field that minimizes transport costs and stabilizes training. Furthermore, environmental awareness is explicitly embedded via Classifier-Free Guidance. Evaluations in dense 2D environments and on a 7-DoF Franka manipulator demonstrate that BridgeFlow achieves up to a 15x inference speedup and a 2x higher valid trajectory rate over state-of-the-art diffusion baselines, alongside robust generalization to entirely unseen environments and arbitrary spatial transformations.

---

## 论文详细总结（自动生成）

# BridgeFlow: 基于流匹配的快速鲁棒SE(2)等变运动规划 —— 详细中文总结

## 1. 核心问题与整体含义

**研究动机**：机器人运动规划中，对刚体变换（旋转、平移）的等变性是空间泛化与鲁棒性的基础。然而，现有学习型规划器面临两难：要么缺乏等变性（将变换后的任务视为新场景，泛化差），要么通过计算昂贵的专用等变网络强制等变性（导致实时推理瓶颈）。同时，生成式模型（如扩散模型）推理速度慢，环境感知依赖测试时优化。

**整体意义**：本文旨在打破等变性与速度之间的权衡，提出一种严格SE(2)等变、快速、环境感知的流匹配运动规划框架BridgeFlow。该工作为高效、鲁棒、几何泛化的运动规划提供了新范式。

## 2. 方法论

### 核心思想
- **不依赖专用等变网络**：通过轻量级**任务中心规范化模块**，在推理前将输入变换到标准坐标系，用标准架构实现严格SE(2)等变。
- **加速流匹配**：用**布朗桥信息先验**替代标准高斯先验，锚定起止点，降低传输成本；配合**上下文感知小批量最优传输**，避免跨上下文错误配对，进一步拉直向量场。
- **环境感知嵌入**：通过**无分类器指导（CFG）**显式地将环境占用图纳入训练，消除测试时梯度优化需求。

### 关键技术细节
1. **问题形式化**：将运动规划建模为条件流匹配，学习SE(2)等变的向量场 \( v_\theta(x_t, t | P) \)，满足 \( v_\theta(T(x_t), t | T(P)) = R \cdot v_\theta(x_t, t | P) \)。

2. **布朗桥先验**：
   - 对N个航点，每点 \( x_{0,i} = \mu_{\text{lin},i} + \sigma_{\text{prior}} \sqrt{\gamma_i(1-\gamma_i)} \cdot \epsilon \)，其中 \( \mu_{\text{lin}} \) 是起止线性插值，方差在端点为0，中间最大。
   - 理论证明（Theorem 1）：相比高斯先验，布朗桥先验的传输成本更低（\( C_{\text{std}} - C_{\text{info}} > 0 \)），且自身SE(2)等变。

3. **上下文感知小批量最优传输**（Algorithm 1）：
   - 将小批量按任务上下文（\(s,g,M\)）划分为子集，在每个子集内使用匈牙利算法匹配初态与专家轨迹，消除跨上下文错误配对，稳定训练。

4. **无分类器指导（CFG）**：
   - 训练时以概率 \( p_{\text{drop}} \) 遮蔽环境条件 \( M \)，学习条件向量场 \( v_\theta^M \) 与无条件 \( v_\theta^\emptyset \)。
   - 推理时：\( v_{\text{CFG}} = v_\theta^\emptyset + \omega (v_\theta^M - v_\theta^\emptyset) \)，\(\omega \ge 1\)。

5. **任务中心规范化**（Theorem 2）：
   - 规范化算子 \( T_{\text{can}}(p) = R_{-\theta}(p - s) \)，将起点平移至原点，起终矢量对齐x轴。
   - 流程：规范输入 → 用标准U-Net预测规范空间向量场 → 逆旋转 \( v_{\text{global}} = R_\theta \hat{v} \)。
   - 严格证明SE(2)等变性：规范空间不变，输出按旋转规则变换。

## 3. 实验设计

### 场景与数据集
- **PointMass2D Dense**：二维密集障碍物环境，200个环境（150训练/50测试），每环境500个起止对，每对20条多模态专家轨迹（由RRT-Connect + B样条生成），通过交换起止对扩展为1000个有向任务。
- **7-DoF Franka Emika Panda**：3D操作任务，对称方形桌面有四个区域A,B,C,D。训练时仅使用起于区域A的轨迹（A→B,C,D），测试时评估其他区域间的OOD任务（严格SE(2)变换）。

### 基准方法
- **MPD**（Motion Planning Diffusion）：扩散模型基线，无环境训练指导，需测试时梯度代价函数。
- **FlowMP**：将MPD的扩散主干替换为普通流匹配，使用高斯先验。

### 评价指标
- Success Rate (SR)：至少一条轨迹成功的任务比例。
- Valid Rate (VR)：有效轨迹在所有候选中的比例。
- Collision Intensity：所有候选轨迹中碰撞航点占比，越低越好。
- Average Length、Diversity、Inference Time。

### 实验设置
- 采样候选数 \(K=40\)。
- 对比不同推理步数（2~5步，MPD额外测试10/15步DDIM）。
- 消融：有无OT、有无布朗桥先验（BridgeFlow即为两者组合）。
- 测试环境泛化：随机新障碍物布局。
- 测试SE(2)变换泛化：对原训练任务施加随机刚体变换。

## 4. 资源与算力

**论文未明确说明使用的GPU型号、数量或训练时长。** 仅提到实验在PyBullet模拟器中进行，数据集由RRT-Connect生成。推理时间在表中给出（如BridgeFlow 5步 0.0133s），但训练算力未提及。

## 5. 实验数量与充分性

### 实验数量
- 在PointMass2D Dense环境进行了**大量定量实验**（表I覆盖20+行，包括不同步数、不同方法、消融变体）。
- 单独做了**SE(2)随机变换测试**（表III，8行数据）。
- 在Franka 7-DoF任务上进行了**两个场景**（In-Distribution和Unseen SE(2) Transfer）的对比（表II）。
- 提供了**可视化示例**（图4）和**布朗桥先验与高斯先验的对比图**（图1）。

### 充分性与公平性
- **充分**：覆盖了核心研究问题（生成质量、环境泛化、几何鲁棒性），消融设计合理（分开验证OT、BB Prior、CFG效果）。
- **客观公平**：基线MPD和FlowMP均为公开先进方法；对基线也集成了CFG确保公平对比；推理步骤一致。
- **不足**：缺少在真实机器人上的实验，以及与其他等变方法（如Equivariant Diffusion Policy）的对比——但后者可能因为计算成本高未被纳入。

## 6. 主要结论与发现

1. **效率与质量**：BridgeFlow仅需2步推理（0.0071s）即可达到100% SR、70.25% VR；5步时VR达80.95%，碰撞强度降至1.67。相比MPD 15步，实现**15倍推理加速**，且VR翻倍（42.48%→80.95%）。
2. **环境泛化**：在随机新障碍物布局下，BridgeFlow保持VR 39.4%，而MPD仅19.05%，证明CFG在结构化流场下更有效。
3. **几何鲁棒性**：在随机SE(2)变换任务下，基线MPD/FlowMP的SR暴跌至40-52%，VR<6%；而BridgeFlow保持SR 96%，VR 74.85%，碰撞强度5.28，验证了任务中心规范化的零样本泛化能力。
4. **Franka高维任务**：BridgeFlow在未见SE(2)变换下VR达80%，远超MPD（19%）和FlowMP（21%），并且路径更短、保持多元性。
5. **自然边界满足**：布朗桥先验使神经网络学会在端点处输出零向量，无需手工“轨迹修补”。

## 7. 优点

- **创新性方法论**：巧妙结合布朗桥先验、上下文感知OT和任务中心规范化，系统性解决等变性与速度的矛盾。
- **轻量级等变实现**：无需复杂等变网络，仅通过坐标变换实现严格等变，兼容标准U-Net，易于集成。
- **理论严谨**：提供等变性证明（Proposition 1, Theorem 1, Theorem 2）和传输成本分析，支撑方法合理性。
- **实验详尽且有说服力**：涵盖两个领域、多种泛化场景，消融实验验证各组件贡献，量化指标充分。
- **实际可用性**：推理延迟低至毫秒级，符合实时控制要求；测试时无需额外梯度优化。

## 8. 不足与局限

- **维度扩展**：当前仅实现SE(2)等变，未扩展到完整SE(3)（3D旋转平移）；论文在结论中将其列为未来工作，但未给出初步结果。
- **未见真实机器人验证**：所有实验在模拟器中进行，未在真实机器人上部署，实用性尚需证实。
- **超参数敏感**：布朗桥先验的噪声方差 \(\sigma_{\text{prior}}\) 和CFG指导权重 \(\omega\) 是手动设定，论文未讨论自适应调节策略。
- **计算资源未披露**：训练成本（GPU型号、数量、时长）未报告，影响可复现性评估。
- **实验对比范围有限**：未与专门的等变扩散规划器（如Equivariant Diffusion Policy, SE(3)-Diffusion Policy）直接比较——尽管它们可能更慢，但等变性能对比有缺失。
- **环境地图表示**：CFG使用二维占用图，对复杂3D环境（如深度图、点云）的扩展性未讨论。
- **隐式偏差**：布朗桥先验基于线性插值，若最优路径非线性程度高（如U形绕障），可能引入先验偏差。

（完）
