Title: Online Control via Counterfactual Tracking

URL Source: https://arxiv.org/pdf/2607.13029v1

Published Time: Wed, 15 Jul 2026 12:03:14 GMT

Number of Pages: 41

Markdown Content:
# Online Control via Counterfactual Tracking 

Yunzong Xu University of Illinois Urbana-Champaign 

xyz@illinois.edu 

Abstract 

We develop a method for online control that competes with general classes of causal policies, beyond the linear-controller classes used by most existing algorithms. Over a horizon of T rounds, we consider a known linear dynamical system subject to adversarial disturbances and convex costs revealed after each action. The method simulates the benchmark policies on the revealed history, uses their counterfactual state–input pairs to form a moving reference, and applies a fixed stabilizing controller to track that reference on the physical system. We call this method 

counterfactual tracking .Counterfactual tracking applies to any measurable class of causal policies that can be simulated from the revealed history and whose counterfactual state–input pairs have bounded diameter at every round. The policies may be nonlinear or dynamic and need not share a parameterization. We establish PAC-Bayes regret guarantees that hold for every posterior over policies and depend on its relative entropy to a chosen prior. On a fixed plant with a tracker of bounded impulse-response gain, a finite class of N policies admits the minimax-optimal √T log N dependence on 

T and N when log N = O(T ).As a central application, we compete with a system-level response ball of stabilizing linear dynamical controllers. The ball bounds the summed impulse-response deviation from the fixed tracker, but imposes no common decay envelope, memory length, or controller-order bound. To our knowledge, this is the first online-control guarantee uniform over such a class. A matching lower bound shows that our guarantee is tight up to constants. 

# 1 Introduction 

Over a horizon of T rounds, consider the known linear dynamical system 

xt+1 = Ax t + Bu t + wt, (1) where xt is the state, ut is the control, and wt is a disturbance satisfying ∥wt∥ ≤ W . On round t, the controller observes xt, chooses ut, incurs the convex cost ct(xt, u t), and then observes the full cost function ct and the next state xt+1 . The current cost and disturbance may depend on the previously revealed history. Given a benchmark policy class Π, we seek a causal controller whose cumulative cost nearly matches that of the best policy in Π, chosen in hindsight and evaluated counterfactually. More precisely, each policy is rerun from the initial condition on the same realized cost functions and disturbances, and its cumulative cost is evaluated along the state–input trajectory that it would have generated. This fixed-realization comparison is the standard benchmark of online nonstochastic control, a problem that has attracted substantial recent attention [ 4 , 3, 22 , 16 , 34 , 33 , 29 , 27 , 12 ]; see [21] for an introduction. The problem joins two traditions of sequential decision-making. Kalman’s state-space models, Bellman’s dynamic programming, and the ensuing theories of optimal and robust control study how 1arXiv:2607.13029v1 [math.OC] 14 Jul 2026 present actions shape future states when the model and objective are specified, under stochastic or worst-case disturbances [ 23 , 9, 38 ]. Online learning and regret minimization, beginning with the work of Robbins, Blackwell, and Hannan, ask how to act before the current objective is known and compare the resulting performance with the best rule in hindsight [ 32 , 11 , 17 ]; see [ 13 , 18 ] for comprehensive introductions. Online control brings these questions together: the controller must choose each action before seeing the current cost, adapt to the realized costs and disturbances without a probabilistic model, and account for how each action changes the states encountered later. Modern online-control algorithms first established regret guarantees against linear-controller benchmarks. The principal benchmark was a stable linear state-feedback class : a collection of policies ut = −Kx t whose gains are bounded and whose closed-loop responses (A − BK )r obey a common geometric decay bound. Cohen et al. obtained efficient O(√T ) regret for known dynamics, stochastic disturbances, adversarial quadratic costs, and such a class [ 14 ]. Agarwal et al. treated adversarial disturbances and general convex costs through disturbance–action control and online convex optimization with memory [ 4 , 6 ]. For unknown dynamics, Hazan, Kakade, and Singh established the first efficient sublinear guarantee, of order T 2/3, against a stable linear state-feedback class [ 22 ]. Simchowitz, Singh, and Hazan subsequently treated partial observation and enlarged the benchmark to stabilizing linear dynamical controllers [ 34 ]. These results established sublinear regret without a probabilistic model for future costs or disturbances, but remained tied to linear-controller classes. Our objective is broader: to design a method that competes with a general policy class supplied as the benchmark. The difficulty is that each benchmark policy is evaluated on its own state trajectory. When a policy π is rerun on the realized sequence, it produces the counterfactual trajectory yπt = ( xπt , u πt )

and incurs ct(yπt ). Even if these counterfactual losses can all be evaluated, an ordinary expert algorithm does not directly produce a controller: different policies prescribe actions at different states, and switching between them changes the state on which all later actions operate. An online-control algorithm must therefore learn which counterfactual trajectory to follow while controlling the physical system that generates its actual trajectory. Existing algorithms usually use one set of closed-loop response coordinates for both tasks. For a stable linear state-feedback class, a common geometric decay bound gives a single memory length that approximates every comparator; disturbance–action coefficients then make the simulated state– input trajectory affine in the decision vector [ 4, 6]. The extension to linear dynamical controllers similarly assumes a common bound on the closed-loop response tails and selects one memory length from that bound [ 34 ]. Other approaches prescribe a memory length [ 29 ], use a shared response dictionary [ 12 ], or assume a shared smooth parameterization and a uniform contraction bound [ 27 ]. These constructions require both a common representation and a convex learning problem. To apply online convex optimization, they choose a vector θ from a convex set and use ℓt(θ) = 

ct(xt(θ), u t(θ)) as the round-t loss. Convexity of ct in the state and action does not by itself make 

ℓt convex in θ; existing methods obtain convexity by making (xt(θ), u t(θ)) affine in θ. A general collection of nonlinear, dynamic, or learned policies may admit neither shared coordinates that represent every trajectory nor a convex loss in such coordinates. This is why methods built around a common controller parameterization are difficult to extend to general policy classes. This leads to the question studied here: can online control obtain sharp regret guarantees against general causal policy classes when the policies share neither controller coordinates nor a common memory or decay bound? We answer this question through a new approach, which we call 

counterfactual tracking .21.1 Main results 

Counterfactual tracking. For each policy π, we simulate the counterfactual trajectory yπt =(xπt , u πt ) that it would produce on the revealed history. An online learning rule aggregates these trajectories into a moving reference ¯yt = ( ¯xt, ¯ut). A fixed stabilizing gain K0 tracks the reference using 

ut = ¯ ut − K0(xt − ¯xt). (2) For every comparator π, regret decomposes exactly as 

> T∑
> t=1
> [

ct(yt) − ct(yπt )] =

> T∑
> t=1
> [

ct(¯ yt) − ct(yπt )]

+

> T∑
> t=1
> [

ct(yt) − ct(¯ yt)].

(3) The first term is the regret from learning the reference among the simulated policy trajectories. The second is the excess physical cost caused by tracking error. Every counterfactual trajectory obeys the same affine dynamics. A convex combination with fixed weights is therefore dynamically feasible; only a change in the mixture weights creates a one-step reference defect. If et = xt − ¯xt, the tracking error obeys 

et+1 = ( A − BK 0)et + δt+1 .

Here δt+1 = A¯xt + B ¯ut + wt − ¯xt+1 is the one-step reference defect: the amount by which consecutive reference points fail to satisfy the plant dynamics. The stable tracker converts cumulative reference defect into cumulative state–input tracking error, and Lipschitz continuity converts that tracking error into excess cost. The learner can therefore average trajectories even when policies or policy parameters cannot be averaged. Counterfactual tracking learns in trajectory space, not parameter space. 

General policy classes. Let ν be a prior on a measurable class Π of causal policies whose counterfactual state–input pairs can be simulated and have diameter at most ∆ at every round. Exponential weighting assigns a Gibbs distribution to the policies, and the reference is the average of their simulated state–input pairs under this distribution. Write KL (ρ∥ν) for the relative entropy of a posterior ρ with respect to ν. For convex costs that are L-Lipschitz, we prove the PAC-Bayes regret bound T∑

> t=1

ct(yt) − Eπ∼ρT∑

> t=1

ct(yπt ) ≤ KL( ρ∥ν)

η + CηL 2∆2Γtr T, (4) for every posterior ρ that is absolutely continuous with respect to ν and every learning rate η > 0

satisfying a stated range condition. Here C is a numerical constant, ∆ is the uniform pointwise diameter bound, and Γtr combines the plant norm with the cumulative state–input impulse-response gain of the fixed tracker. For a finite class of N policies, the uniform prior and a point-mass posterior give 

O

> (

L∆

> [√

Γtr T log N + log N

> ])

regret. On a fixed scalar plant, a matching lower bound shows that the √T log N dependence is optimal when log N = O(T ).When each cost is μ-strongly convex, its value at the weighted average of the policy trajectories is smaller than the weighted average of their costs by an amount proportional to their weighted 3squared distance from that average. The same weighted variance controls how much the Gibbs distribution changes and hence the reference defect. This gives O(( L2Γtr /μ ) log N ) regret for a finite class, with matching dependence on L, μ, N when Γtr = O(1) . These PAC-Bayes results need not be computationally efficient: for a continuous policy class, evaluating the Gibbs average may be intractable. For response classes with affine coordinates, counterfactual tracking instead yields explicit convex algorithms. 

System-level response balls without a common tail. Our central application competes with stabilizing linear dynamical controllers whose closed-loop disturbance responses may have different decay rates and controller orders. System-level synthesis represents such a controller by impulse-response blocks Φ = {Φ[i]}i≥1 subject to affine feasibility constraints [ 37 ]. Relative to the response Φ0 of the fixed tracker, define 

S(R) = 

> 

Φ : ∑

> i≥1
> ∥∥∥

Φ[i] − Φ[i]0 

> ∥∥∥F

≤ R 

> 

,

subject to the affine feasibility constraints. The sum is the centered response gain : if ∥wt∥ ≤ W ,then it bounds the state–input deviation from the tracker by W R at every round. Thus S(R) is a system-level response ball of centered gain R. The bound does not impose a common decay rate, tail envelope, memory length, or controller order. Only the first T − 1 response blocks can affect a length-T trajectory, so the algorithm optimizes all of those blocks rather than choosing a truncation length. Mirror descent in a block norm within a constant factor of ℓ1 gives 

Reg T (S(R)) ≤ CLW R 

> √

T log( eT ), (5) up to factors determined by the fixed tracker. A matching scalar lower bound uses delayed finite-impulse-response controllers and shows that √log T is the price of allowing the centered response mass to occur at any of T delays. To our knowledge, this is the first minimax-optimal online-control guarantee uniform over an infinite-horizon system-level response ball defined only by a bound on centered response gain. Earlier work obtained ˜O(√T ) regret, where ˜O suppresses logarithmic factors, against linear dynamical controllers for which the sum of the closed-loop response-block norms after delay h is bounded by a shared decay function [ 34 ]; other work prescribes a finite response length in advance [ 29 ]. Neither formulation covers S(R): after every cutoff h, some response in S(R) can place all R

units of centered response mass after h. At a response radius proportional to γ−1, up to constants determined by the fixed dimensions and tracker impulse-response sums, the ball contains the stable linear state-feedback class with stability margin γ defined next. At this radius, (5) is proportional to LW γ −1√T log( eT ). Thus this larger class of dynamic controllers retains square-root dependence on T . Its additional √log T factor is minimax necessary because the response mass may appear at any delay. 

Sharp stability dependence for the stable linear state-feedback class. For κ ≥ 1 and 

γ ∈ (0 , 1) , let Kκ,γ denote the class of policies ut = −Kx t satisfying 

∥K∥op ≤ κ, ∥(A − BK )r∥op ≤ κ2(1 − γ)r for every r ≥ 0.

Thus γ measures the common stability margin of the class. We parameterize each comparator by its disturbance-response coefficients and run projected gradient descent in those coordinates. In the 4decay-weighted norm, the comparator radius is proportional to γ−1/2, while changing the response coefficients by one unit changes the counterfactual state–input pair by an amount proportional to 

W γ −1/2. Their product in the projected-gradient bound gives the W γ −1√T scale. Up to constants depending only on the fixed dimensions, κ, and the tracker impulse-response sums, we obtain 

O

> (

LW γ −1√T

> )

regret for globally L-Lipschitz convex costs and 

O

> (

GW 2γ−2√T

> )

regret for costs whose gradients are bounded by GD on the radius-D ball, with D proportional to 

W/γ up to the same fixed factors. We call this condition (G, D )-regularity below. Fixed-plant lower bounds have the same powers of γ, so these results identify the sharp stability-margin dependence under both cost assumptions. They retain the square-root horizon rate, up to logarithmic factors, of disturbance–action and spectral methods [ 4 , 12 ], while showing that counterfactual tracking pays no additional stability-margin penalty on this classical benchmark. The convex-cost results leave a separate question: when does strong convexity improve the regret rate for these policy classes? 

What strong convexity can and cannot do. Discretizing the stable linear state-feedback class, whose gain matrices have dK = dudx entries, and applying the PAC-Bayes theorem gives 

O 

> (

G2W 2

μγ 2 dK log T

> )

.

A fixed-dimensional lower bound matches the dependence on T, W, γ . The finite cover used by the algorithm may be exponential in dK . Strong convexity does not, however, improve the minimax rate of the full system-level response ball. The ball contains T distinguishable response delays, and the √T log T lower bound remains valid for strongly convex costs. Thus strong convexity gives fast regret only when the policy class also limits the number of distinguishable response patterns. 

1.2 Relation to prior work 

The modern online-control line began with efficient regret guarantees against a strongly stable linear state-feedback class. Cohen et al. treated stochastic disturbances and adversarial quadratic costs [ 14 ]. Agarwal et al. then introduced the adversarial-disturbance setting now known as online nonstochastic control, obtaining nearly tight guarantees for general convex costs through disturbance–action control and online optimization with memory [ 4, 6]. Hazan, Kakade, and Singh extended sublinear regret to unknown dynamics, with a T 2/3 rate [ 22 ]. Spectral control improves the running-time dependence on the inverse stability margin while retaining comparable regret guarantees [ 12 ]. Our contribution for this benchmark is a sharp characterization of the stability-margin dependence. The decay-weighted norm separates the comparator’s margin from the fixed tracker’s impulse-response sums, and matching scalar lower bounds show that the factors γ−1 for globally Lipschitz costs and γ−2 for regular costs are unavoidable at fixed dimension, κ, and tracker impulse-response sums. Recent work has also questioned whether a fixed linear state-feedback policy is an adequate benchmark for general convex objectives and proposed constant inputs as an alternative [ 20 ]. We 5take a different route: the benchmark class is supplied as part of the problem, and our goal is to compete with that class without requiring common controller coordinates. The closest prior benchmark of linear dynamical controllers is due to Simchowitz, Singh, and Hazan, who obtained ˜O(√T ) known-system regret, including under partial observation [ 34 ]. Their class Π( ψ) requires every comparator’s sum of closed-loop response-block norms after delay m to be bounded by the same function ψ(m) → 0, and their algorithm chooses m so that ψ(m) is of order T −1. Other work fixes a finite number of disturbance–action response blocks in advance [ 29 ]. These guarantees cover every fixed finite-dimensional stable controller, as well as families sharing a specified tail bound or memory length, but not one system-level response ball with no common tail. System-level synthesis supplies the affine response constraints [ 37 ]. We optimize every response block that can affect the length-T trajectory, so no tail truncation is needed. To our knowledge, this is the first uniform guarantee for the no-tail class S(R), and its Θ( LW R √T log( eT ) ) rate is minimax optimal. The inclusion Kκ,γ ⊆ S(Rκ,γ ), with Rκ,γ proportional to γ−1, converts the response-ball bound into regret proportional to LW γ −1√T log( eT ) on the stable linear state-feedback class. Here the proportionality constants depend only on the fixed dimensions, κ, and tracker impulse-response sums. The class-specific algorithm removes the √log T factor, while the delayed-response lower bound shows that this factor is unavoidable for the larger response ball. System-level responses have also been used for offline and finite-horizon regret-optimal synthesis against clairvoyant input sequences [ 15 , 28 ]. Those minimax synthesis objectives differ from the sequential, fixed-policy regret studied here. Prior fast-rate results use stronger structure than our general policy-class theorem. Agarwal, Hazan, and Singh obtained polylogarithmic regret for strongly convex costs under well-conditioned stochastic excitation [ 3 ]. Foster and Simchowitz allowed arbitrary adversarial disturbances for known quadratic costs [ 16 ], while Simchowitz obtained polylogarithmic guarantees for response-parameterized linear control under strongly convex costs [ 33 ]. Our PAC-Bayes theorem addresses a different question: for any simulatable policy class whose state–input pairs have bounded diameter at every round, strong convexity controls both the cost of averaging policy trajectories and the cost of tracking their moving average. The delayed-response lower bound explains why PAC-Bayes aggregation cannot give fast regret for the unrestricted system-level response ball. Finally, exponential weighting and PAC-Bayes regret bounds with relative-entropy complexity are classical [ 25 , 13 ]; regret–variance refinements make the role of mixture variance explicit [ 36 ]. Earlier reductions connect tracking or control with expert prediction [ 1, 2]. Sequential-complexity frameworks treat arbitrary policy classes in more general stateful games [ 30 , 31 , 10 ]. Controller boosting aggregates black-box controllers under a uniform bounded-memory condition and assumes an oracle that returns a weak controller [ 5]. Counterfactual tracking instead aggregates counterfactual state–input trajectories and uses a stable physical controller to track their moving barycenter. Two complementary lines move beyond linear dynamical systems. Contractive-perturbation methods treat smooth nonlinear and time-varying systems through a shared convex policy pa-rameterization [ 27 ]. Online switching control selects from a finite black-box controller pool under unknown nonlinear dynamics and bandit feedback [ 26 ]. In our full-information, known-system setting, we can simulate every policy’s counterfactual trajectory and obtain sharp √T log N regret for a finite class without shared policy parameters. 

Organization. Section 2 defines the interaction and the fixed-realization benchmark. Section 3 gives the counterfactual-tracking reduction from physical regret to reference regret and cumulative reference defect. Section 4 develops the convex PAC-Bayes theorem. Section 5 derives sharp rates for the stable linear state-feedback class and then develops the no-tail system-level response result. 6Section 6 develops the strongly convex theory and its limits. Section 7 discusses computation and scope. The appendices collect the proofs in the same order. 

# 2 Model and fixed-realization regret 

We work with a known linear dynamical system and full-information costs. Fix a horizon T ,dimensions dx, d u, and matrices A ∈ Rdx×dx , B ∈ Rdx×du . The state starts from x1 = 0 and evolves according to 

xt+1 = Ax t + Bu t + wt, ∥wt∥ ≤ W. (6) On round t, the learner observes xt, chooses ut, and incurs ct(xt, u t). It then observes the full function ct and the next state, and can therefore reconstruct the disturbance as 

wt = xt+1 − Ax t − Bu t.

The environment may choose the current cost and disturbance as a function of the previously revealed history, but not of the current action or the learner’s fresh private randomization. Our upper bounds are pathwise: once the realized sequence is fixed, no probabilistic assumption remains. Because actions determine future states, a comparator must be evaluated by a complete coun-terfactual rollout. A causal policy π may use its own state and the previously revealed costs and disturbances. If it is randomized, we regard its random seed as part of its description. After the interaction, we freeze the realized sequences (ct, w t)t≤T and simulate π from xπ 

> 1

= 0 . Write 

yπt = ( xπt , u πt ) for the resulting state–input pair. For a policy class Π, define 

Reg T (Π) = 

> T∑
> t=1

ct(xt, u t) − inf 

> π∈Π
> T∑
> t=1

ct(xπt , u πt ). (7) Thus the comparator faces the same realized disturbances and cost functions as the learner, but evolves along its own state trajectory. The environment is not rerun against it. This fixed-realization convention is a pathwise strategy benchmark [ 19 ]; it differs from policy regret against a reactive adversary, where the environment is rerun under the comparator’s actions [ 7]. All lower bounds below use oblivious sequences. We equip state–input pairs with the product norm ∥(x, u )∥2 = ∥x∥2 + ∥u∥2. The results use three cost conditions for three distinct purposes. We use global Lipschitzness when no trajectory radius is available: it converts state–input tracking error directly into excess cost. For quadratic-type costs, we instead prove that every physical, reference, and comparator trajectory lies in a radius-D ball and use a gradient bound on that ball. Strong convexity supplies the variance decrease used to obtain logarithmic regret. A convex cost is globally L-Lipschitz if 

> ∣∣

ct(y) − ct(y′)∣∣ ≤ L ∥∥y − y′∥∥

for all y, y ′. It is (G, D )-regular if it is differentiable and ∥∇ ct(y)∥ ≤ GD on the radius-D ball; hence it is GD -Lipschitz there. It is μ-strongly convex on a convex set Y if 

ct(y′) ≥ ct(y) + 〈g, y ′ − y〉 + μ

2

> ∥∥

y′ − y∥∥2

for all y, y ′ ∈ Y and g ∈ ∂c t(y).The model now specifies how every policy is evaluated and the three cost conditions used below. We next isolate the deterministic relation between a learned reference trajectory and the physical trajectory that tracks it. 73 Counterfactual tracking 

A causal reference rule may change after every revealed cost and disturbance. Even if every candidate policy trajectory satisfies the plant dynamics, consecutive reference points chosen by different rules need not belong to one feasible trajectory. Counterfactual tracking measures this inconsistency by the one-step reference defect and converts cumulative defect into physical state–input tracking error. Fix a tracking gain K0 and write F0 = A − BK 0. Before observing ct, a causal reference rule chooses ¯yt = (¯ xt, ¯ut) from the revealed history. The physical controller tracks the reference using 

ut = ¯ ut − K0(xt − ¯xt). (8) The learner supplies ( ¯xt, ¯ut), and the physical controller adds the stabilizing correction −K0(xt − ¯xt).The reference need not satisfy the dynamics. Define its one-step defect by 

δt+1 = A¯xt + B ¯ut + wt − ¯xt+1 . (9) Subtracting the reference transition from the plant dynamics shows that the tracking error et = xt −¯xt

obeys 

et+1 = F0et + δt+1 . (10) Thus the stable matrix F0 filters the defect sequence. We use the following impulse-response sums to bound the cumulative state and state–input tracking errors: 

Λx(K0) = ∑

> r≥0

∥F r 

> 0

∥op ,

Λ( K0) = ∑

> r≥0

∥∥∥∥∥[

I

−K0

]

F r

> 0

∥∥∥∥∥op 

,B+ = ∥[A B ]∥op , Γtr = 1 + Λ( K0)B+.

(11) The gain Λx(K0) converts cumulative reference defect into cumulative state error, while Λ( K0)

converts it into cumulative state–input error. Moreover, ∥A∆x + B∆u∥ ≤ B+ ∥(∆ x, ∆u)∥, and 

Γtr = 1 + Λ( K0)B+ is the coefficient that appears when reference regret and excess tracking cost are combined. We assume Λ( K0) < ∞.Unrolling (10) and summing over time gives 

> T

∑

> t=1

∥yt − ¯yt∥ ≤ Λ( K0) 

> T−1

∑

> t=1

∥δt+1 ∥ , (12) when e1 = 0 . For globally L-Lipschitz costs, this immediately yields 

> T

∑

> t=1

[ct(yt) − ct(¯ yt)] ≤ LΛ( K0) 

> T−1

∑

> t=1

∥δt+1 ∥ . (13) 

Theorem 3.1 (Counterfactual-tracking reduction) . Suppose a causal reference rule satisfies, for every π ∈ Π, T∑

> t=1

ct(¯ yt) −

> T

∑

> t=1

ct(yπt ) ≤ RT (π), 

> T−1

∑

> t=1

∥δt+1 ∥ ≤ Dref  

> T

.

If the costs are globally L-Lipschitz, e1 = 0 , and Λ( K0) < ∞, then 

> T

∑

> t=1

ct(yt) −

> T

∑

> t=1

ct(yπt ) ≤ RT (π) + LΛ( K0)Dref  

> T

. (14) 8The theorem is a reduction from physical regret to two quantities: the reference-to-comparator regret RT (π), and the cumulative reference defect Dref  

> T

, whose contribution to excess tracking cost is LΛ( K0)Dref  

> T

. The policy class determines the attainable reference regret and defect bound; the plant enters through the tracker gain Λ( K0). The next section uses PAC-Bayes analysis to bound both quantities for a Gibbs barycenter. Later, for response-parameterized classes, mirror descent computes the reference by finite-dimensional convex optimization. The proof appears in section A. 

# 4 General policy classes under convex costs 

We first consider policy classes whose members need not share a parameterization. After each disturbance and cost are revealed, we simulate every policy on the realized history. Gibbs weights assign more mass to policies with smaller past counterfactual cost, and the controller tracks the weighted average of their current state–input pairs. This procedure gives a PAC-Bayes regret bound: every comparison distribution ρ over policies pays the complexity term KL( ρ∥ν).After round t, the algorithm must be able to update each policy’s counterfactual state using the revealed wt and ct, and then compute that policy’s action for round t + 1 . We call such a policy simulatable . We also require the counterfactual state–input pairs to have bounded pointwise diameter. For an infinite class, we additionally let (Π , F) be a standard Borel space with prior 

ν, assume that the trajectory and loss maps are measurable, and assume that the vector-valued integrals below exist. These conditions ensure that the Gibbs distribution and its barycenter are well defined. Because past costs and disturbances are fully revealed, every simulatable policy can be advanced before the next distribution is formed. Define 

pt(dπ ) = exp {− η ∑ 

> s<t

cs(yπs )} ν(dπ ) 

> ∫

exp {− η ∑ 

> s<t

cs(yπ′ 

> s

)} ν(dπ ′) , (15) and use the pointwise barycenter of its counterfactual trajectories as the reference: 

¯yt =

> ∫

yπt pt(dπ ). (16) This reference is causal because pt depends only on losses revealed before round t. The physical controller tracks it using (8). Two properties make the Gibbs reference analyzable. First, convexity bounds its cost by the Gibbs-weighted average of the counterfactual costs. Second, if the policy weights were held fixed from round t to round t + 1 , their barycenter would satisfy the plant dynamics: every policy sees the same disturbance, and the dynamics are affine. Hence the reference defect is caused only by the change from pt to pt+1 . Substitution into (9) gives the exact identity 

δt+1 =

> ∫

xπt+1 (pt − pt+1 )( dπ ). (17) Write ∥p − q∥TV = sup A |p(A) − q(A)| for total-variation distance. If the counterfactual state–input pairs have diameter at most ∆ at every round, then 

∥δt+1 ∥ ≤ B+∆ ∥pt − pt+1 ∥TV , (18) so the reference defect is small when successive Gibbs distributions are close. The update from pt

to pt+1 exponentially reweights the policies by the loss ct(yπt ). Because the loss range is at most 

L∆, exponential weighting controls both its PAC-Bayes regret and the change between successive distributions. 9Let ∆ denote the uniform pointwise diameter of the counterfactual state–input pairs: 

sup 

> t≤T

sup 

> π,π ′∈Π
> ∥∥∥

yπt − yπ′ 

> t
> ∥∥∥

≤ ∆. (19) This bounds the range of every counterfactual loss by L∆. The same disturbance drives every policy, so the next-state diameter is at most B+∆. Combining the PAC-Bayes bound for exponential weights with the counterfactual-tracking theorem yields the result below. We write ρ ≪ ν when ρ is absolutely continuous with respect to ν.

Theorem 4.1 (Convex PAC-Bayes counterfactual tracking) . Assume (19) , e1 = 0 , and Λ( K0) < ∞.For every t, suppose ct is convex and L-Lipschitz on the convex hull of the physical point yt and the essential range of {yπt : π ∈ Π}. There are universal constants c, C > 0 such that every η satisfying 

ηL ∆ ≤ c yields, simultaneously for every posterior ρ ≪ ν,

> T∑
> t=1

ct(yt) − Eπ∼ρT∑

> t=1

ct(yπt ) ≤ KL( ρ∥ν)

η + CηL 2∆2Γtr T. (20) For a finite class, take ν to be uniform and ρ = δπ for any comparator π. Then KL (δπ∥ν) = log N .Optimizing η, subject to the range condition in the theorem, gives the following bound. 

Corollary 4.2 (Finite policy classes) . Let Π be a finite class of N ≥ 2 policies, and suppose the assumptions of theorem 4.1 hold. Then 

Reg T (Π) ≤ CL ∆

> [√

Γtr T log N + log N

> ]

. (21) On a fixed scalar plant, the worst-case regret over classes of N policies is Ω( L∆√T min {log N, T }).When Γtr = O(1) , (21) is therefore sharp whenever log N = O(T ).If ν is nonatomic, a point mass δπ is not absolutely continuous with respect to ν. The theorem therefore compares directly with posterior averages, not with an individual policy. Regret against one policy can be obtained by assigning that policy positive prior mass or by covering the policy class and comparing with a nearby cover element. We do not develop a general covering argument here. Computing (16) may require integration over the entire policy class and need not be computa-tionally efficient. When each counterfactual trajectory is affine in finitely many response coefficients, mirror descent computes the reference through finite-dimensional convex optimization. We study such classes next. 

# 5 Online learning over closed-loop responses 

We now replace integration over a general policy class by finite-dimensional convex optimization. This is possible when each counterfactual trajectory is affine in a vector of closed-loop response coefficients. We first study the stable linear state-feedback class, whose common geometric decay permits a finite truncation and a weighted norm matched to that decay. We then remove the shared decay assumption and learn over a system-level response ball with bounded centered response gain. In both cases, an online convex learner updates the response coefficients, uses them to form the reference, and applies (8) on the physical plant. The no-tail system-level response theorem is the main result of this section. 10 5.1 The stable linear state-feedback class 

Let κ bound the gain and transient amplification, and let γ specify the geometric decay rate. For 

κ ≥ 1 and γ ∈ (0 , 1) , define 

Kκ,γ =

{

K ∈ Rdu×dx

∣∣∣ ∥K∥op ≤ κ, ∥(A − BK )r∥op ≤ κ2(1 − γ)r ∀r ≥ 0

}

. (22) We call Kκ,γ the stable linear state-feedback class . Each K ∈ K κ,γ defines the policy ut = −Kx t.Convolving the bounded disturbances with its geometrically decaying closed-loop response gives 

sup 

> t

∥∥∥xKt

∥∥∥ ≤ κ2Wγ , sup 

> t

∥∥∥uKt

∥∥∥ ≤ κ3Wγ . (23) The comparator gain and the tracking gain play different roles. The comparator is chosen in hindsight from Kκ,γ , whereas K0 is fixed in advance to track the moving reference. In addition to the quantities in (11), define 

ΛB (K0) = ∑

> r≥0

∥(A − BK 0)rB∥op .

This is the cumulative state response to an input perturbation. A square matrix is Schur stable 

if all its eigenvalues lie strictly inside the unit disk. To keep tracker conditioning separate from comparator stability, define 

C(A, B ) = inf   

> A−BK 0Schur stable

max {1, Λ( K0), ΛB (K0)}. (24) This quantity is a property of the plant, not of a particular comparator, and is finite whenever Kκ,γ 

is nonempty. We fix a tracker satisfying 

max {1, Λ( K0), ΛB (K0)} ≤ 2C(A, B ). (25) Accordingly, Csys denotes a constant that may depend on the fixed dimensions, κ, and C(A, B ), but not on T, W, γ, L, G , or μ. The notation Osys (·) uses the same convention. The loss K 7 → ct(yKt ) is generally nonconvex, so an online convex algorithm cannot update the feedback gain directly. Relative to the tracker, however, each K ∈ K κ,γ admits disturbance–action coefficients 

M [i] 

> K

= ( K0 − K)( A − BK )i−1, i ≥ 1. (26) After truncating at Hγ , the learner applies 

ut = −K0xt +

> Hγ

∑

> i=1

M [i] 

> t

wt−i. (27) This is the standard disturbance–action parameterization. We update its coefficients in a norm that offsets their geometric decay. Put ρ2 = 1 − γ and define 

∥M ∥2 

> ρ

=

> Hγ

∑

> i=1

ρ−2( i−1) ∥∥∥M [i]∥∥∥2F . (28) The weight ρ−(i−1) cancels the decay of M [i] 

> K

. Consequently, every embedded comparator lies in a ball of radius O(γ−1/2), while changing the coefficients by one unit in this norm changes the 11 counterfactual state–input pair by at most O(W γ −1/2). The first quantity is the comparator radius; the second is the sensitivity of the trajectory to the learned coefficients. Their product is O(W γ −1).We refer to this matched primal–dual scaling as the response geometry. It gives the W γ −1√T regret scale without an additional factor depending on Hγ . To make the algorithm explicit, let yMt be the counterfactual trajectory generated by a fixed response vector M , and set ft(M ) = ct(yMt ). Because 

yMt is affine in M , the loss ft is convex. Projected gradient descent over a weighted ball containing every truncated embedding (26) takes the form 

Mt+1 = Proj ρRγ (Mt − ηg t), gt ∈ ∂ρft(Mt).

Both the projection and the subgradient are taken with respect to the weighted inner product induced by ∥·∥ ρ. The update from Mt to Mt+1 makes successive reference points dynamically inconsistent; (10) filters the resulting reference defect through the stable matrix F0. The ball radius, update conventions, and explicit constants appear in section D. 

Theorem 5.1 (Convex regret for the stable linear state-feedback class) . Assume Kκ,γ ̸ = ∅, and choose K0 as in (25) . Choose Hγ as in section D; in particular, 

Hγ = O

> (

γ−1 log 

> (

2 + Tγ

> ))

.

The weighted-response controller satisfies: (i) for globally L-Lipschitz convex costs, 

Reg T (Kκ,γ ) ≤ Csys LW γ −1√T ; (29) 

(ii) every compared trajectory lies in a ball of radius Dγ ≤ Csys W/γ , and for (G, D γ )-regular costs, 

Reg T (Kκ,γ ) ≤ Csys GW 2γ−2√T . (30) The next lower bound matches both upper bounds: it gives LW γ −1√T for globally Lipschitz costs and GW 2γ−2√T for regular costs. The construction uses a fixed scalar plant on which the tracker eliminates state error in one step. 

Theorem 5.2 (Fixed-plant convex lower bound) . There are universal constants c, C > 0 with the following property. Fix γ ∈ (0 , 1/2] and T ≥ C/γ . On the scalar plant xt+1 = ut + W , there is an oblivious distribution over globally L-Lipschitz convex costs for which every causal randomized controller has expected regret at least 

cLW γ −1√T . (31) 

There is also an oblivious distribution over (G, D )-regular costs, with D = 2 W/γ , for which the expected regret is at least 

cGW 2γ−2√T . (32) At fixed dimension, κ, and tracker conditioning, theorems 5.1 and 5.2 match in T, W, γ . In particular, the lower bound identifies γ−1 and γ−2 as the sharp stability-margin dependence in the two cost regimes. The implementation uses Hγ dudx parameters and runs in time polynomial in Hγ , d x, d u. Under additional spectral assumptions, online spectral control can compress this response representation [ 12 ]; we do not impose those assumptions here. Thus the theorem preserves the square-root horizon dependence established by disturbance–action control [ 4 ], while the lower bound pins down the dependence on the disturbance scale and stability margin. 12 5.2 Online control over system-level responses without a common tail 

The result for the stable linear state-feedback class uses a common geometric decay bound to choose one truncation length for the entire class. Prior response-learning methods likewise choose a truncation length from a shared stability condition or fix the number of response blocks in advance [4, 34 , 29 ]. We instead bound the sum of the response-block deviations from a fixed tracker. As shown below, a radius R limits the state–input deviation from the tracker to W R , but places no restriction on the delay at which that deviation occurs. System-level synthesis describes the resulting class through its disturbance-response matrices. A zero-initialized causal linear time-invariant controller maps past disturbances to states and actions as follows: 

xΦ 

> t

=

> t−1

∑

> i=1

Φ[i] 

> x

wt−i,uΦ 

> t

=

> t−1

∑

> i=1

Φ[i] 

> u

wt−i,

(33) where 

Φ[1]  

> x

= I, Φ[i+1]  

> x

= AΦ[i] 

> x

+ BΦ[i] 

> u

. (34) Equation (34) is the affine state-feedback system-level synthesis constraint [ 37 ]. The tracker K0

induces the baseline response 

Φ[i]0,x = F i−10 , Φ[i]0,u = −K0F i−10 .

Write Φ[i] = [(Φ [i] 

> x

)⊤, (Φ [i] 

> u

)⊤]⊤, and measure the centered response by 

r0(Φ) = ∑

> i≥1

∥∥∥Φ[i] − Φ[i]0

∥∥∥F , S(R) = {Φ : (34) , r0(Φ) ≤ R}. (35) We call r0(Φ) the centered response gain and S(R) the system-level response ball of centered gain 

R. The name records an operational bound: for disturbances satisfying ∥wt∥ ≤ W ,

∥∥∥yΦ 

> t

− yΦ0

> t

∥∥∥ ≤ W r0(Φ) ≤ W R. (36) Thus the radius bounds the state–input deviation from the fixed tracker. It does not bound the delay at which that deviation occurs. The elements of S(R) are absolutely summable stable responses, but they need not share a decay rate or controller order. In particular, the ball is not restricted to static linear state-feedback policies. 

Proposition 5.3 (Scope of the system-level response ball) . Every finite-dimensional, zero-initialized, internally stable linear time-invariant dynamical controller with state feedback belongs to S(R) for some finite R. Conversely, each Φ ∈ S(R) admits an internally stabilizing system-level synthesis realization, possibly of infinite dimension. For the stable linear state-feedback class, 

Kκ,γ ⊆ S(Rκ,γ ), Rκ,γ = κ2∆κ,γ, 0

γ

(1 + χ0Λ0,B 

), (37) 

where ∆κ,γ, 0 = sup K∈K κ,γ ∥K − K0∥F, χ0 =

√

1 + ∥K0∥2op , and Λ0,B = ∑ 

> r≥0

∥F r 

> 0

B∥op . Hence 

Rκ,γ = Osys (γ−1) at fixed conditioning. 

13 The inclusion Kκ,γ ⊆ S(Rκ,γ ) calibrates the response-ball radius against the stable linear state-feedback class. The radius controls the summed deviation from the tracker, not how that deviation is distributed across delays. On the scalar plant below, all centered response mass can occur after any prescribed delay. Consequently, no memory length chosen from R alone can approximate the class uniformly. 

Proposition 5.4 (No uniform response tail) . On a fixed scalar plant, for every R > 0 and h ≥ 0,

sup 

> Φ∈S(R)
> ∑
> i>h
> ∥∥∥

Φ[i] − Φ[i]0 

> ∥∥∥F

= R. (38) 

Hence S(R) admits no uniform tail envelope tending to zero. 

Theorem 5.4 shows why the guarantee of Simchowitz, Singh, and Hazan does not cover the full class S(R) [ 34 ]. Their theorem assumes a common tail envelope ψ(h) → 0 for all comparators and chooses the memory length from that envelope. No such envelope exists for S(R): although each fixed finite-dimensional stable controller has a decaying tail, the class permits its centered response mass to occur at an arbitrarily large delay. At horizon T , response blocks after delay T − 1 cannot affect any incurred cost. We therefore optimize all blocks 1, . . . , T − 1, with no tail-truncation error. Set H = T − 1 and write 

Q[i] = Φ [i] − Φ[i]0 , 1 ≤ i ≤ H. 

The centered response recursion defines a linear subspace VH . To allow response mass at any of the 

H delays without paying a √H factor, choose 

p = 1 + 1log( eH ) , ∥Q∥p, 2 = 

> (H∑
> i=1
> ∥∥∥

Q[i]∥∥∥p

> F
> )1/p

. (39) Then H∑

> i=1
> ∥∥∥

Q[i]∥∥∥F ≤ e ∥Q∥p, 2 . (40) This mixed block norm is within a constant factor of the block-ℓ1 norm, while 12 ∥Q∥2 

> p, 2

is 1/ log (eH )-strongly convex with respect to ∥·∥ p, 2. Consequently, the mirror-descent bound contains 1/√p − 1 =

> √

log( eH ), while (40) prevents a √H dependence. Mirror descent on 

QH (R) = {Q ∈ V H : ∥Q∥p, 2 ≤ R}

therefore pays only √log T for allowing the response to occur at an arbitrary delay. Every Φ ∈ S(R)

has its first H centered blocks in QH (R). For Q ∈ Q H (R), define the exact counterfactual prefix 

¯yt(Q) = 

> t−1∑
> i=1
> (

Φ[i]0 + Q[i])wt−i, ft(Q) = ct

> (

¯yt(Q)). (41) On round t, mirror descent updates Qt using ft, the learner forms the reference ¯yt(Qt), and the physical controller applies (8) . The first T − 1 blocks represent every comparator in S(R) exactly through round T , so the regret bound contains no tail-approximation term. The selected prefix need not extend to an infinite stable response: only its first T − 1 blocks enter the finite-horizon reference, while the fixed stable tracker controls the physical tracking error. 14 Theorem 5.5 (Convex regret over the system-level response ball) . For T ≥ 2 and globally L-Lipschitz convex costs, exact-prefix mirror descent with counterfactual tracking satisfies 

Reg T (S(R)) ≤ eLW R 

> √

T (1 + 2 χ0Λx(K0)) log( eT ). (42) 

For (G, D R)-regular costs, the same bound holds with L = GD R, where 

DR = W [S0 + eR (1 + 2 χ0Λx(K0))] , S0 = ∑

> i≥1
> ∥∥∥

Φ[i]0 

> ∥∥∥F

. (43) The √log T factor is not an artifact of the mirror-descent analysis. Delayed responses force the same factor on a scalar plant. 

Theorem 5.6 (Matching delayed-response lower bound) . There is a universal c > 0 with the following property. Fix T ≥ 2 and L, W, R > 0. On the scalar plant xt+1 = ut + wt, with K0 = 0 ,there is an oblivious distribution over disturbances and globally L-Lipschitz convex costs. Almost surely |wt| ≤ W , and every causal randomized controller satisfies 

E Reg T (S(R)) ≥ cLW R 

> √

T log( eT ). (44) 

At the natural regularity radius D ≍ W (1 + R), the corresponding fixed-plant minimax rate for 

(G, D )-regular costs is 

Θ

> (

GW 2R(1 + R)

> √

T log( eT )

> )

. (45) For every R > 0, the class S(R) contains nonstatic delayed finite-impulse-response controllers. Moreover, whenever R ≥ Rκ,γ , it contains the entire stable linear state-feedback class Kκ,γ . Taking 

R = Rκ,γ = Osys (γ−1) in (42) gives Osys (LW γ −1√T log( eT ) ) regret over this larger response class. The delayed-response lower bound shows that the additional √log T factor is unavoidable for the full response ball. The exact-prefix problem has O(T (dx + du)dx) variables; it is a finite-horizon convex program, but its per-round running time grows with T .These results assume only convexity. We next ask when strong convexity can reduce regret from order √T to order log T , and when the size of the response class prevents such an improvement. 

# 6 Strongly convex costs 

The Gibbs reference is an average of counterfactual state–input pairs. Strong convexity makes the cost of this average smaller than the average counterfactual cost by a term proportional to their variance. Let pt be the Gibbs distribution in (15), let ¯yt be its barycenter, and define 

Vt = Eπ∼pt ∥yπt − ¯yt∥2 . (46) Strong convexity strengthens Jensen’s inequality by the variance term 

ct(¯ yt) ≤ Eπ∼pt ct(yπt ) − μ

2 Vt. (47) The variance-sensitive exponential-weights inequality contributes at most CηL 2Vt on round t. The update from pt to pt+1 changes the next-state barycenter by at most CηB +LV t; the tracker converts this change into at most CηL 2Λ( K0)B+Vt excess cost. Hence the two positive terms total at most 

CηL 2Γtr Vt. If η = O(μ/ (L2Γtr )) , this sum is no larger than the strong-convexity decrease μV t/2 in (47). 15 Earlier logarithmic-regret results require additional control structure: well-conditioned stochastic excitation [ 3 ], known quadratic costs [ 16 ], or a finite response parameterization [ 33 ]. The theorem below instead applies to any simulatable policy class satisfying the stated measurability and integrability conditions. As in regret–variance bounds for online learning [ 36 ], trajectory variance controls the exponential-weighting term; here the same variance also controls the excess cost of tracking the changing Gibbs reference. 

Theorem 6.1 (Strongly convex PAC-Bayes counterfactual tracking) . Let (Π , ν ) satisfy the measur-ability and integrability conditions preceding theorem 4.1, and assume e1 = 0 and Λ( K0) < ∞. For every t, suppose ct is L-Lipschitz and μ-strongly convex on 

conv( {yt} ∪ { yπt : π ∈ Π}) .

There is a universal c > 0 such that every η satisfying 

η ≤ c μL2Γtr 

, (48) 

yields, simultaneously for every ρ ≪ ν,

> T∑
> t=1

ct(yt) − Eπ∼ρT∑

> t=1

ct(yπt ) ≤ KL( ρ∥ν)

η . (49) 

In particular, with the uniform prior, the algorithm has regret at most 

C L2Γtr 

μ log N. (50) 

When Γtr = O(1) and log N = O(T ), the L2μ−1 log N dependence matches the fixed-scalar-plant lower bound. 

If the prior is nonatomic, an individual policy corresponds to a point mass and cannot be used directly in theorem 6.1. For the stable linear state-feedback class, we therefore construct a finite Frobenius-norm cover of the gain matrices and run Gibbs tracking on that cover. The logarithm of the cover size scales with dK = dudx, rather than with the dimension of a truncated disturbance response. The proof chooses an ε-net of Kκ,γ , applies the finite-class PAC-Bayes bound to the net, and bounds the trajectory error between each gain and its nearest net point. Here Xγ bounds comparator states, Dsc bounds all state–input points on which the costs are evaluated, LK is the Lipschitz constant of the map from a gain to its counterfactual trajectory, and Asc multiplies the logarithm of the cover size: 

Xγ = κ2Wγ ,Dsc =

> (√

1 + κ2 + 2Λ( K0)

> )

Xγ ,LK = κ2Wγ + (1 + κ)κ4 ∥B∥op Wγ2 ,

Asc = L2

μ

> (

1 + Λ( K0)B+

> )

, L = GD sc .

(51) A cover in gain space induces a trajectory cover through the estimate 

sup 

> t
> ∥∥∥

yKt − yK′ 

> t
> ∥∥∥

≤ LK

> ∥∥

K − K′∥∥F . (52) 16 Theorem 6.2 (Strongly convex regret for the stable linear state-feedback class) . Suppose the costs are (G, D sc )-regular and μ-strongly convex on the radius-Dsc ball. Let RK = κ√min {dx, d u}. There is a counterfactual-tracking algorithm based on a finite cover such that 

Reg T (Kκ,γ ) ≤ CAsc dK log 

> (

4 + 2RK T LL K

Asc dK

> )

. (53) 

With the conditioned tracker (25) ,

Reg T (Kκ,γ ) ≤ Csys 

G2W 2

μγ 2 dK log 

> (

4 + Csys μT Gd K γ

> )

. (54) An ε-net contributes a PAC-Bayes term proportional to the logarithm of its cardinality, 

dK log (RK /ε ), while replacing a gain by its nearest net point costs at most T LL Kε. Optimiz-ing ε gives the logarithm in (53) . The cover may contain exponentially many gains, so direct implementation is not polynomial in dK .At fixed dimension and plant conditioning, and with G/μ bounded, the next lower bound matches the upper bound in T, W, γ .

Theorem 6.3 (Strongly convex lower bound for the stable linear state-feedback class) . There are universal constants c, C > 0 and a fixed one-state, two-input plant admitting a one-step tracker such that, for every W, μ > 0, γ ∈ (0 , 1/4] , and T ≥ C/γ , there is an oblivious distribution over quadratic costs satisfying: (i) every cost is globally μ-strongly convex; (ii) on the ball of radius D = 4 W/γ , every gradient has norm at most 2μD ;(iii) every causal randomized controller satisfies 

E Reg T (K1,γ ) ≥ cμW 2γ−2 log T. (55) At fixed dimension and plant conditioning, with G/μ bounded, theorems 6.2 and 6.3 agree in 

T, W, γ . Whether the factor dK in the upper bound is necessary remains open. Combined with a finite trajectory cover and a Lipschitz approximation bound, the PAC-Bayes theorem gives logarithmic regret when the logarithm of the cover size is small. The constraint 

r0(Φ) ≤ R alone does not give a horizon-independent trajectory cover: at horizon T , the delayed construction produces T separated response patterns. The next theorem shows that distinguishing among these delays costs √T log T regret even when every cost is strongly convex. 

Theorem 6.4 (Strong convexity does not improve the response-ball rate) . There are universal constants c > 0 and T0 with the following property. Fix T ≥ T0, W, R, μ > 0, and G ≥ 4μ, and set D = W (1 + 3 eR ). On the scalar plant xt+1 = ut + wt, there is an oblivious distribution over disturbances and costs. Almost surely |wt| ≤ W , and every cost is globally μ-strongly convex and 

(G, D )-regular; moreover, every causal randomized controller satisfies 

E Reg T (S(R)) ≥ cGDW R 

> √

T log( eT ). (56) 

On this scalar plant, the lower bound matches the bounded-region upper bound of theorem 5.5 up to universal constants. 

Strong convexity yields logarithmic regret for finite or suitably coverable response families. For the full system-level response ball, however, the learner must distinguish among T possible delay locations, and the lower bound shows that strong convexity does not reduce the resulting √T log T

rate. 17 7 Discussion 

Counterfactual tracking learns from simulated state–input trajectories rather than from controller parameters. On each round, the learner simulates the candidate policies on the revealed cost and disturbance history, aggregates their current counterfactual points into a reference, and applies a stable tracker to that reference. Physical regret is reduced to reference-to-comparator regret and the excess cost caused by one-step reference defects. The policies need not share a parameterization. Two properties of a response class enter the bounds. Centered response gain bounds trajectory size: r0(Φ) ≤ R and ∥wt∥ ≤ W imply a state–input deviation of at most W R from the tracker. A common decay envelope or a small trajectory cover bounds the number of response patterns the learner must distinguish: the decay envelope gives a finite truncation, while the logarithm of the cover size enters the PAC-Bayes bound. Geometric decay supplies both properties. The decay-weighted norm in (28) then makes both the comparator radius and the trajectory sensitivity to the response coefficients proportional to γ−1/2. Bounded centered response gain supplies only the first property. Exact-prefix mirror descent still competes with that larger class, but the delayed-response lower bound shows that arbitrary delay locations necessarily add √log T .Three limitations remain. First, a large trajectory cover increases the PAC-Bayes complexity term and can force large regret. Second, computing a Gibbs barycenter may require integration over the entire policy class, while exact-prefix mirror descent optimizes O(T (dx + du)dx) variables. Third, for nonlinear dynamics, an average of feasible policy trajectories need not itself be feasible under the same disturbance. In that case, even a fixed mixture can create reference defect. These limitations leave two questions. Which policy classes admit computationally efficient trajectory aggregation while preserving bounds on cumulative reference defect or trajectory variance? For nonlinear systems, when can several policy trajectories be combined into a reference with quantitatively bounded one-step defect? 

# A Counterfactual tracking 

We first prove the pathwise reduction used throughout the paper: physical regret is at most reference-to-comparator regret plus LΛ( K0) times the cumulative reference defect. Iterating (10) from e1 = 0 

gives 

et =

> t−1∑
> s=1

F t−1−s 

> 0

δs+1 .

Since yt − ¯yt = ( et, −K0et), the triangle inequality and an interchange of two finite sums yield 

> T∑
> t=1

∥yt − ¯yt∥ ≤  

> T−1∑
> s=1

∥δs+1 ∥ 

> T−1−s∑
> r=0
> ∥∥∥∥∥[

I

−K0

> ]

F r

> 0
> ∥∥∥∥∥op

≤ Λ( K0) 

> T−1∑
> s=1

∥δs+1 ∥ .

This proves (12) . Applying Lipschitz continuity on each round gives (13) . Adding and subtracting  

> ∑
> t

ct(¯ yt) then proves theorem 3.1. We next compute the reference defect created by a changing distribution. Let qt be any probability distribution on policies and let ¯yt = ∫ yπt qt(dπ ). Because every counterfactual trajectory obeys the same affine dynamics, 

δt+1 =

> ∫

xπt+1 (qt − qt+1 )( dπ ). (A.1) 18 In particular, a fixed distribution has zero defect. Linearity of the plant makes the defect depend only on the change from qt to qt+1 ; the policies themselves need not be linear. For Gibbs weights, we therefore need two bounds: a PAC-Bayes bound for the reference cost and a bound on the change between successive distributions. We prove both next. 

# B PAC-Bayes bounds for general policy classes 

The Gibbs analysis uses two properties of exponential weights: its cumulative loss relative to any posterior, and the amount by which one loss update changes the distribution. All integrals below are with respect to the policy variable. All diameter assumptions are understood essentially with respect to ν. Because the horizon is finite and pt ≪ ν for every t, there is a single ν-full set on which the required bounds hold at every round. We restrict all subsequent integrals to this set. 

B.1 Two exponential-weights inequalities 

Let ℓ1, . . . , ℓ T be bounded measurable functions on a standard Borel space with prior ν, and define 

pt(dπ ) ∝ exp 

{

−η ∑

> s<t

ℓs(π)

}

ν(dπ ).

Write at = ess sup ℓt − ess inf ℓt, and let 

Zt =

∫

exp 

{

−η ∑

> s<t

ℓs(π)

}

ν(dπ ).

The proof first bounds each exponential-weights update through the ratio Zt+1 /Z t, then compares the prior ν with an arbitrary posterior ρ through KL( ρ∥ν). Hoeffding’s lemma gives 

− 1

η log Zt+1 

Zt

= − 1

η log Ept e−ηℓ t ≥ Ept ℓt − ηa 2

> t

8 .

Summing this display telescopes the normalizers. The Donsker–Varadhan variational inequality then bounds 

− 1

η log ZT +1 ≤ EρT∑

> t=1

ℓt + KL( ρ∥ν)

η .

Together these estimates give, for every ρ ≪ ν,

> T

∑

> t=1

Ept ℓt − EρT∑

> t=1

ℓt ≤ KL( ρ∥ν)

η + η

8

> T

∑

> t=1

a2 

> t

. (B.1) The variational inequality is valid with value +∞, so no separate integrability argument is needed when KL( ρ∥ν) = ∞.We shall also need a variance-sensitive version. If ηa t ≤ 1/4, then 

> T

∑

> t=1

Ept ℓt − EρT∑

> t=1

ℓt ≤ KL( ρ∥ν)

η + Cη 

> T

∑

> t=1

Var pt (ℓt). (B.2) For each t, apply ez ≤ 1 + z + Cz 2 to z = −η(ℓt − Ept ℓt). This replaces the range penalty ηa 2 

> t

/8 in the one-step bound by Cη Var pt (ℓt). Summing over t and applying the Donsker–Varadhan inequality 19 above gives (B.2) . Both inequalities hold pathwise and therefore remain valid when the realized loss sequence is selected adaptively from the revealed history. We need two estimates on how far one exponential update moves a distribution: a range bound for convex costs and a variance bound for strongly convex costs. Interpolate from pt to pt+1 by 

pt,s (dπ ) = e−sηℓ t(π)pt(dπ )

∫ e−sηℓ t(π′)pt(dπ ′) , 0 ≤ s ≤ 1.

Differentiation under the integral gives, for every bounded Bochner integrable Hilbert-valued map 

h(π),

dds Ept,s h = −ηEpt,s 

[(h − Ept,s h)( ℓt − Ept,s ℓt)]. (B.3) Write ∥·∥ TV for total-variation distance, normalized to lie in [0 , 1] . For a measurable set A, apply (B.3) with h = 1A, integrate over s ∈ [0 , 1] , and take the supremum over A. Since the range of ℓt is 

at, this gives 

∥pt+1 − pt∥TV ≤ Cηa t. (B.4) If ηa t ≤ 1/4, the density ratio of pt,s to pt is bounded above and below by universal constants. Suppose z(π) and h(π) are Hilbert-valued, ∥h(π) − h(π′)∥ ≤ b ∥z(π) − z(π′)∥, and ℓt(π) = ct(z(π)) 

for an L-Lipschitz function ct. Cauchy–Schwarz and the two Lipschitz conditions give 

∥∥Cov pt,s (h, ℓ t)∥∥ ≤

√

Var pt,s (h) Var pt,s (ℓt)

≤ bL Var pt,s (z).

(B.5) The bounded density ratio implies Var pt,s (z) ≤ C Var pt (z). Integrating (B.3) over s ∈ [0 , 1] therefore gives ∥∥Ept+1 h − Ept h∥∥ ≤ CηbL Ept ∥z − Ept z∥2 , (B.6) where we used the pairwise identities 

Var q(h) = 12

∫∫ ∥∥h(π) − h(π′)∥∥2 q(dπ )q(dπ ′), Var q(ℓt) ≤ L2 Var q(z),

and the bounded density ratios along the interpolation. 

B.2 Convex costs 

Proof of theorem 4.1. Set ℓt(π) = ct(yπt ). By (19), at ≤ L∆. Convexity and (16) give 

ct(¯ yt) ≤ Ept ℓt. (B.7) Substituting (B.7) into (B.1) bounds the reference cost relative to every posterior ρ.We next bound the reference defect created by the update from pt to pt+1 . From (A.1), center 

xπt+1 at any point in its essential range. Since 

∥∥∥xπt+1 − xπ′

> t+1

∥∥∥ =

∥∥∥A(xπt − xπ′ 

> t

) + B(uπt − uπ′ 

> t

)

∥∥∥ ≤ B+∆,

equation (B.4) implies 

∥δt+1 ∥ ≤ CηLB +∆2.

The excess physical cost over the reference cost is therefore at most 

CηL 2Λ( K0)B+∆2T. 

Adding this term to (B.1) and using Γtr = 1 + Λ( K0)B+ proves (20). We now specialize the PAC-Bayes bound to a finite policy class and prove its matching lower bound. The variance-sensitive inequalities above then give the strongly convex result. 20 C Finite-class consequences and strongly convex aggregation 

Proof of theorem 4.2. For the uniform prior and the point mass at policy i, KL (δi∥ν) = log N .Apply theorem 4.1 with 

η = c min  

> {

1

L∆ ,

> √

log NL2∆2Γtr T

> }

.

Taking the minimum over i gives (21). Both finite-class lower bounds use sign sequences for which one codeword has large correlation with an independent Rademacher sequence. 

Lemma C.1 (Temporal sign code) . For every m ≥ 1 and N ≥ 2, there are N sign strings 

s1, . . . , s N ∈ {− 1, +1 }m, with repetitions allowed and with the set of distinct strings closed under sign reversal, such that for independent uniform signs σ1, . . . , σ m,

E max 

> i≤Nm∑
> t=1

σtsit ≥ c

> √

m min {log N, m }. (C.1) 

Repeated strings may be replaced by policies that agree through time m and differ thereafter. Proof. For N ≤ ec0m, sample ⌊N/ 2⌋ independent uniform sign strings and include their negatives. Conditional on σ, the absolute inner products are independent copies of the absolute value of a length-m symmetric random walk. The binomial moderate-deviation lower bound implies that their maximum exceeds c√m log N with probability bounded below by a universal constant. Averaging over the random codebook produces a deterministic code. For larger N , retain a code of size ⌊ec0m⌋

and repeat strings until the collection has N members; the bound then has order m. For odd N ,duplicate one string after forming the sign-reversed pairs. Neither duplication changes the maximum. Adjusting constants covers N = 2 .

Proposition C.2 (Finite-class convex lower bound) . For a fixed scalar plant and every N ≥ 2,there is a class of N causal policies with trajectory diameter at most ∆ and an oblivious distribution over globally L-Lipschitz convex costs such that every causal randomized controller has expected regret at least 

cL ∆

> √

T min {log N, T }.

Proof. Take A = B = 0 , zero disturbances, and r = ∆ /2, so the state is identically zero. Apply theorem C.1 with m = T , and let the policies be uit = rs it. Draw independent uniform signs σt

before play and put 

ct(x, u ) = L

2 σtu + L

2 dist( u, [−r, r ]) .

The cost is convex and globally L-Lipschitz. Since the learner chooses ut before seeing σt, its expected stage cost is nonnegative. The distance term vanishes on every comparator, and sign symmetry gives 

E min 

> iT∑
> t=1

ct(0 , u it) ≤ − cLr 

> √

T min {log N, T }.

This proves the claim after absorbing the factor r = ∆ /2.21 C.1 Strongly convex costs 

The convex lower bound uses only the range-sensitive exponential-weights inequality. Under strong convexity, the variance-sensitive inequality also controls the reference defect and gives logarithmic regret. If a cost is L-Lipschitz and μ-strongly convex on a convex comparison region, that region has bounded diameter. Indeed, for any y, y ′ in the region and m = ( y + y′)/2,

μ

8

> ∥∥

y − y′∥∥2 ≤ ct(y) + ct(y′)2 − ct(m) ≤ L

2

> ∥∥

y − y′∥∥ .

Thus ∥∥y − y′∥∥ ≤ 4Lμ . (C.2) 

Proof of theorem 6.1. Again write ℓt(π) = ct(yπt ). Let Vt = Ept ∥yπt − ¯yt∥2. By (C.2) , the loss range is at most 4L2/μ . Hence a sufficiently small universal constant in (48) ensures the range condition required for (B.2) and (B.6). The pairwise variance identity gives 

Var pt (ℓt) ≤ L2Vt. (C.3) Strong convexity and ¯yt = Ept yπt give 

ct(¯ yt) ≤ Ept ℓt − μ

2 Vt,

which is (47). Apply (B.6) with z(π) = yπt and h(π) = xπt+1 . The affine dynamics imply ∥h(π) − h(π′)∥ ≤ 

B+ ∥z(π) − z(π′)∥, and therefore 

∥δt+1 ∥ ≤ CηB +LV t. (C.4) Substituting (C.4) into (13) bounds the excess physical cost over the reference cost by 

CηL 2Λ( K0)B+

> T∑
> t=1

Vt.

Combining this estimate with (B.2), (C.3), and (47) yields 

> T∑
> t=1

ct(yt) − EρT∑

> t=1

ct(yπt ) ≤ KL( ρ∥ν)

η

+

> [

− μ

2 + CηL 2Γtr  

> ]T∑
> t=1

Vt.

The prescribed step size makes the bracket nonpositive, proving (49) . The finite-class conclusion follows by taking the uniform prior, a point-mass posterior, and η = cμ/ (L2Γtr ).The lower bound below requires Lipschitzness only on the bounded region containing the learner and comparator trajectories. Global Lipschitzness is impossible for a globally strongly convex function on an unbounded Euclidean space. 

Proposition C.3 (Finite-class strongly convex lower bound) . For q = min {log N, T }, a fixed scalar plant admits N policies and oblivious μ-strongly convex quadratic costs that are L-Lipschitz on the comparison region and force regret at least c(L2/μ )q.

22 Proof. Take A = B = 0 , zero disturbances, and choose a > 0. Let ct(x, u ) = μ 

> 2

[x2 + ( u − aσ t)2],where the σt’s are independent uniform signs drawn before play. Put r = c0a√q/T / 2, apply theorem C.1 with m = T , and use the policies uit = rs it. Projection of a learner’s action onto [−a, a ]

can only decrease every stage cost, so it is enough to consider physical actions in that interval. On the resulting comparison region the costs are L-Lipschitz with L ≤ 2μa .The fresh sign has mean zero conditional on the learner’s past, whereas the best codeword correlates with the complete sign sequence. Expanding the squares gives 

E Reg T ≥ μar E max 

> iT

∑

> t=1

σtsit − μT r 2

2

≥ cμa 2q ≥ c′ L2

μ q, 

after fixing the universal constant c0 sufficiently small and taking L = 2 μa .This completes the general-policy PAC-Bayes analysis. We next prove the computational convex-cost result for the stable linear state-feedback class. 

# D The stable linear state-feedback class under convex costs 

We prove the upper bound in two steps. First, we analyze projected gradient descent in a decay-weighted response norm and bound the cost of tracking its changing reference. Second, we embed each comparator in Kκ,γ and truncate its response. We then prove the lower bound on a scalar plant. We first justify the conditioning convention used in the theorem. The r = 0 terms in Λ( K0) and 

ΛB (K0) control ∥K0∥op and ∥B∥op , respectively. The r = 1 term in Λ( K0) controls ∥F0∥op , and 

A = F0 + BK 0 then controls ∥A∥op and B+. Moreover, 

∆K ≤

√

min {dx, d u} (κ + ∥K0∥op 

).

Thus every tracker constant below is bounded in terms of the fixed dimensions, κ, and C(A, B )

under (25) . If the stable class is nonempty, any K ∈ K κ,γ is Schur stable and has finite Λ( K) and 

ΛB (K), so the infimum defining C(A, B ) is finite. We now construct the weighted response class used in theorem 5.1. Fix a horizon H, write 

ρ = √1 − γ, and, for M = ( M [1] , . . . , M [H]), define 

vt(M ) = 

> H

∑

> i=1

M [i]wt−i,xMt+1 = F0xMt + Bv t(M ) + wt,uMt = −K0xMt + vt(M ), xM 

> 1

= 0 ,

(D.1) where ws = 0 for s ≤ 0. The loss ft(M ) = ct(xMt , u Mt ) is convex because the displayed trajectory is affine in M . Use the weighted norm in (28) and set 

S2 

> ρ,H

=

> H

∑

> i=1

ρ2( i−1) ,C20 = Λ B (K0)2 + (1 + ∥K0∥op ΛB (K0))2,

Ψ20 = C20 + 2Λ( K0)Λ B (K0)C0.

(D.2) 23 Let ∆K = sup K∈K κ,γ ∥K − K0∥F and 

Rγ = κ2∆K

√γ . (D.3) Starting from M1 = 0 , the algorithm applies 

ut = −K0xt + vt(Mt) (D.4) and, after observing ct and recovering wt, performs projected gradient descent on ft over {M :

∥M ∥ρ ≤ Rγ }, with projection and gradient taken in the weighted inner product. Explicitly, 

Mt+1 = Proj ρRγ (Mt − ηg t), gt ∈ ∂ρft(Mt). (D.5) This is ordinary Euclidean projected gradient descent after the diagonal change of variables M [i] 7 →

ρ−(i−1) M [i].We first bound every fixed reference. If R is the optimization radius and UR = W RS ρ,H , every fixed reference obeys 

∥∥∥(xMt , u Mt )

∥∥∥ ≤ (1 + ∥K0∥op )(Λx(K0)W + Λ B (K0)UR

) + UR. (D.6) 

D.1 Response geometry and tracking 

The weighted norm above determines both the Lipschitz constant of the counterfactual loss and the reference defect created by one projected-gradient update. We bound these two quantities in turn. Weighted Cauchy–Schwarz gives, for every M, N ,

∥vt(M ) − vt(N )∥ ≤ W S ρ,H ∥M − N ∥ρ . (D.7) Passing this perturbation through (D.1) yields 

∥∥∥xMt − xNt

∥∥∥ ≤ W S ρ,H ΛB (K0) ∥M − N ∥ρ ,

∥∥∥(xMt , u Mt ) − (xNt , u Nt )

∥∥∥ ≤ W S ρ,H C0 ∥M − N ∥ρ .

(D.8) Consequently, ∥gt∥ρ ≤ LW S ρ,H C0 whenever the relevant region is L-Lipschitz. Let et = xt − xMt 

> t

. The physical controller and the current reference both use the additive input 

vt(Mt). Subtracting their state equations gives 

et+1 = F0et + xMt 

> t+1

− xMt+1  

> t+1

. (D.9) Because the weighted coefficient ball {M : ∥M ∥ρ ≤ R} has diameter 2R, every update satisfies 

∥Mt+1 − Mt∥ρ ≤ 2R. The trajectory-sensitivity bound (D.8) , the recursion (D.9) , and the tracker’s impulse-response sum therefore bound the state–input tracking error by 2Λ( K0)Λ B (K0)UR. Adding this to (D.6) gives a ball containing the physical trajectory. When R = Rγ , the reference and physical radii are Osys (W/γ ). Every use of local Lipschitzness below is on this common deterministic ball. Equations (12) and (D.8) imply 

> T

∑

> t=1

[ct(xt, u t) − ct(xMt 

> t

, u Mt 

> t

)] ≤ LΛ( K0)W S ρ,H ΛB (K0)

× 

> T−1

∑

> t=1

∥Mt+1 − Mt∥ρ .

(D.10) 24 The standard projected-gradient inequalities are 

> T

∑

> t=1

[ft(Mt) − ft(M )] ≤ ∥M ∥2

> ρ

2η + η

2

> T

∑

> t=1

∥gt∥2 

> ρ

, (D.11) 

∥Mt+1 − Mt∥ρ ≤ η ∥gt∥ρ . (D.12) Combining these displays, using (D.2), and choosing 

η = Rγ

LW S ρ,H Ψ0

√T (D.13) gives, for every M in the weighted coefficient ball {M : ∥M ∥ρ ≤ Rγ },

> T

∑

> t=1

ct(xt, u t) −

> T

∑

> t=1

ct(xMt , u Mt ) ≤ LW R γ Sρ,H Ψ0

√T . (D.14) In (D.2) , the term C20 bounds the squared-gradient contribution, while 2Λ( K0)Λ B (K0)C0 bounds the tracking contribution to regret caused by the updates. Their sum is Ψ20, which gives (D.14) after substituting (D.13). 

D.2 Embedding and truncation 

The preceding bound competes with any coefficient vector in the optimization ball. To compare with a feedback gain K, we embed its infinite disturbance response in that ball and bound the tail omitted after H blocks. Fix K ∈ K κ,γ , set FK = A − BK , and define M [i] 

> K

= ( K0 − K)F i−1 

> K

. The disturbance expansion gives ∑

> i≥1

M [i] 

> K

wt−i = ( K0 − K)xKt ,

so the infinite response produces exactly uKt = −Kx Kt . Moreover, 

∥MK,H ∥2 

> ρ

≤ κ4 ∥K − K0∥2F

> H

∑

> i=1

(1 − γ)i−1 ≤ κ4 ∥K − K0∥2F

γ ,Sρ,H ≤ γ−1/2.

(D.15) Thus MK,H belongs to the optimization ball. Truncating after H omits the tail input ∑ 

> i>H

M [i] 

> K

wt−i,whose norm is at most 

W κ 2 ∥K − K0∥F

(1 − γ)H

γ .

The state error is this tail input passed through the baseline closed-loop response, while the action error also contains the tail input directly. Therefore 

sup 

> t

∥∥∥yMK,H  

> t

− yKt

∥∥∥ ≤ C0W κ 2 ∥K − K0∥F

(1 − γ)H

γ . (D.16) 

Proof of theorem 5.1. Apply (D.14) to MK,H , use (D.15) , and add the Lipschitz cost of (D.16) .Uniformly over K, T∑

> t=1

ct(yt) −

> T

∑

> t=1

ct(yKt ) ≤ LW κ 2∆K Ψ0

γ

√T

+ LC 0W κ 2∆K

T (1 − γ)H

γ .

(D.17) 25 Choose the integer 

H =

⌈ 1

γ log 

(

2 + C0

√T

Ψ0

)⌉ 

.

Since (1 −γ)H ≤ e−γH , the truncation term is at most the first term in (D.17) . Under the conditioned tracker, ∆K , C 0, Ψ0 = Osys (1) , proving (29). Under regular costs, (D.6) bounds every reference, the pointwise tracking bound above bounds the physical trajectory, and (23) bounds every comparator. All three lie in a ball of radius Dγ ≤ Csys W/γ .The gradient bound makes each cost GD γ -Lipschitz on this ball. Setting L = GD γ in (D.17) proves (30). 

Proof of theorem 5.2. Consider xt+1 = ut + W , with gains K(0) = 0 and K(1) = −(1 − γ). Both lie in K1,γ , and, for t ≥ 2,

x(0)  

> t

= W, x(1)  

> t

= Wγ

[1 − (1 − γ)t−1].

For t ≥ 1 + ⌈2/γ ⌉, their separation is at least c0W/γ . Hence T ≥ C/γ implies 

> T

∑

> t=1

(x(1)  

> t

− x(0) 

> t

)2 ≥ cW 2γ−2T. (D.18) Draw independent uniform signs σt before play. For ct(x, u ) = σtLx , the learner’s expected cost is zero because xt is fixed before σt is revealed. Symmetry and the Khintchine inequality give 

E min 

> j∈{ 0,1}
> T

∑

> t=1

σtLx (j) 

> t

= − L

2 E

∣∣∣∣∣

> T

∑

> t=1

σt(x(1)  

> t

− x(0)  

> t

)

∣∣∣∣∣

≤ − cLW γ −1√T . 

This proves (31) . For the regular-cost statement, take D = 2 W/γ and ct(x, u ) = σtGD x . The gradient norm is exactly GD . Replacing L by GD in the displayed Khintchine bound proves (32) .Both calculations remain valid after conditioning on any fixed private random seed, so the lower bounds also apply to randomized learners. All signs are drawn before play, so the distribution is oblivious. The convex analysis is now complete. We next combine the general PAC-Bayes theorem with a finite cover of the gain matrices in the stable linear state-feedback class to obtain logarithmic regret under strong convexity. 

# E The stable linear state-feedback class under strongly convex costs 

We first justify the scales in (51). Every comparator pair satisfies 

∥∥∥yKt

∥∥∥ ≤ √

1 + κ2 Xγ .

By convexity of the norm, the Gibbs reference—a barycenter of comparator trajectories—has the same bound. Two successive distributions on gains can change the next-state barycenter by at most 2Xγ . The tracker filters these reference defects, and its impulse-response sum bounds the 26 state–input tracking error by 2Λ( K0)Xγ . Thus the comparator trajectories, the Gibbs reference, and the physical trajectory all lie in the radius-Dsc ball. We also prove (52). Let ∆t = xKt − xK′ 

> t

. Then 

∆t+1 = ( A − BK )∆ t + B(K′ − K)xK′ 

> t

.

The power-stability bound and sup t

∥∥∥xK′

> t

∥∥∥ ≤ Xγ give 

sup 

> t

∥∆t∥ ≤ κ4 ∥B∥op Wγ2

∥∥K − K′∥∥F .

Moreover, ∥∥∥uKt − uK′

> t

∥∥∥ ≤ κ ∥∆t∥ + Xγ

∥∥K − K′∥∥F .

Combining these inequalities proves the claimed value of LK.

Proof of theorem 6.2. Let ε > 0 and let Nε be a maximal ε-separated subset of Kκ,γ in Frobenius norm. It is an ε-net. Since the gain class is contained in the Frobenius ball of radius RK =

κ√min {dx, d u}, the volumetric estimate gives 

log |N ε| ≤ dK log 

(

1 + 2RK

ε

)

. (E.1) Apply counterfactual tracking with exponential weights to the exact counterfactual trajectories of the gains in Nε, using the uniform prior and η = cμ/ (L2Γtr ). The comparator trajectories, Gibbs references, and physical trajectory all lie in the radius-Dsc ball, so the local Lipschitz and strong-convexity assumptions apply to every point used by the algorithm. Theorem 6.1 therefore gives regret at most 

CAsc log |N ε|

relative to the best gain in the net. For every K ∈ K κ,γ , choose ̂ K ∈ N ε with 

∥∥∥K −̂ K

∥∥∥F ≤ ε. By (52), 

> T

∑

> t=1

∣∣∣∣ct(yKt ) − ct(ŷ Kt )

∣∣∣∣ ≤ T LL Kε. 

Choose 

ε = min 

{

RK , Asc dK

T LL K

}

,

where the fraction is interpreted as +∞ when T LL K = 0 . The regret relative to any K ∈ K κ,γ is therefore at most 

CAsc dK log 

(

1 + 2RK

ε

)

+ T LL Kε. 

Adding the net regret and approximation loss, then substituting the chosen ε, proves (53). Finally, the conditioned tracker gives 

L = GD sc ≤ Csys 

GW γ , LK ≤ Csys 

Wγ2 , Asc ≤ Csys 

G2W 2

μγ 2 .

Furthermore, RK T LL K

Asc dK

≤ Csys μT Gd K γ .

Substitution proves (54). 27 E.1 A fixed-dimensional lower bound 

The upper bound grows as γ−2 log T . We now show that this dependence on the stability margin and horizon is unavoidable even in fixed dimension. The proof uses the following one-dimensional Bayesian information inequality [ 35 ]. We include the short argument to make clear that the lower bound allows randomized controllers. 

Lemma E.1 (Van Trees inequality) . Let ϑ have an absolutely continuous density φ on [a, b ], positive in the interior and zero at the endpoints, with 

Iφ = 

> ∫ba

φ′(v)2

φ(v) dv < ∞.

Conditionally on ϑ = v, let Z1, . . . , Z k be independent observations with differentiable conditional densities fs(z | v). Assume their scores have conditional mean zero, differentiation may be inter-changed with integration, and Is(v) = E[( ∂v log fs(Zs | v)) 2 | ϑ = v] is finite. Then every possibly randomized estimator ̂ ϑ based on these observations satisfies 

E(̂ϑ − ϑ)2 ≥ 1

Iφ + ∑ks=1 EIs(ϑ) . (E.2) 

Proof. An independent random seed may be appended to the observations, so it is enough to treat a deterministic estimator. If q(v, z ) is the joint density and S = ∂v log q, integration by parts, using the vanishing endpoint density, gives 

E[( ̂ϑ − ϑ)S] = 1 .

Cauchy–Schwarz gives 1 ≤ E(̂ϑ − ϑ)2ES2. The prior score and conditionally independent observation scores are orthogonal, whence ES2 = Iφ + ∑ 

> s

EIs(ϑ). This proves (E.2). 

Proof of theorem 6.3. Consider the one-state, two-input plant 

xt+1 = xt + u1,t + u2,t + W, x1 = 0 . (E.3) The tracker K0 = (1 , 0) ⊤ makes A − BK 0 = 0 . Put 

X = Wγ , αt = 1 − (1 − γ)t−1,

and, for θ ∈ [−1/2, 1/2] , define 

Kθ = ( γ − θ, θ )⊤.

Then A − BK θ = 1 − γ, and, for γ ≤ 1/4,

∥Kθ∥2op = ( γ − θ)2 + θ2 ≤ 13 16 .

Thus Kθ ∈ K 1,γ . All these comparators have the same state ¯xt = Xα t, while 

uθ 

> 1,t

= −(γ − θ)¯ xt, uθ 

> 2,t

= −θ¯xt.

Draw θ from the density 

φ(θ) = 2 cos 2(πθ ) 1[−1/2,1/2] (θ).

28 Conditionally on θ, draw independent signs ξt with 

Pr( ξt = +1 | θ) = 1 − αtθ

2 .

All random variables are drawn before play. Define 

ct(x, u 1, u 2) = μ

2

[(x − ¯xt)2

+ ( u1 + γ ¯xt + Xξ t)2 + ( u2 − Xξ t)2].

(E.4) The Hessian is μI 3, so the costs are globally μ-strongly convex. Their centers have norm at most 

2X. Consequently, on the radius-D = 4 X ball, 

∥∇ ct(y)∥ ≤ 32 μD ≤ 2μD. 

The conditional mean satisfies E[Xξ t | θ] = −θ¯xt. Subtract the cost of Kθ and condition on θ and the learner’s past. The variance terms involving Xξ t are identical for the learner and comparator and cancel, while the learner’s state-square term is nonnegative and may be discarded. The remaining conditional expectation is 

E Reg T ≥ μ

2

> T

∑

> t=1

E[(u1,t + γ ¯xt − θ¯xt)2

+ ( u2,t + θ¯xt)2].

(E.5) For t ≥ 2, define the past-measurable estimator ̂

θt = u1,t + γ ¯xt − u2,t 

2¯ xt

.

Projection onto the action direction (1 , −1) shows that the bracket in (E.5) is at least 2 ¯x2 

> t

(̂ θt − θ)2.At round t, the learner’s entire history is measurable with respect to ξ1, . . . , ξ t−1 and an independent private random seed. The plant and the revealed past costs contain no additional observation whose law depends on θ. Hence ̂ θt is an estimator based only on the first t − 1 sign observations, exactly as required by theorem E.1. Direct calculation gives 

Iφ = 4 π2, Is(θ) = α2

> s

1 − α2 

> s

θ2 ≤ 43 α2 

> s

.

Theorem E.1 therefore implies 

E

[

¯x2 

> t

(̂ θt − θ)2]

≥ X2α2

> t

Iφ + 43

∑ 

> s<t

α2

> s

.

Set St = Iφ + 43

∑ 

> s≤t

α2 

> s

. Since log(1 + r) ≤ r,

> T

∑

> t=1

α2

> t

Iφ + 43

∑ 

> s<t

α2

> s

≥ 34 log 

(

1 + 43IφT∑

> t=1

α2

> t

)

.

For t ≥ 1 + ⌈1/γ ⌉, αt ≥ 1 − e−1. Hence T ≥ C/γ makes the final display at least c log T . Combining the estimates and using X = W/γ proves (55) . The construction is oblivious and the van Trees argument already includes private randomization. 29 F Proofs for the system-level response ball 

Recall that S(R) contains the feasible system-level responses whose summed Frobenius deviation from the baseline response Φ0 is at most R. This sum is the centered response gain r0(Φ) . For disturbances bounded by W , the gain bound implies 

∥∥∥yΦ 

> t

− yΦ0

> t

∥∥∥ ≤ W r0(Φ) ≤ W R at every round. We retain the state- and input-response notation from the main text. The baseline blocks are 

Φ[i]0,x = F i−10 , Φ[i]0,u = −K0F i−10 .

We also abbreviate 

Λ0 = Λ x(K0) = ∑

> r≥0

∥F r 

> 0

∥op ,

Λ0,B = ∑

> r≥0

∥F r 

> 0

B∥op , χ0 =

√

1 + ∥K0∥2op .

F.1 Controllers represented by the response ball 

First, every finite-dimensional internally stable linear dynamical controller induces a response in 

S(R) for some finite R, and every response in S(R) has an internally stabilizing realization. We then give an explicit radius that covers the stable linear state-feedback class. 

Proof of the first two claims in theorem 5.3. Write a finite-dimensional, zero-initialized linear dy-namical controller as 

st+1 = Acst + Bcxt, ut = Ccst + Dcxt, s1 = 0 ,

and define 

Fπ =

[

A + BD c BC c

Bc Ac

]

, E =

[

I

0

]

, Cx =

[

I 0

]

, Cu =

[

Dc Cc

]

.

Propagation of a disturbance impulse gives 

Φ[i] 

> π,x

= CxF i−1 

> π

E, Φ[i] 

> π,u

= CuF i−1 

> π

E. (F.1) The first block row of Fπ implies Φ[i+1]  

> π,x

= AΦ[i] 

> π,x

+ BΦ[i]

> π,u

, so the affine system-level synthesis constraint holds. Internal stability makes Fπ Schur. Hence there are C < ∞ and ρ < 1 such that ∥∥F iπ

∥∥op ≤ Cρ i. Both sequences in (F.1) are therefore absolutely summable. The baseline response is absolutely summable as well, and thus r0(Φ π) < ∞.Conversely, if Φ ∈ S(R), then r0(Φ) ≤ R and absolute summability of the baseline imply that its state and input response sequences are absolutely summable. Let 

Φx(z) = ∑

> i≥1

Φ[i] 

> x

z−i, Φu(z) = ∑

> i≥1

Φ[i] 

> u

z−i

be the corresponding stable, strictly proper transfer functions. The affine constraint becomes 

(zI − A)Φ x(z) − BΦu(z) = I. 

The standard state-feedback system-level synthesis realization uses the stable filters 

˜Φx = zΦx − I, ˜Φu = zΦu,

30 and the interconnection ̂

w = x −̂ x, ̂ x = ˜Φx̂ w, u = ˜Φû w. 

Because zΦx = I + ˜Φx has identity feedthrough, this interconnection is causally well posed. Eliminating ̂ w gives u = Φ uΦ−1 

> x

x. Substitution into the plant yields 

[(zI − A)Φ x − BΦu

]Φ−1 

> x

x = w. 

The affine identity in brackets equals I, and therefore x = Φ xw and u = Φ uw. All filters from the exogenous disturbance to the internal signals are built from the stable responses Φx, Φu, ˜Φx, ˜Φu,which proves internal stability; see [ 37 , Theorem 1]. The filters need not be rational, so the realization can be infinite-dimensional. 

Static-feedback part of theorem 5.3. Fix K ∈ K κ,γ , put FK = A − BK , and let ∆K = K0 − K. The response of ut = −Kx t is 

Φ[i] 

> K,x

= F i−1 

> K

, Φ[i] 

> K,u

= −KF i−1 

> K

.

For r ≥ 1, the telescoping identity yields 

P [r+1] := F rK − F r 

> 0

= ∑

> a+b=r−1

F a 

> 0

B∆K F bK .

Consequently, ∑

> i≥1

∥∥∥P [i]∥∥∥F ≤ Λ0,B ∥∆K ∥F

∑

> b≥0

∥∥∥F bK

∥∥∥op ≤ κ2Λ0,B 

γ ∥∆K ∥F . (F.2) The centered input response is 

Φ[i] 

> K,u

− Φ[i]0,u = −K0P [i] + ∆ K F i−1 

> K

.

Using the triangle inequality for the stacked state–input block and then (F.2), 

r0(Φ K ) ≤ χ0

∑

> i≥1

∥∥∥P [i]∥∥∥F + ∥∆K ∥F

∑

> i≥1

∥∥∥F i−1

> K

∥∥∥op 

≤ κ2

γ (1 + χ0Λ0,B ) ∥K − K0∥F .

Taking the supremum over K ∈ K κ,γ proves (37). 

Proof of theorem 5.4. Take the scalar plant A = 0 , B = 1 , and tracker K0 = 0 . Its baseline response is 

Φ[1] 0,x = 1 , Φ[i]0,x = 0 ( i ≥ 2) , Φ[i]0,u = 0 ( i ≥ 1) .

For a delay j ≥ 1, a sign s ∈ {− 1, 1}, and a = R/ 2, define 

Φ[i] 

> j,s,x

= Φ [i]0,x + sa 1{i = j + 1 }, Φ[i] 

> j,s,u

= sa 1{i = j}. (F.3) The scalar response recursion is Φ[i+1]  

> x

= Φ [i] 

> u

, which this response satisfies. Its centered response gain, namely its summed deviation from the baseline, is 2a = R. Given h, choose j > h : both nonzero centered blocks then occur after h, so the tail is R. For every Φ ∈ S(R), the same tail is at most r0(Φ) ≤ R, which proves the matching upper bound. The response is realized by the causal finite-memory controller ut = saw t−j , since past disturbances are recovered from wr = xr+1 −ur.Because no cutoff approximates the whole response ball, the upper-bound algorithm keeps every response block that can affect the horizon. We now define this exact-prefix problem and its mirror-descent update. 31 F.2 Exact-prefix mirror descent 

Fix H = T − 1. For a centered prefix write 

P [i] = Φ [i] 

> x

− Φ[i]0,x , V [i] = Φ [i] 

> u

− Φ[i]0,u , Q[i] =

[

P [i]

V [i]

]

.

Subtracting the baseline and comparator response recursions gives the linear subspace 

VH =

{

Q : P [1] = 0 , P [i+1] = AP [i] + BV [i] (1 ≤ i < H )

}

. (F.4) Put 

p = 1 + 1log( eH ) , α = p − 1, ∥Q∥p, 2 =

( H∑

> i=1

∥∥∥Q[i]∥∥∥p

> F

)1/p 

, (F.5) and QH (R) = {Q ∈ V H : ∥Q∥p, 2 ≤ R}.

Lemma F.1 (Near-ℓ1 geometry) . Let aH = H1−1/p = H1/(2+log H) ≤ e. Then 

> H

∑

> i=1

∥∥∥Q[i]∥∥∥F ≤ aH ∥Q∥p, 2 . (F.6) 

Moreover, Ω( Q) = 12 ∥Q∥2 

> p, 2

is α-strongly convex with respect to ∥·∥ p, 2, also after restriction to VH .Proof. Hölder’s inequality gives ∥Q∥1,2 ≤ H1−1/p ∥Q∥p, 2, and the definition of p gives H1−1/p =exp (log H/ (2 + log H)) ≤ e. For 1 < p ≤ 2, the Ball–Carlen–Lieb inequality states that one half of the squared ℓp(ℓ2) norm is (p − 1) -strongly convex in that norm [ 8 ]. The Frobenius norm is the inner Euclidean norm here. Restriction to a linear subspace preserves the modulus. For Q ∈ Q H (R), define the exact-prefix reference 

¯xt(Q) = 

> t−1

∑

> i=1

(Φ [i]0,x + P [i])wt−i,

¯ut(Q) = 

> t−1

∑

> i=1

(Φ [i]0,u + V [i])wt−i,

¯yt(Q) = (¯ xt(Q), ¯ut(Q)) .

(F.7) The affine constraints imply 

¯xt+1 (Q) = A¯xt(Q) + B ¯ut(Q) + wt (t < T ). (F.8) Initialize Q1 = 0 . On round t, use 

ut = ¯ ut(Qt) − K0(xt − ¯xt(Qt)) . (F.9) After observing ct, set ft(Q) = ct(¯ yt(Q)) . We use the block Frobenius pairing 

⟨G, Q ⟩ =

> H

∑

> i=1

tr 

(

(G[i])⊤Q[i])

32 on VH . Let gt be a subgradient of the restriction of ft to this subspace, and define the Bregman divergence 

DΩ(Q, Q ′) = Ω( Q) − Ω( Q′) − 〈∇Ω( Q′), Q − Q′〉 .

The mirror-descent update is 

Qt+1 ∈ argmin  

> Q∈Q H(R)

{η ⟨gt, Q ⟩ + DΩ(Q, Q t)} . (F.10) This construction is causal: (F.7) uses only disturbances revealed before round t. The learner’s prefix need not admit an infinite stable extension. The constraints in (F.4) make the reference dynamically feasible through round T , and K0 stabilizes the tracking error. 

Lemma F.2 (Prefix sensitivity) . For Q, Q ′ ∈ Q H (R) and t ≤ T ,

∥∥¯yt(Q) − ¯yt(Q′)∥∥ ≤ W a H

∥∥Q − Q′∥∥p, 2 , (F.11) 

∥∥¯xt+1 (Q) − ¯xt+1 (Q′)∥∥ ≤ W a H

∥∥Q − Q′∥∥p, 2 (t < T ). (F.12) 

If ct is L-Lipschitz, then 

∥gt∥p, 2,∗ ≤ LW a H . (F.13) 

Proof. The triangle inequality, ∥M ∥op ≤ ∥ M ∥F, and theorem F.1 give 

∥∥¯yt(Q) − ¯yt(Q′)∥∥ ≤ W

> H

∑

> i=1

∥∥∥Q[i] − Q′[i]∥∥∥F

≤ W a H

∥∥Q − Q′∥∥p, 2 .

Keeping only the state block proves (F.12) . The dual norm bound follows by composing an 

L-Lipschitz convex function with this affine map. The proof splits regret into reference regret and excess tracking cost. A comparator’s visible prefix represents its trajectory exactly, so mirror descent controls reference regret without a truncation error. Movement of the mirror-descent iterate drives the tracking error through the stable tracker, and Lipschitzness converts that error into excess cost. 

Proof of theorem 5.5. Fix Φ ∈ S(R), and let QΦ,H be its first H centered blocks. The homogeneous response recursion puts this prefix in VH , while 

∥QΦ,H ∥p, 2 ≤

> H

∑

> i=1

∥∥∥Q[i]Φ

∥∥∥F ≤ r0(Φ) ≤ R. 

Thus QΦ,H ∈ Q H (R). For every t ≤ T ,

¯yt(QΦ,H ) = ( xΦ 

> t

, u Φ 

> t

). (F.14) This is an identity, not a truncation estimate: blocks after H = T − 1 cannot affect the horizon. Mirror descent with the α-strongly convex regularizer in theorem F.1 gives 

> T

∑

> t=1

(ft(Qt) − ft(QΦ,H )) ≤ DΩ(QΦ,H , 0) 

η + η

2α

> T

∑

> t=1

∥gt∥2 

> p, 2,∗

.

33 Since ∇Ω(0) = 0 , the Bregman term is at most R2/2. Using (F.13), 

> T

∑

> t=1

(ft(Qt) − ft(QΦ,H )) ≤ R2

2η + ηT L 2W 2a2

> H

2α . (F.15) It remains to compare the physical trajectory with the current reference. Let et = xt − ¯xt(Qt).From (F.8) and (F.9), 

et+1 = F0et + δt+1 , δt+1 = ¯ xt+1 (Qt) − ¯xt+1 (Qt+1 ). (F.16) Since e1 = 0 , unrolling the recursion and using ∑ 

> r≥0

∥F r 

> 0

∥op = Λ 0 bounds the sum of the tracking errors by Λ0 times the sum of the reference changes. Applying (F.12) gives 

> T

∑

> t=1

∥et∥ ≤ Λ0W a HT −1∑

> t=1

∥Qt+1 − Qt∥p, 2 . (F.17) The optimality condition in (F.10), tested against Qt, and strong convexity of Ω yield 

∥Qt+1 − Qt∥p, 2 ≤ ηα ∥gt∥p, 2,∗ ≤ ηLW a H

α . (F.18) Moreover, (xt, u t) − ¯yt(Qt) = ( et, −K0et), whose norm is at most χ0 ∥et∥. Hence 

> T

∑

> t=1

(ct(xt, u t) − ct(¯ yt(Qt)) ) ≤ Lχ 0

> T

∑

> t=1

∥et∥≤ ηT L 2W 2a2

> H

α χ0Λ0.

(F.19) Adding (F.15) and (F.19), and using (F.14), gives 

Reg T (Φ) ≤ R2

2η + ηT L 2W 2a2

> H

2α (1 + 2 χ0Λ0). (F.20) For R > 0, choose 

η = R√αLW a H

√T (1 + 2 χ0Λ0) .

Then aH ≤ e and α−1 = log (eH ) ≤ log (eT ) prove (42) . For R = 0 , tracking the baseline has zero regret. We next bound every state–input pair at which a cost or gradient is evaluated. Put S0 =

∑

> i≥1

∥∥∥Φ[i]0

∥∥∥F. Every reference satisfies ∥¯yt(Qt)∥ ≤ W (S0 + aH R). Every comparator satisfies the smaller bound W (S0 + R). Since the diameter of QH (R) is at most 2R, the pointwise form of (F.16) gives 

∥et∥ ≤ 2Λ 0W a H R. 

Thus every physical, reference, and comparator state–input pair lies in the ball of radius 

W [S0 + aH R(1 + 2 χ0Λ0)] ≤ DR.

The preceding proof may therefore use L = GD R whenever the costs are (G, D R)-regular. The upper bound pays √log T for allowing response mass at any visible delay. We next show that delayed controllers force the same factor. 34 F.3 The delayed-response lower bound 

The lower bound requires a delayed response whose action correlates with the current cost signs. The next lemma gives the required √log m improvement when the candidate delay vectors are bounded and separated. We prove this special form directly. 

Lemma F.3 (Rademacher maximum for separated vectors) . There are universal constants c > 0

and n0 such that the following holds. Let n ≥ n0, let A ⊂ [−1, 1] n contain 2 ≤ m ≤ 2n vectors, and suppose 

∥a − b∥ ≥ √n (a̸ = b).

If ξ has independent Rademacher coordinates, then 

Eξ max  

> a∈A

⟨ξ, a ⟩ ≥ c√n log m. (F.21) 

Proof. Let g be a standard Gaussian vector. Gaussian Sudakov minoration [24] gives 

Eg max  

> a∈A

⟨g, a ⟩ ≥ cG

> √

n log m. (F.22) For β > 0, introduce the smooth maximum 

Fβ (z) = 1

β log ∑

> a∈A

exp( β ⟨a, z ⟩).

It satisfies 

max  

> a∈A

⟨a, z ⟩ ≤ Fβ (z) ≤ max  

> a∈A

⟨a, z ⟩ + log mβ .

For each coordinate i, differentiation under the finite sum gives 

∂3 

> i

Fβ (z) = β2Ea∼pz

> [

(ai − Epz ai)3],

where pz (a) is proportional to exp (β ⟨a, z ⟩). Since |ai| ≤ 1, sup z |∂3 

> i

Fβ (z)| ≤ 8β2. Replacing the coordinates of g one at a time by Rademacher signs and using third-order Taylor remainder (the variables match in their first two moments) therefore gives 

|EFβ (g) − EFβ (ξ)| ≤ Cβ 2n. (F.23) Combining the last three displays, 

E max  

> a∈A

⟨ξ, a ⟩ ≥ cG

> √

n log m − Cβ 2n − log mβ .

Choose β = (log m/n )1/3. The two error terms are at most 

Cn 1/3(log m)2/3 = C 

> (

log mn 

> )1/6√

n log m. 

Because m ≤ 2n, the prefactor tends to zero uniformly as n → ∞ . Enlarging n0 absorbs the error into half of (F.22). 35 Lemma F.4 (Delayed cross-correlations) . Let ε1, . . . , ε n and ξ1, . . . , ξ n be independent Rademacher sequences, and set 

Cj =

> n−j

∑

> s=1

εs+j ξs, 1 ≤ j < n. (F.24) 

There is a universal c > 0 such that 

E max  

> 1≤j<n

|Cj | ≥ c

√

n log( en ). (F.25) 

Proof. First suppose n is sufficiently large. Let m = ⌊n/ 4⌋ and, conditional on ε, define vj ∈ Rn by 

(vj )s = εs+j 1{s ≤ n − j}, 1 ≤ j ≤ m. 

Then Cj = ⟨vj , ξ ⟩, ∥vj ∥2 = n − j ≥ 3n/ 4, and ∥vj ∥∞ = 1 . For j < k ,

⟨vj , v k⟩ =

> n−k

∑

> s=1

εs+j εs+k.

This has mean zero. Changing one coordinate of ε changes at most two summands, each by at most two. The bounded-differences inequality gives 

Pr  

> ε

(| ⟨ vj , v k⟩ | > n/ 8) ≤ 2e−n/ 512 . (F.26) A union bound shows that, with probability at least 1/2, every pair has inner product at most n/ 8 in magnitude. On this event, ∥vj − vk∥2 ≥ n. For large n, theorem F.3 applies and gives, conditionally on ε,

Eξ max  

> j≤m

|Cj | ≥ c√n log m. 

The separation event has probability at least 1/2, so averaging this conditional lower bound over ε

proves the claim for large n. For the finitely many remaining values, C1 is a sum of n − 1 independent signs, so Khintchine’s inequality gives E|C1| ≥ √n − 1/√2. Decreasing c proves the result for every 

n ≥ 2.

Proof of theorem 5.6. Use the scalar plant xt+1 = ut+wt with K0 = 0 . Before play draw independent Rademacher sequences (εt)t≤T and (ξt)t≤T , and set 

wt = W ξ t, ct(x, u ) = Lε tu. (F.27) The costs are convex and globally L-Lipschitz. At time t, the learner’s action is measurable with respect to its private seed and signs revealed before t, while εt is fresh. Thus E[εtut] = 0 , and the learner’s expected total loss is zero. For a delay j and sign s, the response (F.3) belongs to S(R) and has uj,s t = s(R/ 2) wt−j , with 

wr = 0 for r ≤ 0. Its loss is 

> T

∑

> t=1

ct(xj,s t , u j,s t ) = sLW R 

2

> T

∑

> t=j+1

εtξt−j = sLW R 

2 Cj .

The best sign and delay have loss at most −(LW R/ 2) max 1≤j<T |Cj |. Therefore theorem F.4 gives 

E inf 

> Φ∈S(R)
> T

∑

> t=1

ct(yΦ 

> t

) ≤ − cLW R 

√

T log( eT ).

36 The learner’s expected loss is zero, so this proves (44) . Because the argument holds after conditioning on the learner’s private seed, it also applies to randomized controllers. All signs are drawn before play, so the adversary is oblivious. For the bounded-region claim, use the state–input radius 

Dsc  

> R

= W (1 + 3 eR ), (F.28) which is (43) on this plant. Apply the preceding construction with L = GD sc  

> R

. Its linear costs have gradient norm exactly GD sc  

> R

, hence are (G, D sc  

> R

)-regular. Since Dsc  

> R

≍ W (1 + R), this proves the lower half of (45); the upper half is the bounded-region part of theorem 5.5. The convex upper and lower bounds now match. The last question is whether strong convexity removes the cost of choosing among many response delays; the next construction shows that it does not. 

F.4 Strong convexity over the system-level response ball 

The next lemma supplies the two ingredients for the lower bound: every subset of delay vectors has a large correlation with fresh signs, while the convolution matrix of all delay vectors has bounded operator norm. 

Lemma F.5 (Separated shifts with bounded convolution norm) . There are universal c, C > 0 and 

T0 such that the following holds for every T ≥ T0. Let M = ⌊T / 4⌋, draw independent Rademacher signs ξ1, . . . , ξ T , and define 

(vj )t = ξt−j 1{t > j }, Xξ = [ v1 · · · vM ] ∈ RT ×M . (F.29) 

With probability at least 1/2,

∥Xξ∥2op ≤ CT log( eT ), (F.30) 

Eε max  

> j∈J

| ⟨ ε, v j ⟩ | ≥ c

> √

T log( e|J|) for every nonempty J ⊆ { 1, . . . , M }, (F.31) 

where ε is an independent Rademacher vector. Proof. For j ≤ M , ∥vj ∥2 = T − j ≥ 3T / 4. The bounded-differences argument in (F.26) , followed by a union bound, shows with probability at least 3/4 that 

| ⟨ vj , v k⟩ | ≤ T / 8 (j̸ = k). (F.32) On this event, {vj , −vj : j ∈ J} has pairwise separation at least √T , coordinate bound one, and cardinality at most 2M ≤ T . For sufficiently large T , theorem F.3 applies simultaneously to every 

J, and proves (F.31). For (F.30) , pad ξ with zeros and embed Xξ in the 2T -dimensional circulant convolution matrix. Its operator norm is 

max 

> 0≤q< 2T
> ∣∣∣∣∣
> T∑
> s=1

ξse−2πiqs/ (2 T ) 

> ∣∣∣∣∣

.

Hoeffding’s inequality for the real and imaginary parts, followed by a union bound over the 2T

frequencies, bounds this by C√T log( eT ) with probability at least 3/4. A submatrix cannot have larger operator norm. Intersecting the two probability-3/4 events proves the lemma. 37 Proof of theorem 6.4. Let D = Dsc  

> R

= W (1 + 3 eR ) from (F.28) . Draw independent Rademacher sequences ξ1, . . . , ξ T and ε1, . . . , ε T before play, and set 

wt = W ξ t, ℓ = GD 

4 , ct(x, u ) = μ

2 (x − wt−1)2 + μ

2 u2 + ℓε tu, (F.33) where w0 = u0 = 0 . The Hessian is μI 2, so the costs are globally μ-strongly convex. On ∥(x, u )∥ ≤ D,using W ≤ D and G ≥ 4μ,

∥∇ ct(x, u )∥ ≤ μ(D + W ) + μD + ℓ ≤ 3μD + GD 

4 ≤ GD. 

Thus every cost is (G, D )-regular. For any causal learner, the plant identity gives xt −wt−1 = ut−1. The fresh sign εt is conditionally independent of ut. Therefore 

E

> T

∑

> t=1

ct(xt, u t) = μ

2 E

> T

∑

> t=1

(u2 

> t−1

+ u2 

> t

) ≥ 0. (F.34) We now construct a hindsight comparator. Let M = ⌊T / 4⌋. For θ ∈ RM , define 

Φ[j] 

> θ,u

= θj , Φ[1]  

> θ,x

= 1 , Φ[j+1]  

> θ,x

= θj (1 ≤ j ≤ M ), (F.35) and set the remaining blocks to zero. This is feasible for the scalar response recursion. With 

θ0 = θM +1 = 0 ,

r0(Φ θ) =  

> M+1

∑

> j=1

√

θ2 

> j−1

+ θ2 

> j

≤ 2 ∥θ∥1 . (F.36) Hence ∥θ∥1 ≤ B := R/ 2 implies Φθ ∈ S(R). With Xξ from theorem F.5, its action vector is 

uθ = W X ξθ. Writing hj = ⟨ε, v j ⟩ and h = ( h1, . . . , h M ), the total comparator loss obeys 

> T

∑

> t=1

ct(yΦθ 

> t

) ≤ μW 2 ∥Xξθ∥2 + ℓW ⟨h, θ ⟩ . (F.37) The linear term rewards correlation with a delayed disturbance, whereas the quadratic term penalizes the squared action norm. Placing the entire ℓ1 budget on one delay maximizes the first effect but can make the second too large. Splitting the budget across k groups preserves a large correlation reward and reduces ∥θ∥22 to B2/k ; we choose k to balance the two terms. Work on the event in theorem F.5; on its complement choose θ = 0 . Put 

q = μW B ℓ = 2μR G(1 + 3 eR ) , k = max 

{

1,

⌈

Kq 

√

T log( eT )

⌉} 

, (F.38) where K is a sufficiently large universal constant. Since G ≥ 4μ, q ≤ 1/(6 e). After increasing 

T0, k ≤ M/ 2. Retain any km of the M delays, where m = ⌊M/k ⌋, and partition them into k

groups of size m, discarding the remainder. Then log (em ) ≥ c log (eT ). In each group Jg, choose jg

maximizing |hj |, and set 

θjg = − Bk sign( hjg ), θj = 0 otherwise . (F.39) 38 Then ∥θ∥1 ≤ B and ∥θ∥22 ≤ B2/k . Conditional on ξ, (F.31) gives 

−ℓW Eε ⟨h, θ ⟩ ≥ cℓW B 

> √

T log( eT ). (F.40) No independence among groups is needed. On the same event, (F.30) gives 

μW 2 ∥Xξθ∥2 ≤ CμW 2T log( eT ) B2

k .

If Kq √T log( eT ) ≥ 1, substitute k ≥ Kq √T log( eT ); otherwise k = 1 and use q√T log( eT ) < 1/K .In both cases, 

μW 2 ∥Xξθ∥2 ≤ CK ℓW B 

> √

T log( eT ). (F.41) Choose K large enough that the right-hand side of (F.41) is at most half the lower bound in (F.40) .The good event has probability at least 1/2, and therefore (F.37) yields 

E inf 

> Φ∈S(R)
> T∑
> t=1

ct(yΦ 

> t

) ≤ − cℓW B 

> √

T log( eT ) ≤ − cGDW R 

> √

T log( eT ).

Together with (F.34) , this proves (56) . Conditioning first on a randomized learner’s private seed proves the randomized statement. The upper bound at the same scale is the bounded-region part of theorem 5.5; strong convexity only restricts the admissible cost sequence. 

# References 

[1] Yasin Abbasi-Yadkori, Peter L. Bartlett, and Varun Kanade. Tracking adversarial targets. In 

Proceedings of the 31st International Conference on Machine Learning , volume 32 of Proceedings of Machine Learning Research , pages 369–377, 2014. [2] Yasin Abbasi-Yadkori, Nevena Lazić, and Csaba Szepesvári. Model-free linear quadratic control via reduction to expert prediction. In Proceedings of the 22nd International Conference on Artificial Intelligence and Statistics , volume 89 of Proceedings of Machine Learning Research ,pages 3108–3117, 2019. [3] Naman Agarwal, Elad Hazan, and Karan Singh. Logarithmic regret for online control. In 

Advances in Neural Information Processing Systems 32 , pages 10175–10184, 2019. [4] Naman Agarwal, Brian Bullins, Elad Hazan, Sham M. Kakade, and Karan Singh. Online control with adversarial disturbances. In Proceedings of the 36th International Conference on Machine Learning , volume 97 of Proceedings of Machine Learning Research , pages 111–119, 2019. [5] Naman Agarwal, Nataly Brukhim, Elad Hazan, and Zhou Lu. Boosting for control of dynamical systems. In Proceedings of the 37th International Conference on Machine Learning , volume 119 of Proceedings of Machine Learning Research , pages 96–103, 2020. [6] Oren Anava, Elad Hazan, and Shie Mannor. Online learning for adversaries with memory: Price of past mistakes. In Advances in Neural Information Processing Systems 28 , pages 784–792, 2015. 39 [7] Raman Arora, Ofer Dekel, and Ambuj Tewari. Online bandit learning against an adaptive adversary: From regret to policy regret. In Proceedings of the 29th International Conference on Machine Learning , pages 1747–1754, 2012. [8] Keith Ball, Eric A. Carlen, and Elliott H. Lieb. Sharp uniform convexity and smoothness inequalities for trace norms. Inventiones Mathematicae , 115:463–482, 1994. [9] Richard Bellman. Dynamic Programming . Princeton University Press, 1957. [10] Kush Bhatia and Karthik Sridharan. Online learning with dynamics: A minimax perspective. In Advances in Neural Information Processing Systems 33 , pages 15020–15030, 2020. [11] David Blackwell. An analog of the minimax theorem for vector payoffs. Pacific Journal of Mathematics , 6(1):1–8, 1956. [12] Anand Brahmbhatt, Gon Buzaglo, Sofiia Druchyna, and Elad Hazan. A new approach to controlling linear dynamical systems. arXiv:2504.03952, 2025. [13] Nicolò Cesa-Bianchi and Gábor Lugosi. Prediction, Learning, and Games . Cambridge University Press, 2006. [14] Alon Cohen, Avinatan Hasidim, Tomer Koren, Nevena Lazić, Yishay Mansour, and Kunal Talwar. Online linear quadratic control. In Proceedings of the 35th International Conference on Machine Learning , volume 80 of Proceedings of Machine Learning Research , pages 1029–1038, 2018. [15] Alexandre Didier, Jerome Sieber, and Melanie N. Zeilinger. A system level approach to regret optimal control. arXiv:2202.13763, 2022. [16] Dylan J. Foster and Max Simchowitz. Logarithmic regret for adversarial online control. In Proceedings of the 37th International Conference on Machine Learning , volume 119 of 

Proceedings of Machine Learning Research , pages 3211–3221, 2020. [17] James Hannan. Approximation to Bayes risk in repeated play. In Contributions to the Theory of Games , volume 3, pages 97–139. Princeton University Press, 1957. [18] Elad Hazan. Introduction to Online Convex Optimization . Foundations and Trends in Opti-mization, 2(3–4):157–325, 2016. [19] Wei Han, Alexander Rakhlin, and Karthik Sridharan. Competing with strategies. In Proceedings of the 26th Annual Conference on Learning Theory , volume 30 of Proceedings of Machine Learning Research , pages 966–992, 2013. [20] Vijeth Hebbar and Cédric Langbort. Revisiting regret benchmarks in online non-stochastic control. arXiv:2504.16581, 2025. [21] Elad Hazan and Karan Singh. Introduction to Online Control . arXiv:2211.09619, 2022; revised 2026. [22] Elad Hazan, Sham M. Kakade, and Karan Singh. The nonstochastic control problem. In 

Proceedings of the 31st International Conference on Algorithmic Learning Theory , volume 117 of Proceedings of Machine Learning Research , pages 408–421, 2020. 40 [23] Rudolf E. Kalman. Contributions to the theory of optimal control. Boletín de la Sociedad Matemática Mexicana , 5:102–119, 1960. [24] Michel Ledoux and Michel Talagrand. Probability in Banach Spaces: Isoperimetry and Processes .Springer-Verlag, Berlin, 1991. [25] Nick Littlestone and Manfred K. Warmuth. The weighted majority algorithm. Information and Computation , 108(2):212–261, 1994. [26] Yingying Li, James A. Preiss, Na Li, Yiheng Lin, Adam Wierman, and Jeff Shamma. Online switching control with stability and regret guarantees. arXiv:2301.08445, 2023. [27] Yiheng Lin, James A. Preiss, Emile Anand, Yingying Li, Yisong Yue, and Adam Wierman. Online adaptive policy selection in time-varying systems: No-regret via contractive perturbations. arXiv:2210.12320, 2023. [28] Andrea Martin, Luca Furieri, Florian Dörfler, John Lygeros, and Giancarlo Ferrari-Trecate. Safe control with minimal regret. arXiv:2203.00358, 2022. [29] Edgar Minasyan, Paula Gradu, Max Simchowitz, and Elad Hazan. Online control of unknown time-varying dynamical systems. arXiv:2202.07890, 2022. [30] Alexander Rakhlin, Karthik Sridharan, and Ambuj Tewari. Online learning: Beyond regret. In 

Proceedings of the 24th Annual Conference on Learning Theory , volume 19 of Proceedings of Machine Learning Research , pages 559–594, 2011. [31] Alexander Rakhlin, Karthik Sridharan, and Ambuj Tewari. Online learning via sequential complexities. Journal of Machine Learning Research , 16:155–186, 2015. [32] Herbert Robbins. Asymptotically subminimax solutions of compound statistical decision problems. In Proceedings of the Second Berkeley Symposium on Mathematical Statistics and Probability , pages 131–149. University of California Press, 1951. [33] Max Simchowitz. Making non-stochastic control (almost) as easy as stochastic. In Advances in Neural Information Processing Systems 33 , pages 18318–18329, 2020. [34] Max Simchowitz, Karan Singh, and Elad Hazan. Improper learning for non-stochastic control. In Proceedings of the 33rd Conference on Learning Theory , volume 125 of Proceedings of Machine Learning Research , pages 3320–3436, 2020. [35] Harry L. Van Trees. Detection, Estimation, and Modulation Theory, Part I . John Wiley & Sons, 1968. [36] Dirk van der Hoeven, Nikita Zhivotovskiy, and Nicolò Cesa-Bianchi. A regret–variance trade-off in online learning. In Advances in Neural Information Processing Systems 35 , pages 35188–35200, 2022. [37] Yuh-Shyang Wang, Nikolai Matni, and John C. Doyle. A system level approach to controller synthesis. IEEE Transactions on Automatic Control , 64(10):4079–4093, 2019. [38] Kemin Zhou, John C. Doyle, and Keith Glover. Robust and Optimal Control . Prentice Hall, 1996. 41