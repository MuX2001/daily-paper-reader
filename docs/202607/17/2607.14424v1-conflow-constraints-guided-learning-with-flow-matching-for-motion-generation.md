---
title: "ConFlow: Constraints-Guided Learning with Flow Matching for Motion Generation"
title_zh: "ConFlow: 基于约束引导的流匹配运动生成"
authors: "Nutan Chen, Jianxiang Feng, Marvin Alles, Botond Cseke"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14424v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 基于流匹配的机器人运动生成，引入约束指导
tldr: 当前Flow Matching在机器人运动生成中仅从数据学习，无法满足任务特定约束，导致训练-推理不匹配。ConFlow框架在训练目标中引入可微约束函数，用条件高斯过程替代标准高斯源分布施加平滑性与边界，并利用不可行演示负监督。在双机器人导航任务中，该方法无需推理时引导，相比标准Flow Matching基线碰撞率显著降低且轨迹质量更高。该工作表明训练时约束集成能有效弥合生成模型的训练-推理差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14424v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1359, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14424v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 645, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14424v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14424v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 724, \"height\": 312, \"label\": \"Table\"}]"
motivation: 机器人运动生成中Flow Matching从数据学习忽视任务约束，导致训练-推理不匹配，生成轨迹不满足约束。
method: 提出ConFlow框架，将可微约束函数融入Flow Matching训练目标，用条件高斯过程替代标准源分布，并利用不可行演示作为负监督。
result: 在双机器人导航任务中，ConFlow无需推理时引导，碰撞率显著降低、轨迹质量优于有/无引导的Flow Matching基线。
conclusion: 验证了训练时约束集成是弥合生成模型训练-推理差距的有效方法，提升约束满足与轨迹质量。
---

## 摘要
近年来，流匹配已成为生成式建模机器人运动生成的重要方法。在其通用形式中，流匹配是一种基于ODE的神经采样器，通过回归与作为数据的运动样本相关的经验流场进行训练。然而，在机器人运动生成中，我们通常有额外的约束条件，这些约束可能不在收集的数据中。当前大多数方法在可用数据上训练流，并使用推理时引导来强制执行任务特定约束。为了解决这种不匹配，我们提出了ConFlow，一种约束引导的流匹配框架，它通过可微的障碍或成本函数直接将约束信息纳入训练目标。为了解决平滑性和边界条件等设计规范，我们建议用条件高斯过程替换流匹配训练中使用的标准高斯源分布。我们的方法还使用不可行的演示作为负监督，提高约束满足度，而无需额外的专家数据。在两个机器人导航任务上的实验表明，ConFlow比标准流匹配基线实现了更低的碰撞率和更高的轨迹质量，无论是否有推理时引导。这些结果验证了训练时约束集成作为缩小生成运动模型中训练-推理差距的有效方法。

## Abstract
In recent years Flow Matching has become a prominent method for generative modeling robot motion generation. In its generic form Flow Matching is an ODE-based neural sampler that is trained by regressing empirical flow fields associated with motion samples as data. However, in robot motion generation we often have additional constraints that might not be present in the collected data. The majority of current approaches train the flow on the available data and use inference-time guidance to enforce task-specific constraints. To address this mismatch, we propose \textbf{ConFlow}, a constraint-guided flow matching framework that incorporates constraint information directly into the training objective via differentiable barrier or cost functions. To address design specifications such as smoothness and boundary conditions, we propose replacing the standard Gaussian source distribution used in flow matching training with a conditional Gaussian Process. Our approach also uses infeasible demonstrations as negative supervision, improving constraint satisfaction without requiring additional expert data. Experiments on a two-robot navigation task demonstrate that ConFlow achieves lower collision rates and higher trajectory quality than standard flow matching baselines, with or without inference-time guidance. These results validate training-time constraint integration as an effective approach to closing the training--inference gap in generative motion models.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前基于流匹配（Flow Matching）的机器人运动生成方法存在“训练–推理不匹配”问题。训练阶段仅从数据（专家演示）学习运动分布，而部署时需通过推理时引导（inference-time guidance）额外施加任务约束（如避障），导致生成轨迹可能偏离数据分布、产生不可行或物理上不合理的运动。
- **研究动机**：推理时引导无法从根本上消除训练与目标之间的差异；后处理微调往往收敛至次优解且增加额外开销。同时，专家演示收集成本高昂，而大量非专家交互数据（包括违反约束的负样本）被忽视。因此，需要一种将约束直接融入训练阶段、同时利用负监督提高数据效率的方法。
- **整体含义**：提出 ConFlow 框架，通过训练时约束引导和结构化先验源分布，弥合训练–推理差距，生成更安全、平滑且满足边界条件的运动轨迹，并降低对专家数据量的依赖。

## 2. 论文提出的方法论

### 2.1 核心思想
- **约束引导训练**：将可微约束函数（如碰撞代价）的梯度直接加入流匹配的训练目标，使网络学习到的速度场内化约束满足能力。
- **结构化源分布**：用条件高斯过程（Conditional Gaussian Process, CGP）替代标准高斯噪声作为源分布，编码轨迹平滑性先验和端点约束（起点/终点）。
- **负监督**：引入不可行演示（violating demonstrations）作为负样本，通过二元可行性掩码仅在负样本上激活约束修正项，引导模型远离违反约束的行为。

### 2.2 关键技术细节
- **约束引导目标函数**（式 (3)）：
  \[
  \mathcal{L}_{CG} = \text{MSE}\left( u_\theta^t - u_t - \lambda_c M(x_1) u_c^t \right)
  \]
  其中 \( u_c^t = \nabla_{x_t} C(x_t) \) 为约束函数梯度，\( M(x_1) \in \{0,1\} \) 标记目标轨迹是否违反约束，仅对负样本施加修正。
- **碰撞约束函数**（式 (6)）：采用 SoftPlus 公式平滑化碰撞惩罚，梯度更平滑。
  \[
  C_\gamma(x_t) = -\frac{1}{\gamma} \log\left(1 + \exp(-\gamma (d(x_t) - r))\right)
  \]
- **条件高斯过程源分布**：使用 RBF 核的 GP，并基于目标轨迹的起点和终点进行条件化，得到后验分布作为源分布（式 (8)）。这使得源轨迹与目标轨迹共享端点且自然平滑，简化了流匹配的传输问题。
- **训练算法**（Algorithm 1）：从混合数据集（正+负）采样，计算约束修正项，更新网络参数。
- **推理**：无需额外引导即可生成满足约束的轨迹；也可可选地加入推理时引导进一步提高安全性。

## 3. 实验设计

### 3.1 任务与数据集
- **任务**：双机器人二维导航（交换位置）。轨迹包含 32 个路点，4 个状态维度（两机器人的 x,y 坐标）。数据中约 30% 包含机器人间碰撞，构成正负混合数据集。
- **数据集规模**：10000 条轨迹（包含正演示和碰撞演示）。未公开具体正负比例，但约 30% 为负。

### 3.2 评测设置
- 生成 256 条轨迹，在三种推理设置下评估：
  - 无引导（no guidance）
  - 机器人避碰引导（robot-avoidance guidance）
  - 障碍物避碰引导（obstacle-avoidance guidance）
- **指标**：
  - 机器人碰撞率（robot-collision rate）
  - 物体碰撞率（object-collision rate，仅用于障碍物避碰设置）

### 3.3 对比方法
- **Flow Matching (FM)**：标准条件流匹配（高斯源分布，无约束训练）。
- **ConFlow (D+)**：仅用正演示训练，但使用约束引导和 CGP 源分布。
- **ConFlow (D+ ∪ D−)**：使用正+负演示训练（完整方法）。
- 附加消融：比较高斯源 vs. CGP 源分布。

## 4. 资源与算力

- **未明确说明**：论文未提供使用的 GPU 型号、数量、训练时长等具体算力信息。仅提到网络为残差 MLP（4 个残差块，隐藏维度 1024），AdamW 优化器，学习率 3e-4，batch size 2048，训练 20001 次迭代。梯度裁剪最大范数 3.0。这些参数可大致推断训练量，但无具体硬件细节。

## 5. 实验数量与充分性

- **实验数量**：主要包括：
  - 表 I：碰撞率消融实验，对比 3 种推理设置 × 4 种方法（ConFlow (D+∪D−)、ConFlow (D+)、FM (D+)、FM (D+∪D−)），共 12 个数据点。
  - 表 II：源分布消融实验，对比 Gaussian vs. CGP，包含碰撞率、平滑度中位数/95%分位数、起点误差、终点误差 5 个指标。
  - 图 2：定性可视化对比高斯源和 CGP 源生成的轨迹。
- **充分性评估**：
  - **充分**：基本覆盖了主要变量（约束训练、负数据、源分布）的消融，并在三种推理设置下验证。
  - **公平**：对比方法基线合理（标准 FM、无负数据的 ConFlow），所有方法在同一任务、同一数据集上评测。
  - **不足**：仅在一个二维双机器人任务上验证，缺乏高维机器人系统（如机械臂）或真实世界平台实验。缺乏与其他训练时约束方法（如约束感知流匹配[5]）的直接比较。无统计显著性检验（如置信区间、多次重复实验）。轨迹质量指标仅包含碰撞率和平滑度，未包含任务成功率、多样性、计算效率等。

## 6. 论文的主要结论与发现

- ConFlow 在所有推理设置下均获得最低碰撞率，验证了训练时约束集成可有效弥合训练–推理差距。
- 即使无推理时引导，ConFlow 的碰撞率（0.016）也低于标准 FM 有引导的情况（0.020），表明约束行为已被内化到速度场中。
- 负演示的加入进一步降低了碰撞率（ConFlow (D+∪D−) vs. ConFlow (D+)），而简单将负数据加入标准 FM 会导致性能下降，说明负监督需要结合约束引导才有益。
- 条件 GP 源分布比高斯源显著降低碰撞率（46%）、提升平滑度（中位数从 0.0678 降至 0.0174）并大幅降低端点误差（数量级减小）。

## 7. 优点

- **创新性**：将推理时引导前移至训练阶段，同时引入结构化源分布和负监督，系统性解决不匹配问题。
- **方法简洁有效**：仅通过修改训练目标和源分布，无需复杂推理时计算，提升安全性和质量。
- **数据效率**：利用非专家负演示降低对专家数据的依赖，实用性强。
- **消融实验完整**：分别验证了约束引导、负数据、源分布三个贡献的独立效果，结论清晰。
- **可视化支持**：图 2 直观展示 CGP 源生成更平滑轨迹，增强说服力。

## 8. 不足与局限

- **实验局限性**：
  - 仅在一个简单的二维双机器人导航任务上验证，缺乏对高维（如 7 自由度机械臂）或真实机器人平台的结果，泛化能力未知。
  - 未与现有训练时约束方法（如约束感知流匹配[5]）或基于强化学习的引导方法（如 FlowQ[1]）进行定量比较。
  - 未报告多次运行均值和方差，无法判断结果的统计稳定性。
  - 轨迹质量指标仅包含碰撞率和平滑度，缺乏任务成功率、计算开销、多样性等关键指标。
- **方法局限性**：
  - 约束函数需可微，且需设计合适的 SoftPlus 参数 γ 和 λ_c，可能对复杂约束（如关节限位、动力学可行性）需要额外工程。
  - 条件 GP 依赖于 RBF 核，核参数（长度尺度、方差）需人工设定或通过最大似然拟合，可能影响性能。
  - 负演示的获取仍需一定标注（标记是否违反约束），且论文未讨论负演示质量（如噪声）的影响。
- **应用限制**：目前仅适用于固定长度轨迹表示，对于变长轨迹或复杂交互任务需扩展。推理时引导虽可选，但论文未充分评估其计算开销。

（完）
