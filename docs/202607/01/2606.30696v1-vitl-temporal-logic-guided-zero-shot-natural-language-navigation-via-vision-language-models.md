---
title: "ViTL: Temporal Logic-Guided Zero-Shot Natural Language Navigation via Vision-Language Models"
title_zh: ViTL：面向时序逻辑引导的零样本自然语言导航的视觉-语言模型
authors: "Kaier Liang, Hengde Dai, Cristian-Ioan Vasile"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30696v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 带时间逻辑约束的零样本自然语言导航
tldr: 现有零样本导航方法仅支持单目标任务，无法处理含时序逻辑的多目标自然语言命令。ViTL框架通过LLM将命令编译为LTL公式并转为DFA，协调多通道值图实现动态重规划；同时引入方向性分数，在观测图像中标记前沿方向并提取VLM每方向分数。在HM3D上验证了零样本长时域导航能力，方向性分数提升了单目标导航的准确性和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30696v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 1017, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30696v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30696v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 906, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30696v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 336, \"label\": \"Table\"}]"
motivation: 现有零样本导航方法仅支持单目标，无法处理含时序逻辑约束的多目标长时域自然语言命令。
method: 任务层用LLM编译LTL公式并转为DFA，协调多通道值图与动态重规划；导航层在观测图上标记前沿方向，从VLM提取方向性分数。
result: 在HM3D上实现零样本长时域导航，方向性分数提升单目标导航准确率与效率，优于基线。
conclusion: ViTL首次实现零样本多目标时序导航，方向性分数是高效导航的关键。
---

## 摘要
使机器人能够遵循自然语言命令完成零样本长时程任务仍然具有挑战性。这需要从自然语言命令中提取隐式的时序与逻辑约束，并据此执行多个子任务。现有的零样本目标导航方法利用视觉-语言模型（VLM）引导未知环境中的基于前沿的探索，但局限于单目标任务。诸如“清理椅子或沙发，然后打开电视”等真实世界命令需要按照时序约束顺序导航至多个目标，而现有的零样本系统无法处理。我们提出ViTL框架，从两个层面解决这一差距。在任务层面，我们使用大语言模型（LLM）将自然语言命令编译为线性时序逻辑（LTL）公式，然后转换为确定性有限自动机（DFA），以协调多通道价值图，并在检测到新目标时触发动态重规划。在导航层面，我们引入方向评分：不在整个视野中产生与方向无关的价值，而是在观测图像上标记前沿方向，并从VLM中提取每个方向上的评分。在Habitat-Matterport 3D（HM3D）上的实验表明，完整的框架能够实现零样本长时程完成具有时序约束的自然语言导航任务，并且方向评分相比基线方法提高了单目标导航的精度和效率。

## Abstract
Enabling robots to follow natural language commands to complete zero-shot long-horizon tasks remains challenging. It requires extracting implicit temporal and logical constraints from natural language commands and executing multiple sub-tasks accordingly. Recent zero-shot object navigation methods use vision-language models (VLMs) to guide frontier-based exploration in unknown environments, but they are limited to single-target tasks. Real-world commands such as "Clean either the chair or the couch, then turn on the tv." require navigating to multiple targets in a temporally constrained order, which no existing zero-shot system can handle. We present ViTL, a framework that addresses this gap at two levels. At the task level, we use a large language model (LLM) to compile natural language commands into Linear Temporal Logic (LTL) formulas, which are then converted into Deterministic Finite Automata~(DFA) that coordinate multi-channel value maps and trigger dynamic replanning when new objects are detected. At the navigation level, we introduce directional score: rather than producing a direction-agnostic value across the entire field of view, we label frontier directions on the observation image and extract per-direction scores from the VLM. Experiments on Habitat-Matterport 3D (HM3D) show that the full framework enables zero-shot long-horizon completion of natural language navigation tasks with temporal constraints, and that directional score improves single-target navigation accuracy and efficiency over the baseline.

---

## 论文详细总结（自动生成）

# 论文 ViTL 结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有零样本目标导航（ObjectNav）方法仅支持单目标任务，无法处理包含时序和逻辑约束的多目标长时程自然语言命令（例如“先清理椅子或沙发，然后打开电视”）。
- **背景**：真实世界机器人需要理解自然语言中的隐式时序依赖（如先后顺序、析取选择），并在未知环境中零样本地完成多目标导航。
- **贡献**：首次提出一个零样本框架 ViTL，能够处理带有复杂时序约束的自然语言导航命令，填补了该领域空白。

## 2. 论文提出的方法论

### 2.1 核心思想
- **双层次设计**：
  - **任务层**：将自然语言命令编译为 LTL（线性时序逻辑）公式，再转换为 DFA（确定性有限自动机），利用 DFA 图结构进行动态目标选择与重规划。
  - **导航层**：提出方向评分（Directional Score），在观测图像的每个前沿方向标注字母，由 VLM 逐方向评分，生成空间变化的稠密价值图，引导探索。

### 2.2 关键技术细节

#### 任务层：从自然语言到自动机
- **LTL 编译**：单次 LLM 调用将命令转为 LTL 公式 φ，例如：`♢((chair ∨ couch) ∧ ♢ tv)`。
- **DFA 转换**：使用标准工具（如 lomap）将 LTL 编译为 DFA。DFA 状态跟踪任务进展，边对应到达某目标。
- **动态目标选择**：维护检测集 D、到达集 R、失败集 B。每条边权重 w(e) 根据目标状态动态赋值（检测到 w_det ≪ 1，失败 w_fail ≫ 1，未检测 1.0）。通过最短加权路径选择下一个目标 ck。
- **多通道价值图**：每个目标类别独立维护一个价值图通道，并行更新，支持目标切换时保留已有探索信息。

#### 导航层：方向评分与平滑价值图
- **方向评分**：将前沿点投影到 RGB 图像，标记字母 A、B、C…，并提供“探索其他地方”选项。通过 VLM 的 next-token logits 计算每个前沿的概率 P(fi|It, ck) 和场景相关性 S_curr = P(Yes|It, ck)。每个前沿的当前价值 V_curr(fi) = S_curr × P(fi|It, ck)。
- **时序融合**：使用场景加权平均融合新旧观测值：  
  `˜V(fi) = V_old(fi) + V_curr(fi) * (S_curr / (S_old(fi) + S_curr))`
- **空间平滑**：基于前沿的方位角进行线性插值，并在 FOV 边界插入虚拟边缘点（衰减因子 β），生成平滑梯度价值图。
- **前沿选择**：选择半径 r 内像素最大值最高的前沿作为导航点，由预训练 PointNav 策略驱动低层动作。

#### 算法流程（Algorithm 1）
- 初始化 DFA、目标集、空价值图 → 选择初始目标 → 循环执行：检测新目标 → 更新 D、重规划 → 更新价值图 → 若目标已检测则直航，否则通过价值图选择前沿探索 → 到达目标则更新 DFA 状态并选新目标，失败则加入 B 并重规划 → 直到 DFA 到达接受状态或超时。

## 3. 实验设计

### 3.1 数据集与场景
- **单目标导航**：HM3D ObjectNav v2 验证集（2000 个 episode，20 场景，6 类目标：chair, bed, potted plant, toilet, tv monitor, couch）。
- **任务级规划器评估**：自行构建的 2000 个自然语言导航任务，每个配有 ground-truth LTL 公式，模板覆盖直到 6 个目标、析取分支、until 约束。
- **全系统评估**：从 HM3D 中选取两个单目标导航成功率接近 90% 的场景（ziup5kvtCCR 和 svBbv1Pavdk）以减少导航层噪声。

### 3.2 基准对比方法
- **单目标导航**：对比 CoW、ESC、L3MVN、VLFM、InstructNav、WMNav、GAMap、SG-Nav-GPT 等零样本方法。
- **任务级规划器**：对比 LLM-planner（使用相同 LLM GPT-5.2，直接在每个决策点提示 LLM 选目标，而不是编译 LTL）。
- **全系统**：对比 ViTL 与 LLM-planner 在相同导航执行器下的路径长度。

### 3.3 主要指标
- 单目标：成功率（SR）、成功加权路径长度（SPL）。
- 任务级：任务完成率、平均探索次数。
- 全系统：平均路径长度。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量、训练时长。仅提及 LLaVA-NeXT-34B 使用 8-bit 量化，YOLOv7-E6E 用于检测，以及使用预训练 PointNav 策略。未提及其他训练开销。
- 因此，**论文未提供详细的算力资源信息**，这是实验透明度方面的一个缺失。

## 5. 实验数量与充分性

- **单目标导航**：在 2000 个 episode 上测试，对比 8 种方法，结果具有统计意义。但仅在一个数据集（HM3D）上验证，未在其他数据集（如 Gibson、MP3D）上测试。
- **任务级规划器**：在 2000 个合成的复杂任务上评估，覆盖不同 pobs 概率（0.1 到 1.0），消融了 LLM 翻译准确率的影响。实验设计公平：ViTL 与 LLM-planner 共享相同 LLM 和环境信息，差异仅在于规划机制。
- **全系统评估**：仅两个场景、50 个互完成 episode，样本量较小，但足以证明趋势。
- **消融实验**：未对方向评分中的时序融合、空间平滑、边缘衰减因子等进行消融，也未分析不同 β 的影响。
- **总体评价**：实验设计较充分，但数据集覆盖有限，消融实验不够全面。

## 6. 论文的主要结论与发现

- ViTL 在单目标零样本导航上达到与当前最优方法相当的性能（SR 54.1%，SPL 30.9），使用开源 LLaVA-34B 而不依赖商业 VLM。
- 任务级规划器：在 2000 个任务中，NL→LTL 翻译准确率 95.3%，一旦翻译正确，DFA 保证 100% 任务完成。而 LLM-planner 仅 88.3% 完成率，且存在时序约束违反和提前终止的问题。
- 全系统评估中，ViTL 平均路径长度比 LLM-planner 减少 15.31%，证明规划优势可转化为实际导航效率提升。
- 方向评分优于统一的 FOV 评分，可产生空间变化的导航价值。

## 7. 优点

- **创新性**：首次将 LTL/DFA 用于零样本多目标导航，并引入方向评分，解决时序约束和空间感知问题。
- **实用性**：使用单次 LLM 调用而非反复查询，降低延迟和错误累积；开源模型可离线部署。
- **形式化保证**：DFA 自动机提供可验证的时序满足性，避免 LLM 常见的逻辑错误。
- **模块化设计**：任务层与导航层解耦，可分别改进；多通道价值图支持高效重规划。
- **实验公平**：任务级规划器评估中控制变量，LLM-planner 使用相同 LLM 和环境信息。

## 8. 不足与局限

- **数据集局限**：仅 HM3D 单数据集，未验证泛化性到其他环境（如 Gibson、ScanNet）。
- **消融不充分**：未对方向评分中的组件（时序融合、平滑方法、边缘衰减因子）进行消融分析，无法量化各贡献。
- **假设限制**：子任务简化为“到达物体”，未考虑操作/交互；动词统一为 reach，损失语义丰富性。
- **实验规模有限**：全系统评估仅两个场景、50 个 episode，统计显著性不足。
- **资源报告缺失**：未说明 GPU 型号、显存、推理时间等，复现难度增加。
- **隐式 bias**：NL→LTL 翻译依赖预训练 LLM，可能存在对特定命令格式的偏见；使用的 LLaVA 模型本身具有知识偏差，可能影响场景评分准确性。
- **未讨论实时性**：缺乏 VLM 推理时间的分析，难以评估实际机器人部署可行性。

（完）
