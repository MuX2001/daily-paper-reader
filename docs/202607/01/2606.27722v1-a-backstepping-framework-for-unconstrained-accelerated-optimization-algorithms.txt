Title: A Backstepping Framework for Unconstrained Accelerated Optimization Algorithms

URL Source: https://arxiv.org/pdf/2606.27722v1

Published Time: Mon, 29 Jun 2026 00:25:13 GMT

Number of Pages: 14

Markdown Content:
> IEEE TRANSACTIONS AND JOURNALS TEMPLATE 1

# A Backstepping Framework for Unconstrained Accelerated Optimization Algorithms 

Song Chen, Jiaxu Liu, and Chao Xu, Senior Member, IEEE 

Abstract — This paper introduces a control-theoretic per-spective on unconstrained optimization algorithms using the backstepping methods. We model the optimization pro-cess as an augmented strict-feedback system given by 

˙x1 = x2, ˙x2 = u, and ˙z = q(x1, z), with a regulated out-put y = ∇f(x1). This formulation recasts the development of unconstrained optimization algorithms as a feedback control problem, where the goal is to design the input 

u to ensure y(t) → 0. By employing backstepping, we recursively synthesize the actual feedback law u after ini-tially selecting a virtual control for x1. For convex objective functions, we develop a general synthesis framework for augmented strict-feedback systems and specialize it to the standard strict-feedback case. This unified framework suc-cessfully recovers the constant-parameter Nesterov flow and the proportional-integral-derivative (PID) accelerated optimizer as direct corollaries. We further establish that, given a fixed virtual control, the universal second-step law is inverse optimal with respect to an induced outer-tracking problem. This reveals that the optimality of the control law is conditionally dependent on the target man-ifold prescribed by the virtual control, rather than holding globally across all possible backstepping designs. Finally, we formulate a formal optimal-backstepping theorem that elevates this optimality principle to the virtual-control stage by solving a reduced Hamilton–Jacobi–Bellman problem. These contributions collectively yield a robust and general backstepping-driven paradigm for the analysis and design of continuous-time unconstrained optimization algorithms. 

Index Terms — Backstepping control, accelerated opti-mization algorithm, inverse optimality, optimal control 

I. I NTRODUCTION 

# CONTROL concepts increasingly shape the design of modern learning systems, not only their analysis. Across 

This work was supported by the Key Research and Develop-ment Project of China National Tobacco Corporation under Grant 110202402018 and the National Natural Science Foundation of China (No. 62573382, No. 12171431, No. 62373323, No. 72342025). (Corre-sponding author: Chao Xu, Jiaxu Liu.) 

Song Chen is with the Department of Mathematics, National University of Singapore, Singapore, Singapore (e-mail: song.chen@nus.edu.sg). Jiaxu Liu is with the School of Mathematical Sciences, Zhe-jiang University, Hangzhou, Zhejiang 310027, China. (e-mail: jiax-uliu@zju.edu.cn) Chao Xu is with the State Key Laboratory of Industrial Control Tech-nology, the Institute of Cyber-Systems and Control, College of Control Science and Engineering, Zhejiang University, Hangzhou, Zhejiang 310027, China, and also with Huzhou Institute of Zhejiang University, Huzhou, Zhejiang 313000, China. (e-mail: cxu@zju.edu.cn). 

contemporary artificial intelligence, control-inspired mech-anisms have become architectural and algorithmic primi-tives: PID feedback ideas have motivated real-time semantic-segmentation architectures such as PIDNet [1], explicit conditional-control modules have been introduced into large text-to-image diffusion models through ControlNet [2], state-space modeling principles rooted in systems and signal pro-cessing now underpin scalable sequence architectures such as S4 [3] and Mamba [4], and PID-type feedback has been used to accelerate deep-learning optimization [5]. These examples span perception, generation, sequence modeling, and training dynamics, yet they convey a common message: control can lead to better learning mechanisms, not merely better post hoc explanations. Taken together, they point to a broader and promising research direction—control for better learning—in which feedback, memory, damping, and state-space structure are treated as design primitives for improving modern learning systems. Within this broad agenda, the present paper focuses on control and optimization. Iterative optimization schemes can often be interpreted as feedback interconnections between an algorithmic update rule and an oracle that provides gradient, stochastic-gradient, or proximal information. From a discrete-time perspective, many optimization algorithms can be repre-sented as feedback interconnections between an algorithmic update rule and an oracle that provides gradient, stochastic-gradient, or proximal information. To make this concrete, consider the standard gradient descent algorithm, xk+1 =

xk − η∇f (xk). From a control-theoretic perspective, this iter-ative process is a simple closed-loop system: the optimization variable xk acts as the system’s “state,” the linear update mechanism xk+1 = xk +ηu k serves as the dynamic “plant” (an integrator), and the gradient computation uk = −∇ f (xk) acts as a nonlinear feedback controller that observes the current state and applies a corrective force and this viewpoint has been considered in [6]. Furthermore, classical momentum and accelerated methods [7], [8] have therefore been studied using integral-quadratic-constraint (IQC) analyses and algorithm-design tools [9], [10], dissipativity-based analyses [11], [12], and Lyapunov arguments for accelerated schemes [13], [14]. In addition, proportional-integral-derivative (PID)-type con-trol ideas have been introduced into stochastic optimization, showing that PID feedback can be used to accelerate training dynamics in deep neural networks [15]. These developments suggest that control theory provides not only a language for describing iterative optimization algorithms, but also a sys- 

> arXiv:2606.27722v1 [math.OC] 26 Jun 2026 2IEEE TRANSACTIONS AND JOURNALS TEMPLATE

tematic framework for analyzing convergence rates, robustness margins, and algorithmic parameter choices. The connection between optimization and control is even more explicit in continuous time, where algorithms are viewed as discretizations or sampled implementations of underly-ing dynamical systems. Accelerated and momentum methods [7], [8] admit ordinary differential equation (ODE) models [16], variational interpretations [17], continuous-time Lya-punov explanations of acceleration [13], [18], and advanced stability characterizations such as fixed-time stable gradient flows [19]. More refined dynamical descriptions further reveal the distinct mechanisms behind Nesterov acceleration and heavy-ball dynamics, for example through high-resolution differential equations [20] and dynamical-systems interpreta-tions of Nesterov acceleration [21]. Beyond purely dissipative flows, Hamiltonian and symplectic perspectives emphasize the roles of energy, damping, and structure-preserving discretiza-tion in optimization dynamics [22], [23]. Moreover, control-theoretic synthesis has been actively used to explicitly design continuous-time optimization dynamics. For unconstrained and hybrid dynamics, synthesis tools have generated contin-uous frameworks [24] and output-feedback-based continuous-time PID optimization algorithms [5], [25]–[27]. Furthermore, constrained optimization problems have been actively refor-mulated as dynamic regulation tasks using dynamic Karush-Kuhn-Tucker (KKT) controllers [28], control barrier functions [29], feedback linearization techniques [30], and explicit feed-back control of Lagrange multipliers [31], alongside formal analyses of the regularity properties of these optimization-based controllers [32]. Overall, the discrete-time viewpoint highlights feedback interconnection, robustness certification, and controller-like tuning of iterative schemes, whereas the continuous-time viewpoint clarifies the energy, damping, and trajectory structures that underlie acceleration optimization dynamics. However, a fundamental limitation of the existing literature lies in its predominantly retrospective approach: one first adopts a predefined optimization algorithm or continuous-time model, and then develops a Lyapunov certificate or feed-back interpretation to characterize the algorithm’s dynamic behavior. This observation motivates the central question of this paper: Can the synthesis of unconstrained optimization algorithms be formulated not as a retrospective analysis of a pre-existing method, but as a proactive nonlinear control design problem? 

To answer this question, we develop a constructive back-stepping framework for continuous-time optimization dynam-ics. The motivation for adopting backstepping is twofold. First, many momentum-based [16], [20] and accelerated optimiza-tion models [5], [33] naturally have a cascaded structure, in which the position variable is driven by a momentum-like state and the momentum dynamics are shaped by a gradient-dependent input. This structure is closely aligned with the strict-feedback form underlying backstepping [34]. Second, backstepping is intrinsically constructive: it recursively builds a stabilizing feedback law together with an associated con-trol Lyapunov function, rather than analyzing a prescribed algorithm after the fact [35]. This constructive versatility has been successfully demonstrated across a wide variety of advanced control paradigms, including output-feedback and event-triggered designs [36], making it particularly suitable for proactive algorithm synthesis. To be specific, in this paper, we cast the optimization process as the regulation of the second-order plant 

˙x1 = x2, ˙x2 = u, (1) possibly augmented with additional dynamic extensions. For unconstrained convex optimization, the condition ∇f (x1) = 0 characterizes optimality under standard assumptions. We therefore take ∇f (x1) as the regulated output and design the dynamics so that 

∇f (x1(t)) → 0.

Under this formulation, an optimization algorithm is not assumed in advance. It is generated through the backstepping procedure: one first designs a virtual control that defines a desired descent manifold for the position dynamics, and then synthesizes the actual control input that drives the momentum state toward this manifold. Furthermore, shifting from retrospective analysis to con-structive synthesis naturally invites a deeper investigation into algorithm optimality. Classical nonlinear inverse-optimal control demonstrates that stabilizing feedback laws constructed via backstepping are inherently inverse optimal with respect to some meaningful cost functionals [37], a property that extends even to modern prescribed-time and stochastic sta-bilization settings [38]. However, translating this property to backstepping-generated optimization algorithms raises a subtle but critical issue: if the synthesized control law is inverse optimal, what exactly is it optimizing—the overall algorithmic trajectory, or merely the tracking of a specific intermediate path? 

Our analysis shows that the latter is the correct interpre-tation. For a fixed virtual control α, the actual control u⋆ is inverse optimal only for the induced outer-tracking problem associated with the manifold selected by α. Hence the inverse optimality of the universal second-step law is conditional rather than global: it is a statement about the optimal tracking of a preassigned target manifold, not an optimality statement over all admissible backstepping designs. Genuine algorithm-level optimality must therefore be lifted to the virtual-control stage. To the best of our knowledge, the explicit recasting of continuous-time unconstrained optimization algorithm synthe-sis as a backstepping design problem for a strict-feedback system—together with the separation between outer inverse-optimal tracking and inner virtual-control optimality—has not been formulated in the existing optimization and nonlinear-control literature cited above. This is the central viewpoint advanced in the present paper. The main contributions are as follows. 1) We establish an explicit bridge between unconstrained optimization algorithms, strict-feedback systems, and recursive backstepping design. Under this bridge, the task of constructing an optimization algorithm is refor-mulated as the task of designing a stabilizing feedback AUTHOR et al. : TITLE 3

law for regulating the output ∇f (x1) of a second-order plant together with its augmented strict-feedback extensions. 2) We derive a general backstepping synthesis for aug-mented strict-feedback systems and a corresponding strict-feedback specialization. This framework recovers a constant-parameter Nesterov flow [8], [16] and the recent proportional-integral-derivative accelerated opti-mizer (PIDAO) [5] as direct theorem-level instances. 3) We prove that, for a fixed virtual control, the universal second-step law is inverse optimal only for an induced outer-tracking problem. This provides a precise theoret-ical statement of why the optimality of u⋆ should be interpreted as “given-manifold optimal tracking” rather than as a globally optimal backstepping design. 4) We formulate a genuine optimal-backstepping princi-ple by imposing optimality at the virtual-control stage through a reduced Hamilton–Jacobi–Bellman problem. This yields a two-layer interpretation: the virtual control is optimal for a prescribed reduced problem, while the actual control is inverse optimal for the induced outer-tracking problem. The remainder of the paper is organized as follows. Sec-tion II formulates the problem. Section III develops the general backstepping framework for gradient regulation. Furthermore, Section IV presents direct theorem-level realizations corre-sponding to the constant-parameter Nesterov flow and the PIDAO flow. Section V establishes the fixed-virtual-control inverse-optimality result and the optimal-backstepping princi-ple. Section VI presents representative examples generated by the two-layer optimality framework. Section VIII concludes. 

II. P ROBLEM FORMULATION 

Consider the unconstrained optimization problem 

min  

> x∈Rn

f (x),

where f : Rn → R is the objective function. To solve this problem efficiently, one often employs accelerated first-order methods, such as the heavy-ball method [7] and Nesterov’s accelerated method [8]; see also the continuous-time interpre-tation in [16]. In continuous time, such accelerated algorithms can be viewed as closed-loop realizations of the second-order plant 

˙x1 = x2,

˙x2 = u, (2) where x1, x 2 ∈ Rn. For example, the feedback law u =

−ax 2 −∇ f (x1) with a > 0 produces the heavy-ball dynamics [7]. More generally, continuous-time optimization dynamics can be interpreted as feedback laws for (2), so that the design of an optimization algorithm becomes a controller-design problem: construct u such that the output y := ∇f (x1)

converges to zero. This viewpoint is also consistent with recent control-inspired optimization constructions such as PIDAO [5]. Motivated by this perspective, we first study the more general augmented strict-feedback system 

˙x1 = x2, ˙x2 = u, ˙z = q(x1, z ),

where z ∈ Rm is an auxiliary state and q : Rn × Rm →

Rm is assumed to be locally Lipschitz. The auxiliary state is introduced to capture additional controller or algorithmic dynamics beyond the basic second-order plant. For example, one may introduce a filtered-gradient state z = ξ satisfying 

˙ξ = −βξ + ∇f (x1) with β > 0, so that ξ acts as a filtered gradient state. When the auxiliary dynamics are absent, this system reduces to the standard second-order plant (2). Let 

f ⋆ := inf  

> x∈Rn

f (x),ϕ(x1) := f (x1) − f ⋆.

The control objective is to design u such that 

∇f (x1(t)) → 0 as t → ∞ , (3) without requiring convergence of x1(t) to any prescribed minimizer. This formulation is natural for convex objectives that are not necessarily strongly convex, where stationarity is the fundamental requirement and the minimizer need not be unique. To achieve (3), this paper adopts a backstepping design for the augmented strict-feedback system [34]. The construction proceeds in two steps. 1) Virtual-control design. The pair (x1, z ) is first viewed as a reduced subsystem with a virtual input x2. One then selects a virtual control 

x2 = α(x1, z ),

so that the reduced dynamics 

˙x1 = α(x1, z ), ˙z = q(x1, z )

render the output y = ∇f (x1) dissipative or contractive in a suitable Lyapunov/storage sense. The purpose of this step is to determine the target manifold and the first-step energy structure that encode the desired optimization behavior. 2) Actual-control design. Since x2 is a state rather than a directly assignable input, the actual control u must be designed so that x2 tracks the prescribed virtual control 

α(x1, z ). To this end, one introduces the backstepping error 

e := x2 − α(x1, z ),

and then constructs u to stabilize the e-dynamics while preserving the first-step dissipation established above. The purpose of this step is to force the full closed-loop system toward the target manifold e = 0 and thereby recover the desired asymptotic regulation ∇f (x1(t)) →

0.To ease the forthcoming analysis, we collect the standing assumptions and auxiliary lemmas used throughout the subse-quent development. 

Assumption 1: The objective function f : Rn → R is of class C2, convex, and L-smooth, i.e., 

∥∇ f (x) − ∇ f (y)∥ ≤ L∥x − y∥, ∀x, y ∈ Rn,

and is bounded from below: f ⋆ > −∞ .4 IEEE TRANSACTIONS AND JOURNALS TEMPLATE 

Under Assumption 1, convexity and L-smoothness imply 

0 ⪯ ∇ 2f (x1) ⪯ LI. (4) 

Lemma 1: [39] Under Assumption 1, the bound 

12L ∥∇ f (x1)∥2 ≤ f (x1) − f ⋆ = ϕ(x1)

holds for all x1 ∈ Rn.

Lemma 2 (Barbalat’s Lemma): [40] Let h : [0 , ∞) → R

be uniformly continuous and satisfy R ∞ 

> 0

|h(t)| dt < ∞. Then 

h(t) → 0 as t → ∞ .

Lemma 3: Suppose that along a trajectory of any system satisfying ˙x1 = x2, the following conditions hold: 1) ϕ(x1(t)) is bounded on [0 , ∞);2) x2(t) is bounded on [0 , ∞);3) R ∞ 

> 0

∥∇ f (x1(t)) ∥2 dt < ∞.Then ∇f (x1(t)) → 0 as t → ∞ .

Proof: Let h(t) := 12 ∥∇ f (x1(t)) ∥2. By Lemma 1, boundedness of ϕ(x1(t)) implies boundedness of ∇f (x1(t)) .Moreover, we have 

˙h = ∇f (x1(t)) ⊤ ddt ∇f (x1(t)) = ∇f (x1(t)) ⊤∇2f (x1(t)) x2.

Using (4), we can derive 

| ˙h| ≤ ∥∇ f (x1(t)) ∥ ∥∇ 2f (x1(t)) ∥ ∥ x2∥≤ L∥∇ f (x1(t)) ∥ ∥ x2∥.

Hence, ˙h is bounded, and h is uniformly continuous. Since 

Z ∞

> 0

h(t) dt = 12

Z ∞

> 0

∥∇ f (x1(t)) ∥2 dt < ∞,

Lemma 2 yields h(t) → 0, i.e., ∇f (x1(t)) → 0.

III. A G ENERAL BACKSTEPPING FRAMEWORK FOR 

GRADIENT REGULATION 

We now develop a recursive construction in the spirit of classical backstepping for strict-feedback nonlinear systems [34], but with the gradient output ∇f (x1) rather than the plant state as the regulated quantity. 

Theorem 1 (General backstepping synthesis): Consider the augmented system 

˙x1 = x2,

˙x2 = u, 

˙z = q(x1, z ),

(5) where z ∈ Rm is an auxiliary state and q : Rn × Rm → Rm

is locally Lipschitz. The case with no auxiliary dynamics is included by taking m = 0 and suppressing the z-dependence. Let a virtual control α(x1, z ) be selected and define the backstepping error e := x2 − α(x1, z ). Assume that the following conditions hold: 1) There exists a continuously differentiable function 

V1(x1, z ) and constants cϕ > 0, cz > 0, σ > 0, and 

λ > 0 such that 

V1(x1, z ) ≥ cϕϕ(x1) + cz ∥z∥2 (6) for all (x1, z ), and such that along 

˙x1 = α(x1, z ) + e, ˙z = q(x1, z )

one has 

˙V1 = −σ∥∇ f (x1)∥2 + λ∇f (x1)⊤e. (7) 2) If (∇f (x1), z ) is bounded, then so is α(x1, z ).We choose the actual control input as 

u = ˙ α − ce − λ∇f (x1), c > 0, (8) where ˙α denotes the total derivative of α(x1, z ) along (5). Then the closed-loop system satisfies 

˙V = −σ∥∇ f (x1)∥2 − c∥e∥2, V := V1 + 12 ∥e∥2, (9) and consequently 

∇f (x1(t)) → 0 as t → ∞ . (10) 

Proof: Since e = x2 − α(x1, z ), one has ˙e = u − ˙α, and under the feedback law (8), 

˙e = −ce − λ∇f (x1).

Therefore, we can obtain 

˙V = ˙V1 + e⊤ ˙e

=  −σ∥∇ f (x1)∥2 + λ∇f (x1)⊤e + e⊤(−ce − λ∇f (x1)) = −σ∥∇ f (x1)∥2 − c∥e∥2,

which proves (9). Hence, V (t) is nonincreasing and bounded from below. By (6), we can derive 

cϕϕ(x1(t)) + cz ∥z(t)∥2 + 12 ∥e(t)∥2 ≤ V (t) ≤ V (0) ,

which implies boundedness of ϕ(x1(t)) , z(t), and e(t). By Lemma 1, ∇f (x1(t)) is bounded. By assumption, bounded-ness of (∇f (x1(t)) , z (t)) implies boundedness of α(x1, z ),hence x2(t) = e(t) + α(x1(t), z (t)) is bounded. Moreover, from (9), we have 

Z ∞

> 0

∥∇ f (x1(t)) ∥2 dt ≤ V (0) − inf Vσ < ∞.

Applying Lemma 3 to the projected (x1, x 2)-trajectory yields (10). 

Corollary 1 (Strict-feedback specialization of Theorem 1): 

Consider the standard strict-feedback system 

˙x1 = x2, ˙x2 = u. (11) Let a virtual control α(x1) be selected and define e := x2 −

α(x1). Assume that there exist a continuously differentiable function V1(x1) and constants cϕ > 0, σ > 0, and λ > 0 such that 

V1(x1) ≥ cϕϕ(x1)

for all x1, and such that along 

˙x1 = α(x1) + e

one has ˙V1 = −σ∥∇ f (x1)∥2 + λ∇f (x1)⊤e. (12) Assume in addition that boundedness of ∇f (x1) implies boundedness of α(x1). If the actual control is chosen as 

u = ˙ α − ce − λ∇f (x1), c > 0,AUTHOR et al. : TITLE 5

where ˙α = ∂α ∂x 1 x2, then the closed-loop system satisfies 

˙V = −σ∥∇ f (x1)∥2 − c∥e∥2, V := V1 + 12 ∥e∥2,

and consequently ∇f (x1(t)) → 0 as t → ∞ .

Proof: This is the special case of Theorem 1 obtained by suppressing the auxiliary state z.

Remark 1: Theorem 1 and Corollary 1 provide a structural framework rather than a unique algorithm. Their role is to separate the design into a first-step choice of the virtual control 

α and a second-step synthesis of the actual input u.1) Choosing the virtual control. The virtual control deter-mines the target manifold 

Mα := {(x1, z, x 2) : x2 = α(x1, z )}.

Its purpose is to shape the reduced dynamics 

˙x1 = α(x1, z ), ˙z = q(x1, z ),

so that the first-step storage function V1 satisfies the dissipation relation (7). Different choices of α lead to different optimization flows. Typical examples include  

> •

a gradient-type virtual control, e.g., 

α(x1) = −k∇f (x1);  

> •

a normalized-gradient virtual control, e.g., 

α(x1) = −k ∇f (x1)

ε + ∥∇ f (x1)∥ , ε > 0;  

> •

an adaptive or preconditioned gradient law, e.g., 

α(x1, z ) = −D(z)∇f (x1),

where D(z) is a state-dependent positive-definite scaling matrix generated by auxiliary dynamics, in the spirit of adaptive-gradient methods. 2) Designing the actual control. Once α has been fixed, the actual control u no longer decides the target manifold; instead, it is chosen to drive the full system toward Mα.This is achieved through the backstepping error 

e = x2 − α(x1, z ),

which measures the deviation from the prescribed virtual behavior. The second-step feedback law (8) is con-structed precisely so that e is stabilized and the full state approaches the manifold e = 0 , while preserving the first-step dissipation encoded by V1.

IV. D IRECT THEOREM -L EVEL APPLICATIONS VIA 

GRADIENT -T YPE VIRTUAL CONTROLS 

Theorem 1 and Corollary 1 already encode a family of optimization dynamics once the virtual control has been spec-ified. In particular, by choosing gradient-type virtual controls, one can directly obtain theorem-level realizations of both the constant-parameter Nesterov flow and the PIDAO flow. The first arises from the standard strict-feedback case covered by Corollary 1, whereas the second arises from the augmented setting covered by Theorem 1. 

Theorem 2 (Constant-parameter Nesterov flow): Let Assumption 1 hold. Consider the strict-feedback plant (11). Select the virtual control α(x1) := −k1∇f (x1) with k1 > 0,and define the backstepping error 

e := x2 − α(x1) = x2 + k1∇f (x1). (13) Choose the first-step Lyapunov function V1(x1) := ϕ(x1).Then, along the first-step dynamics 

˙x1 = α(x1) + e, 

one has ˙V1 = −k1∥∇ f (x1)∥2 + ∇f (x1)⊤e. (14) Hence the hypotheses of Corollary 1 are satisfied with cϕ = 1 ,

σ = k1, and λ = 1 . If the actual control is selected according to the backstepping law 

u = ˙ α − k2e − ∇ f (x1), k2 > 0, (15) then the explicit feedback law becomes 

u = −k2

 x2 + k1∇f (x1) − ∇ f (x1) − k1∇2f (x1)x2, (16) The resulting second-order dynamics are 

¨X + k2 ˙X + (1 + k1k2)∇f (X) + k1∇2f (X) ˙X = 0 , (17) and the corresponding closed-loop trajectory satisfies 

∇f (x1(t)) → 0 as t → ∞ . (18) 

Proof: Since x2 = e − k1∇f (x1), one obtains 

˙V1 = ˙ϕ = ∇f (x1)⊤x2 = −k1∥∇ f (x1)∥2 + ∇f (x1)⊤e, 

which proves (14). Thus the hypotheses of Corollary 1 are satisfied. Moreover, 

˙α = −k1∇2f (x1)x2. (19) Substituting (13) and (19) into (15) yields (16), and therefore (17). The asymptotic property (18) follows immediately from Corollary 1. Recently, Chen et al. [5] introduced the PIDAO from a con-trol perspective. The next theorem shows that its continuous-time dynamics arise directly from the augmented backstepping framework. 

Theorem 3 (PIDAO as a direct application of Theorem 1): 

Let Assumption 1 hold. Consider the augmented strict-feedback system 

˙x1 = x2,

˙x2 = u, 

˙ξ = ∇f (x1).

Choose the virtual control and the backstepping error as 

α(x1, ξ ) := −kd∇f (x1) − ki

a ξ, e := x2 − α = x2 + kd∇f (x1) + ki

a ξ, 

(20) where a > 0, kd > 0, and ki > 0. Define 

ρ := kp − ak d − ki

a , (21) 6 IEEE TRANSACTIONS AND JOURNALS TEMPLATE 

and assume that ρ > 0. Let the first-step Lyapunov function be chosen as 

V1(x1, ξ ) := ρ



ϕ(x1) + ki

2a ∥ξ∥2



. (22) Then, along the first-step dynamics 

˙x1 = α(x1, ξ ) + e, ˙ξ = ∇f (x1), (23) one has 

˙V1 = −ρk d∥∇ f (x1)∥2 + ρ∇f (x1)⊤e. (24) Hence the hypotheses of Theorem 1 are satisfied with z = ξ,

q(x1, ξ ) = ∇f (x1), cϕ = ρ, cz = ρk i/(2 a), σ = ρk d,and λ = ρ; moreover, boundedness of (∇f (x1), ξ ) implies boundedness of α(x1, ξ ). If the actual control is selected according to (8) with c = a and λ = ρ, then 

u = ˙ α − ae − ρ∇f (x1), (25) which yields the explicit feedback law 

u = −ax 2 − kp∇f (x1) − kiξ − kd∇2f (x1)x2, (26) The corresponding second-order dynamics are 

¨X +a ˙X +kp∇f (X)+ ki

Z t

> 0

∇f (X(s)) ds +kd

ddt ∇f (X) = 0 .

(27) Consequently, 

∇f (x1(t)) → 0 as t → ∞ . (28) 

Proof: Differentiating (22) along (23) gives 

˙V1 = ρ∇f (x1)⊤(α + e) + ρ ki

a ξ⊤∇f (x1).

Substituting α = −kd∇f (x1) − ki 

> a

ξ from (20) yields 

˙V1 = ρ∇f (x1)⊤



−kd∇f (x1) − ki

a ξ + e



+ ρ ki

a ξ⊤∇f (x1)= −ρk d∥∇ f (x1)∥2 + ρ∇f (x1)⊤e, 

which proves (24). Since 

˙α = −kd∇2f (x1)x2 − ki

a ∇f (x1), (29) substituting (20), (21), and (29) into (25) yields 

u = −kd∇2f (x1)x2 − ki

a ∇f (x1)

− a



x2 + kd∇f (x1) + ki

a ξ



− ρ∇f (x1)= −ax 2 −



ak d + ρ + ki

a



∇f (x1)

− kiξ − kd∇2f (x1)x2

= −ax 2 − kp∇f (x1) − kiξ

− kd∇2f (x1)x2,

which is (26). Equation (27) is the corresponding second-order form. The conclusion (28) then follows directly from Theorem 1. 

V. F IXED -V IRTUAL -C ONTROL INVERSE OPTIMALITY AND AN OPTIMAL BACKSTEPPING PRINCIPLE 

The next results separate two layers of optimality. The first layer is an inverse-optimal interpretation akin to classical nonlinear inverse-optimal control [37], [41], but specialized here to the backstepping architecture induced by a fixed virtual control. The second layer lifts the optimality requirement to the virtual-control stage itself. Following Remark 1, once the virtual control α is fixed, the target manifold 

Mα := {(x1, x 2, z ) : x2 = α(x1, z )}

is fixed as well. The role of the actual input u is then clear: it must drive the full system toward Mα, equivalently, it must regulate the outer error e := x2 − α(x1, z ) while preserving the first-step dissipation generated by (α, V 1). At first sight, this role is purely stabilizing or tracking in nature. The point of the next result is that the universal second-step law does more than stabilize this outer error: once α has selected the target manifold, the same law also solves an induced optimal control problem for the outer dynamics. Specifically, fix an admissible virtual control α(x1, z ) and a first-step storage function V1(x1, z ) satisfying the hypotheses of Theorem 1. For an arbitrary admissible control input u, the transformed dynamics in the coordinates (x1, z, e ) are 

˙x1 = α(x1, z ) + e, ˙z = q(x1, z ), ˙e = u − ˙α. 

Define 

V := V1 + 12 ∥e∥2,ℓα(x1, z, e, u ) := σ∥∇ f (x1)∥2 + c

2 ∥e∥2

+ 12c ∥u − ˙α + λ∇f (x1)∥2.

Along every admissible input u, the square-completion identity 

˙V + ℓα = 12c ∥u − ˙α + λ∇f (x1) + ce ∥2 ≥ 0. (30) holds. 

Proposition 1 (Fixed-virtual-control inverse optimality): 

With α, V1, V , and ℓα defined above, define for each T > 0

the Bolza functional 

JαT (u) := V (x1(T ), z (T ), e (T )) + 

Z T

> 0

ℓα(x1, z, e, u ) dt. 

Then the control 

u⋆ := ˙ α − ce − λ∇f (x1)

is, for every T > 0, the unique minimizer of the optimal control problem 

min  

> u

JαT (u),

where the minimization is over all admissible controls on 

[0 , T ].

Proof: For an arbitrary admissible input u, one has 

˙V = ˙V1 +e⊤(u− ˙α) = −σ∥∇ f (x1)∥2 +e⊤(u− ˙α+λ∇f (x1)) ,

where the last identity follows from (7). The functional JαT

is the natural Bolza functional associated with the outer AUTHOR et al. : TITLE 7

dynamics, because integrating the square-completion identity (30) over [0 , T ] yields 

JαT (u) = V (x1(T ), z (T ), e (T )) + 

Z T

> 0

ℓα(x1, z, e, u ) dt 

≥ V (x1(0) , z (0) , e (0)) .

(31) Moreover, equality in (31) holds if and only if the square term in (30) vanishes identically, namely, 

u = ˙ α − ce − λ∇f (x1). (32) Hence u⋆ attains the minimum value of JαT . Since ℓα is strictly convex in u, the minimizer is unique. 

Remark 2: The virtual control α selects the target manifold 

Mα := {(x1, x 2, z ) : x2 = α(x1, z )}.

Because e = 0 if and only if the state lies on Mα, the outer variable e = x2 − α(x1, z ) measures the deviation from that manifold. The Bolza functional JαT therefore has a direct geometric meaning. Its terminal term 

V (T ) = V1

 x1(T ), z (T ) + 12 ∥e(T )∥2

measures the residual first-step energy together with the resid-ual distance from Mα at the terminal time. Its running cost 

ℓα(x1, z, e, u ) = σ∥∇ f (x1)∥2 + c

2 ∥e∥2

+ 12c ∥u − ˙α + λ∇f (x1)∥2

penalizes, respectively, the remaining gradient energy, the instantaneous deviation from the selected manifold, and the excess control effort relative to the exact backstepping correc-tion needed to complete the square. From this viewpoint, the proposition says more than “ u⋆

stabilizes e”: once α has fixed the target manifold, the actual control law 

u⋆ = ˙ α − ce − λ∇f (x1)

is precisely the control that steers the full system toward 

Mα with minimum induced outer cost. In this sense, the inverse optimality of u⋆ is an outer tracking optimality relative to a fixed virtual-control design, and the functional JαT is not chosen ad hoc after the fact; it is the Bolza problem naturally generated by the storage identity associated with the chosen virtual control. Figure 1 summarizes this single-layer interpretation. 

Remark 3 (Nesterov flow as an instance of Proposition 1): 

The abstract statement of Proposition 1 becomes very concrete for the gradient-type virtual control 

α(x1) = −k1∇f (x1),

which later yields the constant-parameter Nesterov flow in Theorem 2. In this case, V1(x1) = ϕ(x1), e = x2 +k1∇f (x1),

σ = k1, λ = 1 , and c = k2; the selected target manifold is 

Mα = {(x1, x 2) : x2 = −k1∇f (x1)}.

Fix a virtual control α

The target manifold Mα = {x2 = α(x1, z )} and the outer error e = x2 − α(x1, z ) are determined. 

Induced outer problem 

The storage identity generates the Bolza functional 

JαT (u) associated with the chosen manifold Mα.

Universal second-step law 

Proposition 1 identifies 

u⋆ = ˙ α − ce − λ∇f (x1)

as the unique minimizer of min u JαT (u).

Single-layer inverse optimality 

The actual control is optimal only relative to the fixed virtual-control design. Different admissible choices of α generate different manifolds and different outer problems.            

> the chosen αinduces eand Mα
> complete the square in the outer dynamics
> conditional on the prescribed α
> Fig. 1. Single-layer optimal backstepping design for gradient flow. Once the virtual control αis fixed, it determines the target manifold and the induced outer Bolza functional. Proposition 1 then yields the unique op-timal actual control for this conditional outer problem, while Proposition 2 explains why changing αchanges the single-layer problem itself.

The induced outer optimal control problem is therefore to minimize 

JNest  

> T

(u) := ϕ x1(T )

+ 12 ∥x2(T ) + k1∇f (x1(T )) ∥2

+

Z T

> 0

h

k1∥∇ f (x1)∥2 + k2

2 ∥x2 + k1∇f (x1)∥2

+ 12k2

∥u − ˙α + ∇f (x1)∥2i

dt. 

Proposition 1 then says that the Nesterov feedback term 

u⋆ = −k1∇2f (x1)x2 − k2

 x2 + k1∇f (x1) − ∇ f (x1)

is exactly the unique minimizer of this problem. Thus, once the virtual control has declared that the desired first-step behavior is the gradient manifold x2 = −k1∇f (x1), the Nesterov correction can be interpreted as the optimal way to drive the full second-order system toward that manifold while minimizing the induced outer cost. Proposition 1 is therefore only a conditional statement: once α has been fixed, the second-step law is optimal for the induced outer problem. The next question is therefore which part of the backstepping construction still carries the real design freedom. Proposition 2 answers this by showing that the universal second-step law is essentially fixed by the first-step dissipation identity, whereas the nontrivial freedom remains in the choice of the virtual control itself. 8 IEEE TRANSACTIONS AND JOURNALS TEMPLATE 

Proposition 2 (Residual freedom in the virtual-control design): 

Suppose that V1 ∈ C1 and α ∈ C1 satisfy, for some constants 

σ > 0 and λ > 0, the first-step identity 

∇x1 V1(x1, z )⊤ α(x1, z ) + e + ∇z V1(x1, z )⊤q(x1, z )= −σ∥∇ f (x1)∥2 + λ∇f (x1)⊤e (33) pointwise for all (x1, z, e ). Then 

∇x1 V1 = λ∇f (x1), (34) 

∇x1 V ⊤ 

> 1

α + ∇z V ⊤ 

> 1

q = −σ∥∇ f (x1)∥2 = − σλ2 ∥∇ x1 V1∥2.

(35) Moreover, on the set {(x1, z ) : ∇x1 V1̸ = 0 }, every admissible virtual control admits the decomposition 

α = − ∇z V ⊤ 

> 1

q + σλ2 ∥∇ x1 V1∥2

∥∇ x1 V1∥2 ∇x1 V1 + β, 

∇x1 V ⊤ 

> 1

β = 0 .

(36) 

Proof: Fix (x1, z ) and rewrite (33) as 

 ∇x1 V1 − λ∇f (x1)⊤e = −σ∥∇ f (x1)∥2

− ∇ x1 V ⊤ 

> 1

α − ∇ z V ⊤ 

> 1

q. 

The right-hand side is independent of e. Since the identity holds for every e, we may first set e = 0 , which gives 

−σ∥∇ f (x1)∥2 − ∇ x1 V ⊤ 

> 1

α − ∇ z V ⊤ 

> 1

q = 0 .

Substituting this back into the previous relation yields 

 ∇x1 V1 − λ∇f (x1)⊤e = 0 , ∀e ∈ Rn.

We now prove (34) by contradiction. If ∇x1 V1−λ∇f (x1)̸ = 0 ,choose 

e = ∇x1 V1 − λ∇f (x1).

Then 

 ∇x1 V1 − λ∇f (x1)⊤e = ∥∇ x1 V1 − λ∇f (x1)∥2 > 0,

This contradiction proves (34). Substituting (34) into the e = 0 relation gives 

∇x1 V ⊤ 

> 1

α + ∇z V ⊤ 

> 1

q = −σ∥∇ f (x1)∥2.

Using ∇f (x1) = λ−1∇x1 V1 then yields (35). Finally, on the set {∇ x1 V1̸ = 0 }, decompose α as 

α = a ∇x1 V1 + β, ∇x1 V ⊤ 

> 1

β = 0 .

Substituting this into (35) gives 

a∥∇ x1 V1∥2 + ∇z V ⊤ 

> 1

q = − σλ2 ∥∇ x1 V1∥2,

so 

a = − ∇z V ⊤ 

> 1

q + σλ2 ∥∇ x1 V1∥2

∥∇ x1 V1∥2 .

Substituting this expression for a back into α = a ∇x1 V1 + β

gives (36). 

Remark 4: Equation (36) shows that, once V1 is fixed, the dissipation requirement determines only the component of α

parallel to ∇x1 V1. The tangential component β is free at first order. Figure 2 visualizes this decomposition: ∇x1 V1 is normal to a level set of V1, the constrained part of α lies along this normal direction, and β is the tangential residual 

V1 = const . larger V1

x1

∇x1 V1

a ∇x1 V1

β

α          

> Normal direction fixed by dissipation Tangential direction free
> Fig. 2. Geometric interpretation of Proposition 2. The value of V1
> increases outward across its level sets, so ∇x1V1points in the outward normal direction. The dissipation identity fixes the component of α
> parallel to ∇x1V1, while the orthogonal component βremains free.

freedom. Different choices of β generate different manifolds 

Mα, different errors e = x2 − α, different feedforward terms 

˙α, and therefore different induced performance indices JαT .Hence the inverse optimality of u⋆ does not rank different backstepping designs; it is conditional on the chosen virtual control. A useful special case occurs when the auxiliary contribution vanishes, i.e. when ∇z V ⊤ 

> 1

q = 0 , and when β = 0 . Then (36) reduces to 

α = − σλ2 ∇x1 V1 = − σλ ∇f (x1),

which is a negative-gradient virtual control because λ > 0. In particular, in the standard strict-feedback case with V1(x1) = 

ϕ(x1), one has ∇x1 V1 = ∇f (x1) and hence λ = 1 , so β = 0 

gives 

α = −σ∇f (x1).

This is exactly the gradient-type virtual control used in Theo-rem 2, with σ = k1.Propositions 1 and 2 therefore show that, once a virtual control α has been chosen, the universal second-step law is already optimally determined for the induced outer problem, whereas the genuine design freedom lies in the choice of α

itself. If one further requires the virtual control α to be optimal for a suitable reduced problem, then the entire backstepping construction acquires a genuine two-layer optimality interpre-tation. Figure 3 summarizes this hierarchy. 

Theorem 4 (Optimal backstepping principle): Consider the augmented system (5) and a prescribed reduced running cost 

Lr : Rn × Rm × Rn → R≥0. Assume that there exist a continuously differentiable reduced value function V ⋆ 

> 1

(x1, z ),a continuously differentiable virtual control α⋆(x1, z ), and constants cϕ > 0, cz > 0, λ > 0, and σ > 0 such that 

V ⋆ 

> 1

(x1, z ) ≥ cϕϕ(x1) + cz ∥z∥2, (37) 

∇x1 V ⋆ 

> 1

(x1, z ) = λ∇f (x1), (38) and the stationary Hamilton–Jacobi–Bellman equation for the reduced system 

˙x1 = a, ˙z = q(x1, z ), (39) AUTHOR et al. : TITLE 9

Reduced value function 

V ⋆ 

> 1

satisfies the gradient match ∇x1 V ⋆ 

> 1

=

λ∇f (x1) and solves the reduced HJB equation. 

Virtual-control stage 

α⋆ is optimal for the reduced Bolza problem. 

Proposition 2: the genuine design freedom lies here. 

Actual-control stage 

For fixed α⋆,

u⋆ = ˙ α⋆ − ce − λ∇f (x1)

is the unique minimizer of the induced outer Bolza problem. 

Proposition 1 .

Two-layer optimality 

Reduced optimality of α⋆ together with outer inverse optimality of u⋆

yields the final optimal backstepping interpretation and gradient convergence.            

> Theorem 4 certifies the optimal α⋆
> fix α⋆and induce the outer tracking problem
> combine the two layers
> Fig. 3. Two-layer optimal backstepping design for gradient flow. The reduced value function V⋆
> 1does not by itself constitute an “optimality” claim; rather, by solving the reduced Hamilton–Jacobi–Bellman equa-tion, it certifies that α⋆is optimal for the reduced problem. Once α⋆is fixed, Proposition 1 identifies the optimal actual control for the induced outer problem, and the two layers combine into the optimal backstepping interpretation of Theorem 4.

namely 

0 = min 

> a∈Rn

n

∇x1 V ⋆ 

> 1

(x1, z )⊤a

+ ∇z V ⋆ 

> 1

(x1, z )⊤q(x1, z ) + Lr (x1, z, a )

o

,

(40) holds for all (x1, z ), with minimizer a = α⋆(x1, z ). Assume in addition that 

Lr

 x1, z, α ⋆(x1, z ) ≥ σ∥∇ f (x1)∥2 (41) for all (x1, z ), and that boundedness of (∇f (x1), z ) implies boundedness of α⋆(x1, z ).Define e := x2 − α⋆(x1, z ), and choose the actual control 

u⋆ := ˙ α⋆ − ce − λ∇f (x1), c > 0, (42) where ˙α⋆ denotes the total derivative of α⋆ along (5). Then the following statements hold. 1) For every T > 0, α⋆ minimizes the reduced Bolza functional 

Jr,T (a) := V ⋆

> 1

 x1(T ), z (T ) +

Z T

> 0

Lr (x1, z, a ) dt 

over all admissible reduced controls a(·) for the system (39). 2) The full-order storage function V ⋆ := V ⋆ 

> 1

+ 12 ∥e∥2

satisfies 

˙V ⋆ = −Lr

 x1, z, α ⋆−c∥e∥2 ≤ − σ∥∇ f (x1)∥2−c∥e∥2.

(43) Consequently, 

∇f (x1(t)) → 0 as t → ∞ . (44) 3) For fixed α⋆, the actual control u⋆ is the unique mini-mizer, on every finite horizon [0 , T ], of the outer Bolza functional 

J α⋆ 

> o,T

(u) := V ⋆ x1(T ), z (T ), e (T )

+

Z T

> 0

h

Lr

 x1, z, α ⋆

+ c

2 ∥e∥2 + 12c ∥u − ˙α⋆ + λ∇f (x1)∥2i

dt. 

Therefore the resulting design has a two-layer interpre-tation: α⋆ is optimal for a prescribed reduced problem, whereas u⋆ is inverse optimal for the induced outer tracking problem. 

Proof: By (40), for every admissible reduced control a

one has 

∇x1 V ⋆ 

> 1⊤

a + ∇z V ⋆ 

> 1⊤

q + Lr (x1, z, a ) ≥ 0,

with equality at a = α⋆. Along the reduced system (39), this yields ˙V ⋆ 

> 1

+ Lr (x1, z, a ) ≥ 0. Integration on [0 , T ] therefore gives 

Jr,T (a) ≥ V ⋆

> 1

 x1(0) , z (0) ,

with equality for a = α⋆, which proves item 1. Next, along the full-order system with the control (42), one has ˙V ⋆ 

> 1

= ∇x1 V ⋆ 

> 1⊤

 α⋆ + e + ∇z V ⋆ 

> 1⊤

q

= −Lr

 x1, z, α ⋆ + λ∇f (x1)⊤e, 

where the last identity follows from (40) evaluated at the minimizing virtual control a = α⋆ together with (38). Since 

˙e = u⋆ − ˙α⋆ = −ce − λ∇f (x1), it follows that 

˙V ⋆ = ˙V ⋆ 

> 1

+ e⊤ ˙e = −Lr

 x1, z, α ⋆ − c∥e∥2,

which proves the equality in (43). By (37), V ⋆(t) is bounded from below and nonincreasing, hence ϕ(x1(t)) , z(t), and e(t)

are bounded. Lemma 1 yields boundedness of ∇f (x1(t)) , and the assumed boundedness implication then gives boundedness of α⋆(x1(t), z (t)) . Therefore x2(t) = e(t) + α⋆(x1(t), z (t)) 

is bounded. Moreover, integrating (43) and using (41) gives 

Z ∞

> 0

∥∇ f (x1(t)) ∥2 dt ≤ V ⋆(0) − inf V ⋆

σ < ∞.

Lemma 3 therefore yields (44), which proves item 2. Finally, fix α⋆ and let u be arbitrary. Then 

˙V ⋆ = −Lr

 x1, z, α ⋆ + e⊤ u − ˙α⋆ + λ∇f (x1).

Hence 

˙V ⋆ + Lr

 x1, z, α ⋆ + c

2 ∥e∥2

+ 12c ∥u − ˙α⋆ + λ∇f (x1)∥2

= 12c ∥u − ˙α⋆ + λ∇f (x1) + ce ∥2 ≥ 0.10 IEEE TRANSACTIONS AND JOURNALS TEMPLATE 

Integration on [0 , T ] yields J α⋆ 

> o,T

(u) ≥ V ⋆(0) , and equality holds if and only if u = u⋆. This proves item 3. 

VI. E XAMPLES 

This section illustrates how Theorem 4 generates concrete optimization dynamics from the two-layer optimality principle. We begin with a fully explicit quadratic benchmark, then show how strong convexity sharpens the conclusion to exponential convergence, and finally return to a more general augmented example that works for an arbitrary convex L-smooth objective without prescribing the detailed form of f .

Case 1: Quadratic Objective 

Theorem 4 admits a particularly transparent test example in the standard strict-feedback case with no auxiliary state and quadratic objective 

f (x1) = 12 ∥x1∥2, f ⋆ = 0 , ∇f (x1) = x1.

Fix a constant k > 0 and consider the reduced running cost 

Lr (x1, a ) := 12k ∥a∥2 + k

2 ∥x1∥2.

Choose 

V ⋆ 

> 1

(x1) = ϕ(x1) = 12 ∥x1∥2.

Then ∇x1 V ⋆ 

> 1

= ∇f (x1), so λ = 1 , and the reduced Hamilton– Jacobi–Bellman equation becomes 

0 = min 

> a∈Rn



x⊤ 

> 1

a + 12k ∥a∥2 + k

2 ∥x1∥2



.

Its unique minimizer is 

α⋆(x1) = −kx 1 = −k∇f (x1).

Hence the first layer of optimality is explicit: α⋆ minimizes the reduced Bolza functional 

Jr,T (a) = 12 ∥x1(T )∥2 +

Z T

> 0

 12k ∥a∥2 + k

2 ∥x1∥2



dt. 

Now define the backstepping error 

e = x2 − α⋆(x1) = x2 + kx 1.

Since ˙α⋆ = −kx 2, Theorem 4 gives the actual control 

u⋆ = ˙ α⋆ − ce − ∇ f (x1)= −kx 2 − c(x2 + kx 1) − x1

= −(k + c)x2 − (1 + ck )x1.

Therefore the final optimization dynamics are 

˙x1 = x2, ˙x2 = −(k + c)x2 − (1 + ck )x1.

This algorithm is genuinely produced by the two-layer prin-ciple: the virtual control α⋆ = −k∇f (x1) is optimal for the reduced problem, and then the actual control u⋆ is inverse optimal for the induced outer problem. Indeed, 

Lr

 x1, α ⋆(x1) = k∥x1∥2 = k∥∇ f (x1)∥2,

so Theorem 4 yields the Lyapunov identity 

V ⋆ = 12 ∥x1∥2 + 12 ∥x2 + kx 1∥2,

˙V ⋆ = −k∥x1∥2 − c∥x2 + kx 1∥2.

Thus this quadratic system is a clean benchmark for verifying the two-layer optimality interpretation. In the scalar case with 

k = 1 and c = 1 , one obtains the explicit algorithm 

˙x1 = x2, ˙x2 = −2x2 − 2x1.

Case 2: Strongly Convex Objectives 

Suppose now that f is in addition m-strongly convex with 

m > 0. Then 

∥∇ f (x1)∥2 ≥ 2mϕ (x1),

so the two-layer construction can be specialized to yield an exponentially stable optimization flow. Consider the standard strict-feedback case without auxiliary state, and choose the reduced running cost 

Lr (x1, a ) := 12k ∥a∥2 + k

2 ∥∇ f (x1)∥2 + μϕ (x1),k > 0, μ > 0.

Take 

V ⋆ 

> 1

(x1) = ϕ(x1).

Then ∇x1 V ⋆ 

> 1

= ∇f (x1), so λ = 1 , and the reduced Hamilton– Jacobi–Bellman equation becomes 

0 = min 

> a∈Rn

n

∇f (x1)⊤a + 12k ∥a∥2

+ k

2 ∥∇ f (x1)∥2 + μϕ (x1)

o

.

Its unique minimizer is 

α⋆(x1) = −k∇f (x1).

Hence the first layer of optimality is again explicit: α⋆ is optimal for the reduced Bolza problem induced by Lr .Defining 

e = x2 − α⋆(x1) = x2 + k∇f (x1),

Theorem 4 yields the actual control 

u⋆ = ˙ α⋆ − ce − ∇ f (x1)= −k∇2f (x1)x2 − c x2 + k∇f (x1) − ∇ f (x1)= − k∇2f (x1) + cI x2 − (1 + ck )∇f (x1).

Therefore the resulting two-layer optimality algorithm is 

˙x1 = x2, ˙x2 = − k∇2f (x1) + cI x2 − (1 + ck )∇f (x1).

It is generated by the same two-layer mechanism as before: 

α⋆ = −k∇f (x1) is reduced-optimal, and u⋆ is outer inverse-optimal for the induced tracking problem. Moreover, 

Lr

 x1, α ⋆(x1) = k∥∇ f (x1)∥2 + μϕ (x1),AUTHOR et al. : TITLE 11 

so Theorem 4 gives 

˙V ⋆ = −k∥∇ f (x1)∥2 − μϕ (x1) − c∥e∥2,V ⋆ = ϕ(x1) + 12 ∥e∥2.

Using strong convexity, 

˙V ⋆ ≤ − (2 mk + μ)ϕ(x1) − c∥e∥2 ≤ − min {2mk + μ, 2c}V ⋆.

Hence V ⋆ decays exponentially, and therefore both ϕ(x1(t)) 

and the tracking error e(t) = x2(t) + k∇f (x1(t)) converge to zero exponentially fast. This provides a clean strong-convexity example in which the framework yields not only a two-layer optimality interpretation, but also an explicit exponential convergence estimate. 

Case 3: General Convex Smooth Objectives 

The previous two cases either fix the objective explicitly or add strong convexity. To test the genuine scope of Theorem 4, it is more informative to consider an example that works for an arbitrary objective satisfying Assumption 1. Introduce the auxiliary state 

˙z = −ρz + ∇f (x1), ρ > 0,

and consider the reduced system 

˙x1 = a, ˙z = −ρz + ∇f (x1),

where a is the virtual control. Fix constants kd > 0 and ki > 0,and choose 

V ⋆ 

> 1

(x1, z ) := ϕ(x1) + ki

2 ∥z∥2.

Then 

∇x1 V ⋆ 

> 1

= ∇f (x1), ∇z V ⋆ 

> 1

= kiz, 

so the gradient-matching condition of Theorem 4 holds with 

λ = 1 . Now define the reduced running cost 

Lr (x1, z, a ) := 12kd

∥a + kiz∥2 + kd

2 ∥∇ f (x1)∥2 + kiρ∥z∥2.

Then the reduced Hamilton–Jacobi–Bellman expression be-comes 

∇x1 V ⋆ 

> 1⊤

a + ∇z V ⋆ 

> 1⊤

 −ρz + ∇f (x1) + Lr (x1, z, a )= ∇f (x1)⊤(a + kiz) + 12kd

∥a + kiz∥2 + kd

2 ∥∇ f (x1)∥2.

Hence the unique minimizer is 

α⋆(x1, z ) = −kd∇f (x1) − kiz. 

Therefore the first layer of optimality is completely explicit even though the objective f itself has not been specified beyond convexity and smoothness. Now define 

e = x2 − α⋆(x1, z ) = x2 + kd∇f (x1) + kiz. 

Since 

˙α⋆ = −kd∇2f (x1)x2 − ki ˙z

= −kd∇2f (x1)x2 + kiρz − ki∇f (x1),

Theorem 4 yields the actual control 

u⋆ = ˙ α⋆ − ce − ∇ f (x1)= −kd∇2f (x1)x2 − cx 2

− (1 + ki + ck d)∇f (x1) + ki(ρ − c)z. 

The resulting optimization dynamics are 

˙x1 = x2,

˙x2 = −kd∇2f (x1)x2 − cx 2

− (1 + ki + ck d)∇f (x1) + ki(ρ − c)z, 

˙z = −ρz + ∇f (x1).

This is a genuinely two-layer construction for an arbitrary convex L-smooth objective. The virtual control 

α⋆ = −kd∇f (x1) − kiz

is optimal for the reduced problem generated by Lr , and then the actual control u⋆ is inverse optimal for the induced outer problem. Moreover, 

Lr

 x1, z, α ⋆(x1, z ) = kd∥∇ f (x1)∥2 + kiρ∥z∥2

≥ kd∥∇ f (x1)∥2,

so Theorem 4 gives the dissipation identity 

˙V ⋆ = −kd∥∇ f (x1)∥2 − kiρ∥z∥2 − c∥e∥2,V ⋆ = ϕ(x1) + ki

2 ∥z∥2 + 12 ∥e∥2.

In this sense, the example is both more complex and more general than the quadratic benchmark: it does not presuppose a particular objective function, yet it still yields an explicit algorithm from the two-layer optimality principle. 

VII. N UMERICAL EXPERIMENTS 

To validate the theoretical findings and evaluate the perfor-mance of the proposed backstepping-based optimization algo-rithms, we conduct numerical simulations for both strongly convex and general convex objective functions. For each scenario, we select five representative objective functions and solve the corresponding continuous-time gradient flows derived in Section V. To illustrate the convergence behavior, we plot the trajectory of the gradient norm ∥∇ f (x1(t)) ∥ over time for each test case. 

A. Experiments for Case 2: Strongly Convex Objectives 

For the strongly convex setting, the backstepping framework yields the exponentially stable second-order dynamics given by: 

˙x1 = x2, ˙x2 = −(k∇2f (x1) + cI )x2 − (1 + ck )∇f (x1).

To demonstrate the robustness of this algorithm, we select the following five L-smooth and m-strongly convex objective functions f : R2 → R: 

> •

F1 (Anisotropic Quadratic): f (x) = 12 x21 + 5 x22. A standard benchmark for linear convergence rates.  

> •

F2 (Shifted Quadratic): f (x) = ( x1 − 2) 2 + ( x2 + 2) 2.Tests convergence to a non-origin minimizer. 12 IEEE TRANSACTIONS AND JOURNALS TEMPLATE  

> •

F3 (Regularized Quartic): f (x) = 12 (x21 + x22) +  

> 110

(x41 + x42). Introduces polynomial nonlinearity while maintaining strong convexity.  

> •

F4 (Hyperbolic Cosine): f (x) = cosh( x1) + cosh( x2) +  

> 12

(x21 + x22). Features exponential growth in the gradient.  

> •

F5 (Regularized Pseudo-Huber): f (x) = p1 + x21 +p1 + x22 + 110 (x21 + x22). A smooth approximation of the L1 norm with an added L2 penalty to strictly satisfy strong convexity.     

> Fig. 4. Evolution of the gradient norm ∥∇ f(x1(t)) ∥over time for five representative strongly convex objective functions (Case 2). The logarithmic scale illustrates the exponential decay (linear convergence rate) achieved by the proposed strict-feedback backstepping dynamics.

In our simulations, we set the design parameters to k = 1 .0

and c = 1.0, and initialize the states at x1(0) = [5 , 5] ⊤

and x2(0) = [0 , 0] ⊤. The experimental results, displaying the evolution of the gradient norm, are presented in Figure 4. Consistent with theoretical expectations, all five functions exhibit a linear convergence rate (exponential decay in time), successfully driving the gradient norm to zero. 

B. Experiments for Case 3: General Convex Smooth Objectives 

For the general convex and smooth setting, we evaluate the augmented backstepping dynamics involving the auxiliary state z:

˙x1 =x2,

˙x2 = − kd∇2f (x1)x2 − cx 2 − (1 + ki + ck d)∇f (x1)+ ki(ρ − c)z, 

˙z = − ρz + ∇f (x1).

We deliberately select five functions that lack strong convexity or possess degenerate directions:  

> •

G1 (Pure Pseudo-Huber): f (x) = p1 + x21 + x22 − 1.The Hessian diminishes for large ∥x∥, lacking global strong convexity.  

> •

G2 (Softplus/Logistic Loss): f (x) = ln( ex1 + e−x1 ) + ln( ex2 + e−x2 ). Flat regions in the tails slow down standard gradient methods.  

> •

G3 (Pure Quartic): f (x) = 14 (x41 + x42). The Hessian vanishes exactly at the optimum x∗ = [0 , 0] ⊤. 

> •

G4 (Degenerate Quadratic): f (x) = 12 (x1 − x2)2. The minimizer is not a single point but a line x1 = x2,representing an ill-conditioned scenario.  

> •

G5 (Coupled Pseudo-Huber): f (x) =p1 + ( x1 + x2)2 − 1. Introduces coupling between the variables without strict convexity.     

> Fig. 5. Evolution of the gradient norm ∥∇ f(x1(t)) ∥over time for five general convex and smooth objective functions (Case 3). The results demonstrate the successful asymptotic convergence to stationarity of the augmented two-layer optimal backstepping framework, even in the absence of global strong convexity or in the presence of degenerate directions.

We set the control gains to kd = 1 .0, ki = 1 .0, c = 1 .0,and ρ = 2 .0. Initial conditions are chosen as x1(0) = [5 , 5] ⊤,

x2(0) = [0 , 0] ⊤, and z(0) = [0 , 0] ⊤. As shown in Figure 5, the proposed two-layer optimal backstepping framework successfully drives the gradient norm to zero across all ex-amples. Even in cases with vanishing Hessians (G3) or non-unique optima (G4), the algorithm remains stable and ensures asymptotic convergence to stationarity. 

VIII. C ONCLUSION 

This paper developed a backstepping-based framework for designing continuous-time unconstrained accelerated opti-mization algorithms. The starting point was to reformulate the algorithm-design problem as a controller-synthesis problem for the general augmented strict-feedback system (5), with the gradient output ∇f (x1), rather than the state x1 itself, as the regulated variable. From this viewpoint, backstepping leads naturally to a two-stage design procedure: one first selects a virtual control α together with a first-step storage function V1,and then constructs the actual input u so that the full system is driven toward the manifold selected by α and the output 

∇f (x1(t)) converges to zero. This synthesis viewpoint is broad enough to recover existing accelerated optimization flows. In particular, the constant-parameter Nesterov flow and the PIDAO flow arise in this paper as direct theorem-level realizations obtained from dif-ferent choices of virtual control and first-step dissipation AUTHOR et al. : TITLE 13 

structure. Beyond synthesis, the paper also established an optimal-backstepping interpretation of these constructions. It was shown that, once a virtual control α is fixed, the universal second-step law is inverse optimal only for the induced outer-tracking problem associated with the manifold determined by 

α. The paper then formulated a genuine optimal-backstepping principle by requiring the virtual control itself to solve a prescribed reduced Hamilton–Jacobi–Bellman problem. Under this condition, the final design acquires a true two-layer optimality structure: the virtual control is optimal for the reduced problem, while the actual control is inverse optimal for the induced outer problem. The framework also suggests several natural extensions. First, one may replace the Euclidean energy structure used here by a Bregman-type geometry, thereby connecting backstepping design with non-Euclidean accelerated flows. Second, the same viewpoint may be extended from unconstrained optimization to constrained optimization by treating optimization dynamics as backstepping-based feedback regulation laws compatible with feasibility constraints, projected dynamics, or primal– dual augmentations. Third, the framework opens a path toward online and time-varying optimization, where the objective and its minimizer evolve with time, so that the relevant goal is no longer convergence to a static stationary point, but feedback tracking of a moving optimum. These directions indicate that backstepping may provide not only a synthesis tool for unconstrained accelerated gradient flows, but also a broader control-theoretic design principle for structured optimization dynamics. 

REFERENCES 

[1] Jiacong Xu, Zixiang Xiong, and Shankar P Bhattacharyya. PIDNet: A real-time semantic segmentation network inspired by PID controllers. In Proceedings of the IEEE/CVF conference on computer vision and pattern recognition , pages 19529–19539, 2023. [2] Lvmin Zhang, Anyi Rao, and Maneesh Agrawala. Adding conditional control to text-to-image diffusion models. In Proceedings of the IEEE/CVF international conference on computer vision , pages 3836– 3847, 2023. [3] Albert Gu, Karan Goel, and Christopher Re. Efficiently modeling long sequences with structured state spaces. In International Conference on Learning Representations , 2022. [4] Albert Gu and Tri Dao. Mamba: Linear-time sequence modeling with selective state spaces. In First Conference on Language Modeling , 2024. [5] Song Chen, Jiaxu Liu, Pengkai Wang, Chao Xu, Shengze Cai, and Jian Chu. Accelerated optimization in deep learning with a proportional-integral-derivative controller. Nature Communications , 15(1):10263, 2024. [6] Adrian Hauswirth, Zhiyu He, Saverio Bolognani, Gabriela Hug, and Florian D¨ orfler. Optimization algorithms as robust feedback controllers. 

Annual Reviews in Control , 57:100941, 2024. [7] Boris T Polyak. Some methods of speeding up the convergence of iteration methods. Ussr Computational Mathematics and Mathematical Physics , 4(5):1–17, 1964. [8] Y Nesterov. A method of solving a convex programming problem with convergence rate O(1 /k 2). Proceedings of the USSR Academy of Sciences , 269:3. [9] Laurent Lessard, Benjamin Recht, and Andrew Packard. Analysis and design of optimization algorithms via integral quadratic constraints. 

SIAM Journal on Optimization , 26(1):57–95, 2016. [10] Mahyar Fazlyab, Alejandro Ribeiro, Manfred Morari, and Victor M Preciado. Analysis of optimization algorithms via integral quadratic constraints: Nonstrongly convex problems. SIAM Journal on Optimiza-tion , 28(3):2654–2689, 2018. [11] Bin Hu and Laurent Lessard. Dissipativity theory for Nesterov’s accelerated method. In International Conference on Machine Learning ,pages 1549–1557. PMLR, 2017. [12] Laurent Lessard. The analysis of optimization algorithms: A dissipativity approach. IEEE Control Systems Magazine , 42(3):58–72, 2022. [13] Ashia C Wilson, Benjamin Recht, and Michael I Jordan. A Lyapunov analysis of accelerated methods in optimization. Journal of Machine Learning Research , 22(113):1–34, 2021. [14] Jes´ us Mar´ ıa Sanz Serna and Konstantinos C Zygalakis. The connections between Lyapunov functions for some optimization algorithms and dif-ferential equations. SIAM Journal on Numerical Analysis , 59(3):1542– 1565, 2021. [15] Haoqian Wang, Yi Luo, Wangpeng An, Qingyun Sun, Jun Xu, and Lei Zhang. PID controller-based stochastic optimization acceleration for deep neural networks. IEEE Transactions on Neural Networks and Learning Systems , 31(12):5079–5091, 2020. [16] Weijie Su, Stephen Boyd, and Emmanuel J Candes. A differential equation for modeling Nesterov’s accelerated gradient method: Theory and insights. Journal of Machine Learning Research , 17(153):1–43, 2016. [17] Andre Wibisono, Ashia C Wilson, and Michael I Jordan. A variational perspective on accelerated methods in optimization. Proceedings of the National Academy of Sciences , 113(47):E7351–E7358, 2016. [18] Hao Luo and Long Chen. From differential equation solvers to accelerated first-order methods for convex optimization. Mathematical Programming , 195(1):735–781, 2022. [19] Kunal Garg and Dimitra Panagou. Fixed-time stable gradient flows: Applications to continuous-time optimization. IEEE Transactions on Automatic Control , 66(5):2002–2015, 2020. [20] Bin Shi, Simon S Du, Michael I Jordan, and Weijie J Su. Understanding the acceleration phenomenon via high-resolution differential equations. 

Mathematical Programming , 195(1):79–148, 2022. [21] Michael Muehlebach and Michael Jordan. A dynamical systems perspec-tive on Nesterov acceleration. In International Conference on Machine Learning , pages 4656–4662. PMLR, 2019. [22] Chris J Maddison, Daniel Paul, Lester Mackey, and Arnaud Doucet. Hamiltonian descent methods. arXiv preprint arXiv:1809.05042 , 2018. [23] Michael Muehlebach and Michael I Jordan. Optimization with momen-tum: Dynamical, control-theoretic, and symplectic perspectives. Journal of Machine Learning Research , 22(73):1–50, 2021. [24] Arman Sharifi Kolarijani, Peyman Mohajerin Esfahani, and Tam´ as Keviczky. Continuous-time accelerated methods via a hybrid control lens. IEEE Transactions on Automatic Control , 65(8):3425–3440, 2019. [25] Jiaxu Liu, Song Chen, Pengkai Wang, Shengze Cai, Chao Xu, and Jian Chu. Output feedback-based continuous-time distributed PID optimization algorithms. IEEE Transactions on Network Science and Engineering , 12(2):955–969, 2024. [26] Jiaxu Liu, Song Chen, Shengze Cai, and Chao Xu. A proposal on centralised and distributed optimisation via proportional–integral– derivative controllers (PID) control perspective. IET Cyber-Systems and Robotics , 5(4):e12100, 2023. [27] Jiaxu Liu, Pengkai Wang, Song Chen, Shengze Cai, and Chao Xu. Distributed optimization algorithm design and analysis on cooperation-competition network based on PID control. Journal of the Franklin Institute , 363(5):108483, 2026. [28] Andrej Jokic, Mircea Lazar, and Paul PJ van den Bosch. On constrained steady-state regulation: Dynamic KKT controllers. IEEE Transactions on Automatic Control , 54(9):2250–2254, 2009. [29] Ahmed Allibhoy and Jorge Cort´ es. Control-barrier-function-based de-sign of gradient flows for constrained nonlinear programming. IEEE Transactions on Automatic Control , 69(6):3499–3514, 2023. [30] Runyu Zhang, Arvind Raghunathan, Jeff Shamma, and Na Li. Con-strained optimization from a control perspective via feedback lineariza-tion. arXiv preprint arXiv:2503.12665 , 2025. [31] Vito Cerone, Sophie M Fosson, Simone Pirrera, and Diego Regruto. A new framework for constrained optimization via feedback control of Lagrange multipliers. IEEE Transactions on Automatic Control , 2025. [32] Pol Mestres, Ahmed Allibhoy, and Jorge Cort´ es. Regularity proper-ties of optimization-based controllers. European Journal of Control ,81:101098, 2025. [33] Hedy Attouch, Zaki Chbani, Juan Peypouquet, and Patrick Redont. Fast convergence of inertial dynamics and algorithms with asymptotic vanishing viscosity. Mathematical Programming , 168(1):123–175, 2018. [34] Miroslav Krstic, Petar V Kokotovic, and Ioannis Kanellakopoulos. 

Nonlinear and Adaptive Control Design . John Wiley & Sons, Inc., 1995. 14 IEEE TRANSACTIONS AND JOURNALS TEMPLATE 

[35] Sundarapandian Vaidyanathan and Ahmad Taher Azar. Backstepping Control of Nonlinear Dynamical Systems . Academic Press, 2020. [36] Ji Wang and Miroslav Krstic. Event-triggered output-feedback backstep-ping control of sandwich hyperbolic PDE systems. IEEE Transactions on Automatic Control , 67(1):220–235, 2021. [37] Randy A Freeman and Petar V Kokotovic. Inverse optimality in robust stabilization. SIAM Journal on Control and Optimization , 34(4):1365– 1391, 1996. [38] Wuquan Li and Miroslav Krstic. Stochastic nonlinear prescribed-time stabilization and inverse optimality. IEEE Transactions on Automatic Control , 67(3):1179–1193, 2021. [39] Yurii Nesterov. Introductory Lectures on Convex Optimization: A Basic Course , volume 87. Springer Science & Business Media, 2013. [40] Hassan K Khalil and Jessy W Grizzle. Nonlinear Systems , volume 3. Prentice hall Upper Saddle River, NJ, 2002. [41] Miroslav Krstic and Zhong-Hua Li. Inverse optimal design of input-to-state stabilizing nonlinear controllers. IEEE Transactions on Automatic Control , 43(3):336–350, 1998. 

Song Chen received the Ph.D. degree in oper-ational research and cybernetics from Zhejiang University, Hangzhou, China, in 2025. He is currently a Research Fellow with the Department of Mathematics, National University of Singapore (NUS), Singapore. His research interests lie at the intersection of control theory and artificial intelligence, with a particular focus on control-oriented learning methods and em-bodied AI. His broader expertise includes con-vex optimization, nonlinear control, and machine learning theory with applications in robotics. 

Jiaxu Liu received the bachelor’s degree in mathematics from Renmin University of China, Beijing, China, in 2021. He is currently work-ing toward the Ph.D. degree in operational re-search and cybernetics with Zhejiang University, Hangzhou, China. His research interests include distributed op-timization, convex optimization, robust control, machine learning theory, and their applications in robotics. 

Chao Xu (Senior Member, IEEE), received the Ph.D. degree in mechanical engineering from Lehigh University, Bethlehem, PA, USA, in 2010. He is currently Professor of Controls and Au-tonomous Systems with the College of Con-trol Science & Engineering, Zhejiang University (ZJU). He serves the inaugural Dean of ZJU Huzhou Institute, as well as plays the role of the Managing Editor for two international journals, e.g., IET Cyber-Systems and Robotics (IET-CSR), and Journal of Industrial and Manage-ment Optimization (JIMO). His research expertise is Cybernetic Physics and Autonomous Mobility in general, with a focus on, modeling and control of aerial robotics with applications, machine learning for dynamic systems and control, visual sensing and machine learning for complex fluids.