---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化JEPA世界模型从真实户外机器人数据中学习更具迁移性的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:world-model"]
score: 9.0
evidence: 深度正则化的JEPA世界模型从真实户外机器人数据学习鲁棒潜在动力学
tldr: "真实户外机器人视频存在视觉复杂性和动态不可预测性，现有JEPA世界模型难以学习鲁棒潜在动态。本文在训练时引入深度作为几何先验，联合各向同性诱导演示正则化器(SIGReg)，并增加训练时过参数化(18M参数)。在农业机器人数据集上，冻结表示用于视觉里程计误差降低33%，异常检测分数分离显著提高，多步rollout保真度在域偏移下增强且增益随步长增大，甚至对光照阴影等非几何物理理解也有改善。结果证明轻量级几何先验在不增加推理开销下显著提升紧凑世界模型在真实户外数据上的表示可迁移性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717, \"label\": \"Table\"}]"
motivation: 真实户外环境视觉复杂且动态多变，现有JEPA世界模型难以学习鲁棒动力学，需要有效几何先验引导高熵表示组织。
method: 在JEPA训练中引入深度监督和SIGReg各向同性正则化，并采用训练时过参数化(18M参数)以提升模型容量。
result: "冻结表示视觉里程计误差降低33%；异常检测分数分离提升；多步rollout保真度在域偏移下改善且增益随步长增长；对光照阴影等非几何物理理解也有提升。"
conclusion: 轻量级训练时深度几何先验显著增强紧凑JEPA世界模型在真实户外数据上的表示可迁移性，且不增加推理开销。
---

## 摘要
世界模型，尤其是基于JEPA架构的模型，已被证明能够学习各种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，尤其是在不可预测的户外环境中。我们引入了深度作为几何先验，在训练过程中直接从机器人视频数据中学习更鲁棒的潜在动力学，并处理视觉复杂性。这结合了深度监督与一个各向同性诱导的潜在正则化器（SIGReg），最大化任务无关的潜在多样性，同时限制该多样性的组织方式，其组合目标指向与场景几何结构一致的最高熵表征。为了在不增加推理时间的情况下满足这种更高的复杂性，我们还添加了仅在训练中使用的过参数化。在一个真实农业机器人视频上训练一个1800万参数的模型，我们通过冻结表征的视觉里程计探针、基于预测器的意外检测以及多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探针误差降低了33%，在域内和域外的TartanGround基准上显著提高了意外分数分离度，并在域偏移下改善了多步展开保真度，其增益随着展开步长增加而增大。值得注意的是，我们在与3D几何不直接相关的物理理解（如光照和阴影）的意外分数分离度上也看到了改进。这些结果表明，轻量级的训练时几何先验使紧凑的JEPA世界模型在具有强底层表征的真实户外数据上更加有用和更具迁移性，且不增加推理开销。我们的工作表明，深度作为一种物理基础先验，可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：真实户外机器人视频（如农业机器人行驶数据）包含复杂光照、阴影、运动模糊、植被遮挡等视觉挑战，现有JEPA世界模型主要针对简单模拟或室内场景，难以在真实户外数据中学习鲁棒的潜在动力学，且容易崩溃（collapse）或建模与结构无关的噪声变化。
- **研究动机**：期望通过轻量级训练时几何先验（深度信息）引导JEPA表征关注物理3D结构，同时保持推断时仅需RGB输入，从而提升表征质量、预测能力和跨域迁移性，且不增加推理开销。
- **整体含义**：证明了在紧凑JEPA世界模型（约18M参数）中，训练时加入深度对齐损失可以显著增强表征对几何结构的编码，进而改善下游任务（视觉里程计、异常检测、多步预测）表现，且对非几何物理概念（光照变化）也有提升，为真实世界机器人部署提供实用方案。

### 2. 论文提出的方法论：核心思想、关键技术细节
#### 核心思想
- 在LeWorldModel (LeWM) 架构基础上，增加一个**训练时深度对齐损失**（\( L_{\text{depth}} \)），将RGB嵌入与对应深度相机嵌入（带停止梯度）进行余弦相似度最大化，使表征偏向几何结构。
- 同时保留SIGReg正则化（鼓励各向同性高斯分布，最大化任务无关多样性），两者协同：SIGReg防止坍塌，深度项约束多样性组织方向，共同追求“与场景几何一致的最高熵表征”。
- 为解决因深度约束带来的学习压力，引入**训练时过参数化**：在预测器的MLP上、下投影中加入rank-16的低秩RepLinear分支，训练后通过结构重参数化合并，不增加推理成本。

#### 关键技术细节
- **损失函数**：\( \mathcal{L} = \mathcal{L}_{\text{pred}} + 0.09\,\mathcal{L}_{\text{sigreg}} + 0.1\,\mathcal{L}_{\text{depth}} \)，其中 \( \mathcal{L}_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})]) \)。
- **架构**：ViT-Tiny编码器，嵌入维度192，历史帧数3，预测步长1，6层预测器；训练时过参数化通过线性分支增加容量，推理时架构与基线LeWM相同。
- **训练策略**：仅RGB输入用于预测，深度仅在训练时用于对齐，深度分支使用停止梯度，避免深度编码器梯度更新影响RGB分支。
- **深度预处理**：深度图截断至1米，作为3通道灰度输入（复制）。

### 3. 实验设计
- **训练数据**：Aigen Element真实农业机器人数据集，约31K帧（85个episode），来自前视导航立体相机，包含多样环境。
- **验证/测试**：
  - 域内：3,587帧（9个episode）Aigen验证集。
  - 域外：TartanGround基准（地面机器人仿真数据集），15,608帧（6个episode），覆盖多种环境。
- **评估任务**：
  1. 冻结表征的**视觉里程计线性探针**（VO probe）：回归自运动。
  2. **预测器基于意外检测**：构造四种扰动（传送、时间反转、亮度跳变、颜色交换），评估模型对违反物理预期的敏感度（surprise分离度）。
  3. **多步潜在展开保真度**：给定真实动作序列，自回归展开预测，比较预测潜在与真实帧编码之间的余弦相似度（目标相似度、轨迹相似度），步长h=10和20。
- **对比方法**：
  - 基线：Vanilla LeWM（无深度正则化）。
  - 参考：冻结的DINOv2编码器（作为单帧表征基准，仅在VO探针和rollout中提供参考）。
  - 本文：Depth-Regularized LeWM。

### 4. 资源与算力
- **模型规模**：约1800万参数（18M）。
- **训练配置**：100个epoch，优化器和调度器与基线一致。论文**未明确说明**使用的GPU型号、数量、训练时长，仅提及运行可重复且报告代表性结果。因此资源消耗细节未知。

### 5. 实验数量与充分性
- **实验组数量**：
  - 主要对比：Vanilla LeWM vs. Depth-Regularized（ours），在3个评估任务上进行。
  - 额外分析：跨域潜在几何分析（PCA主成分重叠、主角度、2D可视化），进一步验证表征对齐。
  - 扰动类型：共4种扰动（传送、反转、亮度、颜色），分别报告分离度。
- **充分性与公平性**：
  - 实验覆盖了表征质量、预测行为、跨域迁移等多个维度，设计合理。
  - 使用相同训练配置（100 epochs，相同优化器），重复运行得到一致结果，报告代表性运行，避免偶然性。
  - 但**缺乏消融实验**：未单独评估深度项和过参数化的各自贡献，未测试去掉任一组件的影响；也未对比其他几何先验（如法线图）或不同深度损失权重。
  - VO探针和rollout中对比了DINOv2，但DINOv2并非基线世界模型，仅作为外部参考，对比公平性一般。
  - 域外基准TartanGround为仿真数据，与真实域差异大，适合评估迁移性，但只有一个OOD基准，多样性有限。

### 6. 论文的主要结论与发现
1. **表征质量提升**：视觉里程计探针MSE从0.0022降至0.0015（降低33%），优于DINOv2（0.0029）。
2. **意外检测分离度提升**：域内分离度从0.067增至0.120（准确率饱和均为78.7%）；域外TartanGround分离度从0.035增至0.099（近3倍提升），准确率从87.5%增至89.2%。
3. **非几何物理理解改善**：亮度跳变和颜色交换扰动下，分离度也在域内和域外显著提升（如亮度跳变域内从0.114增至0.247），表明深度先验帮助建立了外观与几何一致的期望。
4. **域外多步rollout保真度提升**：域外TartanGround上，h=10目标相似度从0.716→0.745，h=20从0.458→0.537；增益随步长增加，表明几何形状动力学更鲁棒；域内略下降（可能因牺牲一定过拟合）。
5. **跨域潜在几何对齐更好**：PCA分析显示深度正则化模型的中频主成分重叠更高、分离程度更低，说明隐空间更跨域对齐。

### 7. 优点
- **实用性**：深度仅在训练时使用，推理零额外成本；模型紧凑（18M），适合边缘部署。
- **方法简洁有效**：仅增加一个余弦对齐损失项和一个可合并的过参数化，改动少，收益显著。
- **评估全面**：涵盖表征、预测期望、多步动力学三个层次，并包括域内外对比。
- **解释性强**：通过PCA几何分析直观展示隐空间结构，帮助理解迁移性提升原因。

### 8. 不足与局限
- **缺乏消融实验**：未分离深度项和过参数化的贡献，未探究不同权重、不同架构（如无过参数化）的影响。
- **数据规模单一**：仅约31K帧来自单一农业机器人平台，且仅一个域外基准，泛化性有待验证。
- **离线评估**：rollout为潜在空间指标，未做闭环机器人规划或控制实验。
- **未与其他方法全面对比**：未与更大规模JEPA模型（如V-JEPA）或基于重建的世界模型（如Dreamer）比较。
- **规划未验证**：文中指出采样子规划因潜在空间动态范围不同而未得出结论，故实际规划能力待考。
- **资源细节缺失**：未提供训练时间、GPU型号等，影响可复现性评估。

（完）
