---
title: Trajectory-Regularized Stochastic Optimal Control via KL Divergence
title_zh: 通过KL散度的轨迹正则化随机最优控制
authors: "Mintae Kim, Koushil Sreenath"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22201v1"
tags: ["query:control"]
score: 9.0
evidence: 基于KL散度的轨迹正则化随机最优控制
tldr: 传统随机最优控制未考虑控制轨迹与参考轨迹的偏差。本文提出轨迹正则化随机最优控制，通过KL散度惩罚轨迹分布差异，利用Girsanov定理将KL转化为二次漂移失配代价，保留动态规划结构。线性二次型下得到闭式解，正则化参数可权衡性能最优与参考跟踪。该方法为结合离线学习参考动力学提供了灵活框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22201v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 896, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22201v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22201v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22201v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 848, \"height\": 294, \"label\": \"Figure\"}]"
motivation: 现有随机最优控制缺乏对轨迹分布的约束，导致偏离参考行为，需引入正则化以平衡性能和参考跟随。
method: 在标准随机最优控制中增加控制轨迹与参考轨迹分布的KL散度，通过Girsanov定理将其等效为二次漂移惩罚，修改运行成本并求解HJB方程。
result: 线性二次型问题获得闭式最优策略；实验显示正则化参数调节性能与参考保持的折中，且可适配离线学习的参考动力学。
conclusion: 轨迹正则化随机最优控制通过KL散度有效约束轨迹分布，拓展了经典方法，适用于参考跟踪与控制性能权衡的场景。
---

## 摘要
我们引入了轨迹正则化随机最优控制（TRSOC），它通过受控轨迹分布与参考轨迹分布之间的Kullback-Leibler（KL）散度增强了标准随机最优控制（SOC）。利用Girsanov定理，轨迹KL散度简化为二次漂移失配惩罚，从而产生一个修正的运行成本，该成本保留了动态规划（DP）结构。我们推导了相应的Hamilton-Jacobi-Bellman（HJB）方程，并刻画了最优策略。在线性二次型（LQ）设置中，该公式具有带增广控制成本的闭式解。实验表明，正则化参数在性能驱动和参考保持行为之间引入了一个权衡，包括从离线数据学习参考动力学的情况。

## Abstract
We introduce trajectory-regularized stochastic optimal control (TRSOC), which augments standard stochastic optimal control (SOC) with a Kullback--Leibler (KL) divergence between controlled and reference trajectory distributions. Using Girsanov's theorem, the trajectory KL reduces to a quadratic drift mismatch penalty, yielding a modified running cost that preserves the dynamic programming (DP) structure. We derive the corresponding Hamilton--Jacobi--Bellman (HJB) equation and characterize the optimal policy. In the linear-quadratic (LQ) setting, the formulation admits a closed-form solution with an augmented control cost. Experiments show that the regularization parameter induces a trade-off between performance-driven and reference-preserving behavior, including cases with reference dynamics learned from offline data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：标准随机最优控制（SOC）仅优化任务性能（如跟踪误差和能量），但不限制控制策略导致的轨迹分布与某种参考轨迹分布的偏离。在许多实际场景中，参考行为（如被动动力学、基线控制器的闭环动力学或从离线数据中学习的轨迹分布）包含有用结构。过大的偏离可能导致策略脆弱，无法利用已有结构。
- **整体含义**：本文提出轨迹正则化随机最优控制（TRSOC），通过在SOC目标中添加控制轨迹与参考轨迹分布之间的KL散度，在任务性能和参考保持之间引入可调节的权衡，从而将先验行为（包括名义控制器和数据驱动基线）系统性地融入SOC框架。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在标准SOC的控制‑仿射扩散模型下，定义控制轨迹分布 \( P^u \) 和参考轨迹分布 \( P^0 \)，将目标函数增广为：
  \[
  J(u) = \mathbb{E}^{P^u}\left[\int_0^T \ell(X_t, u_t) dt + g(X_T)\right] + \lambda \,\mathrm{KL}(P^u\|P^0)
  \]
- **关键技术细节**：
  1. **Girsanov定理简化KL**：在扩散矩阵可逆且满足Novikov条件时，轨迹KL散度等价于漂移失配的二次积分：
     \[
     \mathrm{KL}(P^u\|P^0) = \frac12 \,\mathbb{E}^{P^u}\left[\int_0^T \|\sigma(X_t)^{-1}(f(X_t,u_t)-f_0(X_t))\|^2 dt\right]
     \]
  2. **增广运行成本**：定义 \( L(x,u) = \ell(x,u) + \frac{\lambda}{2}\|\sigma(x)^{-1}(f(x,u)-f_0(x))\|^2 \)，保留原始HJB方程形式。
  3. **HJB方程与最优策略**：在控制‑仿射、二次控制成本下，最优反馈律为：
     \[
     u^*(x) = -\tilde{R}(x)^{-1} B(x)^\top \nabla V(x), \quad \tilde{R} = R + \lambda B^\top (\sigma\sigma^\top)^{-1}B
     \]
  4. **线性二次型（LQ）闭式解**：当系统为线性、成本为二次时，TRSOC退化为折扣LQR，仅控制权重矩阵被增广为 \(\tilde{R}\)，满足修正的Riccati方程。
  5. **稳定性分析**：通过Foster‑Lyapunov条件证明闭环扩散存在指数矩有界性和不变测度；轨迹正则化通过增大有效控制权重使反馈更保守，有助于改善耗散性。

### 3. 实验设计：数据集/场景、基准、对比方法

- **场景**：二维随机双积分器（状态4维：位置、速度；控制2维：加速度）。跟踪目标为“8”字形参考轨迹。
- **数据集**：无公开数据集；参考行为分两种：
  - **已知参考控制**：一个完整的PD+前馈跟踪控制器（已知解析形式）。
  - **数据驱动参考**：从已知参考控制器产生的离线轨迹中学习参考动力学（加速度部分用小型神经网络拟合）。
- **基准与对比**：无外部基准方法。论文主要对比不同正则化参数 \(\lambda\) 下的TRSOC控制器，包括 \(\lambda=0\)（标准SOC）、中等\(\lambda\)、大\(\lambda\)（名义控制器）。在数据驱动场景下，同样改变 \(\lambda\) 观察性能‑偏离权衡。
- **对比方法**：未与其他方法（如KL控制、MPPI、风险敏感控制）进行直接对比，仅聚焦自身trade-off展示。

### 4. 资源与算力

- 论文未提及任何GPU型号、数量或训练时长。实验均在CPU上通过离散化动态规划和蒙特卡洛rollout完成，算力需求低。

### 5. 实验数量与充分性

- **实验数量**：主要包含两组实验：
  1. 已知参考场景：展示性能‑偏离的Pareto前沿（图1a）及\(\lambda\)对成本和偏离指标的影响（图1b）。
  2. 数据驱动场景：同样展示权衡（图2a、2b）。
  3. 附加轨迹可视化（图3）显示不同\(\lambda\)下的轨迹样本。
  4. 数据驱动中验证学习漂移的近似质量（图4）。
- **充分性**：实验覆盖了核心权衡的验证，但缺乏与现有方法的定量比较，也未提供误差带或统计显著性分析。对于高维或非线性更强的系统未进行测试。整体实验设计合理但不够全面。

### 6. 论文的主要结论与发现

- **理论结论**：轨迹级KL正则化通过Girsanov定理转换为局部二次惩罚，保持马尔可夫结构和DP/HJB框架。
- **结构性质**：TRSOC提供从纯性能驱动到纯参考保持的连续插值，且正则化参数\(\lambda\)单调增加最优代价。
- **LQ特例**：TRSOC等价于增广LQR，闭式解显式体现噪声感知的控制惩罚。
- **实验发现**：已知参考和数据驱动场景下，\(\lambda\)能有效调节轨迹几何与任务性能的权衡；即使在参考动力学存在近似误差时，TRSOC仍能提供有意义的正则化效果。

### 7. 优点

- **理论优雅性**：利用Girsanov定理将复杂的轨迹分布正则化简化成可微的二次漂移惩罚，无缝融入传统SOC框架，保持DP和HJB结构，无需增广状态空间。
- **通用性**：适用于一般非线性控制‑仿射系统，参考动力学可来自物理模型、基线控制器或离线数据。
- **显式最优策略**：在控制仿射+二次成本条件下给出解析反馈律，LQ下得到闭式Riccati解，便于分析和实现。
- **稳定性分析**：提供Foster‑Lyapunov条件和矩有界性证明，说明正则化可增强闭环稳定性。
- **实验设计清晰**：通过二维跟踪问题直观展示参数\(\lambda\)的调节作用，并验证了数据驱动场景的有效性。

### 8. 不足与局限

- **对扩散矩阵的限制**：要求扩散矩阵可逆且Novikov条件满足，限制了该方法在控制相关扩散或退化噪声情况下的应用。
- **缺乏与现有方法对比**：未与KL控制、MPPI、风险敏感控制等方法进行实验比较，难以评估TRSOC的实际性能优势。
- **实验规模有限**：仅在一个低维系统上验证，未测试高维、非线性更强或复杂约束场景。
- **数据驱动部分近似误差影响**：仅验证了参考动力学近似质量，未深入分析近似误差如何影响最终控制性能的敏感性。
- **未提供不确定性量化**：所有成本通过蒙特卡洛估计给出均值，无置信区间或统计检验，结果直观但缺乏严谨性。

（完）
