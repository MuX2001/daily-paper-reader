---
title: Conformal Constraint Tightening for Chance-Constrained Motion Planning with Unknown Dynamics
title_zh: 未知动力学下机会约束运动规划的共形约束收紧
authors: "Shubham Natraj, Bruno Sinopoli, Yiannis Kantaros"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22409v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 规划器无关的约束收紧方法，为运动规划提供概率保证
tldr: 针对未知动力学系统的运动规划问题，现有方法依赖名义模型，但真实模型偏差导致保证失效。本文提出共形约束收紧方法，利用共形预测对名义到真实轨迹偏差建立概率界，并据此收紧规划约束。理论上证明，在名义模型上求解收紧问题足以保证在真实系统上以指定概率完成任务。实验表明该方法显著提升任务完成率，且与规划器无关。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22409v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22409v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 904, \"height\": 1109, \"label\": \"Figure\"}]"
motivation: 现有运动规划方法依赖准确动力学模型，实际中模型未知，导致任务完成保证失效。
method: 提出规划器无关的约束收紧程序，利用共形预测获取轨迹偏差概率界，并用于收紧规划约束。
result: 理论证明收紧后问题求解等价于原问题在真实系统上以指定概率成功，实验验证任务完成率提升。
conclusion: 共形约束收紧为未知动力学系统运动规划提供概率保证，增强实用性。
---

## 摘要
运动规划算法计算控制序列，驱动自主机器人到达目标区域同时避免不安全状态。现有方法，从基于采样的规划到深度强化学习，通常仅针对标称模型或模拟器提供任务完成保证，当真实动力学未知或难以精确建模时，这些保证可能失效。本文针对具有未知动力学和可用近似标称模型的系统解决了这一局限性，提出了一种与规划器无关的约束收紧过程，为现有规划器在真实系统上配备概率性任务完成保证。我们利用共形预测在规划问题分布上提供标称轨迹与真实轨迹偏差的概率界限。我们使用该界限收紧规划约束，并证明在标称模型下求解收紧后的问题是在真实系统上以规定概率求解原始问题的充分条件。我们通过实验验证了理论保证，并展示了相对于标称模型规划显著改进的任务完成率。

## Abstract
Motion planning algorithms compute control sequences that drive autonomous robots to goal regions while avoiding unsafe states. Existing methods, from sampling-based planning to deep reinforcement learning, typically provide task-completion guarantees only with respect to a nominal model or simulator, which may be invalidated when the true dynamics are unknown or difficult to model accurately. This letter addresses this limitation for systems with unknown dynamics and an available approximate nominal model, contributing a planner-agnostic constraint-tightening procedure that equips existing planners with a probabilistic task-completion guarantee on the true system. We leverage conformal prediction to provide a probabilistic bound on the nominal-to-true trajectory deviation over a distribution of planning problems. We tighten the planning constraints using that bound, and show that solving the tightened problem under the nominal model is a sufficient condition for solving the original problem on the true system with a prescribed probability. We validate the theoretical guarantees empirically and demonstrate substantially improved task completion relative to nominal-model planning.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：运动规划算法通常依赖精确的机器人动力学模型（标称模型）。然而真实系统动力学未知或难以精确建模，导致基于标称模型提供的安全性和任务完成保证在实际执行中可能失效。
- **背景**：现有方法（如采样规划、MPC、深度强化学习）大多假设已知随机模型或结构化不确定性，当模型失配时，其保证不再成立。本文针对“真实动力学未知，仅有近似标称模型”的场景，旨在为任意规划器提供概率性任务完成保证（以至少1-α的概率到达目标且避开障碍）。
- **整体含义**：开发一种与规划器无关的框架，通过数据驱动的方式量化模型失配，并将原始问题转化为在标称模型上求解的收紧约束问题，从而在真实系统上以指定概率完成任务。

## 2. 方法论
### 核心思想
- 利用共形预测（Conformal Prediction, CP）对标称轨迹与真实轨迹之间的偏差进行分布自由的概率上界估计。
- 基于该上界收紧规划问题的约束（膨胀障碍、收缩目标区域），使得在标称模型上求解收紧后的问题，等价于在真实系统上以至少1-α的概率满足原始约束。

### 关键技术细节
1. **非符合性分数（NCS）**：对每个规划问题M，定义最坏情况的标称-真实轨迹偏差：  
   \( R(M) = \sup_{u_{0:T-1} \in \mathcal{U}(M)} \max_{t} \|x_t - \hat{x}_t\|_2 \)
   - 其中 \(\mathcal{U}(M)\) 是所有标称可行的控制序列集合。
2. **校准数据集**：从未知分布 \(D_M\) 中i.i.d.采样 \(N_{cal}=100\) 个规划问题，对每个问题执行若干标称可行控制序列，收集真实轨迹，计算分数 \(R_i\)。
3. **共形阈值 \(\hat{q}_\alpha\)**：计算 \(\{R_1,\dots,R_{N_{cal}},\infty\}\) 的 \((1-\alpha)\) 分位数。保证：  
   \( P( R(M_{\text{test}}) \leq \hat{q}_\alpha ) \geq 1-\alpha \)。
4. **约束收紧**：  
   - \( \mathcal{X}_{\text{free}}^{\hat{q}_\alpha} = \{x \in \mathcal{X} : B(x,\hat{q}_\alpha) \subseteq \mathcal{X}_{\text{free}}\} \)  
   - \( \mathcal{X}_{\text{goal}}^{\hat{q}_\alpha} = \{x \in \mathcal{X} : B(x,\hat{q}_\alpha) \subseteq \mathcal{X}_{\text{goal}}\} \)  
   即通过半径为 \(\hat{q}_\alpha\) 的球对自由空间和目标区域进行腐蚀。
5. **规划器保证**（命题3.4）：任何在标称模型上求解收紧问题 \(M_\alpha\) 的控制序列，以至少1-α的概率在真实系统上满足原始任务要求。

### 流程总结
- **离线**：收集校准数据 → 计算共形阈值 \(\hat{q}_\alpha\)。
- **在线**：给定新规划问题 → 构造收紧问题 \(M_\alpha\) → 用任意规划器（如RRT）在标称模型上求解 → 执行所得控制序列。

## 3. 实验设计
### 场景与数据集
- **Dubins车**（确定性动力学）：标称模型为简单运动学，真实模型存在平移速度减速（参数 \(\beta_v \in \{0.05,0.10,0.15,0.20\}\) 控制失配程度）。
- **平面四旋翼**（随机性动力学，6维状态）：标称模型为单步神经网络预测器（在无噪声版本上训练），真实模型包含高斯过程噪声（截断至[-3,3]）。

### 规划问题分布
- 每个问题定义在25×25环境中，包含5个随机放置的圆形障碍（半径[1,3]），初始位置和圆形目标区域（半径5）均从自由空间均匀采样，目标与起点距离至少15单位。
- 共100个独立测试规划问题。

### 对比方法
- **本文方法**：使用kinodynamic RRT在收紧问题\(M_\alpha\)上规划。
- **基线**：同一RRT直接在原问题\(M\)上对标称模型规划（标称RRT），仅在本文方法能返回解的子集上评估，以确保公平比较。

### 评估指标
- 经验覆盖率（empirical coverage）：真实轨迹与标称轨迹最大偏差≤\(\hat{q}_\alpha\)的返回计划比例。
- 规划器成功率（planner success rate）：在给定迭代预算内找到解的问题比例。
- 任务完成率（task-completion rate）：返回计划中真实轨迹安全到达目标的比例。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等计算资源信息。仅提到使用CPU运行RRT规划（迭代次数：Dubins 2000次，四旋翼75000次）。校准数据收集需要物理访问真实系统（仿真中实现），但未报告具体时间开销。

## 5. 实验数量与充分性
### 实验数量
- **校准**：每个真实系统模型（4个Dubins变体 + 1个四旋翼）各生成 \(N_{cal}=100\) 个规划问题进行校准。
- **测试**：每个模型在 \(1-\alpha \in \{0.30,0.40,\dots,0.90\}\) 共7个目标覆盖水平下分别测试100个独立规划问题。
- **总实验数**：5个模型 × 7个水平 × 100个问题 = 3500次规划执行（每次重复可能因RRT随机性需多次运行，但未说明重复次数，可能为单次运行）。

### 充分性分析
- **优点**：覆盖了不同模型失配程度（4个水平）、确定性/随机性动力学、多个覆盖目标，提供了完整的覆盖率、成功率和任务完成率曲线，且与基线对比。
- **不足**：
  - 每个水平仅100个测试问题，统计稳定性尚可，但未报告置信区间或重复实验（如多次校准/测试随机种子）。
  - 缺少消融实验：例如不同校准集大小的影响、不同NCS近似策略（如使用更多控制序列）的影响。
  - 仅测试了RRT一种规划器，虽声称“规划器无关”，但未验证其他规划器（如MPC）的效果。
  - 未与现有基于模型的不确定性方法（如CC-RRT、Robust-RRT）对比。
- **公平性**：基线采用相同迭代预算，并在相同子集上评估，对比设计合理。

## 6. 主要结论与发现
1. **经验覆盖率始终高于目标值**：在所有模型和覆盖水平下，实际覆盖率≥1-α，验证了共形预测保证的有效性。
2. **任务完成率显著优于基线**：本文方法在Dubins和四旋翼场景下均大幅超过标称RRT（例如四旋翼在1-α=90%时达100%，基线仅约55%）。
3. **规划器成功率随保守度增加而降低**：随着目标覆盖水平升高或模型失配增大，\(\hat{q}_\alpha\)变大，收紧后问题更难求解，但降低幅度在测试范围内尚可接受。
4. **保守性与可靠性之间的权衡**：越高的保证要求（1-α越大）导致计划更保守，但返回计划的可靠性更高。

## 7. 优点
- **规划器无关**：框架可配合任意现有规划器（RRT、MPC等），通用性强。
- **分布自由**：无需假设真实动力学或干扰的分布形式，仅需i.i.d.数据。
- **理论保证清晰**：提供严格的概率性任务完成保证（命题3.4），且证明简洁。
- **实验设计合理**：包括两种不同复杂度系统（低维Dubins、高维四旋翼）、确定性/随机性、多级模型失配，且与朴素基线对比，结果有说服力。
- **可扩展性**：作者指出可扩展到随机动力学（通过调整NCS包含扰动范围）。

## 8. 不足与局限
- **保守性**：最坏情况NCS（式5）导致保守的\(\hat{q}_\alpha\)，尤其在随机动力系统中进行强行上界（如Remark 3.3所述），可能过度收紧约束，降低规划成功率。
- **计算复杂度**：校准阶段需要真实系统执行大量控制序列（每个校准问题需评估10个序列），若物理系统昂贵或费时，可能不可行。
- **未与现有不确定性方法对比**：未与CC-RRT、DR-RRT、Robust-RRT等专门处理不确定性的规划器对比，削弱了相对优势的论证。
- **缺乏多次重复实验与统计分析**：未报告方差或置信区间，无法判断结果稳定性。
- **场景简单**：障碍为圆形且静态，环境尺寸固定，未考虑动态障碍、高维状态空间或真实硬件验证。
- **校准数据分布假设**：要求规划问题i.i.d.，实际中任务分布可能变化（分布漂移），共形预测的边际保证此时可能退化。

（完）
