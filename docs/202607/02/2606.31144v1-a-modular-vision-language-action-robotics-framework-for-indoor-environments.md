---
title: A Modular Vision-Language-Action Robotics Framework for Indoor Environments
title_zh: 一种面向室内环境的模块化视觉-语言-动作机器人框架
authors: "Anindya Jana, Snehasis Banerjee, Arup Sadhu, Ranjan Dasgupta"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31144v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向室内任务的模块化VLA机器人框架
tldr: 针对室内环境中机器人依据自然语言指令执行复杂任务的需求，提出模块化视觉-语言-动作框架。系统并行运行感知与语言流水线：感知流使用OwlViT嵌入在线构建语义体素地图；语言流通过视觉语言模型分类用户指令。受500秒探索时间限制，未完成则使用部分地图。将指令与地图几何、语义信息融合生成详细提示，输入VLM获得可执行动作。该框架有效连接语言与动作，为自主导航提供灵活方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31144v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 919, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31144v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31144v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1579, \"height\": 1906, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31144v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 452, \"label\": \"Figure\"}]"
motivation: 现有方法难以将自然语言指令直接映射到机器人动作，尤其在室内复杂环境中需融合感知与语义理解。
method: 采用模块化架构，并行构建语义体素地图（OwlViT）和VLM指令分类，结合地图上下文生成提示驱动VLM输出动作。
result: 在CMU VLA挑战中，系统能在限时探索后基于部分地图理解指令并执行导航操作。
conclusion: 本文框架证明了模块化VL-A方法在室内环境中实现语言引导的机器人自主作业的有效性。
---

## 摘要
本文介绍了一个为CMU视觉-语言-动作（VLA）挑战赛设计的集成系统，旨在使自主代理能够基于自然语言指令执行复杂任务。我们的框架采用模块化架构，协调环境映射、问题处理和导航。系统运行在两条并行流中：一条感知流水线，利用OwlViT嵌入从实时摄像头数据构建语义体素地图；另一条语言流水线，使用视觉-语言模型对用户指令进行分类。映射有时间限制：如果达到500秒的探索限制，系统将使用部分地图继续。然后，分类后的查询被嵌入地图的几何和语义上下文中，以生成详细的提示供VLM处理。这产生了可执行输出，展示了弥合人类语言与机器人动作之间差距的可行方案。

## Abstract
This paper presents an integrated system for the CMU Vision-Language-Action (VLA) Challenge, designed to enable an autonomous agent to perform complex tasks based on natural language instructions. Our framework employs a modular architecture that orchestrates environment mapping, question processing, and navigation. The system operates in two parallel streams: a perception pipeline that constructs a semantic voxel map from real-time camera feeds using OwlViT embeddings, and a language pipeline that classifies user commands with a Vision-Language Model. The mapping is time-constrained; the system proceeds with a partial map if a 500-second exploration limit is reached. The classified query is then grounded in the geometric and semantic context of the map to generate a detailed prompt for the VLM. This yields an actionable output, demonstrating a capable solution for bridging the gap between human language and robotic action.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何使自主机器人在未知室内环境中，仅依靠自身传感器实时构建环境模型，并基于自然语言指令（如计数、定位、导航）完成复杂任务。
- **研究背景**：传统方法要么依赖预存3D模型（不适用于真实场景），要么缺乏持久世界模型导致无法回答视野外的查询。CMU VLA Challenge 提供了一个基准测试，要求系统在10分钟内完成未知场景的探索、语义理解和动作执行。
- **整体含义**：本文提出一种模块化框架，通过并行感知-语言流水线，将实时语义地图构建与视觉-语言模型推理结合，实现从人类语言到机器人动作的闭环，为实际部署提供可行方案。

### 2. 论文提出的方法论

- **核心思想**：采用模块化架构，将环境感知、语言理解、动作生成解耦为独立模块。感知流构建**时间受限的语义体素地图**，语言流通过VLM分类指令并利用地图上下文生成详细提示，最终输出可执行动作。
- **关键技术细节**：
  - **自主探索与2D网格地图**：基于LiDAR构建2D占用网格，通过覆盖规划算法生成全遍历路径（最小间距采样、距离变换、最优路径计算），并受500秒时间限制。
  - **语义体素地图构建**：OwlViT模型实时提取检测物体的CLIP特征嵌入，与3D坐标、尺度一起渐近构建体素地图（默认体素大小0.05m，后优化为0.15m）。若500秒内未完成探索，则使用部分地图。
  - **VLM查询处理**：用户指令首先通过Gemini 2.0 Flash分类为三类（数值、对象参考、指令跟随）；再提取关键名词，通过文本-特征嵌入相似度定位；最后将物体名称、3D坐标、网格位置等信息融合成上下文提示，重新输入VLM进行推理。
  - **动作生成**：根据分类结果：
    - 数值类：直接输出整数答案；
    - 对象参考类：解析坐标并在RViz中高亮；
    - 指令跟随类：解析高阶规划（如Goto(x,y)，Avoidbetween(...)），动态修改地图障碍物，调用路径规划算法生成并执行轨迹。
- **算法流程（文字说明）**：
  1. 启动ROS节点，同时运行感知流水线和语言流水线。
  2. 感知流：LiDAR → 2D网格地图；摄像头 → OwlViT检测 → 语义特征嵌入 → 体素地图更新。
  3. 若500秒未完成全覆盖，则冻结体素地图。
  4. 语言流：接收用户指令 → Gemini分类 → 提取物体 → 在体素地图中定位 → 生成上下文提示 → Gemini最终推理 → 解析输出。
  5. 根据输出触发相应动作（回答、标记、导航）。

### 3. 实验设计

- **数据集/场景**：使用了CMU VLA Challenge提供的18个Unity场景之一（办公室场景，如图1所示），基于Matterport3D模型、AI Habitat模拟器。
- **Benchmark**：CMU VLA Challenge，任务包括三种类型（数值、对象参考、路径导航），每个任务在新场景中重启，10分钟总时限。
- **对比方法**：论文未进行定量对比实验。文中对比了两种初始方法：
  - **无世界模型方法**：仅依靠BLIP2处理当前帧，无法回答全局问题，随机探索效率极低。
  - **VLA-3D启发的离线方法**：依赖预生成3D场景图，不适于实时自主场景。
  - 此外介绍了SORT3D框架作为比较，但未在相同设置下进行直接对比实验。

### 4. 资源与算力

- **算力需求**：论文未明确说明训练所需算力（因为整个框架为推理部署，无需训练）。使用模型包括OwlViT（google/owlvit-base-patch32）和Gemini 2.0 Flash API，均基于云端/本地推理。
- **硬件提及**：仅提及在GPU环境下使用PyTorch，但未说明具体GPU型号、数量或训练时长。所有核心模块均为预训练模型的推理调用。

### 5. 实验数量与充分性

- **实验数量**：论文仅展示了单一办公室场景下的系统运行结果，给出了三个问题示例（数值、对象参考、复杂对象参考）和一组探索时间优化前后的对比（8分42秒 → 4分17秒，耗时减半）。
- **充分性评估**：**不充分**。缺乏在多个场景、多种任务类型下的系统性定量评估（如成功率、平均任务完成时间、不同地图分辨率影响等）。无消融实验（除时间优化外），未与任何基线方法（如BLIP2、随机探索、离线场景图）进行公平对比。结论的泛化能力存疑。

### 6. 论文的主要结论与发现

- 模块化VLA框架（OwlViT + Gemini + 时间受限语义地图）能够成功完成数值、对象参考和路径导航三类任务，验证了从语言到动作的闭环可行性。
- 时间约束下的部分地图仍能支持合理的语义定位，500秒探索限制可行。
- 通过参数优化（增大采样间隔、放松到达阈值、增大体素大小），将探索时间降低约50%（从8分42秒到4分17秒），且不影响任务准确率（仅凭单个示例推断）。
- 依赖在线VLM API是主要性能瓶颈，本地小模型或离线方案可进一步降低延迟。

### 7. 优点

- **模块化设计**：感知与语言流水线独立并行，便于维护和扩展；各模块可替换（如用SigLip2或Deepseek作为后备）。
- **实时自建世界模型**：无需预存3D模型，适应未知环境，且允许部分地图下的推理，更接近真实部署条件。
- **VLM驱动查询处理**：利用Gemini进行指令分类和上下文融合，有效处理自然语言歧义；三步流水线（分类、定位、生成）清晰。
- **实际优化**：针对时间瓶颈进行了系统参数调优，并给出量化改进（50%时间节省），体现工程实用性。
- **用户界面**：提供了Flask Web界面，便于交互和演示。

### 8. 不足与局限

- **实验验证严重不足**：仅在一个办公室场景上进行演示，未在多个不同场景、不同任务的benchmark上评估，无法证明方法的泛化性；未与任何现有方法进行定量对比；缺乏消融实验（如无地图、不同VLM、不同体素大小的影响）。
- **依赖在线API**：即时推理依赖Gemini API，引入网络延迟和不可靠性，且每秒延迟对10分钟总时限影响显著（论文虽使用快速Flash版本，但仍为在线服务）。
- **静态环境假设**：采用“先探索后执行”顺序，地图在探索后固定，无法处理动态变化（如物体被移动），不适合真实动态场景。
- **无明确训练过程**：所有模型均为冻结预训练，未在目标场景进行微调或领域适应，可能影响特定场景的性能。
- **未讨论失败案例**：示例均为正确输出，未展示分类错误、定位失败、导航偏离等情况，缺乏鲁棒性分析。
- **算力细节缺失**：未说明评估所用GPU型号、内存、推理延迟等关键性能指标，不利于复现和比较。

（完）
