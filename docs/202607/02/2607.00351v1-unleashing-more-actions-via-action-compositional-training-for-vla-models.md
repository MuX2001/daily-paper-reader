---
title: Unleashing More Actions via Action Compositional Training for VLA Models
title_zh: 通过动作组合训练激发VLA模型的更多动作
authors: "Kai Peng, Jie Lu, Xiaojiang Peng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00351v1"
tags: ["query:vla"]
score: 9.0
evidence: VLA模型动作组合训练以泛化到新技能组合
tldr: 视觉-语言-动作模型在机器人操作中常因过拟合特定行为模式而难以泛化，即使新任务仅需组合已有子技能。为此，提出ACT-VLA离线数据增强框架，利用模型潜在任务表示从现有任务自动合成物理有效的新演示，无需额外数据采集。实验表明，该方法显著提升模拟操作任务的成功率，有效缓解分布外泛化问题，为扩展VLA模型行为提供了高效、可扩展的途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 1027, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 528, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1512, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00351v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 244, \"label\": \"Table\"}]"
motivation: VLA模型过拟合特定行为模式，无法泛化到仅需新组合子技能的分布外场景，而扩大高质量数据集成本高昂。
method: 提出ACT-VLA离线数据增强框架，利用模型潜在任务表示从现有任务合成物理有效的新演示，直接用于策略训练。
result: 在模拟操作任务中，ACT-VLA训练的模型成功率显著高于基线，验证了自动合成演示的有效性。
conclusion: ACT-VLA通过自动合成演示扩展训练分布，为提升VLA模型泛化能力提供了高效、可扩展的途径。
---

## 摘要
视觉-语言-动作模型在机器人操控中表现出色，这得益于演示数据的规模和多样性。然而，标准训练范式往往导致VLA模型严重过拟合于特定的行为模式，即使场景仅仅需要相同子技能的新颖组合，也无法泛化到分布外的情况。虽然扩大数据集可以缓解这种过拟合，但获取高质量的机器人数据仍然以劳动密集和成本高昂而闻名。为了解决这一僵局，无需昂贵的人工遥操作，并真正释放更多动作，即让VLA模型能够将已知子技能组合成远超原始演示的可执行行为集合，我们提出了ACT-VLA（VLA模型的动作组合训练），这是一种离线数据增强框架，利用模型的潜在任务表示，直接从现有任务中合成新颖、物理上有效的演示用于策略训练。通过消除额外的手动数据收集，我们的方法自动扩展了训练分布并减轻了过拟合。我们在模拟中具有挑战性的操控任务上评估了我们的方法。实验表明，尽管基线VLA模型由于原始分布过拟合而泛化能力较差，但使用我们合成数据训练的策略实现了显著更高的成功率，验证了利用现有任务进行自动化演示合成为拓宽VLA泛化能力提供了一条有效、可扩展且数据高效的途径。

## Abstract
Vision-Language-Action models excel at robotic manipulation, driven by the scale and diversity of demonstration data. However, standard training paradigms often cause VLA models to severely overfit to specific behavioral patterns, rendering them unable to generalize to out-of-distribution scenarios even when those scenarios merely require novel combinations of identical sub-skills. While expanding datasets can mitigate this overfitting, acquiring high-quality robot data remains notoriously labor-intensive and cost-prohibitive. To resolve this impasse without expensive human teleoperation and to truly unleash more actions,i.e., enable VLA models to compose known sub-skills into a much broader set of executable behaviors beyond the original demonstrations-we propose ACT-VLA (Action Compositional Training for VLA Models), an offline data augmentation framework that leverages the model's latent task representations to synthesize novel, physically valid demonstrations directly from existing tasks for policy training. By eliminating additional manual data collection, our method automatically expands the training distribution and mitigates overfitting. We evaluate our approach on challenging manipulation tasks in simulation. Experiments demonstrate that while baseline VLA models generalize poorly due to original distribution overfitting, policies trained with our synthesized data achieve substantially higher success rates, validating that leveraging existing tasks for automated demonstration synthesis provides an effective, scalable, and data-efficient route to broadening VLA generalization.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉-语言-动作模型（VLA）在机器人操控中表现优异，但标准训练范式导致模型严重过拟合于特定的行为模式，无法泛化到分布外场景，即使这些场景仅需对已学过的子技能进行新组合。例如，模型学会了“将立方体放到矩形托盘”和“将杯子放到圆形盘子”，但当测试要求“将杯子放到矩形托盘”时，模型往往失败。
- **背景**：机器人数据采集成本高昂（需要人工遥操作、环境准备、硬件维护），而可能的任务组合数量随技能数量呈组合爆炸增长，无法通过穷举演示覆盖所有场景。扩大数据集虽可缓解过拟合，但成本不可持续。先前工作表明，VLA模型在潜在空间中编码了可组合的结构，可在推理时通过文本潜在插值（TLI）合成新行为。本文旨在将这一能力转化为离线数据生成引擎，以自动扩充训练分布，无需额外数据收集。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出ACT-VLA（Action Compositional Training for VLA Models），一个离线数据增强框架。它利用预训练VLA模型中的文本潜在表示，通过插值生成已知子技能组合的新颖、物理有效的演示轨迹，并将这些合成数据加入训练集，使模型通过数据暴露内化组合技能，从而在推理时不需任何干预。
- **关键技术细节**：
  - **合成阶段**：采用文本潜在插值（TLI）技术。每个任务对应一个任务特定的文本潜在向量T（通过对演示的文本隐藏状态平均得到）。对于两个基础任务A和B，其文本潜在分别为T₁和T₂。在每个时间步i，修改文本隐藏状态h_T(i)如下：
    \[
    h_T(i) \leftarrow h_T(i) + (1-\alpha)T_1 + \alpha T_2 - [(1-\alpha)T_2 + \alpha T_1]
    \]
    其中\(\alpha = i/\lambda\)线性增加并裁剪到[0,1]。开始时抑制任务B的上下文，加强任务A；后期注入任务B，淡出任务A，从而生成连续的组合轨迹。
  - **数据记录与处理**：
    - **指令设计**：组合任务的指令ℓ由两个基础任务指令的文字成分拆分重组得到，确保语义一致。
    - **质量过滤**：将物理仿真器作为严格过滤器，仅保留成功完成全部子任务的轨迹；同时限制最大步长，排除过长或冗余的轨迹。
    - **合成规模**：为每个组合任务收集约30-40个成功轨迹，使合成数据集规模与原始套件数据集匹配（每套约300-400个演示）。
  - **增强训练**：将合成数据集与原始数据集合并，采用标准行为克隆和相同超参数对VLA模型进行训练，不修改架构。
- **关键设计决策**：与简单将TLI移到训练不同，本文解决了三个挑战：指令设计（需要显式语言指令）、质量控制（需要过滤失败轨迹以避免错误关联）、训练动力学（保持原始与合成数据量平衡以防止灾难性遗忘）。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **Benchmark**：LIBERO仿真套件，包含四个标准套件（LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10）以及两个OOD组合套件（LIBERO-Spatial-OOD和LIBERO-Goal-OOD）。每个套件10个任务。OOD套件包含训练中未出现但可由子技能重新组合得到的任务（例如将物体与目标位置重新配对）。
- **对比方法**：
  - 基线VLA模型：UniVLA、OpenVLA-OFT、GR00T-N1、π0.5（在原始LIBERO演示上训练）。
  - 推理时TLI方法：π0 + 推理时文本潜在插值（来自Li 2026）。
  - 本文方法：在π0.5骨干上使用合成数据增强训练（Ours (π0.5)），另外也在π0骨干上进行了消融实验。
- **评估协议**：每个任务使用3个随机种子，每个种子50个episode，共150个episode/任务。报告各套件平均成功率（%）。所有模型训练30k步，设置相同。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量及训练时长。仅提到所有模型在相同设置下训练30k步。未提供计算资源细节。

## 5. 实验数量与充分性

- **实验组数**：
  - 主要结果（表I）：包含6个套件×若干模型，对比了5个基线+本文方法，共约36个条件（每个条件150×10=1500episode）。
  - 消融实验（表II）：在π0骨干上对比了三种设置（原始数据、推理时TLI、原始+合成数据），共2个OOD套件×3设置=6个条件。
  - 总计两组实验表，实验数量中等。
- **充分性评价**：
  - **正面**：覆盖了多种主流VLA模型作为基线，并在两个OOD套件上展示了显著提升，消融实验控制了骨干和合成数据因素，验证了方法的有效性。
  - **不足**：缺乏在真实机器人上的实验（仅仿真）；未测试更长组合链（仅两技能组合）；未分析不同过滤阈值或合成数据量对性能的影响；仅在一个benchmark（LIBERO）上评估，泛化性有限。消融实验也仅比较了π0骨干，未在更大模型上重复。总体而言，实验设计合理但覆盖范围仍显局限。

## 6. 论文的主要结论与发现

- ACT-VLA在LIBERO-Spatial-OOD上比π0.5提高52.7个百分点（从35.5%到88.2%），在LIBERO-Goal-OOD上提高49.0个百分点（从46.6%到95.6%），同时保持标准套件性能。
- 与推理时TLI相比，训练时合成数据方法取得更高OOD成功率（88.2% vs 81.0%，95.6% vs 85.0%），且消除了部署时的计算开销。
- 消融实验证明，合成数据是性能提升的关键：无合成数据时π0在OOD套件上仅2.0%和16.0%，加入合成数据后达到85.9%和90.5%。
- 结论：自动化演示合成是扩展VLA组合泛化的有效、可扩展、数据高效途径，无需额外遥操作、架构更改或外部规划器。

## 7. 优点：方法或实验设计上的亮点

- **方法上的亮点**：
  - 将推理时潜在操纵转化为离线数据生成，使模型通过数据学习组合能力，部署时无额外开销。
  - 提出系统的数据生成框架：包括指令设计、质量过滤、规模控制，解决了简单使用TLI作为训练数据可能带来的问题。
  - 即插即用：合成数据可直接加入任何VLA训练流程，无需修改模型。
- **实验设计上的亮点**：
  - 专门构造OOD组合套件，直接评估组合泛化能力。
  - 对比了多种SOTA VLA模型，并在同一骨干上公平对比推理时TLI与训练时合成。
  - 消融实验清晰分离了合成数据和骨干的影响。

## 8. 不足与局限

- **实验覆盖**：
  - 仅仿真环境（LIBERO），未验证真实机器人上的sim-to-real迁移。
  - 仅测试两技能顺序组合，未探索更长链组合。
  - 仅使用一个benchmark，通用性待验证。
- **依赖条件**：
  - 合成质量依赖于预训练VLA模型的语义解缠能力；若基础模型较弱，可能成为瓶颈。
  - 需要已有可用的子技能演示（本方法生成的是组合，不是凭空创造新技能）。
- **潜在偏差**：
  - 质量过滤依赖仿真器反馈，若仿真器不准确可能引入偏差；真实场景中无法自动获取失败信号。
  - 合成数据规模与原始数据保持平衡，但该比例是否最优未探索。
- **未明确提及**：计算成本、过滤效率、合成数据多样性等细节。

（完）
