---
title: Fast Asymptotically Optimal Kinodynamic Planning via Vectorization
title_zh: 基于向量化的快速渐近最优运动动力学规划
authors: "Yitian Gao, Andrew Lu, Zachary Kingston"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03987v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: GPU加速的渐近最优动力学运动规划
tldr: 基于采样的运动规划在高维复杂约束下有效但难以实时，现有GPU加速方法依赖CUDA编程。本文提出PAKR，利用JAX和XLA编译器实现大规模并行运动规划，并通过AO-x元算法达到渐近最优性。实验表明PAKR在运行时间上与最新GPU规划器相当，且解质量更优。贡献在于通过标准Python工具实现GPU加速，显著提升可访问性和可移植性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03987v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1732, \"height\": 1101, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03987v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 710, \"height\": 1152, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03987v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 724, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03987v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 726, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03987v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 711, \"height\": 645, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03987v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03987v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1679, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03987v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 640, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03987v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 742, \"height\": 982, \"label\": \"Table\"}]"
motivation: 现有GPU加速运动规划需CUDA编程，限制可访问性与可移植性，难以被广泛采用。
method: 提出PAKR，基于JAX和XLA编译器实现大规模并行，结合AO-x元算法实现快速迭代重规划获得渐近最优性。
result: 实验表明PAKR在运行时间上与SOTA GPU规划器竞争，且解质量更优，可扩展至复杂动力学（如MuJoCo-XLA）。
conclusion: PAKR通过标准Python实现高效并行规划，降低GPU加速门槛，证明向量化在运动规划中的潜力。
---

## 摘要
基于采样的运动规划器已被证明对于具有复杂运动动力学约束和高维度的系统是有效的。然而，这些算法难以实现实时性能，这促使了近期对并行化规划的研究。尽管GPU加速的规划器已经实现了显著的加速，但现有方法需要专门的CUDA编程，限制了可访问性和可移植性。我们提出了并行渐近最优运动动力学RRT（PAKR），这是一种大规模并行的运动动力学规划器，利用JAX和XLA编译器通过标准Python工具实现GPU加速。通过将我们的并行规划器与AO-x元算法相结合，我们通过快速迭代重新规划实现了渐近最优性。我们提供了概率完备性的理论分析，分析了批量大小和分支因子对收敛的影响，并使用MuJoCo-XLA模拟器展示了在复杂动力学上的可扩展性。实验表明，其运行时间与最先进的GPU规划器相当，且解的质量更优。

## Abstract
Sampling-based motion planners have been shown to be effective for systems with complex kinodynamic constraints and high dimensionality. However, these algorithms struggle to achieve real-time performance, leading to recent efforts to parallelize planning. While GPU-accelerated planners have achieved significant speedups, existing approaches require specialized CUDA programming that limits accessibility and portability. We present Parallel Asymptotically Optimal Kinodynamic RRT (PAKR), a massively parallel kinodynamic planner leveraging JAX and the XLA compiler to achieve GPU acceleration through standard Python tooling. By combining our parallel planner with the AO-x meta-algorithm, we achieve asymptotic optimality through fast iterative replanning. We provide a theoretical analysis of probabilistic completeness, analyze the effects of batch size and branching factor on convergence, and demonstrate scalability to complex dynamics using the MuJoCo-XLA simulator. Experiments show competitive runtimes with state-of-the-art GPU planners and superior solution quality.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于采样的运动规划（SBMP）在高维复杂动力学约束下有效，但难以实现实时性能。现有GPU加速方法（如Kino-PAX）依赖于专门的CUDA编程，限制了可访问性和可移植性。
- **整体含义**：本文提出PAKR（Parallel Asymptotically Optimal Kinodynamic RRT），通过JAX和XLA编译器，利用标准Python工具实现大规模并行GPU加速，同时结合AO-x元算法实现渐近最优性，旨在降低GPU加速运动规划的门槛，提升可移植性，并提供与SOTA相当或更优的性能。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将AO-x元算法（通过迭代成本边界重新规划将任何概率完备规划器转化为渐近最优）与大规模并行快速子规划器结合。AO-x天然适合并行化，因为每次内部规划迭代只需快速找到可行（但次优）解，然后通过多次重规划细化。
- **关键技术细节**：
  - **PAKR算法（算法1）**：
    - 外层循环：迭代细化，使用当前最佳成本`c_best`作为阈值。
    - 内层循环（并行扩展）：
      1. 采样`B/A`个目标状态（`X_rand`）。
      2. 通过最近邻搜索找到`B/A`个父节点（`X_near`）。
      3. 为每个父节点采样`A`个随机控制（`U`）。
      4. 并行前向传播所有`B`个状态-控制对，得到新状态和成本。
      5. 使用成本边界裁剪：仅当`cost_to_come + heuristic < c_best`且无碰撞时才插入树中。
      6. 若到达目标区域，更新`c_best`并提取路径，跳出内层循环重新开始更紧的边界。
  - **实现细节**：
    - 使用JAX的XLA编译器将整个规划循环编译为单个GPU内核，消除CPU-GPU传输开销。
    - 静态图架构：树的最大大小`N`和批量大小`B`编译时固定，树由四个连续缓冲区（state、action、parent、cost）表示。
    - 分层最近邻策略：根据树的大小使用不同内核（如树大小在4^6~10之间），减少填充开销。
    - 分支因子`A`：减少最近邻搜索次数（仅`B/A`次），每个父节点采样`A`个控制，平衡探索效率与搜索开销。
    - 支持任意动力学：通过模块化传播器函数（如直接使用MuJoCo-XLA模拟器）。
  - **理论分析**：
    - 概率完备性：由于批量仅引入有限延迟（最多延迟一个批次发现新节点），不影响渐近上采样序列的完备性；分支因子`A>1`保留了所有可能扩展机会。
    - 渐近最优性：内层规划器概率完备，且`c_best`单调递减，满足AO-x假设，在Lipschitz连续动力学和成本函数下收敛到最优解。

## 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法

- **场景与动力学模型**：
  - 与Kino-PAX对比的场景（来自Kino-PAX论文）：Tree（A）、Narrow（B）、House（C）三个3D障碍环境。
  - 动力学模型：6D Double Integrator、6D Dubins Airplane、12D Quadcopter。
  - 与iDb-A*和SST*对比的场景（来自DynoBench）：Unicycle 1（速度控制）、Unicycle 2（力控制）、Acrobot（欠驱动双摆）、Quadcopter（12D四旋翼）。
  - 使用MuJoCo-XLA模拟器的可扩展性测试：Cartpole、Block Push（10D）、Soft-growing vine robot（来自Gao et al.）。
- **Benchmark**：
  - 首次解时间、树大小、成功率、初始解成本、最终解成本（收敛后）。
- **对比方法**：
  - **Kino-PAX**（最先进GPU并行规划器，CUDA C实现）。
  - **iDb-A***（OMPL中的渐近最优搜索规划器）。
  - **SST***（OMPL中的渐近最优采样规划器）。
  - 内部消融：不同批量大小和分支因子对性能的影响。

## 4. 资源与算力

- 文中明确说明：
  - **GPU**：NVIDIA GeForce RTX 4090（16,384 CUDA核心，24 GB VRAM），用于PAKR和Kino-PAX。
  - **CPU**：AMD Ryzen Threadripper PRO 5965WX（24核，3.8 GHz，32 GB RAM），用于iDb-A*和SST*。
- 未提及训练时长（因为运动规划无训练过程），只报告了推理/规划运行时间（毫秒至秒级）。

## 5. 实验数量与充分性

- **实验数量**：
  - 与Kino-PAX对比：3个环境 × 3个动力学模型 × 100次试验 = 900次试验。
  - 收敛分析（PAKR自身）：3个环境 × 3个动力学模型 × 100次试验，记录初始和最终解成本/时间。
  - 与iDb-A*和SST*对比：4个问题 × 100次试验 = 400次试验。
  - 批量大小和分支因子敏感性：1个环境（12D Quadcopter Env A） × 4个批量（4k,8k,16k,32k） × 4个分支因子（2,16,64,128） × 100次试验 = 1600次试验（部分组合未完全报告，但表IV给出了主要结果）。
  - MuJoCo-XLA可扩展性：3个任务（Cartpole, Block Push, Vine），各报告中位数时间。
- **充分性**：
  - 覆盖了多个动力学模型（线性、非完整、欠驱动、高维）、多个环境（简单到复杂）以及不同SOTA规划器。
  - 消融实验研究了批量大小和分支因子，提供了性能趋势。
  - 统计方法：中位数和四分位距（IQR），100次随机试验，合理。
- **客观性与公平性**：
  - 与Kino-PAX对比时，PAKR未使用AO-x（仅单次并行RRT）以保证公平。
  - 与iDb-A*和SST*对比时，PAKR使用了完整AO-x，但对比方法也给予了足够预算（300秒）。SST*超参数经过微调，目标区域扩大以增加成功率，但调整可能对其有利。iDb-A*在Unicycle 2上成功率低（12%），可能因轨迹优化失败，但PAKR在此类复杂问题上表现更好。
  - 总体实验设计较公平，但未与Kino-PAX+（渐近近最优扩展）对比，因后者开源实现不可用。

## 6. 论文的主要结论与发现

- **主要结论**：PAKR通过向量化实现了与SOTA GPU规划器（Kino-PAX）相当或更快的运行时间（通常毫秒级），且解质量更优（树大小更小，成本收敛更快）。结合AO-x后，PAKR能在数十毫秒内显著降低路径成本，达到渐近最优性。在多个动力学问题上，PAKR的首次解时间和最终解成本均优于或等于iDb-A*和SST*，尤其在高维度、非线性的复杂动力学中优势明显。
- **具体数据**：
  - 与Kino-PAX对比：PAKR在6DDI/Tree下2.5ms vs 5.0ms，树大小9k vs 164k。
  - 收敛分析：6D DI在Env A中初始成本1.76（2.3ms），最终成本1.51（28.0ms），成本下降约14%。
  - 与iDb-A*和SST*对比：Unicycle 1中PAKR首次解时间1.3ms vs 254ms（iDb-A*）和600ms（SST*），最终成本4.46s vs 5.10s和4.41s；Acrobot中PAKR最终成本3.22s vs 4.96s和3.39s。
  - MuJoCo-XLA可扩展性：Cartpole 22.82ms，Block Push 283.65ms，Vine 360.25ms，均显著快于原始论文的秒级结果。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 利用JAX/XLA编译整个规划循环为单个GPU内核，消除CPU-GPU传输，且仅需标准Python（无需CUDA编程），大幅提升可访问性和可移植性。
  - 结合AO-x元算法，巧妙地将并行化与渐近最优性统一：不试图与并行中的信息滞后斗争，而是拥抱快速次优规划并通过频繁重规划达到最优。
  - 静态图架构和分层最近邻策略有效减少了内存填充和最近邻搜索开销。
  - 模块化传播器支持任意动力学（包括物理模拟器），展示了可扩展性。
- **实验亮点**：
  - 与多个SOTA方法（GPU、搜索、采样）在多种动力学和场景上对比，覆盖面广。
  - 对批量大小和分支因子进行了系统的消融研究，提供了实用的指导。
  - 使用MuJoCo-XLA模拟器验证了在重型物理仿真上的能力。

## 8. 不足与局限

- **实验覆盖局限**：
  - 未与Kino-PAX+（渐近近最优扩展）对比，因为其开源实现未公开，但PAKR声称是渐近最优的，缺少直接比较。
  - 未在真实机器人或硬件上测试，所有实验均在模拟中进行。
  - 静态图架构要求编译时固定最大树大小和批量大小，可能不适合需要动态调整资源的场景。
  - 分支因子和批量大小的选择对性能敏感，但未提供自适应策略。
- **偏差风险**：
  - 与iDb-A*和SST*对比时，SST*的超参数经过调优且目标区域扩大，可能使其表现更优或更差（成功率74% vs 41%），但PAKR在大多数问题上仍领先。
  - PAKR使用Kino-PAX的环境但模型略有不同（如距离成本 vs 时间成本），可能影响公平性（但作者在收敛分析中明确指出使用距离成本以便可视化）。
- **应用限制**：
  - 适用于低中维度（文中最多12D），未测试更高维度（如30D以上）的扩展性。
  - 对于需要非常长时间规划（如几百秒）的场景，PAKR的重规划策略可能不如传统渐近最优规划器稳定（但文中CPU规划器也给予了300秒，PAKR在毫秒级即找到解）。
  - 依赖JAX和XLA，可能不兼容所有GPU或旧的硬件/驱动。

（完）
