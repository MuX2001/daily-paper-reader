---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:control"]
score: 9.0
evidence: 通过可行动作映射连接强化学习与最优控制
tldr: 针对约束动态系统的控制难题，提出FAOC框架，通过可行动作映射将强化学习抽象动作转换为最优控制问题的可行参数，严格保证约束满足。在机器人乒乓球任务中，FAOC在样本效率和闭环性能上均优于现有基线，实现灵活性与安全性的有效结合。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1621, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1314, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1617, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 1814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1604, \"height\": 619, \"label\": \"Figure\"}]"
motivation: 克服强化学习在约束动态系统中无法保证递归可行性和安全性的局限。
method: 基于优化的可行动作映射，将RL抽象动作转换为状态依赖的OCP可行参数集。
result: 在机器人乒乓球模拟中，FAOC在样本效率和闭环性能上超过最先进基线。
conclusion: FAOC无需专家设计动作空间，有效融合RL灵活性与OC可预测安全性。
---

## 摘要
操作受约束的动态系统需要控制器在强制执行递推可行性和安全约束的同时高效解决复杂任务。为了应对这些相互竞争的要求，我们提出了可行动作最优控制（FAOC），一种新颖的将强化学习（RL）和最优控制（OC）结合的控制框架。关键贡献是一种计算高效的、基于优化的映射算法，它将RL代理的动作从静态抽象集转换为最优控制问题（OCP）的状态依赖可行参数集，从而保证严格满足动态系统的约束。因此，FAOC有效结合了OC的可预测安全性和RL的灵活性。与先前工作相比，RL代理的抽象动作空间不需要专家或启发式设计，且OCP的制定不会因RL无法保证可行性而受损。我们将该方法应用于机器人乒乓球实时运动规划，这封装了这些挑战。通过模拟实验，我们展示了FAOC在样本效率和闭环性能方面均优于最先进的基线方法。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：约束动态系统（如机器人）的控制器需要同时满足复杂任务求解、递推可行性和安全约束。强化学习（RL）擅长处理复杂任务但无法严格保证约束满足；最优控制（OC）能通过约束显式保证安全，但面对非凸、长时域、稀疏奖励问题时计算困难。
- **整体含义**：本文提出 **FAOC（可行动作最优控制）** 框架，旨在桥接RL与OC，融合二者的互补优势——RL的灵活性和OC的可预测安全性，同时解决现有结合方法中动作空间设计依赖启发式、OCP可行性无法保证等问题。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：在RL策略与参数化OCP之间插入一个优化驱动的映射算法，将RL输出的抽象动作（来自静态、几何简单的抽象集）转换为状态依赖的OCP可行参数集，从而保证OCP始终可行，RL只需专注任务策略，无需学习物理约束。
- **关键技术细节**：
  - **拓扑刻画**：在温和条件下（OCP目标连续、约束凸、可行集有界等），证明状态依赖参数集 \(P(x)\) 是紧致、实心、凸的（Lemma 1）。线性MPC情形下进一步给出充要条件（Corollary 1）。
  - **可逆映射算法（Algorithm 1）**：
    - 从抽象集 \(X\) 的内点出发，沿方向 \(d\) 计算边界缩放因子 \(\alpha\)。
    - 通过方向变换 \(\phi(d)\) 映射到目标方向，再计算 \(Y\) 中的边界缩放因子 \(\beta\)。
    - 最终映射点为 \(y = y_c + \frac{\beta}{\alpha} \phi(d)\)。映射是双射（Proposition 1）。
  - **几何畸变缓解**：
    - **2D面积匹配变换**：通过极角归一化面积函数 \(a_X(\xi), a_Y(\xi)\) 构造 \(\phi\)，使得均匀采样保持不变形（Proposition 2 & 3）。
    - **任意维度线性变换**：使用椭球体代理集（如最大体积内切椭球MVIE），导出线性矩阵 \(\Phi = Q_{eY} Q_{eX}^{-1}\)（Proposition 4），近似保持密度。
  - **隐式定义目标集的处理**：当 \(Y = EW + e\) 时，通过提升到高维空间计算内点（Proposition 5 & 6 & 7）和形状矩阵（Proposition 8），无需显式几何表示，离线计算状态无关变换矩阵，在线仅需一次凸优化求边界因子（等式(8)），实现实时计算。
- **算法流程文字说明**：
  1. RL策略根据状态输出均值和协方差，采样得到原始动作，经非线性函数映射到抽象动作 \(\bar{a} \in \bar{A}\)。
  2. 利用Algorithm 1，基于当前状态x的可行参数集 \(P(x)\)，将 \(\bar{a}\) 映射为可行参数 \(p\)。
  3. 将 \(p\) 固定为OCP中的动作编码参数（如终端状态目标），求解OCP得到最优控制序列。
  4. 控制序列作用于系统，观测下一状态，进入下一周期。

## 3. 实验设计
- **应用场景**：机器人乒乓球（8-DoF移动机械臂），需在32ms内规划关节轨迹。任务包含三种技能：球放置、上旋/下旋生成、扣杀。每回合稀疏奖励。
- **Benchmark对比方法**：
  - **1Dpos / 1Dvel / 1Dacc**：来自[30]的一维映射，RL只能指定位置/速度/加速度之一。
  - **2Dsoft**：二维动作空间但不考虑可行性，使用软终端代价（权衡权重Wt和ws）。
  - **FAOC（ours）**：使用线性变换（因2D面积匹配计算量更小且效果相当）。
- **模拟环境**：基于真实机器人数据构建模拟环境，使用合成轨迹和真人击球混合训练。
- **实验组数**：
  - 三个主要实验：(a) 基线（SAC+BRONets）；(b) 算法与架构影响（SimBaV2）；(c) 低RL频率影响（SimBaV2 + 7.8125Hz）。
  - 每个控制器训练5个不同随机种子的agent。
- **评估指标**：归一化奖励、回球率、生成上旋、生成速度、生成下旋、目标距离。每个数据点来自1000次评估episode。

## 4. 资源与算力
- **未明确说明**：论文正文及附录中未提及训练所使用的GPU型号、数量及具体训练时长。致谢部分感谢了Sony AI平台团队，但未透露硬件配置。因此无法评估算力规模。

## 5. 实验数量与充分性
- **数量**：共5种控制器 × 5种子 × 3个实验设置 = 75条训练曲线，每个实验点基于1000次评估，统计图包含p值和显著性标记。
- **充分性**：
  - **基线实验**：FAOC在所有指标上均优于2Dsoft和1D变体，且超过一半的对比具有统计显著性（p<0.05）。
  - **算法框架影响**：使用更强的SimBaV2后，FAOC仍保持优势，8个对比显著。
  - **低频率实验**：FAOC退化最小，13个对比显著，说明其控制性更强。
- **公平性**：对比方法实现了文献中最新方法（如[30]的1D映射、2Dsoft为常见软约束），且超参数（如2Dsoft的权重ws和Wt）经过多组尝试（图4展示了不同ws的表现）。实验设计相对客观。

## 6. 主要结论与发现
- FAOC在样本效率和最终性能上一致优于现有RL-OC混合方法。
- 2D动作空间比1D空间提供更强控制能力（FAOC和2Dsoft表现优于1D变体）。
- FAOC的状态依赖可行动作空间避免了无效探索，显著提升学习效率，并且即使降低RL决策频率（从31.25Hz降至7.8125Hz），性能退化最小。
- 在真实机器人上，FAOC使机器人首次在正式ITTF比赛中击败专业选手（引用了Nature论文[1]）。

## 7. 优点
- **方法论创新**：提出了通用且计算高效的映射框架，兼顾拓扑保证和几何畸变缓解。
- **无需启发式动作空间设计**：RL的抽象集固定为简单凸集（如超立方体），自动映射到状态依赖可行域。
- **严格可行性**：通过硬约束而非松弛惩罚保证OCP可行性，且映射可逆。
- **可扩展性**：支持隐式定义的目标集，通过离线计算状态无关变换矩阵实现实时应用。
- **实验验证充分**：在复杂真实的机器人乒乓球任务上验证，并提供了详细的统计显著性分析。

## 8. 不足与局限
- **实验局限**：
  - 仅在机器人乒乓球单一任务上验证，泛化性未在其他领域证明。
  - 对比方法中1D变体仅实现了文献[30]的一维映射，未对比文献[31]的二维映射（因为[31]假设原点在内点，不适用于此场景）。
  - 低频率实验仅测试7.8125Hz，未探索更低或更高频率的极限。
- **理论局限**：
  - 假设目标集为凸且存在非空内部，对于非凸可行域（如障碍物环境）不直接适用。论文指出RL负责处理非凸方面，OCP负责局部凸区域，但理论框架本身未扩展到非凸。
  - 线性变换只能近似缓解畸变，仅在2D有面积匹配的精确解。
- **算力与成本**：未报告训练所需GPU资源，难以复现或评估计算成本。
- **可复现性**：虽然开源了映射算法和OCP实现（GitHub），但RL训练管道和模拟环境细节依赖商业机构内部代码，可能难以完全复现。

（完）
