---
title: Bilinear Koopman-Based Robust Model Predictive Control for Unknown Nonlinear Systems via Contraction Metrics
title_zh: 基于双线性Koopman的收缩度量鲁棒模型预测控制用于未知非线性系统
authors: "Yuki Higuchi, Kazuhiro Sato"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25658v1"
tags: ["query:control"]
score: 9.0
evidence: 基于Koopman的未知非线性系统鲁棒MPC
tldr: 针对未知非线性系统的约束控制，基于双线性Koopman的鲁棒MPC面临建模误差和多步预测离开流形两大难题。本文提出重投影机制将预测状态拉回流形，获得误差感知的离散时间控制仿射预测器，并基于离散时间鲁棒收缩度量构建同余管，形成终端管MPC框架。理论证明了原始约束的鲁棒满足、递归可行性和收敛性；数值实验表明该方法相比现有Koopman鲁棒MPC在镇定性能上更优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25658v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 858, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25658v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 849, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25658v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 1146, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25658v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 258, \"label\": \"Table\"}]"
motivation: 双线性Koopman逼近精度高，但现有鲁棒MPC未有效处理其建模误差及多步预测的流形偏离问题。
method: 通过重投影修正双线性Koopman多步预测，构建误差感知离散控制仿射模型，并利用鲁棒收缩度量设计同余管MPC。
result: 数值实验验证了该方法对非线性系统的鲁棒镇定，且控制性能优于现有线性/双线性Koopman鲁棒MPC。
conclusion: 所提框架在理论上保证了约束满足、递归可行性与收敛性，在实践中展现了更高的控制精度和鲁棒性。
---

## 摘要
利用Koopman算子理论的数据驱动模型预测控制(MPC)是约束控制未知非线性系统的一种有前景的方法。尽管线性Koopman实现因其简单性而被广泛使用，但双线性Koopman实现可以为非线性控制系统提供显著更高的逼近精度。然而，考虑双线性Koopman实现中建模误差的鲁棒MPC(RMPC)公式仍然有限。本文针对具有一般非线性约束的未知非线性系统，提出了一种基于数据驱动双线性Koopman实现的RMPC框架。一个核心难点在于有限维Koopman预测器不必保持有效提升状态的流形，因此在提升坐标下的多步预测可能离开一步误差保证适用的区域。我们通过将每个预测的提升状态重新投影回流形来解决该问题，从而获得原始状态空间中无需不切实际假设的误差感知离散时间控制仿射预测器。针对该预测器，我们基于离散时间鲁棒控制收缩度量构建位似管，然后制定具有终端约束的基于管的RMPC问题。在所提出的公式下，我们证明了真实闭环轨迹对原始非线性约束的鲁棒满足性、递归可行性以及收敛到目标状态的邻域。数值实验展示了非线性系统的鲁棒镇定性能，以及所提方法在性能上优于现有基于Koopman的RMPC方法的优势。

## Abstract
Data-driven model predictive control (MPC) using Koopman operator theory is a promising approach for constrained control of unknown nonlinear systems. While linear Koopman realizations are commonly used due to their simplicity, bilinear Koopman realizations can provide significantly higher approximation accuracy for nonlinear control systems. However, robust MPC (RMPC) formulations that account for modeling errors in bilinear Koopman realizations remain limited. This paper proposes a RMPC framework for unknown nonlinear systems with general nonlinear constraints based on data-driven bilinear Koopman realizations. A central difficulty is that finite-dimensional Koopman predictors need not preserve the manifold of valid lifted states, so multi-step prediction in lifted coordinates may leave the region where one-step error certificates apply. We address this issue by reprojecting each predicted lifted state back onto the manifold, thereby obtaining an error-aware discrete-time control-affine predictor in the original state space without impractical assumptions. For this predictor, we develop a discrete-time robust control contraction metric based homothetic tube construction, and then formulate a tube-based RMPC problem with terminal ingredients. Under the proposed formulation, we prove robust satisfaction of the original nonlinear constraints by the true closed-loop trajectory, recursive feasibility, and convergence to a neighborhood of the target state. Numerical experiments demonstrate robust stabilization of nonlinear systems and the advantages of the proposed method over existing Koopman-based RMPC approaches in terms of performance.

---

## 论文详细总结（自动生成）

# 基于双线性Koopman的收缩度量鲁棒模型预测控制用于未知非线性系统——论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：未知非线性系统的约束控制是控制领域的重要挑战。Koopman算子理论通过将非线性系统提升到高维空间并近似为线性或双线性动力学，为数据驱动控制提供了有力工具。然而，现有方法存在两大关键问题：
  - 线性Koopman实现逼近精度有限，而双线性Koopman实现虽精度高，但建模误差难以在鲁棒MPC中被充分处理。
  - 有限维Koopman预测器在提升空间中的多步预测可能离开有效提升状态流形，导致预测失效，而现有方法常依赖难以保证的“Koopman字典不变性”假设。
- **整体含义**：本文旨在提出一个**无需字典不变性假设**、能同时保证**原始约束的鲁棒满足**、**递归可行性**和**收敛到目标邻域**的鲁棒MPC框架，通过结合双线性Koopman实现与基于鲁棒控制收缩度量(RCCM)的管式MPC，填补了现有研究的空白。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 核心思想
- 针对双线性Koopman多步预测离开流形的问题，提出**重投影机制**：在每步预测后，将提升状态投影回流形，从而在原始状态空间中获得一个**误差感知的离散时间控制-仿射预测器**。
- 针对该预测器，设计**离散时间鲁棒控制收缩度量(RCCM)**，构建**位似管**，并在此基础上制定**终端管式MPC**，保证闭环系统的鲁棒性、递归可行性和收敛性。

### 关键技术细节

1. **双线性Koopman模型学习（Algorithm 1）**
   - 使用包含状态、状态与输入乘积、输入本身的提升函数字典(2)。
   - 通过最小二乘（式(3)）求解矩阵$\bar{K}$，提取矩阵$A, H_i, B$；通过式(6)学习解码矩阵$C$。
   - 得到一步预测模型：$\tilde{f}(x,u) = C(Az(x) + \sum_i u_i H_i z(x) + Bu)$。

2. **建模误差集估计（Algorithm 2, Lemma 1）**
   - 基于Hoeffding不等式（Lemma 1），用验证数据集对误差超矩形$W$进行概率覆盖估计，确保以高置信度保证$w\in W$。

3. **扰动模型构建**
   - 定义$f_W(x,u,d) = \tilde{f}(x,u) + E(x,u)d$，其中$E(x,u)=I, d\in D=W$。

4. **离散时间鲁棒控制收缩度量(RCCM)（Assumption 1）**
   - 寻找状态依赖矩阵$M(x)$和反馈增益$K(x)$，满足收缩条件(15a)和上下界(15b)。
   - 由此定义度量距离$V(x,\bar{x})$（黎曼距离），并构建反馈控制律$\kappa(x,\bar{x},\bar{u})$（沿测地线积分）。

5. **管设计与约束收紧（Proposition 1, Proposition 2, Theorem 1）**
   - **Proposition 1**：证明在RCCM下，状态距离的上界演化满足$V(x^+, \bar{x}^+) \le \sqrt{1-\rho_c} V(x,\bar{x}) + L_E$。
   - **Proposition 2**：给出约束收紧条件$h_j(\bar{x},\bar{u}) + c_j V(x,\bar{x}) \le 0$，确保测地线上的所有点都满足约束。
   - **Theorem 1**：通过归纳证明，若名义轨迹满足(16)，则真实轨迹保持在管内且满足约束。

6. **终端集与终端约束（Assumption 2, Proposition 3）**
   - 假设存在终端集$X_f$、终端控制器$k_f$、终端成本$\ell_f$满足(18)。
   - **Proposition 3**：在RCCM存在且平衡点满足约束的条件下，可构造$X_f = \{(\bar{x},\delta) | \bar{x}=x_{ref}, \delta \le \delta_f\}$，$k_f\equiv u_{ref}$，$\ell_f\equiv 0$。

7. **管式MPC优化问题（式(22)）**
   - 决策变量：名义轨迹$\bar{x}_{i|k}, \bar{u}_{i|k}$和管半径$\delta_{i|k}$。
   - 约束：名义动力学(22a)、半径扩散(22b)、收紧约束(22c)、当前状态与管半径匹配(22d)、终端约束(22e)。
   - 目标：二次型代价(23)。
   - 在线控制：求解(22)后，使用反馈律$\kappa(x(k), \bar{x}_{0|k}^*, \bar{u}_{0|k}^*)$。

### 算法流程
- **离线设计（Algorithm 3）**：计算RCCM、$L_E, c_j$、终端约束。
- **在线控制（Algorithm 4）**：每个控制周期求解优化问题(22)并施加反馈输入。

## 3. 实验设计

### 仿真场景
- **系统**：倒立摆（连续模型），状态$x=[x_1, x_2]^T$（角度、角速度），输入$u$（扭矩）。
- **约束**：$[-1, -2] \le x \le [1, 2]$，$-20 \le u \le 20$。
- **目标**：从初始状态$[0.2, 1]^T$镇定到原点。
- **采样时间**：$T_s=0.0075$s。

### 数据收集
- 训练数据：$K=10000$个状态-输入样本，均匀采样于约束集。
- 验证/构建数据：各$K'=10000$，$K_c=10000$，独立采样。

### 参数设置
- 提升字典：阶数$\rho=3$的多元多项式乘输入项（公式(27)）。
- 置信度$1-\delta=0.9$，覆盖水平$p=0.9$。
- MPC权重$Q=diag\{1,1\}$，$R=0.1$，预测时域$N=12$。

### 对比方法
- **基线**：Zhang等(2022)的基于**线性Koopman实现的管式MPC**（文献[8]），使用相同观测器$z$，但MPC求解器用MOSEK。
- **自我对比**：本文提出的双线性Koopman+RCCM TMPC。

### 关键结果
- 图2：真实状态、名义状态和管的演化，验证了约束满足和镇定。
- 图3：与基线对比，本文方法管半径更灵活，早期输入使用接近上限，收敛更快。在$k=150$时，实际误差$1.07\times10^{-7}$，远小于理论界$\delta_f/\sqrt{\alpha_1}=0.540$。

## 4. 资源与算力
- **算力说明**：文中仅提到“在MATLAB R2025b on macOS Tahoe 26.3 with Apple M4 Max MacBook Pro (16‑core, 128 GB memory)”上运行。**未提及GPU型号、数量或训练时长**。MPC问题使用CasADi建模、IPOPT求解。基线使用MOSEK求解。因此，无法量化具体GPU算力消耗。

## 5. 实验数量与充分性

### 实验数量
- **单系统（倒立摆）**，一种初始状态。
- **两组仿真**：一组展示本方法轨迹（图2），一组对比基线（图3）。
- **无消融实验**（如不同观测器阶数、不同N、不同误差集估计方法等）。
- **无多随机种子或统计性重复**。

### 充分性与客观性
- **优点**：实验设计清晰，对比了公认的基线方法，证明了理论界与实际性能的差距。
- **不足**：实验规模较小，仅一个系统、一个初始条件，缺乏对模型误差大小、不同约束、不同目标状态的泛化验证。同时，未与其他非线性数据驱动RMPC（如基于神经网络、高斯过程的）对比。未包含实际硬件实验。
- 结论中提及未来需减少保守性，说明作者认识到了局限性。

## 6. 主要结论与发现

- 本文提出的**双线性Koopman+RCCM管式MPC**框架能够有效控制未知非线性系统，同时满足**鲁棒约束满足**、**递归可行性**和**收敛到目标邻域**。
- 理论证明的核心在于：通过**重投影**解决了多步预测离开流形的问题，使双线性Koopman模型的误差能被严格纳入鲁棒框架。
- 数值实验表明，该方法相比线性Koopman TMPC收敛更快、控制输入利用率更高，且实际轨迹误差远小于理论保守上界。
- 终端约束设计（Proposition 3）实现了名义轨迹收敛，从而保证闭环系统收敛到目标邻域。

## 7. 优点

- **理论完备性**：严格证明了约束满足、递归可行性和收敛性，且不依赖难以保证的Koopman字典不变性假设。
- **方法新颖性**：首次将双线性Koopman实现与基于RCCM的管式MPC结合，并设计了重投影机制处理流形离开问题。
- **误差处理合理**：使用Hoeffding不等式提供概率性误差集，并通过构建和验证两步法保证覆盖，避免了过于保守的确定性上界。
- **控制精度高**：数值实验显示实际收敛误差远小于理论上界，表明方法在实践中不保守。
- **计算可处理**：通过RCCM和管式MPC避免了直接min-max优化，求解效率较高。

## 8. 不足与局限

- **实验充分性不足**：仅在一个倒立摆仿真上测试，缺乏多系统、多初始状态、不同约束条件、不同噪声水平的实验。消融实验缺失（如观测器阶数、预测时域、误差界参数的影响）。
- **误差集估计概率性**：基于Hoeffding不等式的误差集只能保证概率性覆盖，且验证数据需独立同分布，不适用于沿闭环轨迹的在线验证。Remark 4作者已承认该局限。
- **稳态误差由保守界主导**：理论提供的终极界$\delta_f/\sqrt{\alpha_1}=0.540$较为宽松，虽实际性能远好，但理论保守性可能限制某些高精度应用。
- **未考虑执行器噪声、传感器噪声**：模型误差集不包含这些额外不确定性。
- **计算要求未量化**：未报告单步MPC求解时间，也未与全阶非线性MPC或其它数据驱动方法（如神经网络MPC）进行计算量对比。
- **离线设计复杂**：RCCM的求解需要求解多项式LMI或SOS优化，对高维系统可能计算负担大（作者提到可用SOS公式，但未展示高维扩展性）。
- **仅适用于控制仿射系统**：方法依赖控制仿射形式，不覆盖一般非线性系统。

（完）
