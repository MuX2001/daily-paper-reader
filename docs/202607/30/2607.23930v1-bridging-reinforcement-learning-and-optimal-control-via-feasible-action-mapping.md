---
title: Bridging Reinforcement Learning and Optimal Control via Feasible Action Mapping
title_zh: 通过可行动作映射桥接强化学习与最优控制
authors: "Stefan Richter, Alberto Giammarino, Guillem Torrente, Sam Blakeman, Peter Dürr"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23930v1"
tags: ["query:control"]
score: 9.0
evidence: 通过可行动作映射桥接强化学习与最优控制
tldr: 针对约束动态系统的控制难题，提出FAOC框架，通过优化映射将RL动作转化为OCP可行参数，严格保证约束满足。FAOC无需专家设计抽象动作空间，结合RL灵活性与OC安全性。在机器人乒乓球运动规划中，FAOC在样本效率和闭环性能上超越现有基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1626, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1621, \"height\": 392, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1314, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1617, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 1814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23930v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1604, \"height\": 619, \"label\": \"Figure\"}]"
motivation: 现有方法难以同时满足约束满足与任务效率，RL缺乏可行性保证，OC缺乏灵活性。
method: FAOC引入可行动作映射算法，将RL抽象动作在线转化为OCP的可行参数集，保证递归可行性。
result: 模拟实验中，FAOC在样本效率和闭环性能上均优于SAC、MPC等基线方法。
conclusion: FAOC有效融合RL与OC，为约束系统控制提供了通用且高效的解决方案。
---

## 摘要
运行受约束的动态系统需要控制器在强制执行递归可行性和安全约束的同时，高效解决复杂任务。为应对这些相互矛盾的要求，我们提出了一种新型控制框架——可行动作最优控制（FAOC），它融合了强化学习（RL）和最优控制（OC）。关键贡献是一种计算高效的、基于优化的映射算法，该算法将RL智能体的动作从静态抽象集合转换为最优控制问题（OCP）的状态依赖可行参数集，从而严格保证动态系统约束的满足。因此，FAOC有效结合了OC的可预测安全性与RL的灵活性。与先前工作相比，RL智能体的抽象动作空间无需专家或启发式设计，且OCP公式不会因RL无法保证可行性而受损。我们将该方法应用于机器人乒乓球的实时运动规划，该场景集中体现了这些挑战。通过仿真实验，我们表明FAOC在样本效率和闭环性能方面均优于最先进的基线方法。

## Abstract
Operating constrained dynamical systems requires controllers to efficiently solve complex tasks while enforcing recursive feasibility and safety constraints. To address these competing requirements, we present Feasible Action for Optimal Control (FAOC), a novel control framework integrating Reinforcement Learning (RL) and Optimal Control (OC). The key contribution is a computationally efficient, optimization-based mapping algorithm that transforms the RL agent's action from a static abstract set into a state-dependent feasible parameter set of the Optimal Control Problem (OCP), guaranteeing strict satisfaction of the dynamical system's constraints. Thus, FAOC effectively combines the predictable safety of OC with the flexibility of RL. In contrast to prior work, the abstract action space of the RL agent does not require expert or heuristic design, and the OCP formulation is not compromised by the inability of RL to guarantee feasibility. We apply our approach to real-time motion planning for robot table tennis, which encapsulates these challenges. Via simulated experiments, we show that FAOC outperforms state-of-the-art baselines in both sample efficiency and closed-loop performance.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

约束动态系统的控制面临两难：强化学习（RL）擅长处理复杂、长时域任务，但难以严格保证约束满足；最优控制（OC）能通过硬约束保证安全，但在非凸、稀疏奖励场景下计算量大且易陷入局部最优。现有RL-OC融合方法通常将OCP参数作为RL动作，但要么采用静态启发式动作空间导致冗余或不可行动作，要么依赖软约束牺牲安全性。核心矛盾在于：RL的抽象动作空间是静态的，而OCP的可行参数集是状态依赖、几何复杂的。论文旨在设计一种通用映射机制，将RL动作从静态凸集精确、可逆地映射到OCP的可行参数集，同时保证OCP始终可行，从而融合RL的灵活性与OC的安全性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：通过一个计算高效的优化映射算法，将RL智能体的输出（来自静态凸抽象集）转换为OCP的可行参数，确保所有映射后的参数都满足约束，且映射是双射的，避免动作别名。

**关键技术细节**：

- **映射框架（Algorithm 1）**：
  - 输入：基点集合 \(X\)（RL抽象动作集）和目标集合 \(Y\)（状态依赖的OCP可行参数集），均为紧、凸、有内点的集合；内点 \(x_c \in \text{int} X\) 和 \(y_c \in \text{int} Y\)；待映射点 \(x \in X\)；方向变换函数 \(\phi\)。
  - 步骤：若 \(x = x_c\)，则 \(y = y_c\)；否则计算从 \(x_c\) 出发经过 \(x\) 的射线与 \(X\) 边界的缩放因子 \(\alpha\)；对方向向量应用变换 \(\phi\) 得 \(d'\)；计算从 \(y_c\) 沿 \(d'\) 方向与 \(Y\) 边界的缩放因子 \(\beta\)；映射结果为 \(y = y_c + (\beta/\alpha) d'\)。
  - 特性：连续、双射（要求 \(\phi\) 连续、可逆、正齐次）。

- **方向变换消除几何失真**：
  - 二维面积匹配变换（Area-Matching）：利用归一化面积函数 \(a_X(\xi)\) 和 \(a_Y(\xi)\)，将基集合的极角映射为目标集合的极角，使均匀采样点的角分布与目标集合面积一致，严格缓解点累积。
  - 高维线性变换：用最大体积内接椭球（MVIE）构造仿射替代集合，得到线性变换矩阵 \(\Phi = Q_{eY} Q_{eX}^{-1}\)，近似保持分布密度。

- **隐式定义集合的映射计算**（实时关键）：
  - **内点计算**：通过求解最大内接球或提升法（Proposition 6, 7）得到鲁棒内点，避免显式几何表示。
  - **形状矩阵计算**：对OCP的高维决策变量集合计算离线椭球替代，利用SVD投影到参数空间，得到状态无关的线性变换矩阵（Example 3）。
  - **边界缩放因子计算**：通过提升到高维空间求解凸优化问题（8）获得，避免投影操作。

- **适用范围**：要求RL动作空间和OCP可行参数集均为紧、凸、有内点；OCP需满足Lemma 1的条件（包括凸性、无衰退方向等）。对线性MPC，若终端约束矩阵满秩，则可行参数集满足这些性质（Corollary 1）。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

**场景**：8-DoF移动机械臂的机器人乒乓球运动规划。高层RL策略决定拦截策略（目标位置/速度），低层OCP生成满足位置、速度、加速度、加加速度约束的轨迹。RL动作是16维目标姿态（每关节2维），OCP基于线性三积分器模型，终端约束为位置-速度目标。

**数据集**：合成轨迹与真实人类击球数据混合（训练用的 incoming shots）。每个 episode 随机选取一种技能（落点、上旋/下旋、扣杀），只有回合结束时给予稀疏奖励。

**Benchmark与对比方法**：
- **FAOC**（ours）：使用所提映射算法，终端硬约束。
- **1Dpos / 1Dvel / 1Dacc**：基于Kiemel & Kröger [30]的一维映射，RL仅指定位置/速度/加速度之一，其余由优化器决定。
- **2Dsoft**：静态二维动作空间（位置+速度），OCP用软终端代价（目标跟踪权重大小及正则化权重需手动调节），无可行性保证。

此外，实验在两种RL算法/架构下进行：
- **基线实验**：SAC + BRONets
- **SimBaV2实验**：分布型批评者 + 更大网络
- **低频率实验**：RL决策频率从31.25Hz降至7.8125Hz（段长128ms vs 32ms）

**评估指标**：返回率（Fraction Balls Returned）、生成上旋（Generated Topspin）、速度（Generated Velocity）、后旋（Generated Backspin）、目标距离（Goal Distance）。所有指标仅对成功返回的回合计算。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU型号、数量、训练时长等）。若未明确说明，也请指出这一点。

论文正文及元数据中**未明确说明具体使用的GPU型号、数量以及训练时长**。可以推断训练涉及多GPU并行（因提到分布式训练 pipeline 和团队贡献），但无量化细节。因此无法提供准确算力信息。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。

**实验数量**：
- 三大实验组：基线（SAC+BRONets）、算法与架构影响（SimBaV2）、低RL频率影响（SimBaV2 + 7.8125Hz）。
- 每组比较5种方法（FAOC, 2Dsoft, 1Dpos, 1Dvel, 1Dacc）。
- 每个方法训练5个不同随机种子的智能体。
- 每个评价点由1000个episode的统计得出。
- 统计显著性检验使用Mann-Whitney U检验（p<0.05）。

**充分性与公平性**：
- 多随机种子及统计检验增强了结论可靠性。
- 对比了不同维度的动作空间和不同软/硬约束策略，覆盖了现有主流方法。
- 在更强RL算法（SimBaV2）下重复实验，验证了方法的鲁棒性。
- 低频率实验检验了方法在更苛刻控制条件下的表现。
- 不足之处：所有实验均在仿真中进行（作者声明无法进行大量真人对比）；1Dacc方法因三个种子返回率低于50%被排除分析，导致部分对比不完整。

总体而言，实验数量充足、设计较为公平客观。

### 6. 论文的主要结论与发现

- FAOC在三个实验设置中均取得**最高样本效率和最终性能**，尤其在返回率、生成上旋、速度方面显著优于所有对比方法。
- 二维动作空间（FAOC和2Dsoft）明显优于一维变体（1Dpos/vel/acc），说明更多自由度可表达更丰富行为。
- 相比2Dsoft，FAOC消除了不可行动作的探索负担，因此在样本效率和最终性能上更优。
- 当RL决策频率降低时，FAOC性能下降最小，而其他方法显著退化，表明FAOC结合二维动作与可行性意识带来更好的轨迹段可控性。
- 更强的RL算法（SimBaV2）可普遍提升各方法，但FAOC仍保持领先优势。

### 7. 优点

- **严格可行性保证**：映射生成的所有OCP参数均保证OCP可行，避免软约束或罚函数带来的不安全风险。
- **无需专家或启发式设计动作空间**：抽象动作空间是简单静态凸集（如 \([-1,1]^n\)），无需人工调节范围。
- **可逆双射**：支持动作别名消除、数据增强、专家示范利用。
- **状态无关的映射计算**：通过离线预计算和高维提升，在线仅需求解轻量QP，适用于实时控制（如31.25Hz）。
- **几何失真缓解**：二维面积匹配和高维线性变换使RL采样更均匀地覆盖可行参数区域，加速学习。
- **实验验证充分**：在多个RL算法、架构和控制频率下验证，统计显著性检验支持结论。
- **开源实现**：映射算法和OCP代码已公开，便于复现和应用。

### 8. 不足与局限

- **凸性假设**：理论保证要求参数集是凸的，虽在非凸场景（如包含障碍物）仍可有效（RL处理非凸，OCP处理局部凸子集），但非严格覆盖所有非线性OCP。
- **实验场景单一**：仅在机器人乒乓球仿真中验证，未在其他领域（如自动驾驶、无人机）测试，泛化性未充分证明。
- **缺乏真实机器人对比**：因实际运动员稀缺，无法在真实系统上对比所有方法（只有FAOC在真实比赛测试，其余仅在仿真）。可能存在仿真与真实的差距。
- **隐式集合计算依赖离线分析**：如MCIS和椭球替代需离线计算，对变化的环境需重新计算，可能限制部署。
- **计算扩展性**：线性变换方法在高维（如>16维）可能需更多离线预计算；在线求解QP的耗时随维度增加，文中未给出详细时间分析。
- **未说明资源和超参数敏感性**：未披露训练GPU型号、时长；2Dsoft的权重 \(W_t\) 和 \(w_s\) 需要调参，而FAOC无需，但未讨论其他超参数的鲁棒性。

（完）
