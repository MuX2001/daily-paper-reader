---
title: Model-Free Q-Learning for Infinite-Horizon Stochastic Linear Quadratic Problems with Regime Switching
title_zh: 无限时域含机制切换随机线性二次型问题的无模型Q学习
authors: "Xinyue Zhang, Na Li, Xun Li, Zuo Quan Xu"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26456v1"
tags: ["query:control"]
score: 9.0
evidence: 带机制切换的随机线性二次型最优控制的无模型Q学习
tldr: 本文研究无限时域连续时间随机线性二次最优控制问题，系统存在体制切换。提出基于自适应动态规划的无模型Q-learning方法，包括在线和离线策略两种架构，仅从状态轨迹数据学习最优控制器。理论上证明了两种架构的等价性，并给出闭环系统稳定性与算法收敛性的严格分析。数值案例表明该方法有效且可行，为复杂随机系统控制提供无模型解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26456v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1655, \"height\": 843, \"label\": \"Figure\"}]"
motivation: 现有控制方法依赖精确系统模型，但实际中模型参数未知或时变，亟需仅依赖在线数据的无模型自适应控制方案。
method: 提出on-policy和off-policy Q-learning算法，利用向量化与Kronecker积实现，仅从状态轨迹数据在线学习最优控制器。
result: 严格证明了两种算法架构等价，并建立了闭环系统稳定性与算法收敛到最优解的理论保证。
conclusion: 所提无模型Q-learning方法无需系统模型知识，通过在线数据即可实现最优控制，数值验证了其有效性和实际可行性。
---

## 摘要
本文研究含机制切换的无限时域连续时间随机线性二次型最优控制问题。我们通过采用自适应动态规划方法，提出了从基于模型设计到无模型设计的范式转变，具体开发了基于策略和离策略的Q学习算法，这些算法仅从在线状态轨迹数据中学习最优控制器。我们工作的理论核心包括对基于策略和离策略架构等价性的完整证明，以及对闭环系统稳定性和算法收敛到最优解的严格分析。为了计算可行性，我们使用向量化和Kronecker积代数实现了这些算法。数值案例研究验证了理论结果，清楚地展示了所提出的无模型控制策略的操作有效性和实际可行性。

## Abstract
This paper addresses infinite-horizon continuous-time stochastic linear quadratic optimal control problems with regime switching. We propose a paradigm shift from model-based design by adopting an adaptive dynamic programming approach, specifically developing on-policy and off-policy Q-learning algorithms that learn the optimal controller solely from online state trajectory data. The theoretical core of our work consists of a complete proof of the equivalence between the on- and off-policy architectures, alongside a rigorous analysis establishing the stability of the closed-loop system and the convergence of the algorithms to the optimal solution. For computational tractability, we implement these algorithms using vectorization and Kronecker product algebra. The theoretical results are corroborated by numerical case studies that clearly demonstrate the operational effectiveness and practical feasibility of the proposed model-free control strategy.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：研究无限时域连续时间随机线性二次型（LQ）最优控制问题，其中系统的动态和成本权重均受到一个有限状态的 Markov 链（机制切换）影响。传统方法依赖于求解耦合代数 Riccati 方程（CAREs），这需要精确知道系统的所有参数矩阵（A_k, B_k, C_k, D_k 等）。但在实际环境中，系统模型可能未知或费用高昂难以获得，因此需要无模型的控制设计方法。

- **研究动机**：现有的基于模型的方法（如通过 Lyapunov 迭代或加速算法求解 CAREs）在模型未知时失效。强化学习（特别是 Q-learning）在离散时间和连续时间确定性/随机 LQ 问题中已有成功应用，但尚未系统应用于含机制切换的随机 LQ 问题。本文旨在填补这一空白，提出仅依赖在线状态轨迹数据的无模型 Q-learning 算法，并给出严格的理论保证。

- **整体含义**：本文提供了一种从数据驱动的角度解决复杂随机最优控制问题的新范式，不需要预先知道系统的任何参数，仅通过在线交互就能收敛到最优控制器，具有重要的理论和实际应用价值。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想

- 基于自适应动态规划（ADP），将传统的 Lyapunov 迭代方案（需要系统模型）转化为可通过数据驱动的 Q-learning 实现的形式。
- 定义 **Q 函数** 为状态-动作对（x, u）的二次型，其参数矩阵 Q_k 包含了系统动态和成本信息。通过在线收集的状态轨迹数据，使用最小二乘法更新 Q_k 的参数，进而更新控制增益。
- 提出两种算法：**on-policy Q-learning**（每次迭代使用当前策略生成新轨迹）和 **off-policy Q-learning**（使用一组固定的离线轨迹数据反复更新）。

### 关键技术细节

1. **Lyapunov 迭代方案（模型已知）**：
   - 初始：给定一个稳定化增益 (K_1^(0), ..., K_K^(0))。
   - 策略评估：解耦合 Lyapunov 方程 (10) 得到 P_k^(i+1)。
   - 策略改进：K_k^(i+1) = -(R_k + D_k^T P_k^(i+1) D_k)^{-1}(B_k^T P_k^(i+1) + D_k^T P_k^(i+1) C_k + S_k)。
   - 该方案需要系统矩阵，且 P_k 之间耦合。

2. **On-policy Q-learning (Algorithm 1)**：
   - 定义 Q_k(x, u) = [x^T, u^T] Q_k [x^T; u^T]。
   - 利用 Q 函数的 Bellman 型恒等式 (29)，通过采集当前策略下的状态轨迹 X^(i)(s)，使用最小二乘法求解 Q_k^(i+1) 的参数（需要引入持续激励 ω 以保证矩阵满秩）。
   - 更新增益 Γ(Q_k^(i+1)) = -(Q_kuu^(i+1))^{-1} Q_kux^(i+1)。
   - 无需系统矩阵，仅需状态和控制的轨迹数据。

3. **Off-policy Q-learning (Algorithm 2)**：
   - 使用一批预先收集的轨迹数据（X(s), u(s)），在每次迭代中借助 Itô 公式推导出关于 Q_k^(i+1) 的线性方程 (41)。
   - 方程中包含未来状态和控制的积分项，通过离散采样近似。
   - 同样只需最小二乘求解 Q_k 参数，无需每次迭代重新生成数据，提高了数据利用效率。

4. **等价性证明**：严格证明了 on-policy 和 off-policy Q-learning 均等价于 Lyapunov 迭代方案，从而继承了后者的稳定性和收敛性。

5. **理论分析**：
   - 证明了每次迭代得到的控制增益都是稳定化的（Theorem 3.1）。
   - 证明了序列 {P_k^(i)} 单调递减且有下界，收敛到 CARE 的解（Theorem 3.2）。
   - 进而得到 Q-learning 算法的收敛性（Theorem 3.4 和 3.5）。

## 3. 实验设计：数据集 / 场景、baseline、对比方法

- **场景**：一个二维状态（n=2）、一维控制（r=1）、两个机制（K=2）的线性随机系统。系统参数包括 A_k, B_k, C_k, D_k（扩散项依赖于状态和控制），以及生成矩阵 Π。成本权重 N_k, S_k, R_k 满足正定性条件。
- **数据集**：没有使用标准公开数据集。实验通过数值模拟生成数据：随机选择初始状态（H=200 个初始点），每个点运行多个样本路径（N=50），每个路径在长时间区间上离散采样（L 步）。
- **Baseline / 对比方法**：论文未显式对比其他方法。主要对比了：
  - 初始稳定化控制器与学得的最优控制器产生的状态轨迹（图 1b, 1c, 1f）。
  - 检查收敛后的 Q_k 是否满足 CARE 残差（||R(P_k^*)|| ≈ 10^{-2} 量级）。
- **实验设置**：给出了两种算法的实现细节，包括初始化、持续激励信号（Gaussian white noise, N(0,8)）、迭代终止条件（||Q_k^(i+1)-Q_k^(i)|| < ε）。

## 4. 资源与算力

- **论文未提及任何 GPU 型号、数量或训练时长**。实验仅使用 MATLAB 或类似软件在常规 CPU 上运行数值模拟即可完成，因为问题规模小。
- 可以指出：这是一种小规模验证，未讨论大规模系统下的计算资源需求。

## 5. 实验数量与充分性

- **实验数量**：仅展示了一个数值示例，包含两个算法各自的结果（收敛曲线、状态轨迹对比）。没有开展多组不同系统参数、不同维度、不同噪声水平的消融实验或敏感性分析。
- **充分性**：实验验证了算法能够收敛到 CARE 且残差较小，证明了方法的可行性。但不够充分：未考虑系统维数增大、机制个数增多、采样误差对收敛的影响、持续激励参数的选择敏感性等。另外，没有和模型已知的 Lyapunov 迭代结果做定量对比（如计算误差随迭代次数的变化）。
- **客观性与公平性**：实验设计合理（使用随机初始状态和多个样本路径），但仅一个示例，不足以全面评估算法的鲁棒性和实际适用性。

## 6. 论文的主要结论与发现

1. **等价性**：On-policy 和 off-policy Q-learning 均等价于 Lyapunov 迭代方案。
2. **稳定性**：只要初始增益是稳定化的，所有后续迭代增益均为稳定化的。
3. **收敛性**：序列 {Q_k^(i)} 和 {Γ(Q_k^(i))} 分别收敛到最优 Q 函数和最优控制增益。
4. **无模型优势**：算法仅需在线状态轨迹数据，不需要系统动态的任何信息。
5. **数值验证**：通过一个两状态、两机制的示例展示了算法的有效性和可行性，学得的控制能更快地使状态趋于零。

## 7. 优点

- **理论严谨**：完整证明了算法的等价性、稳定性和收敛性，这在含机制切换的连续时间随机 LQ 问题中是首次。
- **无模型**：完全摆脱对系统参数的依赖，具有很高的实用价值。
- **两种策略**：提供了 on-policy 和 off-policy 两种版本，off-policy 可重复利用历史数据，提高数据效率。
- **计算实现**：利用向量化和 Kronecker 积将 Q-learning 方程转化为最小二乘问题，便于数值实现。
- **解决耦合**：通过利用无穷时域上的稳定性性质，巧妙避免了未来时刻 Markov 链状态未知带来的耦合困难（无需在线预测）。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了一个小规模的数值示例（2维状态，2个机制），没有进行更高维度、更多机制或与现有模型已知算法的对比实验。
- **未考虑噪声与鲁棒性**：实际中测量噪声、模型误差等可能影响算法性能，论文未讨论。
- **持续激励要求**：on-policy 算法需要注入足够强度的探索噪声以保证数据矩阵列满秩，这在实际系统中可能需要谨慎设计；off-policy 算法也需满足矩阵满秩假设（Assumption 3.2），缺乏针对这些假设不满足时的理论分析。
- **计算复杂度**：随着状态维数和机制数增加，最小二乘问题的变量数快速增长（约 O(K * n^2 + K * n * r + K * r^2)），但论文未分析计算效率或给出高维示例。
- **收敛速度**：没有提供收敛速度的估计或实验收敛所需迭代次数。
- **开放性**：未来工作可扩展至非线性系统、多玩家博弈等，但论文未给出任何扩展分析或预期。

（完）
