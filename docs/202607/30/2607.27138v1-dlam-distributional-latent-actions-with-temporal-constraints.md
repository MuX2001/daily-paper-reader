---
title: "DLAM: Distributional Latent Actions with Temporal Constraints"
title_zh: "DLAM: 具有时间约束的分布化潜在动作"
authors: "Zuojin Tang, Feifan Luo, Haoyun Liu, Botai Yuan, Dekang Qi, Ronghan Chen, Yandan Yang, Tong Lin, Xinyuan Chang, Mu Xu, Bin Liu, De Ma, Zhiheng Ma"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27138v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向视觉语言动作模型的分布性潜在动作模型
tldr: 针对VLA模型缺乏动作标注数据的问题，从无动作视频中提取先验的潜在动作模型因重构训练导致时间一致性不足，且现有结构化方法中确定性转移的残差在递归中累积。提出DLAM，将每个转移建模为对角高斯分布，通过参考帧重构和等距三元组的归一化组合与反转约束均值和方差，并用轻量相关系数处理相邻转移依赖。该方法在保留转移上实现更一致的时间动态和更强重构，在MetaWorld等下游任务中提升策略性能，其中归一化均值约束是重构增益主因。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27138v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27138v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1666, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27138v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27138v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 828, \"height\": 662, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27138v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1802, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27138v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1632, \"height\": 375, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27138v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27138v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 647, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27138v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1717, \"height\": 326, \"label\": \"Table\"}]"
motivation: 现有潜在动作模型缺乏联合生成所需的结构，且确定性转移导致残差递归放大，影响时间一致性。
method: DLAM将每帧转移建模为对角高斯，基于参考帧重构，并通过归一化组合与反转约束均值和方差，用共享相关系数处理相邻依赖。
result: 在保留转移重构任务中，DLAM比基线获得更一致的时间动态和更强累积重建；在MetaWorld、LIBERO等任务中提升策略成功率。
conclusion: 分布化转移建模结合归一化约束显著增强时间一致性，为VLA策略提供有效动作先验，方差与相关系数进一步优化控制性能。
---

## 摘要
视觉-语言-动作（VLA）模型仍然受到稀缺的动作标记机器人数据的限制，而无动作视频提供了丰富的物理变化观察。潜在动作模型可以提取此类先验，但重建训练的编码可能在预测未来观测时缺乏与机器人动作联合生成所需的结构。现有的结构化方法添加了时间约束，但保留了确定性转换点，因此局部推断的转换中的残差误差可能在递归组合下传播并累积。我们引入了DLAM，一种分布化潜在动作模型，将每个转换表示为对角高斯分布。基于参考帧的重建将均值锚定在观察到的视觉变化上，而等间隔三元组上的归一化组合和反转则约束了均值和逐维方差。方差组合使用轻量级共享相关系数来解释共享中间帧的相邻转换之间的依赖性，而反转则取反均值并保持方差。对于下游策略学习，我们冻结编码器并训练一个流匹配策略来联合生成均值转换序列和机器人动作。在保留的转换上，DLAM学习到比现有潜在动作基线更时间一致的潜在动态，并在保留的视频上实现更强的直接和累积重建。在相同的受控π₀迁移协议下，它还在MetaWorld MT50、LIBERO和真实世界操作任务上提高了策略性能。受控消融实验表明，归一化均值约束贡献了重建增益的大部分，而学习到的方差和相关性感知组合在下游控制中提供了互补的改进。

## Abstract
Vision-language-action (VLA) models remain constrained by scarce action-labeled robot data, whereas action-free videos offer abundant observations of physical change. Latent action models can extract such priors, but reconstruction-trained codes may predict future observations without the structure required for joint generation with robot actions. Existing structured methods add temporal constraints but retain deterministic transition points, so residual errors in locally inferred transitions may propagate and compound under recursive composition. We introduce DLAM, a distributional latent-action model that represents each transition as a diagonal Gaussian. Reconstruction conditioned on the reference frame grounds the mean in observed visual change, while normalized composition and reversal over equal-gap triplets constrain both the mean and dimension-wise variance. Variance composition uses a lightweight shared-correlation coefficient to account for dependence between adjacent transitions that share an intermediate frame, whereas reversal negates the mean and preserves the variance. For downstream policy learning, we freeze the encoder and train a flow-matching policy to jointly generate mean transition sequences and robot actions. On held-out transitions, DLAM learns more temporally consistent latent dynamics than existing latent-action baselines and achieves stronger direct and cumulative reconstruction on held-out videos. Under the same controlled $π_0$ transfer protocol, it also improves policy performance on MetaWorld MT50, LIBERO, and real-world manipulation tasks. Controlled ablations show that normalized mean constraints account for most of the reconstruction gain, while learned variance and correlation-aware composition provide complementary improvements in downstream control.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **瓶颈**：视觉-语言-动作（VLA）模型性能受限于**动作标注数据稀缺**，而无动作视频（action-free videos）提供了丰富的物理变化观测。
- **现有方法局限**：已有的潜在动作模型（LAM）仅通过重建学习转移表征，缺乏**与机器人动作联合生成**所需的结构；结构化方法（如ALAM）虽添加了时间约束（组合、反转），但**仍使用确定性点估计**，导致局部残差在递归组合中累积放大。
- **目标**：提出一种**分布化潜在动作模型**，将每帧转移建模为对角高斯分布，使时间约束同时监督均值和方差，提升时间一致性与下游控制性能。

### 2. 方法论：核心思想与技术细节
- **核心思想**：将两个观测帧之间的转移表示为**对角高斯分布**（均值和逐维方差），基于参考帧的重构将均值锚定在视觉变化中；对等间隔三元组施加**归一化组合**和**反转**约束，同时监督均值和方差；方差组合时引入**轻量共享相关系数**处理相邻转移的依赖。
- **关键技术细节**：
  - **编码器**：输入帧对 (O_i, O_j)，输出 K 个 token 的均值 μ_{ji} 和 log-方差 ℓ_{ji}，经裁剪与指数化得标准差 σ_{ji}。
  - **重构**：仅用均值，解码器 D_ω(O_i, μ_{ji}) 重建目标帧，损失 L_rec 为像素级 MSE。
  - **先验正则**：KL 散度将后验推向标准高斯。
  - **归一化组合**：对三元组 (a,b,c)，组合后验 q̃_{ca} 的均值与方差定义为  
    μ̃_{ca} = (μ_{ba} + μ_{cb})/√2,  
    (σ̃_{ca})² = [(σ_{ba})² + (σ_{cb})²]/2 + ρ·σ_{ba}⊙σ_{cb}，  
    其中 ρ = ρ_max·tanh(r) 为共享相关系数（|ρ|<1）。
  - **反转**：R(μ,σ) = (−μ, σ)，即均值取反、方差保持不变。
  - **损失**：L_comp = D(q̃_{ca}, q_{ca})（直接与组合后验的均值和 log-方差差异），L_rev = D(q_{ba}, R[q_{ab}])，总损失 LDLAM = Σ λ_s L_s。
- **下游策略迁移**：冻结编码器，丢弃方差，仅用后验均值序列作为流匹配辅助生成目标，与机器人动作联合训练，不改变π₀骨干。

### 3. 实验设计
- **数据集与场景**：
  - **无动作预训练**：11 个机器人视频数据集（Open X-Embodiment、CALVIN 等），采样权重按图 4 配置。
  - **评测基准**：
    - **MetaWorld MT50**：50 个操作任务，分 Easy/Medium/Hard/Very Hard 四档。
    - **LIBERO**：Spatial、Object、Goal、Long 四个子集。
    - **真实世界**：Piper 6-DoF 臂上的 4 个任务（插入圆柱、插入立方、插花、挂杯）。
  - **对比方法**：
    - **预训练基线**：重构-only LAM、确定性结构化 LAM（ALAM）。
    - **下游策略基线**：RT-2、RoboTron Mani、π₀、π₀.5、OpenVLA、LAPA、UniVLA、JALA 等 20+ 方法。
- **评估指标**：
  - 转移表征：组成残差 R_comp、反转残差 R_rev（尺度归一化）、重建质量 PSNR/SSIM/LPIPS（直接 & 累积）。
  - 下游策略：任务成功率（%）。

### 4. 资源与算力
- **预训练**：64 块 AMD MI308X GPU，训练 57 个 epoch，优化器 AdamW（lr=1e-4，weight decay=1e-4），每设备 batch size=64。
- **策略迁移**：每设备 batch size=32，lr=5e-5，weight decay=1e-4，使用 π₀ 策略（PaliGemma-2B + Gemma-300M）。
- **未明确说明总耗时或能耗**，但算力规模显著。

### 5. 实验数量与充分性
- **实验数量**：广泛覆盖 3 个基准（MetaWorld 50 任务、LIBERO 4 套件、4 个真实场景），对比 20+ 方法，并包含 4 种配置的消融实验。
- **充分性与公平性**：
  - 预训练阶段控制相同数据、架构、训练预算。
  - 迁移阶段使用相同的 π₀ 骨干，仅替换冻结的编码器。
  - 消融实验逐步引入归一化均值约束、学习方差、相关系数，清晰隔离贡献。
  - 实验设计客观、公平，报告了统计显著性（95% CI）和每项任务的详细结果。

### 6. 主要结论与发现
- DLAM 在保留转移上**时间一致性更强**（组成/反转残差更低，且对递归长度不敏感）。
- **直接重建 PSNR 提升 3.45 dB**，**累积重建提升 1.17 dB**，LPIPS 降低 26%~46%。
- 下游策略在 MetaWorld MT50 平均成功率 **87.6%**（比 ALAM 高 2.6 pp），LIBERO **99.0%**（比 ALAM 高 0.9 pp），真实世界平均 **73.8%**（比 ALAM 高 10 pp）。
- 消融表明：**归一化均值约束**贡献大部分重建增益（PSNR +0.87 dB），**学习方差**与**相关系数**提供互补的下游控制增益（成功率分别 +3.2 pp 和 +2.3 pp）。

### 7. 优点
1. **分布化转移建模**：首次将时间约束扩展到高斯后验，方差提供额外训练信号，且下游仅用均值避免不确定性传播。
2. **局部约束**：基于等间隔三元组的组合 / 反转，不假设全局群结构，易于扩展。
3. **轻量相关系数**：共享单一标量 ρ 捕捉相邻转移依赖，计算代价极小。
4. **即插即用**：冻结编码器输出均值序列，可直接接入任何 VLA 策略（如 π₀），无需修改骨干或执行接口。
5. **实验全面**：覆盖模拟 & 真实场景、多种难度、大量基线，消融清晰。

### 8. 不足与局限
1. **长期泛化开放**：约束仅限等间隔三元组（k+k→2k），更长视野（>2k）的可靠性未保证。
2. **方差可能退化为常数**：由于下游不使用方差，仅作为辅助信号，模型可能学习到近似常值方差以规避损失。
3. **共享相关系数欠精细**：单标量 ρ 无法刻画维度或上下文相关的依赖关系。
4. **预训练数据偏差**：主要来自 Open X-Embodiment 和 CALVIN，未涵盖多样化机器人形态，可能影响跨形态泛化。
5. **计算资源需求高**：64 块 MI308X 训练 57 epochs，对一般团队门槛较高。

（完）
