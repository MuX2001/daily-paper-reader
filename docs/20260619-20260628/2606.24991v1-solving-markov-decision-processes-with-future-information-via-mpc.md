---
title: Solving Markov Decision Processes with Future Information via MPC
title_zh: 通过MPC求解带有未来信息的马尔可夫决策过程
authors: "Shambhuraj Sawant, Akhil S Anand, Dirk Reinhardt, Sebastien Gros"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24991v1"
tags: ["query:control"]
score: 9.0
evidence: 利用MPC求解带未来信息的马尔可夫决策过程
tldr: MPC广泛应用于工业与机器人系统，但传统MPC无法保证MDP最优性，而实际MDP常包含未来信息需增广状态。现有MPC-RL方法未考虑这一结构。本文建立了参数化MPC精确表示含未来信息MDP最优值函数与策略的结构条件，并采用RL学习参数，在点质量赛车任务中验证了有效性。该工作为融合未来信息的MPC-RL提供了理论基础与实现框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24991v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 896, \"height\": 507, \"label\": \"Figure\"}]"
motivation: 现有MPC-RL方法未考虑未来信息，无法处理包含预测等增广状态的MDP最优决策问题。
method: 建立参数化MPC能精确表示含未来信息MDP最优值函数与策略的结构条件，并用RL学习其参数。
result: 在点质量赛车任务中验证了该方法能有效利用未来参考信息，实现最优策略。
conclusion: 参数化MPC可作为结构化函数逼近器，为含有未来信息的MDP提供最优策略。
---

## 摘要
模型预测控制（MPC）广泛应用于工业和机器人系统，通过基于有限时域优化的规划来施加约束和嵌入领域知识。然而，尽管有这些优势，MPC方案通常不能为马尔可夫决策过程（MDP）形式的序贯决策问题产生最优策略。最近MPC与强化学习（RL）的结合通过将MPC视为MDP最优策略的参数化模型，并使用数据调整其参数来缓解这一问题。虽然这些方法通常考虑经典的MDP，但许多现实问题在决策时包含未来信息——例如预测、价格或参考轨迹——这些信息必须包含在MDP状态中才能做出最优决策。当前的MPC-RL方法没有直接考虑这种增强状态结构，引发了如何将未来信息纳入MPC以获得最优策略的问题。本文建立了参数化MPC能够精确表示带有未来信息的MDP的最优价值函数和策略的结构性要求。我们进一步证明了这种参数化MPC可以作为结构化函数逼近器，其参数通过RL学习。该方法在具有未来参考信息的点质量赛车任务上进行了说明。

## Abstract
Model Predictive Control (MPC) is widely used in industrial and robotic systems for enforcing constraints and embedding domain knowledge through finite-horizon optimization-based planning. However, despite these strengths, an MPC scheme typically does not yield optimal policies for sequential decision-making problems formulated as Markov Decision Processes (MDPs). Recent combinations of MPC with Reinforcement Learning (RL) alleviate this issue by treating MPC as a parameterized model of the optimal policy of an MDP and adjusting its parameters using data. While these approaches typically consider classical MDPs, many real-world problems include future information--such as forecasts, prices, or reference trajectories--at decision time, which must be included in the MDP state for optimal decision-making. Current MPC-RL approaches do not directly account for this augmented-state structure, raising the question of how to incorporate future information into MPC to obtain an optimal policy. This work establishes the structural requirements under which a parameterized MPC can exactly represent the optimal value functions and policy of an MDP with future information. We further demonstrate that such a parameterized MPC can serve as a structured function approximator, with its parameters learned using RL. The approach is illustrated on a point-mass racing task with future reference information.

---

## 论文详细总结（自动生成）

# 论文结构化总结：通过 MPC 求解带有未来信息的马尔可夫决策过程

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：模型预测控制（MPC）在工业和机器人系统中广泛使用，通过有限时域优化处理约束和嵌入领域知识，但其通常无法为马尔可夫决策过程（MDP）提供最优策略。近年来，MPC 与强化学习（RL）的结合通过将 MPC 视为 MDP 策略的参数化模型并用数据调整参数来缓解这一问题。然而，现有 MPC-RL 方法仅考虑经典 MDP，没有考虑许多现实问题中决策时可获取的**未来信息**（如天气预报、电价、参考轨迹）。这些未来信息必须纳入 MDP 的增广状态才能满足马尔可夫性质，从而做出最优决策。
- **核心问题**：如何构建 MPC 才能使其能够表示带有未来信息的 MDP 的最优价值函数和最优策略？现有 MPC 扩展（如阶段式引入未来信息）缺乏理论保证，且未对齐 MDP 的 Bellman 结构。
- **整体含义**：本文从理论上建立了参数化 MPC 能够精确表示增广状态 MDP 的最优价值函数与策略的条件，并展示了将这种参数化 MPC 作为结构化函数逼近器与 RL 相结合的实际方案，为融合未来信息的 MPC-RL 提供了理论基础与实现框架。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
将 MPC 视为 MDP 的增广状态上的参数化价值函数和策略模型。通过将未来信息作为增广状态的一部分，并在 MPC 预测模型中一致地传播未来信息，使得 MPC 的优化结构能够匹配 MDP 的 Bellman 递归。然后利用 RL（如 PPO）学习 MPC 的参数（成本函数权重、动力学或约束中的可调参数），以优化闭环性能。

### 2.2 关键技术细节

- **增广状态定义**：  
  \(\tilde{s}_t := [s_t, Z_{t,0}^H]^\top\)，其中 \(s_t\) 为物理状态，\(Z_{t,0}^H = [z_{t,0}, \dots, z_{t,H}]\) 为未来相关信息序列（长度为 \(H\)）。增广状态空间 \(\tilde{\mathcal{S}}\)。

- **参数化 MPC 公式**（式 18-20）：  
  在传统 MPC 基础上，将优化目标、动力学、约束均定义在增广状态上。预测模型 \(\tilde{f}\) 分为物理状态传播 \(f_\theta\) 和未来信息更新 \(g_\theta\)（例如简单的序列移动或更复杂的参数化变换）。MPC 输出第 0 步控制作为策略：  
  \(\pi_\theta(\tilde{s}_t) = u_0^\star\)。

- **定理 1（存在性）**：  
  存在参数 \(\theta^\star\) 使得该参数化 MPC 能够精确表示最优状态价值函数 \(V^*(\tilde{s})\)、动作价值函数 \(Q^*(\tilde{s}, a)\) 以及最优策略 \(\pi^*(\tilde{s})\)。证明通过构造合适的阶段成本 \(L_\theta\) 和终端成本 \(T_\theta\)（形如 \(Q^* - \gamma V^*\) 和 \(V^*\)）并使 Bellman 递归在预测轨迹上展开为 telescoping sum 得到。该定理成立的关键前提是 MPC 操作在**增广状态**上，并且预测模型一致地传播未来信息。

- **与 RL 结合**：  
  利用 MPC 灵敏度分析（通过 Lagrangian 和 KKT 条件）计算价值函数和策略关于参数 \(\theta\) 的梯度，从而将参数化 MPC 嵌入标准 RL 更新中。本文具体采用 PPO：MPC 输出作为高斯分布均值构造随机策略，Critic 为两层神经网络（128 单元），训练共计 1×10⁴ 时间步。

## 3. 实验设计

- **场景**：点质量赛车任务（Point-Mass Racing）。物理状态为 \(s = [p_x, p_y, v_x, v_y]^\top\)，控制为力 \(a = [F_x, F_y]^\top\)。动力学为带高斯噪声的离散双积分器。未来信息是未来 \(M=60\) 步的赛道中心线参考位置序列，实验中未来信息长度 \(H=50\)（即增广状态为 \([s_t, Z_{t,0}^{50}]\)）。阶段成本为负的沿赛道前进进度，超出赛道宽度（0.4）终止，折扣因子 \(\gamma=0.99\)。
- **基准方法（Benchmark）**：  
  - (1) **Nominal MPC**：使用朴素公式（式 11）且成本权重固定的基线 MPC。  
  - (2) **PPO-MPC**：只学习成本权重 \(\{W_p, W_v, W_u\}\) 的参数化 MPC，仍然采用朴素的未来信息使用方式（类似式11）。  
  - (3) **PPO-MPCi**：本文提出的方法，学习包括未来信息权重 \(W_z\) 在内的完整参数集 \(\theta = \{W_p, W_z, W_v, W_u\}\)。
- **对比指标**：累计成本（越低越好）和状态轨迹。
- **训练细节**：PPO 训练，5 个随机种子取平均；未提及其他 RL 基线（如纯神经网络策略）或更多对比。

## 4. 资源与算力

- **未明确说明**：论文中未提供 GPU 型号、数量、训练耗时等信息。仅提到训练累计 1×10⁴ 时间步。因此无法评价其算力开销大小或效率。

## 5. 实验数量与充分性

- **实验数量**：仅有一个赛车环境。在该环境下进行了三种方法的对比（Nominal MPC、PPO-MPC、PPO-MPCi），并报告了 5 个随机种子的平均学习曲线和一个代表性轨迹图。
- **充分性评价**：  
  - **正面**：对比了是否学习未来信息权重的消融（PPO-MPC vs PPO-MPCi），验证了学习未来信息利用的有效性；显示了累计成本收敛轨迹。
  - **不足**：  
    - 缺少与纯神经网络策略（如全连接网络策略）的对比，无法突出 MPC 结构化逼近器的优势；  
    - 仅一个任务，泛化性未知；  
    - 未来信息形式简单（参考轨迹），未测试其他类型（如随机预测序列）；  
    - 未来信息长度 \(H\) 固定，未做敏感性分析；  
    - 未提供统计显著性检验或更详细的超参数调优说明。
  - **客观公平性**：对比方法设定合理（未学习的 MPC、学习成本但不学习未来信息的 MPC、学习全部参数的 MPC），但缺乏更多基线。

## 6. 论文的主要结论与发现

- 参数化 MPC 在操作于**增广状态**并一致传播未来信息的前提下，可以表示带有未来信息的 MDP 的最优价值函数和策略（定理 1）。
- 将这种参数化 MPC 作为结构化函数逼近器，利用 RL（PPO）学习其参数是可行的。
- 在点质量赛车任务中，PPO-MPCi（学习未来信息权重）相比仅学习成本权重的 PPO-MPC 和固定权重的 Nominal MPC 获得了更低的累计成本，并能利用未来参考轨迹实现更激进的过弯（cut corners），提高了前进进度。
- 该方法提供了有益的**归纳偏置**：即使在训练前，未来信息也已通过 MPC 结构影响决策，而纯神经网络策略需要从数据中重新学习这种结构。

## 7. 优点

- **理论贡献**：首次建立了参数化 MPC 与带未来信息 MDP 的最优价值函数之间等价性的充分条件，将 [Gros & Zanon, 2019] 的结果扩展到了增广状态场景。
- **方法创新**：提出将未来信息作为增广状态纳入 MPC 预测模型，而非简单地阶段式嵌入，这保证了与 MDP Bellman 结构的一致性。
- **实用性**：通过参数化 MPC 与 RL 的结合，避免了人工设计成本函数和终端成本，允许从数据中自动调节参数。
- **实验验证**：在赛车任务上展示了所提出方法相比于两种基线的性能提升，并直观展示了未来信息利用对于轨迹优化的益处。

## 8. 不足与局限

- **实验覆盖有限**：仅一个简单动力学环境（点质量），未来信息为确定性参考轨迹，未验证随机预测、多模态未来信息或更长时域的情况。
- **缺乏强基线对比**：未与纯神经网络策略（如 MLP Actor）或标准 RL 算法（如 PPO 直接输出动作）进行比较，难以判断 MPC 结构带来的绝对优势。
- **算力报告缺失**：未提供任何计算资源信息，不利于复现与效率评估。
- **定理假设简化**：证明中假设 \(\tilde{f}\) 是确定性的，且未来信息更新 \(g_\theta\) 形式简单（如移位）。实际随机性更强的更新（如天气预测贝叶斯更新）是否仍能通过参数化表示最优策略需要进一步探讨。
- **扩展性问题**：当未来信息维度很高时（如高分辨率图像序列），增广状态维度过大，Critic 学习可能困难，文中未提出解决方案（仅提及未来信息压缩可能使用 VAE，但未实验）。
- **参数化局限**：实验中仅参数化了成本权重和未来信息权重，未考虑参数化动力学或约束；理论中参数化可以是任意的，但实际搜索空间可能很大，学习效率未分析。
- **折扣因子处理**：MPC 中使用的折扣因子 \(\gamma\) 需与 MDP 一致，文中未讨论折扣因子不匹配时的鲁棒性。

（完）
