---
title: ASCII Art Turns LLMs into VLA Controllers
title_zh: ASCII 艺术将 LLM 转变为 VLA 控制器
authors: "Yitao Jiang, Roy Xing, Luyang Zhao, Brian Plancher, Muhao Chen, Devin Balkcom"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21470v1"
tags: ["query:vla"]
score: 9.0
evidence: ASCII渲染将LLM转变为VLA控制器
tldr: 现有视觉-语言-动作（VLA）控制器通常依赖多模态大模型，数据与计算开销大。本文提出将视觉观察渲染为ASCII艺术，作为文本输入微调纯文本LLM，使其成为VLA控制器。在2D操作基准中，模拟与实体机器人上，该控制器能识别任务实体并规划可行动作序列。ASCII渲染可作为轻量级、可解释的模态桥梁，为纯文本LLM用于VLA研究开辟新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21470v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21470v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1874, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21470v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 919, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21470v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21470v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1860, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21470v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1876, \"height\": 931, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21470v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 502, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21470v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1781, \"height\": 488, \"label\": \"Table\"}]"
motivation: 现有VLA控制器依赖多模态骨干，需要大量数据和计算资源，本文探索用纯文本LLM替代。
method: 将视觉观察渲染为ASCII文本，微调LLM使其能基于视觉状态和自然语言指令生成可执行动作。
result: 在2D操作任务中，模拟和实体机械臂均能准确识别实体并规划可行动作序列。
conclusion: ASCII渲染作为轻量级、可解释的模态桥梁，可补充传统VLA流程，拓展纯文本LLM在VLA中的应用。
---

## 摘要
视觉-语言-动作（VLA）控制器通常是通过扩展视觉-语言模型（VLM）并加入动作监督来构建的，依赖于需要大量数据和计算资源的多模态骨干网络。我们证明，当视觉观察通过 ASCII 表示渲染为文本输入时，纯文本大型语言模型（LLM）可以被调整为 VLA 风格的控制器。这种 ASCII 作为视觉的接口使得现有的 LLM 训练和部署堆栈能够高效地基于视觉状态进行条件化、遵循自然语言指令并生成受限的可执行动作。我们使用基于规划的教师提供的专家示范以及 DAgger 进行迭代改进，对多个 LLM 和 VLM 在不同模型家族和规模上进行了微调和比较。在一个二维操作基准测试中，无论是在仿真环境还是在物理机械臂上，得到的控制器都能识别任务相关实体并规划可行的动作序列。我们的结果表明，ASCII 渲染可以作为一种轻量级、可解释的模态桥梁，从图像过渡到文本，补充传统的 VLA 流程，并为仅使用文本骨干网络的 VLA 研究开辟方向。

## Abstract
Vision--Language--Action (VLA) controllers are often built by extending vision--language models (VLMs) with action supervision, relying on multimodal backbones with large data and compute requirements. We demonstrate that a text-only large language model (LLM) can be adapted into a VLA-style controller when visual observations are rendered into a text input using an ASCII representation. This ASCII-as-vision interface enables existing training and deployment stacks for LLMs to efficiently condition on visual state, follow natural-language instructions, and produce constrained, executable actions. We fine-tune and compare multiple LLMs and VLMs across model families and scales, using both expert demonstrations from a planning-based teacher, as well as DAgger for iterative improvement. In a 2D manipulation benchmark, in both simulation and on a physical manipulator, the resulting controllers can identify task-relevant entities and plan feasible action sequences. Our results suggest that ASCII rendering can serve as a lightweight, interpretable modality bridge from images to text, complementing conventional VLA pipelines, and opening directions for VLA research with text-only backbones.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，现根据您提供的论文内容，生成详细的中文总结如下。

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：当前主流的视觉-语言-动作（VLA）模型通常建立在视觉-语言模型（VLM）之上，依赖于多模态骨干网络。这导致两个主要瓶颈：一是训练和部署需要大量的数据和计算资源，门槛较高；二是通常需要大规模的人类遥操作来收集高质量的训练数据。
- **研究动机**：本文旨在探索一种替代方案，即**能否仅使用纯文本的大语言模型（LLM）来构建VLA控制器**，从而规避对多模态训练pipeline和大量图像输入的直接依赖，降低研究门槛和计算开销。
- **核心含义**：本文验证了通过一种轻量级、可解释的“模态桥梁”——ASCII艺术渲染，可以将纯文本LLM高效地转变为能够感知视觉状态、理解自然语言指令并输出可执行动作的VLA控制器，为VLA研究提供了新的、更便捷的路径。

### 2. 方法论

- **核心思想：ASCII-as-Vision（ASCII即视觉）**：核心思想是将二维操作场景的RGB摄像头帧，渲染为一个固定大小的、包含颜色的ASCII艺术文本栅格。这个文本表示作为LLM的输入，使其能够“看到”环境状态。
- **关键技术细节**：
    1.  **ASCII编码器**：将 `1920x1080` 像素的RGB图像下采样并编码为 `96x54` 的ASCII字符栅格。该系统采用一套固定的颜色调色板（共16种颜色），背景映射为空格，实体（物体、目标、夹爪、障碍物）用特定字符和颜色表示，并用边界字符分隔。
    2.  **动作空间**：定义了一个受限的领域特定语言（DSL），控制器输出三种类型的命令：
        -   `MOVE(x， y)`：运动指令，在离散的 `192x108` 动作网格上指定目标坐标。
        -   `GRIP(PICK)` 和 `GRIP(DROP)`：抓取和放置指令。
        -   `UNREACHABLE(BLOCKED | NARROW | UNDEFINED)`：任务不可达声明，并细分了三种不可达类型（路径阻塞、通道狭窄、实体缺失）。
    3.  **学习流程**：
        -   **Stage 0**：基于专家策略（使用A*规划器获得的地面真实几何信息）生成监督训练数据。
        -   **后续阶段**：采用**DAgger**（数据集聚合）算法进行迭代改进。在每一轮，当前策略与环境交互，然后专家（A*规划器）为策略访问过的状态提供修正标签。这有助于解决模仿学习中由于策略执行状态分布与专家状态分布不同而产生的复合误差问题。对基础LLM采用LoRA（低秩适应）进行轻量级微调。

### 3. 实验设计

- **数据集/场景**：在一个2D俯视操作仿真环境中进行。场景包含一个白色夹爪、红/绿/蓝三色圆形可移动物体和X形目标标记，以及其他颜色的静态障碍物。任务包括到达目标点、抓取-放置等操作。构造了五种不同类型的场景来评估模型的可达性推理能力：
    -   `Reachable-Simple` (可到达-简单)
    -   `Reachable-Complex` (可到达-复杂)
    -   `Unreachable-Blocked` (不可到达-路径阻塞)
    -   `Unreachable-Narrow` (不可到达-通道狭窄)
    -   `Unreachable-Undefined` (不可到达-实体缺失)
- **Benchmark**：
    -   **可达场景**：主要指标是任务成功率。辅助指标包括碰撞率和平均步数。
    -   **不可达场景**：评估不可达检测准确率和不可达子类型（BLOCKED/NARROW/UNDEFINED）的预测准确率。
    -   **误报率**：在可达场景中，模型错误输出 `UNREACHABLE` 的概率。
- **对比方法**：对多种LLM和VLM进行了比较：
    -   **模型家族**：Qwen3（文本版）， Qwen3-VL（视觉版）， Llama-3.2（文本版）， Gemma-3（文本版和视觉版）。
    -   **模型规模**：从0.6B到14B参数不等（见表I）。
    -   **对比设置**：为确保公平比较，所有模型使用相同的ASCII观察接口和动作DSL。对于VLM，输入的RGB图像被下采样到与ASCII栅格相同的 `96x54` 分辨率，以隔离模态接口本身的影响。

### 4. 资源与算力

- **未明确说明**：论文中**没有明确说明**训练所使用的GPU型号、数量或训练总时长。仅提到采用了LoRA进行轻量级微调，这通常意味着相对较低的显存需求。这表明该工作的核心吸引力之一是其较低的计算门槛，但具体算力消耗未被量化。

### 5. 实验数量与充分性

- **实验数量**：实验较为充分。主要实验包括：
    - 对12种不同模型进行了20个阶段的DAgger训练和测试。
    - 在5种不同场景上分别评估了成功率和碰撞率等指标。
    - 展示了从Stage 0到Stage 20的阶段性性能提升曲线（图5）。
    - 在真实机器人上进行了定性演示。
- **充分性与公平性**：
    - **优点**：实验设计系统，涵盖了多种模型家族、尺寸和模态，并考虑了公平性因素（统一输入分辨率）。消融了DAgger训练的作用。考虑了多种任务场景（包括可到达和不可到达情况），评估维度全面。
    - **不足**：实验均基于2D操作任务，维度较低，在3D、高自由度或动态环境下泛化能力未知。真实世界的实验仅作为定性演示，缺乏定量指标。仅在一个物理平台（Flexiv Rizon4s）上验证，实验场景有限。

### 6. 主要结论与发现

1.  **ASCII-as-Vision有效**：使用ASCII艺术作为视觉输入的纯文本LLM可以被成功训练成VLA控制器，能够完成多步操作、避障和任务可行性判断。
2.  **阶段式训练增益显著**：通过DAgger进行迭代训练是关键的，它显著提升了模型在闭环控制下的鲁棒性，远超仅依赖专家示范的Stage 0结果。
3.  **纯文本LLM可与VLM竞争**：在相同的ASCII接口和动作DSL下，纯文本LLM在与同等规模的VLM对比时，性能不相上下，在某些指标上甚至更优。例如，Qwen3-8B（文本）在成功率上略优于Qwen3-VL-8B（80.4% vs 79.0%），且误报率更低。
4.  **规模提升带来性能提升**：在Qwen3和Llama-3.2家族中，更大规模的模型在任务成功率和安全性（低碰撞率）上表现更好。
5.  **成功Sim-to-Real迁移**：通过一个确定性的预处理pipeline（包括颜色掩码、去噪和视点矫正），仿真训练的策略可以零样本迁移到真实环境中的物理机械臂上。

### 7. 优点

1.  **方法新颖且轻量**：提出了“ASCII-as-Vision”这一新颖且极其轻量级的思路，巧妙地解决了LLM无法直接处理图像的问题，大幅降低了VLA研究的门槛（数据和算力需求）。
2.  **可解释性强**：ASCII艺术表示是为人眼可读的，使得观察、日志记录和调试变得直观、容易，这是传统图像特征向量所不具备的优势。
3.  **实验设计严谨**：系统性地对比了多个模型家族、规模和模态，并通过统一输入分辨率确保了对比的公平性。对任务的可达性、不可达性子类型等进行了精细分类和评估。
4.  **现实应用验证**：不仅限于仿真，成功展示了从仿真到真实机器人（Sim-to-Real）的迁移，证明了方法的实际可行性。其针对真实世界视差等问题的预处理pipeline也体现了工程上的细致考虑。

### 8. 不足与局限

1.  **信息带宽瓶颈**：ASCII渲染过程是有损的，丢弃了丰富的视觉细节（如纹理、精确几何形状），这可能限制其在处理密集杂乱场景、微小物体或高精度操作任务时的表现。
2.  **任务环境的局限性**：所有实验均在2D俯视操作环境中进行，该环境相对简化。方法在处理3D场景、连续动作空间、复杂操作或动态环境时的有效性尚未得到验证。
3.  **模型训练不稳定性**：不同模型家族对训练超参数的敏感性不同。特别是Gemma系列模型在标准超参数下训练不稳定，表明该方法对不同架构的泛化能力存在差异。
4.  **现实世界的未建模挑战**：真实世界实验中的失败主要归因于光照变化、感知噪声、校准误差和抓取/执行的不确定性，这些都是理想化2D仿真中不存在的，表明Sim-to-Real的真实鲁棒性有待进一步提高。
5.  **模态范围有限**：目前仅处理RGB图像，未探索整合深度信息等其他模态，本文认为这本身是一个有潜力的未来方向。

（完）
