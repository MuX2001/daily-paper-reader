---
title: "Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning"
title_zh: 解耦语义与几何基础：面向语言条件模仿学习的空间视觉提示
authors: "Yanzhe Tang, Xinyu Shao, Yuxuan Hu, Siyu Chen, Bowen Yang, Yajun Gao, Tongtong Cao, Xiu Li, Long Zeng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25360v1"
tags: ["query:vla"]
score: 9.0
evidence: 视觉语言动作模型，空间视觉提示
tldr: "端到端VLA模型将语义推理与空间控制耦合，导致数据受限下的对齐瓶颈。提出SVP-IL解耦架构，利用VLM解析指令为零样本几何掩码作为空间视觉提示，通过轻量特征融合注入动作生成器。在50-100个演示下，平均成功率从24.0%提升至39.5%，标准基准达67.8%。该方法在保持稳定优化的同时显著提升数据效率与泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25360v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25360v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 822, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 808, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 868, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 294, \"label\": \"Table\"}]"
motivation: 现有VLA模型因耦合语义与空间控制，在少量数据下的模仿学习中难以精确目标定位，存在严重的对齐瓶颈。
method: 提出SVP-IL解耦架构，利用VLM生成零样本几何掩码作为SVP，通过直接特征融合注入动作生成器，隔离空间定位与语义推理。
result: "50-100演示下平均成功率从24.0%升至39.5%，标准基准达67.8%，真实实验验证其鲁棒性与数据效率。"
conclusion: SVP-IL通过解耦语义与几何定位，为数据受限的语言条件模仿学习提供了高效且可泛化的新范式。
---

## 摘要
尽管端到端的视觉-语言-动作（VLA）模型在机器人操作中展现出潜力，但其单一范式本质上将语义推理与空间控制耦合在一起。这造成了严重的对齐瓶颈，限制了数据受限模仿学习中的精确目标消歧。为了克服这一问题，我们提出了SVP-IL，一种解耦架构，它从动作生成循环中显式提取空间视觉基础。通过利用视觉-语言基础模型，我们将指令解析为零样本几何掩码，将语言转化为显式的空间视觉提示（SVP）。这些先验知识通过轻量级的直接特征级融合机制注入到连续动作生成器中。这种集成提供了明确且无破坏的空间梯度引导，同时在低数据情况下确保高度稳定的优化。大量实验表明，SVP-IL显著优于最先进的VLA模型和纯视觉运动基线。在仅使用50到100个演示进行训练的情况下，SVP-IL将高度模糊的语言条件任务的平均成功率从24.0%提高到39.5%，在标准基准测试中达到67.8%。真实世界的机器人实验进一步验证了其在非结构化物理环境中的鲁棒性和数据效率。

## Abstract
While end-to-end Vision-Language-Action (VLA) models show promise in robotic manipulation, their monolithic paradigm inherently couples semantic reasoning and spatial control. This creates a severe alignment bottleneck, limiting precise target disambiguation in data-constrained imitation learning. To overcome this, we propose SVP-IL, a decoupled architecture that explicitly extracts spatial visual grounding from the action generation loop. By leveraging vision-language foundation models, we parse instructions into zero-shot geometric masks, translating language into explicit Spatial Visual Prompts (SVP). These priors are injected into a continuous action generator via a lightweight direct feature-level fusion mechanism. This integration provides explicit and uncorrupted spatial gradient guidance while ensuring highly stable optimization under low-data regimes. Extensive experiments demonstrate that SVP-IL significantly outperforms state-of-the-art VLAs and pure visuomotor baselines. Trained on as few as 50 to 100 demonstrations, SVP-IL improves average success rates on highly ambiguous language-conditioned tasks from 24.0% to 39.5%, achieving 67.8% on standard benchmarks. Real-world robotic experiments further validate its robustness and data efficiency in unstructured physical environments.

---

## 论文详细总结（自动生成）

# 论文详细总结：Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning

## 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：端到端视觉-语言-动作（VLA）模型将语义推理、空间定位和连续动作生成耦合在一个单一网络中，导致严重的“语义-空间对齐瓶颈”。在数据受限的模仿学习场景下（仅50~100个演示），模型难以隐式学习跨模态对齐，尤其在包含多个相似干扰物的复杂场景中，容易发生目标误判或空间执行不精确。
- **研究动机**：打破“端到端”的单一范式，将语义理解与几何定位解耦，通过显式的空间视觉提示（SVP）为连续控制提供强几何先验，从而在低数据条件下显著提升语言条件操控的精度和鲁棒性。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：构建解耦感知-控制架构。将语义目标消歧任务从高频动作生成回路中分离出来，交给预训练的视觉-语言基础模型（VLM）完成；连续控制策略仅需专注于基于显式几何掩码的局部空间执行。
- **关键技术细节**：
  1. **空间视觉提示（SVP）生成**：  
     - 使用大语言模型（LLM）解析原始指令 \(l\)，提取目标物体名称 \(c = \text{LLM}(l)\)。  
     - 基于SAM 3模型，以RGB图像 \(I_t\) 和文本提示 \(c\) 作为输入，零样本输出二进制几何掩码 \(M_t = \text{SAM3}(I_t, c)\)。
  2. **特征级融合机制**（核心创新）：  
     - 为避免图像级叠加（如通道拼接）破坏预训练视觉骨干的浅层纹理特征，引入轻量级侧流卷积网络（Side-CNN）将掩码 \(M_t\) 编码为与骨干网络中间层匹配的稠密特征图。  
     - 通过**直接逐元素加法**将这些几何特征注入深层潜在空间：\(F^{(i)} = \Phi_{\text{rgb}}^{(i)}(I_t) + F_{\text{prompt}}^{(i)}\)。该无参数加法作为刚性结构偏置，提供即时稳定的空间梯度引导，并避免参数化门控机制在低数据下的过拟合风险。
  3. **整体框架SVP-IL**：  
     - 将增强后的视觉特征与本体感知状态、原始文本嵌入拼接，作为Diffusion Policy（DP）的动作生成条件。  
     - 推理采用同步管线：LLM仅初始化一次提取目标名；每步使用SAM 3生成掩码，降采样后送入策略；策略以10 Hz输出动作块，插值至300 Hz底层控制。

## 3. 实验设计

- **数据集/场景**：
  - **模拟**：RoboTwin 2.0基准（包含9个标准任务，如Adjust Bottle、Lift Pot等），以及6个自定义语言条件任务（要求根据指令从多个物体中选取特定目标）。
  - **环境变体**：Clean设置（简单背景）和Randomized设置（杂乱的背景、可变光照），用于测试视觉分布偏移鲁棒性。
  - **真实机器人**：Aloha-AgileX双臂平台，包含3个密集杂乱任务（Table Setting、Desktop Cleaning、Bimanual Sorting）。
- **对比方法**：
  - 基线：纯视觉运动策略 ACT、Diffusion Policy（DP），以及SOTA VLA模型 π0。
  - 消融：对比图像级集成策略（Pixel-Overlay、Early-Concat）与本文特征级融合。
- **训练数据量**：标准任务使用50个演示；自定义任务使用100个演示；真实任务使用100个演示。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。只提到“严格遵循官方默认配置”进行π0全参数微调，以及模型均在低数据量下训练。

## 5. 实验数量与充分性

- **实验组数**：
  - 标准基准9个任务 × 4种方法（DP、ACT、π0、SVP-IL）= 36组结果。
  - 自定义任务6个任务 × 4种方法 = 24组结果。
  - 视觉分布偏移实验：两种设置（Clean、Random） × 2种方法（DP、SVP-IL） × 共15个任务 = 60组结果。
  - 消融实验：4种融合策略 × 共15个任务 = 60组结果。
  - 真实实验：3个任务 × 3种方法 × 20轮 = 180次试验。
- **充分性与公平性**：
  - 所有方法共享相同的训练配置和评估协议，对比公平。
  - 消融实验系统对比了不同集成策略，并给出统计均值。
  - 覆盖了模拟和真实场景，包括分布偏移测试。
  - **不足**：未报告方差或置信区间；部分任务（如Hanging Mug）成功率极低，可能暗示任务难度分布不均衡；未与更多近期VLA模型（如RT-2）对比。

## 6. 主要结论与发现

- **解耦架构显著提升数据效率**：在仅50~100个演示的训练下，SVP-IL在标准基准上平均成功率67.8%（DP为42.3%，π0为53.7%）；在高度模糊的语言条件任务上平均成功率39.5%（π0为24.0%）。
- **特征级融合优于图像级集成**：直接逐元素加法比Pixel-Overlay（20.0%）和Early-Concat（22.2%）在自定义任务上提升近一倍（39.5%），且避免了预训练权重的破坏。
- **显式几何先验优于域随机化**：在随机域训练下，DP因过拟合干扰而性能崩溃（14.9%），而SVP-IL仍保持24.0%，且远优于任何条件下训练的DP。
- **真实环境有效**：SVP-IL在真实机器人上平均成功率60.0%，远超DP（28.3%）和π0（31.7%），对颜色、形状和臂路分配具有精准识别能力。

## 7. 优点

- **方法创新**：提出解耦感知-控制范式，将语言条件操控中的语义消歧与空间执行明确分离，从根本上缓解VLA的对齐瓶颈。
- **轻量且即插即用**：特征级融合模块简单（Side-CNN + 加法），可兼容现有基于ResNet/Diffusion的架构，无需复杂设计即可获得巨大提升。
- **极强数据效率**：在低数据（50~100演示）下表现突出，显著优于需要大量数据训练的端到端VLA。
- **实验全面**：覆盖标准任务、自定义语义任务、域偏移、真实部署，消融对比充分。
- **可解释性强**：空间视觉提示（掩码）直观可视化，便于查错和调试。

## 8. 不足与局限

- **依赖上游模型**：SVP性能受限于LLM和SAM 3的精度，透明、反光或严重遮挡物体的掩码可能不准确，导致后续策略失误。
- **计算开销**：SAM 3作为空间提示器在线推理引入延迟（虽通过下采样和动作分块缓解，但动态快速场景中仍受限）。
- **指令范围有限**：当前仅支持显式目标名称提取，无法隐式推断抽象意图（如“清理桌子”），未来需扩展至复杂任务分解。
- **实验统计不够详细**：未报告平均值差和方差，也未在更多VLA模型（如OpenVLA、RT-2）上进行直接比较。
- **任务多样性**：仅涉及桌面操作和简单物体放置/拾取，未验证在移动操作、非刚性物体或更长时域任务上的表现。
- **未讨论正负样本均衡**：低数据下的过拟合风险虽被部分缓解，但未进行正则化或数据增强的全面分析。

（完）
