---
title: "Beyond the Next Step: Variable-Length Latent World Models for Long-Horizon Planning"
title_zh: 超越下一步：用于长时程规划的变长隐世界模型
authors: "Tianqi Du, Qi Zhang, Yifei Wang, Yisen Wang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21775v1"
tags: ["query:world-model"]
score: 9.0
evidence: 变长潜在世界模型用于长时域规划
tldr: "潜在世界模型通常只进行一步预测，递归展开用于长时域规划时误差累积严重。本文提出可变长度潜在世界模型（VLWMs），直接预测变长动作序列对应的未来潜在状态，并采用课程训练策略逐步扩展动作视野。在长时域控制任务上，VLWMs平均提升13%，尤其在需要长期规划的任务上提升显著。该工作提供了一种简单有效的范式来改进潜在世界模型的长时域预测与规划能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1350, \"height\": 807, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21775v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1329, \"height\": 678, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21775v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1161, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1198, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21775v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1019, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有潜在世界模型的一步预测导致递归滚动时累积误差，无法直接匹配长时域规划需求。
method: 提出VLWMs框架，学习预测变长动作序列对应的潜在状态，并用课程训练策略从短程到长程优化。
result: "在长时域控制任务上平均提升13%，大幅优于基线LeWM，尤其长期规划任务。"
conclusion: VLWMs提供了改进潜在世界模型长时域预测与规划的简单有效范式。
---

## 摘要
最近，世界模型作为一种构建智能代理的有前景范式出现，通过学习预测模型来估计基于观测和动作的未来环境状态。特别是，JEPA风格的隐世界模型通过在紧凑表示空间中学习动作条件动力学，为像素空间预测提供了一种高效替代方案。然而，现有的隐世界模型通常依赖于单步预测，并且必须递归展开以进行长时程规划，这导致误差累积以及训练目标与下游规划任务之间的不匹配。为了解决这一局限性，我们提出了变长隐世界模型（VLWMs），该框架学习预测基于可变长度动作序列的未来隐状态。VLWMs不仅训练单步转移，而是直接建模时间扩展动力学，使同一预测器能够评估不同规划跨度的动作计划。我们进一步引入了一种课程训练策略，逐步扩展动作规划跨度，从短程动力学优化到长程预测。在测试时，我们设计了针对VLWMs定制的规划方法，以更好地利用其变长预测能力。在长时程控制任务上的实验表明，VLWMs显著改进了隐空间世界模型，在不同数据集上相比最先进的LeWM平均提升13%，尤其在需要扩展规划的任务中获得更大收益。这些结果表明，VLWM为改进隐世界模型中的长时程预测与规划提供了一种简单而有效的范式。

## Abstract
Recently, world models have emerged as a promising paradigm for building intelligent agents by learning predictive models that estimate future environment states conditioned on observations and actions. In particular, JEPA-style latent world models provide an efficient alternative to pixel space prediction by learning action-conditioned dynamics in compact representation spaces. However, existing latent world models typically rely on one-step prediction and must be recursively rolled out for long-horizon planning, which leads to compounding errors and a mismatch between training objectives and downstream planning tasks. To address this limitation, we propose Variable-length Latent World Models (VLWMs), a framework that learns to predict future latent states conditioned on action sequences of variable lengths. Instead of training only on one-step transitions, VLWMs directly model temporally extended dynamics, allowing the same predictor to evaluate action plans over different horizons. We further introduce a curriculum training strategy that progressively expands the action horizon, stabilizing optimization from short-range dynamics to long-range prediction. At test time, we design planning methods tailored to VLWMs to better exploit their variable-length predictive capabilities. Experiments on long-horizon control tasks show that VLWMs significantly improve latent space world models, achieving 13\% average improvement over the state-of-the-art LeWM across different datasets, with especially large gains on tasks requiring extended planning. These results suggest that VLWM provides a simple yet effective paradigm for improving long-horizon prediction and planning in latent world models.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有潜在世界模型（如JEPA、DINO-WM、LeWM）通常只进行**单步预测**（t → t+1），在长时程规划中需要递归展开，导致**累积误差**（compounding error）和**训练目标与下游规划任务不匹配**。此外，相邻帧存在大量时间冗余，单步预测容易被“短时插值”或“近恒等映射”解决，从而无法捕捉高层动态和长程语义一致性。
- **背景**：世界模型通过学习预测未来环境状态来构建智能代理，其中潜在空间预测（JEPA风格）因避免像素重建而高效，但在长时程任务上表现不佳。此前的工作如Hierarchical World Models（HWM）虽探索了不同长度的动作序列，但依赖固定长度、需要中间高层目标、且需要额外模块。
- **整体动机**：本文提出**可变长度潜在世界模型（VLWMs）**，让单一模型能够预测**任意未来偏移 k** 的潜在状态，直接在训练中引入长程监督，从而缓解累积误差，并统一短程和长程动力学学习。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **变长监督**：不再仅预测下一步，而是训练预测器 g_φ 根据当前潜在状态 s_t 和一段可变长度动作序列 a_{t:t+k-1}，直接预测 s_{t+k}。
- **动作作为令牌**：将每个动作嵌入视为一个 transformer 令牌，与状态令牌交错排列在单一输入序列中（如 [s_t, a_t, a_{t+1}, ..., a_{t+k-1}]），天然支持任意长度动作条件，无需专用条件机制（如FiLM）。
- **课程训练策略**：逐步扩展预测跨度，从 k=1 开始，逐步增加到最大跨度 K_max，每阶段均匀采样当前阶段内的所有 k。避免直接训练长跨度时的不稳定性。
- **规划策略**：在MPC框架内，将规划跨度 H 划分为多个可变长度的块（chunks），每个块直接预测 k_i 步后的潜在状态。提供三种变体：
  - P1：固定但任务自适应的块大小（如 k=3 或 5）。
  - P2：随机采样块大小（均匀分布）。
  - P3：长到短（先大块后小块，类似粗到细搜索）。

### 关键技术细节
- **架构**：编码器 f_θ（ViT-Tiny）、目标编码器 ¯f_θ（EMA更新）、预测器 g_φ（6层因果Transformer，16头，MLP隐藏层2048）。动作编码器 h_ψ 为线性头。
- **训练目标**：L_VLWM = E_{t, k~p(k)} [ || g_φ(s_t, a_{t:t+k-1}) - sg( ¯f_θ(o_{t+k}) ) ||_2^2 ]，其中 p(k) 由课程分布决定。
- **课程调度**：总训练步数 T，最大跨度 K_max=5（帧跳跃5，对应25环境步）。分为 K_max 个阶段，每阶段长度 T/K_max，阶段 j 内均匀采样 k ∈ {1,...,j}。
- **规划求解器**：Cross-Entropy Method (CEM)，每次重规划采样300条候选动作序列，优化30次迭代，精英比例30/300，规划跨度5个规划步（即25环境步）。

## 3. 实验设计

- **数据集/环境**：
  1. **PushT**：2D推动任务（T形块），20000条专家轨迹，平均长度196步。
  2. **OGBench-Cube**：3D机器人抓取放置任务（单立方体），10000条轨迹，每段200步。
  3. **TwoRoom**：2D导航任务（两房间隔一墙一门），10000条轨迹，平均长度92步。
- **基准（Benchmark）**：采用与LeWM相同的评估协议，目标条件规划（给定初始观测和目标观测，目标观测为未来 ∆ 步的帧），∆ ∈ {25, 50, 75, 100}。评估预算为50环境步。
- **对比方法**：
  - **基线**：LeWM（固定一步预测，最新SOTA潜在世界模型）。
  - **VLWM自变体**：P1（固定块大小k=3或5）、P2（随机）、P3（长到短，K_max=3或5）。
  - 额外消融：有无课程训练、线性探测分析。

## 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。仅提到训练使用AdamW优化器，batch size 128，梯度裁剪，bfloat16混合精度，训练最多100个epoch但实际在10个epoch时评估。具体硬件资源未披露。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：3个数据集 × 4种目标偏移 = 12个配置，每个配置报告成功率。
  - 规划策略对比：表1给出了所有VLWM变体（P1、P2、P3）的完整结果，并对比LeWM基线。
  - 课程消融：表2在3个数据集上对比有无课程训练。
  - 线性探测：表3在PushT上对3个物理属性（agent位置、块位置、块角度）进行线性探针评估。
  - 可视化：图3定性展示了TwoRoom上的轨迹对比。
- **充分性与公平性**：
  - 对比方法LeWM的架构、优化和CEM超参数均保持一致，差异仅在于VLWM的变长训练和动作令牌布局，因此归因明确。
  - 在多个环境、多个规划跨度上验证，长程优势明显；但课程消融在OGBench-Cube上增益有限，作者给出了合理分析（该数据集长程难度不显著增加）。
  - 规划策略无单一最优，但提供了不同策略的全面比较，客观反映了变长规划的灵活性。
  - 未进行统计显著性检验（如多次随机seed），但使用了固定seed 3072，且与LeWM采用相同数据划分和评估协议，可信度较高。

## 6. 论文的主要结论与发现

1. **变长监督显著提升长时程规划**：VLWM在所有数据集和所有目标偏移上优于LeWM，尤其长程（∆=75/100）增益最大，如TwoRoom-100从36%提升到68%，TwoRoom-75从34%提升到70%。
2. **课程训练稳定优化**：去除课程后性能普遍下降，尤其在长程任务上，验证了渐进扩展的必要性。
3. **变长规划策略灵活有效**：不同环境/跨度适合不同块划分方式（P1/P2/P3），无需单独训练多个模型。
4. **潜在表示质量提升**：线性探测显示VLWM的潜在表示能更准确地预测物理属性（尤其块角度），表明变长监督促使编码器捕捉更丰富的语义结构。
5. **统一框架简单有效**：将现有一步世界模型视为特例，VLWM在保持架构简单的同时实现显著改进。

## 7. 优点：方法或实验设计上的亮点

- **方法设计**：
  - 将动作作为令牌，与状态令牌交错排列，天然支持变长动作条件，架构简洁且与主干无关。
  - 课程训练避免长跨度训练不稳定，且基于实际困难度逐步引入。
  - 规划阶段提供多种块划分策略，实现单一模型内的粗到细规划，无需额外层次化模块。
- **实验设计**：
  - 使用与LeWM完全相同的环境和评估协议，结果可直接对比。
  - 不仅比较规划成功率，还通过线性探测分析了潜在表示质量，提供了更深入的洞察。
  - 消融实验充分，验证了课程训练和不同规划策略的影响。

## 8. 不足与局限

1. **实验覆盖有限**：仅涉及三个环境（两个2D、一个3D），未在更复杂（如真实机器人、高维控制任务）或更长时间跨度（如∆>100）上验证。且OGBench-Cube上长程增益不大，提示方法可能对某些任务不敏感。
2. **算力资源未披露**：无法评估训练成本，不利于复现和对比。
3. **规划策略选择缺乏自适应**：目前P1/P2/P3需手动选择，没有在线自适应机制，作者也承认这是未来方向。
4. **统计显著性缺失**：未报告多次运行的平均值和标准差，可能受随机种子影响。
5. **线性探测**仅针对PushT，未在其他环境验证表示质量的泛化性。
6. **超参数敏感性**：K_max、课程阶段数、planner的块分布参数等未详细分析敏感性，可能影响实际应用。
7. **与HWM的对比缺失**：虽然论述了与HWM的区别，但未在实验中直接比较，削弱了与最相关工作的竞争力证明。

（完）
