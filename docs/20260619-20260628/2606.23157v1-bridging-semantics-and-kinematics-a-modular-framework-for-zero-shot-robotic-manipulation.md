---
title: "Bridging Semantics and Kinematics: A Modular Framework for Zero-Shot Robotic Manipulation"
title_zh: 桥接语义与运动学：面向零样本机器人操作的模块化框架
authors: "Ali Alabbas, Dipshikha Das, Camillo Murgia, Sainul Ansary, Alaa Elkamash, Philip Long"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23157v1"
tags: ["query:vla"]
score: 10.0
evidence: 使用视觉语言模型的零样本语言引导机器人操作
tldr: "针对半结构化环境中零样本语言引导机器人操作难题，该文提出模块化无训练三阶段框架，通过FastSAM与SoM提示生成视觉锚点解决空间歧义，利用LLM语义路由器将指令转为可验证配置，再由Task Orchestrator调用MoveIt生成无碰撞轨迹。在开放世界顺序操作与密集关系推理两个零样本实验中取得62%端到端成功率，无需领域训练即可可靠执行复杂物理动作。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 864, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 131, \"label\": \"Table\"}]"
motivation: 现有视觉-语言模型存在空间模糊和语义幻觉，难以直接用于机器人低层运动控制。
method: 分解视觉-动作为视觉感知、语义解释和任务执行三阶段，采用FastSAM和SoM提示生成锚点，LLM作语义路由器，Task Orchestrator调用MTC生成轨迹。
result: "在两个零样本场景中实现62%端到端任务成功率。"
conclusion: 无需领域训练或手动坐标编程即可可靠执行复杂物理动作。
---

## 摘要
本文提出了一种模块化的免训练框架，用于在半结构化环境中进行零样本、语言引导的机器人操作。该架构通过将视觉-动作流水线分解为三个步骤来弥合高级推理与低级运动学之间的差距：视觉感知、语义解释和任务执行。为了克服标准视觉语言模型（VLM）固有的空间模糊性和语义幻觉，感知模块使用FastSAM和Set-of-Mark（SoM）提示来动态生成接地、字母数字的视觉锚点。然后，相同的基础模型纯粹作为大型语言模型（LLM）充当语义路由器，将无约束的人类指令转化为可验证、可重配置的配置。最后，这些配置由任务编排器动态解析为MoveIt Task Constructor（MTC），以生成无碰撞轨迹。该框架在两个零样本实验设置中进行了评估：无约束的开放世界顺序操作和密集关系空间推理，在两个场景中实现了62%的端到端任务成功率，展示了其无需特定领域训练或手动坐标编程即可可靠执行复杂物理动作的能力。

## Abstract
This paper presents a modular training-free framework for zero-shot, language-guided robotic manipulation in semi-structured environments. The architecture bridges the gap between high-level reasoning and low-level kinematics by decomposing the vision-action pipeline into three stages: visual perception, semantic interpretation, and task execution. To overcome the spatial ambiguity and semantic hallucinations inherent in standard Vision-Language Models (VLMs), the perception module employs FastSAM and Set-of-Mark (SoM) prompting to dynamically generate grounded, alphanumeric visual anchors. The same foundation model then operates purely as a Large Language Model (LLM) to act as a semantic router, translating unconstrained human directives into verifiable, reconfigurable configurations. Finally, these configurations are dynamically parsed by a Task Orchestrator into MoveIt Task Constructor (MTC) to generate collision-free trajectories. The framework is evaluated across two zero-shot experimental setups: unconstrained open-world sequential manipulation and dense relational spatial reasoning, achieving a 62% end-to-end task success rate across both scenarios, demonstrating its capacity to reliably execute complex physical actions without domain-specific training or manual coordinate programming.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有视觉语言模型（VLM）存在空间模糊性和语义幻觉，难以直接用于机器人低层运动控制；端到端视觉-语言-动作（VLA）模型虽然强大，但参数巨大、需要领域微调且缺乏可解释性，部署在标准机器人硬件上困难。
- **研究背景**：人类可通过自然语言和非语言线索无缝协作，而将这种能力复制到人机共享自主系统中极具挑战。半结构化环境中，机器人需要从零散、自然的人类指令（如“把红色螺丝刀放到簸箕上”）中理解场景并执行多步操作，同时避免对特定对象类别或场景的依赖。
- **整体含义**：本文提出一种模块化、免训练的框架，将高层的语义理解与低层的运动学解耦，通过视觉锚定、语义路由和声明式任务架构，在零样本下将自然语言指令转化为可执行的无碰撞轨迹，无需任何领域微调或手动编程。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：分解视觉-动作流水线为三个紧密耦合的模块：视觉感知、语义解释、任务规划与执行。利用现成的基础模型（VLM/LLM）作为核心推理组件，但通过结构化的中间表示（YAML任务配置文件）避免直接端到端映射。
- **关键技术细节**：
  1. **视觉感知模块**（Perception Module）：
     - 使用FastSAM进行零样本实例分割，结合Set-of-Mark（SoM）提示在分割对象上动态叠加字母数字标识符（如“screw_driver”、“dustpan”）。
     - 基于RGB-D相机图像，建立共享空间词汇（发布TF变换帧），使VLM能通过读取这些可视化标识完成目标定位与关系推理，避免坐标密集的提示。
  2. **语义解释模块**（Semantic Interpretation Module）：
     - 将自然语言指令与带注释的场景表示一起输入本地部署的开放权重VLM（Molmo2，8B参数），产生结构化的YAML任务序列。
     - 模型充当“语义路由器”，将人类意图映射到预定义的可验证任务原语（如pick-and-place, button press），并绑定具体参数（如pick_frame: “screw_driver”, place_frame: “dustpan”）。
     - 输出遵循预验证的YAML模式，确保格式正确性。
  3. **任务规划与执行模块**（Task Planning and Execution Module）：
     - YAML配置文件包含两层：任务模板库（参数化运动模式）和LLM生成的实例（场景特定值）。任务编排器动态解析YAML，将模板参数替换为具体值。
     - 使用MoveIt Task Constructor (MTC) 将每个YAML阶段转换为对应的MTC阶段对象，生成无碰撞轨迹。解耦了任务定义、任务解释与任务执行。
- **公式/算法流程**：无显式公式。流水线简化为：  
  \( \text{自然语言指令} + \text{RGB-D图像} \xrightarrow{\text{FastSAM+SoM}} \text{带标签的图像+TF帧} \xrightarrow{\text{Molmo2}} \text{YAML任务序列} \xrightarrow{\text{Task Orchestrator+MTC}} \text{机器人运动} \)

## 3. 实验设计：数据集/场景、基准、对比方法

- **实验场景**：
  1. **开放世界顺序操作**（Open-World Sequential Manipulation）：半结构化杂乱桌面，含多种通用物体（盒子、螺丝刀、任务板等）。人类通过手机屏显文字下达无约束多步指令（如“从home开始，按红色按钮，然后拿触控笔放到白板上，返回home”）。共30次试验，变化目标对象和空间布局。
  2. **密集空间关系推理**（Dense Relational Spatial Reasoning）：包含3×3、5×5和圆形钉板，以及视觉上相同的物体（如peg）。人类指令如“填充上面一排空孔”或“填充相邻于已填充孔的孔”。共30次试验，变化起始状态和空间关系。
- **基准**：无直接对比其他方法（如VLA模型）。但进行了一项**消融实验**：对比完整架构（FastSAM+SoM）与基线（将未标注的原始图像直接输入Molmo2），分别测试10次，覆盖两个场景。
- **评估指标**：三级条件成功率：
  - 感知成功：所有任务相关对象被正确定位、分割并匹配TF帧。
  - 翻译成功：VLM生成结构有效、时序正确的YAML序列。
  - 执行成功：MTC成功解析并执行无碰撞轨迹。

## 4. 资源与算力

- 文中明确说明：硬件平台为UR5e机器人、Robotiq Hand-E夹爪、Intel RealSense D435深度相机（安装在腕部）。软件运行在本地工作站，配备单块NVIDIA RTX A4500 GPU（20GB VRAM）和64GB RAM。
- **训练时长**：未提及训练时间，因为框架是免训练的（training-free），仅使用预训练模型进行推理。
- 注意：未使用云端大规模计算，全部本地部署。

## 5. 实验数量与充分性

- **实验总数**：两个场景各30次，共60次端到端试验。消融实验20次（基线10次+完整10次）。
- **充分性分析**：
  - 场景覆盖了两种关键能力：开放式多步操作（考验对象泛化与序列编排）和密集空间推理（考验坐标无关的关系理解）。
  - 消融实验直接证明了SoM视觉锚定的必要性（密集钉板任务基线成功率为0%，完整架构为100%）。
  - 但样本量较小（每个场景30次，消融10次），结果可能存在一定随机性。实验条件未公开随机种子或重复次数细节，统计稳定性有待加强。
  - 未与现有SOTA方法（如RT-2、OpenVLA、Gemini Robotics）进行直接定量对比，公平性上有一定不足（虽然作者在论文中讨论了与VLA模型的定性比较，但缺乏同一硬件/场景下的benchmark）。

## 6. 论文的主要结论与发现

- 整体端到端成功率为62%（37/60），其中翻译成功率高达94.8%（37/39），执行成功率为100%（37/37）。说明一旦感知和翻译正确，运动规划可完美执行。
- 感知模块（FastSAM+SoM）是瓶颈：开放世界感知成功76.7%，密集空间感知成功53.3%。失败主要源于FastSAM的部分-整体分割错误（如将锤子柄识别为铅笔）、VLM对大小比较的脆弱性、以及功能状态推断不足（如簸箕已占满）。
- 翻译失败主要由强烈的语言先验导致（如将“press”动作错误关联到刷子，而非按钮）。
- 密集空间推理中，VLM在5×5钉板识别“中间行”时出现偏移，在圆形无规律布局中无法完成“顺时针”操作。
- 消融实验证实：没有SoM标注时，基线在密集钉板任务中完全失败（0%），凸显视觉锚定对空间推理的关键作用。
- **核心贡献**：提出可重配置的YAML驱动的任务架构，将运动规划与语义解耦，实现零样本适应新指令；同时证明了开放权重VLM（8B）配合适当视觉预处理足以完成复杂物理操作。

## 7. 优点

- **模块化与免训练**：无需领域微调或手工编程，可即插即用，易于部署在标准硬件上。
- **可解释性**：通过YAML声明式任务文件，每一步推理都可审计和调试，相比黑盒VLA更安全。
- **视觉锚定（SoM）机制**：巧妙地将空间坐标提取任务从VLM中剥离，使小模型（8B）即可完成关系推理，避免了大型云端模型的高延迟和成本。
- **实验设计严谨**：采用三级条件评估（感知→翻译→执行），可精确定位失败环节；消融实验强有力地证明了SoM的有效性。
- **成功率合理**：在零样本、无任何微调的前提下达到62%端到端成功率，且执行成功率100%，证明了框架在任务编排上的可靠性。

## 8. 不足与局限

- **实验规模偏小**：每个场景仅30次试验，且未报告置信区间或多次重复。统计显著性存疑。
- **缺乏与SOTA对比**：未在同一条件下与端到端VLA模型（如RT-2、OpenVLA）进行量化比较，削弱了方法优越性的证明。
- **感知模块鲁棒性不足**：FastSAM在部分-整体分割（如工具柄误判）、功能状态（如容器已满）上易出错；VLM对大小/空间关系推理敏感（5×5钉板的“中间行”偏移）。
- **语义路由的语言先验问题**：LLM因语言关联产生幻觉（如将“press”映射到刷子），说明单纯的视觉锚定未能完全抑制语言偏见。未来可引入约束解码或主动提示。
- **静态视觉限制**：当前仅使用单个2D视角，在密集遮挡或6-DoF操作场景中受限。未利用3D点云或多视图。
- **无闭环反馈**：一旦YAML生成，执行是开环的（无实时传感器反馈调整）。未来需加入失败检测和动态重规划。
- **运动原语有限**：仅预置了pick-and-place、button press等基本原语，无法支持更复杂的接触式操作（如拧螺丝、插入）。

（完）
