---
title: "Action ControlNet: A Lightweight Delay-Aware Adapter for Smooth Asynchronous Control in Vision-Language-Action Models"
title_zh: 动作控制网络：一种轻量级延迟感知适配器，用于视觉-语言-动作模型中的平滑异步控制
authors: "Tiecheng Guo, Meng Guo"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25985v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向VLA模型平滑异步控制的轻量适配器
tldr: VLA模型推理延迟导致异步控制中动作不连续和抖动。本文提出Action ControlNet (ACNet)，一种轻量延迟感知适配器，通过将已执行运动后缀作为残差条件，冻结大部分动作头并引入少量可训练参数，兼容扩散和流匹配。在Kinetix、Meta-World MT50和真实机器人平台SO-ARM101上，ACNet在推理延迟下提升了鲁棒性，生成了更平滑的异步轨迹。该方法无需全策略重训练，具有轻量高效的优点。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25985v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1815, \"height\": 491, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 887, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25985v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 892, \"height\": 338, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25985v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25985v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25985v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 180, \"label\": \"Table\"}]"
motivation: 异步执行VLA模型时，动作头基于过时观察预测，直接拼接导致动作不连续和抖动，尤其在接触密集型操作中失败。
method: 提出ACNet，利用已执行运动后缀作为残差条件，冻结大部分动作头，仅引入少量可训练参数，兼容扩散和流匹配生成头。
result: 在Kinetix、Meta-World MT50和SO-ARM101上，ACNet在推理延迟下提高了轨迹平滑性和鲁棒性，优于直接拼接。
conclusion: ACNet是一种轻量延迟感知适配器，无需重训练即可有效改善异步控制平滑性，适用于多种VLA模型。
---

## 摘要
视觉-语言-动作模型在通用机器人操作方面展现出强大潜力，但其推理延迟仍然是实现稳定高频控制的主要障碍。异步执行通过将策略推理与动作执行重叠来缓解这一瓶颈，然而，当机器人继续运动时，下一个动作块仍然是根据过时的观测预测的。因此，直接拼接动作块会导致交接不连续、动作抖动以及在接触密集型操作中出现失败。现有的解决方法通常需要全面重新训练策略或采用特定架构的运行时逻辑。本文提出动作控制网络（ACNet），一种轻量级的延迟感知适配器，它利用已执行的动作后缀作为残差条件，用于大部分冻结的动作头部。ACNet保持预训练骨干网络不变，引入少量可训练参数，并与扩散模型和流匹配等生成式动作头部兼容。在Kinetix、Meta-World MT50以及真实世界的SO-ARM101平台上，ACNet在推理延迟下提升了鲁棒性，并生成了比直接拼接动作块更平滑的异步轨迹，同时比完全延迟条件重训练更加轻量。

## Abstract
Vision-language-action (VLA) models have shown strong potential for general-purpose robot manipulation, but their inference latency remains a major obstacle to stable high-frequency control. Asynchronous execution mitigates this bottleneck by overlapping policy inference with action execution, yet the next action chunk is still predicted from stale observations while the robot continues to move. Direct chunk stitching therefore introduces handoff discontinuities, action jitter, and failures in contact-rich manipulation. Existing remedies typically require either full-policy retraining or architecture-specific runtime logic. This work proposes Action ControlNet (ACNet), a lightweight delay-aware adapter that uses the executed motion suffix as a residual condition for a mostly frozen action head. ACNet leaves the pretrained backbone unchanged, introduces few trainable parameters, and remains compatible with generative action heads such as diffusion and flow matching. On Kinetix, Meta-World MT50, and a real-world SO-ARM101 platform, ACNet improves robustness under inference delay and yields smoother asynchronous trajectories than direct chunk stitching, while remaining more lightweight than full delay-conditioned retraining.

---

## 论文详细总结（自动生成）

# 论文总结：Action ControlNet: A Lightweight Delay-Aware Adapter for Smooth Asynchronous Control in Vision-Language-Action Models

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：视觉-语言-动作模型（VLA）在通用机器人操作中表现优异，但推理延迟较大，导致传统同步部署（推理-执行-等待）中频繁出现“停顿-前进”的断续运动，拖长任务完成时间。
- **异步执行的困境**：为消除空闲等待，可采用异步执行（推理与执行重叠），但新的动作块仍基于过时的观测预测，而机器人已运动了一段距离。直接拼接前后动作块会在交界处产生动作不连续、抖动，尤其在接触密集型操作中导致失败。
- **现有方法不足**：现有解决方案要么需要完全重训练策略（成本高、破坏预训练能力），要么依赖特定架构的运行时逻辑（如补充插值、校正头），缺乏通用、轻量的即插即用方案。
- **本文动机**：将异步延迟下的VLA控制问题视为**局部手部交接问题**（而非全局任务理解失败），提出一种轻量级延迟感知适配器ACNet，仅修改动作头部分，保留大部分预训练骨干，实现平滑的异步轨迹。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将已执行的运动后缀（delay action）作为残差条件，注入到大部分冻结的动作头中，作为局部边界校正。
- **问题形式化**：将VLA策略分解为感知语言骨干$B_\omega$和动作头$A_\psi$。异步延迟导致新块的第一可执行动作$a^{(d)}_t$应与上一块的最后已执行动作$a^{(e+d-1)}_{t-e}$连续。定义延迟动作为$d$步已执行动作序列$adelay_t$，填充到完整长度$H$后形成$\widetilde{adelay}_t$。目标是最小化延迟控制风险：结合预测损失$L_{pred}$和边界一致性损失$L_{bd}$。
- **ACNet架构**：
  - **延迟动作编码**：对$\widetilde{adelay}_t$使用轻量Transformer编码器$E_\phi$，并应用与原始动作头相同的终端时序池化，得到条件向量$c_t$。
  - **残差注入**：仅在动作头的最后几个块（默认仅最后一层）将$c_t$通过投影层$Z_{\phi,l}$以残差方式加到隐藏状态$h_l$上：$h'_l = h_l + Z_{\phi,l}(c_t)$。这样仅需少量可训练参数（约占总参数的20%）。
  - **填充策略**：未观察到的未来位置使用可学习填充令牌（learnable padding tokens）。
- **训练目标**：针对流匹配动作头，训练目标为流匹配损失$L_{FM}$，采样延迟$d$、流时间$\tau$、目标块$x_0$和初始噪声$z$，其中速度预测器$v^{ACNet}_{\psi,\eta}$依赖条件$c_{t,d}$。感知语言潜变量可缓存复用，避免每步重算。
- **即插即用**：兼容扩散策略和流匹配等生成式动作头，无需修改骨干网络。

## 3. 实验设计：数据集、基准、对比方法

- **仿真环境**：
  - **Kinetix**（基于RTC协议）：包含多种物理操作任务，评估推理延迟$d \in \{0,1,2,3,4\}$。
  - **Meta-World MT50**：50个连续操作任务，评估延迟$d \in \{0,5,10,15\}$。
- **真实平台**：SO-ARM101机器人臂，包含“将蓝色立方体放入盒子”和“清洁桌面”两个任务，各10次试验。
- **对比方法**：
  - **Naïve Async**：直接拼接动作块。
  - **RTC**：冻结已提交动作，屏幕填充剩余部分。
  - **Training-RTC**：延迟条件重训练（完全更新所有参数）。
  - **ACNet**：本文方法。
- **评价指标**：任务成功率、训练参数比例、端到端推理延迟、控制频率、轨迹平滑度（通过步进jerk衡量）。

## 4. 资源与算力

- 文中未明确说明使用的GPU型号、数量和具体训练时长。
- 仅提及Meta-World实验运行在**单张RTX 4080 SUPER**上。
- Kinetix实验中，基线（RTC、Training-RTC）基于π0策略训练32/24+8 epochs；ACNet基于Evo-1，stage1训练1 epoch，stage2训练24 epochs，冻结骨干后ACNet和池化层额外训练8 epochs。但未给出具体小时数。

## 5. 实验数量与充分性

- **Kinetix**：5种延迟设置，对比4种方法，报告成功率平均值和参数量。3种方法各有一个基线，共4种方法×5延迟=20组主要结果。
- **Meta-World MT50**：50个任务，4种延迟，报告平均成功率和延迟/频率。加上jerk曲线分析（两个任务示例）。共约50×4=200组子任务×延迟组合，统计充分。
- **消融实验**（Meta-World上）：
  - 注入位置：对比仅最后块、前6层、全层、仅第0层。结果支持仅最后块最佳。
  - 填充方式：对比可学习填充、零填充、噪声填充。结果支持可学习填充。
- **真实实验**：2个任务×10次=20次试验，成功率对比。
- **总体评价**：实验覆盖了仿真多任务、多延迟、真实平台，消融实验直接回答了设计选择。但缺乏不同骨干网络（如将ACNet加到π0骨干）的对比实验；延迟范围有限（最大15步）；未测试更长的异步执行间隔或动态延迟。实验充分性良好，但存在一定的骨干依赖偏差。

## 6. 论文的主要结论与发现

- ACNet在Kinetix上平均成功率0.79，接近Training-RTC的0.80，显著优于Naïve Async（0.61）和RTC（0.72），但仅更新约20%参数。
- 在Meta-World MT50上，ACNet平均成功率0.74，匹配Training-RTC，且推理延迟91ms（频率11.0Hz）低于RTC（159ms/6.28Hz）和Training-RTC（134ms/7.46Hz）。
- 真实实验：ACNet双任务成功率20/20（100%），Naïve Async 17/20（85%），且轨迹抖动更小。
- 消融表明：将条件注入动作头最后一块最有效；可学习填充优于零或噪声填充。
- 核心结论：异步延迟可通过轻量级边界条件校正有效缓解，无需全策略重训练。

## 7. 优点

- **轻量高效**：只更新动作头一小部分参数（约20%），保留预训练骨干，大幅降低适应成本。
- **即插即用**：与扩散、流匹配等生成式动作头兼容，无需修改骨干网络或运行时逻辑。
- **理论清晰**：将问题表述为局部边界交接，提供了明确的形式化定义和边界损失。
- **实验设计严谨**：涵盖仿真多任务、多延迟、真实平台；消融实验验证了关键设计。
- **实际表现优秀**：在延迟较大时仍保持接近全重训练的水平，且达到更高控制频率。

## 8. 不足与局限

- **骨干依赖**：ACNet与基线的骨干网络不同（ACNet用Evo-1，基线用π0），性能提升部分归因于Evo-1的轻量性。未在统一骨干下对比，因此无法精确分离ACNet自身贡献与骨干优势。
- **延迟范围有限**：最大延迟仅为15步（Meta-World）或4步（Kinetix），未测试更大延迟（如d > H/2）或动态变化的延迟。
- **未考虑感知延迟**：仅针对动作头，未处理观测本身可能过时的问题（例如视觉处理延迟）。
- **无跨架构实验**：仅测试了流匹配动作头，未验证对扩散策略、显式动作建模等其他生成头的通用性（理论上兼容，但缺少实验）。
- **计算细节缺失**：缺乏训练时间、GPU型号、参数量具体数值（仅给出比例），复现困难。
- **真实实验规模小**：仅两个任务各10次试验，统计意义有限；未包含失败案例分析。
- **可能存在的过拟合风险**：ACNet在训练时采样多个延迟使感知潜变量复用，但若真实部署的延迟分布与训练分布不匹配，效果可能下降。

（完）
