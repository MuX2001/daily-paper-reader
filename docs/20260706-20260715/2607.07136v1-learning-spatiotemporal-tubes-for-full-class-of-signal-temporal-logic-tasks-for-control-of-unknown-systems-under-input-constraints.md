---
title: Learning Spatiotemporal Tubes for Full Class of Signal Temporal Logic Tasks for Control of Unknown Systems under Input Constraints
title_zh: 面向输入约束下未知系统全类信号时序逻辑任务的时空管学习
authors: "Ahan Basu, Ratnangshu Das, Soumyodipta Nath, Siyuan Liu, Pushpak Jagtap"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07136v1"
tags: ["query:control"]
score: 9.0
evidence: 针对未知非线性系统的控制框架，满足信号时序逻辑规范
tldr: 针对未知非线性欧拉-拉格朗日系统在输入约束下满足信号时序逻辑规范的问题，提出基于时空管道的控制框架。每个智能体对应一个时变球管道，其球心和半径由物理信息神经网络联合参数化，并通过将STL鲁棒性纳入损失函数训练学习。设计闭式控制律确保轨迹保持在管道内，同时依据输入约束调整管道演化，从而保证局部和全局STL任务满足。在多智能体场景中引入额外全局鲁棒性度量避免管道碰撞。案例研究验证了方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07136v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1632, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2607.07136v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1294, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2607.07136v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1126, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2607.07136v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1620, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2607.07136v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1548, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2607.07136v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1377, \"height\": 481}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07136v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1505, \"height\": 300}, {\"url\": \"assets/tables/arxiv/2607.07136v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1658, \"height\": 230}, {\"url\": \"assets/tables/arxiv/2607.07136v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 821, \"height\": 315}]"
motivation: 现有方法难以同时处理未知动力学、输入约束和时序逻辑任务，缺乏可学习的保证性控制框架。
method: 用物理信息神经网络参数化时空管道，以STL鲁棒性为损失训练；多智能体引入全局鲁棒性度量，并提出考虑输入约束的闭式控制律。
result: 多个案例成功学习并控制轨迹满足STL规范，验证了框架的有效性和鲁棒性。
conclusion: 提出一种结合学习和控制的新方案，为未知系统在输入约束下的时序逻辑任务提供了理论保证和实用方法。
---

## 摘要
本文提出了一种基于时空管（STT）的控制框架，用于一般未知非线性欧拉-拉格朗日（EL）系统，该系统受输入约束，目标是满足信号时序逻辑（STL）规范，其中系统轨迹限制在STT内保证了相应STL任务的满足。对于单智能体和多智能体场景，每个智能体对应的STT被建模为一个时变球，其中心和半径使用物理信息神经网络（PINN）联合参数化。与智能体对应的STL规范相关的鲁棒性度量作为损失函数纳入训练过程，使得学习到的管能够编码任务级的时间要求。对于多智能体场景，我们引入了与全局任务对应的额外鲁棒性度量，当满足该度量时，确保管之间不会相互碰撞。为了确保系统轨迹保持在学习的STT内，从而满足局部和全局STL规范，我们提出了一种明确考虑输入约束的控制策略。特别地，开发了一种闭环控制律，使轨迹保持在管内部，同时通过根据系统输入约束对管的演化施加边界来调节管的运动。所提出的方法在多个案例研究中得到验证。

## Abstract
This paper presents a Spatiotemporal Tube (STT)-based control framework for general unknown nonlinear Euler-Lagrange (EL) systems subject to input constraints, with the objective of satisfying Signal Temporal Logic (STL) specifications, where confinement of the system trajectory within the STT guarantees the satisfaction of the corresponding STL task. For both single and multi-agent scenarios, the STT corresponding to each agent is modeled as a time-varying ball, whose center and radius are jointly parameterized using a physics-informed neural network (PINN). The robustness metric associated with the STL specification corresponding to the agents is incorporated into the training process as a loss function, enabling the learned tube to encode task-level temporal requirements. For a multi-agent scenario, we introduce an additional robustness metric corresponding to the global task, which, when satisfied, ensures the tubes do not collide with each other. To ensure that the system trajectory remains within the learned STT and thereby satisfies the local and global STL specifications, we propose a control strategy that explicitly accounts for input constraints. In particular, a closed-form control law is developed to keep the trajectory inside the tube while regulating the motion of the tube by enforcing bounds on its evolution depending on the input constraints of the system. The proposed approach has been validated over several case studies.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

现代控制应用（如机器人、无人车）需要满足随时间变化的任务规范，例如在指定时间内到达目标、避开动态障碍等。信号时序逻辑（STL）提供了形式化描述这类任务的语言，并带有鲁棒性度量。然而，将STL规范转化为控制律面临严峻挑战：现有方法要么依赖于精确系统模型（如混合整数规划、模型预测控制、控制屏障函数），要么只能处理STL的子片段（如规定性能控制），要么因状态空间抽象而遭受维度灾难（符号控制）。尤其对于未知非线性系统（如欧拉-拉格朗日系统）、存在输入约束和外部扰动的情况，尚无同时满足“全类STL规范”、“未知动力学”、“输入约束”、“闭环控制器”、“可学习”和“微秒级在线计算”的完整框架。本文旨在填补这一空白。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
将STL满足问题转化为几何约束满足问题：构建一个时空管（Spatiotemporal Tube, STT），即一个时变球形区域 Γ(t) = B(c(t), r(t))，使得任何被限制在这个管内的轨迹都必然满足STL规范。然后设计控制器使系统状态保持在管内，从而无需直接处理STL逻辑，大大简化了控制设计。

### 2.2 关键技术细节

#### (a) 物理信息神经时空管（PINSTT）
- 用物理信息神经网络（PINN）参数化管心和管半径：c(t; Υ), r(t; Υ)。
- 训练数据：从增强时空域 W = [0,2π]^(n-1)×[0,1]×[0,t_f] 采样M个点，覆盖整个连续域（通过ε覆盖）。
- 损失函数包含四个子损失：
  - *L1*：确保管半径大于用户定义下界 r_d（即 –r(t) + r_d ≤ η，η≤0）。
  - *L2*：确保管内任意点的STL鲁棒性为正（即 –ρ_ϕ(y) + δ – η ≤ 0，y = c + λ r s(θ)）。
  - *L3*：限制管心变化速率：‖ċ(t;Υ)‖ ≤ L_c。
  - *L4*：限制半径变化速率：|ṙ(t;Υ)| ≤ L_r。
- 训练后通过定理3.3给出连续域上的形式化验证条件：若 ˆη + L ε ≤ 0（L由Lipschitz常数决定），则整个连续域的管有效。

#### (b) 闭环控制律设计（两阶段）
- **Stage I – 期望速度设计**：  
  v_r(t) = – (x(t)–c(t))/‖x(t)–c(t)‖ · Ψ(e_x) · v  
  其中 e_x = ‖x–c‖/r 是归一化位置误差，Ψ是有界变换函数（如tanh）。
- **Stage II – 加速度级控制**：  
  定义速度跟踪误差 e_v = v – v_r，用指数衰减漏斗约束：–γ_v(t) ≺ e_v ≺ γ_v(t)。  
  控制输入 τ(t) = – diag(Ψ(ε_v)) τ_max，ε_v = γ_v^{-1} e_v。  
  该控制器为闭式、不依赖模型、有界。

#### (c) 可行性条件
- **速度条件**：v ≥ Γ + ‖p_v‖，其中 Γ = L_c + L_r，p_v为漏斗初始宽度。
- **力矩条件**：τ_max ≥ (1/m)[max(–V̲_M, V̄_M) + m_i w + μ_v(p_v – q_v) + a_r]  
  这些条件确保系统有足够能力跟踪管道演化并抵抗扰动。

> **算法流程**（Algorithm 1）：  
> 1. 初始化PINN参数Υ，超参数L_c, L_r，计算Lipschitz常数L，设置ˆη = –Lε。  
> 2. 迭代训练，计算四个子损失加权和，用Adam/SGD优化。  
> 3. 若损失接近0则停止；若停滞则提示“规范无法在最大控制下实现”。  
> 4. 返回训练好的STT Γ(t; Υ)。

### 2.3 多智能体扩展
- 定义全局鲁棒性度量（如保持编队形状、避免智能体间碰撞），将其作为额外损失项纳入联合训练。
- 每个智能体独立应用相同的控制器，但管道参数共享全局任务信息。

## 3. 实验设计

### 3.1 实验场景与数据集
论文进行了四组案例研究（均为仿真，其中第一组有硬件实验）：

| 场景 | 维度 | 系统类型 | 任务描述 |
|------|------|----------|----------|
| **Omnidirectional Robot** | 2D | 移动机器人（Agile LIMO） | 巡航任务：从起点S出发，重复访问目标T1、T2，最终到达G，全程避障。时间窗口60-80s。有硬件实验（标称和1kg负载）。 |
| **Quadrotor** | 3D | 无人机 | 任务：从S出发，10秒内到达T1或T2，19-20秒到达G，全程避障。 |
| **Franka Research 3 Manipulator** | 7-DOF（任务空间） | 机械臂 | 顺序到达任务：在S、T1、T2、T3之间循环，120秒内完成。测试了标称、附加负载、外部扰动三种情况。 |
| **Multi-agent** | 2D × 4 agents | 全向机器人编队 | 四机菱形编队保持，从起点转场到对角目标，同时避障和保持编队结构。 |

### 3.2 基准方法对比
论文在**两个基准STL任务（stlcg-1, stlcg-2）及其简化版（stlfrag-1, stlfrag-2）** 上与以下方法定量比较：
- **RRT***（仅定性提及）
- **MPC**[24]
- **CBF-based**[16]
- **PPC（Prescribed Performance Control）**[12]
- **MILP**[25]
- **Previous STT**[4]

对比指标：
- 是否支持全类STL、形式化保证、未知动力学、有界扰动、输入约束、在线优化（定性，表1）。
- **计算时间**（表2）：离线+在线时间。
- **最大控制输入**（表3）：标称和扰动情况。

### 3.3 实验充分性与公平性
- 实验覆盖了2D、3D、7-DOF、多智能体等多种系统，包含硬件验证（LIMO机器人）。
- 对每个场景都给出了STL规格文本和轨迹图。
- 对比了五种主流方法在计算时间和控制努力上的差异，结果客观。
- 消融实验：未单独做，但通过不同负载和扰动测试了鲁棒性；通过多智能体验证了扩展性。
- **不足**：对比方法没有全部在完全相同硬件条件下运行（不同论文代码可能不同），但作者直接引用了其他文献的结果进行计算时间对比，存在一定公平性风险。不过定性比较表格（表1）是清晰的。

## 4. 资源与算力

**文中说明**：  
> “All experiments were conducted in PyTorch (Python 3.10) on a Windows machine with an Intel Core i7-14700 CPU, 32 GB RAM, and an NVIDIA GeForce RTX 3080 Ti GPU.”

- GPU型号：NVIDIA GeForce RTX 3080 Ti。
- CPU：Intel Core i7-14700。
- 内存：32 GB。
- 训练时间：论文给出了各场景的离线PINSTT训练时间（如Omnidirectional Robot 51.410秒，Quadrotor 5.284秒，多智能体59.5秒），以及在线控制合成时间（微秒级）。
- 但未说明训练轮数（epochs）或批量大小等详细超参数，也未报告GPU使用数量（显然只有一块）。

## 5. 实验数量与充分性

- **实验组数**：共4个主要案例，每个案例包括多个子实验（负载变化、扰动、多智能体）。具体：
  - 全向机器人：仿真 + 硬件实验（标称、负载）。
  - 无人机：单一仿真。
  - 机械臂：标称、负载、扰动三种情况。
  - 多智能体：单一仿真。
- **定量基准对比**：使用了4个基准任务（stlcg-1/2, stlfrag-1/2），对比了5种方法。
- **充分性评价**：多个领域（移动机器人、无人机、机械臂、编队）验证了泛化能力；硬件实验增加了可信度；对比实验覆盖了主流方法。但缺少以下内容：
  - 大规模随机种子重复实验（未报告均值和方差）。
  - 对超参数（如L_c, L_r, 漏斗参数）的敏感性分析。
  - 与最新的基于学习的STL控制器（如BarrierNet [17]）对比。

总体而言，实验设计合理、场景多样，但消融分析和统计重复略有欠缺。

## 6. 主要结论与发现

1. **提出的PINSTT框架能够为未知EL系统学习有效的时空管，且保证连续时间域上的STL满足**（定理3.3）。
2. **结合有界闭式控制律（Stage I + II）可在输入约束下将系统轨迹限制在管内**（定理4.2）。
3. **多智能体扩展通过联合损失函数融入全局鲁棒性，成功实现编队保持和避障**。
4. **计算效率高**：离线训练秒级，在线控制微秒级，远快于MILP、MPC和CBF方法。
5. **控制输入有界性得到保证**，即使在扰动下也未超过预设极限，而此前STT方法在扰动下控制输入会显著增大（表3）。

## 7. 优点

- **全类STL支持**：不限于子片段，覆盖所有布尔和时序算子。
- **形式化保证**：通过Lipschitz分析和ε覆盖，提供连续域验证（定理3.3）。
- **未知动力学 + 有界扰动**：无需系统模型，仅需已知参数上界。
- **输入约束显式处理**：闭式控制器天然满足有界性，无需在线优化。
- **可学习性**：使用PINN自动学习最佳管形状，无需手动指定模板，表达力更强。
- **多智能体可扩展**：统一框架下处理局部和全局任务。
- **硬件验证**：在真实机器人上验证了鲁棒性。

## 8. 不足与局限

- **假设较强**：需要预先知道系统参数的界（Assumptions 1-4），以及管子的Lipschitz常数L_c, L_r的选择依赖对系统速度/力矩边界的知晓。
- **实验覆盖不足**：虽有多场景，但缺乏大规模统计实验（如不同随机种子多次运行）；未进行超参数敏感性分析。
- **基准对比公平性**：表2中的对比方法时间来自文献，可能运行在不同硬件/软件环境；且未在同一代码库下复现对比。
- **STL任务相对简单**：所有任务都是有限时间、静态障碍、已知区域。动态环境和连续避障未测试。
- **多智能体规模**：只有4个智能体，未测试更大规模（如10+）的可扩展性。
- **未分析神经网络训练的不确定性**：由于PINN训练的非凸性，不同初始化可能影响管质量，文中未讨论多次训练的成功率或可复现性。
- **硬件实验仅一个平台**：LIMO机器人是差分驱动，未在更复杂系统（如四轴飞行器）上验证硬件。

（完）
