---
title: "Sparse Robust Optimal Control in Continuous-Time: A Computationally Viable Approach"
title_zh: 连续时间稀疏鲁棒最优控制：一种计算可行的方案
authors: "Siddhartha Ganguly, Ashwin Aravind, Souvik Das, Masaaki Nagahara, Debasish Chatterjee"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11827v1"
tags: ["query:control"]
score: 9.0
evidence: 连续时间稀疏鲁棒最优控制的数值可行算法
tldr: 针对连续时间约束线性噪声系统的稀疏鲁棒最优控制问题，传统方法难以处理无穷多约束和计算可行性。该文将问题建模为半无限规划（SIP），并首次提出一种计算可行的无损转化方法：通过构造一个有限维凸优化问题，能够精确恢复原始SIP的最优值和优化器，同时确保满足无穷多约束。算法还可推广到含参数噪声系统和最小注意力问题。数值算例验证了该框架的有效性和高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11827v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11827v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1873, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11827v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 746, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11827v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1872, \"height\": 537, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11827v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 1602, \"label\": \"Table\"}]"
motivation: 现有稀疏最优控制方法难以同时处理连续时间、系统噪声和无穷多约束，缺乏精确且计算可行的求解算法。
method: 将问题转化为半无限规划（SIP），并构造一个等价的有限维凸优化问题，实现无损求解。
result: 首次提出能精确计算稀疏鲁棒最优控制问题的算法，保证满足无穷多约束，且计算复杂度可行。
conclusion: 该框架可推广至噪声系统和最小注意力问题，数值实验表明其有效性和通用性。
---

## 摘要
本文提出了一种新颖的、数值可行的算法，用于求解连续时间中的稀疏鲁棒最优控制问题。我们考虑一个由常微分方程（ODE）控制的带约束线性噪声系统，目标函数为L1型，符合稀疏最优控制文献。所得的最优控制问题被证明可以转化为半无限规划（SIP）形式。基于这一洞见，我们开发了一个新框架，能够计算精确解——据我们所知，这是稀疏最优控制领域中的首个此类成就。我们证明，求解一个有限且计算可行的凸优化问题可以无损地恢复原始SIP的最优值和相应的优化器，同时确保满足无限多个约束。我们还展示了参数依赖的噪声系统和最小注意问题属于我们的框架，并且可以通过我们的算法高效求解。通过一个基准数值例子说明了我们算法的有效性。

## Abstract
This article presents a novel, numerically viable algorithm for solving sparse robust optimal control problems in continuous time. We consider a constrained linear noisy system governed by an ordinary differential equation (ODE), with an $L^1$-type objective function in line with the sparse optimal control literature. The resulting optimal control problem is shown to admit a semi-infinite programming (SIP) formulation. Building upon this insight, we develop a new framework that enables the computation of exact solutions -- to our knowledge, the first such achievement in the context of sparse optimal control. We demonstrate that a finite and computationally viable convex optimization problem can be solved to recover, in a lossless manner, both the optimal value and the corresponding optimizers of the original SIP, while also guaranteeing satisfaction of uncountably many constraints. We also show that the parameter-dependent noisy systems and the minimum attention problem fall into our framework and can be solved efficiently via our algorithm. The efficacy of our algorithm is illustrated through a benchmark numerical example.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现实中的动态系统常含有不确定性（如过程噪声、参数漂移），而现有稀疏最优控制方法大多仅适用于确定性环境，难以同时处理连续时间、系统噪声和无穷多约束（状态、控制、终端约束对所有时间和所有扰动必须同时满足）。  
- **核心问题**：如何设计一种数值可行的算法，求解连续时间线性噪声系统的稀疏鲁棒最优控制问题（目标为 $L^1$ 范数），在保证约束对无穷多不确定性实现都满足的前提下，精确获得最优解（最优值和优化器）。  
- **挑战**：问题本身是无限维的，且约束集不可数；直接离散化虽然简单，但无法保证约束在整个时间区间和所有扰动下成立，更无法提供鲁棒性保证。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：  
  1. 将原无限维最优控制问题转化为**凸半无限规划（CSIP）**——控制/扰动轨迹通过有限基函数参数化（如分段常数函数），问题变为有限维决策变量但依然有**不可数约束**。  
  2. 证明存在一个**有限维凸优化问题**（通过对约束索引集进行“目标采样”）可以与原CSIP**无损等价**：即最优值和优化器都能精确恢复，同时确保无穷约束被满足。

- **关键技术细节**：  
  - **控制/扰动参数化**：使用有限字典 $\{\psi_i\}$ 和 $\{\phi_j\}$（分段常数函数）对控制 $u(t)$ 和扰动 $w(t)$ 进行线性参数化（$u(t)=\theta\Psi(t), w(t)=\gamma\Phi(t)$）。  
  - **正则化**：引入严格凸正则项 $\varepsilon\Upsilon(\theta)$ 以保证优化器唯一且收敛。  
  - **松弛与全局优化**：构造松弛问题 $F(\bar{x},\varepsilon; T,B)$（仅对 $e_N$ 个采样时间点和扰动参数施加约束），然后通过全局优化（如模拟退火）求解 $\sup_{T,B} F(\bar{x},\varepsilon; T,B)$。  
  - **理论保证**：在严格可行性假设下，证明 $F$ 的连续性，并利用[23][24]的定理得出：该 “max-min” 形式的最优值等于原CSIP的最优值（即无损恢复），且正则化参数 $\varepsilon\rightarrow0$ 时优化器收敛到原问题的最优解。

- **算法流程（SparseRob$( \bar{x}, \varepsilon)$ 架构）**：  
  1. 初始化约束索引 $(\mathcal{T}_0,\mathcal{B}_0)$。  
  2. 外层循环：更新正则化参数 $\varepsilon$。  
  3. 内层循环：使用全局优化（如模拟退火）在 $[0,T]^{e_N}\times\mathcal{S}_w^{e_N}$ 上采样 $(\mathcal{T}^{j,k},\mathcal{B}^{j,k})$，求解对应的有限维凸优化问题（即 $F$ 的值），并记录当前最优。  
  4. 当满足停止准则时输出最终 $\theta$ 和 $\gamma$。

### 3. 实验设计：使用的数据集/场景、基准、对比方法

- **场景**：弹簧‑质量‑阻尼器系统（连续时间线性系统），分为三种情形：  
  - **情形A**：仅有外部扰动（无参数不确定性），状态约束 $|x_1|\le 0.7$，终端约束 $|x_1|\le0.3, |x_2|\le0.3$，扰动范围 $[-0.4,0.4]^2$。  
  - **情形B**：含参数不确定性（$A,B$ 矩阵中的参数在 $[-0.1,0.1]^3$ 内变化），无外部扰动。  
  - **情形C**：鲁棒最小注意力问题（控制速率 $|\dot{u}(t)|$ 有界，目标为 $\int_0^T|\dot{u}(t)|dt$）。  
- **基准**：未使用公开数据集；以自身理论为基准，对比非鲁棒控制和场景优化（Scenario Optimization）方法（1000样本和5000样本）。  
- **对比方法**：  
  - 非鲁棒稀疏最优控制（不处理扰动）——导致大量约束违背。  
  - 场景优化（1000和5000个随机扰动样本）——大幅减少违背但仍有残留，且无严格保证。  
- **实验环境**：Julia v1.1.14，AMD Ryzen 5 5600H（6核12线程），8GB RAM。

### 4. 资源与算力

- 文中**未明确报告GPU型号、使用数量或训练时长**。  
- 仅说明实验在笔记本上运行，CPU为AMD Ryzen 5 5600H，内存8GB。  
- 由于算法涉及内层全局优化（模拟退火）和每次采样后求解凸优化，实际计算时间可能较长，但论文未给出具体耗时数据。

### 5. 实验数量与充分性

- **实验数量**：共3类场景，每类场景展示1‑2张结果图（控制轨迹、状态轨迹）。  
- **充分性评价**：  
  - 覆盖面：涵盖了基本鲁棒情形、参数不确定性情形、最小注意力情形，验证了方法的通用性。  
  - 缺少：无消融实验（如不同字典大小 $N$、不同正则化策略、不同全局优化初值的影响）；未与其他成熟的鲁棒控制（如$H_\infty$、Tube‑MPC）进行定量比较；未报告蒙特卡洛重复次数（状态轨迹虽用了10000次扰动实现，但算法本身只运行一次）。  
  - 客观性：对比了非鲁棒和场景优化，展示了方法在满足约束上的优势，但缺少标准 benchmark 库支持，力度有限。

### 6. 论文的主要结论与发现

- **理论贡献**：首次提出能**精确计算**连续时间稀疏鲁棒最优控制问题的算法，可**无损恢复**半无限规划的最优值和优化器，且保证满足无穷多约束。  
- **方法可推广**：该框架自然推广到含参数不确定性的噪声系统以及鲁棒最小注意力问题。  
- **数值验证**：在弹簧‑质量‑阻尼器系统上，本文方法**99.9%** 的扰动实现下状态/终端约束均满足，远优于非鲁棒和场景优化方法。  
- **稀疏性体现**：控制输入在大部分时间内保持零值（或常值），仅在约束边界附近激活，符合稀疏控制预期。

### 7. 优点：方法或实验设计上的亮点

- **理论上彻底**：放弃了常见的保守近似或概率保证，实现了**精确解**，且对无穷约束有确定性保证——这是稀疏最优控制领域的首次。  
- **框架通用**：可处理状态/控制/终端约束、过程噪声、参数不确定性，并拓展到最小注意力问题。  
- **数值可行**：通过有限维凸优化与全局优化相结合，避免直接处理无穷维问题。  
- **算法结构清晰**：提供通用的 SparseRob 架构，允许用户根据问题性质选择不同的全局优化算法（模拟退火、LIPO、SequOOL等）。  
- **对比公平**：在相同条件下与非鲁棒和场景优化对比，突出了本方法的鲁棒性优势。

### 8. 不足与局限

- **实验覆盖有限**：仅测试了一个基准线性系统（弹簧‑质量‑阻尼器），未涉及高阶系统、非线性系统或真实硬件在环测试。  
- **缺失消融与敏感性分析**：未探讨字典大小 $N$、正则化参数 $\varepsilon$ 衰减策略、全局优化收敛阈值等对结果的影响。  
- **计算效率未量化**：未报告算法运行时间或与现有方法（如直接离散化、有限元）的计算复杂度对比，无法判断其在大规模系统中的实际可行性。  
- **数值误差残留**：实验中仍有约 0.1% 的扰动实现导致约束违背，作者将其归结为数值舍入误差，但未给出解决方案（如加入安全余量）。  
- **未与主流鲁棒控制方法对比**：如 Tube‑MPC、$H_\infty$ 控制、鲁棒模型预测控制等，无法体现该方法相对于已有经典方法的优劣。

（完）
