---
title: "Harness VLA: Steering Frozen VLAs into Reliable Manipulation Primitives via Memory-Guided Agents"
title_zh: 驾驭VLA：通过记忆引导代理将冻结的视觉-语言-动作模型导向可靠的操作原语
authors: "Yixian Zhang, Huanming Zhang, Feng Gao, Xiao Li, Zhihao Liu, Chunyang Zhu, Jiaxing Qiu, Yuchen Yan, Jiyuan Liu, Wenhao Tang, Zhengru Fang, Yi Nie, Changxu Wei, Yu Wang, Wenbo Ding, Chao Yu"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08448v2"
tags: ["query:vla"]
score: 9.0
evidence: 利用记忆增强智能体框架将冻结的视觉语言动作模型引导为可靠操作原语
tldr: 语言条件操作面临接触控制与语义推理的权衡，现有VLA模型在部署扰动下易失败，而纯分析基元则难以处理复杂交互。Harness VLA通过记忆增强的代理框架将冻结的VLA暴露为可重试的接触基元，与固定分析基元库组合，利用执行轨迹学习操作范围而不微调。在LIBERO-Pro、RoboCasa365和RoboTwin C2R上分别提升38.6、25.4和58.4个百分点，显著增强VLA的鲁棒性和泛化能力。
source: arxiv
selection_source: fresh_fetch
motivation: 端到端VLA模型在分布偏移下脆弱，而纯分析基元缺少接触灵活性，需结合两者优势。
method: 构建记忆增强的代理框架，将冻结VLA作为可重试接触基元，与固定分析基元组合，并学习执行轨迹的成功规则与失败模式。
result: 在LIBERO-Pro、RoboCasa365和RoboTwin C2R上，分别提升38.6、25.4和58.4个百分点，超越最相关基线。
conclusion: 无需微调即可扩展预训练VLA到新分布，为语言条件操作提供可靠且可组合的解决方案。
---

## 摘要
语言条件操作需要精确的接触丰富控制以及对语言、场景和长时域的强大推理。端到端的视觉-语言-动作（VLA）模型提供了强大的局部视觉运动技能，但它们是在分布内任务轨迹上训练的，在部署扰动（如语义重定向、目标重新绑定、空间布局变化和不稳定的局部接触）下往往会失败。LLM编码代理提供了互补的语义和组合推理，但纯分析原语在应对不规则抓取、受限放置和铰接物体交互时存在困难。我们提出Harness VLA，一种记忆增强的代理框架，它将冻结的VLA暴露为可重试的接触丰富原语，并将其与一个由分析原语组成的小型固定库组合，用于接地、暂存、运输、导航和释放。该框架不是扩展技能库，而是从任务特定的执行轨迹、全局成功规则和失败模型中学习这些固定原语的操作范围。通过将语义重新接地、非接触执行和VLA重新暂存提升到规划器，同时将冻结的VLA保留用于局部接触丰富阶段，Harness VLA在无需微调的情况下将预训练的VLA扩展到其原始轨迹分布之外。在受干扰的桌面、家庭厨房以及从干净到随机化的双臂操作场景中，Harness VLA在LIBERO-Pro和RoboCasa365上分别比最强相关基线提高了38.6和25.4个百分点，并在RoboTwin C2R上达到了58.4%的性能。

## Abstract
Language-conditioned manipulation requires both precise contact-rich control and robust reasoning over language, scenes, and long horizons. End-to-end Vision-Language-Action (VLA) models provide strong local visuomotor skills, but they are trained on in-distribution task trajectories and often fail under deployment perturbations such as semantic retargeting, goal re-binding, spatial-layout shifts, and unstable local contacts. LLM coding agents provide complementary semantic and compositional reasoning, but purely analytic primitives struggle with irregular grasping, constrained placement, and articulated-object interaction. We present Harness VLA, a memory-augmented agentic framework that exposes a frozen VLA as a retryable contact-rich primitive and composes it with a small fixed library of analytic primitives for grounding, staging, transport, navigation, and release. Rather than expanding the skill library, the harness learns the operating range of these fixed primitives from task-specific execution traces, global success rules, and failure models. By lifting semantic re-grounding, non-contact execution, and VLA re-staging to the planner while reserving the frozen VLA for local contact-rich phases, Harness VLA extends pretrained VLAs beyond their original trajectory distribution without finetuning. Across perturbed tabletop, household kitchen, and clean-to-randomized bimanual manipulation, Harness VLA improves over the strongest relevant baselines by 38.6 and 25.4 percentage points on LIBERO-Pro and RoboCasa365, respectively, and reaches 58.4% on RoboTwin C2R.

---

## 论文详细总结（自动生成）

# Harness VLA：通过记忆引导代理将冻结的视觉-语言-动作模型导向可靠的操作原语

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心挑战**：语言条件操作需要同时具备精确的接触丰富控制能力，以及对语言指令、场景和长时域任务的强大推理能力。端到端的视觉-语言-动作（VLA）模型虽然提供强大的局部视觉运动技能，但其训练依赖于分布内的任务轨迹，在部署时容易因语义重定向、目标重新绑定、空间布局变化和不稳定的局部接触等扰动而失败。
- **现有局限**：
  - LLM编码代理（agent）提供互补的语义和组合推理，但纯分析原语在处理不规则抓取、受限放置和铰接物体交互时存在困难。
  - 直接微调VLA模型成本高且无法适应所有新分布。
- **研究动机**：提出一种无需微调、将冻结VLA与固定分析原语库结合的方法，利用记忆增强的代理框架，使VLA在分布外场景中仍能可靠执行接触丰富操作。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个记忆增强的代理框架（Harness VLA），将冻结的VLA模型暴露为可重试的接触丰富原语，并与一个由分析原语组成的小型固定库（用于接地、暂存、运输、导航和释放）组合。该框架不扩展技能库，而是从任务特定的执行轨迹中学习这些固定原语的操作范围（包括全局成功规则和失败模型）。
- **关键技术细节**：
  - **语义重新接地**：规划器负责将语言指令重新映射到当前场景中的实体，处理目标重新绑定和语义变化。
  - **非接触执行与VLA重新暂存**：规划器将非接触阶段（如导航、运输）提升到高级决策层，仅在需要局部接触丰富阶段时调用冻结VLA，并允许重试。
  - **记忆机制**：通过执行轨迹学习操作范围（成功规则和失败模式），从而在不微调VLA的情况下，自适应地调整原语调用策略。
  - **算法流程（文字描述）**：
    1. 输入语言指令和场景观察。
    2. 规划器首先使用分析原语执行接地、暂存、运输等非接触步骤。
    3. 当需要执行接触丰富操作（如抓取、放置）时，调用冻结的VLA作为可重试原语。
    4. 若VLA执行失败（通过学习到的失败模型检测），规划器重新调整执行步骤（如重新暂存或改变接触策略）并重试。
    5. 所有执行轨迹被记录，用于更新成功规则和失败模型，提升后续任务的成功率。
- **公式或算法**：文中未提供显式公式，核心逻辑为 **Memory-Augmented Agent (MAA)** 框架，将冻结VLA视为一个 **可重试接触基元（Retryable Contact Primitive, RCP）**，其余基元为固定分析基元。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：
  - **LIBERO-Pro**：受干扰的桌面操作场景（包含语义重定向、目标重新绑定、空间布局变化等扰动）。
  - **RoboCasa365**：家庭厨房场景。
  - **RoboTwin C2R**：双臂操作场景，从干净环境到随机化环境。
- **基准**：对比了“最强相关基线”（具体基线名称未在摘要中给出，推测为现有VLA模型或LLM编码代理方法，如RT-2、Octo等或基于规划的基线）。
- **评估指标**：任务成功率（百分比）。

## 4. 资源与算力

- **文中未明确说明**：在提供的摘要和元数据中，没有提及使用的GPU型号、数量、训练时长等算力信息。由于方法无需微调冻结VLA，仅需少量计算以运行代理推理和记录轨迹，推测其算力需求远低于端到端微调，但具体数值无法从给定内容获知。

## 5. 实验数量与充分性

- **实验数量**：共在三个独立数据集/场景上进行了评估，每个场景包含多种扰动条件（如语义、空间、接触稳定性等）。
- **充分性分析**：
  - **正面**：覆盖桌面、家庭厨房、双臂操作三种不同复杂度和接触需求的场景，且包含从干净到强随机化的分布偏移，具有较好的泛化性测试。
  - **客观公平**：对比了“最强相关基线”，但文中未具体列出基线名称和设置，无法完全判断是否公平。消融实验（如是否使用记忆机制、是否使用固定原语库等）未在摘要中提及，但可能存在未公开的完整论文中。
  - **局限性**：缺少对每个场景内多个具体任务的成功率细分、只报告整体提升百分点，也未进行统计显著性检验。实验数量相对较少，但考虑到场景的全面性，基本充分。

## 6. 论文的主要结论与发现

- **主要结论**：Harness VLA在无需微调的情况下，将预训练VLA扩展到其原始轨迹分布之外，显著提升了对部署扰动的鲁棒性。
- **定量结果**：
  - 在LIBERO-Pro上，比最强相关基线提升 **38.6个百分点**。
  - 在RoboCasa365上提升 **25.4个百分点**。
  - 在RoboTwin C2R上达到 **58.4%** 的成功率（原文为“reaches 58.4%”，可能表示相较于基线提升该绝对值，或最终性能）。
- **定性发现**：将语义重新接地、非接触执行和VLA重新暂存提升到规划器，保留冻结VLA用于局部接触丰富阶段，是可靠组合的关键。

## 7. 优点：方法或实验设计上的亮点

- **无需微调**：完全冻结预训练VLA，避免昂贵的重新训练和灾难性遗忘，可即插即用。
- **记忆增强代理**：通过学习操作范围（成功规则和失败模式）而非扩展技能库，使系统能够自适应地处理新分布，具有强泛化性。
- **可重试机制**：允许VLA失败后重新规划，结合分析原语提供稳定非接触操作，大幅提高成功率。
- **简洁高效**：固定原语库规模小（接地、暂存、运输、导航、释放），避免了过度工程化。
- **实验场景多样**：覆盖桌面、家庭、双臂操作，涵盖从简单到复杂的接触控制挑战。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅报告了三个基准的整体提升，缺少对各类扰动的单独消融分析（如仅语义变化、仅空间变化等）。
  - 未给出在不同规模VLA模型（如不同参数量）上的表现，泛化性结论受限于使用的具体VLA。
- **对比基线不透明**：“最强相关基线”未明确命名，读者无法直接复现或比较。
- **资源与算力缺失**：未报告任何计算开销，使得实际部署成本难以评估。
- **依赖固定原语库**：分析原语的覆盖范围可能在某些未见任务中不足（如复杂铰接物体交互），需要人工设计。
- **失败模式学习可能不完整**：仅从任务特定轨迹中学习，面对全新类型的失败可能无法应对，存在冷启动问题。
- **缺乏人体实验或真实机器人部署细节**：论文可能仅基于仿真环境，摘要未明确说明实际机器人平台（如是否在真实机器人上验证）。

（完）
