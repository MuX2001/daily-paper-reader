---
title: Uncertainty-Guided Sparse Refinement for Action Chunking Transformer Policies
title_zh: 面向动作分块 Transformer 策略的不确定性引导稀疏细化
authors: "Chenyang Wang, Yuntian Wang, Xiaoxiong Yang, Dingde Jiang, Siao Liu, Yang Liu"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15840v1"
tags: ["query:robot-learn"]
score: 9.0
evidence: 面向操作任务的动作分块视觉运动策略稀疏精修
tldr: "长时程机器人操作的动作分块视觉运动策略常因少数关键时间步预测误差而失败，而非整段动作均匀变差，导致均匀细化低效且缺乏针对性。为此，UGR提出由粗到细的稀疏细化框架：先预测完整动作块，从粗隐藏状态估计逐步时序不确定性，再用二值掩码仅对最不确定时间步施加残差修正。在RoboTwin五项双臂任务上，UGR在四项取得最佳成功率，较ACT基线最高提升13%绝对值。该工作将细化增益归因于不确定性引导修正，为动作分块策略提供更高效、精准的改进路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15840v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15840v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1877, \"height\": 1041, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15840v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1842, \"height\": 840, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15840v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1301, \"height\": 213, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15840v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 281, \"label\": \"Table\"}]"
motivation: 动作分块策略的失败常集中在少数关键时间步，均匀细化既低效又缺乏针对性。
method: UGR采用由粗到细框架，预测完整动作块并估计逐步时序不确定性，用二值掩码仅对最不确定时间步做残差修正。
result: "在RoboTwin五项双臂任务中四项最佳，较ACT基线最高提升13%绝对值，并优于全块与位置无关块细化。"
conclusion: 不确定性引导的稀疏细化能精准修复关键时间步误差，为动作分块策略提供高效改进方向。
---

## 摘要
学习用于长时程机器人操作的分块式视觉运动策略仍然具有挑战性。近期的动作分块方法通过预测时间上扩展的动作序列展现出良好性能。然而，其失败往往由少数关键时间步的预测误差主导，而非整个动作分块上均匀较差的预测，这使得均匀细化效率低下且针对性不足。为解决这一瓶颈，我们提出了不确定性引导细化（Uncertainty-Guided Refinement, UGR），一种面向分块式视觉运动策略的稀疏细化框架。具体而言，UGR 采用由粗到细的设计：首先预测完整的动作分块，从粗粒度隐藏状态估计每步的时间不确定性，并仅对由二值掩码选出的最不确定时间步施加残差校正。不确定性分支与粗动作预测器解耦，从而能够将细化收益清晰地归因于不确定性引导的校正，而非额外的预测器容量。在 RoboTwin 基准的五个双臂操作任务上的大量实验表明，UGR 在四个任务上取得最佳成功率，相较 ACT 基线绝对提升最高达 13%，并且在消融实验中优于全分块细化和位置无关的块细化。

## Abstract
Learning chunk-based visuomotor policies for long-horizon robot manipulation remains challenging. Recent action-chunking methods have shown promising performance by predicting temporally extended action sequences. However, their failures are often dominated by prediction errors at a small number of critical timesteps rather than uniformly poor predictions across the entire action chunk, making uniform refinement inefficient and insufficiently targeted. To address this bottleneck, we propose Uncertainty-Guided Refinement (UGR), a sparse refinement framework for chunk-based visuomotor policies. Specifically, UGR follows a coarse-to-refine design: it first predicts a full action chunk, estimates per-step temporal uncertainty from the coarse hidden states, and applies residual correction only to the most uncertain timesteps selected by a binary mask. The uncertainty branch is decoupled from the coarse action predictor, enabling clean attribution of the refinement gains to uncertainty-guided correction rather than additional predictor capacity. Extensive experiments on five dual-arm manipulation tasks from the RoboTwin benchmark show that UGR achieves the best success rate on four tasks, improves over the ACT baseline by up to 13% absolute, and outperforms both full-chunk and position-agnostic block refinement in ablation studies.

---

## 论文详细总结（自动生成）

# 论文总结：Uncertainty-Guided Sparse Refinement for Action Chunking Transformer Policies

## 1. 核心问题与整体含义
- **研究背景**：长时程机器人操作中的视觉运动模仿学习面临复合误差问题，小步预测误差会随长时程累积并将策略推入分布外状态。动作分块方法（如 ACT）通过预测时间上扩展的动作序列缓解该问题。
- **核心问题**：ACT 类分块策略的失败往往并非整段动作均匀变差，而是集中在少数关键时间步，例如接触、对齐、交接、机构触发等接触丰富过渡阶段。因此，对整段动作分块做均匀细化既低效，也缺乏针对性。
- **整体含义**：论文主张应把细化容量按时间步难度进行数据依赖的分配，只修正最不确定、最易出错的时间步，从而以更稀疏、更精准的方式提升分块式视觉运动策略的长时程操作成功率。

## 2. 方法论
- **核心思想**：提出不确定性引导细化（UGR），采用“由粗到细”框架：先由 ACT 预测完整粗动作分块，再估计每个时间步的时序不确定性，最后仅对最不确定的 top-ρK 时间步施加残差校正。
- **统一掩码形式**：
  - 粗动作分块为 \(A_0\)，残差为 \(\Delta A\)，二值掩码为 \(m\)，最终动作为 \(A_1 = A_0 + m \odot \Delta A\)。
  - Full refinement：\(m=1\)，细化全部时间步。
  - Block refinement：随机选择一个长度为 \(B=12\) 的连续块置 1，其余为 0。
  - UGR：按预测不确定性选择 top-\(\lfloor \rho K \rfloor\) 个时间步置 1，论文默认 \(\rho=0.3\)，\(K=50\)。
- **不确定性估计**：
  - 在 ACT 粗解码器隐藏状态 \(H_0 \in \mathbb{R}^{K \times d}\) 上接一个轻量线性不确定性头，输出每个时间步的标量 log-variance \(s_k\)。
  - 关键设计是使用 stop-gradient：\(s_k = \text{Linear}(\text{sg}(h_k^{(0)}))\)，使不确定性分支不直接更新粗动作预测器。
  - 掩码由 top-k 排序生成：选择 \(s_k\) 最大的 \(\lfloor \rho K \rfloor\) 个时间步。
- **训练目标**：
  - 粗预测损失：对粗动作分块做 MSE。
  - 细化损失：仅对掩码选中的时间步集合 \(S\) 做 MSE。
  - 不确定性损失：采用异方差回归负对数似然，鼓励粗误差大的时间步预测出更大的 \(s_k\)。
  - 另含 ACT 原有 CVAE 的 KL 正则项。
  - 总损失为 \(L = L_{\text{coarse}} + L_{\text{refine}} + \lambda_u L_{\text{uncert}} + \lambda_{KL} L_{KL}\)，实验中 \(\lambda_u=1\)，\(\lambda_{KL}=10\)。
- **实现细节**：不确定性头仅用于时间步选择，不进行关节级重加权；未选中时间步直接复制粗预测；细化分支复用 ACT 表示，无额外视觉编码。

## 3. 实验设计
- **数据集 / 场景**：使用 RoboTwin 基准中的五个双臂操作任务：handover block、pick dual bottles、hanging mug、open microwave、click alarmclock。采用 demo_clean 设置，每个任务 50 条演示，按 episode 级别 80% 训练、20% 验证。
- **输入与模型配置**：三个 RGB 相机视角：cam_high、cam_right_wrist、cam_left_wrist；机器人状态与动作维度均为 14；动作分块长度 \(K=50\)。ACT 骨干为 ResNet-18 视觉编码器加 Transformer，隐藏维度 512，前馈维度 3200，4 层编码器、7 层解码器、8 个注意力头。
- **对比方法**：
  - 主实验：ACT、Diffusion Policy（DP）、UGR。
  - ACT 家族消融：act_base、act_cmask（因果解码器掩码
