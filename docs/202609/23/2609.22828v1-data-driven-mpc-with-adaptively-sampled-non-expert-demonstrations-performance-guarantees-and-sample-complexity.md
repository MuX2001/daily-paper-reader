---
title: "Data-Driven MPC with Adaptively Sampled Non-Expert Demonstrations: Performance Guarantees and Sample Complexity"
title_zh: 基于自适应采样非专家演示的数据驱动MPC：性能保证与样本复杂度
authors: "Shijie Pan, Agustin Castellano, Enrique Mallada"
date: 2026-09-19
pdf: "https://arxiv.org/pdf/2609.22828v1"
tags: ["query:control"]
score: 9.0
evidence: 带性能保证的数据驱动模型预测控制
tldr: 针对用有限时域代理MPC轨迹逼近无限时域最优控制的问题，论文将轨迹视为非专家演示，提出基于记忆的非参数策略，构造有限时域代理值函数的Lipschitz正则化上包络，从而给出可计算性能证书且运行时无需显式优化。理论上建立相对最优性保证，推导满足预设相对误差所需的MPC时域条件与样本复杂度。火箭着陆实验验证理论并展现近最优性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.22828v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1305, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.22828v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 344, \"height\": 199, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.22828v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 509, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.22828v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 887, \"height\": 589, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.22828v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 876, \"height\": 180, \"label\": \"Table\"}]"
motivation: 有限时域MPC轨迹与无限时域最优策略存在差距，且非专家演示采样效率与性能保证不足。
method: 提出基于记忆的非参数策略，对有限时域代理值函数构造Lipschitz正则化上包络，作为可计算上界，并规避运行时显式优化。
result: 建立相对无限时域最优值的相对最优性保证，给出MPC时域充分条件与达到预设相对误差的样本复杂度界。
conclusion: 火箭着陆实验验证理论预测，策略取得近最优性能，表明该方法能兼顾性能证书与计算效率。
---

## 摘要
我们研究利用由代理有限时域模型预测控制（MPC）生成的轨迹，为动力系统进行数据驱动的策略构建，目标是逼近无限时域最优控制策略。我们将这些轨迹解释为非专家演示，并提出一种基于记忆的非参数策略，该策略构建有限时域代理值函数的Lipschitz正则化上包络，其可作为所得策略值的一个显式上界，从而在运行时绕过显式优化的同时提供可计算的性能证书。我们建立了相对于无限时域最优值函数的相对最优性保证，并推导了为达到规定相对误差所需的充分MPC时域条件以及样本复杂度界。在火箭着陆任务上的数值实验验证了理论预测，并展示了接近最优的性能。

## Abstract
We study data-driven policy construction for dynamical systems using trajectories generated by surrogate finite-horizon Model Predictive Control (MPC), with the goal of approximating an infinite-horizon optimal control policy. We interpret these trajectories as non-expert demonstrations and propose a memory-based, nonparametric policy that constructs a Lipschitz-regularized upper envelope of the finite- horizon surrogate value function, which serves as an explicit upper bound on the value of the resulting policy, thereby providing a computable performance certificate while bypassing explicit optimization at runtime. We establish relative optimality guarantees with respect to the infinite-horizon optimal value function and derive sufficient MPC horizon conditions, together with sample complexity bounds, for achieving a prescribed relative error. Numerical experiments on a rocket landing task validate the theoretical predictions and demonstrate near-optimal performance.

---

## 论文详细总结（自动生成）

# 论文总结：基于自适应采样非专家演示的数据驱动 MPC

## 1. 核心问题与整体含义
- **研究动机**：模型预测控制（MPC）通过滚动时域求解有限时域问题来近似无限时域最优控制，但存在“时间—精度”权衡：长预测时域性能好但实时计算负担大，短时域计算可行但最优性差距可能较大。
- **数据驱动背景**：已有工作尝试用神经网络、核回归、高斯过程、决策树等学习 MPC 策略映射，实现快速在线推理，但通常缺乏明确的样本复杂度、性能保证，以及随数据增加而单调改进的理论刻画。
- **核心问题**：论文关注两个问题：
  1. MPC 的优化时域 \(N\) 如何影响滚动时域控制器（RHC）的性能？
  2. 若用 RHC 生成的轨迹构建数据驱动策略，其性能如何随 \(N\) 变化？
- **整体含义**：论文将有限时域 MPC 轨迹视为“非专家演示”，不要求访问无限时域最优专家数据，提出一种基于记忆的非参数策略，并给出相对于无限时域最优值函数的相对最优性保证、MPC 时域充分条件与样本复杂度界。

## 2. 方法论
- **问题设定**：考虑离散时间系统 \(x_{t+1}=f(x_t,u_t)\)，无限时域折扣代价为 \(J(x_0)=\min \sum_{t=0}^{\infty}\gamma^t l(x_t,u_t)\)。目标是使学习策略 \(\pi\) 的值函数 \(J^\pi\) 满足相对误差准则：
  \[
  \sup_{x\in X}\frac{J^\pi(x)-J(x)}{J(x)+\eta}\le \mu
  \]
  其中 \(\eta>0\) 避免分母为零，\(\mu>0\) 为容忍度。
- **非专家演示**：不直接求解无限时域问题，而是求解带终端代价 \(F(x_N)\) 的有限时域问题：
  \[
  J_N(x_0)=\min \sum_{t=0}^{N-1}\gamma^t l(x_t,u_t)+\gamma^N F(x_N)
  \]
  并设计 \(F(x_N)\le J(x_N)\)，即终端代价是真实尾部代价的下近似。轨迹数据形式为
  \[
  \mathcal D=\bigcup_k\{(\tilde x_{t,k},\tilde u_{t,k},\tilde J_{t,k})\}
  \]
  其中 \(\tilde u_{t,k}\) 是 MPC 第一步控制，\(\tilde J_{t,k}=J_N(\tilde x_{t,k})\)。
- **非参数策略**：定义值感知最近邻策略
  \[
  \pi_{\mathcal D}(x)=\tilde u_{\ell(x)},\quad
  \ell(x)=\arg\min_i\{\tilde J_i+\lambda\|x-\tilde x_i\|_X\}
  \]
  即选择“非专家值 + Lipschitz 正则距离”最小的样本，并应用其第一步控制。
- **可计算上包络**：定义
  \[
  J^\lambda_{\text{ub}}(x)=\delta^{-1}\min_i\{\tilde J_i+\lambda\|x-\tilde x_i\|_X\}
  \]
  作为策略值的上界。若数据满足一定覆盖条件，则可给出策略性能证书。
- **关键理论结果**：
  - **Proposition 1**：在 Assumptions 2–5 下，有
    \[
    (1-\gamma^N(1+C))J(x)\le J_N(x)
    \]
    且当 \(N\) 足够大时，可得到 \(\delta\)-Bellman 不等式：
    \[
    \delta l(x_0,u_0^F(x_0))\le J_N(x_0)-\gamma J_N(f(x_0,u_0^F(x_0)))
    \]
    其中 \(\delta\le 1-\frac{C\gamma^N}{v(1-\gamma^N(1+C))}\)。
  - **Theorem 1**：给出 MPC 时域 \(N\) 的充分条件，使 \(\delta\)-Bellman 不等式成立；并证明若
    \[
    \lambda\ge \kappa\delta^{-1}(1-\gamma L_f)L_J
    \]
