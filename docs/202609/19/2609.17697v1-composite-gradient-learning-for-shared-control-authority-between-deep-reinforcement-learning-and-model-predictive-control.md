---
title: Composite-Gradient Learning for Shared Control Authority Between Deep Reinforcement Learning and Model Predictive Control
title_zh: 深度强化学习与模型预测控制间共享控制权的复合梯度学习
authors: "Giray Önür, Azita Dabiri, Bart De Schutter"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17697v1"
tags: ["query:control"]
score: 9.0
evidence: 深度强化学习与模型预测控制共享控制权的复合梯度学习
tldr: 在共享控制权限的DRL-MPC框架中，DRL智能体与MPC各生成部分控制输入，但常见学习范式把MPC视为环境，忽略其对控制的贡献及与DRL的交互。本文提出复合梯度学习CGL，将两者控制输入表示为联合动作，并在训练更新DRL时显式建模其交互。在两种多类高速公路交通网络上评估，CGL在弱交互下收益有限，在强交互下部分训练轮次优于把MPC当环境或仅部分纳入学习的方法，但平均控制性能提升仍较温和。为DRL-MPC耦合控制提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.17697v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 497, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17697v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17697v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1830, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17697v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 826, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17697v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 862, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17697v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 895, \"height\": 484, \"label\": \"Figure\"}]"
motivation: 常见DRL-MPC共享控制学习把MPC当作环境，未显式考虑MPC控制贡献及其与DRL智能体的交互，限制协同性能。
method: 提出复合梯度学习CGL，将DRL与MPC控制输入建模为联合动作，并在训练更新DRL时计入两者交互，使MPC融入学习过程。
result: 在两种多类高速公路交通网络上，弱交互时CGL收益有限；强交互时部分训练轮次优于将MPC当环境或仅部分纳入学习的方法，但平均增益温和。
conclusion: CGL为显式耦合DRL与MPC的共享控制学习提供了可行方案，但强交互下的平均性能提升仍有限，需进一步优化。
---

## 摘要
将深度强化学习（DRL）与模型预测控制（MPC）相集成的方法，通过结合二者互补的能力，正越来越多地用于控制自主系统。DRL 通过与环境的交互学习控制策略。MPC 则利用系统模型在考虑约束的同时优化控制输入。在具有共享控制权限的 DRL-MPC 框架中，DRL 智能体和 MPC 控制器各自确定一部分控制输入。然而，常见的学习形式将 MPC 视为环境的一部分，因而没有显式考虑 MPC 对控制的贡献或其与 DRL 智能体的交互。本文提出一种新颖的复合梯度学习（CGL）方法，该方法通过将 DRL 和 MPC 的控制输入表示为联合动作，并在训练期间更新 DRL 智能体时考虑二者的交互，从而将 MPC 控制器集成到学习过程中。CGL 在两个具有不同 DRL 与 MPC 控制输入交互强度的多类高速公路交通网络上进行了评估，并与将 MPC 视为环境一部分或仅部分地将 MPC 纳入学习的替代方法进行了比较。结果表明，在弱交互下，CGL 带来的收益有限，但在强交互下，它在一部分训练运行中学习到比替代方法性能更高的控制策略，尽管平均控制性能增益仍然不大。

## Abstract
Integrated deep reinforcement learning (DRL) and model predictive control (MPC) methods are increasingly used to control autonomous systems by combining their complementary capabilities. DRL learns control policies through interaction with the environment. MPC uses a system model to optimize control inputs while accounting for constraints. In DRL-MPC frameworks with shared control authority, both the DRL agent and the MPC controller each determine part of the control inputs. However, common learning formulations treat MPC as part of the environment and therefore do not explicitly account for MPC's contribution to control or its interaction with the DRL agent. This paper proposes a novel composite-gradient learning (CGL) method that integrates the MPC controller into the learning process by representing the DRL and MPC control inputs as a joint action and accounting for their interaction when updating the DRL agent during training. CGL is evaluated on two multi-class freeway traffic networks with different strengths of interaction between the DRL and MPC control inputs and it is compared with alternative methods that treat MPC as part of the environment or that only partially incorporate MPC into learning. The results show that CGL offers limited benefit under weak interaction, but learns higher-performing control policies than the alternative methods in a subset of training runs under strong interaction, although the average control-performance gains remain modest.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：DRL 与 MPC 各有优势：DRL 无需显式模型、部署时前向计算快；MPC 能利用模型进行约束优化，但在线计算成本较高。二者结合的层级 DRL-MPC 框架中，高层 MPC 以低频更新部分控制输入，低层 DRL 以高频更新另一部分控制输入，从而共享控制权。
- **核心问题**：在共享控制权的 DRL-MPC 框架中，常见做法把 MPC 视为环境的一部分。但 MPC 在预测未来状态时需要使用 DRL 策略产生的未来低层控制输入，因此 DRL 策略参数会间接影响 MPC 的高层控制输入。若训练时忽略这种影响，学习公式是不完整的，尤其在 DRL 与 MPC 控制输入强耦合时可能限制性能。
- **整体含义**：论文提出复合梯度学习 CGL，将 DRL 与 MPC 的控制输入建模为联合动作，并在更新 DRL 智能体时同时考虑 DRL 动作的直接效应和通过 MPC 控制律产生的间接效应。目标是在强耦合场景下学习到更优控制策略，同时通过层级感知采样降低 MPC 计算复用成本。

## 2. 方法论

- **层级 DRL-MPC 框架**：
  - 高层 MPC 控制输入 \(u_h\) 低频更新，低层 DRL 动作 \(u_l\) 高频更新。
  - MPC 在预测时使用固定副本的 DRL 策略 \(\pi_\theta\) 生成预测低层输入，因此 MPC 控制律可写为  
    \(u_h = \pi_{\text{mpc}}(x, \pi_\theta, \eta_{\text{mpc}})\)。
  - 这意味着 DRL 策略参数 \(\theta\) 会影响 MPC 优化问题中的预测低层输入，进而影响 MPC 解。

- **MDP 建模**：
  - 将共享控制问题建模为 MDP，一个 MDP 步对应一个低层控制间隔。
  - 状态为 DRL 观测 \(x_{rl}\)。
  - 联合动作定义为  
    \(u = [u_l^\top, u_{h|l}^\top]^\top \in U_l \times U_h\)，  
    其中 \(u_{h|l}\) 是把 MPC 高层输入按低层时间尺度表示并零阶保持后的结果。
  - 奖励评估低层控制间隔内的控制性能。

- **CGL 学习算法**：
  - 基于 DDPG 的 actor-critic 结构，含 actor \(\pi_\theta\)、critic \(Q_\phi\)、目标 actor \(\pi_{\theta'}\)、目标 critic \(Q_{\phi'}\) 和 replay buffer。
  - Critic 目标为  
    \(y_i = r_i + \gamma(1-\delta_i) Q_{\phi'}(x^
