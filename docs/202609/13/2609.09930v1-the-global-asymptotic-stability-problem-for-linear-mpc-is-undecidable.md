---
title: The Global Asymptotic Stability Problem for Linear MPC Is Undecidable
title_zh: 线性MPC的全局渐近稳定性问题不可判定
authors: Johan Löfberg
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.09930v1"
tags: ["query:control"]
score: 9.0
evidence: 约束线性MPC全局渐近稳定性的不可判定性
tldr: 线性MPC的稳定性判定通常依赖仿真或保守条件，本文关注其全局渐近稳定性是否可判定。作者对带约束有限时域线性MPC进行可归约性证明，构造多种从不可判定问题到稳定性判定的归约。结果表明即便在一步预测、单位权重、唯一最优解和全局可行等强假设下，该判定仍不可判定。该结论排除长时域、维度增长、递归可行性失效和最优解不唯一等常见成因。
source: arxiv
selection_source: fresh_fetch
motivation: MPC稳定性分析常依赖仿真或保守判据，需明确全局渐近稳定性判定本身的计算复杂度。
method: 将不可判定问题归约到带约束有限时域线性MPC的全局渐近稳定性判定，覆盖预测状态盒、硬输入盒、二次软化输入盒及固定维度。
result: 即使预测时域为一、状态/输入/终端权重为单位阵、最优解唯一且全局可行，稳定性判定仍不可判定；另将状态和输入维数分别固定为3和6。
conclusion: 不可判定性并非源于长时域、维数增长、递归可行性失效或最优解不唯一，揭示线性MPC稳定性分析的根本计算极限。
---

## 摘要
我们证明，判定受约束有限时域线性模型预测控制的全局渐近稳定性是不可判定的。该结论在时域为1、状态、输入和终端权重均为单位矩阵、最优解唯一且全局可行时成立。独立的归约分别涵盖预测状态盒约束、硬输入盒约束和二次软化输入盒约束。第四个归约将状态维数和输入维数分别固定为3和6。因此，不可判定性并非由长时域、维数增长、递归可行性失效或非唯一性所导致。

## Abstract
We prove that deciding global asymptotic stability for constrained finite-horizon linear model predictive control is undecidable. This holds at horizon one with identity state, input, and terminal weights, unique optimizers, and global feasibility. Separate reductions cover predicted-state boxes, hard input boxes, and quadratically softened input boxes. A fourth reduction fixes the state and input dimensions to three and six. Hence undecidability is not caused by long horizons, growing dimensions, failures of recursive feasibility, or nonuniqueness.

---

## 论文详细总结（自动生成）

# 论文总结：《线性MPC的全局渐近稳定性问题不可判定》

## 1. 核心问题与研究背景

- **研究动机**：模型预测控制（MPC）是工业过程控制中最成功的方法之一，其优势在于能直接在在线优化中处理多变量动态与约束。但一个令人不安的事实是：**闭环稳定性并非有限时域MPC公式的内在属性**——它必须通过终端约束/终端代价、合适的局部控制律、足够长的预测时域，或对滚动时域律进行后验分析来保证。
- **核心问题**：对于带约束的有限时域线性MPC，判定其闭环是否**全局渐近稳定**（相对于可行集 X_N）是否可判定？即：是否存在一个算法，对任意实例都能终止并正确判定？
- **背景与缺口**：
  - 已有结果证明一般分段仿射（PWA）系统的稳定性不可判定（Blondel 等：二维不连续PWA；三维连续PWA；饱和线性系统），Korda 也证明由 ReLU 网络表示饱和时不可判定。
  - 但 **"线性MPC ⇒ PWA闭环"这一蕴含方向对于迁移不可判定性恰好是反的**：在更大类上不可判定的问题，限制到 MPC 这一高度结构化的子类（反馈必须是有限时域二次规划的优化器，参数依赖与目标函数与预测模型绑定）后**可能变为可判定**。
  - 因此需要构造**反向的、保持稳定性的归约**：从已知不可判定问题归约到 MPC 稳定性判定问题。

## 2. 方法论：核心思想与关键技术

- **核心思想**：以饱和线性系统（及固定维连续PWA映射）的全局渐近稳定性这一已知不可判定问题为源问题，对每个有理矩阵 A，**可计算地（多项式时间）构造**一个有理一步（N=1）MPC实例 I(A)，使得 I(A) 全局渐近稳定 ⇔ sat(A·) 全局渐近稳定，从而构成多一归约（many-one reduction）。所有构造均满足：严格凸QP、最优解唯一、对任意状态全局可行（递归可行性自动成立）、原点为闭环平衡点。

- **关键技术细节（四种独立归约）**：

  - **(A) 仅预测状态约束**：被控对象 x_{k+1} = 2Ax_k + u_k，目标 ‖x_k‖²+‖u_k‖²+‖x_{k+1}‖²，约束仅 x_{k+1} ∈ B^n（单位盒，可视作终端/预测输出约束），无输入约束。消去 u_k 后目标化为 2‖z − Ax_k‖² + 常数，故 z* 是 Ax_k 到 B^n 的**欧氏投影**，即 z* = sat(Ax_k)。全局可行（取 z=0, u=−2Ax_k），严格凸保证唯一性。
    - **推论**：额外加入 x_{0|k}=x_k ∈ B^n 后，可行集恰为紧致不变多面体 B^n，不可判定性依然成立（源系统一步进入 B^n，全局收敛可由第一步之后推出，Lyapunov 稳定性是局部性质）。

  - **(B) 仅输入约束**：引入无记忆增广状态 (z,y)，被控对象 z_{k+1}=u_k，y_{k+1}=3Az_k−u_k，目标为恒等权重二次型，约束仅 u_k ∈ B^n。目标化简为 3‖u_k−v‖²+6‖v‖²（v=Az_k），投影给出 u*_k = sat(Az_k)，且最优解只依赖 z_k。配合**引理6（无记忆增广下的稳定性等价）**：对连续 f, h 且 f(0)=h(0)=0，增广系统 (f(z), h(z)) 全局渐近稳定 ⇔ z_{k+1}=f(z_k) 全局渐近稳定，得到稳定性等价。

  - **(C) 二次软化输入约束**：被控对象 z_{k+1}=−Az_k+2u_k，y_{k+1}=4Az_k−u_k，目标含 6‖s_k‖² 松弛惩罚，约束 −1−s ≤ u ≤ 1+s、s ≥ 0（**对物理状态与输入无硬界**）。**引理8（泄漏投影）**给出闭式解 u* = (v + sat(v))/2，s*_i = ((|v_i|−1)_+)/2。代入第一式后线性泄漏项被抵消：z_{k+1} = −v + 2u* = sat(v)，从而**软化约束仍精确恢复饱和**。

  - **(D) 固定维度（n_x=3, n_u=6）**：改用固定维连续PWA源问题，通过**上镜图（epigraph）提升**实现。**引理10**基于连续PWA函数的凸差分（DC）分解，构造有效可计算的有理凸PWA函数 p_i, q_i，满足 φ_i = p_i − q_i 且 p_i, q_i ≥ |φ_i|、在原点为零。取被控对象 x_{k+1} = v_k − w_k = [I −I]u，目标为恒等权重二次型，约束为 v_{k,i} ≥ p_i 的各仿射片、w_{k,i} ≥ q_i 的各仿射片。由于 p_i ± φ_i ≥ 0，上镜图下角 (p(x), q(x)) 满足一阶最优性条件，且 Hessian ≻ 0 保证唯一最优，最终 x_{k+1} = φ(x_k)。

## 3. 实验设计

- **本文为纯理论/证明型论文，不含任何数据集、基准（benchmark）或数值实验**。
- 不存在与传统方法或基线的性能对比；其"验证"形式是**命题、引理、推论与定理的数学证明**（定理3、定理4、推论5、引理6、命题7、引理8、命题9、引理10、命题11、定理12）。
- 可视为"设计"的是**四类相互独立的归约构造**，分别覆盖：预测状态盒、硬输入盒、二次软化输入盒，以及固定状态/输入维数。

## 4. 资源与算力

- **文中未提及任何算力资源**（无 GPU 型号、数量、训练时长等），这与论文的理论性质一致。
- 唯一与工具相关的说明出现在致谢：作者使用 OpenAI GPT 5.6 Sol 改进第 III–IV 节的构造，使用 OpenAI Codex GPT 5.6 Sol 协助部分起草与校对；作者声明已独立验证所有 AI 生成内容并对文章负全责。

## 5. 实验数量与充分性

- **无实验组数可言**：不存在数据集划分、消融实验或对比实验。
- 就**理论覆盖的充分性**而言：
  - 四类归约分别针对不同约束类型（状态、输入、软化输入）与固定维度情形，**论证了不可判定性并非源于某一种约束类型**；
  - 所有构造统一满足 N=1、Q=R=P=I、唯一最优解、全局可行、原点为平衡点等强条件，**逐一排除了长时域、维数增长、递归可行性失效、最优解不唯一等常见成因**；
  - 归约是可计算的（多项式时间），满足多一归约的严格性要求。
- **客观性与公平性**：作为数学证明，其"客观性"取决于证明的正确性而非实验设计；论文对外部源定理（定理1、定理2）有明确引用与依赖，归约链条清晰。但**缺乏任何数值或计算层面的独立验证**。

## 6. 主要结论与发现

- **核心结论**：带约束有限时域线性MPC的全局渐近稳定性判定问题是**不可判定的**——不存在对所有实例都能终止并正确判定的算法。
- **结论的强形式**：即使预测时域固定为 N=1、状态/终端/输入权重均为单位矩阵、QP 严格凸且最优解唯一、对任意状态全局可行，不可判定性依然成立。
- **分项结论**：
  - 分别对"仅预测状态约束""仅输入盒约束""二次软化输入盒约束"三类子类成立；
  - 在预测状态变体中，即使可行集被限制为紧致不变多面体 [−1,1]^n，不可判定性仍成立；
  - 第四个上镜图归约将状态维数固定为 3、输入维数固定为 6，不可判定性依旧成立（仅混合状态–输入不等式的数量与系数承载计算编码）。
- **解释性结论**：不可判定性**不能归因于**长时域、状态/输入维数增长、最优解非唯一、全局可行性或递归可行性的失效。
- **实践含义**：该不可能性针对**通用判定算法**，并不妨碍控制器设计或通过充分条件验证具体控制器。标准终端要素（终端集、终端控制器、终端代价下降）仍适用；SOS/KKT、MILP、不变集、显式PWA 方法可提供严格证书。可判定子类包括：无约束LQ-MPC（归结为特征值检验）以及由终端条件保证稳定性的构造。一般方法必须允许"不确定"的回答、局限于充分条件，或在某些实例上不终止。

## 7. 优点

- **问题定位精准**：清晰指出"MPC ⇒ PWA"的蕴含方向无法用于迁移不可判定性，从而说明该问题此前虽被"高度预期"但并未被证明，填补了逻辑缺口。
- **归约构造优雅且可验证**：三种饱和实现分别基于欧氏投影、输入投影与"泄漏投影"，均有闭式解；上镜图构造利用 DC 分解，把 PWA 映射的精确实现转化为 QP 的角点最优性论证，技术路线干净。
- **结论强度高**：在 N=1、恒等权重、唯一最优、全局可行等最简化条件下仍不可判定，说明障碍是"结构性"而非"工程性"的，理论意义显著。
- **归因分离**：通过多个独立归约分别覆盖状态约束、输入约束、软化输入约束与固定维度，逐一排除可能被误认为是成因的因素。
- **学术诚实**：明确指出结论不否定控制器设计，并列出可判定子类与实用验证手段；同时公开说明 AI 辅助并声明已验证。

## 8. 不足与局限

- **无任何数值或计算验证**：纯理论论文，未给出示例实例、仿真或计算实验来佐证构造的可实现性。
- **依赖外部源定理**：结论建立在 Blondel 等的饱和线性系统与固定维连续PWA不可判定性结果之上；若源问题或引用版本理解有误，结论链会受影响。
- **固定维度结果的适用范围有限**：定理12 需要 **6 个输入**及**一般的混合状态–输入不等式**；能否在"少数状态 + 标量输入"或"固定简单约束结构"下保持不可判定性，论文明确列为**开放问题**。
- **篇幅较短（6页）**：构造与证明较为紧凑，部分细节（如软化问题中松弛关联矩阵与惩罚矩阵的编码、DC 分解的片数增长）未充分展开。
- **实践指导性有限**：结果是最坏情形下的不可判定性，对具体控制器工程实践的直接可操作建议不多；不能直接给出"如何设计可验证控制器"的算法。
- **AI 辅助的潜在风险**：尽管作者声明独立验证，构造与部分草稿由 AI 协助完成，对部分读者可能构成额外的可信度审视点。
- **覆盖面**：未讨论输出约束、非线性/时变MPC、鲁棒MPC、分布式MPC 等变体是否同样不可判定。

（完）
