Title: Risk-Aware Optimal Control with Rulebooks

URL Source: https://arxiv.org/pdf/2609.05199v1

Published Time: Mon, 07 Sep 2026 01:04:24 GMT

Number of Pages: 15

Markdown Content:
# Risk-Aware Optimal Control with Rulebooks 

Tichakorn Wongpiromsarn 1

Abstract — We consider safety-critical control problems in-volving multiple requirements with different priorities and un-certainty in their evaluation. We represent these requirements using risk-aware rulebooks, where each requirement is assigned a risk measure and an acceptable threshold, and a priority relation is defined among the requirements. Each requirement induces a risk-evaluation function that maps a policy to the risk associated with its violation. We formulate risk-aware optimal control with rulebooks as a lexicographic optimization problem over excess risks and develop an anytime filtering and branch-and-bound algorithm that progressively tightens the certified optimality gap while characterizing the corresponding set of policies at each priority level. The algorithm returns a policy together with these gaps, which bound its suboptimality. We prove that these gaps are valid for any finite computational budget and, under additional assumptions, converge to zero as the computational budget increases. We evaluate the algorithm on a synthetic benchmark with a known optimum and a realistic highway-merging simulation with CVaR-based collision, rear-braking, headway, and comfort rules. 

I. I NTRODUCTION 

Control of safety-critical autonomous systems typically requires balancing several objectives, including safety, reg-ulatory requirements, progress, and other performance ob-jectives, which may have different priorities. For example, in a highway merging maneuver, avoiding collision should have higher priority than obeying a three-second headway rule, while headway in front and behind the ego vehicle may have the same priority. If all objectives cannot be simulta-neously satisfied, the controller should violate lower-priority objectives before higher-priority ones. This creates a multi-objective optimization problem with a priority structure, not just a scalar cost minimization problem. Uncertain environments (e.g., interactions with other road users) further complicate this problem because the same pol-icy can produce different outcomes, making its performance uncertain. A standard formulation in stochastic control and reinforcement learning is to optimize the expected cumula-tive cost or reward associated with a sequence of decisions [1], [2], but expectation can obscure the risk posed by rare outcomes with severe consequences. Other risk measures capture different aspects of uncertainty. Worst-case risk con-siders the most adverse possible outcome, Value at Risk (VaR) specifies a quantile of the distribution of a quantity of interest, and Conditional Value at Risk (CVaR) measures the expected risk in the tail beyond the VaR threshold  

> *This work was supported in part by NSF under Grant CNS-2141153. The author gratefully acknowledges Konstantin Slutsky for valuable discussions and guidance that strengthened the mathematical rigor of this work.
> 1Tichakorn Wongpiromsarn is with the Department of Computer Science, Iowa State University, Ames, IA 50010

nok@iastate.edu 

[3]. CVaR is a standard example of a coherent tail-risk measure and admits a useful optimization representation [4]– [6]. Risk-sensitive and risk-constrained formulations have been studied in MDPs [7], stochastic shortest-path planning [8], reinforcement learning [9], stochastic system verification [10], and risk-aware motion planning and control [11]–[13]. These works evaluate, optimize, or constrain risk within a specified model, but they do not directly address policy selection under multiple prioritized risk criteria. Rulebooks provide a formal way to describe priorities among requirements and objectives, with each represented by a rule [14], [15]. Risk-aware rulebooks extend this formalism to uncertain environments by assigning a risk measure and a threshold to each rule [16]. Different rules can use different risk measures, including expected value, worst-case risk, VaR, CVaR, or other measures. Policies with risk below the threshold of a rule are considered equally acceptable for that rule. Thus, once a high-priority rule is certified to be below its threshold, further reducing its risk provides no additional benefit, and the algorithm can proceed to the next rule level. This threshold-based priority structure leads to a differ-ent computational problem from standard risk-sensitive or risk-constrained optimization. A particularly related MDP planning method first optimizes CVaR and then, among all CVaR-optimal policies, optimizes expected value [17]. Our formulation extends this idea by allowing more than two rule levels, allowing each level to use its own risk measure and threshold, and applying when the risk-evaluation functions are black boxes rather than being derived from a fixed MDP model. Existing lexicographic optimization methods [18]–[20], lexicographic MDPs [21], and lexicographic MPC methods [22] handle prioritized objectives and constraints. However, they rely on problem-specific structure, such as explicit objective and constraint functions, transition and reward models, or system dynamics and stage costs. Refer-ences [23], [24] also consider optimization and exploration of unknown functions under Lipschitz assumptions, but focus on safe exploration rather than prioritized optimization with rule-specific risk measures and thresholds. This paper develops an algorithm for computing an op-timal policy under risk-aware rulebooks when the risk-evaluation functions are black boxes. We assume that each risk-evaluation function is Lipschitz continuous but do not require gradients, convexity, or an analytic expression. Our algorithm builds on the branch-and-bound principle of main-taining lower and upper bounds over regions of the search space [25]–[27]. Unlike classical branch-and-bound methods, which use these bounds to guide refinement toward a global minimizer of a single objective, our algorithm maintains arXiv:2609.05199v1 [eess.SY] 4 Sep 2026 bounds at each rule level to eliminate regions that cannot contain an optimizer with respect to the rule levels processed so far. Thus, our refinement strategy focuses on charac-terizing the entire set of policies that remain potentially optimal and providing optimality-gap guarantees under a finite computational budget, rather than identifying a single global optimizer as in classical branch-and-bound. The contributions of this paper are as follows. First, we formulate risk-aware optimal control with rulebooks as a lexicographic optimization problem over excess risks. This extends existing risk-aware control formulations by allowing each rule to have its own risk measure and threshold together with explicit priorities among rules. Second, we introduce an anytime filtering and branch-and-bound algorithm for black-box risk-evaluation functions. Unlike conventional sequential approaches to lexicographic optimization, our algorithm does not require each rule level to be solved exactly before pro-ceeding to the next. Instead, it uses certified bounds obtained within a finite computational budget to propagate certified policy sets across the rule levels. Third, we prove that the certified optimality gap returned by the algorithm is valid for any finite computational budget and, under additional assumptions, converges to zero as the computational budget increases. Fourth, we demonstrate the method on a synthetic benchmark with a known optimum and on a realistic highway merging simulation using CVaR as the risk measure for collision, rear-braking, headway, and comfort rules. II. P RELIMINARIES 

Let Ξ denote the set of objects to be evaluated, e.g., trajectories that describe the states of all relevant agents in the world. A rulebook compares these objects by quantifying their violations of each rule and ordering the rules by priority [14], [15]. Throughout the paper, R and R≥0 denote the set of real and non-negative real numbers, respectively and 

[x]+ = max {x, 0} for any x ∈ R.

Definition 1: A rule is a function r : Ξ → R≥0 that measures the degree of violation of its argument. 

r(x) < r (y) means that x violates r less than y does. In particular, r(x) = 0 indicates that x satisfies the rule. Although we call r a “rule”, it can encode not only a legal or hard constraint but also a performance criterion, such as comfort, efficiency, or user preference. 

Definition 2: A rulebook is a tuple R = ⟨R, ≲⟩, where R

is a set of rules and ≲ is a preorder on R specifying their relative priority. A preorder generalizes a partial order by allowing distinct elements s1 and s2 to satisfy both s1 ≲ s2 and s2 ≲ s1.Throughout the paper, we assume that R is finite. For 

r, r ′ ∈ R, we write r > r ′ to denote that r has strictly higher priority than r′, i.e., r̸ ≲ r′ and r′ ≲ r. The preorder allows three cases: (1) strict priority ( r > r ′) (2) incomparability (r̸ ≲ r′ and r′̸ ≲ r), and (3) equal rank ( r ≲ r′ and r′ ≲ r). This structure generalizes standard weighted formulations, which correspond to placing all rules at equal rank, and classical lexicographic formulations, which correspond to a total priority ordering. As shown in [15], the classical rule-book formalism generalizes temporal-logic-based planning and optimization-based control in the deterministic setting. It can be proved [14] that a rulebook R induces a preorder 

≲R on Ξ. The interpretation of x ≲R y is that x is at least as good as y. Formally, x ≲R y if, for every rule r ∈ R for which r(x) > r (y), there exists a higher-priority rule r′ > r 

such that r′(x) < r ′(y). We say that x is “strictly better than” y, denoted by x < R y, if x ≲R y but y̸ ≲R x.III. P ROBLEM FORMULATION 

Consider a system operating under uncertainty. Let 

(Ω , Σ, Pr ) be a probability space that models the uncertainty, which may arise from system dynamics, sensing, state-estimation and prediction errors, the behavior of external agents, or other factors. Let Π ⊂ Rd be a set of policies available to the system. Consider a rulebook R = ⟨R, ≲⟩, where each rule ri ∈ R

is associated with a risk measure ρi and threshold γi. For each policy π ∈ Π, let rπi : Ω → R≥0 be a measurable nonnegative random variable representing the violation of ri

when π is executed. Its distribution captures the uncertainty in evaluating ri under policy π and is induced by the proba-bility measure Pr on (Ω , Σ, Pr ). Define the risk-evaluation function qi : Π → R≥0 associated with ri by qi(π) = ρi(rπi ).The value qi(π) represents the risk incurred by policy π with respect to rule ri as quantified by the risk measure ρi. Note that ρi may differ across rules and can be any risk measure, including expected value, worst-case, VaR, or CVaR. The threshold γi specifies the largest acceptable risk for rule ri. Risks at or below this threshold are treated as equally acceptable for ri, allowing lower-priority rules to influence the decision once an acceptable level of performance with respect to ri has been achieved. We say that π ∈ Π satisfies 

rule ri if qi(π) ≤ γi. A rule ri is satisfiable on a set Π′ ⊆ Π

if some π ∈ Π′ satisfies it. For convenience, define the excess risk function ei : Π → R≥0 by ei(π) = [ qi(π) − γi]+.This value is zero exactly when π satisfies ri, and otherwise equals the amount by which its risk exceeds γi. Policies are compared according to the rulebook preorder. 

Definition 3: A policy π ∈ Π is at least as good as π′ ∈

Π, denoted by π ≲R π′, if for every rule ri ∈ R such that 

ei(π) > e i(π′), there exists a higher-priority rule rj > r i

such that ej (π) < e j (π′).

Definition 4: A policy π ∈ Π is strictly better than π′ ∈

Π, denoted by π < R π′, if π ≲R π′ but π′̸ ≲R π.

Problem 1 (Risk-aware optimal control with rulebooks): 

Compute an optimal policy π⋆ ∈ Π such that π̸ <R π⋆ for every π ∈ Π.We make the following assumptions to ensure that Prob-lem 1 is well posed (see Proposition 1). 

Assumption 1: The policy space Π ⊂ Rd is compact and nonempty. 

Assumption 2: For every rule ri ∈ R, the risk-evaluation function qi : Π → R≥0 is Lipschitz continuous with known constant Li, i.e., |qi(π) − qi(π′)| ≤ Li∥π − π′∥2 for all 

π, π ′ ∈ Π.Remark 1: As proved in [16], the relation ≲R on Π is a preorder. Hence the induced strict relation <R is acyclic, which prevents preference cycles (e.g., policy π1 is strictly better than π2, π2 is strictly better than π3 but π3 is strictly better than π1) and gives a well-defined notion of optimality. IV. A NYTIME LEXICOGRAPHIC OPTIMIZATION VIA 

FILTERING AND BRANCH -AND -B OUND 

As shown in [15], any rulebook R can be refined into a rulebook RT = ⟨RT , ≲T ⟩, whose priority relation ≲T is a total order. The refined rulebook is consistent with the orig-inal in the sense that no preference from R is contradicted, i.e., for any two policies π, π ′ ∈ Π, π ≲R π′ =⇒ π ≲RT π′

and π < R π′ =⇒ π < RT π′. Although policies that are incomparable under R may become comparable under RT ,an optimal policy for RT is also optimal for R.In the remainder of the paper, we assume without loss of generality that the rulebook has been refined into a total order with RT = {r1, . . . , r m} and ri >T ri+1 for all 

i. Problem 1 can then be formulated as a lexicographic optimization problem over the excess-risk vector e(π) =  e1(π), . . . , e m(π). Since each qi is Lipschitz continuous, the corresponding excess risk function ei is continuous. Given any rule level i ∈ { 1, . . . , m } and compact subset 

Π′ ⊆ Π, let αi(Π ′) = min π∈Π′ ei(π) denote the smallest excess risk with respect to rule ri achievable over the policy set Π′. Since Π′ is compact and ei is continuous, the extreme value theorem ensures that the minimum is attained and hence αi(Π ′) is well-defined [28]. The lexicographic struc-ture naturally induces a sequence of nested sets of candidate policies. Let Π⋆ 

> 0

= Π , and for each i ∈ { 1, . . . , m }, define 

Π⋆i = {π ∈ Π⋆i−1 | ei(π) = αi(Π ⋆i−1)}. In other words, Π⋆i

is the set of policies that are lexicographically optimal with respect to the first i rules and the rulebook-optimal policy set is Π⋆m. Starting from the nonempty compact set Π, the extreme value theorem and continuity of ei ensure that each 

Π⋆i is compact and nonempty, so the construction is well defined. Together with the consistency of the refinement, this establishes existence of an optimal policy for Problem 1. 

Proposition 1: Problem 1 admits an optimal policy. In many applications, high-priority rules correspond to safety requirements and are associated with thresholds that define acceptable levels of risk. Once the risk falls below the prescribed threshold, further reducing it provides no additional preference under that rule. Lower-priority rules then determine the preference among policies satisfying the higher-priority rule. Optimization is required only for rules whose thresholds cannot be achieved. Formally, let Πi =

{π ∈ Π | qi(π) ≤ γi} denote the set of policies that satisfy rule ri. If ri is satisfiable on Π⋆i−1, then αi(Π ⋆i−1) = 0 

and Π⋆i = Π ⋆i−1 ∩ Πi. Thus, all policies in Π⋆i−1 ∩ Πi are considered equally acceptable with respect to ri and the ith step is reduced to a feasibility-filtering step. On the other hand, if ri is not satisfiable on Π⋆i−1, then ei(π) = qi(π)−γi

on Π⋆i−1, so minimizing ei is equivalent to minimizing qi.This observation motivates an algorithm that combines viability-style feasibility filtering with Lipschitz branch-and-bound optimization to progressively restrict the set of candidate policies as it processes the rule hierarchy. The main design principle is to keep two kinds of information: a box collection, which provides a certified lower bound, and certified “incumbents”, i.e., concrete feasible policies already found by the algorithm (using branch-and-bound terminology), which can be returned by the algorithm and provide a certified upper bound. 

A. Admissible Domains, Box Bounds and Certificates 

Under a finite computational budget, computing the exact sets Π⋆i is generally infeasible, since it requires solving a global optimization or global level-set characterization prob-lem for a black-box risk-evaluation function. We therefore compute a sequence of certified admissible domains that contain the exact lexicographic solution sets. 

Definition 5: The certified admissible domains are defined recursively by D0 = Π and 

Di = {π ∈ Di−1 | ei(π) ≤ αi}, (1) where αi ∈ R≥0 satisfies αi ≥ αi(Di−1).By construction, each certified admissible domain contains the corresponding exact lexicographic solution set, i.e., Π⋆i ⊆

Di. To quantify this relaxation, define εi = αi − αi(Di−1).Then, Di is precisely the εi-optimal set for rule ri over the certified admissible domain Di−1. Because αi(Di−1) is generally unknown, the algorithm computes a certified upper bound on εi. If εi = 0 for every rule level, then each certified admissible domain coincides with the exact lexicographic solution set, i.e., Di = Π ⋆i for all rule levels i.. To compute Di, the algorithm represents subsets of Rd

by finite collections of closed axis-aligned boxes. For a box 

B ⊆ Rd, let c(B) be its center and define its covering radius as rad( B) = √d 

> 2

l(B), where l(B) = max j lj (B) is the length of the longest side of B and lj (B) is the length of its jth side. Every point in B lies within Euclidean distance 

rad( B) from c(B).For every rule ri and box B, define the box bounds 

qi(B) = qi(c(B)) − Li rad( B),qi(B) = qi(c(B)) + Li rad( B),ei(B) = [ qi(B) − γi]+, ei(B) = [ qi(B) − γi]+,wi(B) = qi(B) − qi(B) = 2 Li rad( B) = Li

√d l (B).

(2) This construction assumes that qi(π) can be queried at policy points c(B) for each box B. As shown in the following lemma, qi(B) and qi(B) are certified lower and upper bounds on the risk-evaluation function qi over B ∩ Π, while 

ei(B) and ei(B) are the corresponding bounds on ei and certify how much a policy in B ∩Π can violate ri beyond the acceptable level γi. Finally, wi(B) quantifies the uncertainty width induced by Lipschitz continuity of qi over B.

Lemma 1: Suppose c(B) ∈ Π and qi(B), qi(B), ei(B),

ei(B) and wi(B) are defined by (2). Then, for every policy π ∈ B ∩ Π,

qi(B) ≤ qi(π) ≤ qi(B), (3) 

ei(B) ≤ ei(π) ≤ ei(B), (4) 

ei(B) − ei(B) ≤ wi(B). (5) 

Proof: Since c(B) ∈ Π and π ∈ Π, Assumption 2 gives |qi(π) − qi(c(B)) | ≤ Li∥π − c(B)∥2. Since π ∈ B,

∥π −c(B)∥2 ≤ rad( B). Rearranging yields (3). Equation (4) follows from (3) because x 7 → max {x − γi, 0} is monotone. Finally, by (2), qi(B) − qi(B) = wi(B). Since [b]+ − [a]+ ≤

b − a for all a ≤ b, we obtain ei(B) − ei(B) ≤ qi(B) −

qi(B) = wi(B).The algorithm does not depend on the particular construc-tion in (2), which uses the box center c(B) to compute the bounds. Any construction is valid if the bounds satisfy (3)– (5) for every policy π ∈ B ∩ Π as all correctness results rely only on these inequalities, not how the bounds are computed. The algorithm processes the rules sequentially. As will be explained in Section IV-B, when processing rule level 

i, it starts with a box collection O satisfying Di−1 ⊆S 

> B∈O

B together with a nonempty finite certified incumbent set C ⊆ Di−1. It then iteratively prunes and refines O.Pruning removes boxes in O that cannot contain a minimizer of ei over Di−1, whereas refinement improves the resolution of O. Throughout this process, the algorithm ensures that 

O always covers all minimizers of ei over Di−1, i.e., 

arg min π∈Di−1 ei(π) ⊆ S 

> B∈O

B.To support this pruning and refinement process, the algo-rithm maintains a lower certificate αi, an upper certificate 

αi, and a certified optimality gap εi. At the beginning of rule level i, these certificates are initialized as 

αi = min  

> B∈O

ei(B), α i = min  

> π∈C

ei(π), ε i = αi − αi. (6) As the box collection O is pruned and refined, the current box-based lower bound min B∈O ei(B) may increase or decrease. To ensure that αi is monotone nondecreasing, the algorithm maintains αi as a running lower certificate by updating it only when the newly computed box-based lower bound is larger than its current value: 

αi ← max {αi, min  

> B∈O

ei(B)}. (7) Since every box collection O encountered during the pruning and refinement process continues to cover all minimizers of 

ei over Di−1, each newly computed value of min B∈O ei(B)

is a valid lower bound on αi(Di−1) and so is their maximum, 

αi. In contrast, the certified incumbent set C only grows over time, so simply recomputing αi and εi using (6) at each update is sufficient to ensure that αi and εi are monotone nonincreasing, without requiring the analogous running update used for αi.

Lemma 2: Suppose that every initialization and update of 

αi and αi through (6) and (7) uses sets satisfying ∅̸ = C ⊆ 

Di−1 and arg min π∈Di−1 ei(π) ⊆ S 

> B∈O

B. Then, 

αi ≤ αi(Di−1) ≤ αi.

Proof: Consider an arbitrary π⋆ ∈

arg min π∈Di−1 ei(π), i.e., ei(π⋆) = αi(Di−1). For each certificate update k during rule level i, let O(k)

and C(k) denote the corresponding current sets, and define β(k) = min B∈O (k) ei(B). By assumption, 

arg min π∈Di−1 ei(π) ⊆ S 

> B∈O (k)

B, so there exists 

B⋆ ∈ O (k) with π⋆ ∈ B⋆. Hence 

β(k) = min  

> B∈O (k)

ei(B) ≤ ei(B⋆) ≤ ei(π⋆) = αi(Di−1).

Since αi = max k β(k), with β(k) ≤ αi(Di−1) for all k,it follows that αi ≤ αi(Di−1). Finally, since C ⊆ Di−1,it follows that αi = min π∈C ei(π) ≥ min π∈Di−1 ei(π) = 

αi(Di−1), which completes the proof. Based on (4) and Lemma 2, ei(B) provides a certified lower bound on ei(π) for any π ∈ B ∩ Π, while αi is a certified upper bound on the optimal value of ei(π) for any 

π ∈ Di−1. As a result, if ei(B) > α i, then B cannot contain a minimizer of ei over Di−1. This motivates the following notion of a competitive box and the associated sound pruning result. 

Definition 6: A box B ∈ O is competitive for rule ri if 

ei(B) ≤ αi.

Lemma 3: Suppose ∅̸ = C ⊆ Di−1. Then, any box B with 

ei(B) > α i satisfies B ∩ arg min π∈Di−1 ei(π) = ∅.

Proof: For every π ∈ B ∩ Π, (4) gives ei(π) ≥

ei(B) > α i. By definition of αi in (6) and the assumption that ∅̸ = C ⊆ Di−1, there exists an incumbent πinc ∈ Di−1

with ei(πinc ) = αi. Therefore, for every π ∈ B ∩ Π,

ei(π) > ei(πinc ) ≥ min π∈Di−1 ei(π). Therefore, B ∩

arg min π∈Di−1 ei(π) = ∅.For a satisfiable rule, αi(Di−1) = 0 . Boxes with qi(B) ≤

γi are certified feasible, boxes with qi(B) > γ i are certified infeasible, and the remaining boxes are ambiguous. Once 

αi = 0 has been certified, the pruning test removes the certified infeasible boxes and leaves only certified feasible or ambiguous boxes. For an unsatisfiable rule, the same pruning test is branch-and-bound optimization on ei. The following lemma provides the stopping criterion for a satisfiable rule. Once every box in O satisfies qi(B) ≤ γi, the set Di−1 ∩ Πi

has been characterized exactly, so no further subdivision is required at rule level i.

Lemma 4: Suppose Di−1 ⊆ S 

> B∈O

B and let O+ =

{B ∈ O | qi(B) ≤ γi}. Then, Di−1 ∩ Πi ⊆ Di−1 ∩S 

> B∈O +

B. If, in addition, qi(B) ≤ γi for every B ∈ O +,then Di−1 ∩ Πi = Di−1 ∩ S 

> B∈O +

B.

Proof: We first show that Di−1 ∩ Πi ⊆ Di−1 ∩S 

> B∈O +

B. Consider an arbitrary π ∈ Di−1 ∩ Πi. Since O

covers Di−1, there exists a box B ∈ O such that π ∈ B. By (3), qi(B) ≤ qi(π). Since π ∈ Πi, by definition, qi(π) ≤ γi.Combining these inequalities, we get qi(B) ≤ qi(π) ≤ γi.Hence, by definition, B ∈ O + and therefore π ∈ S 

> B∈O +

B.This proves the first inclusion. Now suppose that every box B ∈ O + satisfies qi(B) ≤ γi.We show the reverse inclusion. Consider an arbitrary π ∈

Di−1 ∩ S 

> B∈O +

B. Then π ∈ Di−1 and π ∈ B for some 

B ∈ O +. Applying (3) again gives qi(π) ≤ qi(B) ≤ γi, so 

π ∈ Πi. Therefore, Di−1 ∩ S 

> B∈O +

B ⊆ Di−1 ∩Πi. The two inclusions together imply Di−1 ∩ Πi = Di−1 ∩ S 

> B∈O +

B.

B. Certified Anytime Algorithm 

Algorithm 1 implements the recursion in (1). Its state at the beginning of rule level i is a certified admissible domain Di−1, a box collection O, and a finite certified incumbent set C. As proved later in Lemma 9, these sets satisfy Di−1 ⊆ S 

> B∈O

B and ∅̸ = C ⊆ Di−1. Lines 1–3 initialize these sets with D0 = Π . The initial box collection 

O may be any finite collection of closed boxes whose union contains Π, e.g., a bounding box of Π. Such a collection exists because by Assumption 1, the set Π is compact. The initial certified incumbent set C may be any nonempty finite subset of Π, e.g., a sampled or user-provided policy in Π.The nonemptiness of C is needed only to define a finite upper certificate αi.The helper B OUNDS in lines 6 and 25 computes and stores 

qi(B), qi(B), ei(B), ei(B), and wi(B) for each box B in the first argument. It may use any certificate satisfying (3)– (5), e.g., the center construction (2) when applicable. The helper I NIT CERT in line 7 initializes the certificates αi, αi,and εi according to (6). The helper U PDATE CERT in line 26 updates αi according to (7), while recomputing αi and εi

according to (6). The algorithm employs three routines that prune box collection O. When αi > 0, line 10 calls competitive prun-ing, CP RUNE (O, α i, i ), which returns the set of competitive boxes {B ∈ O | ei(B) ≤ αi}, as justified by Lemma 3. When αi = 0, then Lemma 2 and nonnegativity of ei

imply αi(Di−1) = 0 . In this case, line 13 calls threshold pruning, TP RUNE (O, i ), which returns {B ∈ O | qi(B) ≤

γi}, as justified by Lemma 4. Finally, domain pruning, DP RUNE (O, i ) in lines 5 and 22 removes boxes that are not competitive for some previous rule level j < i . Specifically, for i = 1 , DP RUNE (O, i ) simply returns O. For i > 1, it returns {B ∈ O | ej (B) ≤ αj , ∀j < i }. Although CP RUNE 

and TP RUNE remove boxes that are not competitive for the current rule level i, bisection (lines 19–21) may create child boxes that are no longer competitive for an earlier rule level 

j < i , even when their parent box was. Thus, DP RUNE is invoked before I NIT CERT and U PDATE CERT to remove such boxes and ensure that αi is computed only from boxes that have not already been certified to lie outside Di−1.The set R of refinable boxes is constructed in line 11 when αi > 0 and line 14 when αi = 0 . When αi > 0,

R is simply the box collection remaining after CP RUNE .When αi = 0 , line 14 calls TR EFINE (O, i ), which returns the set of ambiguous boxes {B ∈ O | qi(B) > γ i}. If no ambiguous box remains, line 16 terminates the current level using Lemma 4. Otherwise, lines 19–21 bisect the widest refinable box along the longest dimension. Specifically, sup-pose B⋆ = Qdk=1 [ak, b k]. Choose the longest side j ∈

arg max k(bk −ak). Split at the middle mj = ( aj +bj )/2 and generate B− =  Q  

> k<j

[ak, b k] × [aj , m j ] ×  Q  

> k>j

[ak, b k]

and B+ =  Q  

> k<j

[ak, b k] × [mj , b j ] ×  Q  

> k>j

[ak, b k].

Algorithm 1 Anytime Optimization with Rulebooks  

> 1:

D0 ← Π 

> 2:

initialize finite box collection O such that D0 ⊆S 

> B∈O

B 

> 3:

initialize nonempty finite C ⊆ D0 

> 4:

for i = 1 , . . . , m do  

> 5:

O ← DP RUNE (O, i ) 

> 6:

BOUNDS (O, i ) 

> 7:

INIT CERT (O, C, i ) 

> 8:

while computational budget remains do  

> 9:

if αi > 0 then  

> 10:

O ← CP RUNE (O, α i, i ) 

> 11:

R ← O  

> 12:

else  

> 13:

O ← TP RUNE (O, i ) 

> 14:

R ← TR EFINE (O, i ) 

> 15:

if R = ∅ then  

> 16:

break  

> 17:

end if  

> 18:

end if  

> 19:

B⋆ ← arg max B∈R wi(B) 

> 20:

(B+, B −) ← BISECT (B⋆) 

> 21:

O ← REPLACE (O, B ⋆, {B+, B −}) 

> 22:

O ← DP RUNE (O, i ) 

> 23:

S ← CS AMPLES ({B+, B −}, D i−1) 

> 24:

C ← C ∪ S  

> 25:

BOUNDS ({B+, B −}, i ) 

> 26:

UPDATE CERT (O, C, i ) 

> 27:

end while  

> 28:

Di ← PROPAGATE (Di−1, α i, i ) 

> 29:

C ← C ∩ Di 

> 30:

end for  

> 31:

return B EST (C, m ) and gaps ε1: m

The helper CS AMPLES ({B+, B −}, D i−1) in line 23 re-turns a finite set of sampled policies from B+ and B− that are certified to lie in Di−1. The sampling procedure may select any policies from B+ and B−, e.g., their centers, and returns only those certified to lie in Di−1. For i = 1 ,certification simply requires checking that a sampled policy 

π lies in Π. For i > 1, this can be done by checking that 

ej (π) ≤ αj for all j < i . For efficiency, CS AMPLES may certify an entire box by checking that ej (B) ≤ αj for all 

j < i , which guarantees that every policy in B lies in Di−1.After processing rule level i, line 28 calls P ROPAGATE ,which conceptually returns the set in (1) and line 29 filters the incumbents to C ∩ Di before the next level. In prac-tice, however, the algorithm does not have to construct Di

explicitly. Since Di is only used for membership tests (in CS AMPLES and line 29), it suffices to store only the thresh-old αi, from which membership in Di can be determined. Also, note that the filtered set C ∩ Di in line 29 is nonempty because arg min π∈C ei(π) ⊆ C ∩ Di. Finally, B EST (C, m ) in line 31 returns any element of arg min π∈C em(π).C. Correctness of Algorithm 1 

To prove the correctness of Algorithm 1, we begin by showing that the pruning step preserves the coverage prop-erty required by the subsequent correctness proof. For a ∈ R,define bDi(a) = {π ∈ Di−1 | ei(π) ≤ a}.

Lemma 5: Suppose bDi(αi) ⊆ S 

> B∈O

B holds immedi-ately before the conditional statement (line 9). Then, the same inclusion holds immediately after the execution of CP RUNE (line 10) when αi > 0, and immediately after the execution of TP RUNE (line 13) when αi = 0 .

Proof: First, suppose αi > 0. Consider an arbitrary 

π ∈ bDi(αi). By definition, ei(π) ≤ αi. Also, by assumption, 

bDi(αi) ⊆ S 

> B∈O

B, so there exists B ∈ O with π ∈ B

immediately before line 10. By (4), ei(B) ≤ ei(π) ≤ αi, so CP RUNE retains B. Hence, bDi(αi) ⊆ S 

> B∈O

B after line 10. Now suppose αi = 0 . Then, bDi(αi) = Di−1 ∩ Πi. Con-sider an arbitrary π ∈ bDi(αi). Since π ∈ Πi, by definition, 

qi(π) ≤ γi. Also, by assumption, bDi(αi) ⊆ S 

> B∈O

B, so there exists B ∈ O with π ∈ B immediately before line 13. By (3), qi(B) ≤ qi(π) ≤ γi, so TP RUNE retains B. Hence, 

bDi(αi) ⊆ S 

> B∈O

B after line 13. We now establish the invariants of a fixed rule level in the next four lemmas. For these lemmas, fix an arbitrary rule level i ∈ { 1, . . . , m } and assume that Assumption 2 holds, and that, at the beginning of level i, immediately before line 5, Di−1 is compact, Di−1 ⊆ S 

> B∈O

B, and C

is a nonempty finite subset of Di−1. Let 

Mi = arg min 

> π∈Di−1

ei(π), (8) denote the set of minimizers of ei over Di−1. Since Di−1 is compact and ei is continuous, Mi is nonempty and compact. 

Lemma 6: Immediately after the execution of I NIT CERT 

(line 7) and immediately before and after every execution of UPDATE CERT (line 26) during level i, Mi ⊆ bDi(αi).

Proof: Immediately after line 7, αi is computed from 

C ⊆ Di−1, which is nonempty by assumption. Hence 

αi(Di−1) ≤ αi, and therefore Mi ⊆ bDi(αi).During each while-loop iteration, no line before line 26 changes αi. Therefore the inclusion that holds immediately after the preceding execution of I NIT CERT or U PDATE CERT 

still holds immediately before line 26. Immediately after line 26, αi is recomputed from C ⊆ 

Di−1, which is nonempty because it is nonempty at the beginning of the level by assumption, and every preceding execution of line 24 adds S ⊆ Di−1 returned by CS AMPLES .Thus αi(Di−1) ≤ αi, and hence Mi ⊆ bDi(αi) immediately after line 26. 

Lemma 7: At the beginning of each iteration of the while loop, i.e., immediately before line 9, 

bDi(αi) ⊆ [

> B∈O

B. (9) 

Proof: We use induction over the iterations of the while loop to show that (9) holds at the beginning of every iteration. Before line 5, Di−1 ⊆ S 

> B∈O

B by assumption. If i = 1 ,DP RUNE does not remove any box from O. Suppose i > 

1 and consider any π ∈ Di−1. Since Di−1 ⊆ S 

> B∈O

B,there exists a box B ∈ O with π ∈ B. Since π ∈ Di−1,

ej (π) ≤ αj for all j < i . By (4), ej (B) ≤ ej (π) ≤ αj for all j < i . Thus line 5 retains B. Therefore Di−1 ⊆ S 

> B∈O

B

still holds after line 5. Since bDi(αi) ⊆ Di−1 ⊆ S 

> B∈O

B,(9) holds before the first iteration of the while loop. The only lines in the while loop that can remove points from S 

> B∈O

B are the pruning lines 10, 13, and 22. Note that while line 21 replaces B⋆ by B+ and B−, by construction 

B⋆ = B+ ∪B−, so S 

> B∈O

B is unchanged. Additionally, the only line in the while loop that can change the value of αi is 26. We prove (9) by induction over the while-loop iterations, considering only these relevant lines. Suppose (9) holds at the beginning of an arbitrary iteration, and let a be the value of αi at that time. Lemma 5 gives 

bDi(a) ⊆ S 

> B∈O

B after line 10 when a > 0 and after line 13 if a = 0 . Consider line 22 and an arbitrary π ∈ bDi(a).Since bDi(a) ⊆ S 

> B∈O

B before this line, there exists B ∈ O 

with π ∈ B. Since π ∈ bDi(a), by definition, π ∈ Di−1, so 

ej (B) ≤ ej (π) ≤ αj for all j < i . Thus, line 22 retains B

and bDi(a) ⊆ S 

> B∈O

B immediately before line 26. Finally, line 26 cannot increase αi because C only grows in the while loop in line 24. Thus, after line 26, bDi(αi) ⊆

bDi(a), and (9) holds at the beginning of the next iteration. 

Lemma 8: Immediately before every execution of INIT CERT and U PDATE CERT during level i,

∅̸ = C ⊆ Di−1 and Mi ⊆ [

> B∈O

B. (10) 

Proof: We first prove ∅̸ = C ⊆ Di−1 immediately before lines 7 and 26. This holds before line 7 by assumption. During the while loop, the only line that changes C before line 26 is line 24, which adds the finite set S ⊆ Di−1

returned by CS AMPLES . Hence, ∅̸ = C ⊆ Di−1 immediately before every execution of lines 7 and 26. We next prove Mi ⊆ S 

> B∈O

B immediately before line 7. From the proof of Lemma 7, Di−1 ⊆ S 

> B∈O

B holds after line 5, and hence Mi ⊆ Di−1 ⊆ S 

> B∈O

B immediately before line 7. It remains to prove Mi ⊆ S 

> B∈O

B immediately before line 26. From the proof of Lemma 7, right before line 26, 

bDi(αi) ⊆ S 

> B∈O

B. Lemma 6, applied immediately before line 26, gives Mi ⊆ bDi(αi) ⊆ S 

> B∈O

B immediately before line 26. 

Lemma 9: If the while loop terminates, then at the end of level i (i.e., immediately after line 29), (i) Di is compact; (ii) Di ⊆ S 

> B∈O

B;(iii) αi − αi(Di−1) ≤ εi;(iv) C is a nonempty finite subset of Di.

Proof: To prove (i), we note that the only line at level 

i that assigns a value to Di is line 28, which simply sets 

Di = bDi(αi). Since ei is continuous, bDi(αi) is a closed subset of Di−1. Since Di−1 is compact, Di = bDi(αi) is compact. To prove (ii), from Lemma 7, the inclusion (9) holds at the beginning of each iteration of the while loop. If the computational budget is exhausted before an iteration begins or immediately after line 26, the same inclusion holds immediately before line 28. If line 16 terminates the loop, then αi = 0 . From Lemma 7, the inclusion (9) holds immediately before line 9 and Lemma 5 gives the same inclusion after line 13. Therefore, in all cases, immediately before line 28, the inclusion (9) holds. Since line 28 sets 

Di = bDi(αi), we have Di ⊆ S 

> B∈O

B.To prove (iii), Lemma 8 verifies the hypotheses of Lemma 2 at every execution of I NIT CERT and U PDATE -CERT . Therefore the final certificate values at level i satisfy 

αi ≤ αi(Di−1). Subtracting αi from this inequality and replacing εi = αi − αi give αi − αi(Di−1) ≤ εi.Finally, we prove (iv). Immediately before line 29, C is finite and satisfies C ⊆ Di−1, because by assumption, it is a nonempty finite subset of Di−1 at the beginning of the level, and the only line that modifies it before line 29 is line 24, which adds the finite set S ⊆ Di−1 returned by CS AMPLES .The set C is also nonempty because it is nonempty at the beginning of the level and the algorithm only adds policies before line 29. Choose πinc ∈ arg min π∈C ei(π). By (6), 

ei(πinc ) = αi. Since πinc ∈ Di−1, line 28 implies πinc ∈ Di.Therefore C ∩ Di is nonempty. Line 29 replaces C by C ∩ Di,so after that line C is a nonempty finite subset of Di.We now use the invariants established in Lemma 9 to prove the correctness of Algorithm 1. 

Theorem 1: Suppose Assumptions 1 and 2 hold, and each rule level terminates. Then, Algorithm 1 returns a policy bπ

that satisfies ei(bπ) − αi(Di−1) ≤ εi, for all i ∈ { 1, . . . , m },i.e., εi is a valid finite-budget optimality certificate. 

Proof: We first show that the hypotheses of Lemma 9 hold at the beginning of every rule level. The proof is by induction on the level i. For i = 1 , line 1 sets D0 = Π ,which is compact by Assumption 1. Line 2 initializes O so that D0 ⊆ S 

> B∈O

B, and line 3 initializes C as a nonempty finite subset of D0. Hence Lemma 9 applies at level 1.Now suppose the hypotheses of Lemma 9 hold at the beginning of an arbitrary level i. Since level i terminates by assumption, Lemma 9 implies, immediately after line 29, that Di is compact, Di ⊆ S 

> B∈O

B, and C is a nonempty finite subset of Di. Thus, the hypotheses of Lemma 9 hold at the beginning of level i + 1 . By induction, they hold at the beginning of every rule level. Applying Lemma 9(iii) at each level gives 

αi − αi(Di−1) ≤ εi, i = 1 , . . . , m. (11) Lemma 9(iv), applied at level m, gives ∅̸ = C ⊆ Dm after line 29. Therefore line 31 is well-defined and returns some 

bπ ∈ C ⊆ Dm. Additionally, by definition of Di in (1), Di ⊆

Di−1 for all rule level i. Hence Dm ⊆ Di for every i ≤

m. Since bπ ∈ Dm, we have bπ ∈ Di for every i. Using the definition of Di again gives ei(bπ) ≤ αi for every i.Combining this with (11) yields ei(bπ) − αi(Di−1) ≤ αi −

αi(Di−1) ≤ εi for all i ∈ { 1, . . . , m }.

D. Convergence 

The previous subsection proves that Algorithm 1 returns a policy with a certified optimality gap εi at every rule level 

i. This subsection studies the asymptotic behavior of the algorithm as the computational budget increases. Throughout this subsection, fix an arbitrary rule level i ∈ { 1, . . . , m } and assume the hypotheses of Lemma 9 hold. We index the reachable snapshots of the while loop by the number N ∈ N of completed full iterations of the while loop at level i. Snapshot N is taken in the (N + 1) -st iteration of the while loop, immediately after the construction of R, namely after line 11 when αi > 0, and after line 14 when αi = 0 . The incumbent set and certificate values at snapshot N are denoted by C(N ), αi(N ), αi(N ), and εi(N ).These certificates are those computed by I NIT CERT (line 7) when N = 0 and by U PDATE CERT (line 26) at the end of the N -th iteration when N ≥ 1 before the subsequent pruning step. Let O−(N ) denote the corresponding value of O at this certificate-update line. The subsequent pruning step uses αi(N ) to construct the box collections O(N ) and 

R(N ). Since the pruning step does not modify C(N ) or recompute the certificates, the pre-pruning quantities C(N ),

αi(N ), αi(N ), and εi(N ) and the post-pruning collections 

O(N ) and R(N ) all belong to the same snapshot N .The set of boxes selected for possible refinement is 

R(N ) = 

(

O(N ), αi(N ) > 0,

{B ∈ O (N ) | qi(B) > γ i}, αi(N ) = 0 . (12) If line 16 terminates level i, this final snapshot has R(N ) = 

∅. Define the refinement width at snapshot N by 

δi(N ) = 

(

max B∈R (N ) wi(B), R(N )̸ = ∅,

0, R(N ) = ∅. (13) The first two results show that longest-side refinement drives this refinement width to zero. 

Lemma 10: The sequence {δi(N )}N ≥0 is monotonically nonincreasing. 

Proof: If Li = 0 , then (2) gives wi(B) = 0 for every box B, and the result is immediate. Suppose Li > 0. Since wi(B) = Li

√d l (B) and Li and 

d are constant, wi(B) is proportional to the longest side length l(B). Consider an arbitrary j < N . Since the next bisection is performed, R(j)̸ = ∅. Line 19 selects B⋆ ∈

arg max B∈R (j) wi(B), so wi(B⋆) = δi(j). Let B+ and B−

be the children created by line 20. Longest-side bisection gives l(B+), l (B−) ≤ l(B⋆). Hence, wi(B+), w i(B−) ≤

wi(B⋆) = δi(j).We want to show that wi(B) ≤ δi(j) for every B ∈ R (j +1) . By construction, R(j + 1) ⊆ (R(j) \ { B∗}) ∪ { B+, B −}

because lines 22, 10–11, and 13–14 only remove boxes or assign R from the boxes that remain after pruning. Since 

wi(B+), w i(B−) ≤ δi(j) and any B ∈ R (j) \ { B∗} is unchanged from snapshot j to j + 1 , we can conclude that 

wi(B) ≤ δi(j) for every B ∈ R (j + 1) . Therefore, if R(j +1) ̸ = ∅, then δi(j + 1) = max B∈R (j+1) wi(B) ≤ δi(j).Otherwise, δi(j + 1) = 0 ≤ δi(j).Theorem 2: δi(N ) → 0.

Proof: If Li = 0 , then δi(N ) = 0 for all N . Suppose 

Li > 0. By Lemma 10, {δi(N )}N ≥0 is monotonically nonincreasing and bounded below by zero. Hence, by the monotone convergence theorem, there exists δ∞ ≥ 0 such that δi(N ) → δ∞. Suppose, for contradiction, that δ∞ > 0,and set ε = δ∞/2 and η = ε/ (Li

√d).Let Fη = {B ∈ S  

> N∈N

O(N ) | l(B) ≥ η} be the set of generated boxes whose longest side length is at least η. We first show that Fη is finite. Fix B0 ∈ O (0) and choose q ∈

N such that l(B0)/2q < η . Consider an arbitrary sequence 

B0, B 1, . . . , B H in which Bj+1 is one of the two boxes obtained from Bj by line 20. It is enough to bound H such that l(BH ) ≥ η.Let cj ∈ {1, . . . , d } be the coordinate bisected when passing from Bj to Bj+1 . Since bisection does not increase any side length, l(B0) ≥ l(B1) ≥ · · · ≥ l(BH ). Thus, if 

l(BH ) ≥ η, then for every j ∈ { 0, . . . , H − 1},

lcj (Bj ) = l(Bj ) ≥ η > l (B0)/2q , (14) where the equality follows from line 20, which always bisects a longest side of the box. Fix a coordinate c ∈ { 1, . . . , d }, and let j1 < · · · < j h be all indices j ∈ { 0, . . . , H − 1} such that cj = c. Then, 

lc(Bjs ) = lc(B0)/2s−1, ∀s ∈ { 1, . . . , h } (15) because only previous splits with cj = c change this side length. Additionally, setting j = js in (14) gives lc(Bjs ) >l(B0)/2q . Combining this with (15) gives 

lc(B0)2s−1 > l(B0)2q , ∀s ∈ { 1, . . . , h }.

Since lc(B0) ≤ l(B0), this is possible only if s ≤ q. Hence 

h ≤ q for every coordinate c. Summing over coordinates gives H = Pdc=1 |{ j ∈ { 0, . . . , H − 1} | cj = c}| ≤ dq .Therefore, for this fixed B0, every generated box B with 

l(B) ≥ η is obtained after at most dq bisections from B0.There are at most Pdq k=0 2k such boxes. Since O(0) is finite, the total number of generated boxes B with l(B) ≥ η is finite. Since δi(N ) → δ∞ and ε = δ∞/2 > 0, based on the definition of convergence, there exists N0 such that 

δi(N ) ≥ ε > 0, ∀N ≥ N0. (16) Since δi(N ) > 0, it follows from (13) that R(N )̸ = ∅ for all N ≥ N0. Hence line 19 selects a box B⋆N ∈ R (N ) with 

wi(B⋆N ) = δi(N ). Using wi(B) = Li

√d l (B) and (16), 

l(B⋆N ) = δi(N )

Li

√d ≥ εLi

√d = η. 

Thus 

B⋆N ∈ F η , ∀N ≥ N0. (17) Because there are infinitely many indices N ≥ N0, (17) requires infinitely many selections from Fη . But whenever a generated box B is selected at line 19, lines 20–21 remove 

B from O and replace it by its two children. Hence the same generated box cannot be selected at any later snapshot. Therefore (17) requires infinitely many distinct boxes in Fη ,contradicting the fact that Fη is finite. This contradiction shows that δ∞ = 0 .We now analyze the asymptotic behavior of the certified optimality gap εi(N ) and the policies returned by the algo-rithm. We first prove the convergence of εi(N ).

Lemma 11: The sequence {εi(N )}N ≥0 is monotonically nonincreasing and converges to a limit εi, ∞ ≥ 0.

Proof: During a fixed rule level, C(N ) only grows, so 

αi(N ) = min π∈C (N ) ei(π) is monotonically nonincreasing. By the update rule in (7), αi(N ) is monotonically nonde-creasing. Hence εi(N ) = αi(N ) − αi(N ) is monotonically nonincreasing. Lemma 2 and Lemma 8 give αi(N ) ≤

αi(Di−1) ≤ αi(N ), so εi(N ) ≥ 0. By the monotone convergence theorem, {εi(N )}N ≥0 converges. The preceding lemma does not characterize the limiting value εi, ∞. To show that εi(N ) converges to zero, we additionally assume that line 2 initializes O as an exact finite union of closed axis-aligned boxes representing Π.Additionally, we assume that the incumbent set contains a certified policy whose value approaches the optimum over 

Di−1 at the rate of the refinement width. 

Assumption 3: At line 2, O is initialized as a finite set of closed axis-aligned boxes satisfying Π = S 

> B∈O

B.

Assumption 4: There exists κi ≥ 0 such that, for every reachable snapshot N , there is a policy πN ∈ C (N ) ∩ Di−1

satisfying 

ei(πN ) ≤ αi(Di−1) + κiδi(N ). (18) Assumption 3 is satisfied whenever Π can be represented as a finite union of closed axis-aligned boxes. Assumption 4 requires the incumbent set to contain a policy whose excess risk converges to the optimum at the same rate as the box resolution. It is satisfied, e.g., if CS AMPLES generates a sufficiently dense set of certified policies in Di−1, or if it generates one certified policy from each box in the current box collection. Since Mi ⊆ S  

> B∈O (N)

B, at least one box in the current box collection contains a minimizer. At snapshots with αi(N ) > 0, (12) gives R(N ) = O(N ), so every box in O(N ) has width at most δi(N ). Lipschitz continuity then implies that a certified policy sampled from such a box has excess risk within O(δi(N )) of αi(Di−1).

Lemma 12: Under Assumption 3, every box generated by Algorithm 1 satisfies B ⊆ Π and c(B) ∈ Π.

Proof: By Assumption 3, the initial box collection satisfies Π = S 

> B∈O

B. Thus, every initial box B ∈ O 

satisfies B ⊆ Π. Algorithm 1 refines boxes only by axis-aligned bisection, which replaces a box by subsets of that box. Thus, every generated box remains contained in its initial ancestor and therefore in Π. Hence, B ⊆ Π for every generated box B. Because axis-aligned boxes are convex, their centers satisfies c(B) ∈ B ⊆ Π.By Lemma 12 and Lemma 1, the center-based construc-tion (2) is a valid implementation of B OUNDS . We next show that centers of boxes in O(N ) become arbitrarily close to the certified admissible domain as the box size decreases. For any nonempty compact set S ⊆ Π and any π ∈ Π, define 

dist( π, S ) = min π′∈S ∥π − π′∥2.

Lemma 13: Suppose Assumption 3 holds. Let 

{Nk}k≥1 ⊆ N be a sequence of reachable snapshot indices with Nk → ∞. For each k, choose one box 

Bk ∈ O (Nk). If l(Bk) → 0, then dist( c(Bk), D i−1) → 0.

Proof: By Lemma 12, Bk ⊆ Π and c(Bk) ∈ Π for every k. If i = 1 , then D0 = Π , so dist( c(Bk), D 0) = 0 for every k.Suppose i > 1. Consider a sequence {Bk} with l(Bk) →

0. We first show that every convergent subsequence of 

{c(Bk)} has its limit in Di−1. Consider an arbitrary sub-sequence of {c(Bk)}, denote its indices by k1 < k 2 < . . . ,and let its limit be π0. In other words, c(Bkℓ ) → π0 as 

l → ∞ . Since c(Bkℓ ) ∈ Π for every ℓ and Π is compact, 

π0 ∈ Π.Now fix an arbitrary j < i . Since Bkℓ ∈ O (Nkℓ ) ⊆O−(Nkℓ ), the box Bkℓ has already been processed by DP RUNE (line 5 when Nkℓ = 0 and line 22 when Nkℓ ≥ 1). Hence, ej (Bkℓ ) ≤ αj . Combining this with (4) and (5) gives 

ej (c(Bkℓ )) ≤ ej (Bkℓ )

≤ ej (Bkℓ ) + wj (Bkℓ )

≤ αj + wj (Bkℓ ).

Since l(Bkℓ ) → 0, wj (Bkℓ ) = Lj

√d l (Bkℓ ) → 0. Moreover, since c(Bkℓ ) → π0 and ej is continuous, ej (c(Bkℓ )) →

ej (π0). Taking limits in the inequality above therefore yields 

ej (π0) ≤ αj . Since j < i was arbitrary, we can conclude that ej (π0) ≤ αj for all j < i . Therefore 

π0 ∈ { π ∈ Π | ej (π) ≤ αj , ∀j ∈ { 1, . . . , i − 1}} = Di−1.

Suppose, for contradiction, that dist( c(Bk), D i−1)̸ → 0.Then there exist ϵ > 0 and a subsequence, which we relabel as {Bk}, such that dist( c(Bk), D i−1) ≥ ϵ for every k. Since every c(Bk) ∈ Π for every k and Π

is compact, the Bolzano–Weierstrass theorem implies that 

{c(Bk)} has a convergent subsequence {c(Bkℓ )}. By the preceding paragraph, c(Bkℓ ) → π0 for some π0 ∈ Di−1.Thus, dist( c(Bkℓ ), D i−1) ≤ ∥ c(Bkℓ ) − π0∥2 → 0. But this contradicts dist( c(Bkℓ ), D i−1) ≥ ϵ for every ℓ.

Theorem 3: Under Assumptions 3 and 4, εi(N ) → 0.

Proof: Let a⋆i = αi(Di−1). Lemma 2 and Lemma 8 give, for every reachable snapshot N ,

αi(N ) ≤ a⋆i ≤ αi(N ). (19) We first prove the result when Li = 0 . In this case, qi and 

ei are constant on Π. Since ∅̸ = C(N ) ⊆ Di−1, we have 

αi(N ) = a⋆i . Also, ei(B) = a⋆i for every B ∈ O −(N ), so (6)–(7) give αi(N ) = a⋆i . Hence εi(N ) = 0 for every N .Assume Li > 0. Since αi(N ) is nonnegative and non-increasing, either αi(N ) = 0 eventually or αi(N ) > 0

for every N . We next prove the result when αi(N ) = 0 

eventually. By (19), a⋆i = 0 eventually. Since ei(B) ≥ 0

and (6)–(7) preserve αi(N ) ≥ 0, (19) gives αi(N ) = 0 

eventually. Hence εi(N ) = 0 eventually. It remains to prove the result when Li > 0 and αi(N ) > 0

for every N . Then (12) gives R(N ) = O(N ). Assumption 4 gives πN ∈ C (N ) ∩ Di−1 such that ei(πN ) ≤ a⋆i + κiδi(N ).Since πN ∈ C (N ), αi(N ) ≤ ei(πN ). Therefore 

0 ≤ αi(N ) − a⋆i ≤ κiδi(N ). (20) We now prove that αi(N ) → a⋆i . Since R(N ) = O(N ),(13) gives wi(B) ≤ δi(N ) for every B ∈ O(N ). By Theorem 2, δi(N ) → 0, and since wi(B) = Li

√d l (B),we have max B∈O (N ) l(B) → 0. Together with Lemma 13, this implies max B∈O (N ) dist( c(B), D i−1) → 0. Thus, ηN =max B∈O (N )

 dist( c(B), D i−1) + rad( B) → 0.Fix B ∈ O (N ) and choose πB ∈ Di−1 such that ∥c(B) −

πB ∥2 = dist( c(B), D i−1). Then, 

ei(B) = max {qi(c(B)) − Li rad( B) − γi, 0}≥ ei(c(B)) − Li rad( B)

≥ ei(πB ) − Li∥c(B) − πB ∥2 − Li rad( B)

≥ a⋆i − LiηN .

Therefore 

mN = min   

> B∈O (N)

ei(B) ≥ a⋆i − LiηN . (21) We now relate mN to αi(N ). By the snapshot convention, 

O−(N ) is the box collection used in I NIT CERT when N = 0 

and in U PDATE CERT when N ≥ 1. Thus 

αi(N ) ≥ min   

> B∈O −(N)

ei(B). (22) Since αi(N ) > 0, line 10 produces O(N ) from O−(N )

by removing exactly the boxes satisfying ei(B) > α i(N ).Lemma 6 gives Mi ⊆ bDi(αi(N )) . Lemma 7 and Lemma 5 give bDi(αi(N )) ⊆ S  

> B∈O (N)

B. Hence Mi ⊆ S  

> B∈O (N)

B.Therefore there exist πm ∈ Mi and Bm ∈ O (N ) such that 

πm ∈ Bm. By (4) and (19), mN ≤ ei(Bm) ≤ ei(πm) = 

a⋆i ≤ αi(N ). Therefore every box removed from O−(N )

has lower bound strictly larger than mN , and 

min  

> B∈O −(N)

ei(B) = min   

> B∈O (N)

ei(B) = mN . (23) Combining (21), (22), and (23) gives 

αi(N ) ≥ a⋆i − LiηN . (24) Finally, (19), (20), and (24) imply 

0 ≤ εi(N ) = αi(N ) − αi(N ) ≤ κiδi(N ) + LiηN .

Since δi(N ) → 0 and ηN → 0, εi(N ) → 0.

Theorem 4: For every reachable snapshot N , let bπN ∈

arg min π∈C (N ) ei(π). Under Assumptions 3 and 4, if 

{Nk}k≥1 is a sequence of reachable snapshot indices with 

Nk → ∞ and bπNk → bπ, then bπ ∈ Mi.

Proof: By the definition of bπN and αi(N ), ei(bπN ) = 

αi(N ) for every snapshot N . By Lemma 2 and Lemma 8, 

0 ≤ ei(bπN ) − αi(Di−1) ≤ αi(N ) − αi(N ) = εi(N ). By Theorem 3, εi(N ) → 0. Therefore, along the given sequence 

Nk → ∞ , ei(bπNk ) → αi(Di−1).It remains to show that the limiting policy bπ attains this optimal value. Since bπNk ∈ C (Nk) ⊆ Di−1 for every k and 

Di−1 is compact, the limit satisfies bπ ∈ Di−1. By continuity of ei, ei(bπ) = lim k→∞ ei(bπNk ) = αi(Di−1). Since bπ ∈

Di−1 and achieves the minimum value of ei over Di−1, we can conclude that bπ ∈ Mi.V. E XPERIMENTAL RESULTS 

We evaluate Algorithm 1 in two complementary settings. The synthetic benchmark provides a known ground-truth solution for quantitative evaluation, whereas the highway merging simulation demonstrates the algorithm’s practical applicability to black-box risk-aware control. 

A. Synthetic Benchmark 

We first evaluate Algorithm 1 on a synthetic single-rule scenario ( m = 1 ) for which the exact solution is available in closed form, and compare it against 2 baseline optimizers. For simplicity, we omit the subscript 1 throughout this subsection and write q, e, α, γ, and L instead of q1,

e1, α1, γ1, and L1. Because the exact solution is known, this experiment enables direct evaluation of the algorithm’s convergence, certificate quality, and anytime performance. 

Setup. The policy space is the box Π = [ −1, 1] 2 and the risk-evaluation function is the shifted Rosenbrock-type function 

q(π) = q(x, y ) = 12

  12 − x2 + ( y − x2)2. (25) This function has a unique minimizer at π⋆ = (0 .5, 0.25) ,where q(π⋆) = 0 . When γ = 0 , α(Π) = 0 , the rule is satisfied only at π⋆, and Π⋆ 

> 1

= Π 1 = {π⋆} provide an exact solution against which all optimizers can be compared. Any 

γ > 0 enlarges the feasible set to the sublevel set {π : q(π) ≤

γ} around π⋆, in which every policy attains the minimum excess risk α(Π) = 0 .Since |∂q/∂x | = |x− 12 −4x(y−x2)| ≤ | x− 12 |+4 |x|(|y|+

x2) ≤ 9.5 and |∂q/∂y | = 2 |y − x2| ≤ 2( |y| + x2) ≤ 4 for all x, y ∈ [−1, 1] , ∥∇ q(x, y )∥2 ≤ √9.52 + 4 2 ≈ 10 .31 , so any L ≥ 10 .31 is a valid Lipschitz constant. We use the conservative value L = 11 throughout. Because Π is a single closed axis-aligned box, Assump-tion 3 is satisfied by initializing the box collection as O =

{Π}. By Lemma 12 and Lemma 1, we implement B OUNDS 

using the center-based construction in (2) and implement CS AMPLES by returning the centers of newly generated child boxes, which are automatically certified to be in Π. The initial incumbent set is C = c(Π) = (0 , 0) .

Optimizers. We compare Algorithm 1 against 2 baselines. The first is a best-first Lipschitz branch-and-bound (B&B) [27]. It uses the same the box representation as Algorithm 1 but differs in box selection. Instead of the widest-refinable-box rule on line 19, it applies the best-first selection B⋆ ←

arg min B∈O e(B). The selected box is bisected and replaced by its children, while boxes satisfying e(B) > α are discarded. This pruning criterion is identical to CP RUNE on line 10 of Algorithm 1. We consider two variants of this baseline that differ only in the computation of e(B). The 0 200 400 600 800                           

> N(iterations)
> 10 4
> 10 3
> 10 2
> 10 1
> 10 0
> 10 1
> (N) Algorithm 1 (proposed) Best-first B&B (classical) Best-first B&B (thresholded) Uniform grid 0200 400 600 800
> N(iterations)
> 010 6
> 10 5
> 10 4
> 10 3
> 10 2
> 10 1
> 10 0
> 10 1
> (N) Algorithm 1 (proposed) Best-first B&B (classical) Best-first B&B (thresholded) Uniform grid 0200 400 600 800
> N(iterations)
> 2 × 10 0
> 3 × 10 0
> 4 × 10 0
> | (N)|
> Algorithm 1 (proposed) Best-first B&B 0200 400 600 800
> N(iterations)
> 2 × 10 0
> 3 × 10 0
> 4 × 10 0
> | (N)|
> Algorithm 1 (proposed) Best-first B&B

Fig. 1. The shifted Rosenbrock-type single-rule scenario. The left and right columns correspond to γ = 0 and γ = 0 .05 . The top row shows the anytime gap ε(N ). The bottom row shows the cover measure |O (N )|. The two best-first variants generate the same O, so only one curve is shown. 

classical variant treats the excess-risk function e as an L-Lipschitz function and use e(B) = e(c(B)) −L rad( B). The 

thresholded variant instead uses the certified lower bound 

e(B) defined in (2), which exploits the threshold structure of e. Both variants maintain the same incumbent set C and the upper certificate α as in Algorithm 1. The second baseline is uniform grid search, where the policy space is discretized into an axis-aligned n × n regular grid with n equally spaced values per axis, giving n2 grid points. At iteration ℓ ∈ { 1, . . . , n 2}, the algorithm evaluates 

e at πℓ =  −1 + (( ℓ−1) mod n) ∆ , −1 + ⌊(ℓ−1) /n ⌋ ∆,where ∆ = 2 

> n−1

. The incumbent is bπN = arg min ℓ≤N e(πℓ).We refer to Algorithm 1 and both best-first B&B variants collectively as the box-based methods . For each optimizer, we record an anytime trace indexed by the iteration counter 

N . Each snapshot contains the best incumbent bπN and its excess risk e(bπN ). For the box-based methods, we addition-ally record the certified lower and upper certificates α(N )

and α(N ), the certified optimality gap ε(N ) = α(N ) −

α(N ), the current box collection O(N ) together with the per-box bounds q(B) and q(B), and the total Lebesgue measure of the cover, |O (N )| = P 

> B∈O (N)

Qdj=1 lj (B).Since the uniform-grid baseline does not produce certified lower bounds, we use ε(N ) = e(bπN ) − α(Π) , in place of the certified optimality gap. The computational budget is specified by the iteration limit 

Nmax . For the box-based methods, one iteration performs one box bisection and two evaluations of q at the child-box centers, for a total of 2Nmax black-box evaluations. For the uniform-grid baseline, one iteration evaluates q at a single grid point. Unless stated otherwise, we set Nmax = 900 for all methods, corresponding to a 30 × 30 grid. 

Results. Fig. 1 compares the 4 optimizers for γ = 0 (satisfied only at π⋆) and γ = 0 .05 (satisfied on a nontrivial sublevel set). The gap ε of classical best-first B&B remains large in both cases because it repeatedly refines only the most promising box, leaving many boxes with overly conservative lower bounds e(B). Replacing this lower bound by the thresholded construction in (2) removes this conservatism without changing the box collection O or incumbent set C

since the two variants perform the same refinements and generate the same incumbents. Algorithm 1 achieves the same certified optimality gap as the thresholded baseline in both experiments, while the uniform-grid baseline reaches comparable incumbent quality but does not provide a certi-fied optimality gap. The benefit of Algorithm 1 is most apparent in the cover measure |O (N )|, which it contracts substantially faster than both best-first B&B variants. This difference reflects the different objectives of the refinement strategies. Best-first B&B is designed to locate a global minimizer efficiently, whereas Algorithm 1 is designed to certify and eliminate regions that cannot contain lexicographically optimal poli-cies. The widest-refinable-box rule spreads refinement across the competitive region so that regions far from π⋆ quickly become uncompetitive and can be pruned. Best-first B&B instead focuses refinement on the currently most promising box, so pruning relies primarily on improvements in the incumbent value α. When γ = 0 .05 , Algorithm 1 eventually switches from CP RUNE to TP RUNE after an incumbent π

with q(π) ≤ γ is found, but the algorithm continues to refine the widest ambiguous boxes and therefore maintains faster cover contraction than best-first B&B. These experiments isolate the contributions of the pro-posed method. Comparing the classical and thresholded best-first B&B isolates the benefit of exploiting the threshold structure, while comparing the thresholded best-first B&B with Algorithm 1 isolates the benefit of the widest-refinable-box rule. The former improves the certified optimality gap 

ε, whereas the latter produces substantially tighter box col-lection O.

B. Highway Merging Simulation 

We next consider a highway merging scenario where an ego vehicle merges into an adjacent lane between a front vehicle and a rear vehicle as shown in Fig. 2. The complexity of the uncertain vehicle interactions makes the resulting risk-evaluation functions impractical to characterize analytically, so they are treated as black boxes. 

Scenario. The ego vehicle starts in its current lane and must merge into a gap between front and rear vehicles in the adjacent target lane over a fixed time horizon [0 , T ]. We use 

X and Y to denote longitudinal and lateral coordinates in a straight-road frame aligned with the X-axis. The current-and target-lane centerlines are Y = 0 and Y = wlane ,respectively, and the merging corridor is L = [ X0, X T ] ×

[−wlane /2, 3wlane /2] .Let π denote an ego policy, which determines the ego trajectory, and let ω denote a realization of the surrounding vehicles’ behavior, which affects how they respond to the ego trajectory. We define collision avoidance rules r1,k , a rear-vehicle braking rule r2, three-second headway rules r3,k , and comfort rules r4,acc and r4,curv for k ∈ { front , rear } as 

rπ

> 1,k

(ω) = max  

> t∈[0 ,T ]

[dcol − dπk (t, ω )] +,rπ 

> 2

(ω) = max  

> t∈[0 ,T ]

[−aπ

> rear

(t, ω )] +,rπ

> 3,k

(ω) = max   

> t∈[0 ,T ]

χπ (t)[ Tgap − hπk (t, ω )] +,rπ

> 4,acc

(ω) = max  

> t∈[0 ,T ]



max 

 aπ

> ego

(t)

acomf 

, −aπ

> ego

(t)

−bcomf 



− 1



> +

rπ

> 4,curv

(ω) = max  

> t∈[0 ,T ]

 |κπ

> ego

(t)|

κcomf 

− 1



> +

.

(26) Here, dπk (t, ω ) denote the signed clearance from the ego vehicle to vehicle k. Thus, r1,k measures violations of the re-quired collision clearance dcol . The rear-vehicle acceleration 

aπ

> rear

(t, ω ) defines the braking magnitude [−aπ

> rear

(t, ω )] +,which is penalized by r2. The time headway hπk quantifies the longitudinal gaps to vehicle k. Thus, r3 measures the violations of the desired minimum time headway Tgap , while the ego vehicle occupies the target lane, as determined by the target-lane overlap factor χπ (t) ∈ [0 , 1] . The comfort rules r4,acc and r4,curv depend only on the ego trajectory through its longitudinal acceleration aπ

> ego

(t) and curvature 

κπ

> ego

(t), respectively. The constants dcol , Tgap , acomf , bcomf ,and κcomf specify the collision clearance, desired time head-way, comfortable acceleration, braking, and curvature. The numerical values of these constants are listed in Table I. The preorder relation is defined by r1,front , r 1,rear >r2 > r3,front , r 3,rear > r4,acc , r 4,curv , where r1,front and 

r1,rear have equal rank, as do r3,front and r3,rear . The two comfort rules r4,acc and r4,curv also have equal rank. We refine this rulebook by aggregating rules with equal rank to obtain a total-order rulebook RT with RT =

r1, r 2, r 3, r 4 and r1 > r2 > r3 > r4. For i ∈ {1, 3},the front and rear rules are aggregated using the maximum: 

rπi (ω) = max k∈{ front ,rear } rπi,k (ω). The two comfort rules are aggregated using a weighted sum: rπ 

> 4

(ω) = rπ

> 4,acc

(ω) + 

λcurv rπ

> 4,curv

(ω), where the dimensionless parameter λcurv ≥

0 controls the relative weight of curvature discomfort. Under these definitions, insufficient surrounding-vehicle response may lead to violations of r1 and r3, whereas an overly aggressive rear-vehicle response may violate r2.These effects are captured through the corresponding risk-evaluation functions. For the rules that involve uncertain interactions, we use CVaR as the risk measure: qi(π) = 

CVaR βi (rπi (ω)) for i ∈ { 1, 2, 3}, where βi ∈ (0 , 1) is the tail probability. The comfort rule r4 depends only on the ego trajectory, so rπ 

> 4

(ω) = rπ 

> 4

for all ω and q4(π) = rπ 

> 4

.

Flatness-based policy parameterization. We exploit differ-ential flatness to reduce the search dimension and parameter-ize the flat output by a B´ ezier curve in the Bernstein basis. The kinematic bicycle model underlying this representation is defined for a vehicle state (X, Y, ψ, v ), where (X, Y ) is the position of the rear-axle center, with steering input δ and longitudinal acceleration input a:

˙X = v cos ψ, ˙Y = v sin ψ, ˙ψ = vℓwb 

tan δ, ˙v = a. merging corridor L                 

> P2P3P4P5
> rear car front car
> P0
> P1
> P6P7
> zθ(t)
> merge gap free parameters θ= ( P2, P 3, P 4, P 5)
> Fig. 2. B´ ezier parameterization of the flat output for highway merging. The ego vehicle merges into the gap between the rear and front vehicles. The fixed control points P0,P1,P6,P7enforce the boundary positions and velocities. The free control points P2, . . . , P 5are constrained to lie within the indicated longitudinal slabs and form the decision vector θ.

The flat output is the rear-axle position z(t) = ( X(t), Y (t)) .Let Pj = ( xj , y j ) ∈ R2 denote the B´ ezier control points. The degree-n B´ ezier parameterization of the rear-axle tra-jectory is zθ (t) = Pnj=0 Pj (θ)βj,n (t/T ), where βj,n (τ ) =  nj

τ j (1 − τ )n−j is the Bernstein basis of degree n. We choose n = 7 so that, after fixing endpoint positions and velocities, four interior control points remain as free search variables. The endpoint position and velocity constraints are imposed by fixing 

P0 = ( X0, Y 0), P1 = ( X0, Y 0) + T

7 v0e(ψ0),P7 = ( XT , Y T ), P6 = ( XT , Y T ) − T

7 vT e(ψT ),

where e(ψ) = (cos ψ, sin ψ). This gives ˙zθ (0) = v0e(ψ0)

and ˙zθ (T ) = vT e(ψT ). The free control points form the decision vector θ = ( P2, . . . , P 5) ∈ R8.Let ∆X = XT − X0 > 0, Ymin = −wlane /2, and Ymax =3wlane /2. For each free control point j ∈ { 2, 3, 4, 5}, define 

x− 

> j

= X0 + j − 1/27 ∆X, x+ 

> j

= X0 + j + 1 /27 ∆X. 

The box of free B´ ezier control-point coordinates is 

Θbox =

> 5

Y

> j=2

 [x− 

> j

, x + 

> j

] × [Ymin , Y max ].

Thus, as illustrated in Fig. 2, each free control point can move laterally across the full corridor width while remaining within a longitudinal slab centered at its nominal progress location. These slabs give each component of θ a clear geometric interpretation and encourage forward progress without discretizing the full state-input trajectory. Assume that ∥ ˙zθ ∥2 > 0 over [0 , T ]. Given any θ, the vehicle state and inputs are recovered from zθ by 

vθ = ∥ ˙zθ ∥2, ψθ = atan2( ˙Yθ , ˙Xθ ),aθ = ˙z⊤ 

> θ

¨zθ

∥ ˙zθ ∥2

, δθ = arctan( ℓwb κθ ),

where the signed curvature of the rear-axle trajectory is 

κθ = ˙Xθ ¨Yθ − ˙Yθ ¨Xθ

∥ ˙zθ ∥32

. (27) The policy space for this experiment is 

Π = {θ ∈ Θbox |zθ (t) ∈ L , v θ (t) ≤ vmax ,

− bmax ≤ aθ (t) ≤ amax ,

|κθ (t)| ≤ κmax , |δθ (t)| ≤ δmax ,

∀t ∈ [0 , T ]}.

The constants vmax , amax , κmax , and δmax are physical lim-its, whereas acomf ≤ amax and κcomf ≤ κmax define softer comfort targets in the rulebook. The specific parameters used in the experiment are provided in Table I. 

Simulation-based oracle. In this experiment, the black-box oracle evaluates qi(π) using Monte Carlo simulation of surrounding-vehicle interactions 1. For each candidate policy 

π ∈ Θbox represented by the decision vector θ, the oracle simply recovers the ego states and inputs from the flatness-based parameterization: zπ = zθ , vπ = vθ , ψπ = ψθ ,

aπ = aθ , κπ = κθ , and δπ = δθ . It then checks the feasibility constraints defining Π and evaluates the rules on the grid 

tℓ = ℓ∆t, ℓ = 0 , . . . , K , with K = T / ∆t. At grid time tℓ,the ego footprint center is 

cπ (tℓ) = ( Xπc (tℓ), Y πc (tℓ)) = zπ (tℓ) + ℓc(cos ψπ (tℓ), sin ψπ (tℓ)) ,

where ℓc is the distance from the rear axle to the footprint center of the ego vehicle. The ego quantities used in the rules (26) are aπ

> ego

(tℓ) = aπ (tℓ), κπ

> ego

(tℓ) = κπ (tℓ), and 

χπ (tℓ) = min 

n

1, [Y πc (tℓ)−wlane /2+ wveh /2] +

> wveh

o

.The oracle models each vehicle as a rectangle of length ℓveh and width wveh . For each Monte Carlo sample, a behavior realization ω is drawn for each surrounding vehicle k ∈ {front , rear }, specifying its initial-state perturbations ϵSk , ϵvk, response mode Mk ∈{prompt , delayed , weak , nonresponsive }, response delay 

τk, standstill clearance ∆0,k , desired time headway Tk, re-sponse gains Kg,k , K v,k , maximum acceleration and braking magnitude ak, max , b k, max , and disturbance sequence ξk,ℓ .The initial longitudinal position and speed are sampled as 

Sπk, 0(ω) = ¯Sk, 0 + ϵSk and vπk, 0(ω) = [¯ vk, 0 + ϵvk]+, where 

¯Sk, 0 and speed ¯vk, 0 are the nominal values listed in Table I. The response mode captures driver responsiveness: prompt 

drivers have short reaction delay and nominal gains, delayed 

drivers have longer delay, weak drivers have reduced gains, and nonresponsive drivers have zero gains. Under ω, vehicle 

k evolves longitudinally in the target lane and, at time tl,is centered at (Sπk,ℓ (ω), w lane ) with zero heading, speed 

vπk,ℓ (ω), and acceleration aπk,ℓ (ω). At time tl, vehicle k

perceives the ego state at the delayed time tkℓ = [ tℓ − τk]+.The perceived longitudinal gaps are 

bgπ

> front ,ℓ

(ω) = Sπ

> front ,ℓ

(ω) − Xπc (tfront  

> ℓ

) − ℓveh ,

bgπ

> rear ,ℓ

(ω) = Xπc (trear  

> ℓ

) − Sπ

> rear ,ℓ

(ω) − ℓveh .

Let ∆des ,π k,ℓ (ω) = ∆0,k + Tkvπk,ℓ (ω) denote the desired clearance of vehicle k, where ∆0,k is the standstill clearance 

> 1In practice, this oracle is usually replaced by a neural network surrogate.

and Tkvπk,ℓ is the speed-dependent clearance induced by the desired time headway Tk. The delayed ego speed perceived by vehicle k is uπk,ℓ = vπ (tkℓ ). Define the response terms 

Γπ

> rear ,ℓ

(ω) = Kg, rear (bgπ

> rear ,ℓ

(ω) − ∆des ,π 

> rear ,ℓ

(ω)) + Kv, rear (uπ 

> rear ,ℓ

− vπ

> rear ,ℓ

(ω)) ,

Γπ

> front ,ℓ

(ω) = Kg, front [∆ des ,π 

> front ,ℓ

(ω) − bgπ

> front ,ℓ

(ω)] +

+ Kv, front [uπ 

> front ,ℓ

− vπ

> front ,ℓ

(ω)] +.

The gains Kg,k and Kv,k characterize driver responsiveness to the desired clearance and to the speed of the ego vehicle, respectively. To enforce the acceleration and braking limits, define clip k(x) = min {ak, max , max {− bk, max , x }} . The ac-celeration of vehicle k is given by aπk,ℓ (ω) = clip k

 ξk,ℓ +

χπk,ℓ Γπk,ℓ (ω), where the disturbance ξk,ℓ represents unmod-eled driver variation and the delayed target-lane overlap factor χπk,ℓ = χπ (tkℓ ) activates the response as the ego enters the target lane. The longitudinal states are propagated using the standard kinematic model: for ℓ = 0 , . . . , K − 1,

Sπk,ℓ +1 (ω) = Sπk,ℓ (ω) + vπk,ℓ (ω)∆ t + 12 aπk,ℓ (ω)∆ t2,vπk,ℓ +1 (ω) = max {0, v πk,ℓ (ω) + aπk,ℓ (ω)∆ t},

We now define the quantities used in the rule definitions (26). Let Rπ 

> ego ,ℓ

denote a rectangle of length ℓveh and width wveh , centered at cπ (tℓ) with heading ψπ (tℓ), and let Rπk,ℓ (ω) denote an axis-aligned rectangle, centered at 

(Sπk,ℓ (ω), w lane ). The signed clearances dπk (tℓ, ω ) in r1 is the signed distance between Rπ 

> ego ,ℓ

and Rπk,ℓ (ω), positive when they are disjoint and negative when they overlap. The headways used in r3 are defined as 

hπ

> front

(tℓ, ω ) = gπ

> front

(tℓ, ω )

vπ (tℓ) , hπ

> rear

(tℓ, ω ) = gπ

> rear

(tℓ, ω )

vπ

> rear ,ℓ

(ω) ,

with the convention that the headway is simply set to Tgap 

when the corresponding speed is zero. Here, 

gπ

> front

(tℓ, ω ) = Sπ

> front ,ℓ

(ω) − Xπc (tℓ) − ℓveh ,gπ

> rear

(tℓ, ω ) = Xπc (tℓ) − Sπ

> rear ,ℓ

(ω) − ℓveh .

are the realized longitudinal gaps. A risky ego merge can therefore produce a collision or headway violation when a sampled driver has large delay, weak gains, unfavorable disturbances, or no response. Before optimization begins, we draw a fixed scenario set ΩMC = {ω1, . . . , ω MMC } and reuse it for every policy evaluation. This makes qi a deterministic black-box function. For a fixed policy π, the oracle rolls out each scenario 

ωj ∈ ΩMC , computes rπi (ωj ) for i ∈ { 1, 2, 3} based on (26), and returns the empirical CVaR at tail probability βi:

qi(π) = min 

> η∈R

η + 1

βiMMC 

> MMC

X

> j=1

[rπi (ωj ) − η]+

 .

The comfort rule is deterministic, so q4(π) = rπ 

> 4

is evaluated directly from zπ . Since the computation of qi is external to Algorithm 1, we use the number of qi evaluations as the primary black-box budget. This separates oracle usage from the cost of the optimization procedure. For computation time, we exclude the time spent evaluating qi and report only the time spent executing Algorithm 1. 

Lipschitz constants. Following [29], [30], we employ a sampling-based method to estimate Li for each i ∈{1, . . . , 4}. Note that each rπi (ωj ) is Lipschitz in π over 

Θbox because the flatness-recovery and rule computations are Lipschitz on Θbox , provided the relevant denominators are bounded away from zero. We therefore draw a set of i.i.d. policy pairs {(π(k) 

> a

, π (k) 

> b

)}Npair  

> k=1

. For each ri ∈ R and 

ωj ∈ ΩMC , we compute the maximum pairwise slope bLij =max k      

> rπ(k)
> ai(ωj)−rπ(k)
> bi(ωj)
> ∥π(k)
> a−π(k)
> b∥2

, which provides a lower bound on the corresponding Lipschitz constant Lij . Since empirical CVaR cannot change by more than the largest change in any 

rπi (ωj ), we can conclude that qi is Lipschitz with constant 

Li ≤ max j Lij . In practice, we use the inflated estimate 

Li = α max j bLij , for some α > 1.

Implementation of Algorithm 1. We initialize O = {Θbox }

and C = {π0}, where π0 ∈ Π is chosen as a smooth trajec-tory following standard techniques for B´ ezier curves [31], without considering the surrounding vehicles. The initial excess risk is e(π0) = (0 , 0.28 , 0, 0.6) . The algorithm is assigned an overall computational budget of 50ms across all rule levels, corresponding to a 20Hz planning rate for real-time operation. The cumulative computation time includes all operations of Algorithm 1, excluding the time spent evaluating qi through the black-box oracle. The refinement loop terminates when either the overall time budget is reached or, for rule levels before the final level, Niter = 50 

iterations are completed. Once the time budget is reached, the algorithm completes the remaining operations for the current level and proceeds through subsequent levels, where the refinement loop terminates immediately without fur-ther refinement. These non-refinement operations add only a small computational overhead. We use the center-based construction in (2) to compute the bounds in B OUNDS . The CS AMPLES helper uses the center of each given box as a candidate and returns those certified to lie in Di−1.

Results. Algorithm 1 takes an average of 50 .3ms and issues an average of 370 oracle calls in total, with a variation of 

±5% across runs. The returned policy is identical across runs as the final policy is reached within the first 22ms .The collision-avoidance level r1 starts with α1 = 0 , as initialized by I NIT CERT , and terminates its refinement loop in the first iteration at approximately 0.01ms because TR E- 

> FINE

returns an empty refinable set R = ∅. No refinement is performed, so C = {π0} remains unchanged at the end of the level. The rear-braking level r2 then starts with α2 =0.28m /s2 and therefore proceeds in the CP RUNE branch. As shown in Figure 3, α2 decreases to 0.06 , and 0m /s2

at 0.46 and 0.66ms , respectively (iterations 11 and 15 ). At the first occurrence of α2 = 0 , the corresponding q2 value is 0.07m /s2 below γ2 and the refinement loop switches to the TP RUNE branch. Although some subsequent candidates constructed during refinement have smaller values of q2,TABLE I NUMERICAL VALUES USED IN THE HIGHWAY MERGING SIMULATION .

Group Quantity Value Scenario and vehicle Horizon, grid and lane width T = 5s , ∆t = 0 .1s , wlane = 3 .6m 

Ego boundary conditions (X0, Y 0) = (0 , 0) , (XT , Y T ) = (65 , w lane ), ψ0 = ψT = 0 , v0 = vT = 15m /s

Vehicle dimensions ℓveh = 4 .5m , wveh = 2 .0m , ℓwb = 2 .8m , ℓc = 1 .25m 

Physical limits Speed, acceleration, curvature, steering vmax = 25m /s, amax = 6m /s2, bmax = 8m /s2 κmax = 0 .25m −1, δmax = 0 .5rad 

Rulebook constants Collision, headway, and comfort dcol = 0 , Tgap = 3s , acomf = 1 .5m /s2, bcomf = 3 .0m /s2, κcomf = 0 .15m −1, λcurv = 1 

Rule thresholds γ1 = 0 .015m , γ2 = 2 .75m /s2, γ3 = 2 .0s , γ4 = 0 

Surrounding vehicles Nominal initial positions and speeds ¯Sfront ,0 = 45m , ¯Srear ,0 = −35m , ¯vfront ,0 = ¯ vrear ,0 = 15m /s, which is ∼ 5.3s time gap Initial perturbations ϵSk ∼ N (0 , 12)m , ϵvk ∼ N (0 , 0.52)m /s

Physical limits brear ,max = 6m /s2, bfront ,max = 3m /s2, arear ,max = 2m /s2, afront ,max = 1m /s2

Disturbances sample ˜ξk,ℓ ∼ N (0 , 0.52)m /s2 and clip to [−1.5, 1.0]m /s2

Rear Response Desired clearance ∆0,rear = 5m , Trear ∼ Unif[1 .0, 1.6]s 

Mode probabilities Pr( Mrear = prompt) = 0 .45 , Pr( Mrear = delayed) = 0 .25 ,

Pr( Mk = weak) = 0 .20 , Pr( Mk = nonresponsive) = 0 .10 

Prompt τrear ∼ Unif[0 .2, 0.5]s , Kg, rear ∼ Unif[0 .05 , 0.15]s −2, Kv, rear ∼ Unif[0 .2, 0.4]s −1

Delayed τrear ∼ Unif[0 .8, 1.5]s , Kg, rear ∼ Unif[0 .05 , 0.15]s −2, Kv, rear ∼ Unif[0 .2, 0.4]s −1

Weak τrear ∼ Unif[0 .2, 0.5]s Kg, rear ∼ Unif[0 .0125 , 0.0375]s −2, Kv, rear ∼ Unif[0 .05 , 0.1]s −1

Nonresponsive τrear ∼ Unif[0 .2, 0.5]s , Kg, rear = Kv, rear = 0 

Front Response Desired clearance ∆0,front = 5m , Tfront ∼ Unif[1 .0, 1.6]s 

Mode probabilities Pr( Mfront = prompt) = 0 .30 , Pr( Mfront = delayed) = 0 .20 ,

Pr( Mfront = weak) = 0 .20 , Pr( Mfront = nonresponsive) = 0 .30 

Prompt τfront ∼ Unif[0 .2, 0.5]s , Kg, front ∼ Unif[0 .0125 , 0.0375]s −2, Kv, front ∼ Unif[0 .05 , 0.1]s −1

Delayed τfront ∼ Unif[0 .8, 1.5]s , Kg, front ∼ Unif[0 .0125 , 0.0375]s −2, Kv, front ∼ Unif[0 .05 , 0.1]s −1

Weak τfront ∼ Unif[0 .2, 0.5]s Kg, front ∼ Unif[0 .003125 , 0.009375]s −2,

Kv, front ∼ Unif[0 .0125 , 0.025]s −1

Nonresponsive τfront ∼ Unif[0 .2, 0.5]s , Kg, front = Kv, front = 0 

Monte Carlo oracle Empirical CVaR β1 = 0 .05 , β2 = 0 .1, β3 = 0 .1, MMC = 200 

α2 remains zero due to the threshold structure. Throughout the level, the lower certificate remains α2 = 0 and the incumbent set grows to |C| = 37 . Level r2 terminates at approximately 3.8ms upon reaching the Niter iteration limit. At the beginning of the headway level r3, I NIT CERT finds a policy π ∈ C with q3(π) ≤ γ3, and thus initializes 

α3 = 0 . The refinement loop then executes the TP RUNE 

branch at each iteration and terminates at approximately 

15 .6ms with |C| = 10 upon reaching the Niter iteration limit. Finally, the comfort level r4 is initialized with α4 = 0 .46 .The level then runs for the remaining ∼ 34ms with α4

decreases to 0.45 , 0.44 , and 0.40 at 21 .2, 21 .5, and 21 .9ms ,respectively (iterations 16 , 17 , and 18 ). The final incumbent 

π⋆ = BEST (C, 4) has e(π⋆) = (0 , 0, 0, 0.40) , i.e., rules 

r1, r2 and r3 are all satisfied and r4 is violated by 0.40 .Figs. 3 shows the upper certificates αi and the size |C| of the incumbent set as computation time proceeds. VI. C ONCLUSIONS AND FUTURE WORK 

This paper presented an anytime algorithm for risk-aware optimal control with rulebooks when the risk-evaluation functions are treated as black boxes. The formulation com-pares policies lexicographically using thresholded excess risks, allowing each rule to have its own risk measure and threshold within the rulebook priority order. The algorithm combines Lipschitz branch-and-bound with filtering. The theoretical results show that the certificates returned by the algorithm provide valid bounds for any finite computational budget. Under additional assumptions, the certified optimal-ity gap converges to zero and every limit point of the returned incumbents is optimal. The synthetic benchmark illustrates how exploiting the threshold structure and widest-refinable-box selection improve certificate quality and cover contrac-0 10 20 30 40 50 Algorithm 1 computation time (ms) 0.0 0.1 0.2 0.3 0.4 upper certificate  i       

> level 2
> level 3 level 4
> 1
> 2
> 3
> 4010 20 30 40 50 Algorithm 1 computation time (ms) 0510 15 20 25 30 35 the size | | of incumbent set  level 2
> level 3 level 4

Fig. 3. Upper certificate αi for i = 1 , . . . , 4 (top) and the size |C| 

of the incumbent set (bottom) vs. Algorithm 1’s computation time. αi is recorded at each snapshot while level ri is processed. |C| increases during the refinement of each rule level and decreases sharply at the end of the level due to the filtering C ∩ D i on line 29. 

tion, while the highway-merging simulation demonstrates the method in a realistic application. Several directions remain open for future work. First, the convergence analysis relies on conservative global Lipschitz constants and sampling assumptions. Developing weaker convergence conditions and adaptive local Lipschitz esti-mates could reduce conservatism. Approaches that avoid requiring a known Lipschitz constant altogether provide another promising direction [32]. Second, the current cer-tificates assume deterministic black-box evaluations of qi.When qi is estimated from finite Monte Carlo samples or neural surrogates, statistical uncertainty and surrogate approximation error needs to be incorporated into the box bounds. Finally, learning different elements of rulebooks (e.g., rules, thresholds, priorities) from expert demonstrations or observed safety outcomes will enable the framework to adapt to different environments and operating conditions. REFERENCES [1] D. P. Bertsekas, Reinforcement Learning and Optimal Control . Athena Scientific, 2019. [2] R. S. Sutton and A. G. Barto, Reinforcement Learning: An Introduc-tion . MIT Press, 2nd ed., 2018. [3] A. Majumdar and M. Pavone, “How should a robot assess risk? towards an axiomatic theory of risk in robotics,” in Robotics Research 

(N. M. Amato, G. Hager, S. Thomas, and M. Torres-Torriti, eds.), (Cham), pp. 75–84, Springer International Publishing, 2020. [4] P. Artzner, F. Delbaen, J.-M. Eber, and D. Heath, “Coherent measures of risk,” Mathematical Finance , vol. 9, no. 3, pp. 203–228, 1999. [5] R. T. Rockafellar and S. Uryasev, “Optimization of conditional value-at-risk,” The Journal of Risk , vol. 2, no. 3, pp. 21–41, 2000. [6] G. C. Pflug, “Some remarks on the value-at-risk and the conditional value-at-risk,” in Probabilistic Constrained Optimization: Methodol-ogy and Applications (S. P. Uryasev, ed.), pp. 272–281, Boston, MA: Springer US, 2000. [7] Y. Chow, A. Tamar, S. Mannor, and M. Pavone, “Risk-sensitive and robust decision-making: a CVaR optimization approach,” in Advances in Neural Information Processing Systems (C. Cortes, N. Lawrence, D. Lee, M. Sugiyama, and R. Garnett, eds.), vol. 28, Curran Asso-ciates, Inc., 2015. [8] M. Ahmadi, A. Dixit, J. W. Burdick, and A. D. Ames, “Risk-averse stochastic shortest path planning,” in 2021 60th IEEE Conference on Decision and Control (CDC) , pp. 5199–5204, 2021. [9] Y. Chow, M. Ghavamzadeh, L. Janson, and M. Pavone, “Risk-constrained reinforcement learning with percentile risk criteria,” Jour-nal of Machine Learning Research , vol. 18, no. 167, pp. 1–51, 2018. [10] L. Lindemann, L. Jiang, N. Matni, and G. J. Pappas, “Risk of stochastic systems for temporal logic specifications,” ACM Trans. Embed. Comput. Syst. , vol. 22, Apr. 2023. [11] A. Hakobyan, G. C. Kim, and I. Yang, “Risk-aware motion planning and control using CVaR-constrained optimization,” IEEE Robotics and Automation Letters , vol. 4, no. 4, pp. 3924–3931, 2019. [12] X. Yang, Y. Zhao, Y. Hu, Z. Yang, P. Zhu, Y. Sun, and C. Liu, “Risk-aware and scalable hierarchical motion planning for large-scale robotic swarms via CVaR-constrained MPC,” IEEE Transactions on Automation Science and Engineering , vol. 23, pp. 1683–1700, 2026. [13] T. Nyberg, C. Pek, L. Dal Col, C. Nor´ en, and J. Tumova, “Risk-aware motion planning for autonomous vehicles with safety specifications,” in 2021 IEEE Intelligent Vehicles Symposium (IV) , pp. 1016–1023, 2021. [14] A. Censi, K. Slutsky, T. Wongpiromsarn, D. Yershov, S. Pendleton, J. Fu, and E. Frazzoli, “Liability, ethics, and culture-aware behavior specification using rulebooks,” in 2019 International Conference on Robotics and Automation (ICRA) , pp. 8536–8542, 2019. [15] T. Wongpiromsarn, K. Slutsky, and E. Frazzoli, “Formal specification and control synthesis of autonomous robots using rulebooks,” IEEE Transactions on Robotics , vol. 42, pp. 1330–1350, 2026. [16] T. Wongpiromsarn, “Risk-aware rulebooks for multi-objective trajec-tory evaluation under uncertainty,” IEEE Control Systems Letters ,vol. 10, pp. 397–402, 2026. [17] M. Rigter, P. Duckworth, B. Lacerda, and N. Hawes, “Planning for risk-aversion and expected value in MDPs,” in 32nd International Conference on Automated Planning and Scheduling (ICAPS) , vol. 32, pp. 307–315, 2022. [18] H. Isermann, “Linear lexicographic optimization,” Operations-Research-Spektrum , vol. 4, pp. 223–228, 1982. [19] K. Miettinen, Nonlinear Multiobjective Optimization , vol. 12 of Inter-national Series in Operations Research & Management Science . New York, NY: Springer, 1999. [20] M. Ehrgott, Multicriteria Optimization . Berlin, Heidelberg: Springer, 2 ed., 2005. [21] K. H. Wray, S. Zilberstein, and A.-I. Mouaddib, “Multi-objective MDPs with conditional lexicographic reward preferences,” in Proceed-ings of the Twenty-Ninth AAAI Conference on Artificial Intelligence ,AAAI’15, pp. 3418–3424, AAAI Press, 2015. [22] Y. Rasekhipour, I. Fadakar, and A. Khajepour, “Autonomous driving motion planning with obstacles prioritization using lexicographic optimization,” Control Engineering Practice , vol. 77, pp. 235–246, 2018. [23] Y. Sui, A. Gotovos, J. Burdick, and A. Krause, “Safe exploration for optimization with Gaussian processes,” in Proceedings of the 32nd International Conference on Machine Learning (F. Bach and D. Blei, eds.), vol. 37 of Proceedings of Machine Learning Research , (Lille, France), pp. 997–1005, PMLR, 07–09 Jul 2015. [24] M. Turchetta, F. Berkenkamp, and A. Krause, “Safe exploration for interactive machine learning,” in Advances in Neural Information Processing Systems (H. Wallach, H. Larochelle, A. Beygelzimer, F. d'Alch´ e-Buc, E. Fox, and R. Garnett, eds.), vol. 32, Curran As-sociates, Inc., 2019. [25] S. A. Piyavskii, “An algorithm for finding the absolute extremum of a function,” USSR Computational Mathematics and Mathematical Physics , vol. 12, no. 4, pp. 57–67, 1972. [26] B. O. Shubert, “A sequential method seeking the global maximum of a function,” SIAM Journal on Numerical Analysis , vol. 9, no. 3, pp. 379–388, 1972. [27] R. Horst and H. Tuy, Global Optimization: Deterministic Approaches .Springer, 3 ed., 1996. [28] H. L. Royden and P. Fitzpatrick, Real analysis . Hoboken, NJ: Pearson, 5th ed., 2023. [29] G. R. Wood and B. P. Zhang, “Estimation of the Lipschitz constant of a function,” Journal of Global Optimization , vol. 8, pp. 91–103, 1996. [30] C. Malherbe and N. Vayatis, “Global optimization of Lipschitz functions,” in Proceedings of the 34th International Conference on Machine Learning - Volume 70 , ICML’17, pp. 2314–2323, JMLR.org, 2017. [31] G. Farin, Curves and surfaces for CAGD: a practical guide . San Francisco, CA, USA: Morgan Kaufmann Publishers Inc., 5th ed., 2001. [32] D. R. Jones, C. D. Perttunen, and B. E. Stuckman, “Lipschitzian optimization without the Lipschitz constant,” Journal of Optimization Theory and Applications , vol. 79, pp. 157–181, 1993.