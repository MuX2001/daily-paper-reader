---
title: On the invariance of risk-sensitive LQR gain under input randomization
title_zh: 风险敏感LQR增益在输入随机化下的不变性
authors: Yeongjun Jang
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02363v1"
tags: ["query:control"]
score: 9.0
evidence: 风险敏感LQR最优增益；最优控制理论
tldr: 风险敏感线性二次调节器（LQR）的最优增益在输入随机化下保持不变，尽管看起来违反直觉，因为风险敏感LQR不满足确定性等价，且输入噪声会增大有效过程噪声。然而，输入噪声同时进入系统动力学和代价函数，其总效应最终相互抵消，增益被保留。因此，最优增益及相关Riccati递推无需重新计算，最优成本的增量可闭合形式评估。这有助于在隐私或探索等需要输入随机化的场景中应用风险敏感LQR。
source: arxiv
selection_source: fresh_fetch
motivation: 风险敏感LQR常需结合输入随机化（如水印、差分隐私），但增益是否需要重新计算尚不明确。
method: 从理论上分析输入随机化对风险敏感LQR最优增益的影响，证明其不变量性质并推导代价增量公式。
result: 最优增益和Riccati递推保持不变，代价增量可用闭合形式表达。
conclusion: 该结果简化了风险敏感LQR在隐私、探测和路径积分控制中的应用，无需重新优化增益。
---

## 摘要
本文表明，风险敏感线性二次型调节器（LQR）问题的最优增益在输入随机化下保持不变，即当控制器有意向标称控制输入中注入噪声时。乍一看这似乎是反直觉的，因为风险敏感LQR不满足确定性等价原理，且输入随机化会增大有效过程噪声。然而，增益得以保持的原因在于输入噪声不仅进入系统动力学，还进入成本泛函，其对增益的总影响最终消失。因此，不必重新计算最优增益及其相关的Riccati递推，最优成本的增量可以方便地以闭式形式评估。这一结果促进了风险敏感LQR在采用输入随机化以实现隐私或探索的应用中的使用，例如用于重放攻击检测的水印、差分隐私和路径积分控制。

## Abstract
This paper shows that the optimal gain of the risk-sensitive linear quadratic regulator (LQR) problem is invariant under input randomization, i.e., when the controller deliberately injects noise into the nominal control input. This appears counterintuitive at first glance because certainty equivalence does not hold for risk-sensitive LQR and input randomization inflates the effective process noise. Nonetheless, the gain is preserved because the input noise enters not only the system dynamics but also the cost functional, and its total effect on the gain eventually vanishes. Consequently, the optimal gain and its associated Riccati recursion need not be recomputed, and the increment in the optimal cost can be readily evaluated in closed form. This result facilitates the use of risk-sensitive LQR in applications that employ input randomization for privacy or exploration, such as watermarking for replay attack detection, differential privacy, and path integral control.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机与背景：**
- 风险敏感LQR（又称线性指数二次型调节器，LEQR）是标准LQR的一种推广。它通过最小化指数二次代价函数的期望值，让控制器能够对低概率高代价的轨迹赋予更高权重，从而处理风险敏感问题。当风险参数 θ→0 时，它退化为标准（风险中性）LQR。
- 与标准LQR的重要区别在于：风险敏感LQR的Riccati递推依赖于过程噪声的协方差 Wₜ，因此**不满足确定性等价原理**（certainty equivalence）。这意味着过程噪声统计特性一旦改变，最优增益就需要重新计算。
- 输入随机化（input randomization）是指控制器有意向标称输入中注入额外噪声，广泛应用于重放攻击检测的水印、差分隐私保护、路径积分控制等场景。这种做法会改变有效过程噪声的协方差（从 Wₜ 变为 Wₜ + BₜΣₜBₜ⊤），因此在风险敏感LQR框架下，直观上应该会导致最优增益改变。
- **核心科学问题**：在风险敏感LQR中，最优增益在输入随机化下是否会改变？若改变，会改变多少？

**整体含义：**
- 论文的答案是出人意料的：风险敏感LQR的最优增益在输入随机化下**保持不变**。尽管这不是由确定性等价原理导致的，而是因为输入噪声同时进入系统动力学和代价函数，二者对增益的影响恰好相互抵消。该结果实用意义重大——它意味着在水印、隐私保护、路径积分控制等需要输入随机化的应用中，无需重新计算最优增益和Riccati递推，并可闭式评估度量的成本增量。

---

## 2. 论文提出的方法论

**核心思想：**
- 将控制输入分解为标称输入和输入噪声两部分：uₜ = ūₜ + eₜ，其中 eₜ ~ N(0, Σₜ)，且独立于过程噪声 wₜ。在此设定下对问题进行动态规划推导。
- 通过在代价函数中同时考虑输入噪声的直接影响（u⊤Rₜu 项中包含 eₜ）及其经由系统动力学的间接影响，借助高斯积分引理（Lemma 3）和配平方技术，证明两者对Riccati递推的影响相互抵消，进而增益不变。

**关键技术与数学细节：**
- **风险敏感LQR的标准结果（Lemma 1）**：最优控制律为线性状态反馈 uₜ∗ = K̃ₜxₜ，其增益由一组后向Riccati递推给出；代价函数为 x⊤Pₜx + ηₜ 的二次型形式。递推公式中包含过程噪声协方差 Wₜ，体现确定性等价的失效。
- **主定理（Theorem 1）证明思路**：利用反向归纳法。假设 t+1 时刻的值函数为二次型，通过Bellman最优性原理逐步化简每步期望，并对输入噪声取期望。关键步骤是：在动态模型中，输入噪声 eₜ 经过 Bₜ 进入系统，其协方差贡献为 BₜΣₜBₜ⊤；同时代价函数中 Rₜ 项对 eₜ 的惩罚产生额外二次型结构。配平方后，二者影响相互抵消——原问题递推中新出现的 ˜Hₜ（= Rₜ + Bₜ⊤P̃ₜ₊₁Bₜ）在与噪声相关的项和对输入噪声取期望的步骤中依然保持一致，最终最小化发生在 ūₜ = K̃ₜxₜ。
- **额外可行性条件**：输入随机化引入了新的可行性条件（矩阵 Ξₜ := Σₜ⁻¹ − 2θH̃ₜ ≻ 0）。该条件仅在风险厌恶情形（θ > 0）下有可能失败，与原始可行性条件（Sₜ ≻ 0）共同约束问题适定性。
- **闭式代价增量（Proposition 1）**：由于增益不变，代价变化仅来自常数项 η̄ₜ 的差异，得到 J̄* − J* = −(1/2θ) Σₜ log|ΞₜΣₜ| > 0，即输入随机化使最优成本增加，但增量可由闭式表达轻松算出。
- **联合可行性分析（Lemma 2）**：定义参数集合 Θ = {θ ∈ R\{0} | (3) 良定义且 (4)(9) 成立}，证明其具有非空性、单调性和上有界性——即存在常数 θ̄ > 0 使得 Θ = (−∞, θ̄)\{0}。这保证只要风险参数足够小，原始问题和输入随机化问题可联合可行。
- **等价零和动态博弈（Theorem 2）**：对 θ > 0，输入随机化风险敏感LQR可等价地看成一个控制器与两个对抗者之间的零和博弈。对抗者通过扰动状态和输入来最大化成本，同时受到基于 Wₜ⁻¹ 和 Σₜ⁻¹ 的二次惩罚。在最优控制器策略下，对抗者的最优输入扰动策略 dₜ∗ = 0，直观说明控制器偏离最优增益只会为对抗者创造利用空间。

**验证方法说明**：论文采用纯数学证明（含附录中 Lemma 3 高斯积分引理及 Lemma 1 的严格证明），未进行数值仿真。

---

## 3. 实验设计

- 论文为**纯粹的理论分析论文**，没有数值实验或仿真部分。
- 不涉及任何数据集、benchmark 或对比方法。
- 论文的"验证"方式为严格的数学归纳证明（Theorem 1 通过反向归纳，Lemma 2 通过归纳构造），附录中包含了证明所需的全部技术引理及其证明。
- 需要指出的是：文中没有提供数值示例来直观展示理论结果（如验证增益确实不随 Σₜ 改变），也没有与其它方法进行定量比较。

---

## 4. 资源与算力

- 论文未提及任何计算资源需求。
- 作为纯理论论文，不需要GPU、大规模训练或专门的算力基础设施。
- 文中的"计算"仅指手工推导和数学证明（如Riccat递推的符号推演），复杂度分析不是论文关注点。

---

## 5. 实验数量与充分性

- 没有实验组数问题，因为**论文不依赖实验验证**。
- 就理论的充分性而言：主定理（Theorem 1）的证明对 θ>0 给出完整推导，并注明 θ<0 的情形可类似得到（不过未给出详细证明）；Lemma 2 从三个性质（非空、单调、上有界）出发完整刻画了联合可行参数集合；Theorem 2 的博弈等价性也给出了归纳证明。
- 从客观与公平角度：理论推导逻辑严密，引用现有文献结果（Jacobson 1973；Whittle 1981）作为基础，附录中的引理证明（Lemma 3）完整自洽。
- **不足之处**：数值仿真的缺失意味着定理的实际行为（如对有限时域长度的敏感性、Riccat递推在接近可行性边界时的数值表现等）无法被直观呈现；定理中假设条件满足后的实际性能也没有进行定量验证。

---

## 6. 论文的主要结论与发现

1. **增益不变性**：输入随机化不会改变风险敏感LQR的最优状态反馈增益 K̃ₜ，也不改变Riccati递推（3）。增益不受 Σₜ 影响的根本原因不是确定性等价，而是输入噪声在动力学（通过 Bₜ 项）和代价（通过 Rₜ 项）中的双重作用相互抵消。
2. **代价增量可闭式评估**：输入随机化使最优代价增加，增量为 −(1/2θ)Σₜ log|ΞₜΣₜ| > 0。因为增益不须重算，该增量可以低成本地直接计算。
3. **可行性条件性质**：新的可行性条件 Ξₜ ≻ 0 与原始条件 Sₜ ≻ 0 在风险敏感参数上具有一致性——对足够小的 θ 二者同时成立。参数集合 Θ = (−∞, θ̄)\{0} 给出了简单实用的参数选取指导。
4. **博弈论新视角**：输入随机化风险敏感LQR等价于一个零和动态博弈。在均衡处对抗者的最优输入扰动为零，直观解释了增益不变的现象。
5. **实际意义**：该结果为风险敏感LQR在水印、差分隐私、路径积分控制等依赖输入随机化的应用中提供了理论支撑，消除了重新计算最优增益的障碍。

---

## 7. 优点

- **问题选择精准且有价值**：切入了一个实际应用中被长期回避的重要问题——LQR（尤其是风险敏感LQR）结合输入随机化时增益是否需重新计算，此前没有被严格分析过。
- **发现具有反直觉性和新颖性**：在确定性等价失效的设定下找出增益不变性，从学术性上具有较高的理论价值。
- **推演技巧清晰、逻辑严谨**：通过巧妙的高斯积分（Lemma 3）将问题转化为配平方形式，使噪声的双重作用（动力学与代价）清晰可见。归纳证明结构完整，中间的关键步骤（如由 Lemma 3 得出 P̃ₜ₊₁ ⪰ 0、H̃ₜ ≻ 0）都有依据。
- **结果具有实用价值**：闭式代价增量公式为工程实践提供极大便利，不用重算Riccati递推即可获知加噪成本；参数选取建议（θ 足够小则联合可行）简单易用。
- **提供多角度理解**：博弈论等价表述增强了理论直觉，对理解为什么均衡时输入扰动为零（防御方的最佳策略是保持最优增益）很有帮助。

---

## 8. 不足与局限

- **缺少数值验证**：没有任何仿真或数值示例。论文对"增益实际保持不变""成本以闭式增量增长"等结果没有用具体数值直观展示，对可读性和结果可信度有一定影响。
- **θ < 0 的情形处理不够对称**：Theorem 1 和 Lemma 1 对 θ<0 仅注明"类似可证"（analogously），未给出详细推导；Theorem 2 明确只针对 θ>0，对风险寻求情形缺乏博弈等价分析。
- **有限时域限制**：结果仅限于有限时域（N ∈ ℕ）。作者在总结中也承认向无限时域扩展是未来工作——实际控制中最常见的LQR设置恰恰是无限时域或稳态情形，这限制了结果在长期运行系统中的应用。
- **未考虑随机输入噪声与过程噪声相关的情形**：假设 eₜ 与 wₜ 相互独立，输入噪声白且高斯；如果存在相关性或有色噪声，不变性结论未必成立，需另行分析。
- **计算复杂度讨论有限**：虽然指出通用可重复使用增益可避免重算成本，但作者没有量化风险敏感LQR中Riccati递推的复杂度，也没有与重算成本做定量对比来体现该方法带来的收益。

（完）
