---
title: Relationally Grounded Latent World Models for Autonomous Driving
title_zh: 面向自动驾驶的关系接地潜在世界模型
authors: "Fabian Schmidt, Markus Enzweiler, Abhinav Valada"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24626v1"
tags: ["query:world-model"]
score: 9.0
evidence: 关系语义监督的潜在世界模型表征
tldr: "潜在世界模型虽能预测自动驾驶场景，但所保留的关系语义往往是隐式的。本文以交通场景图作为特权语义监督，从nuScenes 3D标注构建以actor为中心的场景图，用冻结文本编码器编码其序列化关系结构，并在训练时对齐视觉潜表征，推理时移除该分支。在nuScenes上，轨迹平均L2误差降低5.9%至0.622，碰撞率降低52.4%至0.217，且优于无结构caption目标。结果表明，显式关系结构能有效提升潜世界模型表征，且不增加测试时计算与标注需求。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24626v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 825, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24626v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1814, \"height\": 914, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.24626v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24626v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 506, \"label\": \"Table\"}]"
motivation: 潜在世界模型虽能预测驾驶场景，但关系语义常为隐式；本文探究交通场景图能否作为特权语义监督。
method: 基于LAW，从nuScenes 3D标注构建以actor为中心的场景图，用冻结文本编码器编码序列化关系结构，训练时对齐视觉潜表征，推理时移除监督分支。
result: "在nuScenes上，轨迹平均L2误差从0.661降至0.622（5.9%），碰撞率从0.456降至0.217（52.4%），并优于无结构caption语义目标。"
conclusion: 显式关系结构是有效的特权语义监督，可提升潜世界模型表征，且推理时无需场景图、3D标注或额外计算。
---

## 摘要
潜在世界模型为自动驾驶学习预测性表示，但这些状态所保留的关系语义往往仍是隐式的。我们研究交通场景图能否作为潜在世界表示的特权语义监督。在 LAW 的基础上，我们从 nuScenes 3D 标注中构建以行为主体为中心的场景图，使用冻结的文本嵌入模型对其序列化关系结构进行编码，并在训练期间将视觉潜在表示与该语义目标对齐。我们在推理时移除监督分支，因此既不需要场景图也不需要 3D 标注，且不增加测试时计算量。在 nuScenes 上，相对于我们重新训练的 LAW 基线，我们的方法将平均轨迹 L2 误差从 0.661 降至 0.622（5.9%），并将碰撞率从 0.456 降至 0.217（52.4%）。它还优于非结构化的描述文本式语义目标，支持了显式关系结构对潜在世界模型表示学习有益的观点。

## Abstract
Latent world models learn predictive representations for autonomous driving, but the relational semantics these states preserve often remain implicit. We investigate whether traffic scene graphs can serve as privileged semantic supervision for latent world representations. Building on LAW, we construct actor-centric scene graphs from nuScenes 3D annotations, encode their serialized relational structure using a frozen text embedding model, and align the visual latent representations with this semantic target during training. We remove the supervision branch at inference, so it requires neither scene graphs nor 3D annotations and adds no test-time computation. On nuScenes, our method reduces average trajectory L2 error from 0.661 to 0.622 (5.9%) and collision rate from 0.456 to 0.217 (52.4%) relative to our retrained LAW baseline. It also outperforms an unstructured caption-style semantic target, supporting the benefit of explicit relational structure for latent world-model representation learning.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：潜在世界模型（latent world models）能够学习自动驾驶场景的预测性表示，但其所保留的**关系语义**（如车辆之间、车辆与自车之间的相对位置、邻近性、交互关系）通常是隐式的。
- **背景**：现有方法多通过未来潜在状态预测、几何先验或规划目标进行监督，但这些目标并未显式约束交通参与者之间的关系。对驾驶而言，行为主体的重要性不仅取决于外观，也取决于其与自车的相对位置和交互。
- **核心问题**：交通场景图能否作为**训练时的特权语义监督**，用于提升潜在世界模型表示学习，同时在推理阶段不依赖场景图、3D 标注或额外计算？
- **整体含义**：论文提出一种“关系接地”的训练策略，在不改变推理路径的前提下，将结构化关系语义注入视觉潜在表示，从而改善轨迹预测与碰撞性能。

## 2. 方法论

- **总体思想**：基于 LAW 潜在世界模型，在训练阶段额外引入交通场景图语义对齐目标；推理阶段完全移除场景图、文本编码器和对齐头。
- **场景图构建**：
  - 从 nuScenes 的 3D 标注中构建以行为主体为中心的场景图 \(G_t=(N_t,E_t)\)。
  - 节点为自车及周围交通参与者（车辆、骑行者、行人等），边编码相对方向、邻近性、可见性等关系。
  - 采用 GraphPilot 的 Actor-Only 表示，并序列化为“主语–关系–宾语”结构化文本 \(s_t=\text{Serialize}(G_t)\)。
- **语义目标编码**：
  - 使用冻结的文本嵌入模型（Qwen3-Embedding-8B）编码序列化场景图，得到语义目标 \(z^{SG}_t=E_{\text{text}}(s_t)\)。
- **视觉潜在对齐**：
  - 对 LAW 的视觉潜在表示 \(V_t\) 进行均值池化，再经 MLP 投影到同一语义空间：\(z^{vis}_t=P(V_t)=\text{MLP}(\text{MeanPool}(V_t))\)。
  - 使用余弦距离对齐视觉表示与场景图语义：  
    \[
    L_{SG}=d_{cos}(z^{vis}_t,z^{SG}_t)
    \]
  - 梯度传播至视觉编码器和对齐头，文本编码器保持冻结。
- **可选时序扩展**：
  - 对未来场景图 \(G_{t+1}\) 同样序列化并编码，得到 \(z^{SG}_{t+1}\)。
  - 对世界模型预测的未来潜在状态 \(\hat{V}_{t+1}\) 做相同投影，得到 \(\hat{z}^{vis}_{t+1}\)。
  - 定义时序对齐损失：  
    \[
    L_{temp}=d_{cos}(\hat{z}^{vis}_{t+1},z^{SG}_{t+1})
    \]
- **训练目标**：
  - 总损失为原始 LAW 损失加当前状态对齐和可选时序对齐：  
    \[
    L=L_{LAW}+\lambda_{SG}L_{SG}+\lambda_{temp}L_{temp}
    \]
  - \(\lambda_{SG}\) 和 \(\lambda_{temp}\) 控制语义监督强度。
- **推理**：所有场景图、文本嵌入和对齐组件仅用于训练，推理时丢弃，LAW 原始推理流程不变。

## 3. 实验设计

- **数据集 / 场景**：
  - 使用 **nuScenes** 规划基准。
  - 采用 perception-free 的 LAW 架构，输入为多视角图像。
  - 训练时额外使用 nuScenes 3D 标注构建场景图监督目标；推理时不需要这些标注。
- **Benchmark 与指标**：
  - 评价预测自车轨迹的 **L2 位移误差** 和 **碰撞率**。
  - 预测时域为 1 秒、2 秒、3 秒，并报告平均值；两者均为越低越好。
- **对比方法**：
  - **重训练的 LAW 基线**：在相同设置下从头训练，作为受控对比对象。
  - **非结构化文本语义目标**：使用 caption 风格描述，而非显式“主语–关系–宾语”图结构，用于区分结构化关系监督与一般文本语义对齐。
  - **结构化场景图监督**：论文提出方法。
- **主要实验设置**：
  - 默认使用结构化场景图序列化、冻结 Qwen3-Embedding-8B、\(\lambda_{SG}=0.1\)、\(\lambda_{temp}=0\)。
  - 时序对齐仅在相应消融实验中启用。

## 4. 资源与算力

- 文中明确提到：所有实验使用 **四块 NVIDIA H200 GPU**，并按照 LAW 官方超参数和训练计划从头训练。
- 使用相同 LAW 架构和训练设置，仅增加语义监督目标。
- **未明确说明**：
  - 具体训练时长、总 GPU 小时数、能耗或成本。
  - 是否进行多次随机种子重复实验。
- 因此，算力信息只可确认 GPU 型号与数量，训练时长等细节缺失。

## 5. 实验数量与充分性

- **实验组数大致包括**：
  - 表 I：3 种方法对比（LAW 基线、非结构化文本、结构化场景图），覆盖 1s/2s/3s 和平均值。
  - 表 II：约 12 组消融，包括当前状态对齐权重 \(\lambda_{SG}\) 的 6 个设置，以及时序对齐 \(\lambda_{temp}\) 与联合权重的多组设置。
- **充分性**：
  - 消融覆盖了当前状态对齐强度、未来状态对齐强度以及联合对齐，能够说明主要趋势。
  - 对比了重训练 LAW 基线和 caption 风格语义目标，具备一定公平性。
- **局限性**：
  - 仅使用 **nuScenes 单一数据集**，未验证跨数据集泛化。
  - 仅基于 **LAW 单一潜在世界模型架构**，未迁移到其他潜在驾驶模型。
  - 未报告多次运行的均值、方差或统计显著性。
  - 未与 GraphWorld、GraphPilot 等结构化关系方法进行端到端直接实验对比，仅作为背景引用。
  - 因此实验设计受控且方向清晰，但覆盖面和稳健性验证仍有限。

## 6. 主要结论与发现

- **结构化场景图监督有效**：
  - 相对于重训练 LAW 基线，平均 L2 误差从 0.661 降至 0.622，相对降低 **5.9%**。
  - 平均碰撞率从 0.456 降至 0.217，相对降低 **52.4%**。
- **优于非结构化文本目标**：
  - 非结构化 caption 目标将平均碰撞率降至 0.312，但平均 L2 略微升至 0.670。
  - 结构化场景图相比非结构化文本进一步降低 L2 **7.2%**、碰撞率 **30.4%**。
- **关系结构带来归纳偏置**：
  - 场景图显式强调相对方向和邻近关系，更贴近交互感知驾驶需求。
  - 碰撞率大幅下降说明该监督有助于表示自车与周围行为主体的空间关系。
- **对齐强度非单调**：
  - \(\lambda_{SG}=0.1\) 取得最佳综合权衡。
  - 过强当前状态对齐不一定持续增益，说明语义对齐更适合作为辅助约束。
- **时序对齐的权衡**：
  - 仅时序对齐在中等权重下可改善轨迹误差，但不改善碰撞。
  - 联合当前–未来监督可进一步降低 L2，例如 \(\lambda_{SG}=\lambda_{temp}=0.5\) 时平均 L2 降至 0.594，降低约 10.1%，但碰撞率 0.387，仍高于仅当前状态对齐的 0.217。
  - 当前状态关系接地对碰撞降低最有效；时序接地更利于轨迹精度，但需谨慎加权。

## 7. 优点

- **训练时特权监督，推理零开销**：
  - 推理阶段不需要场景图、3D 标注、文本编码器或对齐头，不增加测试时计算。
- **方法通用性较强**：
  - 该目标不局限于 LAW，原则上可迁移到其他暴露可训练场景表示的潜在驾驶世界模型。
- **结构化关系 vs 非结构化文本的对照清晰**：
  - 通过 caption 风格基线证明增益并非仅来自“增加语义监督”，而来自显式关系结构。
- **碰撞率改善显著**：
  - 平均碰撞率降低 52.4%，对安全相关应用具有吸引力。
- **消融分析有启发性**：
  - 揭示当前状态对齐和未来状态对齐在轨迹精度与碰撞性能之间的不同权衡。

## 8. 不足与局限

- **依赖训练时 3D 标注**：
  - 虽然推理不需要，但训练仍需 nuScenes 3D 标注，限制在无标注数据上的扩展。
- **场景图序列化敏感**：
  - 语义目标通过文本序列化后由冻结文本模型编码，可能受序列化方式、模板和语言表达影响。
- **仅单一数据集与单一架构**：
  - 未在 nuScenes 之外验证，也未迁移到 LAW 之外的潜在世界模型。
- **缺乏统计稳健性报告**：
  - 未说明随机种子、方差或显著性检验，性能提升的稳定性未知。
- **未与图原生编码器或直接图方法对比**：
  - 未来工作才计划探索 relational GNN、graph transformer 等图原生编码器。
- **时序对齐存在权衡**：
  - 未来状态监督可提升轨迹精度，但可能损害碰撞性能，实际部署需仔细调参。
- **长尾与分布偏移未知**：
  - 未分析复杂交互、罕见场景或跨城市/跨天气条件下的泛化能力。
- **算力与训练成本细节缺失**：
  - 仅说明四块 H200，未报告训练时长、能耗或多次实验成本。

（完）
